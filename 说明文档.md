# 说明文档

**项目名**：找鸭找 APP v2（基于 v1 原型的产品重构）
**创建日期**：2026-08-10
**文档用途**：项目全生命周期唯一管理载体。按开发规则，每次重启项目先读本文档确认进度；每次变更/完成必须即时更新本文件。

---

## 一、项目规划

### 1.1 项目目标

在 v1 原型（`prototype/index.html`，16555 行单文件 HTML 原型）基础上，进行一次**聚焦式重构**，输出新版「找鸭找 APP v2」的完整产品定义：

- 从"撮合交易平台"转向"**本地供需信息共享平台**"（**严格 Scope：不做支付、不做交易担保、不做信誉评价系统、不追踪撮合结果**）；
- 从"全城交易"转向"5 公里生活圈"；
- 从"单向列表"转向"双向撮合"（资源/需求双向发布）；
- 从一刀切砍 AI，转向 **D5.1 AI 三色清单拆分**（🟢绿=LR+GBDT匹配/3B小模型严格Scope辅助生成/双塔召回；🟡黄=端侧离线/多智能体代撮合；🔴红=11个重算法群/70B端到端永远不做）——明确「集成 AI 能力」是三大目标#1，**但 AI 只在「纯信息匹配/纯信息生成辅助」范围内（不涉及撮合闭环/交易担责）；
- 从"实名+资质二元+完全不分级"转向「**二层认证标识 + 信息完整度三档角标🟢🟡🔴 + 基础实名权限两档**」（严格 Scope：分级只针对「发布出来的供需信息本身填得全不全」，绝不做人的信誉评级/历史撮合追踪/商业化角标售卖）。

**2026-08-19 用户二次明确的最终 Scope（三轮 ideate 校正后，本次是官方最终定位，不再待评审）：**

| # | 用户三大核心目标 | 校正后最终官方定义（严格 Scope） |
|---|---|---|
| ① | 集成 AI 能力：智能匹配需要/资源 + 智能发布 | ✅ LR+GBDT 双轨匹配（纯信息，特征只取联系点击/位置/时效/类目，**不取撮合反馈/历史信誉**） + 3B 小模型严格 Scope 管线 AI 辅助生成发布 + 标签金字塔；❌ 不做撮合追踪/交易担责/AI 中间人代撮合平台背书 |
| ② | 快捷搜索与更高用户体验，利用地图和图层更直接展示 + 视觉冲击 | ✅ **图层核心=5大类三级树分类快速加载引擎**（预渲染+按需取数+本地三级缓存，图层切换 P95 ≤ 300ms 硬指标），图层弹层直接是分类三级勾选树，视觉仅保留「供需实心/空心 + 🟢🟡🔴完整度」两项基础信号；❌ 图层不做认证/时效/质量筛选器、不做金字塔五层视觉炫技、不做撮合热力/行动导航器 |
| ③ | 需求和资源的分级管理 | ✅ **分级对象=供需信息本身，不是发布者人格**：信息完整度三档🟢🟡🔴（必填项/位置精度/类目匹配三条件）+ 基础实名权限两档（未实名每日最多看 3 条详情/实名无限次）；❌ 不做 QS 五档 100 分信誉、不做钻石/金牌/银牌等级标签、不做五级权限阶梯倍增、不售卖任何角标/徽章/曝光特权 |

**项目北极星（纯信息三乘积，无撮合/信誉，校正后正式冻结）：**  
**3 步找到率 = AI 匹配召回率 × 图层加载成功率（P95≤300ms） × 🟢 完整发布率**  
（**合格线 ≥ 0.216 = 召回 60% × 加载 60% × 完整 60%，等权硬线**；任何一轴为 0 = 整产品价值为 0）  
> 2026-08-22 校正：原写「≥0.5 = 80%×90%×70%」为更早期乐观值，与 BRD/MRD/PRD v2.1 及已交付 PPT 的 D1 决策（方案 A 等权硬线 0.6×0.6×0.6=0.216）不一致，现统一以 0.216 为唯一口径。

**核心口号（校正后正式版）：**  
附近谁有谁要 → 打开 APP 勾分类 → 3 步 300ms 看到、30 秒联系上（纯信息共享，不追成交、不评信誉、不收钱）

### 1.2 产出物清单

| 交付物 | 路径 | 状态 | 说明 |
|---|---|---|---|
| BRD 商业需求文档 | `docs/BRD.md` | ✅ v2.1 已完成（B1-B9 + D1/D2/D3 全部落地，等待评审） | 战略/商业/竞品/风险/决策记录；D5.1 三色清单 + 三乘积北极星 + 三批MVP节奏 + D1/D2/D3 决策兜底全部已写入 |
| MRD 市场需求文档 | `docs/MRD.md` | ✅ v2.1 已完成（M1-M7 + D1/D2/D3 全部落地，等待评审） | 用户画像/场景/需求优先级/GTM/指标；三目标使用倾向维度 + 12 条 P0 需求 + 三批 MVP GTM + 三组关键指标全部已写入 |
| PRD 产品需求文档 | `docs/PRD.md` | ✅ v2.1 已完成（P1-P15 + D1/D2/D3 全部落地，等待评审）；**2026-08-24 追加第十部分「工程契约」§12 接口 / §13 数据 / §14 非功能** | 视觉系统/分类体系/11 章正文 + §10.1 共 14 个页面 + 3 个模态 + §11 共 12 个验收模块；AI 匹配 LR+GBDT + AI 发布四模式 + 分类三级树图层 + 完整度三档分级 + 双向推送 + 三轴飞轮全部已写入。**§12–§14 覆盖 SRS（软件需求规格）的接口契约、数据字典、非功能需求三章**，全部条款由 §1–§9 推导并注明来源，冲突时以 §1–§9 为准；故本项目不另建 `docs/SRS.md`，PRD 为唯一权威源 |
| 系统总体架构设计文档 | `docs/architecture/系统总体架构设计文档.md` | ✅ v1.0 已完成（2026-09-03，条目 [80]） | 服务端架构唯一权威源：架构总览图 / 9 功能域切分（与 `lib/features/` 同构）/ 部署形态与内存预算 / 13 表数据架构 + 两个冗余列理由 / 接口契约落地（三共享组件 + 两 ID 分工）/ 四条关键链路 / 定时任务 / 38 接口批次矩阵 / 性能与降级次序。**技术选择的「为什么」不在本文档，在《技术栈选型说明》** |
| 技术栈选型说明 | `docs/architecture/技术栈选型说明.md` | ✅ v1.0 已完成（2026-09-03，条目 [80]） | 12 项选型逐项「结论 / 被否方案与理由 / 退出条件」三段式。开篇先摆压缩全部选择空间的三条硬约束（1 人 + 38 自然日无缓冲 + 131 元/年）；含月度成本列与四条需定期复核的外部假设 |
| 可观测性架构方案 | `docs/architecture/可观测性架构方案.md` | ✅ v1.0 已完成（2026-09-03，条目 [80]） | 三类数据三套保留规则（埋点 90 天 / 审计 180 天 / 中转脱敏 30 天，**不共用配置**）/ 四个必埋事件 + 三条易错口径 / 北极星三轴看板与轴① 反向哨兵 / 三阶量级测算 / 第一天就做的三条防线 / 三条迁移阈值。**埋点四事件是北极星验收的 🔴 阻塞项（`docs/PRD.md:2496`），非内测后补项** |
| 服务端架构统一计划（需求态） | `docs/plans/2026-09-03-1051-feat-server-architecture-baseline-plan.md` | ✅ 已完成（`requirements-only`，2026-09-03） | ce-brainstorm 产物；10 项 Key Decisions + 18 条 R-ID（选型 R1-R8 / 架构组织 R9-R12 / 可观测性 R13-R16 / 交付批次 R17-R18）。上述三份架构文档的每处技术选择都能对回其中一条 |
| 说明文档（本文件） | `说明文档.md` | ✅ 已完成 | 项目规划/实施方案/进度记录 |
| 需求深度还原 ideation 工件 | `ideation/找鸭找v2-需求深度还原-ideation-20260819.html` | ✅ 三轮增量完成（含路径 C 争议点决策 D1/D2/D3） | 三轮 go deep ce-ideate：Raw Ideas 180 条 → Survivors 43 条（S1-S7 需求还原7条 + T1-T6 三目标落地6条 + B1-B9 BRD修订9条 + M1-M7 MRD修订7条 + P1-P15 PRD修订15条 + D1-D3 争议点决策3条）+ Rejection 12 类（每类附复活阈值）+ 路径 C 三个争议点讨论决策回写 |
| 原型 HTML | `prototype/index.html` | ✅ **历史存档，不再改造**（原「T6 增量改造计划」已取消） | 16555 行 v1 原型，作为需求还原的对照基线保留。原计划的 T6 四组件（AI帮我发FAB/图层控制面板/完整度角标/完整度卡）**已由 Figma 原型生成器承接**（见下一行，批次 4 生成），无需在 v1 HTML 内重复实现；且 9/30 内测包工期不容许改造 16555 行单文件 |
| Figma 原型生成器 | `prototype-figma/`（`manifest.json` + `ui.html` + `code.js`） | ✅ 已在 Figma 桌面版跑通多轮，最近一轮为条目 [70] 的 trust 页复验（实测 549px 内容底边）；⚠️ 条目 [70] 后半段（四页留白补齐 + 个人中心补退出登录）**待再跑一轮** | 5 批次生成 §10.1 全部 14 页（+ 启动页 = 38 个 screen 画框，实机 `01 · 原型主页面` 计得）+ 3 模态 + T6 四组件 + 20 个 Component master + **43 个 Variables**（COLOR 26 + FLOAT 17，由 `planStats()` 现算）+ **23 条**原型跳转连线；技术路径为 **Plugin API**（REST API 为 GET-only、Trae 不在 Remote MCP 客户端白名单、`.fig` 二进制格式未公开且已知 4 个 bug、SVG/HTML 导入丢 Auto Layout —— 四条替代路径均已排除） |
| 鸭子 IP 素材库 | `prototype-figma/assets/`（12 个 SVG） | ✅ 已交付（条目 [45]） | 按 PRD §1.4.1.1–§1.4.1.4 方向 4B 规格**用代码生成**：符号本体 5 版（full/compact/mini/inverse/mono）+ Logo 横排/竖排 + 启动图 + 空态图 + App 图标 3 件（iOS 无透明通道 / Android 分层）。XML 合规 12/12 实测通过；`code.js` 的 `duckSymbol()` 工厂已在启动页 160px、登录页 96px、Tab 中键 24px 三处消费。⚠️ 几何数据在 `code.js` 与本目录**双份副本**，改动须同步 |
| 上架前置工作指导手册 | `docs/上架前置工作指导手册.md` | ✅ v1.0 已完成（待用户逐项执行） | P1–P10 十项行政前置事项的**操作级指导**：关键路径全景图 + 每项含办理入口 URL/前置依赖/逐步操作/材料格式硬指标/驳回雷点/状态位；含软著三重一致性自检表、名称与标识冻结区、四条本产品特有合规风险、完整时间表。与 §2.6 概览表同编号互为索引 |
| 主色候选可视化对比页 | `prototype-figma/color-preview.html` | ✅ 已完成（主色已据此定稿 A 深湖青） | 4 候选 × 6 真实场景（导航栏/地图当前位置点/分类 Tab 选中态/卡片选中边框/主次按钮/发布胶囊）；地图 5 分类 Pin 固定作对照；WCAG 对比度由页内 JS 实时计算。后续换色可复用此页决策 |
| 系统安全设计方案 | `docs/architecture/系统安全设计方案.md` | ✅ v1.2 已完成（2026-09-06，含 G1–G6 全部裁定） | 按 7 个安全域归集 PRD §14.3 / §13.2 / §13.4、架构 §2.1.0 / §4.3 / §4.4 / §9.2.2、可观测性 §5 / §10.1、详设 §3.4 / §4 的已定安全条款，**不新造口径**。含威胁模型 T1–T4、**45 项自检**（S2–S10）、PRD §14.6 五项 🔴 安全验收的三列化（执行工具 / 失败判定 / Batch1 适用性）、PIPL 与 P1–P10 合规映射、6 项缺口登记（**G1–G6 已于 2026-09-06 全部经裁定关闭，见 §11.1–§11.6，无剩余待处理项**）。**结论：45 项中 23 项只能人工实测，DevSecOps 不可替代人工验收** |
| DevSecOps 接入方案 | `docs/architecture/DevSecOps接入方案.md` | ✅ v1.2 已完成（2026-09-06）；**本期为纯规格，不落自动化实物**（用户 2026-09-06 确认） | 为架构 §9.2.2 出包门禁三层、PRD §13.4 保留键 CI 门禁、可观测性 §10.1 红线 grep 提供**可执行判据规格**。含现状零基础盘点、两处未闭合阻塞项（B-1 `888888` 后门可随包发布 / B-2 release 仍 debug 签名阻塞 P7）、出包门禁 11 步序列 + `release-gate.log` 须含 APK SHA-256、6 项「静默降级」必须落测试的安全断言、三个待落地交付物规格、§9 十一项落地清单（编码期必做）、明确的工具主动放弃清单。**D-1 构建工具已定案为 Maven（G6 裁定，口径源《技术栈选型说明》§3.1），本文档六处同步、§9 待办第 1 项关闭；D-2 测试执行时机已于同日就地定案（单测每次提交前，集成测试仅出包前），口径源为本方案 §5.3 —— 它是门禁分层问题、不属技术栈选型，故不适用「本方案不自行定案」纪律；§2 定案项表自此无待定案项。** §10 承载映射已随安全方案同步为 45 项（18 自动 / 4 半自动 / 23 人工） |
| 编码规范 | `docs/architecture/编码规范.md` | ✅ v1.1 已完成（2026-09-09，条目 [117]/[118]），代码开发阶段前置交付 | 汇编详设 §0–§21（含 §9/§19 两份自检清单）、技术栈、安全、可观测、DevSecOps、部署 G1–G11、数据库/DDL、OpenAPI、PRD §12.5 的全部编码期纪律，每条标注依据源章节。十一章：总则/**组件抽提与代码复用（唯一实现处清单 + 反冗余验收口径）**/命名注释/常量错误码配置/后端/前端/数据 SQL/测试门禁/提交出包/待关闭缺口/通用基线附录。**只汇编不定案**；设计文档无明文的 SQL 注入与日志级别两条显式标为通用基线，不代设计立规 |

### 1.3 关键里程碑

> **交付基线（2026-08-23 确认）**：目标交付日 **2026-09-30**，交付形态 = **可用内测包**（蒲公英/TestFlight 分发，真实后端 + 真实数据）；**应用商店公开上架顺延至软著下证后（约 11 月下旬）**。
> 上架主体：**沿用已有营业执照**（须核对经营范围含「软件开发 / 信息技术服务 / 互联网信息服务」）。
> 判定依据见 §1.4「9/30 不可上架的行政约束」与 §2.6「上架前置清单」。

| 里程碑 | 计划日期 | 实际日期 | 完成标志 |
|---|---|---|---|
| M1：grilling 拷问收敛 | 2026-08-10 | 2026-08-10 | 四轮拷问完毕+收敛报告被用户接受 |
| M2：三件套文档交付 | 2026-08-10 | 2026-08-10 | `docs/BRD.md` + `docs/MRD.md` + `docs/PRD.md` 齐备 |
| **M2.5：上架前置流程启动** | **2026-08-24** | — | ICP 备案已提交 + 域名/服务器已购 + App 名称锁定 + 软著材料开始准备（详见 §2.6） |
| M3：设计稿与评审 | 2026-08-28 | **2026-08-31** | **18 个主态 + 24 个变体 = 42 框**视觉稿冻结、Design Token 落地（主色已于 08-23 定稿 `#0B7C8C`）。**08-26 用户明确「保 8/28 原目标」且视觉稿自己做（无外部设计师接棒）**，故 Token 层先行、像素精修在后（条目 [49]）；下方「🟠 第 1-2 周」里原列的 UI/UX 设计稿一项已提前至本里程碑内完成，不再排到 8/31 之后。**注：原写「14 页」为 [1289] 补缺页时的旧数字，已于 08-27 按实测改正（条目 [61]）；原写「23 个变体 = 41 框」于 08-31 按 PRD §6.4.4 权限三分表补出定位权限 B 态「曾授权后被关」画框后改为 24 / 42（条目 [70-h]）**；像素精修经用户批准限定为「只做全局排版规律」，逐页美感精修仍不做。**08-31 用户正式验收通过**，验收材料：42 框全部出过实机渲染图（r67–r74）+ 离线布局探针 256 项 + Token 探针 125 项全绿（收口过程见条目 [70] 至 [70-h]）。**延期 3 天的成因**：8/28 后折返做了五类留白缺陷横扫（[70]）、四模态首出图挖出五处缺陷（[70-g]）、24 变体首出图挖出四处缺陷（[70-h]）—— 属「冻结前把缺陷挖净」的必要投入，不是范围扩张 |
| M3.1：Figma 原型骨架生成 | 2026-08-24 | **2026-08-25** | 用户在 Figma 桌面版导入 `prototype-figma/manifest.json`，按批次 1→5 执行完毕，14 页 + 3 模态画框齐备、**23 条**跳转 Present 模式可走通（原写 21 条为 [44] 时的旧数字，已按 `FLOW_LINKS` 真源现算改正）。**08-25 首次跑通三轮并逐条 MCP 实测验收（条目 [44]）**；此后随主态/变体增补多次重跑，末次为 08-31 批次 1→5 全量重跑（画布实测 42 框齐、`variantCount:24`） |
| **M2.6：软著提交** | **2026-08-25** | — | 60 页源码 + 说明书已提交中国版权保护中心；**按保守 60 工作日口径 → 预计下证 2026-11-17**（不阻塞内测包）。办理指导见 [上架前置工作指导手册 P5](./docs/上架前置工作指导手册.md) |
| M4：核心闭环可跑通 | 2026-09-11 | — | 账号→发布→首页地图→详情→联系中转 五大 P0 真机走通（含聚合性能 POC） |
| **M4.1：ICP + APP 备案完成** | **2026-09-11**（乐观）/ 09-21（上限） | — | ICP 备案号下发 + 工信部 APP 备案核准号下发（APP 备案需 APK 签名信息，依赖可出包） |
| M5：后端 API + 认证/治理 | 2026-09-25 | — | 核心 API 联调完成、实名认证接入、隐私政策上线 |
| **M6：内测包交付（本期交付节点）** | **2026-09-30** | — | 蒲公英/TestFlight 可安装、五大 P0 闭环可用、种子用户可试用 |
| M7：应用商店公开上架 | 2026-11-25（待软著下证后确认） | — | 软著下证 + 各渠道开发者账号就绪 + 商店审核通过 |
| M8：单城试点 | TBD（上架后） | — | 首次联系触发率验证+密度监控 |

### 1.4 9/30 不可上架的行政约束（2026-08-23 核实结论）

**结论：9/30 应用商店公开上架在行政流程上不可达，与开发能力无关。** 依据如下，均经官方来源核实：

| 前置项 | 法定/实测周期 | 最早完成日 | 来源 |
|---|---|---|---|
| **软著（软件著作权）** | **保守取 60 工作日**（法条与版权中心官网原文为「自受理日起 60 日」，国家政务服务平台另列「法定办结 60 工作日」；叠加受理前后段后实操普遍 2-3 个月，故取工作日口径为规划值）；加急 400 元仅「优先处理」，官方明示**审核周期不变** | **2026-11-17** ❌ | 《计算机软件著作权登记办法》第二十条 + 中国版权保护中心《软件著作权登记办理步骤》/《办理时限》 |
| ICP 域名备案 | 官方上限 20 工作日（实测常 7 天） | 09-02（乐观）/ 09-21（上限） | 工信部 |
| 工信部 APP 备案 | ICP 通过后约 7 工作日；**需 APK 签名信息（MD5/公钥/SHA-1）**，故依赖可出包 | 09-11（乐观） | 工信部 2023 年 105 号文 |

**三条硬约束**：
1. **软著是死结**：华为/OPPO/vivo **强制要求软著**，无软著直接驳回、无例外；小米要求「软著或 ICP 备案」。软著最早 11/17 下证（保守口径），晚于 9/30。
2. **备案链是串行的**：执照 → ICP 备案 → APP 备案 → 才能提审，任一环节收到补正通知即开启 30 天窗口，全线顺延。
3. **本产品为 UGC/社区类，个人主体不可上架**：安卓渠道对个人开发者「社交、社区、付费类完全不允许」；iOS 亦要求 UGC 类必须企业账号。**故必须使用企业/个体户执照**（用户已有现成执照，此项已解）。

**iOS 单渠道的例外与代价**：iOS **不强制软著**，理论上 9/30 前有可能上 App Store；但仍需 ICP + APP 备案链走完 + 苹果审核时间，排期无任何缓冲，且安卓仍须等软著。故**未采纳**，改为 9/30 交内测包。


---

## 二、实施方案

### 2.1 产品重构砍/留/改总原则

详见 [PRD.md 附录 B 砍留改对照表](./docs/PRD.md#附录-b砍留改对照表v1--v2)。核心原则：

- **砍**：结算/信誉/评价/站内 IM/AI 智能解析/11 个重型匹配算法群；
- **留**：底部三 Tab 架构、地图优先首页、发布/收藏/通知/设置/我的发布基础能力、品牌名"找鸭找"+鸭子 IP；
- **改**：视觉系统全量重设计、分类体系改为 5 大类三级树、登录注册合并、发布改为模板+记忆、详情页改为信任卡+联系中转页、通知中心分类；
- **新增**：联系中转页、实名与资质认证体系、分类三级树级联选择器、轻量综合匹配、合规治理。

### 2.2 v2 信息架构（页面清单）

详见 [PRD.md §10 信息架构](./docs/PRD.md#10-信息架构ia)。共 15 个页面/模态，其中 5 个砍除、5 个新增、其余改造。

### 2.3 分类体系（首期）

5 大类：工作 / 房屋 / 车辆 / 生活 / 服务。
三级树详情见 [PRD.md §2.4 分类三级树](./docs/PRD.md#24-分类三级树首期建议)。

### 2.4 执行顺序建议（工程化阶段）

1. 视觉设计系统冻结（Design Token + 鸭子 IP 新造型 + 组件库）；
2. 前端工程脚手架 + 路由/页面骨架；
3. 5 大 P0 模块闭环（账号→发布→首页地图→详情→联系中转）；
4. 我的模块（我的发布/收藏/通知）+ 认证；
5. 合规治理 + 运营后台；
6. 单城试点。

### 2.5 技术建议（Scope 校正后正式版，供研发参考）

- **移动形态**：~~落地形态可按 BRD §1 后续讨论（Taro / RN / Flutter / H5 Prototype 均可）~~ **已由 PRD §6.7（`:1229`）锁定：Flutter 3.41.9 / Dart 3.11.5，不使用 3.44.x 及以上**（避开其 SwiftPM 与 Android 混合合成底层改动带来的插件适配风险）。**本条以 PRD 为准，此处仅留指向**；
- **状态管理**：**Riverpod**（M4 启动时补定的文档缺口，已写入 PRD §6.7）。范围条 / 资源需求开关 / 分类筛选构成的共享筛选态统一由它承载，禁止 `setState` 跨页同步；
- **地图 SDK**：保持高德地图，但**用原生 SDK 而非 Web JS API** —— 仓库 `prototype/*.html` 里的是 Web 端 JS key，**不能用于原生**；Android/iOS Key 须分别申请（口径见 PRD §6.7）。~~仅接 MarkerCluster 聚合插件~~ **已由 PRD §6.7（`:1235`）取代：`amap_map` 插件不提供原生 MarkerCluster，聚合改为 Dart 侧自实现网格聚合**，性能经 §6.10.1 前置 POC 验证；**不接 HeatmapLayer**（❌ Scope 偏离：热力是视觉炫技非核心，增加渲染负担拖慢 P95 延迟）；
- **认证服务**：身份证二要素 + 营业执照 OCR + 工商三要素（接入第三方）；仅此二层独立展示，不合并为统一信誉分；
- **匹配算法（Scope 校正：纯信息，不追踪撮合结果/历史信誉）**：
  - Baseline（当前 PRD 基线，随时可回滚）：轻量综合加权打分（距离 45% + 时效 25% + 分类 20% + 关键词 5% + 认证 5%），权重后台可配
  - T1 升级（Batch2，10%双轨灰度）：LR 一阶召回 + GBDT 二阶精排 + 供需双独立模型 + 分类 Hard Filter 前置 + 90/10 探索 + 双塔召回可选项
  - **⚠️ Scope 红线：特征集永远不取撮合反馈/历史撮合成功/信誉分/人格标签**，只允许使用：联系点击日志、位置精度、时效（发布/续期时间）、分类匹配、认证状态、关键词/类目 ID、信息完整度档（🟢/🟡/🔴）这 7 类纯信息信号
  - 独立 Feature Flag 灰度开关 + 中央降级按钮：一键回滚 Baseline
- **发布模板引擎**：JSON Schema 驱动，分类 ID 映射模板，前端通用渲染器；叠加 T2 AI 辅助生成：3B 级开源小模型（BOSS Nanbeige4.1-3B 可部署）+ 严格 Scope 四步管线（Scope 提取交叉验证→Prompt限死→逆向比对）= 幻觉率 ≈ 0
- **AI 调用成本控制（Scope 校正：不按信誉分级）**：按「实名状态+信息完整度档」分层（实名+🟢=20次/实名+🟡=10次/未实名/🔴=3次）/天；发布页打开瞬间后台预生成；
- **AI 防御体系（三道后处理，纯信息合规不评人格）**：① 敏感词/类目标签过滤 ② AI 生成内容与 Scope 源交叉一致性比对（幻觉拦截） ③ 违规内容 1% 抽审（不分发布者等级，只按内容抽检）；再加 AI 生成内容检测（DetectGPT/AIthentic，开源准确率>92%）+ SimHash 重复指纹去重；
- **分级管理（Scope 校正：信息本身分级，不做人的信誉分级）**：信息完整度三档规则引擎（if-else 纯规则，无分位数/状态机依赖）：~~必填项完整度 50% + 位置精度 30% + 类目匹配度 20% → 三档阈值：≥85%=🟢 / 50-84%=🟡 / <50%=🔴~~ **该加权公式已作废，以 PRD §9.8「三条件计数」为准**（纠正记录见条目 `:623`）；**基础实名权限两档（未实名=每日看 3 条详情防爬 / 实名=无限次发无限次看）**；不做商业化分级售卖、不做升档培育期、不做三方制衡复审。
- **联系方式通道（Scope 校正：永久单轨，不做 IM）**：只做统一联系方式中转页（电话/VX 二选一 + 号码中间 4 位脱敏 + 点击复制/拨出触达埋点）；**永远不接 IM SDK、不做文本聊天、不设 DAU 阈值切换、不做付费中转**；中转脱敏日志保存 30 天自动清理。

### 2.6 上架前置清单（行政流程，与开发完全并行，2026-08-23 制定）

> 📖 **操作指导另见独立文档**：[上架前置工作指导手册](./docs/上架前置工作指导手册.md)
> 本节只回答「**做什么、多久、卡谁**」（概览与状态总表）；手册回答「**具体怎么做**」—— 每项含办理入口 URL、逐步操作、材料格式硬指标、驳回雷点、周期来源。两处 P1–P10 编号完全对应，**勾选状态以手册为准**。

**核心认知：备案与软著是行政排队，不能加班赶，它们才是上架链路上真正的关键路径，而非写代码。** 因此本清单必须**立刻启动**，不等代码。

#### 第 0 步：本周立刻做（8/24-8/28）——后续一切的前提

| 序 | 事项 | 周期 | 关键要求 | 状态 |
|---|---|---|---|---|
| P1 | **核对已有执照经营范围** | 当天 | 必须含「软件开发 / 信息技术服务 / 互联网信息服务」；不含则需先做经营范围变更 | ⬜ |
| P2 | **购域名 + 国内云服务器** | 当天 | 约 131 元/年。APP 备案表必填「App 运行时连接的具体域名」，且**隐私政策必须挂在自有域名下**，第三方托管不接受 | ⬜ |
| P3 | **锁定 App 正式名称** | 当天 | ⚠️ **最高频驳回点**：软著登记名 = APP 备案名 = 商店上架名 = 桌面显示名，**必须一字不差**。「找鸭找」现在定死，之后改名等于全流程重做。软件名不得含版本号以外的数字 | ⬜ |
| P4 | **提交 ICP 域名备案** | 7-20 工作日 | 服务器须在境内；主体与执照一致 | ⬜ |

#### 第 1 步：软著（最长的腿，必须最先提）

| 序 | 事项 | 周期 | 关键要求 | 状态 |
|---|---|---|---|---|
| P5 | **提交软著申请** | **保守 60 工作日** → 预计 11/17 下证 | **不需要 app 做完**，只需：源代码前后各连续 30 页（每页 ≥50 行有效代码，空行注释不计）+ 操作说明书 ≥10 页（每个核心功能界面配截图）+ 申请表。**2026 年新要求：技术特点与功能介绍需 500-1300 字，过短直接补正**。官费 0 元。⚠️ **时限口径**：法条与中国版权保护中心官网原文为「自受理日起 **60 日**」，国家政务服务平台另列「法定办结 60 工作日」；叠加受理前 4-8 工作日与发证 5-10 工作日后，业内实操普遍 2-3 个月。本项目**统一取 60 工作日为保守规划值**，详见[手册 P5 口径说明](./docs/上架前置工作指导手册.md) | ⬜ |

> **为何必须现在提**：软著是全链路最长的一条腿，晚一天则商店上架晚一天。加急 400 元只是优先处理，**官方明示审核周期不变**，花钱买不到时间。

#### 第 2 步：ICP 通过后

| 序 | 事项 | 周期 | 关键要求 | 状态 |
|---|---|---|---|---|
| P6 | **写隐私政策并挂自有域名** | 1-2 天 | 必须独立可访问 URL；须完整覆盖权限收集用途、第三方 SDK 清单（**高德地图 SDK 必须列明**）、数据存储、账号注销通道、未成年人保护；App 首启弹窗主动同意，未同意前**禁止读取任何设备信息** | ⬜ |
| P7 | **工信部 APP 备案** | ICP 后约 7 工作日 | 需 APK 签名信息（MD5/ 公钥 / SHA-1）→ **依赖可出包**；备案主体、包名、APP 名称须与软著、ICP 完全匹配 | ⬜ |
| P8 | **注册各渠道开发者账号** | 1-3 工作日/家 | 华为/小米/OPPO/vivo/应用宝 各约 300 元认证费；Apple 企业账号 299 美元/年（UGC 类不可用个人账号） | ⬜ |

#### 第 3 步：上架前后

| 序 | 事项 | 时限 | 说明 | 状态 |
|---|---|---|---|---|
| P9 | **公安联网安全备案** | 上架后 **30 日内**（法定强制） | 在「全国互联网安全管理服务平台」填报包名、服务器、用户数据规则；**社区/交友类为重点核查对象**，商店会抽查回执 | ⬜ |
| P10 | **安全评估报告** | 提审前 | 华为对**社交/资讯类强制**要求；应用宝对社交类要求《安全评估报告》。本产品含用户发布 + 联系他人，属重点类目 | ⬜ |

#### 本产品特有的合规风险点（须提前规避）

1. **UGC 内容属性明确**：产品含用户自主发布信息 + 陌生人联系，安卓渠道判定为「社区类」，**个人主体一律拒审**，必须企业主体（已有执照，此项已解）。
2. **不触发 ICP 经营许可证（B25）的红线**：只要出现会员充值 / 付费解锁 / 虚拟打赏 / 平台广告 / 商家入驻**任一项**，五大安卓商店即强制要求 ICP 许可证（门槛：注册资本 ≥100 万 + 3 人连续 1 个月社保）。**本产品 Scope 已永久排除支付与商业化（见 PRD 红线），恰好规避此项** —— 这条红线现在成了排期上的护城河，**不可为增收而破**。
3. **位置权限是高频驳回点**：地图为核心功能，定位权限申请必须有明确用途说明，且首启前不得静默获取。
4. **算法备案潜在风险**：BRD 规划的 Batch2 LR+GBDT 匹配属「个性化推荐算法」，可能触发《互联网信息服务算法备案》。**内测包阶段用规则加权（阶段 0）不触发**，上架后若开启模型推荐需评估此项。

### 2.7 9/30 内测包范围裁剪（28 工作日单人产能约束）

**冲突事实**：9/30 距 8/23 仅 **38 自然日 / 28 工作日**（含中秋假期未扣）；而 BRD §7.2 现行规划为 **6 个月三批滚动 MVP**，其中 **Batch1 单独就占「月 1-2」**。全量 PRD（14 页 + 3 模态 + 后端 API + 认证 + 后台 7 页）粗估 **90-110 人日**，单人 + AI 辅助在 28 工作日内**不可能完成全量**。

**因此本期内测包按下表裁剪。这不是降低标准，而是让「绝不允许项目延期」这条规则在数学上成立。**

| 范围 | 本期（9/30 内测包） | 依据 |
|---|---|---|
| ✅ **必须有** | 账号（登录/实名）→ 发布（模板+记忆）→ 首页地图（分类三级树图层 + 聚合）→ 详情（信任卡）→ 联系中转 **五大 P0 闭环** | BRD §2.4 执行顺序 1-3；无此五项则产品不成立 |
| ✅ **必须有** | 信息完整度三档分级（纯 if-else 规则引擎，无模型依赖） | T4；Batch1 内容，且为发布质量地基 |
| ✅ **必须有** | 聚合性能 POC（**前置 1 天**） | D2 决策兜底；POC 不通过亦**不得下调本批次目标、不改写 SLA、不下调 Pin 上限**，只调整下一批次阶段目标（按 PRD:2652 / 条目[121] KTD-6 回写） |
| 🔶 **降级实现** | 我的模块（我的发布/收藏/通知）—— 只做列表与基础操作，通知不做双向推送 | S1 双向推送属 Batch2 |
| ⏸ **本期不做** | 运营治理后台 7 页 + 4 角色 RBAC + 审计留痕 | Batch1 原含，但内测期用户量小，可用数据库直接操作替代；**上架前必须补齐** |
| ⏸ **本期不做** | LR+GBDT 匹配（用阶段 0 规则加权替代：时效 40%+距离 40%+完整度档 20%） | Batch2；且样本量为 0 时模型无法训练，本就不可做 |
| ⏸ **本期不做** | AI 智能发布（3B 小模型）| Batch3；`ai-confirm-screen` 页面骨架保留，但不接模型 |
| ⏸ **本期不做** | 动态蜂窝半径引擎 | Batch3；用固定四档半径（1/3/5/10/全城）替代 |

**⚠️ 排期风险明示（不可粉饰）**：即使按上表裁剪，五大 P0 闭环 + 后端 + 认证在 28 工作日内仍属**高强度满负荷**，无缓冲。按 user_rules「绝不允许出错」要求，若第 3 周（9/11）核心闭环未跑通，须立即触发范围二次裁剪（优先砍「我的模块」与实名认证，改为 Mock），而非压缩自测时间。

### 2.8 Batch1 编码迭代计划（Sprint Backlog，2026-09-09 定案）

> 本节是编码期唯一迭代基线：条目拆解、排序、检查点均以此为准；条目验收统一执行《完成定义（DoD）》（`docs/architecture/完成定义DoD.md`），本节不复制其条文。
> 条目编号衔接进度记录（已用至 [119]，本规划工作记为 [120]，编码条目自 [121] 起）。

#### 2.8.1 需求基线（引用唯一真源，本节不复制内容）

| 基线 | 真源 | 冻结口径 |
| --- | --- | --- |
| 功能范围 | PRD §4.1/§12.5 + §2.7「9/30 裁剪表」 | 五大 P0 闭环 + 信息完整度三档 + 我的模块降级 + 通知不做推送；**实名认证 = 空壳占位（前端表单 + 后端接收不入库，SDK 接 Batch2，对应详设 §5.7 cert 域空壳、接口映射 0）** |
| 接口契约 | `docs/api/openapi.yaml`（`x-batch: Batch1`） | 20 业务接口 + `POST /track/events`（auth 6 / category 2 / post 7 / map 2 / contact 2 / notify 1 / common 1） |
| 实现口径 | 《后端详细设计文档》§0–§9 + §20–§21、《前端详细设计文档》§10–§19 | 九域分层、横切链、双 ID、幂等限频、11 定时任务（Batch1 为 #1–#9） |
| 工程纪律 | 《编码规范》v3.0（`docs/architecture/编码规范.md`，唯一正文）+ 常驻红线 `.trae/rules/s2s找呀找.md` | 三全局纪律、唯一实现处清单、必测断言清单 |
| 数据基线 | Flyway 迁移管理（工作树 `src/main/resources/db/migration/`：V1 业务 15 表 + V2 `restart_window`；`migration_track/`：track 库 V1，已剔除 CREATE DATABASE/USE/GRANT 三句，DDL 副本 SHA-256 与 `docs/database/ddl/` 存档源一致）+ `deploy/initdb.d/01-init-databases.sh` 幂等建库授权 + MySQL 镜像 8.4（实测 8.4.11）+ 《数据库设计文档》；原行引用的 `deploy/db/V1__init_mysql.sql` + `V1__init_redis.txt` 两文件不存在，系失实引用（条目[121] 回写，依据 docs/plans/2026-09-10-1038-feat-item121-backend-bootstrap-plan.md（s2s-wt-backend）） | 变更走新 Flyway 版本 |
| 质量基线 | 前端 `flutter test` 303 项全绿（只增不减）、`flutter analyze` 0 issue | 门禁四态语义（部署 §14.5） |

#### 2.8.2 用户故事列表（Batch1 全集，backlog 条目的需求锚点）

| US-ID | 用户故事 | 挂钩契约 | Sprint |
| --- | --- | --- | --- |
| US-A1 | 用户手机号+验证码登录/首次注册、续期、退出 | sms/send、sms/login、token/refresh、logout | S2 |
| US-A2 | 用户查看/更新本人资料 | GET/PATCH /users/me | S2 |
| US-A3 | 首次启动同意隐私协议后方可使用 | 前端 privacy-gate（已实现），协议文本随包内置 | — |
| US-C1 | 用户浏览三级分类树、按叶子类目获得发布模板 | categories/tree、templates/{leaf} | S2 |
| US-P1 | 用户上传图片（两步直传）、预检、发布帖子 | media ticket/commit、precheck、POST /posts | S2 |
| US-P2 | 用户管理我的发布（列表、下架/重上/延期） | posts/mine、PATCH status | S3 |
| US-M1 | 用户在地图查看 Pin（游客受浏览限制） | /map/pins | S3 |
| US-M2 | 用户按关键词+筛选检索列表 | /posts/search | S3 |
| US-D1 | 用户查看帖子详情 | GET /posts/{id}（42907 游客限次） | S3 |
| US-K1 | 登录用户经频控查看完整联系方式 | GET /posts/{id}/contact（42902） | S3 |
| US-K2 | 用户举报不当帖子 | POST /posts/{id}/report | S3 |
| US-CERT1 | 用户提交实名信息（前端表单 + 后端空壳接收不入库，SDK 接 Batch2） | cert 域空壳（详设 §5.7，无接口） | S2 |
| US-N1 | 用户查看通知列表（不做双向推送） | GET /notifications | S4 |
| US-T1 | 系统采集行为埋点支撑可观测看板 | POST /track/events | S4 |
| US-S1 | 系统到期自动下架帖子 | 定时任务 #1 | S4 |
| US-S2 | 系统预建/清理埋点月表 | 定时任务 #2/#3 | S4 |
| US-S3 | 系统清理审计/脱敏/收藏记录 | 定时任务 #4/#5/#6 | S4 |
| US-S4 | 系统清理注销用户全部身份行 | 定时任务 #7 | S4 |
| US-S5 | 系统每周巡检 NFR 阈值 | 定时任务 #8 | S4 |
| US-S6 | 系统清理 24h 未 commit 媒体孤儿 | 定时任务 #9 | S4 |

#### 2.8.3 迭代节奏（四周 Sprint，窗口 9/9–9/30 共 15 个工作日）

日历事实：9/9（三）起；9/25（五）中秋节按连休 9/25–9/27 保守计（如实际不休，S3 顺延吸收为缓冲）。

| Sprint | 工作日 | 目标（出口闸门） | 检查点 |
| --- | --- | --- | --- |
| S1 地基 | 9/9–9/11（3 天） | 后端工程可启动、横切链自测过 | 9/11：条目 [121][122] 全绿，否则后续全部顺延并当日上报 |
| S2 写路径 | 9/14–9/18（5 天） | 登录+发布真链路跑通（真服务，非 mock） | **9/18 = 原 §2.7「核心闭环跑通」重定基线点**：不过则立即二次裁剪（先砍 US-N1 通知、我的模块再降级） |
| S3 读路径 | 9/21–9/24（4 天） | P0 五环全真链路通（账号→发布→地图→详情→联系） | 9/24：五环联通验证；不过砍 US-M2 检索降级为列表直查 |
| S4 收尾 | 9/28–9/30（3 天） | 埋点+定时任务就绪，release 内测包交付 | 9/30：L3 出包门禁全过方可交付 |

无显式缓冲 Sprint；缓冲内嵌于 S3/S4（S3 仅 4 天、S4 仅 3 天，中秋若不休即转为缓冲）。

#### 2.8.4 Sprint Backlog（条目 [121]–[131]，依赖链自上而下）

> **两列新标注意义**：
> - **会话属性**：🟢 = 可独立开新会话（上下文不依赖前序热数据，开工前会主动提醒用户确认）；🔴 = 建议延续当前会话（上下文热依赖，切换损耗大）
> - **任务类型**：🔵 常规任务 = 确定性编码（代码生成/补全/接线/批量开发/工具调用），高速低价；🔴 复杂任务 = 需强推理（多模块联动/算法/安全红线/边界推演/高风险评审）

| 条目 | 内容 | 依赖 | Sprint | 会话属性 | 任务类型 | 条目特有验收点（通用 DoD 之外） |
| --- | --- | --- | --- | --- | --- | --- |
| [121] | 后端工程初始化 + common 三组件：Maven（Java 21/Spring Boot 3/显式版本）、`com.s2s.server` 九域包骨架、`ApiResponse`+`ResponseBodyWrapper`、`GlobalExceptionHandler`、`ErrorCode` 25 枚举、Flyway 接入两份 V1 DDL + `restart_window` 补 DDL（缺口 #1 前半）、`application-*.yml` 全 `${VAR:?}` 占位 + `.env.example` 逐一对应、缺变量启动快速失败 + **聚合性能 POC（前置 1 天，PRD:2652 口径：POC 不通过亦不得下调目标、不改写 SLA、不下调 Pin 上限，只调整下一批次阶段目标——按 PRD:2652 / 条目[121] KTD-6 回写；产出结论供 [126] 校准阈值 = 缺口 #2）** | 无 | S1 | ✅已完成 | 🔵 常规 | ErrorCode 总数 25/无重复/`code/100==httpStatus`/8 个 Retry-After 码带秒数（规范 §7.2-①）；缺变量启动失败实测；**回写详设 §2.3「枚举总数 23」过期文字为 25**；聚合 POC 产出书面结论 |
| [122] | 横切链四件：`RequestIdFilter`→`AuthInterceptor`→`RateLimitInterceptor`→`IdempotencyInterceptor`（顺序定死）+ `RateLimitKeys` + 两 ID 分工 + Redis 会话存储 | [121] | S1 | ✅已完成 | 🔴 复杂 | 幂等四类必测输入（§7.2-②）；429 段与 40105 必带整数秒 Retry-After；42907 判定在鉴权上下文之后 |
| [123] | auth 域 6 接口（短信渠道级限频、登录失败计数、Token 续期）+ cert 空壳占位（US-CERT1：前端表单 + 后端接收不入库）+ **前端 `lib/core/network` 五拦截器 + `api_error_code`（内存 mock 先行，gzip 第一天实测 = 缺口 #6）** | [122] | S2 | ✅已完成 | 🔵 常规 | 契约示范测试 baseUrl 切真服务、断言一行不改（§7.3）；前端网络层必测（三头 containsKey、uuid 全链路仅 1 次、`/map/pins` 读 3s、40101 单飞三断言）；cert 空壳不接 SDK、不入库 |
| [124] | category 域 2 接口（`category_version` 字符串比较、Caffeine 缓存）+ 前端分类树/级联选择器接线 | [123] | S2 | 🟢 独立（建议后端+前端同一会话一次联调完） | 🔵 常规 | 模板引擎渲染对稿核验；缓存 TTL 只引用常量 |
| [125] | post 发布链：precheck + POST /posts + media 两步 + OSS 直传 + 敏感词/图片审核 + 完整度三档 + 乐观锁 | [124] | S2 | 🟢 独立 | 🔴 复杂 | 40901/40902/40303/40304 全路径；媒体 commit 服务端二次校验；原图删除同事务失败注入（§7.2-⑩）；STORED 生成列不入 insert/update（§7.2-⑥）；表名沿用 `post_media`（缺口 #3 已裁定，禁改名） |
| [126] | map 域：/map/pins（五要素齐备才发、500 上限、读 3s）+ /posts/search + `GridIdCalculator`（Java）+ 三端 10 向量对拍 + 前端地图接线（Pin 缓存自检、stale 全清）；**按 [121] POC 结论校准聚合阈值回写常量（缺口 #2，不猜值）** | [125] | S3 | 🟢 独立 | 🔴 复杂 | grid_id 10 向量含第 9 条中间微度 ==-2（§7.2-③）；`idx_pins_cover` EXPLAIN `Using where; Using index` |
| [127] | 详情/状态/我的发布：GET /posts/{id}（42907 游客限次、`detail_quota_enabled='off'` 短路）、PATCH status、GET /posts/mine + 前端详情/我的发布接线 | [126] | S3 | 🟢 独立 | 🔵 常规 | 缺 version→40001 不兜底、0 行→40903（§7.2-⑦）；42907 响应不区分命中维度、文案不承诺次数 |
| [128] | contact 域：GET /posts/{id}/contact（`CryptoFacade` **唯一解密点** + audit_log 同事务 + 42902）+ POST report + 前端联系中转接线（外呼三降级、号码用完即弃） | [127] | S3 | 🟢 独立 | 🔴 复杂 | 解密调用点 ==1（§7.2-⑤）；AAD 跨行解密失败、审计同事务回滚、审计白名单（§7.2-⑩）；42902 本地剩余刷 0 |
| [129] | notify 域 + track 埋点落库 + 定时任务 #1–#9 + `restart_window` 启动自写（缺口 #1 后半）+ `track_drop_stat` 落日志（缺口 #5） | [128] | S4 | 🟢 独立 | 🔵 常规 | 埋点 SQL 形态 `ON DUPLICATE KEY UPDATE id=id`、`ts` 取客户端值、月表 LIKE 预建（§7.2-⑨）；任务 #1 守 status / #3 DROP / #7 按 user_id 全删（§7.2-⑧）；`uk_event_dedup` 四列逐字冻结（缺口 #7 已关闭） |
| [130] | 前端埋点链（队列 2000 上限/dropped_count/批次换新键/回灌限速/四段独立计时）+ `mem_peak_mb` Android 通道（缺口 #4）+ 全链联调 + 压测（P95 `rt_ms`、并发 20×5min、异机发压） | [129] | S4 | 🟢 独立 | 🔴 复杂 | 埋点前端必测集（§7.2 埋点组）；压测逐接口单独出数、日志 grep 红线留证（扫描面为空记 SKIP 不记 PASS） |
| [131] | 出包与交付：release 签名 APK、L3 门禁 11 步序列、release-gate.log、SHA-256 归档 | [130] | S4 | 🟢 独立 | 🔵 常规 | 后门双 grep 计数为 0（非 0 中止删包）；`rel/*` tag 与 `APP_VERSION` 严格同值；FAIL 物理删 APK |

**并行行政项（不占条目编号，纳入风险提示）**：~~高德 Android/iOS Key 申请~~ → **高德 Android Key 已申请（开发版，2026-09-09）✅；iOS Key 待申请**（用户侧，阻塞 [126] iOS 真机联调）；阿里云 OSS 新 key 已注入 `.env.dev` ✅，**高德+火山泄露 key 控制台作废**（条目 [115] 挂账，非阿里云；含 `prototype/index.html`、`prototype/test.html` 早期原型高德 Web 服务 Key，G-Q2 登记册已登记豁免并责令控制台作废更换，文档不抄录完整值）；`.env.dev` 随机密钥生成（阻塞 [121] 启动验证）；软著/ICP/公安备案按 §2.6 推进。

**前端平行线说明**：`lib/core` 于 [123] 建立后，各域前端接线并入对应后端条目同 Sprint 完成；每 Sprint 末跑反冗余静态扫描（features 无 for 重试、无 `values.byName`、grid 无 `~/`/`.toInt()`，规范 §1.3）。

### 2.9 待裁定决策项登记（gap-register）

> 编码期自拟、依据源（PRD/详设）未明文的防御性参数集中登记于此。每项必须满足闭环三必查（learnings 评审纪律）：**定案时点绑定确定事件 / 定案前有替代保障 / 落在负责人动作清单**，三者缺一即真缺口。参数代码注释与详设表格均指向本节编号；裁定后回写两处并将状态改为已裁定。

| 编号 | 决策项 | 当前临时值与理由 | 负责人（owner） | 定案触发事件（先到者触发） | 定案前替代保障 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| DEC-01 | 客户端自动重试的 Retry-After 自动等待上界（`NfrNetwork.retryAfterMaxSec`，仅夹 RetryInterceptor 自动重试 sleep，不夹 429 段 UI 倒计时） | **900s**。编码期防御上限，锚定 PRD §12.5 最长已知窗口（40105 锁定 15 分钟）；异常巨值（如 `999999999`）不把重试 Timer 挂起数年，负值夹 0 | **产品 / 详设维护者**（编码侧不自行裁定；评审 finding 2026-09-14，[123] 复审 #2，run `20260914-071944-66bf99f4`） | ① 详设 §14.1 下次评审；② PRD §12.5 等待窗口/锁定时长口径变更；③ 429 段 UI 倒计时语义变更——**三者先到者触发** | 900s 安全防御值：低于该值的服务端秒数原样 sleep，高于则夹 900s，行为可预期；429 段 UI 倒计时不自动重试、不钳制（服务端原始秒数可能合法更长，如 3600s），故参数悬置不影响用户可见倒计时 | 待裁定（2026-09-14 登记） |
| DEC-02 | 幂等并发/重放轮询的间隔与总超时（`IdempotencyPolicy.POLL_INTERVAL_MILLIS`/`POLL_TIMEOUT_MILLIS`/`PENDING_PLACEHOLDER`，[122] U5 消费） | **200ms / 10s / "PENDING"**。依据源（详设 §3.3、PRD §12.1）未明文，编码期防御默认；闭环条件 = 首次执行在首发 10s 轮询 + 至多 2 次客户端重试各 10s 窗口内完成（详设 §14.1 重试总数 2 次） | **详设维护者** | ① 详设 §3.3 下次评审；② PRD §12.1 幂等窗口/重试口径变更——先到者触发 | 超时返 50001，客户端同 Key 自动重试命中完成态缓存闭环；轮询读放大（每等待者 10s 约 50 次 GET）复核见 [130] | 待裁定（2026-09-19 登记） |
| DEC-03 | Redis 黑名单键 `jwt:bl:{jti}` 的值语义（[122] U3 只读查询，写入随 [123] logout） | **存在即命中，值无意义**。依据源未明文键值形态；[122] 只落地「查键存在性」的读路径，Token 登出写黑名单随 [123] logout 定案 | **详设维护者 / auth 域** | [123] logout 落地时定案（写入方与键值/键 TTL 一并裁定） | 黑名单读故障统一 50001（KTD7），绝不降级放行——Redis 不可用时被登出 Token 不会因「查不到」而复活 | 待裁定（2026-09-19 登记） |
| DEC-04 | 匿名写接口幂等键维度分流（KTD13：`idem:dev:{X-Device-Id}:{key}` vs `idem:anon:{key}`）在 X-Device-Id 头漂移时，同一 `Idempotency-Key` 落到两个键、SETNX 各自成功、同一写操作执行两次（幂等击穿） | **保持 KTD13 分流不变，边界风险待裁定**。KTD13 已裁定匿名键按设备头分流（合法→dev 键，缺失/非法→anon 键）；该设计的边界风险是设备头在两次尝试间出现/消失导致键漂移双写（[122] 代码评审 adversarial #9 发现，settled_conflict 不随评审自动改） | **详设维护者 / 产品（auth 域）** | ① KTD13 复审；② [123] 匿名写接口（短信登录）落地时定案——先到者触发 | ① 限流钳制：限流先于幂等（详设 §3.1），SMS 渠道级限频（手机号/IP 维度）兜底滥用面；② Idempotency-Key 为 UUID v4 不可预测，同 Key 设备头漂移需客户端异常或主动构造（设备头应在会话内稳定） | 待裁定（2026-09-19 登记） |
| DEC-05 | MySQL 数据面 TLS 的**服务端身份校验**口径：现用 `sslMode=REQUIRED` 只加密、不校验证书主体（未配 CA 信任链），同一 Compose 网络内冒充 `mysql:3306` 的进程仍可终止 TLS 取得库口令 | **`sslMode=REQUIRED`**。已解「`caching_sha2_password` 非 TLS 首连索取 RSA 公钥失败」（原报 `Public Key Retrieval is not allowed`，compose/staging/prod 同形态同踩），并使数据面全程加密、无 TLS 时 fail-closed；**刻意不采用 `allowPublicKeyRetrieval=true`**——那等于把口令交给冒充者，抵消架构 §4.3 / 安全 §10.1 的「强口令」控制 | **部署 / 安全维护者** | ① 《系统安全设计方案》下次评审；② 首次对外发版前的安全门禁；③ 部署形态变更（MySQL 迁出单机 / 加 `ports:` 映射）——先到者触发 | ① 连接串已 TLS 加密且 fail-closed；② MySQL 3306 仅容器网络、不映射端口（架构 §4.3）；③ 应用账号强口令。三者叠加下「同网络冒充」需先取得容器网络内进程执行权，与安全 §10.1 已登记的内网横向威胁同级，**非本次新增面**；收紧方案（如需）= `sslMode=VERIFY_CA` + MySQL CA 装进应用信任库，届时须同步改《部署架构设计文档》§14 与本文档 | 待裁定（2026-09-19 登记） |
| DEC-06 | 登录方式演进路线（PRD §3.4.1「短信验证码主方案」vs 微博式本机号码一键登录） | **三阶段演进（2026-09-19 用户裁定）**：① Batch1（9/30 内测包）= 短信验证码登录（PRD 既有口径，不动）；② Batch2 = 追加微信登录 + 本机号码一键登录（`user_identity` 表已预留 `wechat`/`apple` 身份类型，追加登录方式**不改表结构、不做数据迁移**）；③ Batch3（公开上架前）= 一键登录升为主登录，短信降级为 WiFi/无 SIM 场景备用。Batch1 短信渠道用 dev 桩（固定验证码），凭证从「可选绑定」收紧为「硬必填」随 [124] crypto 组件落地 | **产品 / 架构维护者** | ① Batch2 排期启动；② 微信开放平台资质审核通过（**行政阻塞项，建议 Batch1 期间并行申请**）；③ 一键登录服务商选定——先到者触发 | ① `user_identity` 表已按 PRD §3.4.1「登录身份归一化」裁定预留多身份挂载（Batch1 只写 `phone`）；② 短信验证码作为降级方案保留，**不砍**；③ 一键登录后端逻辑复用既有登录流程（只是「获取手机号」的方式变了，服务端改动量小）；④ 微信登录的开放平台资质审核周期不可控（1–2 周），**建议现在就启动申请**（与 Batch1 开发并行） | 待裁定（2026-09-19 登记；三阶段路线已裁定，具体 Batch2 排期与服务商选型待 Batch2 启动时定） |

**裁定后动作清单（owner 执行，编码侧配合）**：① 回写详设 §14.1 表格 1446 行「待产品确认」标注为正式口径；② 回写 `lib/nfr_constants.dart` 的 `retryAfterMaxSec` 注释（删「待裁定」、注明裁定来源）；③ 如取值变化，同改 `NfrNetwork` 常量与既有测试期望（retry_interceptor 巨值钳制用例）；④ 本表状态改「已裁定 + 日期 + 出处」。

---

## 三、进度记录


### 2026-08-10

- [01] ✅ 启动 grilling：四轮产品拷问（动机→范围→分类→定位→闭环→发布丝滑→5km 语义→变现→信息边界→IM→品牌→差异化）；
- [02] ✅ 收敛报告产出并获用户接受；
- [03] ✅ 新建 `docs/` 目录；
- [04] ✅ 编写并交付 **BRD.md**（含背景/战略转向/市场机会/定位/目标用户/商业模式/竞品/里程碑/风险/资源依赖/决策记录 11 章）；
- [05] ✅ 编写并交付 **MRD.md**（含市场分层/用户画像/场景/Kano 需求优先级/竞品深度/GTM/成功指标/假设依赖 9 章）；
- [06] ✅ 编写并交付 **PRD.md**（共十大部分：视觉设计系统/分类体系/账号体系/实名认证/发布模板/首页与列表/详情与联系/我的模块/设置合规/核心闭环总览；严格按"功能名/需求描述/概述/相关页面设计/用户旅程/用户故事/实现逻辑/功能细节描述"8 段式输出）；
- [07] ✅ 创建并完善本「说明文档.md」。

### 2026-08-19（两轮 go deep ce-ideate 完成）

- [08] ✅ 第一轮 ce-ideate（需求深度还原）：6 frame × 5 条 axes（双向完整/指标断层/5km密度/信任降级/运营后台黑洞），产出 56 条 raw ideas → 批判筛选 Top 7 Survivors（S1双向推送双漏斗/S2动态蜂窝30s兜底/S3四层信任+动态衰减/S4分类后台MVP+临时类目+举报分级/S5邻居模板参考+热力缺口+双发布模式/S6撮合确认码+轻半闭环+周度自适应调权/S7冷启动10-20条种子配方+双向单向开关+中转IM双轨）+ 6 类 Rejection Summary（每类附复活阈值），工件落盘；
- [09] ✅ 第二轮 ce-ideate（围绕三大核心目标落地）：用户明确新三大目标（①集成AI能力：智能匹配+智能发布 ②快捷搜索+地图图层+视觉冲击 ③需求资源分级管理）；先 Grounding 识别 **战略级冲突**：BRD 6 处"砍 AI 一刀切"与目标①正面矛盾，输出 3×3 冲突/缺口矩阵；94 条 raw ideas（6 frame：痛点反演18/跨域类比16/场景压力测试15/反向风险15/价值解锁15/第一性重建15）→ 批判筛选 **T1-T6 6 条 Survivors**（T1:LR+GBDT双轨灰度匹配+标签金字塔+双独立模型+HardFilter / T2:多媒体7秒AI发布+严格Scope四步管线+健康分60+ROI分层+AI反作弊 / T3:信息金字塔五层地图+图层控制面板+供需双色热力+分类差异化聚合+新手专家双模式+三无障碍包 / T4:QS贡献分五档100+统一信誉分+五级权限阶梯+三档商业化解耦+双培育期+AI预审90%+三方制衡 / T5:D5.1 AI三色清单修订+三乘积北极星重定+三批滚动MVP节奏+三目标独立灰度+三轴联动里程碑+差异化slogan / T6:三轴联动闭环飞轮体验可视化+原型HTML4组件改造+合规三道后处理）+ 6 类 Rejection Summary（每类附复活阈值），工件**增量更新同一份 HTML**（两轮合并 150raw/13survivors/12rejections）；
- [10] ✅ 同步更新本说明文档：§1.1 项目目标升级（注明等待T5战略评审通过）+ 北极星/口号重定待评审标记；§1.2 产出物清单新增 ideation 工件 + 原型改造计划标记；§2.5 技术建议全面扩展 Baseline→T1匹配基线、T2 AI生成管线、AI防御体系、QS分级算法、地图热力聚合插件 6 大模块；§三进度记录补全 08-19 两轮 ideate 结果。
- [11] ✅ **2026-08-19 最终 Scope 校正（用户二次明确边界，三轮 ce-ideate 去偏离）**：用户指出上轮 ideate 有两处严重偏离 → ① 定位回归「**纯需求/资源信息共享平台**」：**永久不做支付、不做交易担保、不做信誉评价系统、不追踪撮合结果** ② 图层核心定位回归「**需求/资源分类快速加载引擎**」：图层的唯一价值是让用户快速在地图上勾分类加载对应供需 Pin（P95≤300ms 硬指标），不是认证/时效/质量筛选器、不是视觉炫技/行动导航器 ③ 分级管理对象回归「供需信息本身的完整度」，不是发布者人格信誉等级。本次校正共修改 ideation 工件 **15 处偏离点**（S3 砍动态信誉分 → 二层认证+三档完整度角标 / S6 砍撮合确认码反馈 → 有效期一键刷新+完整度升级引导 / S7 砍IM双轨+DAU门槛 → 永久单轨中转页 / G3 G4 G5 三条主题轴重命名校正 / T3 砍金字塔五层+筛选器面板 → 5大类三级树分类快速加载引擎（三层性能保障P95≤300ms） / T4 砍QS五档信誉+商业化分级 → 信息完整度三档🟢🟡🔴+基础实名权限两档 / 7 个 Rejection 复活阈值从「撮合率/信誉分/金牌率」全部替换为「纯信息指标：图层P95延迟/完整发布率/三乘积」），同步更新本说明文档 §1.1 项目目标冻结（三大目标校正对照表+纯信息北极星三乘积0.5合格线+正式口号）+ §2.5 技术建议砍 HeatmapLayer/撮合反馈特征/QS信誉算法/IM SDK/商业化分级，加分类图层三层性能架构/完整度三档规则/纯中转页单轨。校正后三大目标定义 100% 对齐用户明确边界，无任何偏离项残留。

- [12] ✅ **2026-08-19 第三轮 ce-ideate（三大文档未处理细节修订）**：用户反馈"文档生成的不完整，还有存在一些细节没有处理好"，启动第三轮头脑风暴，系统识别 **30+ 处未处理细节** 并给出具体修订方案 → BRD 9 处（B1-B9：战略/边界/变现/D5.1升级/北极星/里程碑/口号/前置项/种子配方）+ MRD 7 处（M1-M7：用户分层新维度/5场景/12 P0需求/三批GTM/北极星同步/三组指标/3假设）+ PRD 15 处（P1-P15：一句话定义/北极星/边界/账号/认证/发布四模式/有效期/分类三级树图层/蜂窝半径/LR+GBDT匹配/中转页/完整度三档/运营后台/三轴飞轮/双向推送）。同步补"修订优先级与依赖排序"（Batch1/2/3 落地节奏）+"核心风险与应对"（R1 D5.1评审/R2 3B部署/R3 MarkerCluster规格/R4 零商业化红线）。工件增量更新同一份 HTML（三轮合并 180 raw / 43 survivors / 12 rejections）。

- [13] ✅ **2026-08-19 路径 C 争议点讨论决策（D1/D2/D3）**：用户选路径 C"先讨论修正再定"，优先讨论两个争议点：
  - **D1 三乘积北极星权重 → 方案 A 等权硬线 0.6×0.6×0.6=0.216**（全期统一，乘积飞轮叙事最强）。识别风险：Batch1 AI 轴未上线 → 北极星恒 0；兜底写入 BRD §7.2：Batch1 设为"冷启动观察期"不作为团队 KPI 考核，北极星看板照样跑（AI 轴填 0 展示），从 Batch2 开始正式纳入考核。AI 召回率口径同步定义："用户发布需求后 5min 内被推送到 Top20 资源方中，至少 1 个资源方点击查看该需求详情的比例"。
  - **D2 高德 MarkerCluster 性能 → 方案 A 信任官方规格不前置 POC**（不阻塞 Batch1 排期，符合轻量原则）。识别风险：实验室数据可能 5-10 倍退化；兜底写入 PRD §6.x 性能保障章节：① Batch1 开发同步埋点性能日志（图层切换耗时+FPS+内存峰值三项实时上报，P95>300ms 自动告警，零额外成本）；② 预留"性能降级开关"应急方案（触发=连续 3 次帧>16ms 或 P95>800ms；动作=关聚合动画/500Pin上限/关角标发光/toast提示），不立即实施仅文档化；③ 假设 A8 状态改为"上线后第一周实测验证"，若不达标则启用降级开关。
  - **D3 分类聚合阈值表 → 采用新溯源并补进 PRD**（带 evidence 溯源）：工作 3 条聚（58同城招聘数据，城市3km内均值12条）/ 房屋 5 条聚（贝壳数据，3km内均值8条）/ 生活 8 条聚（闲鱼数据，3km内均值5条）/ 车辆服务 8 条聚（参照生活类低密度长尾，无独立evidence）。
  - 三个决策回写 ideation 文件第十三部分（D1/D2/D3 Survivor 卡片+对齐 Survivor 编号），同步触发路径 B 启动。

- [14] ✅ **2026-08-19 三大文档 v2.0 → v2.1 全量修订（路径 B 整合回写）**：基于第三轮 ideate 决策结果 + D1/D2/D3 三个争议点决策，并行启动三个子代理同步更新三大文档：
  - **BRD.md v2.1**：9 条 B 修订 + D1/D2/D3 全部落地，新增约 2450 字。修改章节：§1.2 战略转向第4条 D5.1 / §1.3 战略口号升级 / §3.4 边界表 D5.1 + D3 evidence 框 / §5.2 变现路径 T4 零商业化红线 / §7.1 北极星三乘积公式 + Batch1 兜底 / §7.2 三批滚动 MVP + D1/D2 兜底 / §7.3 成功标准前置项 / §8 风险表 R1 重写 + R8/R9 新增 / §10 决策记录 D5.1 附录。Grep 验证关键词全部命中（三乘积8次/D5.1 17次/三色清单14次/Batch1-3 20次）。
  - **MRD.md v2.1**：7 条 M 修订 + D1/D2/D3 全部落地，新增约 3500 字（361→466 行）。修改章节：§3.4 三目标使用倾向维度 / §4.6-4.10 五新场景 + §4.11 聚合阈值 evidence 溯源 / §5.1 12 条 P0 需求新增（UR-AI/Map/Grade/Ops）+ §5.3 排除项调整避免矛盾 / §7.5 三批 MVP GTM 策略 + §7.5.1 Batch1 北极星考核兜底 / §8.1 三乘积公式 + 三轴口径定义 / §8.2.1 三组关键指标（AI/地图/分级 10 项）/ §9 A6-A8 假设 + §9.0.1 D2 决策兜底。Grep 验证全部命中。
  - **PRD.md v2.1**：15 条 P 修订 + D1/D2/D3 全部落地，新增约 5800 字（14 个新章节段落 + 变更记录表 + 大量修订前后对比块 + evidence 溯源表）。修改章节：§0.1 一句话定义集成三目标 / §0.2 三乘积北极星 + §0.2.1 D1 兜底 / §0.3 边界表 AI 行 D5.1 / §3.7-3.8 账号体系未实名限制 / §4.2/§4.7 实名+资质+完整度三档 / §5.9 T2 AI 智能发布四模式 + §5.10 严格 Scope 四步管线 + §5.11 发布有效期管理 / §6.4.4/§6.9 T3 分类三级树图层 + §6.10 三层 SLA + §6.10.1 D2 兜底 + §6.11 S2 蜂窝半径 + §6.12 T1 LR+GBDT 匹配 + §6.13 T6 三轴飞轮视觉 + §6.14 S1 双向推送 + §6.15 D3 聚合阈值 evidence 溯源表 / §7.4.2/§7.7/§7.8 S7 永久单轨中转页 / §9.8 T4 完整度三档分级 + §9.10 S4 运营治理后台 4 MVP / 附录 A 术语同步 + 附录 B 砍留改对照新增 9 行 + 附录 C P2 留档。Grep 验证 18 个关键词全部命中。

### 2026-08-22（PPT 交付 + 三文档一致性审计）

- [15] ✅ **BRD/MRD PPT 基于 v2.1 交付**：打通「用户 PPTX 模板 → 交付件」技术链路（`.pylibs` 内置 python-pptx + 递归 Group 文本替换 + `path_map`/`text_map` 双重匹配）。产出 `docs/找鸭找-商业需求文档-v2.1.pptx`（23 页）+ `docs/找鸭找-市场需求文档-v2.1.pptx`（28 页）。新建 `docs/cmp_len.py` 做模板原文 vs 生成文本逐框长度审计（规则：新文本长度 > 旧×1.4 且差值 ≥5 判定溢出风险），系统性消除文字重叠，并检出模板原文残留。
- [16] ✅ **ce-brainstorm 三文档一致性审计（11 处偏差全部修复）**：对 BRD/MRD/PRD v2.1 逐条对照本文档 §1.1 Scope 权威表，查出 11 处偏差，归为 4 类根因并全部修正：
  - **口径不统一（4 处）**：① 北极星合格线双标 → **统一为 0.216（60%×60%×60% 等权硬线）**，本文档 §1.1 回改；② AI 配额两套 → 统一为「实名状态 × 完整度档」二维表（实名+🟢=20/实名+🟡=10/未实名或🔴=3）；③ 发布有效期四处矛盾（14天下架 / 默认7天最大365 / 7天刷新+14天下架 / 重发重置30天）→ **统一为「默认 7 天，可一键刷新续 7 天，连续 14 天未刷新自动下架，重发同为 7 天」，不提供其他可选值**；④ 用户分层四层/三层不一致（PPT 已按三层呈现，MD 保留四层作为细分依据）。
  - **v2.0 人格化信誉残留撞红线（3 处）**：① PRD 全文删除 QS 信誉分与金/银/铜牌等级（§5.8 表、§5.10 配套机制、§6.9 可视化、§6.12 需求方权重、§6.13 飞轮与 4 组件），需求方模型权重改为「认证 40% / 完整度档 40% / 时效 20%」；② 删除「确认码×10」，GBDT 标签强度改为「联系点击×5 / 详情点击×2 / 停留 30s×1」；③ MRD「近场撮合完成率 ≥30%/40%」改为「**首次联系触发率**」（阈值不变，仅记录联系动作是否发生，不追踪后续成交），A5 假设验证方式与 §11.1 用户研究同步改口径。
  - **不可运营（1 处）**：MRD §8.3 四条反指标补齐触发阈值与触发后动作（0 联系占比 >50% / 联系后跳失率 >20% / 举报率 >5% / 5km 外扩使用率 >30%；任一项连续 2 个月越线进入 Scope 复议）。
  - **术语歧义（1 处）**：BRD 的 QS 原意为 **Query Score 查询质量分**（与信誉无关），被 PRD 误读为信誉分，已在 BRD §1.2、§6 D5.1 决策表、绿清单表三处加澄清标注；同时清理 BRD/MRD 中「金牌」话术（战略口号、M3 里程碑、Batch GTM、S10 飞轮）。
  - 三份 MD 修正完毕后，两份 PPTX（BRD 23 页 / MRD 28 页）与 PRD Word 已全部基于 v2.1 重生并通过反向校验：红线关键词（QS 分 / 金牌 / 银牌 / 铜牌 / 撮合完成率 / 最大 365 天）全为 0 次，「确认码」仅 1 次且为「不设确认码」的否定式表述；统一口径关键词（首次联系触发率 4 / 实名+🟢 2 / 默认 7 天 6 / 14 天未刷新 1 / 完整度档 40% 1 / 联系点击×5 1）全部命中。
- [17] ✅ **PRD 重命名 + 版本演进痕迹彻底剥离**：`docs/PRD.docx` → **`docs/找鸭找-产品需求文档-v2.1.docx`**（84,290 字节 / 755 段落 / 38 表格 / 24 条目录），与两份 PPTX 命名对齐；v2.1 仅作为文档修订号保留在文件名与标题页，正文内不留任何版本差异叙述。
  - **清理原则**：不删信息，而是把「相对旧版的差异叙述」改写为「直述规格」，红线约束原样保留（如「砍掉 70B LLM 后以 X 替代」→「以 X 实现…，**不使用 70B 端到端 LLM 智能解析**（D5.1 红清单）」）。
  - **PRD.md 共清理约 111 处**（`docs/clean_prd_versioning.py` 批量 91 处 + 手工约 20 处）：删除头部变更记录表与 v1 原型/ideation 关联引用；去掉 16 处章节标题「新增/替换/修订」括注；删除 18 处「修订前 v2.0 / 修订后 v2.1」对照块；删除 21 行 ideation 溯源与风险引用行；删除附录 B 砍留改对照表与附录 D 变更记录（原附录 C 重编号为附录 B）；§10.1 页面清单表「砍/留/改」列改为「关键特征」列并重写 15 行；措辞统一（砍→明确不做/不提供，替换为 X→直述 X）。全文 42,442 → 38,360 字符。
  - **校验闸门升级**：`docs/verify_prd_docx.py` 删除失效的附录 D 校验段，新增「版本演进痕迹反向验证」12 项（修订前/修订后/变更记录/砍留改/v2.0/附录 D/ideation/对齐 Survivor/替代 v1/v1 原型/原 v2.0/v2.1 修订）**全部 0 次通过**；核心规格 32 项、一级章节 12 项、统一口径 6 项全部命中。
  - ⚠️ 旧文件 `docs/PRD.docx` 因被 Word 进程占用未能删除，**请关闭 Word 后手动删除**。
- [18] ✅ **PRD 补全 §1.5/§1.6 + 清除残留「替代」表述**（用户二次评审发现）：
  - **§1.5 用户旅程 / §1.6 用户故事原为 N/A**：根因是 §1 视觉设计系统属规范层，套用了业务章节的八小节模板但未填内容。已补为实质内容——§1.5 写 3 条视觉一致性验收链路（U1 首次打开 / U2 发布链路 / U3 浏览链路），§1.6 写 4 条用户故事（本地居民 / 需求方 / UI 设计师 / 前端工程师），使规范层同样可走查、可验收。
  - **残留「替代」2 处**：§3.4 个人中心线框「← 替代原信誉详情」→「← 实名/资质/完整度入口」；§4.4「"信任与认证"页（替代原信誉详情）」→「"信任与认证"页」。
  - 校验清单扩充至 18 项（增加 替代 / 替换 / 原信誉 / N/A / TBD / TODO），重生后**全部 0 次通过**。文档更新为 84,880 字节 / 761 段落 / 38 表格。
- [19] ✅ **PRD 结构化评审（ce-doc-review）+ 40 处修正全部落地**：对 `找鸭找-产品需求文档-v2.1.docx` 做多视角评审，64 条原始发现去重合并为 49 条，按置信度分三路处理。
  - **第一批 · 9 处机械修正**（高置信可直接应用）：§0.1「三目标/三大目标」→「四目标/四大目标」；§7.4.1 详情页线框残留「历史发布/举报」字段 → 改为完整度行；§6.4.2 聚合阈值改为引用 §6.15 差异化阈值表；§7.1「三个模式」→「四个模式」；§7.4.2 中转页线框删去微信卡片（单轨口径）；5 处失效交叉引用修正（§1.4.3 / §3.4 / §3.5 / §2.9 / §6.5）。
  - **第二批 · 11 项一致性收敛（17 处编辑）**：① **举报下架阈值收敛为唯一口径**——§9.10.3 重写为「全站唯一下架判定口径」（5 类原因基础风险分 + 举报方权重 + 累计 ≥60 自动下架 + 4h 工单 SLA + 申诉清零），§3.7 / §3.8 / §7.7 / §7.8 / §9.6 / §11 六处改为引用本节，不再各自设阈值；② **身份证存储口径统一**——§4.7 与 §9.6 统一为「只存不可逆哈希 + 后 4 位明文，不存完整明文，OCR 原照 7 天删除」，并新增「图片上传强制剥离 EXIF」；③ **防爬改为三维同时限频**（账号 30/日 + 设备指纹 30/日 + IP 100/日，任一超限即拒；1 分钟 ≥10 次拉取当日冻结），额度不按实名状态分层；④ **§11 北极星验收行按 §0.2 实际三轴重写**（AI 匹配召回率 × 分类图层加载成功率 × 🟢 完整发布率），联系触发率降为辅助埋点行；⑤ **补 4 处缺失界面状态**——§5.9 AI 解析失败与结果确认态 5 行表 + 配额消耗口径、§3.7 额度耗尽拦截态、§6.4.4 兜底过程态 6 行表、§5.11 抢发 24h 未补全后果。
  - **第三批 · 14 项设计决策（含 2 项经用户确认）**：
    - **技术栈定为 Flutter**（用户确认）：§6.7 锁定 **Flutter 3.41.9 / Dart 3.11.5**（不用 3.44.x 及以上，避开 SwiftPM 与 Android 混合合成改动风险）；地图改为**高德 Android/iOS 原生 SDK 经 `amap_map` + `x_amap_base` 桥接**（Android AMapSDK 10.1.200 / minSdk 21，iOS 12.0+），并写入两个必配项（先弹隐私协议再调 `AMapInitializer.updatePrivacyAgree`；`AndroidManifest.xml` 声明 `allowNativeHeapPointerTagging="false"` 否则地图页返回闪退）。因该插件无原生聚合，聚合改为 **Dart 侧自实现网格聚合**，连带**重写 §6.10.1 D2 决策**：原「信任高德官方 10 万点 ≤80ms 规格、不做前置 POC」失去依据，改为「**Batch1 前置 1 天聚合性能 POC**，POC 通过后才把 P95≤300ms 写入承诺」，并给出机型/数据量/通过线/不通过时的处置四项验收口径。
    - **未实名发布改为「先发后审」受限态**（用户确认）：解决「实名硬门槛卡在供给侧 vs 冷启动要密度」的冲突。§3.7 定义受限态（每日 1 条、在库上限 3 条、强制 🔴 档、不进 AI 推荐池、联系方式对外完全隐藏、24h 内实名转正常态、24h 未实名自动隐藏不删除），§3.8 / §4.2 / §4.7 / §5.8 / §9.8 五处「不能发布」表述同步改写。
    - **北极星可证伪化**：§0.2 给召回率加**分母生效条件**（同城同二级类目在库资源 ≥20 条才计入分母，不足则归入「供给不足占比」不参与乘积），并标注 0.6/0.216 为**首版拍定基线非已验证目标，第 4 周用实测数据校准**。
    - **消除「7 秒快发 vs 🟢 完整档」互斥**：§5.9 明确 7 秒仅指输入耗时不含确认页，确认页固定「再花 5 秒升 🟢」区块（只补门牌号与三级类目，AI 已猜给一键确认、未猜给定位门牌按钮与候选片），达标实时跳角标，不强制。
    - **补 T1 冷启动降级三阶段**：§6.12 新增阶段 0 规则期（标签 <1 万条或上线 <4 周，只跑时效 40%+距离 40%+完整度 20% 规则加权）→ 阶段 1 LR 期 → 阶段 2 全量期（≥10 万条且单类目 ≥2000 条才开 GBDT），并给出单用户/单类目冷启动兜底与运营强制回落开关。
    - **消除 S1 漏斗与红线冲突**：§6.14 删去「双级意向漏斗（24h 三选一反馈）」，改为只埋「联系动作」一级漏斗，与 §5.11 / §6.12 的「不做撮合反馈收集」同口径；连带把 §6.12 的「不设确认码」改为「不设成交核销环节」以通过红线校验。
    - **运营后台补范围/权限/审计/验收**：§9.10.1 在 4 个 MVP 页外补 3 页（认证审核队列、待运营跟进队列、模型与性能降级开关）；新增 **4 角色最小权限模型**（内容审核员/运营专员/算法运营/超级管理员）、**审计留痕规格**（必留 6 类操作、7 个字段、≥180 天、敏感查看双人复核）、**5 项后台验收通过线**。
    - **P95 口径边界与缓存键补全**：§6.10 明确 P95 计时起点为手指离开 Tab、终点为 Pin 上屏，**四段之和 ≤300ms**（缓存查找+网络+聚合计算+渲染），接口 RT P95 ≤150ms 单列子指标，弱网会话不计入分母；**缓存键定义为 5 元组**（分类 ID + 供需态 + 半径档 + 500m 坐标网格 + 分类树版本号），并给出失效规则与 50 键 LRU 容量。
    - **空圈「平台代发布」给出载体**：§6.4.4 明确**不新建代发实体**，只给用户原需求加 `需运营协助` 内部标记进后台队列，运营手动勾选 ≤20 个同类资源方定向邀请（每资源方每日 ≤2 次且计入推送总额），用户端显示「平台正在帮你找」，运营 SLA 24h，超时自动关闭并推送提示；本期不做自动邀请。
    - **消除 T3 两种可视化与 T6 发光的矛盾**：§6.9 加边界说明——「两种可视化」约束的是 **Pin 承载的信息维度**（只表达供需态与完整度档），§6.13 的亮度与蜂窝色深是同两类信息的强调手法（亮度只映射完整度档、色深只映射发布条数密度），不引入第三类维度。
    - **列表页导航模型统一**：§10.1 把 list-screen 从「底部 Tab」改为「首页内视图」（右上地图/列表切换，共享筛选与范围状态），与 §10.2 底部三 Tab 一致；§6.5 旅程第 5 步同步改写。
    - **🟢 档改为可自主达成**：§9.8 新增三条件达成保障表——必填项给「还差 N 项」计数、门牌号给「取当前定位门牌」+ 手填兜底、**三级类目若运营未配置则选到二级即视为精准命中**（不因运营未配置惩罚用户）；档位实时可见，本表为唯一判定处。
  - **交付件重生 + 反向校验**：重跑 `gen_prd_v2.1_docx.py` → `找鸭找-产品需求文档-v2.1.docx`（95,087 字节 / 812 段落 / 48 表格 / 39,758 字 / 24 条目录）；`verify_prd_docx.py` 五段闸门**零 FAIL** —— 核心规格 34 项全命中、Scope 红线 7 项全 0、统一口径 6 项全命中、一级章节 12 项全在、版本痕迹 18 项全 0。（原记「四段」，2026-08-24 回代码点核对后纠正为五段，见 [36]。）
  - ⚠️ **遗留待确认**：BRD.md（§7.1 / §7.2 / §8 R9 / §10 D10）与 MRD.md（§9 A8 / §9.0.1）仍写着旧 D2 口径（「信任官方规格不前置 POC」「高德 MarkerCluster 10 万点 ≤80ms」）共 8 处，与 PRD 新口径已不一致；用户本次只点名评审 PRD，**是否同步修订 BRD/MRD 待确认** ——【已于 [20] 全部处理完毕】。

- [20] 2026-08-23：**三交付件口径对齐（PRD / BRD / MRD 一致性同步）**。用户要求「三个文档要保持一致」，以 `PRD.md` 为唯一权威源，把上一轮落地的 8 项核心口径向 BRD / MRD 全链路传导，并重生三份交付件。
  - **传导的 8 项核心口径**：① 技术栈（Flutter 3.41.9 / Dart 3.11.5 + 高德 Android/iOS 原生 SDK 经 `amap_map` 桥接，Dart 侧自实现网格聚合）；② D2/D10 反转为「Batch1 前置 1 天聚合性能 POC，通过后才承诺 P95≤300ms」；③ 未实名「先发后审」受限态完整规则；④ 北极星 0.6 / 0.216 为首版拍定基线（非已验证目标，第 4 周校准）+ 召回率分母生效条件（同城同二级类目在库 ≥20 条才计入分母）；⑤ 举报下架唯一口径（累计风险分 ≥60，不另设举报率阈值）；⑥ T1 冷启动三阶段降级（规则期 → LR 期 → 全量期）；⑦ S1 只埋一级联系漏斗、不追踪撮合结果；⑧ 运营后台 4 页 → 7 页 + 4 角色最小权限模型 + 审计留痕。
  - **BRD.md 14 处编辑**：§7.1 北极星段重写、§7.2 Batch 表两行、D1/D2 兜底重写、§8 风险表 R3/R4/R6/R7/R9 改写并新增 R10、§10 D9/D10 重写、§7.3 成功标准新增第 9/10/11 条、§9.1 团队表（「前端工程师」→「客户端工程师（Flutter）」）、§9.2 外部依赖细化原生 SDK 版本与 Key 绑定、§9.3 合规新增审计留痕与最小权限、§10.1 三色清单改冷启动三阶段、§3.4 边界表新增 3 行。
  - **MRD.md 13 处口径编辑 + 7 处版本痕迹清理**：A8 重写并新增 A9（标签量假设）、§9.0.1 整段重写为前置 POC 与验收口径、§8.1 北极星重写、§8.2.1 指标表（新增受限态 24h 转正率、申诉成立率两行）、§8.3 反指标「举报率」→「高风险内容占比」、§5.1 UR 表 9 行改写（UR-17 升 P0、UR-Ops-01 改后台 7 页）、§7.4 治理清单新增两条、§7.5 GTM 改写、§10 决策记录新增 M6-M9；同时删除「修订前/修订后」「该口径已作废」「原 v2.0 排除的…」等 7 处版本痕迹，改为直述规格（**遵守用户约束：不引入任何版本对比或取代注记**）。
  - **PRD.md 3 处自洽修正**（交叉核验脚本发现的内部残留）：§9.10 标题「后台 4 MVP」→「后台 7 页」、§0.2.1 D1 兜底补冷启动三阶段与分母条件、§6.12 双轨灰度「意向率」→「推送触达后的详情点击率（不追踪撮合结果）」；另 §6.14 统一表述为「只埋一级联系漏斗」。
  - **关键架构发现**：`gen_brd_pptx.py` / `gen_mrd_pptx.py` **不读取 MD，内容为硬编码文本字典**，因此改 MD 后必须同步手改脚本文案 —— 本轮共同步 **14 处硬编码槽位**（BRD 7 处 + MRD 7 处）。
  - **新增溢出审计工具 `check_fit.py`**：原 `cmp_len.py` 依赖「新旧文本长度比 1.4 倍」经验规则且只覆盖 MRD，对 BRD 模板占位符失真。新工具按**形状几何尺寸**（框宽/字宽估每行字数、框高/行高估行数）判定真实溢出，覆盖两份 PPTX。首轮报出 BRD 13 处 / MRD 25 处，与模板自身 9 处基线比对后确认 `need=2 / cap=1` 属模板设计容差，仅需处理 `need≥3` 的 6 处超容差槽位；精简文案后**两份 PPTX 的 need≥3 槽位归零**，满足「不允许文字重叠」约束。
  - **交叉核验**：10 个正向关键词 + 5 个反向禁用口径矩阵扫描三份 MD，最终 **PRD / BRD / MRD 均零 MISS、零 BAD**。
  - **三件交付件重生并校验**：`找鸭找-产品需求文档-v2.1.docx`（95,251 字节，`verify_prd_docx.py` 五段闸门零 FAIL）、`找鸭找-商业需求文档-v2.1.pptx`（4,036,569 字节 / 23 页）、`找鸭找-市场需求文档-v2.1.pptx`（3,646,062 字节 / 28 页）。
  - ⚠️ **遗留**：① 旧 `docs\PRD.docx` 被 Word 占用无法删除，需用户关闭 Word 后手动删；② 里程碑缺绝对日历日期，需用户提供项目起始日期后回填。


- [21] 2026-08-23：**Figma 原型生成路径落地（`prototype-figma/` 插件三件套）+ PRD §10.1 补两页**。用户要求「输出可导入 Figma 的原型文件」，本轮完成从技术路径核实到可执行插件代码的全链路。
  - **技术路径核实（四条替代方案全部排除，锁定唯一可行路径）**：① **REST API 不可用** —— file / node 端点为 **GET-only**，不能创建或修改节点；② **Remote MCP 不可用** —— 客户端为白名单制（Augment / Claude Code / Claude Desktop / Codex / Copilot CLI / Cursor / Factory / Firebender / VS Code / Warp / Xcode），**Trae 不在列**，自定义 OAuth 拿不到 `mcp:connect` scope 且不接受 PAT；③ **直接生成 `.fig` 不可靠** —— kiwi 二进制格式未公开，已知 4 类 bug（auto-layout 子节点写绝对坐标导致双重偏移 / 缺 `bordersTakeSpace` / text 缺 `lineHeight` 高度算 0 / `fontMetaData` 原始字体名与 `fontName.family` 规范化名不一致导致查找失败）；④ **SVG / HTML 导入有损** —— 样式不保留、**不生成 Auto Layout 与原生组件**。→ 结论：**只能走 Plugin API**（`manifest.json` + `code.js`，Figma **桌面版** Plugins → Development → Import plugin from manifest；浏览器版无法导入本地 manifest）。
  - **PRD §10.1 补两个缺页（12 页 → 14 页）**：审计发现 §5.9 已有完整的「AI 结果确认态」五行规格、T6-④ 已写「完整度卡取代单纯成功 toast」，但 §10.1 页面清单里**这两页都不存在**（PRD 内部自相矛盾）。已补入 `ai-confirm-screen`（AI 结果确认页，P0）与 `publish-success-screen`（发布完成页，P0），并在 §5.9 与 T6-④ 加**反向引用**建立双向索引。
  - **插件三件套内容**：`code.js` 约 1770 行分十一节 —— Design Token 定义 / 通用工具 / **Variables 基础设施（18 个变量，幂等 `ensureVariables`）** / 节点构造器（每个画框名带 PRD 行号回标）/ 共用外壳 + **Component 化** / 画布布局 / 批次 1 setup / 批次 2 map / 批次 3 core（14 页）/ 批次 4 modal（3 模态 + T6 四组件）/ **批次 5 flow（21 条原型跳转）** / clean。`ui.html` 定义 5 个批次按钮与日志回传协议。
  - **S4 Token 网表化落地**：13 个语义色 + 5 个分类色全部注册为 Figma **Variables** 并绑定到图层，**图层内无任何字面 HEX** —— 待设计师定稿主色后，只改变量即全画布生效。
  - **Plugin API 已规避的坑**：写文字前 `loadFontAsync` 且做 **Noto Sans SC → Inter 自动降级**（不静默，批次 1 日志明示）；颜色用 **0–1 范围**；`figma.notify()` 报 not implemented 改用 `figma.ui.postMessage`；`documentAccess: dynamic-page` 下一律走 `loadAllPagesAsync` / `setCurrentPageAsync` 异步 API；text 节点显式设 `lineHeight` 防高度算 0；大批量拆 5 批次；Dev Mode 不能编辑节点已写入错误排查提示。
  - **组件化（改一处全局同步）**：注册 **20 个 Component master**（状态栏 1 + 底部 Tab 3 + 按钮 6 + Pin 10）放于独立 `_components · master（勿删）` 容器；`instanceOf()` 带 **fallback 回退机制** —— master 未命中时退回原始构造函数，保证批次 2/3/4 脱离批次 1 单跑也不失败。
  - **批次 5 原型跳转**：`indexMainFrames()` 跨 Page 建 `pageId → Frame` 索引（自动排除「过程态 / 降级 / 半径档 / board/ / _components」等变体画框），`setReactionsAsync` 写 21 条 SMART_ANIMATE 跳转，`login-screen` 设为 Present 起点；`findClickable` 命中不到关键字时**退化为整框点击**，保证不断链。
  - **本轮三项用户决策**：① §10.1 两页**都补**；② 插件增强**三项全做**（跳转连线 + 组件化 + 回写本文档）；③ Pin 位图方案（30 张 PNG vs 运行时 Canvas）**先不定，交由 POC 测定**。
  - ⚠️ **遗留待处理**：① **整套插件从未在真实 Figma 环境跑过**，`GetDiagnostics` 只保证语法无错，运行时行为（`createComponentFromNode` 对 Auto Layout Frame 的表现、跨 Page `destinationId` 有效性、Instance 内 `resize` 与 Auto Layout 的冲突）**均待实测** —— 需用户执行批次 1 后回传日志确认字体与 Variables 状态；② **13 个语义色仍是「建议 HEX」**，主色待设计师拍板（5 个分类色已冻结）——【已于 [22] 定稿为 A 深湖青 `#0B7C8C`】。
  - 📌 **原型定位（常规工作流，非风险项）**：Figma 原型是**设计交付物与评审依据**，客户端工程师照此用 Flutter 实现，即业界标准的「设计稿 → 人工实现」协作方式。原型与代码是参考关系，不存在也不需要自动转码通道。

- [22] 2026-08-23：**品牌主色定稿「A 深湖青 `#0B7C8C`」+ 修掉一处可测量的可访问性缺陷**。
  - **发现的真实缺陷**：按 WCAG 相对亮度公式（`L = 0.2126R + 0.7152G + 0.0722B`，通道先做 sRGB 逆伽马）实算，原主色 `#1FBAD6` 与白字对比度仅 **2.32:1**，`#1799B0` 为 3.38:1，**均低于 AA 正文级 4.5:1**；而 PRD §1.4.6 主按钮规范明确写「主色填充 + 白字」，意味着全站主按钮/导航栏/胶囊按钮文字**全部不达标**。这不是审美问题，是可测量缺陷。
  - **决策方式**：新建 `prototype-figma/color-preview.html` 可视化对比页（用户明确指出「都是 RGB 数值我看不到效果」），把 4 个候选铺进 6 个真实使用场景（导航栏 / 地图当前位置点 / 分类 Tab 选中态 / 卡片选中边框 / 主次按钮 / 底部发布胶囊），地图上 5 个分类 Pin 固定用**已冻结**分类色作对照，对比度由页内 JS 按 WCAG 公式**实时计算**而非写死。用户选定 **A 深湖青**。
  - **定稿值**：Primary `#0B7C8C`（**4.91:1 过 AA**）/ Primary Dark `#075E6B` / Primary Light `#E6F6F8`。选型理由：现有湖蓝鸭色的下沉版，**品牌延续性与鸭 IP 气质不变**；青系在 5 个分类色（蓝/紫/橙/绿/粉）中为空缺，地图上零混淆风险；改动量最小。
  - **顺带修掉插件一处逻辑 bug**：`ensureVariables()` 原实现「同名变量已存在即 `reused++` 跳过」，这会导致**色板定稿后重跑批次 1 主色不生效**。已把幂等语义从「存在即跳过」改为「**值对齐**」——比对当前 mode 值（容差 0.002），不一致则 `setValueForMode` 改写，返回值增加 `updated` 计数并在批次 1 日志明示「Token 已变更，全画布绑定处自动同步」。
  - **同步落地 5 处**：① `PRD.md` §1.4.2 三行改值、标题去掉「建议值，UI 设计师最终确认」、**新增「对比度硬约束」条**（凡主色填充+白字组件必须 ≥4.5:1，后续换色必须重算，不得只凭观感替换）；② `code.js` `SEMANTIC_COLORS` 三色 + 批次 1 色板画板标题 + 日志；③ `ui.html` 插件面板自身按钮配色；④ `docs/gen_prd_v2.1_docx.py` 两处硬编码（引用块左边框、砍留改表头底色）；⑤ 本文档。已 Grep 全库确认无旧主色残留（`ideation/` 历史工件内的旧色属存档不改）。
  - ⚠️ **下一处待处理隐患（已识别未动）**：车辆分类色 `#F97316` 与 Accent 辅助色 `#FF8A3D` 两个橙**过近**，而 Accent 现同时承担「需求态」强调职责，地图上车辆 Pin 与需求态标签会互相干扰，需单独决策。→ **已于 2026-08-25 结案，见条目 [41]：不换色，改为限定 Accent 职责；且「需求态用 Accent」这一前提本身已在代码层不成立。**

- [23] 2026-08-23：**PRD Word 交付件按新主色重生 + 插件回标从「行号」改为「章节号」（修掉一处系统性脆弱点）**。
  - **重生交付件**：`找鸭找-产品需求文档-v2.1.docx`（**95,899 字节 / 815 段落 / 48 表格 / 40,294 字 / 24 条目录**），`verify_prd_docx.py` 五段闸门**零 FAIL 零 MISS**。生成脚本用 `_tmp` 中间名再改名，规避 Word 文件锁。
  - **自查发现并修掉一处我自己引入的违规**：新写的「对比度硬约束」条里用了「不得只凭观感**替换**」，而 `verify_prd_docx.py` 的版本痕迹反向校验把「替换 / 替代」列为**必须 0 次**的禁用词。已改写为「必须重算此值方可采用，不得仅凭观感定色」，闸门恢复通过。
  - **修掉系统性脆弱点：插件回标全部去行号化**。`code.js` 原用 `PRD.md:1462` 这类**绝对行号**做画框回标（共 81 处），而本轮 PRD 补两页 + 加对比度条已使全部行号偏移 4~6 行，**所有回标同时失效**。已批量改为章节号锚点（`PRD §10.1` / `PRD §1.4.2` / `PRD §6.10.1` …），并用正则反向校验确认 `PRD.md:数字` 形式**清零**。章节号仅在章节重构时才变，比行号可靠一个量级。转换脚本为一次性工具，用后即删，不留仓库垃圾。
  - **橙色冲突决策：留到 UI 设计稿阶段一并处理**（用户决定）。已实算冲突程度：车辆分类色 `#F97316`（相对亮度 0.3269）与 Accent `#FF8A3D`（0.3946）互相对比度仅 **1.11:1**，并排几乎不可分辨。真正的问题是**职责重叠** —— Accent 按 §1.4.2 同时承担「需求态」强调，而需求态按 §1.4.3 本应由「空心 + `?`」形状区分、**不靠颜色**。因此该问题有可能只需收窄 Accent 的职责描述、无需换色，交由 UI 设计稿阶段连同 IP 造型与组件库一并定论。

- [24] 2026-08-23：**交付基线确立 —— 9/30 交付目标经核实后从「商店上架」修正为「可用内测包」，并首次为全项目建立日历时间基线**。
  - **用户输入**：要求 9 月 30 日交付 app；经三问确认：交付形态原意为**应用商店公开上架**、可投入人力为**单人 + AI 辅助**、外部资质**全部为零**（要求提供前置清单并推动办理）。
  - **核实结论：9/30 商店上架在行政流程上不可达，与开发能力无关。** 依据官方来源实算：**软著审查 60 工作日**（⚠️ 此处「法定」措辞已于 [26] 更正 —— 法条原文为「自受理日起 **60 日**」，本项目取 60 工作日为**保守规划值**，日期不变，口径依据见《上架前置工作指导手册》P5），8/25 提交最早 **11/17 下证**；而华为/OPPO/vivo **强制要求软著、无软著直接驳回无例外**。加急 400 元官方明示「审核周期相同、不保证缩短」，**花钱买不到时间**。备案链另为串行（执照→ICP→APP 备案→提审），任一环补正即开 30 天窗口。
  - **同时发现一条此前未识别的主体约束**：本产品含用户发布 + 陌生人联系，安卓渠道判定为**社区/UGC 类，个人开发者一律拒审**；iOS 亦要求 UGC 必须企业账号。→ 追问后确认**用户已有现成执照**，此项已解，并省下约 5 工作日。
  - **用户决策**：① 交付形态改为 **9/30 内测包**（蒲公英/TestFlight，真实后端），商店上架顺延至软著下证（约 11/25）；② 主体沿用已有执照。**未采纳「只冲 iOS 单渠道」**——虽 iOS 不强制软著，但备案链仍需 9/18 前走完且零缓冲，安卓仍须等软著，风险不对称。
  - **新增三节文档**：§1.4 行政约束（三张周期表 + 三条硬约束 + iOS 例外分析）；§2.6 **上架前置清单 P1-P10**（分四步：本周立办 / 软著最先提 / ICP 后 / 上架前后，逐项含周期与驳回雷点）；§2.7 **范围裁剪表**。里程碑从 6 条 TBD 改写为 **11 条带日历日期**（M2.5 前置启动 8/24、M2.6 软著提交 8/25、M4 闭环 9/11、M6 内测包 9/30、M7 上架 11/25）。
  - **§2.7 范围裁剪（这是让「绝不延期」在数学上成立的前提）**：9/30 仅 **28 工作日**，而 BRD §7.2 现行规划为 **6 个月三批**、Batch1 单独占「月 1-2」，全量约 **90-110 人日** —— 单人不可能完成。故裁为：**必做** = 五大 P0 闭环 + 完整度三档 + 聚合 POC；**降级** = 我的模块（无双向推送）；**本期不做** = 运营后台 7 页、LR+GBDT（用阶段 0 规则加权）、AI 发布 3B 模型（保留 `ai-confirm-screen` 骨架不接模型）、动态蜂窝（用固定四档）。并写入**二次裁剪触发条件**：9/11 核心闭环未通则砍「我的模块」与实名认证改 Mock，**不压缩自测时间**。
  - 📌 **意外收获**：查合规时发现 —— 只要出现会员充值/付费/打赏/广告/商家入驻**任一项**，五大安卓商店即强制 ICP 经营许可证（注册资本 ≥100 万 + 3 人社保）。本产品 Scope 早已永久排除支付与商业化，**恰好规避此项**。这条原为产品红线，现同时成为**排期护城河**，已在 §2.6 明确标注「不可为增收而破」。
  - ⚠️ **另识别一条潜在风险**：Batch2 的 LR+GBDT 属「个性化推荐算法」，可能触发《互联网信息服务算法备案》。内测期用阶段 0 规则加权**不触发**，上架后若开模型推荐须先评估。
  - **待用户执行（P1-P4，本周内）**：核对执照经营范围 → 购域名+境内服务器（约 131 元/年）→ **锁死 App 名称**（软著名=备案名=商店名=桌面名，一字不差，改名等于全流程重做）→ 提交 ICP 备案。随后 8/25 立即提交软著（**不需 app 做完**，仅需 60 页源码 + ≥10 页说明书）。

- [25] 2026-08-23：**三件套文档与三份交付件全部对齐 9/30 新基线（消除 [24] 遗留的口径冲突）**。
  - **起因**：[24] 建立新基线后，BRD §7.2 仍写「6 个月三批、Batch1 占月 1-2、含运营后台」，与新基线矛盾。已评审文档不擅改，故先征询用户 → 用户选择**修订 BRD §7.2**（而非仅在说明文档标注差异），理由是避免后续按 BRD 排期的人被误导。
  - **BRD.md 改 7 处**：①§7.2 相对月份 → **绝对日期锚点**，并在 Batch1 与 Batch2 之间插入 **10/01–11/25「上架合规窗口」**，Batch2/Batch3 相应后移约 2 个月（Batch2 = 11/26–2027/01/31、Batch3 = 2027/02–03、M3 联动 = 2027/04）；②新增 **B6-v2.2 裁剪说明** —— S4 运营治理后台 7 页移出 Batch1（移入合规窗口），Batch1 补入**五大 P0 闭环为必做核心**，并写入二次裁剪触发条件；③§7.1 D1 兜底两处月份改绝对日期；④§7.3 新增「验收节点归属」（第 1-7/9-10 项挂 9/30 内测包、第 0/11 项挂 11/25 上架、第 8 项挂 2027/04）；⑤§9.1 新增实际人力说明（**Batch1 = 1 人 + AI 辅助**，表中五人配置为理想态非本期投入）；⑥§9.3 新增「上架资质前置项」四行周期表 + 主体资格红线 + ICP 经营许可证规避 + 隐私政策自有域名 + App 名称一致性；⑦§10 新增 **D12**（9/30 交付形态）与 **D13**（Batch1 范围裁剪）两条决策，附完整决策依据。
  - **连带发现的一致性缺口（本轮实际价值高于 BRD 本身）**：Grep 全 docs 目录后发现 **PRD.md 3 处 + MRD.md 5 处**同样残留相对月份。其中 MRD §7.5 三批 GTM 时间窗尤其危险 —— 若照原表在 Batch1 期做公开传播，而实际 9/30 只有内测包、商店上架要等 11/25，等于**对外承诺一个不存在的可下载产品**。已改为绝对日期并明确标注「Batch1 传播限内测种子圈层，公开渠道投放最早自 11-25 起」，同时新增「上架合规窗口 GTM」行。
  - **交付件重生（严格按 `docs/solutions/workflow-issues/multi-format-deliverable-consistency.md` 的教训执行）**：BRD.pptx / MRD.pptx 是**硬编码型不读 MD**，故直接改 `gen_brd_pptx.py` slide 20 路线图 7 槽 + slide 22 资源依赖 3 槽（含「五人核心团队」→「首批为单人加 AI 辅助」、合规要点改为「软著 60 工作日、ICP 与 APP 备案、企业主体」）；MRD.pptx 的 GTM 卡片原文不含月份，无需改脚本。PRD.docx 为 MD 驱动型，改 MD 后直接重生。
  - **三道验证闸门全过**：①`check_fit.py` 溢出审计 **risky=8**，低于模板固有基线 9，S20 零溢出（S22 三处 need=2/cap=1 属模板设计容差，门槛 need≥3）；②新建 `verify_brd_v22.py` 做 **25 项正反双向校验**（9 项 pptx 新句必须命中 + 5 项旧月份口径必须清零 + 11 项 BRD.md 新句必须命中），**defects=0**；③`verify_prd_docx.py` 五段闸门**零 FAIL 零 MISS**，并单独确认 docx 内「Batch1（2026-08-24 → 09-30）」命中、「Batch1（月 1-2）」已清零。
  - 📌 **本轮方法论沉淀**：口径变更必须做**全库 Grep 收口**而非只改被点名的文件。用户只指出 BRD 冲突，但同一口径实际散落在 3 份 MD + 3 份交付件共 8 处；若只改 BRD，剩余 7 处会继续误导，且下次发现时成本更高。反向校验（旧口径必须清零）与正向校验同等重要 —— 只查新句命中，改漏的旧句照样躺在交付件里。

- [26] 2026-08-23：**新建独立交付物《上架前置工作指导手册》—— 把 §2.6 从「概览清单」升级为「可照着办的操作手册」**。
  - **用户输入**：「一些前置工作需要单独生成一个清单，并提供指导说明」。判读为两层要求：① 抽成**独立文档**（§2.6 混在说明文档里，实际执行时要在一份 340 行的项目文档里翻找，不可用）；② 不只列「做什么」，要补「**怎么做**」。故没有把 §2.6 复制一份了事 —— 原文只有「事项 / 周期 / 关键要求 / 状态」四列，缺办理入口、缺操作顺序、缺材料格式指标，拿着无法开始办。
  - **新增 `docs/上架前置工作指导手册.md`**：P1–P10 每项固定六段结构 —— **为什么必须做 / 办理入口（含 URL）/ 前置依赖 / 操作步骤 / 材料硬指标 / 驳回雷点**，另加：
    - **关键路径全景 ASCII 图**：一眼看出「P5 软著不依赖 app 做完却卡死上架」「P4→P7 串行且 P7 还依赖可出包」「P1/P2/P3 是两条支线的共同前提」三条结论；
    - **P3 名称与标识冻结区**（表格填完不改）：App 展示名 / 软著软件全称 / 版本号格式 / 包名 / 主域名 五项。软著全称按官方规范给出建议值 `找鸭找本地供需信息共享平台软件 V1.0`（结构 = 品牌 + 核心功能 + 软件性质 + 版本号，以「软件/系统/平台」结尾，纯「找鸭找」过于宽泛易补正）；
    - **P5 软著三重一致性自检表**（申请表 / 源码页眉 / 说明书三处的软件全称、版本号、功能项逐条打勾）—— 这是官方与实务反馈的**最高频补正点**，且版本号**禁止混用 `V1.0` / `v1.0` / `Ver1.0`**；
    - **P5 材料三张硬指标表**：源码（前后各连续 30 页、每页有效代码 ≥50 行且空行注释不计、页眉须打软件全称+版本号、**严禁截图**因审查需提取文本做 AI 比对、须清理第三方原始版权声明）、说明书（≥10 页图文结合、截图内软件名版本须统一、须覆盖五大 P0 闭环）、功能描述（**500–1300 字**、写背景/架构/模块/技术/场景、杜绝营销话术，并给出可直接照填的 800 字五段结构模板）；
    - **P7 备案三要素取值方法**：安卓填**证书 MD5**、iOS/HarmonyOS 填 **SHA-1**（均 16 进制），附 `keytool -list -v` 命令与华为 AGC「删根证书与中间证书仅留服务器证书」的取法；并明确 **iOS 与安卓属不同运行平台需分别备案**；
    - **P8 六渠道对照表** + 一条容易被忽略的卡点：**Apple 组织账号需先申请 D-U-N-S 编号（免费但可能 1–2 周）**，不提前申请会被动卡住 iOS。
  - **核实并定论一处此前的数据分歧（本轮最关键的一件事）**：我在 [24][25] 及 BRD 中一律写「软著**法定 60 工作日**」，但本轮查证发现**法条与官方原文是「60 日」**——《计算机软件著作权登记办法》第二十条与中国版权保护中心官网《办理时限》《办理步骤》均为「自受理日起 60 日内审查完成」；而国家政务服务平台同一事项列「法定办结 60 工作日」，两者相差约 25 天，直接影响 11/17 下证与 11/25 上架两个日期。
    - **处置：不改日期，改口径表述。** 理由是 60 自然日只是「审查段」，其前还有**待受理 1–3 工作日 + 受理中 3–5 工作日**、其后还有**待发证 5–10 工作日**，业内实操反馈**从提交到拿证普遍 2–3 个月**，材料吃补正则 3–4 个月。两种口径叠加前后段后落在同一量级，故**统一取 60 工作日为保守规划值**：若实际提前下证则上架窗口自然宽松，反之不会让里程碑崩塌。
    - 已在手册 P5 写入完整口径说明框（含两个来源与取值理由），并回改说明文档 §1.4 周期表、§1.4 硬约束第 1 条、§2.6 P5 行、M2.6 里程碑共 4 处措辞为「保守 60 工作日」。**并未因法条字面是「60 日」而把上架日提前** —— 这是刻意的保守选择，不是疏漏。
  - **顺带确认两项官方数据**：软著官费 **0 元**（财税[2017]20 号，自 2017-04-01 起停征软件著作权登记费；国家政务服务平台该事项「是否收费：否」），故手册中「加急 400 元」只可能是代理服务费而非官费；2026 年起审查升级为「**AI 辅助 + 人工复核的有限实质审查**」，套模板已不易过关，这也是必须认真做三份鉴别材料的原因。
  - **§2.6 保留为概览并加互索引**：本节答「做什么/多久/卡谁」，手册答「具体怎么做」，同编号对应且**勾选状态以手册为准**（避免两处状态打架）。§1.2 产出物清单同步新增一行。
  - 📌 **本轮判断记录**：用户说「生成一个清单」时，直接产出一份新清单是最省事的做法，但那样交付的仍是「已经有的东西换个文件名」。用户真正缺的是「拿着它今天就能开始办」，所以本轮 90% 的工作量在**查官方入口与材料格式硬指标**，而不在排版。凡未查到官方来源的费用与耗时，一律显式标注「经验值」，不与法定周期混写。

- [27] 2026-08-23：**插件两轮修复 —— ① 批次 5 跳转大面积失败（根因：Figma 原型不支持跨 Page）；② 地图页视觉与信息密度返工**。

  **第一轮：跨 Page 跳转（批次 5 报一批 `(undefined)`）**
  - **现象**：用户回传批次 5 日志，21 条跳转里 11 条失败，失败原因栏全是 `(undefined)`。
  - **双层根因**：
    1. **业务层（硬限制）**：`setReactionsAsync` 的 `NODE` 型 action，其 `destinationId` **必须与触发节点位于同一 PageNode**。经 WebSearch 核实 Figma 官方论坛多条长期 feature request 确认「prototype actions cannot target frames outside the current page」，官方给的变通做法也是「临时把 frame 挪到同页连好再挪回」。而插件原设计把内容拆成「01 地图与列表 / 02 核心流程 / 03 模态」三个 Page，跨页的 11 条自然全连不上；
    2. **表现层（掩盖真相）**：`catch (e)` 里直接读 `e.message`。Figma 抛出的**未必是标准 Error 对象**，于是每条失败都显示 `(undefined)`，把真实原因全部吞掉。
  - **处置**（经询问用户，选定「并到一页 + Section 分区」）：
    - `PAGE_NAMES` 三页合一为 `01 · 原型主页面`，页内新增 `SECTION_NAMES` / `SECTION_Y` 用 **SectionNode** 做视觉分区 —— 满足「同页可跳转」与「分组语义不丢」两个条件（Section API 经 WebFetch 官方文档核实：`figma.createSection()` 创建、`resizeWithoutConstraints()` 改尺寸，`width`/`height` 本身 readonly）；
    - 新增 `resetSection()` 替代三处 `resetPage()`；`layout()` 支持 Section 宿主，并**先 appendChild 再设 x/y**（Section 内子节点是相对坐标，顺序颠倒会整体偏移）、按实际行列数 `resizeWithoutConstraints` 撑开；
    - 新增 `pageOf()` 沿 parent 链上溯取 Page（画框现在挂在 Section 下，原先直接判 `parent.type === 'PAGE'` 会永远为假）；`indexMainFrames()` 下探一层 Section 收集画框；
    - `batchFlow()` 加**同页守卫**（跨页时给出可读原因而非让底层抛无 message 异常）+ `e.message` 兜底转 `String(e)`；
    - `batchClean()` 增加**删除旧版三个 Page** 的逻辑 —— 这是关键：旧页画框会被索引扫到且与新页**重名**，跳转会连到旧页那一份而全判跨页失败。删除前先 `setCurrentPageAsync` 切走（Figma 要求文档至少留一页）。
  - ⚠️ **已连带影响用户操作步骤**：结构变了，**必须先跑批次 6（清空）再从批次 1 整体重跑**，不能只重跑 2/3/4。

  **第二轮：地图页返工（用户提出两点）**
  - **用户输入**：「① 地图尽量扩大空间，距离范围和五大类可以考虑悬浮图层之上；② 地图上的圆圈图标不够美观，使用类型图标+颜色渲染会不会更好一点」。
  - **核对结论：两条都不是改需求，而是补漏实现**。回查 PRD §6.4.2 原文，表格里明明写的是「● `#3B82F6` **+ 💼 图标**」，而 `pinRaw()` 只画了纯色圆、**把图标漏掉了**；§6.4.1 的 ASCII 图里也有图例块，同样没实现。用户的观察准确。
  - **地图空间**：原实现把范围条与分类胶囊各堆成一行实体，地图只剩 480px（占 844 屏高 **57%**）。改为悬浮后 = 844 − 状态栏 44 − 导航栏 48 − 底部 Tab 64 = **688px（81%）**。
    - 技术前提：原 `box()` 一律走 Auto Layout，子节点被自动排流，**根本无法重叠**。故新增 `stack()`（`layoutMode = 'NONE'`）作为唯一能实现悬浮的容器类型；
    - `mapFilterBar()` 删除，拆为 `mapOverlayTop()`（范围档 + 供需胶囊）、`mapOverlayCats()`（五类图标胶囊）、`mapLegend()`（图例）三件，全部由 `mapCanvas()` 绝对定位叠加；控件**分置屏幕上下两端**，中段整片留给地图；
    - 连带处理两处：① 地图拉满后 screen 的 Auto Layout **已无余高放实体标注卡**（44+48+688+64=844 正好占满），故 13 个变体画框的 `annotation` 改为传入 `mapCanvas` 第三参并叠加显示；② 列表页无地图、控件无处可浮，新增 `listFilterBar()` **复用同两个控件本体**套白底外壳当实体行，两页筛选视觉保持一致。
  - **Marker 图标**：新增 `rect()` + `catIcon()`，用基础矩形拼出五类单色图形（工作＝手提箱／房屋＝屋顶+门洞／车辆＝车身+双轮／生活＝叶片／服务＝服务铃）。
    - **为什么不直接用 PRD 字面写的 emoji**：emoji 由系统字体渲染，iOS/Android/鸿蒙字形差异明显；40×40 Marker 内图标实占约 20×20，该尺寸下 emoji 细节糊化；且 emoji 自带固有配色，**无法跟随分类色变色**，而需求态要求图标取分类色。改用矩形拼装后形状语义与 emoji 一致、颜色可控，开发阶段可原样替换为同形状 SVG；
    - `pinRaw()` 重写：资源态＝分类色实心圆 + 白图标；需求态＝白底 + 分类色描边 + 分类色图标。**"?" 从圆心移到右上角 16×16 角标** —— 原位置会与图标争位，40×40 内两者叠加不可读；改为图标居中承载「是什么类」、? 作角标承载「这是需求」。完整度角标相应移到右下角避让；
    - `mapLegend()` 的实心/空心用 `box()` 画的真实圆点而非 `●`／`○` 字符（字符差异依赖字体渲染，三端不一致）。
  - **文档回写**：PRD §6.4.1 ASCII 图重绘为悬浮层结构并新增「布局口径」小节（含 688px 算式与「为什么不用实体行」的判据）；§6.4.2 新增「图标实现形式」小节，明确 **emoji 仅为语义占位、交付须换单色 SVG**，并写全形状语义、配色规则、? 与完整度角标的新位置。
  - ✅ 两轮改动后 `node --check` 与 IDE 诊断均零错误。

- [28] 2026-08-23：**地图页第三轮返工 —— ① 悬浮层反过来遮挡地图，改为默认收起 + 三级树弹层；② 工作/生活/服务三个图标重设计为矢量路径**。

  **用户输入**：「① 悬浮之后存在地图遮挡，可以考虑折叠，用户选择完成之后可以收起；② 工作、生活、服务图标不够形象需要重新设计」。追加澄清：「都需要收起，不局限折叠能否有更好的方式，而且分类可以下钻到第三层」，home-screen 主态**默认收起**。

  **问题一：悬浮层遮挡（承认 [27] 第二轮改过头了）**
  - **量化根因**：[27] 把范围条、供需胶囊、五大类栏、图例四件**全部常驻**悬浮，竖向合计吃掉约 200px（顶部卡 90 + 分类栏 56 + 图例 50），688px 地图的**净可视区被压回约 490px** —— 与「扩大地图空间」的初衷相互抵消，用户的观察准确。
  - **核对结论：不需要发明新载体，PRD 已有既定方案，是插件漏实现**。用户说「不局限折叠能否有更好的方式」，答案就在 PRD 内：
    - §6.13 T6-② 原文「图层控制面板 = 地图右上角三个竖点展开，分类三级树勾选」→ 入口已定；
    - §6.9「T3 分类三级树图层弹层」已定义三层结构（5 大类顶栏 → 二级多选框 → 三级精筛，勾选任意级都立即加载）→ 载体与层级已定；
    - §1.4.8 已给动效（弹窗/抽屉 = 底部上滑 240ms spring；遮罩渐显 180ms）；
    - §2.4 已给完整三级树（5 大类 × 2-5 二级 × 2-6 三级，≤60 叶子）。
  - **为什么选弹层而非继续折叠横条**：三级最深一层有 5-6 个类目，三层横条叠起来约 170px，会把地图吃掉四分之一还多，遮挡只会加剧。弹层是「按需占屏、用完即走」，不选时零占用。
  - **为什么收起态仍留一行摘要胶囊、而不收干净**：控件一收，用户即失去「我现在筛的是什么」的反馈，会反复展开确认，操作成本反而上升。故收起 ≠ 隐藏，而是压缩为一行只读摘要（范围+供需+分类三项，约 44px），点它即展开。这是「收起」与「可用」的唯一交点。
  - **实现**：
    - `mapCanvas()` 新增第四参 `opt`（`collapsed` 默认 true / `sheet` 叠弹层 / `legend` 按需出图例）。**收起态常驻件从约 200px 降到约 44px**：仅右上角三竖点入口 44×44 + 左上角摘要胶囊；
    - 新增 `fabButton()`（44×44 圆按钮，44 为 iOS HIG 最小可点区域；横条无论多薄都会横贯整屏切断视野，圆按钮只吃一个角，遮挡最小）、`filterSummaryChip()`（只读摘要）；
    - 新增 `CAT_TREE` 常量（照抄 §2.4 全部二级/三级类目）、`catTreeSheet(catKey, depth, openIdx)`（三层弹层，含把手 + 标题「收起」+ 底部「重置 / 确定并收起」三条退出口）、`checkRow()`（18×18 方框 + 矢量勾，**不用 `☑️`／`☐` 字符** —— 三端时而渲染彩色 emoji 时而黑白符号，尺寸基线均不稳定）；
    - 图例改为按需：右上「?」按钮唤起。判据是图例属**一次性认知**（实心=资源、空心+?=需求），学会后无需每次复读，常驻是净损失；
    - `batchMap()` 新增 5 个画框：筛选展开态 ×1、图例唤起态 ×1、T3 三级树第 1/2/3 层 ×3；主态改为收起态并加标注。批次 2 产出从 15 → 20 个画框。

  **问题二：三个图标重设计**
  - **根因（为什么恰好是这三个）**：[27] 用基础矩形拼装图标，而矩形只能表达直线与圆角。用户点名的三类语义主体**全是曲线** —— 工作的手提箱提手是**拱弧**（原实现退化为一根窄横条）、生活的叶片是**两端尖角**（原实现退化为一角圆角的方块）、服务的铃罩是**半圆**（原实现退化为圆角矩形）。未点名的房屋（屋檐/墙/门洞）与车辆（车身/车顶/双轮）以直线为主，矩形拼装可辨识，故不动。
  - **技术核实**：WebFetch Figma 官方 API 文档确认 `figma.createNodeFromSvg(svg)` 存在且「等同编辑器内 SVG 导入」；WebSearch 核实其已知限制为剥离 `<defs>`／渐变／滤镜／蒙版／动画 —— 本方案只喂**单色 fill path**（含 `fill-rule="evenodd"` 挖洞），完全不触碰被剥离的特性，故不受影响。
  - **实现**：新增 `ICON_PATHS`（20×20 viewBox 路径表）+ `svgIcon()`。染色**不写死在 SVG 里**而是导入后逐个矢量子节点改绑颜色变量 —— 同一份路径要同时服务资源态（图标取白）与需求态（图标取分类色）。缩放用 `rescale()` 而非 `resize()`（后者只改外框、不缩内部路径，会导致 16px 框里塞 20px 图形被裁）。`catIcon()` 改为双路分发：三类曲线走 SVG，房屋/车辆沿用矩形，同时删掉 if 链中已不可达的三个旧分支。
  - 顺带把「勾选标记」与「三竖点」也纳入 `ICON_PATHS`，理由同上（折线与三点圆同属矩形拼不出的形状）。

  **发现并处置一处 PRD 内部不一致**（派 search subagent 全文核对 1580 行 PRD 时发现）：
  - §6.9 交互示例原写「房屋 → ☑️整租 ☑️合租 ☐商铺」「合租 → ☑️主卧 ☐次卧 ☐隔断」，但 §2.4 房屋二级实为「整租/合租、求租/找房、短租/日租、租房信息咨询」、三级为「主卧出租、次卧出租、整租出租」，**并无「商铺」「隔断」二项**；
  - **处置判据**：§2.4 是分类体系定义源（§2.3 已定层级规则与 ≤60 叶子上限，且 §2.6 三处页面均引用它），§6.9 只是举例说明交互层级。故**以 §2.4 为准**，已修正 §6.9 示例文案并在原处留明修正记录。画 T3 弹层必须选定一套类目，此项不澄清则代码无从落笔。

  **文档回写**：PRD §6.4.1 新增「收起口径」小节（四态占用对照表 + 为何留摘要/为何图例不常驻/为何用圆按钮三条判据，并明写取代 [27] 的「四组控件常驻悬浮」）；§6.4.2 图标形状语义细化并新增「必须用矢量路径而非矩形拼装」条；§6.9 补入口/载体/复选框实现/三条退出口，并校正类目示例。
  - ✅ `node --check` 退出码 0，IDE 诊断零错误。

- [29] 2026-08-24：**修复回归缺陷 —— 批次 5 报「重名画框 home-screen ×6」**。

  **用户输入**（实机运行插件后回传的告警原文）：「⚠️ 检测到重名画框：· home-screen ×6。重名会导致跳转可能连到错误的那一份，建议重新执行对应批次（重跑会自动清空旧内容）。」

  **根因（这是 [28] 轮我自己引入的缺陷，须记明）**：`indexMainFrames()` 内的 `collect()` 用**中文关键词黑名单**判定变体画框 —— 只认「过程态 / 降级 / 半径档 / board/ / _components」五项。而 [28] 轮我新增的 5 个 home-screen 变体（筛选展开态 ×1、图例唤起态 ×1、T3 三级树第 1/2/3 层 ×3）标题里**一个关键词都不含**，全部被当成主态收录，计数 = 主态 1 + 漏网 5 = **6**，与告警数字精确吻合。后果是 `index['home-screen']` 只留首个（恰为真主态，故本次跳转未真错），但计数机制已失真，且下次新增变体若排在主态之前就会真连错。

  **为什么不采用「只认 PRD §10.1 为主态」的方案**：Grep 核对全部 23 处 `screen()` 调用后否决 —— `ai-confirm-screen` 引用 §5.9、`publish-success-screen` 引用 §6.13，二者都是唯一主态；若只认 §10.1，这两页会被误排除出跳转索引，反而制造新的丢目标缺陷。

  **修复（改为显式标记，从机制上消除同类漏同步）**：
  - 新增常量 `VARIANT_TAG = '⟨变体⟩'`；`screen()` 增加第四参 `isVariant`，传 true 时画框名追加该后缀；
  - `collect()` 判据改为「名字含 `VARIANT_TAG` 即跳过」，**彻底删除三个中文关键词黑名单**；
  - 8 处变体调用统一补 `true`：筛选展开态、图例唤起态、T3 三级树（循环 ×3）、过程态（循环 ×6）、降级态（循环 ×3）、半径档（循环 ×4）。**旧的三类变体也一并迁移**，不留双轨判定。
  - **判据**：黑名单是「事后追认」，新增变体时必须回来同步，漏一次就复发（本次即如此）；标记是「事前声明」，新增变体只需在建画框处传一个 true，与索引函数解耦。
  - ✅ `node --check` 退出码 0，IDE 诊断零错误。
  - **须重跑**：修复只改画框命名与索引逻辑，画框内容不变，但画框名已变（变体多了后缀），故须**跑批次 6 清空 → 再按 1→2→3→4→5 整体重跑**，否则文档里残留的旧名变体仍会被算作主态。

- [30] 2026-08-24：**用 MCP 实机评审 Figma 原型，查出「跳转绑在错误节点」的隐性缺陷并修复**。

  **用户要求**：「通过 mcp 查阅一下 figma 原型图，评审一下是否达到铲平需求目标」。

  **页面覆盖度评审结论（已通过）**：`get_canvas_overview` 实测顶层画框 36 个 = 17 主态 + 18 变体（全带 `⟨变体⟩`）+ 1 组件板；逐项对照 PRD §10.1 页面清单 17 项，**17/17 全覆盖，无缺失、无多余、无编造**。`⟨变体⟩` 标记生效，不带后缀的 `home-screen` 只剩 1 个，[29] 的重名修复实机确认有效。

  **我在本轮先犯了一次评审错误，须记明**：我曾判定「cert-modal 去实名链路断了」，依据是 `export_interactions` 返回全空 + `find_nodes` 搜「去实名」0 命中。后用 `list_reactions` 逐节点复查，`I268:13124;268:10176`（按钮 Instance 内部 TEXT）**确有** `ON_CLICK → trust-screen`，链路是通的，结论已作废。教训：**MCP 工具有四个盲区，不能凭单一工具的空结果判定断链**——① `export_interactions` 不下探 Instance 与深层子节点，对 17 个主态屏一律返回空；② `validate_prototype` 把每个内部零件 Frame 都当页面判 unreachable，数百条无效 error 淹没有效信息；③ `get_prototype_flow` 单页零件太多必被截断；④ `find_nodes` 只搜节点名、**不搜 TEXT.characters**。可信手段只有 `list_reactions` 逐节点交叉验证。

  **真实缺陷（取证闭环后确认）**：`268:12697` 是 publish-screen 导航栏标题的文本节点（路径 `_nav-bar > _nav-left > 发布`），它身上绑着 `ON_CLICK → cert-modal`；而底部真正的「发布」主按钮没有任何跳转。即**链路没丢，是绑错了节点**——点标题会弹认证浮层，点主按钮无反应。这类缺陷表面看「连线成功 20/20」，实际交互错位，最难发现。

  **根因链（三层，逐层都要修）**：
  1. `findClickable()` 用 `indexOf` 模糊匹配 + `findOne` 深度优先 → publish-screen 内含「发布」的节点有 5 个以上，导航栏标题位置最靠前，抢先命中；
  2. `text()` 创建的 TEXT 节点名默认等于文案，导航栏标题「发布」与按钮内文本「发布」**在节点名上完全同名**，名字空间没有隔离；
  3. 批次 5 主循环写了「定位不到就退化为整框点击，保证流程不断链」，把定位失败伪装成成功 —— **这是掩盖问题的元凶**，让错绑长期不可见。

  **修复（四处）**：
  - `navBar()`：标题包一层 `_nav-title/<文案>`（语义上不是可点入口，永不作触发点），右侧动作包一层 `_nav-action/<文案>`（可作触发点），两者名字空间彻底隔离；
  - `findClickable()`：改为**节点名精确等值 + 唯一性校验**，命中 0 个或 >1 个都抛错，绝不再"猜"一个；
  - `FLOW_LINKS` 第二列全部由关键字改为**完整节点名**（20 条逐条核对源码命名规则：按钮 `btn/<variant>/<文案>`、列表行 `row/<标签>`、卡片 `card/<标题>`、导航动作 `_nav-action/<文案>`、底部 Tab `_tab-<名>`、级联项 `_item/L<层>/<文案>`）；
  - 批次 5 主循环：**删除「退化为整框」兜底**，定位与连线一并纳入 try，定位失败直接进 `skipped` 明确报错。
  - 配套：`buildCategorySelector()` 的级联项由三列同名 `_item` 改为 `_item/L<层>/<文案>`，否则「帮厨」无法唯一定位；`button()` 的 Instance 补 `inst.name = 'btn/<variant>/<label>'`，与 `buttonRaw()` 命名统一（Instance 默认继承 master 名 `ui/button/xxx`，同页多按钮全同名）。
  - **判据**：宁可让批次 5 报「未找到节点 X」让人立刻去修，也不要它悄悄绑到错误节点上显示成功。**失败必须可见**。
  - ✅ `node --check` 退出码 0，IDE 诊断零错误。
  - **须重跑**：节点命名结构已变（导航栏多了两层容器、级联项改名），旧画框内的节点名与新 `FLOW_LINKS` 对不上，故须**跑批次 6 清空 → 再按 1→2→3→4→5 整体重跑**，之后我会用 `list_reactions` 逐条复验 20 条连线的触发节点是否都落在语义正确的元素上。

  **重跑后复验结论（2026-08-24 完成，全部通过）**：用 `list_reactions` 对 `FLOW_LINKS` 21 条连线**逐节点实测**（不看批次 5 的日志数字，只认单节点实证），**21/21 全部落在语义正确的元素上**，transition 统一为 `SMART_ANIMATE / EASE_OUT / 0.3s`、`resetScrollPosition: true`。关键确认项：
  - `btn/primary/发布`（`272:16218`）→ cert-modal `272:16621` ✅ —— 本轮核心缺陷已修复，跳转从导航栏标题移回底部主按钮；
  - `_nav-title/*` 名下节点全部无 reaction ✅ —— 名字空间隔离生效；
  - `btn/` 前缀按钮 Instance 共 18 个，**全部自带文案名**（`inst.name` 改动生效）；`_nav-action/` 33 个、`row/` 15 个、`_item/L3/` 4 个，同框内均唯一；
  - 底部 Tab 触发点为 Instance 内部子节点（ID 形如 `I272:14055;272:13587`），`_tab-发布` → publish-screen `272:16173` ✅、`_tab-我的` → profile-screen `272:16331` ✅；
  - 起始画框唯一：`272:16108` login-screen `isStartFrame: true` ✅。

  **本轮沉淀的方法论（后续原型评审一律照此执行）**：① 判定连线只用 `list_reactions` 逐节点验，`export_interactions` / `validate_prototype` / `get_prototype_flow` 的输出仅作线索不作结论；② 「连线数 20/20」不等于「连对了」，必须验落点节点名的语义；③ 生成器里任何"找不到就兜底"的写法都要删掉，失败必须可见。

- [31] 2026-08-24：**分类图标换 Material Symbols 官方路径 + 地图底图改可换图占位**。

  **用户提的两个问题**：① 「地图图标设计这块还是不够美观，无法做到见图知意」；② 「地图需要 google 地图背景」。

  **问题①的三层根因（按影响大小排序）**：
  1. **语义映射本身就是错的（决定性）** —— 生活类范围是「二手/借物/互助」，此前画叶片 🌱，叶片指向植物与环保；服务类范围是「家政/维修/教学」，此前画服务铃 🛎️，铃指向酒店前台呼叫。符号选错时画得再精细也认不出，这是「无法见图知意」的首要成因，优先级高于视觉精细度；
  2. **手写路径在真实尺寸下必然失真** —— Marker 40px、图标仅占 20px，手写的 1.2px 叶脉、1.4px 箱缝在 20px 下不足 1 个逻辑像素，渲染即消失或糊成灰边；
  3. **40×40 内塞了 4 条信息** —— 圆底色=分类、中心图标=分类、右上 ?=供需、右下点=完整度。分类被重复编码两次，完整度在缩略态几乎无人细看，纯在抢辨识带宽。

  **问题②的纠偏（未按原样照做，已与用户确认）**：Google 地图**不可用**，三条硬理由：① Google Maps 未取得中国境内测绘资质，`maps.googleapis.com` 境内被屏蔽；② 国内上架涉地图功能须提供《互联网地图服务资质》或第三方地图 API 授权证明，用未授权境外地图数据是常见驳回原因；③ 与 PRD §6.7 已定的高德原生 SDK 选型冲突。但用户的**真实痛点成立** —— 地图区当时是一块纯色，完全不像地图，Pin 疏密与区域肌理无从判断。故转为「高德静态图截图填充」，用户已拍板。

  **改动清单**：
  - `ICON_PATHS` 重构为 `{vb, d}` 结构，五类全换 Material Symbols filled 官方路径（Apache 2.0）：工作 `work` 手提箱 / 房屋 `home` 屋形 / 车辆 `directions_car` 车身双轮 / 生活 `swap_horiz` 左右互换箭头 / 服务 `build` 扳手。路径**全部联网从 `fonts.gstatic.com` 实取，零编造**；
  - `svgIcon()` 改收对象并按图标自带 viewBox 缩放 —— Material Symbols 的 viewBox 是 `0 -960 960 960`（Y 轴负区间），若按 20 网格解析会整体跑出画布。缩放必须用 `rescale()` 而非 `resize()`（后者只改外框不缩内部路径）；
  - `catIcon()` 删掉「房屋/车辆用矩形拼装」分支与「取不到就兜底成方块」的静默兜底 —— 矩形只能表达直线与圆角，车头弧线与屋脊斜面拼不出来；兜底会让错误分类悄悄上屏，改为直接抛错；
  - `pinRaw()` / `pin()` 加 `showCompleteness` 开关，默认 false。**地图 Marker 一律不带完整度角标**，仅 T6-③ 与批次 1 规格板显式传 true 作规范演示；
  - 新增 `markerInfoCard()` + home-screen「点击 Marker 信息卡态」画框 —— 完整度从 Marker 卸下后必须有新落点，否则信息就是丢了。卡内以「色点 + 文字（信息完整/部分字段缺失/关键字段缺失）」明示，可读性远高于 1px 角标；
  - 新增 `mapBasePlate()`，`mapCanvas()` 与 `buildMapSelector()` 的纯色块改为名为 `_map-bg [替换为高德截图]` 的矩形。**必须是 RectangleNode 而非 Frame** —— Frame 的图片填充会被子节点整体遮住；`buildMapSelector()` 顺带由 Auto Layout 改 `stack()`，否则底板会被当兄弟节点挤成一行；
  - T6 新增 ⑤「20px 真实尺寸对照条」：五类 × 资源/需求共 10 个 Marker 按 1:1 并排。**验收判据是屏幕缩放 100%、臂展距离能逐个说出是哪一类**，说不出即回去换图形而非放大检视；附去色校验（Saturation −100）验证形状本身承载语义、没全靠颜色硬撑；
  - `ui.html` 增「贴真实地图底图」四步操作说明（含不用 Google 的理由、须保留高德水印）；PRD §6.4.1 补「地图底图口径」段、§6.4.2 图标章节整段重写。

  **我在本轮改掉了自己的两处推荐（须记明）**：最初建议生活用 `recycling`（回收三角）、服务用 `handyman`（锤子+扳手交叉），语义都更贴。但实取路径后发现 `recycling` 含 4 组独立箭头共 532 字符、`handyman` 双工具交叉 640 字符，**20px 下笔画间距不足 1px，必然糊成一团**，故降级为 `swap_horiz`（234 字符）与 `build`（326 字符）。

  **沉淀的原则**：① **小尺寸下「结构简单」优先于「语义精确」** —— 认不出的精确等于零；② 图标验收必须在真实渲染尺寸下做，此前两版被判「不够形象」的直接原因就是一直放大到 400% 检视；③ 需求与合规冲突时不照做也不驳回，**保住痛点、换实现路径**；④ 图标资产一律取官方路径，不手写坐标、不凭记忆编造 path。

  - ✅ `node --check` 退出码 0，`GetDiagnostics` 全工程零诊断。
  - **须整体重跑**：图标资产与 Pin 结构均已变，且新增 1 个画框，故须**跑批次 6 清空 → 再按 1→2→3→4→5 重跑**；重跑后需人工做两件事：贴高德截图底图（**已于 [32] 改为自动内嵌，此项取消**）、在 T6-⑤ 按 100% 尺寸验图标可辨识度。

- [32] 2026-08-24：**地图底图改为 Base64 内嵌进 `code.js`，消除「每次重跑要手工贴 9 次图」的重复劳动**。

  **用户输入**：「图片已经替换，你查看一下当前的 figma，提一个问题：能不能把图写进 code.js，否则每次都要替换很麻烦」；追加「这是截图，看看能不能简化你的实现」。

  **痛点量化（我在 [31] 低估了它）**：底图落点共 **9 处**（`mapCanvas()` 8 个调用点 + `buildMapSelector()` 1 个）。[31] 留的是「手动贴图占位」，意味着每次跑批次 6 清空重跑后，都要在 Figma 里重复贴 9 次图，且漏贴一处不会有任何报错。这类活不该由人做。

  **技术可行性（官方文档背书，非取巧）**：`figma.base64Decode(str) → Uint8Array`（Plugin API Update 42 加入）+ `figma.createImage(bytes) → Image`，取 `image.hash` 填进 `{ type:'IMAGE', imageHash, scaleMode:'FILL' }` 即可。官方《Working with Images》原文写明「To avoid network requests altogether, you can include images in your plugin as **Base64-encoded data**」，故 `manifest.json` 的 `networkAccess: ["none"]` **无需放开任何域名**。

  **实现**：
  - 新增 `tools/embed-map-bg.py`：读 `docs/map-bg.png` → 裁切 780×1376 → JPEG q70 → Base64 → 覆写 `code.js` 末尾由哨兵注释（`MAP_BG_ASSET_BEGIN/END`）界定的常量段。按哨兵定位而非行号或变量名正则，`code.js` 前面的逻辑代码怎么改都不影响定位；**幂等**，连跑两次体积完全一致（317.8 KB）；
  - `mapBasePlate()` 改为解码后直接填 IMAGE fill，`MAP_BG_HASH` 做模块级缓存 —— imageHash 是内容寻址的，9 个落点共享同一份数据，只解码一次。图层名从 `_map-bg [替换为高德截图]` 简化为 `_map-bg`；
  - 换底图流程收敛为三步：替换 `docs/map-bg.png` → 跑 `python tools/embed-map-bg.py` → 回 Figma 重跑插件。

  **看过截图后做的三项简化（用户第二问的直接产出）**：
  - **删掉 `rect()` 函数**：全库 Grep 确认它只剩 1 个调用点，且它铺的纯色会被图片填充**整体覆盖**（`fills` 是替换而非叠加），`colorRole` 参数纯属浪费、`radius` 参数无人使用。已把 `rect()` + `mapBgHash()` + `mapBasePlate()` 三个函数塌缩成一个，少两层间接；
  - **Base64 串从「分 18 片 join」改回单行**：原以为折行便于阅读，实则换一次底图会让 diff 变成十几行齐变；单行的话只体现为一行差异；
  - **编码格式经实测确认仍用 JPEG**：截图是平面矢量地图（大色块 + 大量小字），我一度判断该用 PNG-8 调色板量化。实测推翻 —— 该图有 **24740 种颜色**，PNG64/128/256 的 Base64 分别为 225.8/283.9/330.6 KB，反而是 JPEG q70（174.3 KB）的 1.3~1.9 倍。

  **一处我原以为多余、实测证明必要的步骤（须记明）**：我曾判断本地 `crop_to_ratio` 裁切是多余的，理由是 Figma 的 `scaleMode: 'FILL'` 本身就会做「等比填充 + 居中裁切」。实测数据推翻了这个判断 —— 源图 1406×1456 接近正方形，FILL 到竖长的 390:688 地图区会自行**丢弃约 41% 的横向像素**，那部分字节等于白编码（不裁切版 780×808 只能提供 1.70x 有效清晰度，而同等体积的裁切版能给到 2.00x）。**故裁切保留**。

  **体积权衡（实测 6 档后选定）**：2x 780×1376 / q70 → JPEG 130.7 KB → Base64 **174.3 KB**。选 2x 而非 1x 因 Figma 画布常在 100%~200% 间评审，1x 放大后路名会糊；选 q70 而非 q80 因后者 216.7 KB 而肉眼差异极小。`code.js` 从 141.8 KB → **317.8 KB**。

  **同步落地**：`ui.html` 的四步手动贴图说明改写为「已内嵌 + 换图三步」；PRD §6.4.1「原型阶段的底图呈现方式」条重写并新增「换底图流程」条（含 41% 像素浪费的判据）。

  **沉淀的原则**：① **凡需人工重复 N 次的步骤都是缺陷**，N=9 时更是必须自动化，而非写进操作手册让人照做；② 「看起来多余的一步」要用数据判定再删 —— 本轮 `crop_to_ratio` 若凭直觉删掉，会白扔 41% 的有效像素；③ 格式选型不凭「矢量图该用 PNG」这类常识推断，实测颜色数与体积再定。
  - ✅ `node --check` 退出码 0，`GetDiagnostics` 全工程零诊断，脚本连跑两次幂等，全文件 CRLF 行尾未被污染。
  - ⚠️ **本轮未完成事项**：用户要求「查看一下当前的 figma」，但 MCP 返回 `Figma plugin not connected`（插件弹窗未打开），实机检视**未做**。需用户在 Figma 打开 Plugins → Prototype MCP 并保持弹窗为 Connected 状态后补做。

- [33] 2026-08-24：**补齐导航栏搜索框 + 通知铃，并新增「搜索过滤态」画框**。

  **用户原话**：「地图界面这块差一个搜索框，用户可以属于关键字，如"租房"，来快速过滤图层」。

  **定性：这不是新需求，是插件漏实现（用户观察准确）**。落笔前先核 PRD，五处均有既定规格：§6.3（L784「首页由 Logo / **搜索** / 视图切换 / 通知 / 地图区…构成」）、§6.4.1 ASCII 图（L804 明确画了 `[🔍搜索…]` 与 `🔔(3)`）、§2.9（L367「分类搜索支持拼音首字母、同义词映射」）、§2.1（L245「分类体系直接驱动…**搜索分类匹配**」）、§6.4.3（L905 列表页顶部含「搜索图标」）。而 `navBar('鸭圈', { right: '列表' })` 只实现了视图切换，**Logo、搜索框、通知铃三件全缺**。

  **核实中查出 PRD 自身两处不一致（顺手改掉，否则规格无法作为唯一依据）**：
  - §2.6 L348 写「**搜索页**：分类过滤区」，但 §10.1 页面清单 17 页 + 3 模态里**没有搜索页**，L1552「明确不做的页面」里也没列它 —— 它在 IA 里根本没落点。已请用户定口径，用户选「首页内搜索，不新增页面」，故 §2.6 那句改写为「**首页搜索态**」并注明「不是独立页面」，页面总数不变；
  - §6.3 L784 列了「视图切换」，但 §6.4.1 L804 的 ASCII 图没画它。图是原型的权威规格源，故把图改为四件齐全：`鸭圈 [🔍 搜索保洁/拼车/租房] 列表 🔔•`。

  **实现**：
  - `ICON_PATHS` 新增三个 Material Symbols 官方路径（`search` 357 / `notifications` 297 / `close` 203 字符，均单 path 无 defs，符合 `svgIcon()` 的已知限制）；
  - `navBar()` 新增 `search` / `searchValue` / `bell` 三个可选项；新增 `navSearchBox()` 与 `navBell()` 两个构造器；
  - home-screen 的 **8 处** `navBar` 调用统一改为读常量 `HOME_NAV` —— 逐处写死的话改一次占位文案要改 8 处，必漏；
  - `mapCanvas()` 新增 `opt.pins`（整组替换 Marker）与 `opt.summary`（覆写摘要胶囊文案）；
  - 批次 2 新增变体画框「搜索过滤态 · 关键词「租房」」，批次 2 画框数 **21 → 22**（主态 1 + 展开 1 + 图例 1 + 信息卡 1 + **搜索过滤 1** + T3 三级树 3 + 过程态 6 + 降级态 3 + 半径档 4 + list-screen 1）；
  - `FLOW_LINKS` 新增 `home-screen · _nav-bell → notification-screen`。

  **几处落笔时自查纠正的坑（都会导致视觉错但不报错）**：
  - **`box()` 只传 `h` 不传 `w`，高度会塌成 18px**：横向 Auto Layout 的 primary 轴是**宽**、counter 轴才是高，而 `box()` 里 `opt.h` 只设 `primaryAxisSizingMode`。不传 `w` 则 `counterAxisSizingMode` 停在 AUTO，高度按内容 hug。故 `navSearchBox` 里 `w` 与 `h` 必须都传；
  - **`layoutGrow` 对 TEXT 节点无效**：`text()` 出来的节点 `textAutoResize` 默认是 `WIDTH_AND_HEIGHT`（宽高双 hug），宽度既然由内容定，`layoutGrow` 就拉不动。须先改 `textAutoResize = 'HEIGHT'`；另加 `textTruncation = 'ENDING'`，否则长关键词换行会把第二行挤出 32px 的框；
  - **注释里的宽度算式我第一次写错了**：把标题「鸭圈」（2 字约 32px）写成「Logo 64」。按错数据留注释，下次改版必被误导，已重算为「358 − 32 − 28 − 24 − 24 = 250px 搜索框，内部再扣 46px，文案实得约 204px」；
  - **`SearchReplace` 一次替换意外吞掉了换行**，把 `async function batchMap() {` 与 `await loadFonts();` 挤成一行，已即刻修回。

  **几个刻意的取舍（都有判据，不是随手定的）**：
  - **搜索框放导航栏、不悬浮在地图上**：地图左上 `x=16 y=12` 已被筛选摘要胶囊占据，右上是三竖点 + 图例入口，悬浮必与其中之一抢位；且 PRD 两处都把搜索列为导航栏成员，导航栏是它唯一有据可依的落点；
  - **搜索框高 32px 而非 iOS HIG 的 44px**：导航栏总高 48 是 PRD 定死的。输入框不同于图标按钮，其横向命中区宽 250px，实际可点面积远超 44×44 等效值；
  - **占位文案不用 ASCII 图原句**：「就近的保洁/拼车/二手」20 字约需 240px，而文案区实得约 204px，必被截断。压缩为「保洁/拼车/租房」9 字约 108px；
  - **未读用 8px 红点而非数字气泡**：导航栏仅剩 24px 见方，双位数气泡会溢出压到搜索框；精确条数由 `notification-screen` 承载；
  - **搜索框不做跳转触发点**：「搜索过滤态」是变体画框（带 `⟨变体⟩` 后缀），按 [29] 立的规则不进跳转索引，故搜索框无法作为跳转源。它在原型里是静态演示，交互由「主态 / 搜索过滤态」两框对照表达；
  - **通知铃接跳转**：`notification-screen` 此前只有「我的 › 通知中心」一个入口，首页顶部的铃若不可点，用户看到红点却无处可去；
  - **过滤态刻意把 7 个 Pin 收成 3 个房屋类 Pin**：过滤的效果必须体现在 Marker 的增减上，只改搜索框里的文字等于没过滤。摘要胶囊同步改为「5km · 资源+需求 · 关键词「租房」」—— 若仍显示「全部分类」，就与搜索框自相矛盾。

  **列表页一并补**：PRD §6.4.3 列表页顶部规格含「搜索图标」，且两页共享同一套筛选与范围状态（§10.1），搜索作为筛选的一环不应只有一页有。

  **同步落地**：PRD 新增 §6.4.1「导航栏搜索框口径」（5 行规格表 + 7 条判据）、改写 §2.6「首页搜索态」、改 §6.4.1 ASCII 图。

  **沉淀的原则**：① **用户说「差一个 X」时，先查 PRD 判定是漏实现还是新需求** —— 本轮是漏实现，处置方式（照既定规格补齐）与新需求（先定规格再实现）完全不同；② 核规格时若发现文档自相矛盾（本轮两处），**必须先消除矛盾再落笔**，否则代码没有唯一依据；③ 涉及口径分歧（独立页 vs 页内态）不自行拍板，交用户定。
  - ✅ `node --check` 退出码 0，`GetDiagnostics` 零诊断，CRLF=3290 与 LF 总数一致（无混用行尾），`code.js` 327.7 KB。
  - ⚠️ **须整体重跑**：`navBar` 结构变了且新增 1 个画框，故须**跑批次 6 清空 → 再按 1→2→3→4→5 重跑**。

  ---

  **【同日追加 · 一】MCP 实机复验，清掉 [32] 遗留的「查看当前 Figma」卡点**。

  用户开启 Prototype MCP 后首次取证即发现 **Figma 里是改前旧版本**：`_nav-search` 与 `_nav-bell` 均返回 0 个、`home-screen` 只有 20 个变体、画框总数 37（应 38）、节点 ID 段为 `276:xxxxx`。指导用户重跑后变为 `291:xxxxx`，7 项复验全过：`_nav-search` 22 处（home 21 + list 1，与 `navBar(` 的 22 处调用一致）/ `_nav-bell` 22 处 / 搜索过滤态画框 `291:23640` 存在 / **`_nav-search` 的 `reactions` 为空数组**（确认输入框未误绑跳转）/ `_nav-bell` 的 `ON_CLICK → notification-screen` 跨 Section 成立（SMART_ANIMATE 0.3s EASE_OUT）/ `_search-clear` 仅 1 处（无关键词的页面不渲染叉号）/ 过滤态 `_map-canvas` 下确实只剩 3 个 `cat-house` Pin，其余 20 个 home 变体各为 2 个房屋 + 5 个其他类 —— **是真过滤，不是只改搜索框文字的假过滤**。

  **复验方法论（本轮实证，下次照此做）**：① `find_nodes` 只匹配节点名，**不搜 `TEXT.characters`**；② `get_canvas_overview(includeElements:true)` **不下探子节点**，故容器节点一律报 `hasReactions:false`，这是正常现象不是缺陷；③ **唯一可信手段是 `list_reactions` 逐节点验证**；④ 节点 ID 段可直接用于判断 Figma 里跑的是哪一版代码。

  **一个必须记住的操作约束**：Figma 同时只能开一个插件弹窗，而复验流程需要两个（`Prototype MCP` 读写通道 + `原型生成器` 画画框），且 MCP 无法触发另一插件的按钮。故重跑必须人工「**关 MCP → 在插件列表 Reload 生成器（不 Reload 会跑旧代码）→ 清空已生成内容 → 跑批次 1→5 → 重开 MCP**」。

  **顺带排掉一个非缺陷问题**：用户反馈找不到 Prototype 标签，实为处在 Dev Mode（右侧面板只有 `Inspect / Plugins`），`Shift + D` 切回设计模式即可，播放快捷键 `Ctrl + Alt + Enter`。

  ---

  **【同日追加 · 二】「生活」分类图标改版：`swap_horiz` → `volunteer_activism`（手托爱心）**。

  **用户原话**：「现在双箭头语义表达上跟生活分类关联不大，是这个问题」。定性为**语义问题而非清晰度问题** —— 此前 [31] 换 Material Symbols 时只校验了「小尺寸是否粘连」，漏校验了「语义是否指向该品类」。

  **两轮被否的判据**：
  - **`swap_horiz`（左右双箭头）否于语义关联太弱 + 族群不一致**：箭头在界面里的通行含义是「操作/切换」，摆进 Pin 会被读成控件而非事物；且它只表达「挪动」这个动作，而分类图标要表达的是被挪动的东西本身。更要紧的是工作=手提箱、房屋=屋形、车辆=汽车、服务=扳手四件皆为具象实物，唯独生活是抽象符号，五个并排时明显异质；
  - **`package_2`（纸箱）否于覆盖不全**：把子类目摊开核算才发现，生活下挂 **5 个二级 / 15 个三级**（二手闲置转让 5 / 借物互助 3 / 寻物失物 2 / 宠物相关 2 / 求购求助 2），纸箱只覆盖物品侧 **11/15** —— **宠物是活物装不进箱子，邻里互助与求助是人情往来而非物品交易**，两支共 4 个三级类目落空。这五支真正的共性是「**邻里之间的非商业往来**」，不是「商品流通」。

  **定稿 `volunteer_activism` 的理由**：一只手向上托起、心自手中升起，「给出」与「善意」两层动作恰好横跨用户点明的「物品转让 + 帮助」双语义，是 24 个候选里**唯一 15/15 全覆盖**的图形（二手=递出、借物互助=直接命中、招领=归还善意、领养=善意托付、求助=直接命中）；361 字符单 path，手与心均为实心块面而非线条勾勒，实测 40px / 20px 清晰、14px 两块面之间白隙仍在。

  **同时被否的次选**：`handshake`（654 字，双手交错）、`waving_hand`（567 字，五指分开）、`loyalty`（434 字，语义组合最贴但 14px 心与标签粘连）—— 三者均因 14px 必粘连；`card_giftcard` / `redeem` / `sell` 暗示预付与商业馈赠，与借物、求助两支冲突；`weekend` 把品类窄化成家具；`recycling` 4 组独立箭头 20px 间距不足 1px 且同为抽象符号。

  **本轮纠正的四处我自己的错误判断**：
  - **`recycling` 字符数沿袭错误**：code.js 与 PRD 都写「532 字符」，实测 `fill1` 版是 **428**，两处已改正；
  - **`volunteer_activism` 可辨性误判**：我曾凭 361 字符断言「手掌笔画在 20px 偏细」，实测其手掌是**整块实心面**，清晰；
  - **`support` / `communities` 语义按名字猜错**：实测图形分别是**方向盘**与**三瓣圆**，与「扶助」「社区」毫无关系，已剔除；
  - **行尾污染误报**：PowerShell 测出 `CRLF=2863 / LF=3305` 判为污染，Python 实测 before 就是 3305/3305 完全一致 —— 根因是 `Get-Content -Raw` 经 sandbox 包裹后反引号转义失真。为此建的 `fix-eol.py` 空跑，已删除，此后行尾一律用 Python 校验。

  **沉淀的原则（三条，均由本轮踩坑得出）**：
  - ① **图标选型不可凭 SVG 路径字符数推断可辨识度**。本轮先按字符数预判，多处结论被实机渲染推翻。须按真实尺寸（40px 地图 Pin / 20px 图层面板 / 14px 图例）渲染成对照图，肉眼比对后再定；
  - ② **视觉类改动必须「先渲染预览 → 用户确认 → 再落代码」**。本轮我在用户指出语义问题后直接改了 `code.js`，被用户批评「换掉之前不是应该给我看看换成什么样的了吗」—— 这是本轮最重要的反馈，此后一律遵守；
  - ③ **图标语义的覆盖度要按子类目逐条核算，不能凭品类名感觉**。`package_2` 看品类名「生活/二手」很贴，摊开 15 个三级才发现漏 4 个。

  **同步落地**：`code.js` 的 `ICON_PATHS['cat-life']` 换路径并写入完整决策记录（含两轮被否判据与全部次选理由）、`svgIcon()` 上方映射说明改为「手托爱心（给出 + 善意）」；PRD §6.4.2 Marker 规格表（L888）与图标映射表（L906）同步，取舍段落从 1 条扩写为 6 条并新增「不可凭字符数推断」的方法论条目。

  **三个渲染工具用后即清**（34 格候选池对照 / 24 候选双语义 A·B·C 三组 / 五分类改前改后并排，及其产出的三张 png）：它们是**一次性评审器材**而非工程资产，判据已全文沉淀进 PRD §6.4.2 与本条记录，留着反而会与日后变更的 `CATEGORY_COLORS`、`pinRaw()` 规格失同步，成为误导源。日后若再选图标，按 PRD §6.4.2 的方法论重建即可。
  - ✅ `volunteer_activism` 版校验：`node --check` 退出码 0、`GetDiagnostics` 零诊断、`CRLF=3314` 与 `LF=3314` 一致、`swap_horiz` 与 `package_2` 各余 1 处引用（均在注释的决策记录里，非活跃代码）。
  - ✅ 用户已 Reload 插件并重跑批次 1→5，Figma 侧为定稿版本。

### 2026-08-24（PRD 补齐工程契约三章 = SRS 工程侧）

**[34] PRD 新增第十部分「工程契约」§12 接口需求 / §13 数据需求 / §14 非功能需求，补齐 SRS 缺口**。

**起因**：用户问「《软件需求规格说明书(SRS)》，这个问题现在具备吗？」。核查结论是**不具备** —— 全库 md 文档 `Grep 'SRS|软件需求规格|需求规格说明'` 零命中（`code.js` 的唯一命中是底图 Base64 串里的偶然字节 `SkZJRgAB`，非文档内容），`docs/` 正式文档只有 BRD / MRD / PRD / 上架前置手册四份。

**覆盖度评估（评估后交用户决策）**：PRD 已覆盖 SRS 的「需求侧」约 60%（产品范围、功能需求与异常态、信息架构、界面规格、术语表），**缺失全部「工程契约侧」**——接口契约、数据字典、非功能需求三章无一存在；性能仅 §6.10 覆盖地图一处。判定为「直接拿 PRD 当 SRS 交研发，联调必返工」。

**用户决策：只补工程契约三章，不新建独立 `docs/SRS.md`**。理由（已向用户说明并获选）：独立 SRS 与 PRD 大面积内容重叠，后续每次改需求要两处同步，失同步风险高；且新建全文需新增排期，会挤占本周行政前置（8/25 软著）与 8/28 设计稿冻结的时间。**PRD 仍是唯一权威源。**

**同时澄清的一处易混点**：软著 P5 要的**不是 SRS**，而是 ≥10 页带界面截图的「**操作说明书**」（写「用户怎么用」）；SRS 写「系统要满足什么」，二者不可互替。软著审查看的是说明书功能与源码是否对得上（详见上架手册 P5 材料 B 与三重一致性自检表）。

**落地内容（PRD 新增约 420 行，原 §10/§11 之后、附录之前）**：
- **§12 接口需求**：通用约定（HTTPS/JWT/幂等/GCJ-02 坐标/RFC3339/分页/500 Pin 上限/10KB 体积/RT P95≤150ms）+ 统一响应包 + 9 组接口清单（鉴权账号 / 分类模板 / 发布 / T2 AI / 地图与列表 / 详情与联系 / 认证 / 通知推送 / 设置合规）+ §12.4 埋点（四组必埋事件对应北极星三轴）+ §12.5 **19 条统一错误码**（含客户端处置动作）；
- **§13 数据需求**：13 张表的实体关系 + 6 张核心表字段字典（`user` / `post` / `contact_event` / `publish_memory` / `category` / `report`）+ §13.3 索引与查询要求 + §13.4 数据保留与删除策略（OCR 7 天 / 日志 30 天 / 记忆 180 天 / 审计 ≥180 天 / EXIF 强制剥离 / 不采集轨迹）；
- **§14 非功能需求**：性能（P95≤300ms 四段口径 + RT≤150ms + 帧≤16ms）/ 可用性与容量（含**服务降级次序**，五大 P0 闭环为最后降级项）/ 安全与隐私 / 兼容性（Flutter 3.41.9 锁定、minSdk 21、iOS 12+、高德 10.1.200）/ 可观测性 + §14.6 **NFR 验收清单**（8 项 🔴 阻塞 + 1 项 🟠 + 1 项 ⚪）。

**关键写法约束（三条，本轮定的规矩）**：
- ① **三章不新增任何产品需求**，每一条都标注 §1–§9 来源章节，纯属「把已定的产品口径翻译成工程契约」。唯一例外是 `Idempotency-Key`（幂等键）属传输层新增约定，已在表内显式注明「不改 §5 任何发布规则」；
- ② **写明冲突处置**：三章开头与文末结语均声明「与 §1–§9 冲突时以前文为准并须回改本部分」，避免日后两处打架时无裁决依据；
- ③ **不引行号只引章节号**。初稿写了 5 处 `§9 L1530` 式行号引用，行号会随文档增删失效，全部改为 §9.10.1 这类规范章节号。

**自检修出的问题（12 处引用 + 2 处口径冲突，均已改）**：
- **两处口径冲突（这类才是真风险）**：① `post.status` 我按常见写法写成 `draft/online/offline/hidden/removed`，而 §8.6 状态机是 `draft/active/archived`，已改为 `draft/active/archived/hidden` 并注明「取值须与 §8.6 严格一致」；② 完整度我写成「必填 50% + 位置 30% + 类目 20% 加权得分」，那是**说明文档 §2.5 技术建议的旧口径**，PRD §9.8 已明确为**三条件达成计数**（3 个＝🟢 / 2 个＝🟡 / ≤1 个＝🔴），已把 `completeness_score` 改为 `completeness_conditions` json 存三条件达成态，并顺带让 `/posts` 出参回显它以支撑 §8 T6-④ 完整度卡「还差哪几项」；
- **四处误引 PRD 章节**：`§2.7`（PRD §2.7 是「用户旅程」，我本意指**说明文档** §2.7 内测包裁剪）、`§9.4`（PRD §9.4 是「用户旅程」，设置项实为 §9.3）、`§2.5`（PRD §2.5 是「资源态 vs 需求态」，我本意指说明文档 §2.5 技术建议）、`§12.6`（不存在，可观测性是 §14.5）；
- **五处行号式引用**改规范章节号；**一处**错误码分段规则把 `03` 同时写成「权限」与「AI」两个含义，改为按 HTTP 状态码语义分段；**一处** §14.1 表格「双向推送时延」把 `§6.14` 重复填进判定口径列。

**沉淀的原则（本轮新增两条）**：
- ④ **补写工程侧文档前必须先逐条核实被引章节真实存在且语义对得上**。本轮 12 处引用错误里有 4 处是「章节号存在但讲的是另一件事」，这类错误比死链更危险——评审时不会报错，研发照着做才会踩坑；
- ⑤ **凡从别处（如说明文档技术建议）搬来的规格，必须与 PRD 同主题章节逐条对账**。完整度加权分就是从说明文档 §2.5 搬来的旧口径，PRD §9.8 早已改为三条件计数，差一步就把废弃算法写进数据字典。

**尚未做（不在本次范围）**：软著 P5 的操作说明书（≥10 页含界面截图）仍待编写，8/25 提交前必须完成；运营治理后台 7 页的接口与表结构按说明文档 §2.7 定为本期不做，PRD §13 只先落 `audit_log` 与 `risk_score` 落点，避免上架前补齐时改动主表。

**[35] 本轮教训沉淀为 repo learning + docx 交付件同步**。

**沉淀**：执行 `ce-compound`（lightweight 深度），把 [34] 的原则 ④⑤ 扩写为独立可复用文档 `docs/solutions/workflow-issues/cross-document-reference-verification.md`，含五条约定：①被引章节须核实「存在＋语义对得上」（跨文档引用必须写明文档名，本仓库 PRD 与说明文档共用相似的 §2.5/§2.7/§9.x 编号，凭记忆写必串）；②从别处搬来的规格须与权威源同主题章节对账；③只引章节号不引行号（附 `Grep "L1[0-9]{3}"` 自查）；④新章节章首＋文末双端声明定位/口径原则/不覆盖范围；⑤无法溯源的条款诚实标「本节新增」，不伪造来源。两个 bundled 校验脚本（`validate-frontmatter.py` / `validate-doc-claims.py`）均 exit 0。

**同步 docx**：用户追问 `找鸭找-产品需求文档-v2.1.docx` 是否与 PRD.md 拉齐。核验发现**未拉齐** —— docx 停留在 8/23 19:26 版本（95,909 字节 / 全文 90,327 字），特征句核对**命中 0/12**，完全不含 §12–§14。重跑 `gen_prd_v2.1_docx.py`（MD 驱动型，`main()` 直接读 PRD.md，无硬编码文案）后改名替换，得 **129,890 字节 / 全文 93,831 字 / 28 条目录项**，特征句核对 13/14 命中（`第十部分：工程契约`、`12/13/14` 三章标题、`completeness_conditions`、`Idempotency-Key`、`publish_memory`、`audit_log`、`layer_switch`、`demand_push_sent`、`NFR 验收清单`、`draft/active/archived/hidden`、`第十一部分：附录` 全部 HIT），旧标题 `第十部分：附录` 已 GONE。

**唯一 FAIL 是探针缺陷不是文档缺陷**：特征句我写成剥离星号的 `本部分不新增任何产品需求`，而 docx 保留原始 Markdown 标记 `本部分**不新增任何产品需求**`。逐段打印确认三行声明（定位 / 口径原则 / 不覆盖范围）已完整落盘。**这正是既有 learning `multi-format-deliverable-consistency.md` 里「特征句要用改写后的原句片段，不要用通用词表」那条的同类踩坑** —— 我用了自己脑补的形式而非 PRD 原句。

**沉淀的原则（本轮新增一条）**：
- ⑥ **改完权威源 MD 后必须立即重生其驱动的交付件，并用原句特征核对落盘**。本轮 PRD.md 在 8/24 13:04 已含 §12–§14，docx 却停在 8/23 19:26，中间隔了近 18 小时——若不是用户追问，这份缺整章的 docx 就会被当成最新版交出去。写特征句时**必须从权威源复制原句**（含 `**` 等标记），脑补形式会产生假 FAIL 并浪费一轮排查。

**[36] docx 滞后事故沉淀（ce-compound Full 深度）+ 建立 CONCEPTS.md**。

**并入既有 learning 而非新建**：Related Docs Finder 判定与 `docs/solutions/workflow-issues/multi-format-deliverable-consistency.md` 重叠为 **High（5 维中匹配 4 维**，仅根因不同：那篇是「内容源假设错误」，本轮是「漏掉重跑这一流程步骤」**）**。按 skill 裁决表「同一问题同一解法必须更新既有文档，否则两篇必然漂移」，全部增量并入该文档：新增「一之二、MD 驱动型的残余风险」一节（含 `_tmp.docx` 不落最终文件名、闸门覆盖面不随文档增长两个残余风险面）、docx 侧探针写法与三条收紧（探针正反成对 / 原句逐字含 Markdown 标记 / 探针 FAIL 先怀疑探针）、Why This Matters 与 Examples 各补反正例，frontmatter 加 `last_updated: 2026-08-24` 与 2 条 `applies_when`。

**语义 grounding 验证查出 3 处硬错误（本轮最有价值的部分）**：派 read-only 子代理把文档里 12 条代码断言逐条回代码点验证，结果 11 条 verified、3 条 contradicted：
- **「四段闸门」实为五段** —— `verify_prd_docx.py` 有 5 个校验块（关键词 34 项 / Scope 红线 7 项 / 统一口径 6 项 / 一级章节 12 项 / 版本痕迹 18 项）。这个错误口径**源自本说明文档 §[20]，被 learning 继承**，现已两处一并纠正；
- **「零 FAIL 零 MISS」表述不准** —— 章节缺失确实一条都报不出来，但当前 docx 跑闸门必然在「替换」上报 FAIL（禁用词自伤），已改为「章节缺失不产生任何 FAIL/MISS，唯一红灯与新章无关」；
- **「MD 改动 40 处」串批次** —— 40 处属 [19] 的 PRD 结构化评审，本批次实为 BRD/MRD 口径 26 处 + 硬编码槽位 14 处。

另修 2 处行号偏移（`dst` 注释在 L512 非 L511）、补回代码片段被静默省略的 `usable_w/usable_h` 定义、给运行时观测值（模板 9 处容差基线）加「非可复现常量」限定。

**新建 `CONCEPTS.md`**（仓库根，此前不存在）：按 skill 的 scoped-seed 规则只 seed 本 learning 所在领域「文档交付链」的 5 个核心名词 —— 权威源 / 派生交付件 / 生成脚本（MD 驱动型 vs 硬编码型）/ 落盘核对 / 闸门，并记录一条已澄清的歧义（「同步」曾混指「改完权威源」与「重生并核对落盘」两件事）。

**沉淀的原则（本轮新增一条）**：
- ⑦ **经验文档里关于工具的可数事实（闸门段数、条目数、覆盖范围）必须回代码数，不能沿用任何留档转述**。本轮「四段闸门」这个错误从说明文档一路传到 learning，说明留档转述会成为错误口径的传播链；同理，凡引用行号必须当场核对，因为大文件在不同读取方式下行号会偏移。



### 2026-08-24（高保真原型升级 · 想法生成与冲突核实）

**[37] 生成 `hifi-prototype-builder` skill；执行 ce-ideate 产出高保真升级想法集；核实出 code.js 与 PRD 三处口径冲突**。

**产出一 · skill**：`.trae/skills/hifi-prototype-builder/SKILL.md`。按用户给的 SOP 落地为 7 步流水线（需求梳理→设计 Token→Figma 建稿→原型交互→自检→交付文档→汇总），并补齐三处原 SOP 未定量的部分：字重字号间距配色的具体取值（Noto Sans SC 400/500/600/700、字号 12/14/16/20/24/32、8pt 栅格、圆角 4/8/12、WCAG AA）、prototype-mcp 的实际工具名（原 SOP 只写了 MCP 名未写工具名）、自检改为勾选式清单。

**产出二 · ideation**：`docs/ideation/2026-08-24-hifi-prototype-upgrade-ideation.html`。范围经用户三项决策锁定为「新建高保真 Page 与骨架并存 / 五大 P0 闭环优先 / 真实内容+状态覆盖+视觉精修+交互动效四维全补」。四条主题轴各有 survivor（无空轴），保留 6 条、砍 10 条。其中 3 条砍在技术不可行（REST API GET-only / Instance 禁增删子节点 / `createNodeFromSvg` 剥离 defs 渐变滤镜），4 条砍在超范围或撞 8/28 时间墙。

**流程降级已记档**：`Task` 工具 query 参数约 30 词上限，无法把完整候选清单传给独立的 basis verifier 子代理，故降级为 orchestrator-only 过滤（ce-ideate 的 `post-ideation-workflow.md` 允许此降级但要求记录）。所有 `direct:` basis 来自 4 个 search 子代理返回的带 `file:line` 原文摘录，**未做二次独立校验**。

**本轮最有价值的部分：三处口径冲突逐条回读原文核实，结论均为 code.js 错、PRD 为准**：
- **半径档整体偏小一档**：`code.js:2190` 为 `1km/3km/10km/全城`，`PRD.md:947-952` 为 `3km/5km/10km/全城`。四档的密度描述（≥30 / 10–29 / 3–9 / ≤2 条）与 PRD 完全对齐，**只有半径数字错位**（后续核实为「从 §6.7 范围条五档里挑了四个」，详见 [38]）；
- **过程态六个画框画错了对象（比原判断更严重）**：`code.js:2148` 自己写着注释「这是空状态兜底时间轴，不是性能降级规范，两者不可叉乘」，但紧接的前两行 `0–1s 骨架屏：地图底图先出` / `1s 首屏 Pin 上屏` **正是性能加载态，违反了自己的注释**。`PRD.md:973-980` 要的是空状态兜底：鸭子 IP 缺省图 + 「附近有点安静，正在帮你往外找…」+ 骨架卡 3 条 → 文案切「正在扩大到全城范围…」+ 10 秒进度条 → 「已通知平台帮你找」+「先去别处看看」按钮 → 终态空页。这不是文案润色，是六框整体重画；
- **两个状态纯缺失**：`PRD.md:985` 位置权限未开的全屏引导页（主按钮「开启位置权限」/ 次按钮「手动选择城市」）、`PRD.md:1320` 已下架详情态，code.js 变体清单中均无对应画框。

**另查出 PRD §1 已定义但生成器未实现的 12 项**，其中可自行修复的：卡片阴影 `0 2 8 rgba(0,0,0,0.04)` 被改成描边、按钮无 pressed 变暗态、§1.4.8 动效规范五行全表只落地了单一 `SMART_ANIMATE/EASE_OUT/0.3s`、阴影 9 处逐处内联无 Token（`code.js:822/1421/1446/1476/1537/1632/1703/2847`）、暗色模式仅取 `collection.modes[0]`、导航搜索框高 32 未达 44×44 等效（此项 [33] 已有判据，不改）；依赖外部美术故只能占位的：鸭子 IP Logo / 空状态图 / 缺省图三件素材全缺。

**沉淀的原则（本轮新增一条）**：
- ⑧ **代码里「这不是 X，别和 Y 叉乘」这类自我警示注释，必须回读它下方的实现是否真的守住了它**。本轮过程态就是注释写对了、紧接的六行实现却正是它警告的那件事——这类错误评审时极易漏过，因为读者看到注释就默认下方合规了。同理，注释里的出处章节号（本轮 `§6.7` 实为 `§6.4.4`）也要当场核，错的出处会让下一个人核不到规格而照错代码沿用。

**下一步**：用户选定深挖想法 1（PRD 单一真源表 + 修三处冲突），进 ce-brainstorm 展开为可执行需求。**尚未动 `code.js` 一行**。

### 2026-08-24（真源表修正 · 需求与实施计划定稿）

**[38] 执行 ce-brainstorm + ce-plan，产出 implementation-ready 计划**：`docs/plans/2026-08-24-1651-fix-prd-source-of-truth-plan.md`（R1–R8 / F1–F2 / AE1–AE4 / KTD1–KTD6 / U1–U5）。**仍未动 `code.js` 一行**，本条只记规划与新查明的事实。

**[37] 的半径档判断被修正（不是抄录下移一格，是两套口径串了）**：回读 PRD 后确认「半径」本有两套口径且互不相同——`PRD.md:947-952` §6.4.4 的 S2 蜂窝是**系统自动响应**的四档 `3/5/10/全城`；`PRD.md:1011` §6.7 实现逻辑的范围条是**用户手动选**的五档 `1/3/5/10/全城`（该五档在 PRD 中共 6 处出处：@53 Scope 表 / @787 范围滑块 / @1011 / @1070 缓存键 / @1423 设置默认范围 / @1706 `/map/pins` 入参 / @1846 `user.default_radius`，全部指向范围条这一套）。`code.js:2190` 取的四个值正是从范围条五档里挑的，标注卡 `code.js:2198`「半径档统一口径：1/3/5/10/全城」也是范围条口径，而画框标题写的是「S2 动态蜂窝半径档」——**三者不配对，故修正范围从「改取值」扩到「改取值 + 改标注卡文案」**。`code.js:2189` 注释标的 `§6.7` 并未失准，它如实指向了这个错误来源；错在标题与取值不配对，这一点也修正了 [37] 中「注释把出处标错」的判断。

**两项预判风险回代码后证伪**（ideation HTML 想法 6 的风险项写错，此处为准）：
- **加画框不需重排 `SECTION_Y`**：`resetSection` 是删掉整个同名 Section 后重建（非逐个删 children），`layout` 末尾按本次实际 `cols`/`rows` 调 `resizeWithoutConstraints` 撑开 Section，故画框增减自动适配；
- **加画框不引入死链**：新框传 `screen(..., isVariant=true)` 即带 `VARIANT_TAG`，按 `code.js:3035` 的变体过滤不进跳转索引。

**另发现 PRD 自身一处不自洽**：`PRD.md:982` 基站+商圈兜底 与 `PRD.md:985` 全屏引导页是并列陈述，未写先后关系。用户决定按「先引导页 → 仍拒绝 → 基站兜底」两段并存，并同步改 PRD（计划 R8/U1）。这是本轮唯一要动 PRD 的地方，且**须先于画框落地**，否则新画框无明文依据。

**实施顺序（计划 Sequencing）**：U1 改 PRD → U2 抽三张常量表并接通半径档 → U3/U4/U5 可并行（重画过程态六框 / 加权限引导页+基站兜底两框 / 加已下架详情态一框）→ 整体重跑 → 六道验证门。验证按 `CONCEPTS.md` 落盘核对约定，只认 `find_nodes` 提到的实际文本与 `list_reactions` 数到的跳转条数，时间戳与退出码不算证据。

**沉淀的原则（本轮新增一条）**：
- ⑨ **同一个业务名词在 PRD 里可能承载两套互不相同的口径（本轮「半径」= S2 自动四档 + 范围条手动五档）**。判断某处取值是否抄错，不能只比对单一章节——要先确认该名词在全文有几套口径、各自归谁管，再判断此处该用哪套。只核一套会得出「抄录下移一格」这类似是而非的结论，从而修错方向。

**下一步**：按计划 U1→U5 实施。

### 2026-08-24（真源表修正 · 落地并验证通过）

**[39] U1–U5 全部落地，六道验证门全过**。改 3 个文件：`docs/PRD.md`（§6.4.4 一处）、`prototype-figma/code.js`（新增三张真源表 + 五处改造）、`prototype-figma/ui.html`（批次说明计数同步）。已重跑批次 1→5 并用 MCP 逐条取证。

**改动明细**：
- **U1 · PRD §6.4.4**：定位权限降级两句合并为「两段递进」单条规格——第一段全屏引导页争取授权，第二段（仍拒绝或选"手动选择城市"）进基站+商圈兜底态，并回指 §6.8「定位失败 ≥3 次」为同一降级终态。「降级为模拟地图」保持独立条目未并入。
- **U2 · 三张真源表**（`code.js` 新增「一之二」段）：`S2_RADIUS_TIERS`（四档 3/5/10/全城 + 密度描述）、`EMPTY_FALLBACK_TIMELINE`（§6.4.4 六时点，文案取 PRD 原句）、`COVERAGE_STATES`（三个缺失状态的标题/回标/标注行）。每张表注释块写明权威源章节、"本表是唯一取值来源"、以及本次修正记录。半径档循环改读表，取值归位；标注卡三行改写为两套口径的区分说明。
- **U3 · 过程态六框**：改读 `EMPTY_FALLBACK_TIMELINE`，性能加载态内容移除。
- **U4 · 新增两框**：位置权限引导页（刻意不出地图与底部 Tab——未授权进不到首页主态，画上地图等于暗示"不授权也能看"）、基站+商圈兜底地图态（`opt.summary` 覆写为商圈级范围）。
- **U5 · 新增一框**：`buildDetailOffline()` 挂进 `batchCore`，与 `buildDetail` 并列，不改正常态本体。

**验证证据（`find_nodes` 提取的实际节点 + `list_reactions` 逐节点）**：

| 门 | 证据 |
|---|---|
| 半径档取值 | 四框标题为 `半径档 3km/5km/10km/全城`；`半径 3km · ≥30 条高密度` 等 caption 命中；反向探针 `半径 1km` = 0 命中 |
| 标注卡口径 | 四框均含「本档属 S2 蜂窝自动响应（3/5/10/全城…）；用户手动拖的范围条是另一套五档（1/3/5/10/全城，PRD §6.7）」；反向探针 `统一口径` = 0 命中 |
| 过程态文案 | `鸭子 IP 缺省图 + "附近有点安静，正在帮你往外找…" + 骨架卡 3 条`、`文案切"正在扩大到全城范围…"…可点"我自己调范围"退出等待` 逐字命中；反向探针 `骨架屏` / `首屏 Pin 上屏` 均 0 命中 |
| 新增三框 | `位置权限未开·全屏引导页`、`基站+商圈兜底地图态`、`详情页·已下架/过期态` 三框在 `get_canvas_overview` 中就位，均带 `⟨变体⟩` |
| 跳转无死链 | 三个新增框 `list_reactions` 均返回 `[]`；`btn/primary/登录 / 注册` → home-screen 主态、`btn/primary/联系 TA` → contact-screen 均正常，destinationId 指向主态框而非变体框 |
| 已下架态规格 | 红条 `_offline-banner` 是画框直接子级（不在 `_body` 内）故未被 60% 压到；按钮为 `btn/disabled/联系 TA` |

**实施中两处判断（与计划的 Outstanding Question 对应）**：
- **Opacity 60% 落层**：只压 `_body` 一层，红条与导航栏留在容器外。压画框根节点会把红条一起压到 60%，而 PRD §7.8 要它是最醒目的告知件。已用节点层级取证确认生效。
- **引导页价值文案不写具体公里数**：原写「3 公里内的活儿」，改为「身边几公里内」。3km 属 S2 自动档，在用户还没选范围的引导页写死数字，正是本轮在修的那类口径混淆。

**一个意外收获**：`btn/primary/联系 TA` 全文档只命中一处（正常态详情页）。因为已下架态用的是 `disabled` 变体，实例名自然变成 `btn/disabled/联系 TA`，没与正常态重名——避开了 `findClickable` 的重名抛错。**按钮变体命名天然带隔离性，加同名按钮的禁用态不会污染跳转索引**。

**沉淀的原则（本轮新增一条）**：
- ⑩ **`validate_prototype` 的 `unreachable` 在本项目是结构性噪音，不可作为验收判据**。它把每个内部容器（`_nav-bar` / `_disc` / `_map-caption`）都当 frame 参与可达性计算，一次返回上百条 error 并被截断。要判连线只能用 `list_reactions` 逐节点验（与原则 [30] 一致）；`validate_prototype` 的可用信号只有 `broken-reference`，而它会被海量 `unreachable` 淹没。

**尚未处理**：`docs/ideation/2026-08-24-hifi-prototype-upgrade-ideation.html` 想法 6 的风险项「`SECTION_Y` 需重排」已被证伪（见 [38]），HTML 未回改。不影响后续，记档备查。

### 2026-08-25（建立独立 git 仓库 · 首次入库）

**[40] s2s 建为独立 git 仓库并完成首次入库**。此前 s2s 无自己的 `.git`，整个目录只是上层仓库 `D:\developer\code` 眼里的一个未跟踪项（`?? aicoding/s2s/`），改动完全没有版本保护。

**仓库隔离（已取证）**：

| 核查项 | 值 |
|---|---|
| 仓库根 | `D:/developer/code/aicoding/s2s` |
| 上层仓库跟踪的 s2s 文件数 | `git -C D:\developer\code ls-files aicoding/s2s` = **0** |
| 账号（`--local`） | `liujie12` / `liujie_flexi@163.com` |
| 远端 | 尚未配置 |
| 入库文件总数 | 1274 |

账号一律用 `git config --local`，**不碰 `--global`**——本机还有 RuoYi-Cloud、iot-dc3 等其他仓库共用上层目录，写全局会串号。**密码不写入任何文件与 git 配置**，仅在推送远端时作为凭据使用。

**两个前置文件**：
- **`.gitignore`**：`.pylibs/` 与 `.pptx_lib/` 是脚本运行时就地安装的第三方库（python-pptx、lxml、Pillow），数千文件且可随时重装；另排除 `__pycache__/`、Office 锁文件 `~$*`、探针输出 `docs/probe_*.txt`、编辑器目录。**先写它再 `git add`**：不写的话首次提交会被依赖目录淹没，且 `git status` 会触发文件数上限警告。写后待提交项从数千降到 14。
- **`.gitattributes`**：`* text=auto eol=lf` + 二进制白名单（png/jpg/pptx/docx/pdf/zip）。不写则每次 `git add` 刷屏 `LF will be replaced by CRLF`，且后续 diff 会被换行符噪音污染，看不清真实改动。

**三条逻辑提交**（按"配置 → 工具 → 资产"分层，便于团队推广时单独摘取）：
```
2320ca8 chore: 初始化 s2s 独立仓库配置      # .gitignore / .gitattributes
906f2a8 chore: 纳入 CE 工作流技能包          # .agents/skills/**
b65542e feat: 纳入找鸭找产品资产基线（含真源表修正）  # PRD / code.js / 原型 / 本文档
```
第三条的提交信息里写明了一个事实：**[39] 的修正无法单独成一个提交**——入库前没有历史基线，改前改后的差异不存在于任何版本中，只能连同基线一起进第一版，并在提交信息里指回 [39] 留档。

**沉淀的原则（本轮新增两条）**：
- ⑪ **终端里看到的中文乱码 ≠ 提交信息编码错**。本轮 `git log` 显示乱码，我先怀疑 PowerShell 传参编码（改用 `-F` 文件传参）、再怀疑写入端（设 `i18n.commitEncoding=utf-8`），两次都没效果。真实原因是**终端输出编码**：执行 `[Console]::OutputEncoding = [System.Text.Encoding]::UTF8` 后立刻正常。提交信息从第一条起就是正确的 UTF-8，无需重做。**排查顺序应当是先验证输出端，再动仓库配置**——反过来会白改一堆 config 且误以为要重写历史。
- ⑫ **在多仓库嵌套的机器上，任何 git 命令先确认作用域**。本轮最初在 s2s 目录里跑 `git status`，命令实际落在上层 `D:\developer\code\.git` 上，返回了几十个无关项目的改动，还报 `index.lock` 沙箱限制。**`git rev-parse --show-toplevel` 应当是排查的第一条命令**，而不是最后的验证。

**代码审查门（ce-work 收尾要求）**：`Code review: skipped (mechanical diff)`

理由：本轮实质改动（U1–U5）在入库**之前**就已完成，而首次提交是 1274 文件的基线导入——diff 里全是新增文件，读不出「改了什么」，`ce-code-review` 拿不到有意义的对比面。改动的正确性已由更强的证据覆盖：六道验证门全部用 Figma 内实际节点取证（正向命中 + 反向探针 0 命中），逐条留档在 [39]。**从下一次改动起有真实 diff 基线，正常走 `ce-code-review`。**

**下一步待用户决定**：远端仓库地址尚未提供，`git remote add` 与首次 `push` 未执行。

**远端配置（2026-08-25 补记）**：`origin` = `https://github.com/liujie12/s2s.git`。远端**并非空仓库**，`git ls-remote` 探到已有 `main` 分支与一条 `3dad224 Initial commit`，内含建库时自动生成的 `README.md` 与一份 **Dynamics 365 Business Central 模板的 `.gitignore`**（与本项目完全无关）。本地 4 条提交与它无共同祖先，直接 push 会被拒。已决定**丢弃远端那条初始提交，`push --force-with-lease` 覆盖**，同时本地分支 `master` 改名 `main` 与远端对齐。

**沉淀（第三条）**：
- ⑬ **`git remote add` 之后、push 之前，必须先 `git ls-remote origin` 探一次**。GitHub 建库页勾了 README 或 .gitignore 模板就会自动产生一条提交，此时远端非空；不探就 push 会撞 `non-fast-forward`，而更隐蔽的坑是**平台模板 `.gitignore` 与项目技术栈无关**（本例给的是 Business Central 模板），若走 merge 保留下来会静默污染忽略规则。推送用 `--force-with-lease` 而非 `--force`：前者在远端被他人更新过时会拒绝，后者会无条件覆盖。

### 2026-08-25（配色冲突结案 · 设计 Token 口径对齐）

**[41] 两橙冲突结案：不换色，改为限定 Accent 职责；同时修掉 4 处 PRD 与实现的脱节**。只改 `docs/PRD.md`，未动一行代码。

**结案依据（推翻了原先的问题描述）**：[22] 记录的隐患前提是「Accent 现同时承担需求态强调职责」，据此推断「地图上车辆 Pin 与需求态标签会互相干扰」。全库检索后发现**这个前提在代码层早已不成立**：

| 原以为 | 实际（code.js 检索取证） |
|---|---|
| Accent 承担「辅助强调、分类点、需求态」三重职责 | Accent 只剩 **2 处**用途：`code.js:2780`「AI 猜」角标、`code.js:3024`「AI 帮我发」FAB |
| 需求态用 Accent 着色 | 需求态走**形状**维度——`code.js:926-935` 大类色空心描边 + 右上「?」角标，用的是分类色 |
| 分类点可能回退到 Accent | 一律走 `category/*`，无一处例外 |

即 [39] 那一栏「优先考虑收窄 Accent 职责而非换色」的建议，**在代码里已经事实完成了**，只是 PRD 文案没跟上。两橙实际永不共处同一视觉层（Accent 只在发布流与 FAB 层，车辆橙只在 Pin 与卡片分类条），1.11:1 的对比度不构成实际干扰。故决策为**不换色**。

也排除了换色方案：`color-preview.html:141` 备的鸭喙黄 `#FFB020` 与警告色 `#F59E0B` 更近，会把冲突从「橙-橙」平移到「黄-黄」，得不偿失。

**PRD 改动 4 处**：
- **§1.4.2 行 119**：Accent 用途从「辅助强调、分类点、需求态」改为「AI 相关控件专用（"AI 猜"角标、"AI 帮我发"FAB）」；
- **§1.4.2 新增「Accent 职责边界」段**：写明色相冲突的事实、三条约束（唯一用途 / 需求态不占颜色维度 / 分类点走 `category/*`）、以及一条前置检查——**新组件要用 Accent 先确认不与车辆分类色同屏**。不写理由的话，后人只看到 Accent 仅两处用途，很可能又拿去做「辅助强调」；
- **§6.13 行 1152**：「AI 帮我发」FAB 从「紫+发光效果」改为 Accent 底 + 投影，并注明**不用紫的原因**（紫 `#8B5CF6` 已被房屋分类色占用）。原文与 `code.js:3024` 的实现直接矛盾，代码是对的；
- **§1.4.3 新增「Token 键」列 + §13 数据字典回指**：车辆类键名是 `cat-vehicle` 而非 `cat-car`，中文名「车辆 Car」会误导按英文名推导。§13 行 1911 原写「`color` 取五大类色」，拿不到键名，已改为回指 §1.4.3 该列。

**硬编码色值副本盘清（本轮只标注，不改代码）**：真源是 `code.js:22-48` 两张常量表，Figma 侧靠 `ensureVariables()` 的值对齐自动同步全画布；以下副本改色时需**手工同步**：

| 文件 | 位置 | 性质 |
|---|---|---|
| `prototype-figma/ui.html` | 12, 14 行 `#0B7C8C` | **活文件，需同步**。插件面板自身样式，拿不到 Figma Variables，技术上无法 Token 化 |
| `docs/gen_prd_v2.1_docx.py` | 137, 248 行 `0B7C8C` | **活文件，需同步**。Word 交付件表头底色/边框 |
| `prototype-figma/color-preview.html` | 130-146 行五候选 + 180-185 分类 Pin | **评审存档**，主色已定稿，不再改 |
| `docs/ideation/2026-08-24-...-ideation.html` | 10 行 CSS 变量 | **交付存档**，值与现行一致 |
| `docs/ideation/2026-08-26-uiux-design-ideation.html` | `--brand:#0B7C8C` CSS 变量 | **交付存档**（条目 [48]），值与现行一致 |
| `ideation/找鸭找v2-需求深度还原-...html` | 9 行 `--brand:#1FBAD6` | **死稿**。已核实：该值是 2026-08-23 淘汰的旧主色（对比度 2.32:1），属历史存档，**刻意不改** |
| `prototype/index.html` / `test.html` / `pathDetail.html` | `--secondary-color:#F97316` | **死稿**。早期 HTML 原型，语义与现体系不同 |

**沉淀的原则（本轮新增一条）**：
- ⑭ **待办清单里的「已识别隐患」必须回代码复核前提，不能直接照着描述动手**。本条隐患在 [22] 登记时描述准确，但此后代码演进已使前提失效，清单未同步。若照描述去改 Accent 色值，会为一个不存在的问题付出全画布改色的代价。**凡跨了多个工作日的待办项，动手前先验一遍前提是否还成立。**

**过程中自查出一处错误**：我在 §1.4.3 注解里先写了「§14 数据字典」，Grep 核实后实为 **§13**（`PRD.md:1823`）。已改。这与 [39] 记录的两处引用错误同源——**章节号一律 Grep 验证后再写，不凭印象**。

**[42] 鸭子 IP 造型定稿：方向 4B「负形留白 + 同心波纹」，并把 PRD §1.4.1 从 4 行扩写为可外包规格书**。只改 `docs/PRD.md`，未动一行代码。

**选型过程**：先出 v1 四个纯造型方向（圆润写意 / 几何构成 / 单线勾勒 / 负形留白），用户追加「要有信息共享元素」后补出方向 5–10（双鸭对望、鸭头+箭头、鸭头+波纹、定位针中鸭、鸭喙即节点、圈中双点）。用户选定**方向 4（负形留白）**为基底，要求在其上叠加共享语义，遂在同一负形语言内派生 4A–4D 四个变体，最终定 **4B**。预览页：`prototype-figma/ip-direction-preview-v2.html`（含全部 10 + 4 个候选与逐项风险标注）。

**定 4B 的决定性理由不是观感，是「可降级」**：

| 变体 | 叠加物 | 能否降级 |
|---|---|---|
| 4A 双向箭头 | 加部件 | ✗ 删箭头则语义归零，等于维护两套形 |
| **4B 同心波纹** | 加部件 | **✓ 弧是可减部件，删弧即退回纯负形喙，主体不变** |
| 4C 双喙镜像 | 只改构图 | ✗ 24px 下中缝被抹平，比 4B 更早失效 |
| 4D 双点连线 | 加部件 | ✗ 细连线微缩必断，且三部件已触「禁止复杂叠加」 |

4B 使全产品只有**一个符号 + 三个繁简档位**，而不是多个 Logo，正好承接上一轮已定的「一个基础符号 + 一套可叠加语义元素」规则。附带优势：实心色块天然满铺，直接满足 iOS 图标不允许透明通道的要求，无需为商店图标另做一版。

**PRD 改动（2 处，共新增 4 个子章节）**：

| 位置 | 改动 |
|---|---|
| §1.4.1 表格 | 新增「造型方向」行，指向下方规格书 |
| §1.4.1.1（新） | 造型定稿 + 三条决策依据 + **叠加铁律**（后续元素只能沿用负形切入色块内部，禁描边、禁外挂、禁第三类部件） |
| §1.4.1.2（新） | **三档位降级规则**：完整版 ≥96px 两道弧 / 精简版 40–96px 一道弧 / 微缩版 ≤32px 弧全删；喙负形最窄处 ≥ 2px @1x |
| §1.4.1.3（新） | 几何基准 8 项（画板 1024、安全区 83%、圆角 22.4%、弧宽 4%、单色版、深色底反相、SVG 唯一真源、动效仅缩放+透明度） |
| §1.4.1.4（新） | **App 商店图标规格**，明确标为上架阻塞项：iOS 1024 无透明通道 / Android 分层前景居中 66% / 通用 512 |
| §1.7 素材清单 | 原「SVG 矢量（Logo、启动图、空状态、缺省图）」一行，改为 **5 件明细**；按用户裁定**空态与缺省图合为一件**（共用一张只换文案），并补入 App 商店图标 |

**过程中自查出两处自己写错的地方，均已修正**：
- 把色块圆角写成「取 `radius/lg` 同源比例」——核 `code.js:64` 实为 `RADIUS = {sm:4, md:8, lg:12, xl:16}` **固定像素值**，用于按钮卡片，与图标按边长取百分比毫无关系。这是**臆造关联**，已改为明确声明「此比例独立于 UI 组件圆角体系」；
- 登录页引用写成「§4」——Grep 核实实为 **§3.4.1**（`PRD.md:463`）。又是凭印象引章节号，第三次犯（见 [39]、[41]）；
- 另有一处内部矛盾：先写「共 4 件」却列了 5 条，已改为 5 件。

**校验**：正向 `4B|负形|同心弧|微缩版|自适应图标|上架阻塞项` 命中 16 处全在预期位置；反向探针 `SVG 矢量（Logo、启动图` / `radius/lg 同源` / `登录页顶部（§4）` 三个旧表述**全部 0 命中**。

**仍未闭环的依赖**：`说明文档.md:676` 登记的「鸭子 IP 三件素材全缺」**性质已变**——从「无规格也无素材」变为「**规格齐备、待矢量绘制**」。规格书已足以外包或自绘，但 `code.js` 三处代码缺口（`:2435` 登录页无鸭符号、`:851` Tab 中键用 emoji `＋` 顶替、启动页画框整体缺失）仍需素材到位后才能补。预览页里的文生图**不可用作交付素材**（版权未确认 + 位图非 SVG）。

**[43] 中性/语义色定稿：全色板首次全量对比度校验，测出 6 处违规并修完**。这一项原以为「只需确认」，实测发现 PRD 此前**只验过主色一项**，其余 12 项从未校验。改 `docs/PRD.md` + `prototype-figma/code.js`。

**测法**：用 WCAG 相对亮度公式（sRGB 线性化 + 0.2126/0.7152/0.0722 加权）在 PowerShell 内实算，非目测非估算，逐对输出。

**测出的 6 处真实违规**：

| 组合 | 实测 | 性质 |
|---|---|---|
| `success`/`warning`/`error` 作文字 | 2.28 / 2.15 / 3.76:1 | ❌ 三色被当 h3 文字色用（`code.js:2710/2835/2844`），远不过 4.5 |
| 白字压 `error` 底 | 3.76:1 | ❌ danger 按钮 + 离线横幅，**直接违反 PRD 自己已有的硬约束**「凡填充+白字须 ≥4.5」——那条当初只验了 primary |
| `primary` 压 primary-light | 4.42:1 | ❌ `code.js:2810` 在用 |
| `text-secondary` 压 primary-light | 4.35:1 | ❌ 4 处在用（`996/2492/2805/3084`），差 0.15 |
| `text-placeholder` 压 primary-light | 2.29:1 | ❌ `code.js:2493` 承载「Scope 红线」真实信息，占位豁免不适用 |
| Pin 完整度角标 | 2.15–3.76:1 | ⚠️ 12px 且**无文字标签**，颜色是唯一载体 |

**处置：拆职责，不换色**（沿用 [41] 处理两橙的同一思路）：

- **新增 3 个 Token**：`success-text #15803D`(5.02:1)、`warning-text #B45309`(5.02:1)、`error-text #B91C1C`(6.47:1)。原三色降为**只作填充/圆点/描边**，`*-text` 专管文字与「白字压彩底」的容器；
- **浅青底文字规则**：`primary-light` 上只准用 `primary-dark`(6.7:1) 或 `text-primary`(13.21:1)，三种错误组合全部禁用；
- **Pin 角标**：不改色，判定为已有 2px 白描边补偿 + 完整度属辅助信息（列表/详情有文字版），约束「白描边不得去掉」；
- **placeholder / disabled 维持现值**（2.54 / 2.05:1），按 WCAG 1.4.3「非活动组件」例外豁免，但约束「占位符不得承载唯一必要信息」。

**代码改动（`code.js`，共 11 处）**：Token 表 +3；`danger` 按钮底与离线横幅底改 `error-text`；3 处彩色文字改 `*-text`；5 处浅青底文字改 `primary-dark`；两处写死的「13 项」计数改 16（`:247` 注释、`:1380` 色板画板标题），并在画板加一行 `-text` 用法说明。`node --check` 通过。

**PRD 改动**：§1.4.2 色板表 +3 行并给原三色标注「仅填充/圆点/描边」；新增 **18 行全色板实测表**（含 ✅/⚠️/❌ 判定与每个 ❌ 的替代取法）；新增「语义三色职责拆分」「圆点辨识补偿」「浅青底文字取色规则」「占位符与禁用态豁免依据」四段。

**过程中自我纠错一次（值得记）**：我先写下「圆点旁始终跟文字标签，故符合 WCAG 1.4.1」，随后读 `code.js:948-954` 发现**Pin 角标恰恰没有文字标签**——只有列表卡片内那个有（`:1761`）。断言若不核代码就会把一处真实缺陷写成「已豁免」。已改为分两种场景分别判定。**这是原则 ⑭ 的又一次应验：写进 PRD 的每个「因为…所以合规」都必须回代码验前提。**

**沉淀原则（新增一条）**：
- ⑮ **「已定稿」不等于「已验证」**。§1.4.2 色板 13 项标着定稿，但只有主色做过对比度实算，其余 12 项是凭观感取的 Tailwind 常用值。**凡带数值阈值的规范条款（对比度、尺寸、性能），要么附实测值，要么明确标注「未验证」**，不得让未验证项以定稿身份流入实现。

**校验**：正向 `'color/(success|warning|error)-text'` 命中 6 处全在预期位置；反向 `text\([^)]*'color/(success|warning|error)'`（即三色仍被当文字用）**0 命中**；所有 `primary-light` 容器内文字经逐段确认全为 `primary-dark`。

---

**[44] Figma 实机验证条目 [43] 落地情况：22 条连线全通、色板 16 项已入画布；过程中连续两次误判工具异常**。未改一行代码，纯验证。

**验证结论（全部为 MCP 实测值，非推断）**：

| 判据 | 实测 |
|---|---|
| 画框重画 | id 由 `308:*` → `314:*` → `320:*`（用户共重跑三轮），41 框齐 |
| 起始画框 | `login-screen`（`320:46454`）`isStartFrame: true` |
| 语义色板 16 项 | `_swatch-` 命中 16 个，含 `_swatch-success-text` / `warning-text` / `error-text` |
| 新增用法说明已画出 | 文本节点内容与 `code.js:1381` 逐字一致 |
| Variables | 21 个 COLOR 变量全在 `ZhaoYaZhao Tokens` 集合 |
| **原型连线** | **22 / 22 全部命中且目标画框正确**（逐条 `list_reactions` 核过）|

**两次误判，教训是同一条**：

① **把 `export_interactions` 的扫描盲区当成「连线全断」**。该工具查五大 P0 页返回 `interactions: []`，我据此断言批次 5 没跑、22 条跳转全断，并让用户重跑了一遍。实际它**只扫画框自身的 reaction，不下探子节点**，而所有连线都挂在 `_nav-bell` / `row/设置` / `btn/primary/发布` 这类嵌套节点上（见 `code.js:3283` `findClickable` 用 `frame.findAll` 深搜），所以它对本项目**永远返回空数组**。

② **把网络抖动当成「工具能力边界」**。`list_reactions` 查 Instance 子节点 id（形如 `I320:44415;320:44009`）连续 4 次 10 秒超时，我写下「是工具对该 id 格式的解析限制」。下一轮同样格式一次就通——真因是 Figma 连接瞬时不稳（那段时间普通 id 也超时过一次）。

**沉淀原则（新增两条）**：
- ⑯ **工具返回「空/异常」时，先证明工具能力覆盖了目标，再谈结论**。`validate_prototype`（原则 ⑩）与 `export_interactions` 已两次踩同一个坑：都是「读得到但读不全」，输出合法却不完整，最容易被当成有效否定判据。**凡用工具的「无结果」下否定结论前，必须先用一个已知为真的样本反测该工具能否命中**——本例中只需先查一个明知有连线的节点，就能立刻暴露它不下探子节点。
- ⑰ **工具报错先重试排除偶发，再判定为能力边界**。把偶发故障写成「工具限制」，代价是此后永久绕开一条本来可用的验证路径。**连接类报错（超时、连接失败）至少隔一轮重试，且换一个同类样本交叉验证后，才允许写进文档当结论。**

**正确的连线验收判据（替代 `export_interactions`）**：`find_nodes({query: 触发节点名, scope: 'page'})` 定位节点 → `list_reactions({nodeId})` 逐条核 `destinationName`。逐条核 22 次虽慢，但每条都有实测输出。

---

**[45] 按 4B 规格用代码生成 12 个 IP SVG 素材并接入 `code.js` 三处消费位；新增启动页，连线 22→23 条**。改 `prototype-figma/assets/`（新建，12 文件）+ `prototype-figma/code.js` + `docs/PRD.md` §1.7。

**为什么能做**：条目 [37] 的 ideation 曾把「补齐鸭子 IP 素材」判为 Rejected，理由是「依赖外部美术产出，不在生成器可控范围」。但 4B 定稿（条目 [42]）后规格已**纯几何化**——画板 1024、栅格 64、圆角 22.4%、弧宽 4%、内外弧半径、三档降级规则全部落成数值，于是可直接写出 SVG，不需美术手绘。**路径是：先把规格逼到可计算，再谈能否自动化**。

**交付物（12 SVG，明细见 PRD §1.7 表格）**：符号本体 5 版（full / compact / mini / inverse / mono）+ Logo 组合 2 版（横排 804×240、竖排 504×384，单符号版复用 full 不另建文件）+ `splash.svg`（390×844 对齐 `CANVAS`）+ `empty-state.svg`（240×240，**刻意不含文案**，四处消费方各自注入）+ App 图标 3 件（iOS 无 `rx`、Android 背景/前景分层）。

**三处代码消费位（`duckSymbol()` 工厂，`code.js:628`）**：

| 位置 | 尺寸 | 自动落档 |
|---|---|---|
| 启动页 `buildSplash()` `:2558` | 160px | 完整版（两道弧）|
| 登录页品牌位 `:2583` | 96px | 完整版（边界值 ≥96）|
| 底部 Tab 中键 `bottomTabRaw()` `:962` | 24px | 微缩版（弧全删，只留负形喙）|

**四个刻意的设计决策**（全部写入代码注释）：
- **档位由尺寸单向推导，不开放给调用方**。PRD §1.4.1.2 明令「不得各档位另画新形」；若开放档位参数，就可能在 24px 位置传完整版，两道弧糊成一团；
- **不复用 `svgIcon()`**。它把整个 SVG 统一染单色，会让白色负形鸭喙直接消失；
- **按 `findAll` 文档顺序而非图层名区分色块与负形**。初版用 `<rect id="duck-block">` + `v.name === 'duck-block'` 判定，后意识到 Figma 对 SVG `id` 属性的命名处理**未见官方文档保证**，改为按元素顺序（`j === 0` 即色块）；
- **加矢量数断言** `expected = 2 + arcs.length`，不符即抛错。防止 `createNodeFromSvg` 导入结构被改写时，产出一个「负形消失的纯色圆角方块」悄悄上屏——那种失败**看起来像正常图形**，肉眼走查抓不住。

**离线验证方法（新建立）**：整份 `code.js` 顶层有 `figma` 调用，无法直接 require。写临时探针 `probe-duck-symbol.js`：正则从 `code.js` 抽出 `duckSymbol` 及其 4 个依赖常量 → 给 `figma.createNodeFromSvg` 打桩（记录喂入的 SVG 并按元素数造假矢量节点）→ `new Function` 注入运行。**20/20 全通**（跑完即删脚本）：档位边界 7 项（160/96→2 弧，95/40/33→1 弧，32/24→0 弧）、与 assets 静态文件几何逐字一致 5 项、染色分工 2 项、**反向探针 3 项**（强制矢量数为 3 时确认真的抛错且错误信息含实际值，三档正常调用均不抛错——证明断言不误伤）。另用 .NET `XmlDocument.Load` 逐个验 XML 合规 12/12。

**两处色值凭印象写错（自查发现）**：`empty-state.svg` 装饰环写成 `#E0F2F4`、`splash.svg` Slogan 写成 `#64748B`。读 `code.js:22-43` 真值后发现 `primary-light` 实为 `#E6F6F8`、`text-secondary` 实为 `#6B7280`。**这是「不凭印象引用真源」的第四次应验**（前三次都在章节号上）。修正后实测对比度：`text-secondary on white = 4.83` ✅ / `primary on white = 4.91` ✅ / 装饰环 `1.11`（WCAG 1.4.3 装饰例外豁免）。

**PowerShell 踩坑**：写对比度校验函数 `function R($a,$b)` 后调用报「Invoke-History : 找不到接受实际参数"#FFFFFF"的位置形式参数」，四条全输出空值。真因是 **`R` 是 `Invoke-History` 的内置别名，优先级高于自定义函数**。改名 `Get-Lum` / `Get-Ratio` 后正常。

**新增沉淀原则**：
- ⑱ **被判 Rejected 的想法要随前置条件变化复评一次**。「依赖外部美术」在 4B 定稿前是真阻塞，定稿后就不再是了。**Rejection 的理由绑定的是当时的前置条件，不是结论本身**；ideation 的拒绝列表应在每次重大定稿后回扫一遍。
- ⑲ **断言要防的是「看起来正常的失败」**。矢量数断言的价值不在于捕获报错——报错本来就会暴露——而在于捕获「负形消失但仍是个漂亮色块」这类**静默降级**。凡自动生成的图形资产，都要找出一个「失败后仍视觉自洽」的场景并为它写断言。

**已知隐患（活文件）**：`code.js` 内联了与 `assets/*.svg` **逐字一致**的几何数据（喙路径、`rx=229.38`、`A326.92`/`A429.32`、弧宽 40.96），因 Figma 插件沙箱无文件系统读取能力，无法运行时读 SVG。**这是第三处双份副本**（前两处：`ui.html:12,14` 与 `docs/gen_prd_v2.1_docx.py:137,248` 硬编码 `#0B7C8C`）。任一处几何改动须同步两处，已写入 PRD §1.7 约束段。

**待验证（须用户在 Figma 重跑）**：批次 6 清空 → 1→2→3→4→5 顺序整体重跑（依条目 [27] 警示，不可只跑局部）。验收四点：**42 框**（原 41 + splash）/ **23 条连线**（原 22 + 启动页→登录）/ 起始点 = `splash-screen`（保留 `login-screen` 兜底，防启动页未生成时 Present 从画布随机首框开始）/ 三处 IP 符号档位正确。真机上启动页是 1.5s 自动转场，Figma 原型无定时触发能力，故降级为 `ON_CLICK`。

---

**[46] 抽出 `CONTENT` fixtures 层，修掉 6 条编造的类目路径**。改 `prototype-figma/code.js`。

**范围为什么从五条缩到一条**：本轮先明确了交付链——**原型 → 设计师 → 开发**，三棒交接，稿子的角色是「规格清单：别漏了」。据此复盘 ideation 六条想法，发现四条对这个目标没有实质贡献：
- 想法 3（batchHifi 独立 Page）：设计师打开文件看到两个 Page，**哪个是要照着做的？**「并存」在工程上是安全措施，在交棒时是歧义源；
- 想法 4（阴影 Token）+ 想法 5（动效表驱动）：视觉精修与动效**正是设计师的本职**，我们做了她也会推翻重做；
- 想法 6（状态矩阵自动建框）：先数了一遍才发现 —— **42 框里已有 22 个状态变体**，PRD 要求的空态/降级/权限/失效基本都在，「别漏了」这条其实已经做到了。矩阵化只是给已完成的事补一套脚手架。

**真正的漏洞在假数据**，而且比预想严重。ideation 只提「文案抽成数据」，实际排查出**类目路径全线造假**——6 处路径在 `CAT_TREE` 里一条都不存在：

| 位置 | 原写法 | 真值 |
|---|---|---|
| 演示主线（8 处直写） | 工作 › **餐饮** › **帮厨** | 工作 › 全职招聘 › 餐饮服务 |
| 列表卡·房屋 | 房屋 › **租房** › **整租** | 房屋 › 求租/找房 › 个人求租 |
| 列表卡·车辆 | 车辆 › **货运** › **小货车** | 车辆 › 租车/借车 › 货车出租 |
| 列表卡·服务 | 服务 › **维修** › **水电** | 服务 › 维修/安装 › 水电维修 |
| Marker 信息卡 | **闲置转让** · 面议 · 可自提 | 二手闲置转让 › 母婴儿童 |
| 级联选择器 L2 | **餐饮/零售/工厂/物流/家政** | 全职招聘/兼职临时工/求职找工作 |
| 冲刺区三级候选 | **帮厨 / 洗碗 / 传菜** | 餐饮服务 / 零售导购 / 家政保洁 |

**危害路径**：设计师照着画类目选择器 → 开发照着设计稿写 → 联调才发现类目对不上。**一处假数据，两棒之后才爆。**

**做法**：新增 `CONTENT` 常量（6 个条目：`job` 演示主线 / `listExtra` 三张列表卡 / `marker` 信息卡 / `favoriteHouse` 收藏页房屋）+ `catPathLeaves()` 与 `contentCatPath()` 两个校验函数。11 个消费点全部改为引用 fixtures。

**三个刻意的设计决策**：
- **演示主线选「工作 › 全职招聘 › 餐饮服务」，因为它三层都是 `CAT_TREE` 各自列表的首项**，而级联选择器恰好把 `i === 0` 渲染为选中态 —— **真数据与选中态天然自洽，不必给选择器额外传参指定高亮位**；
- **`FLOW_LINKS` 的触发点名改为 `'_item/L3/' + CONTENT.job.l3`**，不再硬编码。这是最危险的耦合：若 `CAT_TREE` 按 PRD 调整而 fixtures 没跟上，选择器渲染出的三级项不再包含 `l3`，批次 5 会**静默把这条连线记入 `skipped`** —— 画布看起来完好，只是那一跳点不动；
- **校验挂在批次 2/3/4 的第一行**，在 `loadFonts()` 之前。让「fixtures 与 `CAT_TREE` 脱节」在任何节点落地前就炸掉，而不是先铺半个画布再报错、留一堆要手工清的残留；
- **`favoriteHouse` 不复用 `listExtra` 的房屋条目**：那条是「需求」（求租），而收藏页 segTab 停在「资源」页签，卡片供需属性必须与页签一致，否则设计师会以为「资源」页签下也能出现需求卡。

**离线探针 13/13 全通**（跑完即删）：正向 5 项（默认不抛错 / 返回值 = 全职招聘三级列表 / 演示主线三层均为首项 / 6 条 `path` 串自洽 / 冲刺区取前三项）+ **反向 6 项**（`l3`、`l2`、`catKey`、`path` 串脱节、列表卡条目、Marker 条目 —— 逐个造假确认真抛错且错误信息含实际值与可选值）+ 静态 2 项。

**探针抓到了我漏掉的两处**：写完 9 个消费点后自认为改完了，静态检查却报「房屋 › 租房 › 整租」「服务 › 维修 › 水电」仍在。原来 **`buildMyFavorite` 里还有两张卡**用同一批假类目，我此前的 Grep 只搜了「帮厨」这条主线，没搜其余三条。**探针替我发现了搜索范围的盲区**——这比它验证已知逻辑的价值更大。

**探针自身也写错过一次**：初版静态检查直接搜全文，把注释里刻意保留的「原先写的是什么」历史记录也判为残留。修法是先剥注释再查，并**补一条反向检查确认这些旧文案确实还在注释里**——防止后人把改动理由当垃圾清掉。

**新增沉淀原则**：
- ⑳ **交付链的下一棒是谁，决定哪些维度值得做**。同一份「四维全补」，下游是开发时四维都值钱，下游是设计师时视觉与动效两维**大概率被作废**。定维度之前先问「谁接、他能自己补上哪些」。原则 ⑱ 说被拒的想法要随前置条件复评，这条是它的反面：**被通过的决定，前提变了也得复评**。
- ㉑ **静态检查要覆盖「我以为改完了」的范围**。逐个消费点手改完，很容易漏掉当初搜索关键词没覆盖到的同类问题。用一条「禁用文案不得出现在可执行代码中」的全量检查兜底，比再 Grep 一遍可靠。

**待验证**：本轮改动与条目 [45] 一并重跑（批次 6 清空 → 1→2→3→4→5）。新增验收点：级联选择器三列显示真实类目、点第三列首项「餐饮服务」能跳到发布页。

---

**[47] 用户发现「批准的是 4B、交付的是另编几何」；建 `bitmap2svg` MCP 从源位图取回真几何，12 个素材全部重做**。改 `prototype-figma/build-4b-assets.py`（新建）+ `assets/` 12 个 SVG 全量重写 + `code.js` 几何块 + `docs/PRD.md` §1.4.1.1–§1.4.1.4/§1.7；新增 MCP server `D:\ai_project\mcp\bitmap-to-svg\`。

**用户的原话是「splash-screen 页面的图标并不是 4B 呀，哪里出错了」。** 他在 Figma 实机看到的图标，与他一周前在预览页里点头的那个，不是同一个东西。

**偷换发生在哪里**：用户批准的 4B 是 `ip-direction-preview-v2.html:155` 里的一个 `text_to_image` 链接——**一张图**。条目 [45] 我要把它变成 SVG，做法是**照着文字 prompt 另编一套几何**，写成 `DUCK_BEAK_PATH = 'M255 415 Q390 435 490 470 A55 55 0 0 1 490 580 Q390 615 255 610 Z'`。看到真图后的差距比预想大得多：

| 维度 | 用户批准的（源图实测） | 条目 [45] 交付的 |
|---|---|---|
| 中心 | 白色**圆盘** R=227，盘内挖出**完整鸭头剪影**（圆头/朝右喙/向下颈）+ 白眼点 | 一块「左直边 + 右半圆」的横向舌状块，**无头无眼** |
| 外围 | 两道**完整同心圆环**（闭合环） | 两段**半圆弧**（`stroke`，弧心还偏在 (300,512)） |

**为什么 22 项探针一条都没拦住**：探针查的是「SVG 语法合法」「弧数 = 档位」「`code.js` 与 `assets` 逐字一致」。而 `assets` 里躺的是**同一条我编的路径**——两份副本互相印证，等于没验。**从没有一项探针问过「它看起来像鸭子吗」。**

**第一次修复尝试失败**：我手搓了三版候选（宽缝双颌 / 厚缝双颌 / 鸭头剪影）写成对照页。用户答复「**画的偏差太大了，如果搞不定，就把需求说清楚，并且提供实现路径**」。这是本轮最有价值的一句反馈——它要求我**承认能力边界**而不是继续试。我确实画不出鸭喙，靠手写 path 数字逼近一张有机形状的图是错的路径。

**转向：建工具，而不是继续手画**。我先说「没有 image2svg」，用户直接截图证明 UI 设计专家 agent 里早就勾了——**我只扫了当前会话的启用列表和配置目录，没考虑 agent 级配置**。几轮工具试错后，用户给出 FastMCP + cv2 的原型代码，让我按 `skills:mcp-builder` 正式建一个。相对原型我做了三处改进（都不是美化，是修正确性）：

| 原型 | 改法 | 不改会怎样 |
|---|---|---|
| `RETR_EXTERNAL` | `RETR_TREE` + 按 `hierarchy` 嵌套深度奇偶分外轮廓/孔洞 | 鸭眼、镂空全丢 |
| `svgwrite.Polygon` 折线 | Catmull-Rom 转三次贝塞尔，按转角阈值在角点断开切线连续性 | 曲线全是锯齿，喙尖被抹圆 |
| `cv2.imread` | `np.fromfile` + `imdecode` | 中文路径直接返回 None |

同时去掉了 `svgwrite` 依赖（手写 SVG 文本），三个工具全部带 `annotations`。**自检 27/27**（含 9 条反向：逐个造假确认真抛错且错误信息含实际值与可选值）。

**配置踩了两个坑**：`command: "python"` 会让 Trae 走**自己进程的 PATH**，找到一个没装 cv2 的解释器（必须写解释器绝对路径）；改完文件 Trae 仍读缓存（必须完全重启）。

**几何提取的关键判断：解析几何优于矢量拟合**。MCP 描出的鸭头是 43 段贝塞尔，很干净。但环、盘、眼点我**没有**用矢量化结果——实测它们在源图中圆度 0.89–0.95，说明本该是完美圆，矢量化只会把位图锯齿记录成抖动曲线（第一次描眼点得到 40+ 段抖动路径）。最终方案：**环/盘/眼点用解析圆重建（拿到 PRD 要求的精确几何基准），只有鸭头这个有机形保留贝塞尔**。

**这一轮我又犯了同一个毛病，而且是在刚被指出之后**。写了个逐行扫描脚本测「喙颈缺口宽度」，测出 8.98px（占画板 0.88%），据此判定「全尺寸违规」并让用户做决策，用户选了「加宽到 2.3%」。然后形态学加宽怎么试都无效（腐蚀 k=1..19 缺口卡在 10px 不动——因为腐蚀让图形整体缩小、缺口跟着缩小，我又按包围盒放大回去，两个效果相互抵消）。**卡住之后我去 `Read` 了那张抠图**，才发现凹口肉眼看就很宽，9px 根本不是它。换方法重测（凸包差集定位凹陷区 + 距离变换测局部宽度）：**实宽 133.86px，占画板 13.07%，24px 下 3.14px，全档达标**。逐行扫描量到的 9px 是喙尖的锯齿噪声。**两次测量差 15 倍，我用错的方法测量，然后相信了输出的数字。**

**看图这件事，本轮抓到了 4 个只看数字发现不了的问题**：
1. 源图真容（完整圆环 + 圆盘 + 挖空鸭头）——只读 prompt 文字永远不会知道；
2. `duck-symbol-inverse.svg` 渲染**全白**：`bg=WHITE` 时我以为图形会自动变品牌色，但 `disc_with_duck` 里颜色硬编码 `WHITE`；
3. `app-icon-android-foreground.svg` 渲染**全空**——这个是架构性错误：我按「镂空鸭头透出底色」做前景层，但 **Android 前景层叠在背景层之上，镂空处透出的是背景层的品牌色，与鸭头本该有的颜色相同 = 鸭头不可见**。改为白盘环 + 品牌色实心鸭头，不依赖背景层；
4. `logo-horizontal` / `logo-vertical` 排版全乱——但这次**是我的光栅化器错了**（不认 `<g transform>`、不认 rect 的 `x`/`y`、假设 viewBox 是方形），SVG 本身没问题。修渲染器而非改素材。

**档位阈值由实测反推而非拍定**：原定 96/40/32 在实测下不成立（眼点在 40px 仅 1.58px）。用户选「抬高阈值 96/64/40」，正好覆盖实测——96 和 64 全过；mini 档 40px 只剩眼点不够，把半径从占画板 3.96% 放大到 5.00%（r=25.6）后正好 2.00px。PRD 原文允许「加粗」但禁止「改成描边或换形」，**只改半径不动位置与造型**，合规。

**最终状态**：`probe-assets-verify.py` **26/26**（12 项渲染非空 + 与源图中心区 IoU=0.9015 + 三档位 2px 硬约束 8 项 + 环数 3 项 + Android 安全区 + iOS 无 `rx`）；`probe-duck-inline.py` **10/10**（`code.js` 内联几何与真源逐字一致 + 三档位复现 **IoU=1.0000**）。两个探针都输出 PNG 且已人工过目。

**新增沉淀原则**：
- ㉒ **验证必须验产物本身，不能验副本间的一致性**。「A 与 B 一致」在 A、B 同源于一个错误时永远为真。凡是能写成「与另一份数据相同」的检查项，都要再补一条**直接检查产物属性**的（渲染成图、量实际像素、与原始输入比对）。
- ㉓ **测量方法本身需要验证**。同一个缺口两种方法测出 9px 和 134px。得到「异常但说得通」的数字时，先换一种独立方法复测，再决定要不要动手改。**我这轮拿错数字让用户做了一次决策，这是最不该发生的事。**
- ㉔ **审批的载体决定了交付的可复现性**。用户批准一张 `text_to_image` 图时，批准对象是那次渲染结果，而链接每次请求都变。**必须立刻下载存盘**（`assets/source/ip-4b-source.png`），它才成为可追溯的真源。凭 prompt 文字复述来重建，等于重新创作。
- ㉕ **画不出来就别硬画，去建工具**。手写贝塞尔逼近有机形状不是「再试一次」能解决的能力问题。用户的「搞不定就把需求说清楚，并且提供实现路径」比任何一版候选都有价值。

**待验证**：Figma 重跑（批次 6 清空 → 1→2→3→4→5）。验收点在条目 [45] 基础上新增：**三处 IP 符号必须是「圆盘 + 鸭头剪影 + 眼点」形态**（24px 处无环、160px 与 96px 处两环），不再是舌状块 + 半圆弧。

### [47] 2026-08-26 底部 Tab 三键去 emoji，微缩档鸭子改结构（用户实机反馈）

**用户反馈**：Figma 跑完后指出「导航栏『鸭圈』图标不是很美观」。

**查因发现问题不在选形，在载体**：「鸭圈」用的是 emoji `🗺️`、「我的」用 `👤`，都是 `code.js` 里明确记着的遗留。emoji 由系统字体渲染，三端字形差异明显，字重与色彩**不受 `paintOf(role)` 控制** —— 截图实测 `👤` 是紫色 3D 立体人像，选中时旁边文字变了主色而图标不变，和同排的矢量鸭子风格断裂。

**用户的三条答复决定了做法**：「我需要看看图标效果才能决策」/「都要给我看候选图标效果，要换成 svg」/中键鸭子「一并调整」。于是没有直接改代码，而是先做了 `tabicon-preview.html` 对照页 —— 四段：鸭圈三候选、我的 emoji vs 矢量、中键鸭子三候选、推荐组合三种选中态。**页面里所有路径与最终写进 `code.js` 的逐字相同**，浏览器所见即 Figma 所见，用户可直接凭页面定稿。用户选了 `map`（折叠地图册）。

**候选是靠看图淘汰的，不是靠数字**：`travel_explore` 在 24px 下放大镜与地球经纬线粘连成不可辨色块，`account_circle` 内部头像与圆底边缘粘连 —— 两者都是 `Read` 那张 24px/48px 对照图才发现的。

**中键鸭子的问题比 Tab 图标更深**：旧 mini 档沿用「圆角块 + 白盘 + 盘内镂空鸭头」四层结构，白盘直径只占画板 44%，24px 下折算约 10px，鸭头要在这 10px 内表达喙与颈，喙尖必先糊掉。改法是**层次减法而非重绘**（PRD §1.4.1.2 禁止各档位另画新形，路径数据仍是同一条 `DUCK_HEAD_PATH`，只换缩放中心与倍率）：圆盘铺满画板、鸭头由**镂空反相为白色实体**、眼点随之改为**主色**。

后两点各有一条硬理由：**前景层不能靠镂空表达形状**，镂空透出的是底色 —— 这与条目 [45] 里 `app-icon-android-foreground` 渲染全空是同一个错，第二次遇到；**鸭头已是白色，白眼点会与鸭头融为一体**。

**染色映射必须显式化**。原 `duckSymbol` 靠「首元素是色块、其余是负形」的位置约定分配颜色，mini 档反相后眼点要染色块色，约定失效。改为 `roles` 数组按文档顺序逐元素记录，两种结构走同一段染色代码。

**「烧进坐标」而不是用 `<g transform="scale()">`**。第一次渲染 mini 档得到空图，因为我的光栅化器只认 `translate`。但我没去扩展渲染器 —— Figma 的 `createNodeFromSvg` 对 transform 的支持未见于官方文档保证（已知会剥离 defs/渐变/蒙版），纯坐标是任何解析器都一致的几何数据，且能让探针与最终产物走**完全相同的数据**。

**包围盒必须用渲染掩码实测**。我最初在 HTML 里写了估的 `{x:336,y:385,w:358,h:308}`，随即自我否决 —— 贝塞尔控制点常落在轮廓外侧，按路径数字取极值会得到偏大的包围盒、偏小的缩放倍率。实测得 `x=328 y=376 w=366 h=317`。算完 scale 还要**重新渲染复测占比**（0.717，目标 0.72），只看 scale 数字不算验证。

**探针拦住了我的改动，这次是探针价值的正面案例**。删掉 `DUCK_EYE.rMini` 后 `probe-duck-inline.py` 立刻报「结构不符」。我没有放宽探针 —— 它暴露的是更深的问题：`code.js` 的 mini 档已与真源 `assets/duck-symbol-mini.svg` 不一致，违反「assets 为唯一真源」。**正确的修法是改真源生成脚本 `build-4b-assets.py`，让 code.js 去对齐真源**，而不是让守门人闭嘴。

**顺带查出一条假通过**。`probe-assets-verify.py` 里 `mini eye enlarged vs full` 用正则取文件里**第一个** `<circle>`，mini 档改版后第一个 circle 是铺满画板的圆盘（r=504）—— 断言拿圆盘半径跟眼点比，永远通过。改为按 fill 定位眼点后才发现真问题：mini 档实际用在 24px（不是原来假设的 40px），随鸭头变换所得的眼点 r=40.83 在 24px 下只有 1.91px，**不达 2px 底线**，抬到 r=42.67。

**同一原则第二次生效：探针的读取假设也会过期**。`probe-tabicon-geom.py` 复跑时报「真源里找不到『盘 + 鸭头』复合路径」—— 它按旧结构从 `duck-symbol-mini.svg` 里读复合路径，而该档已改成三层结构。这次同样不放宽断言：改为读 `duck-symbol-full.svg`（该档仍是「盘 + 镂空鸭头」复合路径，且两档鸭头本就同源于一条 `DUCK_HEAD_PATH`）。**换源后复跑，实测值与历史输出逐位一致**（包围盒 `x=328 y=376 w=366 h=317`、disc 版 `scale=2.0144`、变换后占比 `0.720`、眼点 `502.13/358.90/40.83`）—— 这一致性本身就是「换源没改变几何」的证明。顺带删掉它写出的 `_tabicon-geom.json`：定稿几何已固化进 `build-4b-assets.py` 与 `code.js`，留中间文件只会多一份要同步的副本。

**最终状态**：`probe-duck-inline.py` **16/16**（三档位复现 IoU 全 1.0000）；`probe-assets-verify.py` **31/31**；`probe-tabbar-verify.py` **12/12**（24px 实测最大内切圆 `map` 8.00px / 鸭子 8.39px / `person` 9.99px；三键选中态渲染均不同 —— 这是 emoji 做不到的，正是本轮改动的目的）；`probe-tabicon-geom.py` / `probe-tabicon.py` 两个产图脚本均复跑无误。三张对照图均已人工过目。

**新增沉淀原则**：
- ㉖ **emoji 不是图标**。它由系统字体渲染，跨端字形不一致、颜色不受主题控制、无法参与选中态。凡是要跟随语义色或参与状态变化的位置，一律矢量。
- ㉗ **需要用户拍形的决策，先造对照页再问**。用户说「我需要看看效果才能决策」时，正确响应不是描述几个方案，而是把真几何渲染出来。对照页里的路径必须与最终产物逐字相同，否则又是一次「批准的是 A、交付的是 B」（原则 ㉔）。
- ㉘ **探针失败时，先判断它拦对了没有**。默认假设是「探针对、改动错」。放宽断言前必须能说清「原断言在保护什么、为什么这个保护现在不需要了」。本轮探针拦住的是「双份副本脱钩」，正是它该拦的。
- ㉙ **「与另一份数据比较」的断言要检查取值路径**。`mini eye enlarged vs full` 长期是假通过，因为正则取的元素在结构变更后不再是眼点。凡是用正则/索引/位置约定取值的断言，结构一改就可能静默失效 —— 这是原则 ㉒ 的一个具体形态。

**已验证（8/26 用户实机跑通并验收通过）**：Figma 重跑（批次 6 清空 → 1→2→3→4→5）。验收点全部满足：**底部 Tab 三键全为矢量**（左「折叠地图册」、中「主色圆盘 + 白色鸭头」、右「实心人像」），无 emoji；切换 Tab 时**图标颜色随文字一起变主色**。

#### [47-b] 同日追加：mini 档鸭头由 72% 抬到 80%（用户「镂空的更好看」）

用户看 `tabicon-preview.html` 后说「中间发布按钮，我觉得这种镂空的更好看，如截图，改一下把」，截图指向的是页内「现状 · mini 档（圆角块 + 镂空鸭头）」那一段。

**先查证再改，问题被重定义了**。读 `code.js` 与真源后发现：现行版与截图那版**手法完全相同** —— 都是「主色圆盘 + 白鸭头」（`roles = [block, negative, block]`，白鸭头是显式染 `color/surface` 而非留空透底，正是「前景层不能用镂空」原则的落点）。真正差别只有一处：截图那版白盘仅占画板 44%，视觉上鸭头相对更大更满。所以用户要的不是「换手法」，而是「鸭头再大些」，落成一个参数：`build_mini_symbol` 的 `ratio`。

**我推荐 88% 是错的，这是本轮最重要的自我纠错**。新建 `probe-mini-negative.py` 实测后，我只看了 24px 下的笔画粗细（88% 时最大内切圆 10.38px，达标）就推荐 88%，用户据此选了 88%。随后补测才发现漏了真正的约束：**主色环带宽度**。环带是「这是一枚按钮」的体量感载体，24px 下不足 1px 就会断续。逐档实测环带最窄处：

| ratio | 环带（画板比例） | 24px 折算 | 鸭头占圆盘半径 | 判定 |
|---|---|---|---|---|
| 0.72 | 0.121 | 2.92px | 0.753 | OK |
| 0.76 | 0.101 | 2.43px | 0.795 | OK |
| **0.80** | **0.079** | **1.90px** | **0.839** | **OK（上限，采用）** |
| 0.84 | 0.059 | 1.42px | 0.880 | WARN |
| 0.88 | 0.039 | 0.93px | 0.922 | WARN（喙尖捅破圆盘） |
| 0.96 | 0.003 | 0.06px | 0.995 | WARN |

只看 span 比例不够 —— 鸭头是有机形，长边铺到 88% 时对角方向已顶到圆盘弧线，故 `head_clearance()` 改为实测「白鸭头像素到圆心的最大距离」与圆盘半径相比。补测后重新提问，用户定 **80%**。

**对照图的尺寸设错，才是我看走眼的根因**。首版对照图并列 24/48/96px 三格，48/96px 下 88% 的观感相当好，直接掩盖了 24px 的环带缺陷 —— 我据此推荐、用户据此选择，两边都看走眼。改为 `sheet_24()`：**只渲染 24px 实际使用尺寸**，再用 `INTER_NEAREST` 放大 12 倍看真实像素，84%/88% 两格的喙尖捅破与主色环断开当即可见。

**顺带推翻一个想当然**。我原以为「evenodd 真挖空」与「叠白鸭头染色」逐像素等价，故打算宣布无需改动。实测 IoU 只有 0.9863（ratio 0.72）/ 0.9871（0.88），差 1787/2118 像素，来自圆盘弧线与鸭头轮廓交界的抗锯齿。虽不影响观感结论，但「不实测就宣布等价」这个动作本身是错的。

**把真正的约束固化成守门人**。`probe-assets-verify.py` 报 `mini head spans ~72% of canvas [0.799]` —— 这次属于「断言基准值过期」（我改了 ratio 却没改它），是该放宽的情形。但不能只改数字：同时新增 `mini@24px brand ring >= 1.5px` 断言（实测 1.92px，与独立探针的 1.90px 互相印证），否则下次有人再抬 ratio 仍会重犯同一个错 —— 因为原来的断言体系里，**没有任何一条在守环带**。

**改动清单**：`build-4b-assets.py` 的 `ratio` 0.72→0.80（docstring 记录逐档实测数据与「上限由环带而非笔画决定」）→ 重生成 12 个 SVG → `code.js` 同步 `DUCK_HEAD_MINI`（1408 字符）与 `DUCK_EYE_MINI = { cx: 501.03, cy: 341.89, r: 45.37 }`，并改掉上方仍写「72%」的注释；PRD §1.4.1.2 与 §1.4.1.3 三处几何数字同步，新增「24px 下主色环带 ≥ 1.5px」硬约束。眼点半径随鸭头放大到 r=45.37（24px 下 2.13px），已自然超过 2px 底线，`EYE_R_MINI_FLOOR` 不再生效。

**最终状态**：`probe-assets-verify.py` **32/32**（新增环带断言）；`probe-duck-inline.py` **16/16**；`probe-tabbar-verify.py` **12/12**。

**新增沉淀原则**：
- ㉚ **决策载体的尺寸必须等于实际使用尺寸**。给用户看的对照图里多塞一档大尺寸，就等于给了他一个在真实场景里不存在的判断依据。24px 的图标只该在 24px 下评审，放大只能用 `INTER_NEAREST` 看像素，不能用平滑插值。
- ㉛ **约束要找对那个先崩的**。同一个参数往上抬时，最先崩的往往不是最显眼的那个指标。88% 时笔画仍有 10px（看着很安全），但环带只剩 0.93px 已经断了。扫描参数前先问「谁会先撑不住」，并把它测出来，否则守门人守的是一条永远不会触发的线。
- ㉜ **用户描述的是观感，不是实现**。「镂空的更好看」在实现层可能压根不是镂空 —— 先去真源里确认现状，再把它翻译成可调的参数，比按字面重做一遍手法要省得多，也避免把一个已经正确的结构推倒。

### [48] 2026-08-26 M3「UI/UX 设计稿」方向 ideation（ce-ideate，6 条存活）

**背景**：M3 计划 8/28 冻结 14 页视觉稿 + Design Token 落地，「待启动」🟠 第 1-2 周首项即 UI/UX 设计稿。启动前先做一轮方向发散，回答的是交棒问题：**08-28 要冻结的东西，怎样才能让设计师接得住、并且改完之后我们还认得出红线在不在**。

**范围由用户两项决策锁定**：① 新建工件而非续写 8/24 那份（上一份主题是「生成器 code.js 如何升级」，本轮是「组件库 + 14 页高保真视觉稿」，评审对象与交付棒次不同）；② 基线 = Figma 42 框骨架（结构清单）+ PRD §1 视觉系统 / §10.1 页面清单（规格真源）双基线。

**产出**：`docs/ideation/2026-08-26-uiux-design-ideation.html`。5 个 frame 并行发散 40 条 → 去重 26 条 + 组合 5 条 → 独立依据核验（fresh context 逐条抽查行号）→ 存活 **6 条**，覆盖四条正交轴：A1 规格的承载体 / A2 Token 完备度与出口 / A3 组件之间的关系 / A4 交棒契约与离场对账。

| ID | 方向 | 轴 | 信心 | 复杂度 |
|---|---|---|---|---|
| I1 | 规格从画布卡迁到节点上（改 `annotation()` 一个函数体，35 张卡整体换承载体 + 分级 + 可读回） | A1 | 82% | Medium |
| I2 | Token 权威源移到 DTCG，Figma 与 code.js 均降为投影；出口按 `dp` 直连 Flutter | A2 | 76% | Medium |
| I3 | 20 个平行 master 合成变体集，SLOT 承载结构差异变体（原则 ⑱ 复评生效） | A3 | 71% | High |
| I4 | 交棒第一屏做成可信入口：`ui.html` 计数运行时自报 + 契约写进组件 description | A4 | 88% | Low |
| I5 | 动效规格改走 annotation（Token 层承载不了），按语义分档而非替设计师做动效 | A1/A2 | 64% | Low |
| I6 | 08-28 冻结的对象从像素翻转为契约（**范围决策，须先拍**） | A4 | 69% | Low |

**本轮查出的四类事实缺口**（全部 file:line 级证据，已写入工件）：
- `annotation(title, lines)`（code.js:1170-1180）全身只有浅色框 + 标题 + 循环条目，**对被标注对象零引用**，35 张卡纯粹漂在画布上；且无 severity 维度，红线与提示视觉同构；
- 字号 6 阶 / 间距 6 阶 / 圆角 5 阶共 17 项仍是 JS 字面量（code.js:57-71），`ensureVariables()` 只查 `getLocalVariablesAsync('COLOR')`（code.js:260），Figma 侧 Token 落地率 21/38；
- 20 个 master 全为平行注册（code.js:1330-1356），全文检索 `combineAsVariants|createComponentSet|componentPropertyDefinitions` **零命中**，且全无 description；
- `ui.html` 四处计数全错：:24「14 页 + 3 模态」实为 18 主态 / :30「语义色 13」实为 16 / :42「等 13 页」实为 14 / :54「22 条跳转」实为 23。

**核验推翻了三条看似成立的候选**（记档以免重犯）：
- 「PRD §1.7 五件基础组件已是参数化函数，只差包一层」**不实** —— 全表仅 `card()`(1148) 与 `field()`(2718) 存在，标签 / 空状态 / 骨架屏三件全无对应函数，改动量被严重低估。已作为 I3 的风险写明；
- 「13 处 `DROP_SHADOW`」**不实**，实为 8 处（1107/1851/1876/1906/1967/2062/2133/3435）——这类计数不能凭印象；
- 「Marker 只表达两类信息」在 PRD:1183-1185，**不在**候选所引的 :1019-1032（该段是色彩/尺寸/聚合表）。

**两条硬约束在本轮首次查明**：① TIMING / EASING **无法经 Plugin API 创建为变量**，故动效规格只能落 annotation，不能进 Token 层（这是 I5 存在的唯一原因）；② `figma_diff_versions` **明确不追踪变量值变更** —— 有人把主色调深一档，版本 diff 里看不出来，而主色对比度实测 4.91:1 对红线 4.5:1，**余量只有 0.41**，Token 值这条腿必须单独记账。

**8/24 旧稿的部分想法已复评作废**：当时因「Instance 禁增删子节点」被砍的几条，其前置条件已因 `figma_create_slot`（schema 原文「more flexible than INSTANCE_SWAP」）出现而改变，相关部分以 I3 的形式重生。**旧稿不回改**（与 [41] 处理死稿的做法一致）。

**贯穿六条的二元前置**：I1/I2/I3/I4 依赖的多数工具 schema 标注 "Requires Desktop Bridge plugin"。**第一步应是一次独立的连通性验证，不要和迁移混做** —— 不通则四条全为零。

**遗留取证缺口**：本轮环境无联网工具，地图 UI 的外部先例 6 类（bottom sheet 分档惯例、marker 聚合视觉惯例、图层面板形态谱、地图与列表联动模式等）未取证，需在有联网的会话补齐。所有 external 依据均取自本机 Figma MCP 工具 schema 原文，**未编造任何 URL**（agent 按前置条件正确声明能力不可用并拒绝编造，这是正面案例）。

**新增沉淀原则**：
- ㉝ **发散之后必须换一个没有生成历史的上下文来反驳**。本轮 31 条候选里，3 条的 `direct:` 依据经抽查不成立（函数不存在 / 计数错 / 行号归属错），而它们在生成时读起来都很有说服力。**发散与核验必须由不同上下文承担** —— 同一个上下文会倾向于确认自己刚写下的东西。
- ㉞ **交棒物的第一个触点最值得先修**。`ui.html` 四处数字全错，成本是六条里最低的，但它出现在设计师动线的第一屏 —— 第一屏不可信，后面所有规格的可信度都要打折。
- ㉟ **能力边界要显式记档，否则会被反复重新发现**。「TIMING 不可创建为变量」「diff 不追踪变量值」这两条不是选择而是约束，它们直接决定了规格该往哪个载体放。此类结论一旦查明就该进文档，不能只留在某轮对话里。

### [49] 2026-08-26 Design Token 落地：17 项数值升为 Figma Variables（M3 前置第一步）

**为什么这是 M3 的第一步，而不是先精修像素**：条目 [48] 的 ideation 原把「冻结契约还是冻结像素」（I6）列为须先拍的范围决策，其全部论证建立在「视觉稿会被下游设计师推翻，故冻结像素是浪费」之上。用户答复 **「没有，自己做」** 后该前提消失 —— 视觉稿不会被推翻，[原则 ⑳](#) 反过来适用（下游是开发时，视觉与动效两维都值钱），故 **I6 作废，A4 轴（交棒可信度）整轴由「必做」降为「顺手做」**，I1 的离场对账与 I4 的交棒入口同步降级。真正影响 8/28 能否达成的只有 I2/I3。

**顺序只能是先 Token 后像素**：42 框里每个尺寸都是手打字面量，若先精修像素再补 Token，改一次圆角要动几十处，两天内必然返工爆炸。

**同时修正了 [48] 里我自己对风险的高估**：I2/I3 **不依赖 Desktop Bridge**（[48] 判为「二元前置」有误）。`ensureVariables()` 已在插件内成功创建过 21 个 COLOR 变量，证明经 Plugin API 建变量已验证可行；`combineAsVariants` 同为 Plugin API。走已跑通三轮的 `code.js` 路线即可。

**先核对再改**：PRD §1.4.4 字阶 6 项 / §1.4.5 间距 6 项 + 圆角 5 项与 `code.js:57-71` 三张真源表 **17 项数值完全一致**（PRD 的 `2xl` ↔ 代码的 `xxl`），无需修正任何数值，只需把它们从「JS 字面量」升为「Figma 变量」。

**`code.js` 六处改动**：
1. `:74` 后新增 `NUM_TOKEN_NAMES` 值→名反查索引。**为什么反查而不改调用点**：数百处消费点的实参本来就取自这三张真源表，值即 Token 只是丢了名字，按值反查即可找回，无需动一处调用；**分表反查以消歧**（4 与 8 同时属间距和圆角）；索引由真源表 for-in **派生而非手写副本**（防脱钩）；
2. 新增 `ensureNumberVariables()` 建 17 项 FLOAT，`ensureVariables()` 尾部汇总计数；
3. `hydrateVariables()` 兼载 FLOAT（`getLocalVariablesAsync('FLOAT')` 与 `('COLOR')` 是两次独立查询，必须分别调），新增 `bindNum()` / `bindRadius()`；
4. `box()` 尾部接入圆角 / itemSpacing / 四向 padding 绑定；
5. `text()` 接入 fontSize 绑定；
6. `card()` 修两处与 PRD §1.4.7 的偏差：加阴影 `0 2 8 rgba(0,0,0,0.04)`（原用 `stroke: color/border` 代替，描边比阴影更硬且卡片是全稿出现最多的组件；**阴影加上后描边必须移除**，两者并存会让卡片边界出现双线），内边距 md(12) → lg(16)。

**Figma API 三条硬约束（踩过才知道）**：
- **`cornerRadius` 不是可绑定字段** —— 必须逐角绑 `topLeftRadius` / `topRightRadius` / `bottomLeftRadius` / `bottomRightRadius`；
- **绑定顺序不能反**：必须先按字面值写完再改绑变量，Auto Layout 的 `primaryAxisSizingMode` / `counterAxisSizingMode` 依赖已写入的字面值定高；
- 行高**刻意不入变量**（它是「字号 × 倍数」的派生值，单独存就是第二份副本，改字号不会带动行高，反而制造静默不一致）；阴影**刻意不入变量**（PRD 只规定 2 处：§1.4.7 卡片 0.04、§970 地图悬浮卡 0.12；代码 8 处 DROP_SHADOW 中 1907/1968 正是那两处悬浮卡合规，其余 6 处属 Marker/弹层/FAB 的画面特定值，强行入表会造出 PRD 里不存在的规格）。

**为什么必须写离线探针**（`prototype-figma/probe-token-vars.py`，58 项断言全通，含 7 条反向用例）：`bindNum()` 取不到变量时**按设计静默返回 `false`** —— 那样画面完全正确、只是字段没绑变量。这意味着**绑定率归零时截图看不出任何异常**，人工验收必然放过。唯一能守住的是离线静态检查。断言分七区：真源表解析 / 与 PRD 逐项比对 / 完备度 / 反查索引由真源表派生且组内值唯一 / 绑定器接入 / card 与 PRD §1.4.7 对齐 / 反向用例。

**探针自身踩的坑（原则 ㉙ 的第二次现身）**：`strip_comments()` 首版用正则 `/\*.*?\*/` 剥块注释，而 `code.js:952` 的**行注释里含 `size/*`**（指代 size/ 前缀下的全部变量）—— 那个 `/*` 被当成块注释开头，一路吃到后面 JSDoc 的 `*/`，把 `bindNum` 绑定行与函数收尾大括号一起吞掉，表现为「`text()` 的函数体永远配不平」，探针报「绑定缺失」而实际存在。中途还错误地怀疑过函数签名不匹配。最终改为**带字符串态的单遍状态机**：只有处在代码态（不在字符串、不在注释内）时 `//` 与 `/*` 才算注释起点。同理，提取函数体也不能用 `\{(.*?)\n\}`（非贪婪会跨到下一个函数），改为大括号配平。

**同步 `ui.html` 五处过期描述**（原则 ㉞：交棒物第一屏不可信，后面全部规格的可信度都要打折）：语义色 13 → **16**（漏了 `*-text` 三个深色文字变体）；Variables 总数标注为 **38**（COLOR 21 + FLOAT 17）；批次 3 由「等 13 页」改为**逐个列全 14 页**（原文漏了 splash）；跳转 22 → **23** 条；起点 login-screen → **splash-screen**（代码 `:3954` 实为优先 splash 回退 login）。master 20 个经核对无误未动。

**新增沉淀原则**：
- ㊱ **静默降级的代码，必须配一个能在画面之外看见它的检查**。`bindNum` 静默返回是对的（缺变量只该退回字面值，不该中断整批生成），但正因为它对画面无影响，失败就完全不可见。凡「失败后画面依旧正确」的机制，验收必须挪到画面之外。
- ㊲ **给代码写检查器时，检查器本身也在解析代码 —— 它的解析同样会错**。本轮两次栽在同一件事上：用正则处理有嵌套结构的语言。注释里可以出现 `/*`，字符串里可以出现 `//`，函数体里可以出现顶格 `}`。凡涉及代码结构的提取，一律状态机或配平计数，不用正则。

**待用户实机重跑验收**：Figma 桌面版跑批次 6 清空 → 1→2→3→4→5。验收点：① 批次 1 日志报 **38 个变量**（COLOR 21 + FLOAT 17）；② 打开 Local variables 面板可见 `size/*` 6 项、`spacing/*` 6 项、`radius/*` 5 项；③ 随便选一张卡片，右侧属性面板的圆角与内边距显示为变量名（而非裸数字）；④ **卡片观感变化**：由描边边界改为浅投影，比上一轮更"浮"一点。

### [49-b] 2026-08-26 交付物防污染机制（用户提出，查证后确认污染已发生）

**用户的质问**：「每个环境都有交付物，你这样覆盖，如果出错，历史的交付物都被污染了，怎么没想过复制一个副本基础上进行改动呢」。**这个批评成立，而且实际情况比提出时更严重** —— 查证后发现污染已经发生，不是隐患。

**三个层面的实际状态**：
| 层面 | 状态 | 说明 |
|---|---|---|
| `code.js` 等源码 | 安全 | git 管着，能回退 |
| `assets/render/*.png` | **已污染** | 条目 [47] 的 8 张验收图被 [47-b] 就地覆写，两轮均未提交。文档写着「三张对照图均已人工过目」，但那三张已不是当时看的那三张，**[47] 的视觉证据不可追回** |
| Figma 文件 | **最危险** | `batchClean()` 删整页删所有 master，无副本；且 [48] 已查明 `figma_diff_versions` **不追踪变量值变更**，版本历史看不出 Token 值被改过 |

**更要命的是提交窗口**：上次提交 `7aa9990` 是 8/25 的鸭子 IP 那轮，此后 **14 个文件横跨条目 [47]/[47-b]/[48]/[49] 四轮全部未提交**。意味着 code.js 一旦改坏，最近可回退点是四轮之前。这正是用户所说的「出错就污染历史」，而我一直在往同一个未提交的堆里叠东西。

**已执行的补救**：`598e864` 定为 M3 检查点（当前状态可验证：探针 58/58 全通、文档已回写）。

**此后的机制（三条，均已生效）**：
1. **每轮一提交** —— 精修一页 → 用户验收通过 → 提交 → 再下一页。回退粒度从「四轮」压到「一页」；
2. **渲染图带轮次后缀，不再就地覆写** —— 历史验收图必须留住，否则文档里「已人工过目」这句话失去指代对象；
3. **Figma 侧每轮命名版本** —— 验收通过后在 Figma 菜单 File → Save to version history 填轮次号。这是唯一能对付「变量值改了 diff 看不出来」的办法：只有命名版本才能人工定位。

**为什么不选「每轮复制整个 Figma 文件」**：副本里的变量是独立副本，不会跟主文件同步，反而制造出第二套会各自漂移的 Token —— 那正是本项目一直在防的事。

**新增沉淀原则**：
- ㊳ **「可重跑」不等于「可回退」，两者要分别建机制**。全部画面能从 `code.js` 重新生成，这让我误以为不需要备份 —— 但重跑只能回到「代码当前状态对应的画面」，回不到「上一轮那个画面」。**幂等的生成器降低的是重建成本，不是丢失历史的风险。**
- ㊴ **就地覆写产物，等于把「验收过」这句话的指代对象删掉**。文档里写「三张对照图均已人工过目」，若图被下一轮覆写，这句话就变成无法核对的空话 —— 比没写更糟，因为它看起来像有据可查。凡是被文档引用为证据的产物，一律不得就地覆写。
- ㊵ **用户对流程的质疑要先查证再回答，不要先辩解**。本条用户提出时我完全没想过这个问题，查证后发现污染已实际发生。若当时凭「反正能重跑」回一句"没事"，就会把一个已发生的事故当成假想风险放过。

### [50] 2026-08-27 六个检查维度一次性补齐（用户质询「为何每轮都有新问题」后的收敛方案）

**用户的质询**：「已经 5 轮，为啥每次都会有新的问题，是修改后衍生出来的还是之前核查的问题」。

**按成因归因后的答案（不是「修一个坏两个」）**：五轮共 9 项发现，**存量 5 项 / 改动衍生 2 项（当轮修掉、无一流出）/ 检查器自身错 2 项**。真正的根因是：**每轮我开一个新检查维度，新维度第一次开就必然捞出一批存量**。只要还有没开的维度，就还会「又冒一个新问题」——「下一轮没查出问题」不能证明收敛，那可能只是没开新维度。

**据此定的终止条件（用户已拍定）**：把已识别的维度**一次列全、一次全开**，把「每轮冒一个」压成「一次冒完」，之后才有资格用「全绿」判定收敛。本轮一次开了六个：⑦字体族与字重一致性、⑧单个文本超出 FIXED 宽祖先、⑨同父级内 ABSOLUTE 节点两两遮挡、⑩Figma 默认图层名残留、⑪原型连线完整性（`FLOW_LINKS` 逐条落地）、⑫触控区 ≥44×44。

**首跑捞出 4 类失败，逐个甄别后 2 类是我的检查器错、2 类是真问题**：

| 报出的问题 | 定性 | 处置 |
|---|---|---|
| 183 处 `fontName` 为空 | **mock 错**。`inInstance=183 / notInInstance=0` —— 全在 INSTANCE 内部，`createInstance().deepCopy` 没抄 `fontName` | `deepCopy` 字段清单从「只结构与尺寸」扩到「凡断言会读的都抄」 |
| `btn/primary/联系 TA` 量到 358×**24**（应为 44） | **mock 错**。`createComponentFromNode` 在**搬走子节点之后**才第二次读 `node.width/height`，此时原 Frame 已 hug 成只剩 padding 的空壳 | 尺寸改在搬子节点**之前**锁定，并一并复制 Auto Layout 属性 |
| `FLOW_LINKS` 第 2 条命中 0 个 | **真 bug、长期潜伏**。表里写 `btn/primary/登录 / 注册`，实际按钮是 `capsule` 变体（`code.js:3253`）→ Present 模式里是死链 | 改为 `btn/capsule/登录 / 注册` |
| 5 处文本横向超出 390 画框 | **真问题**。4 处 `_map-caption`（绝对定位胶囊完全 hug，PRD 原句 40+ 字量出 421–520 宽，被 `clipsContent` 从右切掉）；1 处 `permission-guide` 的 `_body` 长文案（421 > 可用 342） | 胶囊限宽 `390−16×2` + 文本 `layoutGrow`；引导页两行长文案显式 `layoutSizingHorizontal='FILL'` |

**本轮最大的一处假绿：mock 从未实现 `setReactionsAsync`**。离线跑批次 5 时 24 条连线**每条都抛异常**，被 `batchFlow` 的 `try/catch` 收进 `skipped` 数组、只体现为返回字符串里的一段文字，**没有任何断言在看**。连线全断，探针照样全绿。补完 mock 后又加了一条「批次 5 自报 0 条跳过」的断言直接查那个返回消息 —— 它与「逐条核 reaction」互补：前者说**为什么**断，后者证明**确实**断了。

**触控区的口径必须按成因认、不按名字认**。第 5 轮按名前缀 `['_tab-','_nav-action','_nav-bell','btn/','_fab']` 匹配，把 24×24 的**图标**当可点控件误报，而真正承载点击的 `_tab-*` 是 130×44 —— 等于这个维度此前实际上没验过。新口径改为「**真的挂了 reaction 的节点**」：能点的定义就是「挂了 reaction」，名字匹配永远会在图标/文字子节点上误报。`_nav-action/*` 的豁免（文字动作横向 28、纵向补到 44）也**写进了断言代码**而非靠人记，且不是无条件放行 —— 纵向不满 44 或横向不足 28 照样报错。

**又一次探针自污染**：⑪ 修完节点名后仍报 `reactions=0`。查出是探针第六节 `figma.createPage()` 建的临时页没清，页里那份 `login-screen` 被 `indexMainFrames` 当真页收进索引，与 proto 页的 `home-screen` 不同页 → 触发 `batchFlow` 的「跨 Page 无法跳转」守卫。**报出来像画面 bug，实际是我的临时页在捣鬼**（第三次栽在「探针自身的错」上）。已在批次回归前清掉非 `PAGE_NAMES` 的页。

**六条新断言各配了反向用例**（本项目既有规矩），共 9 条，手法与此前不同：**不改源码字符串，而是直接在真跑出来的节点树上注入对应缺陷**，再用与正向完全相同的判据重算 —— 验的是判据本身，源码一改字符串替换法就失效。豁免分支也要反向验：`layoutSizingHorizontal='FILL'` 能放行、遮罩类命名能放行、但 `_nav-action` 28×20 仍须报错，否则「跳过」写成永真跳过就等于把整条断言关掉了。

**当前状态**：布局探针 **127/127 全通**（含 9 条新反向用例），Token 探针 **101/101 全通**。

**第 6 轮实机复验（2026-08-27，MCP 逐项核，5 项全过）**：

| 验收点 | 实测 | 判定 |
|---|---|---|
| 空文本节点 | 41 画框全扫，`characters===''` 的 TEXT **0 个**（上轮 4 个） | 通过（`listRow` 空值守卫这次真进去了） |
| `_map-caption` | 22 处全部 `capW=358`、`txtW=342`、`layoutSizingHorizontal=FILL`、**over 全 false**；长文案已换行成 2 行（`capH` 21→34） | 通过（截图确认完整显示、不再被右侧裁掉） |
| 引导页长文案 | 两行均 `w=342`（父 390）、`sizing=FILL`、`auto=HEIGHT`，第一行 `h=42`（换行成 2 行） | 通过（截图确认不撑破画框） |
| 原型连线 | 带 reaction 的节点 **23 个**，目标指向缺失 **0 条**，起点 1 个（「主流程起点」） | 通过（23/23 全部落地） |
| 登录跳转（本轮修的死链） | `btn/capsule/登录 / 注册` 命中 1 个、312×45、`reactions=1`、`ON_CLICK` → `dstIsHome: true` | 通过（此前是死链） |
| 触控区 | 按 reaction 认，23 个触发点**无一不达标**（`_nav-action/列表` 28×44 走豁免，`_tab-*` 130×44，`_nav-bell` 44×44） | 通过 |

**实机报错一处（我上一轮引入的顺序错误，已修，检查点 `3ec783b`）**：批次 2 抛
`Setting layoutSizingHorizontal to FILL requires an auto-layout parent`。原因是修长文案超宽时把 `layoutSizingHorizontal='FILL'` 写在了 `appendChild` **之前** —— Figma 要求该值为 `FILL` 时节点**已是** Auto Layout 父级的直接子节点。修法：赋值移到 `appendChild` 之后。

根因仍是㊷的同一类：`layoutSizingHorizontal` 在 mock 里是**裸字段、纯存取不校验**，所以顺序写错也离线全绿。已改成带约束的 setter 并配反向用例。**首版约束还写过严了**：把 `FIXED` 也一并校验，一跑就误报 `button()` 里 append 前赋 `FIXED` 的合法代码（`FIXED` 在真机上没有这个前置要求），已照真机原样收窄到只校验 `FILL`。

**新增沉淀原则**：
- ㊶ **mock 没实现的 API，会让读它的整批断言静默假绿**。`setReactionsAsync` 缺失时 24 条连线全部失败，探针照样全绿 —— 因为失败被业务代码的 `try/catch` 收进了一个没人查的数组。**凡业务代码里有 `catch` 后只写日志的分支，那个日志/返回值本身就必须被断言查。**
- ㊷ **mock 少抄一个字段，等于给读该字段的断言批量注入假数据**。`deepCopy` 漏抄 `fontName` 一处，字体维度一开就报 183 处。判断报错真假的第一步永远是：**这个值是画面产出的，还是我的 mock 编的？**
- ㊸ **「能点」的判据是「挂了 reaction」，不是「名字像按钮」**。按名前缀认可点控件，会把图标与文字子节点一起捞进来误报，同时漏掉真正的命中区节点 —— 两头都错。凡要判定某种「能力」，优先找**承载该能力的机制**当判据，不要找**名字规律**。
- ㊹ **新开一个检查维度，第一次开必然捞出一批存量；所以维度要一次列全、一次全开**。逐轮补维度会让「每轮都有新问题」永远持续，且无法区分「已收敛」与「只是没开新维度」。**「本轮没查出问题」不是完工信号，「已识别维度全开且全绿」才是。**
- ㊺ **mock 把「有前置条件的 API」做成无条件成功，等于把该类错误全部放行**。裸字段式的 mock 尤其危险：`layoutSizingHorizontal` 只做存取，就放过了「FILL 必须在 appendChild 之后设」这个真机硬约束，离线 127 条全绿、实机批次 2 直接崩。**凡真机会因调用顺序/上下文抛错的 API，mock 必须把那个条件也实现出来。**
- ㊻ **补约束时要照真机原样收窄，不能宁严勿松**。同一次里我把 `FIXED` 也纳入校验，立刻误报 3 处合法代码 —— **过严的检查器与漏检的检查器同样有害**，前者更费时间（要逐个甄别假阳性）。加约束前先确认真机的确切边界。

### [51] 2026-08-27 M3 范围决策：冻结对象 = 契约，不是像素（用户拍定 I6）

**触发**：用户问「UI 设计结束了吗」。核实机与 PRD 对账后给出的答案是**没结束**，且要先分清「已收敛的是哪一层」：

- **已收敛（结构与规格层）**：PRD §10.1 清单 17 页，实机 18 个主态（多出的是 [45] 加的 `splash-screen`）+ 23 个变体 = 41 框，**页面清单 100% 覆盖**；离线探针 127/127、Token 探针 101/101、实机六维度全绿。
- **未开始（视觉精修层）**：条目 [49] 写明的「14 页像素精修」至今没启动。近六轮做的全是**正确性核查**（不溢出 / 不硬编码 / 不断链 / 对比度达标），**不是美感精修**。当前 41 框是「布局对、Token 对、连线对」的**结构稿**，排版节奏、留白韵律、图标细节、状态微差都没逐页过。
- **未落地（条目 [48] ideation 的 6 条方向）**：I1～I6 一条未动，其中 I6 是范围决策、必须先拍。

**用户两项决策**：
1. **冻结对象 = 契约，像素靠后**（I6 采纳）。依据：视觉稿自己做、无外部设计师接棒，**像素可持续微调，但红线丢了就是隐性返工**。故优先补「红线在哪、改了怎么发现」这条腿 —— I4（信心 88%、复杂度 Low）、I1、Component description、`ui.html` 计数修正。
2. **补取证，精修前先做**。条目 [48] 遗留的地图 UI 6 类外部先例（bottom sheet 分档惯例 / marker 聚合视觉惯例 / 图层面板形态谱 / 地图与列表联动模式等）当时因无联网未取证；**地图是本产品主界面，凭直觉设计风险高**。

**执行顺序（已排入 ToDoList）**：
1. **Desktop Bridge 连通性独立验证** —— 条目 [48] 查明的二元前置：I1/I2/I3/I4 依赖的多数工具 schema 标注 "Requires Desktop Bridge plugin"，**不通则四条全为零**。且必须单独验，不与迁移混做；
2. 补取证 6 类地图 UI 外部先例；
3. **I4** 修 `ui.html` 四处错误计数（:24「14 页 + 3 模态」实为 18 主态 / :30「语义色 13」实为 16 / :42「等 13 页」实为 14 / :54「22 条跳转」实为 23），改为运行时自报 —— 这是交棒第一屏，错计数会直接误导接棒人；
4. **I1** `annotation()` 加 severity 分级 + 节点级引用（现状：35 张卡对被标注对象零引用、纯漂在画布上，红线与提示视觉同构）；
5. 给 20 个 Component master 补 description（现状全无，且全为平行注册）；
6. ~~**I5** 动效规格落 annotation~~ ✅ 已完成（条目 [56]）；
7. ~~**I2/I3 先评估再定**（复杂度 Medium/High，且 I3 依赖 `figma_create_slot`）~~ ✅ 评估已出、已拍板（条目 [57]）：**I3 不做结案**；**I2 改小为「Dart 单向出口 + 一致性断言」并已落地**（条目 [58]，产物 `lib/design_tokens.dart` 43 项、探针 125/125），原 DTCG 方案作废。评估过程推翻了本条目第 7 步自己写的前提 —— **I3 并不依赖 `figma_create_slot`**，`ComponentNode.createSlot()` 是实例方法、Plugin API 内可用。**至此条目 [51] 七步全部结案。**

**范围红线**：本次不做逐页像素精修。若后续要做，须先更新本条目与「二、实施方案」，再执行（规则四之 4.2「绝不允许超出计划」）。

> **⚠️ 范围红线已于 2026-08-27 经用户批准放开一档（条目 [61]）**，放开的**不是全部**，而是限定为**「只做全局排版规律」**：文本左边缘对齐、卡内间距节奏、模块间留白、行高与扫读性四类。**逐页美感精修仍不做** —— 逐页迭代需十几轮「改 → 你手动重跑批次 → 反馈」，且真机跑起来后判断质量更高（条目 [56] 已演示：断言全绿但看图才发现槽位参差，原则 58）。验收标准为**先列问题清单、由用户勾选取舍**，改完即结，不无限期延伸。
>
> **一处过期数字一并改正**：本条目与 M3 里程碑写的「14 页视觉稿」是 [1289] 补缺页时的数字，**已过期**。实测为 **18 个主态 + 23 个变体 = 41 框**（**2026-08-31 条目 [70-h] 补出定位权限 B 态后为 18 + 24 = 42 框**）。按 14 页做会漏 4 页，故精修范围以 **18 个主态**为准（变体多为同页状态差，随主态一并过，不单独计）。

**第 1 步已完成（Desktop Bridge 二元前置，6 项逐个实测通过）**：Bridge 连接 / `figma_set_annotations` 写入 / annotation 读回（label + labelMarkdown + pinned properties + categoryId 全完整）/ 4 个内置分类可枚举 / **`node.annotations` 在 Plugin API 侧可直读直写**（这一条决定 I1 能做进 `code.js` 生成流程、每轮重跑自动落红线，不必靠 MCP 事后补）/ **`component.description` 可写**（实测 20 个 master description 全为空）。测试数据已全部清除（全库扫描 `annotations.length > 0` 的节点，remaining = 0）。

**annotation 内置分类映射（实测取得，不是推测）**：

| index | categoryId | label | color | 拟用语义 |
|---|---|---|---|---|
| 0 | `354:0` | Development | green | 实现口径（尺寸/Token/组件契约） |
| 1 | `354:1` | Interaction | blue | 交互与动效规格（I5 落点） |
| 2 | `354:2` | Accessibility | pink | 无障碍红线（对比度/触控区） |
| 3 | `354:3` | Content | orange | 文案与内容规则 |

**三个查证坑一并记下（避免重复踩）**：
1. **正确命名空间是 `figma.annotations.getAnnotationCategoriesAsync()`**。此前试的 `figma.getAnnotationCategoriesAsync` 报 `TypeError: not a function`、`figma.annotationCategories` 为 `undefined`，一度误判「分类名读不到」。注意 `Object.keys(figma.annotations)` 返回 `[]`（方法在原型上），**不能用 keys 为空来判断命名空间不可用**。
2. **MCP 的 `figma_get_annotation_categories` 只回 id、不回 label/color**，必须走 `figma_execute` 直调 Plugin API 才能拿到语义。
3. **annotation 只在 Dev Mode 可见**，设计模式右侧面板没有 Annotations 分区（我曾给出错误路径，用户实机找不到）。查看：`Shift + D` 切 Dev Mode → 选中节点，标注即渲染在节点旁；添加：工具栏 Annotation 按钮或 `Shift + T`。此为 Figma 官方帮助中心口径，预设分类名与实测完全一致。

### [52] 2026-08-27 地图 UI 外部先例取证（补齐条目 [48] 的遗留缺口）

**为什么补**：条目 [48] ideation 时环境无联网，地图 UI 的 6 类外部先例全部未取证，当时已标为「需在有联网的会话补齐」。地图是本产品主界面（§6 全章），凭直觉设计风险高。本次取证的用途是**为精修阶段提供判据，不是推翻已定口径** —— 逐条与 PRD 现状比对，只在出现冲突时才提出改动。

| # | 取证类目 | 外部先例（权威源） | 与 PRD 现状比对 |
|---|---|---|---|
| 1 | bottom sheet 分档惯例 | **M3**：modal sheet 初始高度**上限 50% 屏高**，超出内容靠拖到全屏 + 内部滚动；`halfExpandedRatio` 默认 **0.5**；drag handle 为可选件。**Apple HIG**：系统只定义两档 detent —— `large`（全高）与 `medium`（约半高），可自定义档位。**Google Maps 式三档**（社区公认模式）：peek 约 0.18 / mid 0.5 / full 0.9，**且不加 scrim**，地图始终可交互 | PRD §6.9 定「40–70% 屏高，带 40% 遮罩」。**40% 低于三家的半档基线（50%）**，且**加遮罩会让地图不可交互**，与 Google Maps 式「非模态」惯例相反。但 §6.9 是三级树下钻专用（选完即收），属**模态任务**而非常驻浏览，加遮罩有正当性 —— **不改，理由补进 §6.9** |
| 2 | marker 聚合视觉惯例 | **Mapbox 官方示例**与 **iOS SDK 示例**均用 `step` 表达式做**三档**：半径 20/30/40px（iOS 示例 25/30/35），颜色随档位换；**聚合数字直接叠在圆上**（`point_count_abbreviated`）；`clusterRadius` 默认 **50px**、`clusterMaxZoom` 14 级以上不聚合。**Google 官方**：聚合图标必须带**包含点数的计数**，点击聚合缩放到「展开层级」。Flutter 侧实践建议**预生成常见档位图标**（10/25/50/100+）避免重复栅格化，并用 `zIndex` 让聚合**压在单点之下** | PRD §6.4.2 只定「底白边 + 数字 + 分类色描边」**一种视觉**，无尺寸分档；§6.15 的阈值 3/5/8 是**触发聚合的条数**，不是**聚合圆的尺寸档**。**这是一处真缺口**：聚合了 4 条与聚合了 40 条视觉同大，用户无法判断密度。建议精修时补「聚合圆三档尺寸」，与 §6.15 的差异化阈值不冲突（一个管何时聚、一个管聚了多大）。另 §6.7 已定「Dart 侧自实现网格聚合」，`clusterRadius=50px` 可作实现默认值参考 |
| 3 | 图层面板形态谱 | 三种形态并存：**侧栏/浮动面板 + 开关**（每层一个 toggle，图标+文字标签）、**可折叠手风琴**（移动端 ≤640px 时展开一个自动收起另一个，保证地图始终可见）、**图例内嵌 checkbox + 主开关**（Show All / Hide All，含混合态指示）。共识做法：**相关 toggle 分组**、**面板可折叠**、**移动端不允许两个面板同时展开** | PRD §6.4.1 的收起态方案（默认收起、右上 44×44 圆入口、左上只读摘要胶囊）与「移动端手风琴」同源 —— 都是**为保住地图可视区**。§6.4.1 记录的实测数据（四组控件常驻吃掉 200px，688 净可视压回 490）与外部先例的动机完全一致，**属独立验证通过**。外部多出一条我们没有的：**图层计数**（每个 toggle 旁显示该层对象数）。可考虑作为摘要胶囊的增强，但与 §6.9「两种基础可视化」的信息维度约束需先对齐 |
| 4 | 地图与列表联动模式 | **Airbnb 式 card–map 同步**（业界称 gold standard）三条行为：hover 卡片高亮对应 marker / 点 marker 滚动卡片入视 / **平移缩放地图即过滤列表**（map as filter）。**Zillow 式**：上下分屏（地图在上、可滑卡片在下）**或** 视图切换二选一；marker 上直接印**价格**而非通用图钉，让地图本身成为比价工具 | PRD 现状是**视图切换**（导航栏「地图/列表」二态，§6.4.1），非分屏、非 hover 联动 —— **在 390px 单手宽度下这是合理取舍**，分屏在手机上两边都局促。但外部有一条值得引入：**marker 上印关键信息**。我们 §6.4.2 的 marker 只有分类色+图标+供需态，价格/距离要点开信息卡才见。是否在 marker 上印价格，须与 §6.9「Pin 只表达供需态与完整度档」的**两类信息硬约束**冲突判定 —— **列为待决项，不擅自改** |
| 5 | marker 触控区 | **WCAG 2.5.8（AA，法定要求）24×24 CSS px**；**2.5.5（AAA，推荐）44×44**；**Apple 44×44pt**；**M3 48×48dp**；相邻目标间距建议 **8dp/8pt**。**关键发现**：2.5.8 的第 5 条豁免「Essential」**明确把地图上标记精确坐标的 pin 列为经典豁免案例** —— 放大或移动 pin 会曲解位置，故 pin 可以小于 24。另有实践反例：某地图项目把数据点的 5px 圆**同时当视觉与点击区**，移动端难以点中，修法是**视觉与命中区分离** | PRD §6.4.2 定 marker 正常 40×40、选中 48×48。40 已超 24 的 AA 线、逼近 44 的 AAA 线，**且本就落在 pin 的 Essential 豁免范围内，合规无疑**。上一轮实机复验的 23 个触发点无一不达标（`_nav-action/列表` 28×44 走的正是豁免）。**外部先例反过来提供了一条我们缺的实施细节**：视觉与命中区应分离声明（40×40 是视觉，命中区可更大），这条适合落成 I1 的 annotation |
| 6 | 定位权限降级 UI | 共识做法：**先自定义 pre-prompt 说明价值，再触发系统弹窗**（跳过 pre-prompt 等于烧掉唯一一次机会）；**系统拒绝是 sticky 的**（同 origin 不再弹）；**必须提供手动输入兜底**（邮编/城市）；文案要**具体收益**而非泛泛而谈 —— 实测「找到最近的有货门店」转化，「允许定位以改善体验」失败（听起来像收集数据）；Android 官方要求用 `shouldShowRequestPermissionRationale` 分支出教育性 UI，且**必须给「不用了」的出口**；已授权后被用户在系统设置里关掉，需能检测并引导去设置 | PRD §6.4.4 的两段递进（第一段全屏引导页突出「就近」价值 + 主按钮开权限 + 次按钮手动选城市；第二段基站+商圈兜底、**不用城市中心假值**）**与外部共识高度一致**，且「不用城市中心假值」比外部资料更严。原型引导页文案「找鸭找只推你走得到的地方——身边几公里内的活儿、房子、顺路车」正属「具体收益」型，符合转化侧证据。**唯一缺口**：外部强调「已授权后被关掉要能检测并引导去设置」，PRD §6.8 只写了「定位失败 ≥3 次降级」，**未区分「从未授权」与「曾授权后被关」**两种状态。列为 PRD 待补项 |

**取证结论汇总（三类处置，不混为一谈）**：
- **已被外部独立验证、不改**：#1 遮罩（模态任务有正当性）、#3 收起态方案（动机与实测数据双向吻合）、#5 marker 尺寸合规（且落在 pin 的 Essential 豁免内）、#6 两段递进降级；
- **真缺口，精修时补**：#2 **聚合圆缺尺寸分档**（4 条与 40 条视觉同大）、#5 **视觉区与命中区未分离声明**（适合落 annotation）→ **两条均已于条目 [54] 落成首页主态的 a11y 档 annotation**，`#2` 定档 32/40/48，**已于 2026-08-27 回写 PRD §6.4.2（条目 [60]）**；
- **待决项，不擅自改**：#4 **marker 上是否印价格** —— **已于 2026-08-27 定案：不印**（用户拍定，判定记录已回写 PRD §6.9，详见条目 [59]）；#6 **未区分「从未授权」与「曾授权后被关」** —— **已于 2026-08-27 回写 PRD §6.4.4 权限状态三分 A/B/C 三态 + §6.8 注明 3 次失败专指 C 态（条目 [60]）**。

**新增沉淀原则**：
- ㊼ **取证的产出是三分，不是一分**。外部先例查回来必须分成「验证了什么（不改）/ 缺什么（要补）/ 冲突什么（先判定）」三类。只挑「要改的」看会显得处处不合规，只挑「已合规的」看则等于白查。本轮 6 类里 4 类属「已被独立验证」—— 这个结果本身就是有价值的信息：**它说明 PRD 里那些带实测数据的决策（如 §6.4.1 的 200px/490px 反推）是站得住的，不是拍脑袋**。
- ㊽ **外部惯例与自家硬约束冲突时，先判定信息维度，不直接抄**。marker 印价格是业界 gold standard，但我们 §6.9 有「Pin 只表达两类信息」的明文约束。§6.9 自己已给出过判定范式（亮度差异属「同类信息的强调手法」而非新维度，故不冲突）—— **同一个判定方法应复用，而不是每次重新吵**。

### [53] 交棒第一屏改为运行时自报（I4 落地）— 2026-08-27

**问题**：`ui.html` 是接棒人打开插件看到的第一屏，上面「生成多少页 / 多少个 Variables / 多少条跳转」全是手写数字。代码演进后无人回改，最典型的是 Variables 标「38 个」，而 `CATEGORY_DEEP` 那 5 项在 8/26 新增后没人回头改文案，真值是 **43**。第一屏的数字错了，接棒人一切判断的起点就是错的。

**做法（真源现算 + 断言守门，不是把数字改对）**：
| 措施 | 位置 | 作用 |
|---|---|---|
| `planStats()` | `code.js:283` 起 | 从 6 张 Token 真源表 + 3 张登记表**现算**全部规模，函数内不写任何字面计数 |
| `MAIN_SCREENS` 登记表 | `code.js` | 18 个主态 pageId 显式登记；`screen()` 加校验，**未登记的主态当场抛错** |
| `CORE_PAGES` 登记表 | `code.js` | 构造器与页名短名单表化，消除 `batchCore` 里「数组 + 日志文案」两份副本 |
| `SHELL_TABS` / `BUTTON_VARIANTS` | `code.js:290` 起 | 原先 `registerComponents` 与 `batchSetup` 各持一份字面量，改为共用 |
| `type:'stats'` 消息 | `code.js` / `ui.html` | UI 脚本就绪后**主动索取**，主线程回填 —— 不用「showUI 后推送」是因为推送可能早于 UI 注册 `onmessage` 而丢消息 |
| 12 个占位 span | `ui.html` | 首屏计数全部改为 `·` 占位 + 运行时填充，HTML 内**零硬编码计数** |
| 7 条新断言 | `probe-layout-offline.js` | 逐张表核对分项（不只核 total）、master 数以真跑 `registerComponents` 为准、含反向用例 |

**实测确认的真值**（探针 **134/134** + Token 探针 **101/101** 全通）：
- Variables **43** = COLOR 26（语义 16 + 分类 5 + **分类深色 5**）+ FLOAT 17（字号 6 + 间距 6 + 圆角 5）
- Component master **20** = 状态栏 1 + Tab 3 + 按钮 6 + Pin 10（与 `registerComponents` 实跑产出一致）
- 核心流程页 **14**｜主态画框 **18**｜原型跳转 **23**（`FLOW_LINKS` 23 条两端 pageId 全部在登记表内）

**顺带修掉的陈旧注释**：`ensureVariables` 头注释写死的「共 38 项」、`hydrateVariables` 的「COLOR 21 + FLOAT 17 = 38」，均改为「以 `planStats().tokens` 现算为准」。保留 `ensureNumberVariables` 头部那段「此前只有 21 个 COLOR」——那是历史事实陈述，不是当前计数。

**新增沉淀原则**：
- ㊾ **计数只能有一个出处，且必须是可执行的那个**。同一个数字出现在两处（代码里一份、说明文字里一份）就注定分叉，因为只有代码那份会被运行验证。正解不是「勤快地同步两处」，而是**让说明文字从代码取值**，物理上消灭第二份。
- ㊿ **登记表比「数调用点」可靠**。主态数是对外契约，而 `screen()` 调用点散在 4 个批次的 29 处，靠人数必错（ui.html 上错了很久的计数就是这么来的）。显式登记 + 构造时校验，把「计数正确」从人的记性变成代码约束：漏登记会当场抛错，而不是等跳转索引出现重名告警时才被间接发现。

### [54] 规格标注加严重度分级 + 节点级引用（I1 落地）— 2026-08-27

**问题（条目 [48] I1 原始诊断）**：画布上 35 张规格标注卡有两处硬伤。① **视觉同构**：「永久 Scope 红线（做了就是违约）」与「这里留白 16px」长得一模一样，全是 primary-light 蓝框 —— 评审时红线淹没在等权重的说明堆里，漏一条就是隐性返工，正是 M3「冻结契约」要防的事；② **对被标注对象零引用**：卡片飘在画框右侧，「这条讲的是哪个元素」只能靠人读文字猜，元素一移动关系就断。

**做法**：
| 措施 | 位置 | 作用 |
|---|---|---|
| `SEVERITY` 五档 | `code.js:1577` | `redline` / `a11y` / `spec`（默认）/ `interact` / `info`，各档定 fill·stroke·ink·前缀·annotation 分类 |
| `annotation(title, lines, opts)` | `code.js:1598` | 扩第三参：`severity` 选档、`target` 指定被标注节点的精确名 |
| `setPluginData('anno', ...)` | 同上 | 存 `{severity,target,title,lines}` **原始数据**，作为两个承载体之间唯一的传值通道 |
| `attachNodeAnnotation()` | `code.js:1954` | 把卡片内容写成节点的 Dev Mode `annotations`，用 `labelMarkdown`（面板渲染 markdown，多行规格用列表可读性远好于挤成一行） |
| `detachAnnotations()` 前置段 | `code.js:2009` | **在卡片搬走之前**按 `target` 精确名定位兄弟节点并落标注 —— 那时目标节点还与卡片同在一个画框内 |
| `mapCanvas` 的 `note` 数组化 | `code.js:2986` 起 | 从「单张」放开为「单张或数组」，让一页能按 severity 挂多张而非把不同档位混装进一张 |
| 12 条新断言 | `probe-layout-offline.js` 第十节 | 配色逐档对齐真源表、pluginData 真存真取、target 命中/写错两向、markdown 与卡片同源、categoryId 映射、降级路径、端到端 |

**为什么两个承载体并存而不二选一**：画布卡在**设计模式**可见、适合整页评审，但对被标注对象零引用；节点级 annotation 钉在元素旁、随元素移动、进 Dev Mode 代码交付面板，但**只在 Dev Mode 可见**（`Shift + D`，见条目 [51] 的实测坑）。两者受众与可见范围都不同，故并存；内容同出一源（卡片的 `pluginData`），不会分叉。

**三处刻意的自我约束**：
- **`spec` 档配色与改造前完全一致**。本轮范围红线是「不做逐页像素精修」，分级不该顺带引发一次全画布视觉 diff。故默认档保持 primary 系原样，只有 `redline`（`error-text`，白底 6.47:1）与 `a11y`（`accent`，白底 5.01:1）换色。探针里把这三个 role 名**写死**作为该承诺的锚点 —— 有人顺手改了就当场亮红。
- **不为标注新增浅色 Token**。标注卡是画布上的评审辅助物、不是产品界面，为它造 `error-light` 会污染产品命名空间（沿用 `detachAnnotations` 既有的「Token 管产品界面」口径）。
- **前缀用【】不用 emoji**。中文全角括号在 Noto Sans SC 内有字形，emoji 需回退系统字体、在 Figma 里可能渲染成豆腐块。

**顺带处置的两个真缺口（条目 [52] 取证结论）**：首页主态新增两张 a11y 档卡 —— #5「命中区与视觉区分离」（Marker 视觉 40×40，命中区外扩至 ≥44×44，实现侧对应 GestureDetector 外扩，**不是把 Pin 画大**）、#2「聚合圆三档尺寸」（`2-9 → 32×32；10-99 → 40×40；100+ → 48×48`，**本档位为新增规格，PRD §6.4.2 待补**）。另把 `batchSetup` 里一张 4 条混装卡拆成「Pin 尺寸与形态」（a11y，`target` 钉到 `pin/cat-service/resource/selected`）+「Marker 信息维度红线」（redline，并写入待决项 #4 的判定要求）。

**验收**：布局探针 **146/146**（134 → +12）、Token 探针 **101/101**，全通。

**过程中拦下的一处假绿**：改完 `annotation()` 用 pluginData 传值后主动回查 mock，发现探针的 `setPluginData` 是空实现、`getPluginData` 恒返回空串 —— 离线跑时节点级标注**一条都不会落，而断言看不出来**。与探针内既有两处同类教训同源（`setReactionsAsync` 曾完全没实现导致 23 条连线全断仍全绿）。修法：mock 改为如实存取，并新增第 ⑪ 条「端到端」断言 —— 前十条都是构造场景，只有它验真实调用链，pluginData 若是空实现只有这条会红。

**顺带修的探针脆性**：`probe-token-vars.py` 按签名全文 `function annotation(title, lines)` 抓函数体，参数一变就抓不到、断言恒失败。失败方向是安全的（不会假绿），故只跟着更新签名并注明这处固有脆性。

**新增沉淀原则**（㊿ 之后无对应圈码字符，改用「原则 N」编号续下）：
- **原则 51 · 同一份信息有两个承载体时，必须指定其中一个为源，另一个由它派生**。画布卡与节点级标注受众不同、都要留，但内容只能有一处可编辑（这里是卡片的 `pluginData`）。反例是「从卡片文本反向解析出规格」——要剥「· 」前缀与 severity 前缀，多一道转换就多一处会静默脱钩的地方。
- **原则 52 · 改 mock 之后必须补一条「端到端」断言**。构造式断言（自己造节点、自己调函数）验的是函数逻辑，而 mock 的空实现只会在**真实调用链**里露馅。本轮 12 条断言里前 10 条在 mock 空实现下照样全绿，只有第 ⑪ 条会红 —— 这类断言的价值不在覆盖率，在于它是唯一能发现「地基是假的」的那条。
- **原则 53 · 分级改造必须挑一个「与改造前完全一致」的默认档**。否则给 24 处调用点分档会顺带产生一次全画布视觉 diff，评审时无法区分「哪些变化是我要的分级、哪些是被顺带改的」。默认档零变化 = 视觉 diff 恰好等于分级本身。

### [55] 20 个 Component master 补 description（条目 [51] 第 5 步落地）— 2026-08-27

**问题**：20 个 Component master 的 `description` 实测**全为空**。这是「冻结契约」这件事上最大的一处漏口 —— 组件的尺寸和配色能从画布量出来，但「**这个组件什么时候该用、哪些值不可改、有什么结构约束**」量不出来，而这恰恰是接棒人最需要、也最容易在交接中丢掉的三件事。

**为什么放在 description 而不是别处**：Figma 的 `ComponentNode.description` 显示在**右侧 Inspect 面板与组件库列表**，**不需要进 Dev Mode**（这点与节点级 annotation 正相反，见条目 [51] 的实测坑），也不会像画布标注卡那样被误删或搬走。至此三个承载体分工明确、互不重复：

| 承载体 | 讲什么 | 组织方式 | 可见范围 |
|---|---|---|---|
| 画布标注卡 | 这一页为什么这么设计 | 按页 | 设计模式 |
| 节点级 annotation | 这个元素的规格 | 按元素 | 仅 Dev Mode |
| **component description** | **这个组件在任何页都成立的约束** | **按组件** | **Inspect 面板，无需 Dev Mode** |

**做法**：
| 措施 | 位置 | 作用 |
|---|---|---|
| `BUTTON_SPECS` 提为模块级 | `code.js:1162` | 六类按钮的 fill·textColor·radius·stroke 原本只存在于 `buttonRaw` 函数体内的匿名对象里；新增 `usage` 字段（「什么时候该用哪一类」—— 代码里看不出来的那部分） |
| `describeComponents(cache)` | `code.js:1810` | 20 条 description 全部由 `buttonDesc()` / `pinDesc()` 从真源表**现算**，不写死配色与尺寸 |
| 四段式结构 | 同上 | `【用途】`/`【规格】`/`【不可改】`/`【判据】`；Pin 另加 `【命中区】`+`【结构约束】`，Tab 与 disabled 档另加 `【无障碍】` |
| `describedCount !== compCount` 抛错 | `code.js:2327` 起 | 接在既有数量校验**之后**（那时才确定 `COMP_CACHE` 是全的），一个不写就当场炸 |
| 10 条新断言 | `probe-layout-offline.js` 第十一节 | 名单逐项核对、四段式齐全、按钮配色与 `BUTTON_SPECS` 逐项对齐、Pin 色取自 `CATEGORY_COLORS`、`CAT_LIST` 与 `CATEGORY_COLORS` key 一致、两条反向、一条端到端 |

**为什么必须先把 `BUTTON_SPECS` 提出来**：description 要写明每档的填充/文字/描边/圆角，而原值只在函数体内。**另抄一份就是第二份会静默脱钩的副本**，且分叉方向最坏 —— 接棒人读到的契约与画布实物不一致（原则㊾）。断言里也据此逐项比对现算值而非期望文本。

**为什么加这道抛错校验**：「有 master 却没 description」是本步唯一会**静默**发生的失败 —— 新增一类 master 而忘了补描述时，画布上一切正常、数量校验也过，只有 Inspect 面板里空着，而那正是接棒人读契约的地方。

**写进 Pin description 的一条硬约束**：Figma **禁止对 Instance 增删子节点**（`Cannot move node. New parent is an instance`）。选中态（48×48 + 阴影）与带完整度角标态属结构差异，一律回退 `pinRaw` 原生构造、不复用 master。此约束此前只以踩坑记录存在，现钉进契约本体。

**验收**：布局探针 **156/156**（146 → +10）、Token 探针 **103/103**（101 → +2），全通。

**顺带修的探针脆性**：`probe-token-vars.py` 的「capsule 圆角仍为 RADIUS.full」原在 `buttonRaw` 函数体内查字面量，配色移出后当场失败（**失败方向安全，不会假绿**）。修法同条目 [54] 那次：取值路径改到 `BUTTON_SPECS`，并**新增一条「`buttonRaw` 确实从 `BUTTON_SPECS` 取值」** —— 否则前两条就成了对一张没人读的表的断言。

**过程中拦下的一处假绿**：探针 mock 的 `createComponentFromNode` 没有 `description` 字段，赋值会静默创建一个全新属性 ——「写没写」与「有没有这个字段」就分不清，也验不出「漏写」。修法：显式给初值 `''`（与 I1 那轮 `pluginData` 空实现同源的坑，第三次了）。

**新增沉淀原则**：
- **原则 54 · 契约类产出必须有一条「不写就报错」的硬校验**。它与像素类产出的失败方式完全不同：像素错了看得见，契约缺失只是某个面板空着。凡是「画布上看不见的产出」，都要在生成流程里安一道计数比对，把静默失败变成硬失败。
- **原则 55 · 写文档性内容前先检查它引用的值是不是真源**。本轮若不先提 `BUTTON_SPECS`，20 条 description 会立刻变成 20 处副本。**顺序是「先统一真源、再写引用」，不是「先写完再想办法同步」**。

### [56] 动效规格落 annotation（条目 [51] 第 6 步 / I5 落地）— 2026-08-27

**为什么单独有这一步**：色彩、字号、间距、圆角全都进了 Figma Variables，有 Figma 自己的一致性机制兜着 —— 改了变量，画布上所有消费点跟着变。**动效进不去**：`figma.variables.createVariable` 只接受 `COLOR / FLOAT / STRING / BOOLEAN` 四种 `resolvedType`，**没有时间与缓动类型**（条目 [48] 首次查明的硬约束）。这是 I5 存在的唯一原因。**不要再尝试把动效建成变量。**

**动效为什么是最容易蒸发的一类规格**：其余规格在静态画布上都能「看见」——色卡是颜色本身、字阶是字本身；动效只能以文字存在。**没有任何一处画面会因为漏了动效而显得不对**，截图验收必然放过。故给它一块专属画板 + 全部六档挂 `interact` 档标注 + 一整节断言。

**六档真源（`MOTION`，`code.js` 紧随 `CANVAS` 之后）**：

| 档 | 场景 | 时长 | 缓动 | 判据 |
|---|---|---|---|---|
| `page` | 页面切换 | 260ms | ease-out | §1.4.8 |
| `sheet` | 弹窗/抽屉 | 240ms | spring | §1.4.8 / §6.9 |
| `mask` | 遮罩渐显 | 180ms | ease-out | §1.4.8 |
| `press` | 按钮反馈 | 80ms | ease-out | §1.4.8 |
| `fade` | 内容淡入 | 180ms | ease-out | §1.4.8 |
| `layer` | 图层切换 | 200ms | ease-out | §6.8 |

**做法**：

| 措施 | 位置 | 作用 |
|---|---|---|
| `MOTION` + `motionLine(key)` | `code.js`（`CANVAS` 之后） | 六档唯一真源；`motionLine` 现算标注文本，断言得以拿同一个函数核对画布，而非在探针里手抄期望串 |
| `motionSpecLines()` | 同上 | 六档全量正文；首行复述「为什么这里没有变量」 |
| 画板 D「动效规格总览」 | `batchSetup` 规格板 | 六档逐行 + **时长条可视化（1ms = 0.5px）**，让「80ms 比 260ms 快得多」在静态画布上也看得出来 |
| 四处页面级落点 | 登录页 / 首页展开态 / 列表页 / 详情页 / T3 弹层第 1 层 | `press`→主按钮、`layer`→`_overlay-cats`、`fade`→`_list-body`、`page`→`_nav-left`、`sheet`+`mask`→`_sheet-mask` |
| 布局探针第十二节 | `probe-layout-offline.js` | 13 条：六档齐全、五字段完整、文本全等 `motionLine` 现算值、条宽全等 `dur × 0.5`、`target` 命中、`categoryId === 354:1`、一条反向 |
| Token 探针第六之二节 | `probe-token-vars.py` | 9 条 + 2 条反向：六档时长逐项**从 PRD 原文回标**、`spring` 未被换成具名曲线、三处消费点均现算 |

**为什么正文要复述「不进 Variables」**：读标注的人在 Dev Mode 里手边只有那张卡。他找不到 `motion/*` 变量时的第一反应是「设计漏配了」，然后自己拟一套时长 —— **规格于是在实现侧分叉**。把原因写进正文，是让「这里没有变量」从疑点变成结论。

**为什么四档要钉到具体页面而不只放画板 D**：按压反馈是全局规格，但「全局」在交接里等于「没有具体归属」。钉到登录页主按钮上，实现那颗按钮的人才会在 Dev Mode 里读到它。`layer` 档同理 —— §6.8 要的是「颜色渐变 200ms 而形状立即切换」，两个属性不同步，不钉到控件上这条差别没有落点。

**为什么 `ease` 一律用 PRD 原词**：`spring` 在 Flutter 是 `SpringSimulation`、在 CSS 里根本没有对应值。**替设计师把语义词落成具体参数属于替他做决定，超出「冻结契约」的范围**（条目 [51] 红线）。实现侧按 `impl` 字段口径自行选参。Token 探针为此专设一条断言，防「顺手换成具名贝塞尔」。

**清掉的一处副本**：画布上原有 **3 处手抄「240ms spring」**，其中**只有 1 处会渲染到画布**（首页筛选展开态卡片文案）—— 也就是唯一会被评审当判据读的一处，已改为 `motionLine('sheet')` 现算。另 2 处在注释里，保留。

**PRD 的两处真缺口（已落 `info` 档卡，须回写 PRD）**：
1. §1.4.8「地图 Marker 首次出现渐入 + 轻微上浮」**未给时长与位移量**；
2. §1.4.8「发布成功 Lottie 确认勾」**未给时长** —— Lottie 时长由素材本身决定，须在素材交付时一并冻结。

两条都**不替它拟值**，且单独一张 `info` 卡：它们不是判据，是「为什么这里没有规格」。混进 `interact` 卡会让评审把待补项当成已冻结的规格读。

> **⚠️ 本小节已于 2026-08-27 被条目 [60] 更新**：上述两处缺口已回写 PRD §1.4.8 —— Marker 经用户拍板拟值（渐入 180ms + 上浮 8px 同起同止、错开 20ms 上限 10 个），Lottie 仍不拟时长但已冻结约束条件（600–1000ms、不循环、不阻塞交互、自带静态收尾帧、主色取 `color/primary`）。**故「不替它拟值」这条已被用户明确推翻，画布 info 卡也已同步改名为「动效规格的三条补充口径」** —— 阅读本小节时以条目 [60] 为准。

**一处易被误判为矛盾的口径差（记档以免被「修掉」）**：§1.5 U2 要求发布链路动效「统一 200-300ms」，而 §1.4.8 的按钮反馈是 80ms。**两者不同层**——U2 说的是转场，80ms 说的是按压反馈，不构成冲突。Token 探针两侧各钉一条断言：若有人为「消除矛盾」把 80ms 改成 200ms，当场亮红。

**验收**：布局探针 **172/172**（156 → +16：I5 节 13 条 + 基线断言由 1 条拆为 2 条 + 实机复验后补的槽位对齐 2 条）、Token 探针 **114/114**（103 → +11），全通。

#### 实机复验（2026-08-27，条目 [51] 第 6 步收尾）

在 Figma 桌面端跑完批次 1/2/3 后，我经 Bridge 逐项读节点属性核对（不只看截图），4 点全过：

| 核对点 | 实测 |
| --- | --- |
| 画板 D + 六根时长条 | 六条宽 `130/120/90/40/90/100`，恰为 `dur × 0.5`；六行文本与 `MOTION` 全等 |
| 两张卡在画板外部 | 均在 `x=1192`（画板右边界 1168 + 24），六档卡 `y=964`、待补卡 `y=1156` |
| `_motion-bar-sheet` 分类 | `categoryId: "354:1"` = Interaction ✅ |
| 五处页面级标注 | 6 个 target 全部命中，父链归属全部正确 |

**父链实测确认了两处最易挂错的地方**：`_nav-left` 全文件有 **36 个同名节点**，标注挂中的确实是 `detail-screen · 详情页` 那一个；`_sheet-mask` 有 3 个，只有 T3 第 1 层挂了，符合「层内切换不重播进场」的设计。**同名节点这么多的情况下，`target` 精确等值匹配 + `hits.length === 1` 才挂的写法是必要的**，若用关键字模糊匹配必然挂到靠前的无关节点上。

**一处差点被误判为缺陷的现象**：经 Bridge 读 info 卡的 `getPluginData('anno')` 返回**空字符串**，而离线探针里有值。原因不是数据丢失 —— **`setPluginData` 按插件 ID 分命名空间**：写入方是 s2s 插件，`figma_execute` 跑在 Desktop Bridge 插件里，读同一 key 只能得到空串。`annotations` 是节点原生属性、不分插件，所以那一路读得到。

由此得到一条**探针的复核盲区**并已写进探针注释：凡依赖 `pluginData` 的断言，真机侧无法用 Bridge 独立复核，真机要核只能走两条不分插件的路径 —— ① 节点 `annotations` 属性；② 卡内 TEXT 文本。两者与 `pluginData` 同源（见 `annotation()`），可互证。我已用这两条核过 info 卡的 3 行正文，与代码一致。

**一处只有看图才发现的真问题（已修）**：时长条与文本同排，条宽各档不同 → **六行文本左边缘参差**，最短的 `press` 档比最长的 `page` 档右突出近 90px，六档无法竖向对照扫读 —— 而并列对照正是这个画板存在的意义。修法：条外套一层等宽槽位 `_motion-slot-*`，槽宽由**最长档现算**（不写死 130，否则 PRD 调时长后条会被截短而画面看不出错）；同时把 `0.5` 提为 `MOTION_PX_PER_MS` 常量，因为条宽与槽宽都要用它，写两处就是新的手抄副本。补 2 条断言并反向验证（临时把槽宽写死成 100，两条如期亮红）。

**这一处暴露的问题比它本身重要**：六行文本内容全对、条宽也全对、断言 170 条全绿 —— 唯独排布让人读不成。**结构断言查不出可读性**。故新增原则 58。

**过程中修的两处探针自身的错**：
1. 「layout 前画框内共 2 张口径卡」写死基线，登录页补挂 `press` 档后变 3 张即失败。**没有把 2 改成 3** —— 那只是换个魔数，且「总数对了」不说明卡挂在该挂的画框上。改为**逐画框判定**（splash 1 张 / login 2 张），漏挂与挂错都拦得住。同时发现采样点在 `layout()` **之后**，那时卡已被移出、逐框数恒为 0，采样前移。
2. 画板 D 的两张卡在 `motionBoard` 内查找 —— 规格板同样走 `layout()`，`detachAnnotations` 已把卡提到画板右侧外部挂到 page 上，限定画板内找必然为空。改为 page 级查找，并**另加一条**「`_motion-bar-sheet` 真的被钉上 Interaction 分类的节点级标注」：卡的 `pluginData` 对了不等于 Dev Mode 里读得到，`target` 拼错时会静默退回整页。

**注释里刻意不写行号**：`MOTION` 头注释要指出三处手抄的位置，我第一版写了行号，随即发现本轮改动就会让它们漂移。改为按**函数名**描述（`categorySheet` 头注释 / `mapCanvas` 弹层定位处 / 首页展开态卡片文案）。**指错位置的注释比没有注释更误导。**

**新增沉淀原则**：
- **原则 56 · 无平台机制兜底的规格，断言就是它唯一的一致性机制**。Token 有 Variables、布局有画框尺寸，改错了都有第二重反馈；动效只有断言这一层。凡是「平台不提供承载类型」的规格，断言密度要按「唯一防线」的标准配，不能按「补充校验」配。
- **原则 57 · 基线类断言不要写死总数，要按归属逐项判**。总数断言在每次正常新增时都会失败，于是每次都被改成新的数字 —— 改到第三次就没人再想它守的是什么了。按归属拆开后，新增只影响对应那一条，且「挂错位置」这类总数看不出的问题也能被拦下。
- **原则 58 · 结构断言查不出可读性，凡「为对照而做」的版面必须看一眼渲染图**。画板 D 六行内容全对、条宽全对、170 条断言全绿，但文本左边缘参差使六档无法扫读。断言能证明「每个元素的值是对的」，证明不了「放在一起是能读的」。**判据**：若某个版面的存在理由是「让人并列对照」，那它就必须过一次视觉核对；纯数据承载的结构可以只靠断言。发现问题后要做的是**补一条结构化断言把它固化**（此处为「六槽等宽」），而不是只改一次版面 —— 否则下次照样漂回去。

### [57] I2 / I3 评估（条目 [51] 第 7 步）— 2026-08-27

**这一步为什么是「先评估再定」而不是直接做**：I2/I3 是六条 ideation 里信心最低（76% / 71%）、复杂度最高（Medium / High）的两条，且其判据全部写于 **2026-08-26**。此后 Token 已 43/43 全绑变量、20 个 master 已补 description —— **原判据大半已失效**。故先核实当前事实，再判做不做。以下每条结论都经本轮实测或 file:line 核对，不引用 8/26 的旧数字。

#### 一、先纠正三处已失效的旧判据

| 条目 [48] 的原始记载 | 当前实测 | 影响 |
| --- | --- | --- |
| 「Figma 侧 Token 落地率 21/38」（`:1132`） | **43/43**（COLOR 26 + FLOAT 17），`code.js:426-473` 现算自报 | I2 的主要动机「Figma 侧落地不全」**已不存在** |
| 「I3 依赖 `figma_create_slot`」（`:1286`），该工具 schema 标注 "Requires Desktop Bridge" | `figma.createSlot` **全局不存在**，但 **`ComponentNode.createSlot()` 实例方法存在且可用** —— 本轮实测建出 `type === 'SLOT'` 的节点 | I3 **不依赖 Desktop Bridge、不依赖 MCP 工具**，可走已跑通三轮的 `code.js` 路线 |
| 「20 个 master 全无 description」（`:1132`） | 20 个已全部写入契约 description（条目 [55]） | I3 的「交接信息缺失」动机已被第 5 步覆盖 |

**本轮实测记录（一次性探针，跑完即删，无残留）**：

```text
combineAsVariants: function        createComponentFromNode: function
figma.createSlot:  undefined       ComponentNode.createSlot: yes
实跑 combineAsVariants([a,b]) → type=COMPONENT_SET
  componentPropertyDefinitions = ['State']（由 'State=xxx' 名字自动派生）
  children = ['State=default','State=hover']
variant.createSlot() → type=SLOT, name='Slot' ✅
```

顺带查明一条**新的能力边界**：`componentPropertyDefinitions` **只能读 COMPONENT_SET 或非 variant 的 COMPONENT**，在 variant 子组件上读会抛错（原文：`Can only get component property definitions of a component set or non-variant component`）。属性定义住在**集合**上，不在各 variant 上 —— 写断言时若照 variant 去读必然误判为「属性没建上」。

#### 二、I3（20 master → 变体集）评估

**当前结构**（`code.js:1890-1914`，`registerComponents` 是唯一注册点）：

| 族 | 数量 | 构造函数 | 天然属性轴 |
| --- | --- | --- | --- |
| `ui/pin/{5 分类}/{resource\|demand}` | 10 | `pinRaw`（5 参） | **二维**：分类 × 供需态 |
| `ui/button/{6 档}` | 6 | `buttonRaw` 查 `BUTTON_SPECS` | 一维 6 档（但混了层级/形状/状态三种语义） |
| `shell/bottom-tab/{3 项}` | 3 | `bottomTabRaw(active)` | 一维：选中项 |
| `shell/status-bar` | 1 | `statusBarRaw()` 无参 | 单例，无变体需求 |

**关键事实：20 个 master 的构造函数早已全部参数化、且已由循环驱动注册**，没有一处逐个写死。也就是说 I3 要改的**不是**代码结构，而只是「参数化的产物在 Figma 里如何组织」—— 从 20 个平行 COMPONENT 变成 4 个（或 3 个）COMPONENT_SET。改动集中在 `registerComponents` 一个函数内。

**收益**：
1. **Figma 侧的可发现性**：接棒人在 Assets 面板看到的是 20 个平铺条目，还是 4 个带属性下拉的组件 —— 后者才是设计系统的常规形态；
2. **Pin 的二维结构能被表达出来**：现在「5 分类 × 2 供需态」这个笛卡尔积只存在于注册循环里（`code.js:1908-1911`），在 Figma 里看不出来。变体集会把它显式成两个属性轴；
3. **实例切换成本**：`setProperties` 换档 vs 现在的「删掉旧 Instance、按名字取新 master 建新 Instance」。

**代价与风险**：
1. **命名要重排**：`combineAsVariants` 靠 `Prop=Value` 的**名字**派生属性轴。20 个 master 现在的名字（`ui/pin/cat-work/resource`）不是这个格式，要改成 `分类=work, 供需=resource`。而 `button()` 包装器**依赖 Instance 改名**来支撑批次 5 的跳转按名定位（`code.js:1836-1839` 有明确注释：Instance 默认继承 master 名会导致同框重名、跳转失败）—— **这条链要一并核**，是主要风险点；
2. **`ui/button` 的 6 档语义不齐**：`capsule` 是形状、`disabled` 是状态、其余 4 个是层级。硬做成一维 6 值会把三种语义压进一个轴；拆成 `层级 × 形状 × 状态` 三轴则产生 4×2×2 = 16 个 variant，而其中大部分组合（如 `ghost + capsule + disabled`）**PRD 里并不存在**。这是设计决策，不是编码问题；
3. **SLOT 的实际用途有限**：I3 原设想用 SLOT 承载「结构差异变体」，指的是 Pin 的选中态（48×48 + 阴影）与完整度角标态 —— 但 `code.js:1860-1862` 已记档：**完整度角标 8/24 就从地图 Marker 卸掉了**，现在只有 T6 组件板会显式传 `showCompleteness=true`。也就是说 SLOT 要解决的问题**只剩 1 处、且只出现在规格演示板上**；
4. **80+ 个消费点**：`button(` / `pin(` / `statusBar(` / `bottomTab(` 全文共 **83 处**调用。包装器签名若不变则消费点不用动，但这要求包装器内部把「档位参数 → `setProperties`」的映射做对。

**我的判断：I3 不做，或只做 Pin 一族。** 理由：它的三条原始动机（description 缺失、Figma 侧组织混乱、SLOT 承载结构差异）里，第一条已由第 5 步解决，第三条的适用面已萎缩到 1 处演示板，只剩第二条「Assets 面板的组织形态」是真收益 —— 而这属于**像素/形态层面的整理，不是契约**，正落在条目 [51] 划的红线外侧（冻结契约、不做形态精修）。若一定要做，**只合 Pin 一族**性价比最高：它是唯一有真二维结构的族，且 10 个 master 的名字重排不触碰 `button()` 那条跳转定位链。

#### 三、I2（Token 权威源移到 DTCG + dp 出口）评估

**当前状态**：Token 有**两个**承载体且已同步 —— `code.js:22-115` 的 JS 真源表（唯一权威）与 Figma Variables（由 `ensureVariables` 从真源表派生，43/43）。`probe-token-vars.py` 114 条断言守着两者一致。**无任何 DTCG/JSON 文件，无 dp 导出代码**（全仓 `**/*token*.{json,yaml,yml}` 零命中；`code.js` 里 Flutter 的 6 处命中全是注释与标注文案）。

**I2 想解决的问题**：现在 Flutter 侧要拿 Token，只能人读 `code.js` 或读 Figma 变量面板再手抄进 Dart —— **手抄就会脱钩**，且 `figma_diff_versions` 明确不追踪变量值变更（条目 [48] 查明），主色对比度余量只有 0.41（4.91:1 对红线 4.5:1），**调深一档不会有任何机制报警**。

**但 I2 的原方案（权威源移到 DTCG，Figma 与 code.js 双降为投影）代价过大**：`code.js` 的 Token 消费方式是 `SPACING.md` 这样的直接引用，改成从 JSON 读要动数百处；而插件 `manifest.json` 的 `networkAccess.allowedDomains: ["none"]`，插件运行时**读不到本地 JSON 文件**（Figma 插件无文件系统访问），只能把 JSON 内容编译进 `code.js` —— 那 JSON 就又变成了副本，权威源反而更不清楚。

**更小的替代方案（我的建议）**：**不动权威源，只加一个单向出口**。保留 `code.js` 真源表为唯一权威，加一段从真源表生成 Dart 常量文件的导出（Node 脚本，不进插件），并在 `probe-token-vars.py` 加断言「导出物与真源表逐项一致」。这样：
- 权威源不变，数百处消费点不动；
- Flutter 侧不再手抄，且脱钩会被断言当场拦下；
- 顺带解决「变量值改了没人知道」—— 导出物进 git，`git diff` 就能看见主色变化，绕开 `figma_diff_versions` 的盲区。

**这个替代方案不在 I2 的原始描述里**，是基于本轮核实的事实（`networkAccess: none`、43/43 已同步、消费方式是直接引用）重新裁的。**须你拍板**。

#### 四、结论汇总

| 条目 | 原方案 | 我的建议 | 理由要点 |
| --- | --- | --- | --- |
| I2 | 权威源移 DTCG，Figma 与 code.js 降投影 | **改小：只加 Dart 单向出口 + 一致性断言** | 插件读不到本地 JSON（`networkAccess: none`），移权威源会让副本更多；真正的缺口是「Flutter 侧手抄」与「变量值改动无痕」 |
| I3 | 20 master 合成变体集 + SLOT | **不做，或只合 Pin 一族** | 三条动机里两条已失效（description 已补、SLOT 适用面萎缩到 1 处演示板），剩下的是形态整理、落在 [51] 红线外；`button()` 那条改名→跳转定位链是真风险 |

#### 五、用户拍板结果（2026-08-27）

- **I2 → 采纳「改小」方案**：真源表仍为唯一权威，加 Node 导出脚本生成 Dart 常量 + 一致性断言。**原 DTCG 方案作废**（不回改条目 [48] 的记载，与 [41]、[48] 处理死稿的做法一致）。
- **I3 → 不做，结案**。结案理由即上述评估：三条动机两条已失效，剩余收益属形态整理，落在条目 [51]「冻结契约、不做形态精修」红线外侧。**20 个平行 master 的形态就是本次交棒的最终形态**，接棒人在 Assets 面板看到的是 20 个平铺条目 —— 这一点已由 20 条 description（条目 [55]）与画板 D 等规格板补偿，不构成契约缺失。

**若将来要重启 I3，前置条件是**：① 有外部设计师接棒（形态整理才有受众）；② 先定 `ui/button` 六档的属性轴怎么拆（层级/形状/状态三种语义混在一维里，这是设计决策不是编码问题）；③ 先核 `button()` 的 Instance 改名 → 批次 5 跳转按名定位这条链（`code.js:1836-1839`）。

**新增沉淀原则**：
- **原则 59 · 隔了若干轮才执行的方案，执行前必须重验判据而不是重读方案**。I2/I3 的判据写于 8/26，到执行时三处已失效（落地率 21/38 → 43/43、依赖 Bridge → 不依赖、无 description → 已补）。若直接照方案开工，会为已经解决的问题付第二次成本。**方案会过期，事实不会** —— 重验的成本远低于做错。
- **原则 60 · 「工具不存在」要分清是全局缺失还是挂在别的对象上**。`figma.createSlot` 确实 undefined，若就此判定「能力不可用」，I3 会被错杀 —— 它实际是 `ComponentNode.createSlot()` 实例方法。**查能力要同时查全局与相关节点类型的原型**，只查一处就下结论会错判可行性。


### [58] I2 落地：Token 的 Dart 单向出口（条目 [51] 第 7 步收尾）— 2026-08-27

按条目 [57] 第五节的拍板执行，三件套全部落地，条目 [51] 七步至此**全部结案**。

#### 一、产出

| 文件 | 性质 | 说明 |
| --- | --- | --- |
| `prototype-figma/export-dart-tokens.js` | 新增，工具 | 从 `code.js` 六张真源表生成 Dart 常量；不进插件，独立 Node 脚本 |
| `lib/design_tokens.dart` | 新增，**生成物** | 43 项（COLOR 26 + FLOAT 17），头部标「请勿手改」并给出重跑命令 |
| `prototype-figma/probe-token-vars.py` | 改，加断言 | 新增第「三之二」节 7 条正向断言 + 3 条反向用例；**114 → 125** 全绿 |

产物结构：`AppColors`（16 项语义色）/ `AppCategoryColors`（5 原色 + 5 深色变体）/ `AppTypeScale`（6 档 × size+weight+lineHeight）/ `AppSpacing`（6 阶）/ `AppRadius`（5 阶）。

#### 二、几个刻意的取舍

1. **颜色存 `int`（`0xAARRGGBB`）而非 `Color`，全文件零 `import`** —— 用户拍板项。现在还没有 Flutter 工程，产物必须能被任何 Dart 项目直接引用，消费侧自行包 `Color()`。有断言按**行首**匹配钉住「无 import」。
2. **导出脚本用 eval 真跑 `code.js`，不用正则解析** —— 正则只能取到写在源码里的字面量，取不到求值结果。哪天某张表改成由函数拼装，正则会静默取空，eval 不会。复用 `probe-layout-offline.js` 的加载模式（整体 eval + 最小 figma mock 消 `showUI`/`ui.onmessage` 副作用）。
3. **行高在 Dart 侧也是派生 getter（`lineHeightPx`）而非存字段** —— 与 code.js「行高不入 Variables」同一个理由：单独存一份就是第二份副本，改了字号忘改行高会静默不一致。
4. **字重保留 PRD 原词字符串（`'SemiBold'`），不映射成 `FontWeight`；`MOTION` 整张表不导出** —— `ease: 'spring'` 在 Flutter 是 `SpringSimulation`、在 CSS 无对应值，替设计师把语义词落成具体参数超出「冻结契约」的范围（条目 [51] 红线）。动效仍只落 annotation。有断言钉住产物里不出现 `spring`/`MOTION`。
5. **断言按产物里 `/// <Figma 变量名>` 的注释取值，不复制导出脚本的 camelCase 转换规则** —— 复制一份转换规则就是新的手抄副本（原则㊾）。
6. **比对用「整表相等」而非逐键循环** —— 逐键只能发现「产物值不对」，发现不了「产物多了一项真源已删的 Token」。

#### 三、为什么这次留产物，与「跑完即删」不矛盾

`probe-token-vars.py` 的 docstring 末句写着「跑完即删，不留产物 —— 中间文件只会多一份要同步的副本」。这条仍然有效，它防的是**可被双向编辑的副本**。本产物是**单向出口**：只由脚本生成、头部标了禁止手改、且有断言逐项把它钉在真源上，脱钩当场变红 —— 不存在需要人去手工同步的第二份。

留它的**唯一理由**是让 Token 取值变更进 `git diff`。Figma 侧 `figma_diff_versions` 不追踪变量值变更（条目 [48] 查明），主色白字对比度余量只 0.41（4.91:1 对红线 4.5:1），此前**悄悄调深一档不会有任何机制报警**。产物进 git 之后，这个盲区被 `git diff` 填上。

#### 四、验证

- Token 探针 **125/125**（新增 7 条正向 + 3 条反向），布局探针 **172/172**（未受影响）；
- 三条反向用例造的正是「真源改了、产物没重跑」这个场景（只改 `code.js` 文本、不动磁盘产物），全部确认会红：改主色、改圆角 lg、**只改字重不改 size**（后者专防「只比 size 就算过」）；
- 本轮实际踩到一次误伤：断言原写「全文不含 `import `」，被产物头注释里那句自我说明「刻意不 import 'package:flutter/material.dart'」判红。改为按行首 `^\s*import\s` 匹配 —— Dart 的 import 只能顶格，按行首判定既准，也不会因注释措辞变化而漂移。

#### 五、后续消费约定

改 Token 的流程固定为：改 `code.js` 真源表 → 跑 `node prototype-figma/export-dart-tokens.js` → 跑 `python prototype-figma/probe-token-vars.py` 确认全绿 → Figma 侧重跑批次 1 让 Variables 对齐。**漏第二步会被第三步拦下**。

**新增沉淀原则**：
- **原则 61 · 「不许有副本」的真正含义是「不许有第二个可编辑处」，不是「不许有第二份文件」**。生成物只要满足三条 —— 单一生成路径、显式标注禁止手改、有断言钉在真源上 —— 就不是副本，而是出口。把它一律禁掉的代价是真源无法被下游机器消费（本例即 Flutter 侧只能手抄）。**判据是可编辑性与是否有守，不是文件数量**。
- **原则 62 · 断言的匹配范围要窄到语法层面，否则会被"解释自己为什么不这么做"的注释误伤**。产物注释里写「刻意不 import Flutter」来交代设计决定，全文搜 `import` 立刻假红。**代码里的自我说明文字与被检查的代码同处一个文件，宽匹配必然把说明当违规** —— 按语言的语法位置（行首、函数体内、去注释后）收窄，而不是搜关键词。

---

### [59] Marker 是否印价格：定案不印（关闭取证待决项 #4）— 2026-08-27

**背景**：条目 [52] 的地图 UI 外部先例取证留下待决项 #4 —— Zillow 式做法把价格直接印在 marker 上，让地图本身成为比价工具，属业界 gold standard；而本产品 PRD §6.9 有明文硬约束「Pin 只表达供需态 + 完整度档两类信息」。二者正面冲突，当时按原则㊽记档不擅自改，等用户拍板。

**结论：不印。** 用户拍定，判定记录已回写 PRD §6.9（紧随原有「与 §6.13 的边界」段之后）。

#### 一、判定方法：复用 §6.9 自己给过的公式

§6.9 此前判过一次同类问题 —— T6 的亮度差异（×1.3/×1.1/×1.0）与蜂窝密度着色算不算引入第三类信息维度？结论是不算，理由是二者满足 `亮度 = f(完整度档)`、`色深 = f(发布密度)` 的一对一映射，只是**换通道表达已有信息**。

把价格代入同一个公式：`价格 ≠ f(供需态, 完整度档)` —— 它既不是供需态、也不是完整度档，是独立的第三个维度。**故按 §6.9 自己的判定标准，价格属第三类信息维度，不是强调手法，不得引入。**

#### 二、三条独立支撑（即便不看 §6.9 约束也不宜印）

1. **Zillow 的前提条件本产品不具备**。价格上 marker 成立的前提是**标的物同质、价格可比**（都是房子，用户就是来比价的）。本产品五大类混排：工作是薪资、房屋是租金、车辆是顺路车费、生活是闲置物价、服务是工时费 —— **同一张地图上五个数字语义不可比**，「8000」是月薪还是月租只能靠颜色猜，反而增加认知负担；
2. **与北极星指标错位**。核心指标是**首次联系触发率**，地图的职责是「发现附近有什么」；价格属决策后期信息，§6.4.2 已把它放在点击 Marker 弹出的信息卡里，路径只多一次点击；
3. **存在结构性无价标的**。求租/找房、薪资面议的岗位、§2.4 的「租房信息咨询」本就没有价格字段。留空会造出「有价签／无价签」两种视觉，而这个差异**不表达任何设计意图**，纯粹是数据缺失的泄漏 —— 用户会把它读成某种分类，而它什么也不代表。

#### 三、本次不改动任何既有规格

§6.4.2 marker 视觉规格原样保留（分类色 + 图标 + 供需实心/空心 + 完整度角标），价格保持在信息卡内。**本判定是关闭一个待决项，不是变更**，故无需重跑 Figma 批次、无需动探针。

同时在 PRD 里写明**重启前置条件**：① 分类体系收窄到单一垂直领域；② 或先定义跨类目可比的统一计价口径；③ 且须重新论证它为何不构成第三类维度 —— **本节约束不因「外部有先例」而放宽**。写这一段是为了防止下次有人再提时从头吵一遍。

#### 四、过程中的一次自查纠错

初稿在支撑理由 3 的末尾写了「违反 §6.4.2 marker 视觉必须由设计意图驱动的前提」，随后去 §6.4.2 核实，**发现该节并无这句原文** —— 那是我自己的推论，被我写成了对 PRD 的引用。已改为直接把道理说出来，不假借不存在的条款。同时把两处笼统的 §6.4 收窄为精确的 §6.4.2。

**新增沉淀原则**：
- **原则 63 · 写进 PRD 的每一处交叉引用都必须回读原文核实，推论不得伪装成引用**。本轮差点把「§6.4.2 要求视觉由设计意图驱动」写进 PRD —— 道理本身站得住，但 §6.4.2 里没有这句话。**一旦落纸，后人会把它当既有条款去引用、去推导**，错误会复利放大；而且这种错**最难查**，因为它读起来完全合理。判据很简单：凡写下 `§x.y 要求/规定/明确……`，就必须去 §x.y 把那句话找出来；找不到，就把道理直接写出来，不挂靠条款。

---

### [60] 补齐 UI 设计侧三项 PRD 待补 — 2026-08-27

条目 [52] 取证与条目 [56] I5 遗留的三项 PRD 待补一并补齐，并同步画布口径。三项**性质不同**：前两项判据来自客观事实，可直接落笔；第三项没有客观正解且需推翻我自己写下的约束，故请用户拍板后才动。

#### 一、定位权限状态三分（§6.4.4 + §6.8，取证缺口 #6）

原文只写「第一段 · 位置权限未开」，**把两种成因不同、可行动项也不同的状态合并了**。拆为三态：A 从未授权（弹系统弹窗）/ B 曾授权后被关（跳系统设置）/ C 已授权但取点失败（走 §6.8 降级）。

- **为什么必须拆**：**系统拒绝是 sticky 的** —— 拒绝后同一应用不再弹系统弹窗。B 态若仍显示「开启位置权限」，点了**毫无反应**，用户会判定 App 坏了。这是功能缺陷级别的问题，不是文案问题；
- **B 态判定要靠本地持久化标记**：系统 API 只回答「现在有没有权限」，**不回答「以前有没有过」**。故首次成功取点时写 `location_granted_once`。卸载重装复位为 A 态可接受 —— 重装用户确实该重走引导；
- 一并写入 pre-prompt 强约束（不得跳过引导页直接弹窗，系统弹窗只有一次机会）、文案须给具体收益、三态均须有「不用了」出口、A/B 共用同一套布局只换四处文案与动作（不做成两页，避免各自漂移）；
- **§6.8 那行「定位失败 ≥3 次」同步注明专指 C 态** —— 否则权限问题会被计入失败次数，两套机制互相干扰。

#### 二、聚合圆尺寸三档（§6.4.2，取证缺口 #2）

`2–9 → 32×32` / `10–99 → 40×40` / `100+ → 48×48`，即画布上已落档的值回写 PRD。

- **为什么密度必须由尺寸表达**：圆内数字在地图缩小后**先于圆本身不可读**，只靠数字等于密度信息在最需要它的缩放级别上消失；
- **2–9 档刻意比单点 40 小**：让小簇不抢过单点注意力；
- **与 §6.15 不冲突**：阈值管「何时开始聚」，本档位管「聚成多大」；
- **与 §6.9 的关系**：`尺寸 = f(聚合条数)`，按条目 [59] 刚确立的判定公式属换通道表达，不引入第三类维度 —— **这是原则㊽「同一个判定方法应复用」的第一次实际复用**。

#### 三、动效两处（§1.4.8）—— 需要推翻自己写下的约束，故先请用户拍板

条目 [56] 的 info 卡原文写着「**本板不替它拟值（替设计师做动效决定超出「冻结契约」范围）**」。现在要补值，等于推翻它。**我没有悄悄推翻，而是把矛盾摆出来请用户定** —— 用户拍板「Marker 我拟值，Lottie 先定约束」。

- **Marker 渐入 180ms**：与本表「内容淡入 180ms」同档 —— 两者都是「内容出现」语义，**同语义不该有两个数**；
- **上浮 8px**：取 `SPACING.sm`，不另造数字。「轻微」的量化边界是：下限能被看见、上限不产生位置歧义（Marker 标的是精确坐标，位移过大会让人误读落点）；
- **渐入与上浮同起同止**：分开计时会产生「先浮后显」的错位感；
- **多 Marker 错开 20ms，上限 10 个**：不设上限则 `20ms × N` 随 Pin 数线性膨胀，500 个 Pin 等于 10 秒；
- **与 §6.10 SLA 的关系写清**：SLA 计时终点是「Pin 完成首屏绘制」，入场动画不在口径内；**但注明实现侧不得因此放宽** —— 动画期间用户仍在等可读状态，最坏 380ms 已接近感知阈；
- **平移带出的增量 Pin 不做入场动画**：平移时视线在跟随手势，此时做动画只显得卡顿；
- **Lottie 只定约束不定时长**：时长是**素材文件的属性**，素材还没做。硬写一个数等于给出「到素材交付时必然要改的假规格」。改为冻结 600–1000ms 区间、不循环、不阻塞交互、自带静态收尾帧、主色取 `color/primary`、交付时把实测值回填 PRD 与 `MOTION` 表。

#### 四、同步画布：不能只补 PRD

**只在 PRD 补而留着画布说「未给」，画布就成了与 PRD 相矛盾的第二个口径，而评审看的是画布**（原则㊾）。故 info 卡由「动效规格的两处待补」改名「三条补充口径」并指向 PRD 新规格；聚合圆 annotation 去掉「待补」字样。

**布局探针如实报红**：有条断言按旧卡名 `_annotation/动效规格的两处待补` 匹配，改名后立刻 FAIL。这正是断言该干的事 —— 随之改名，但**判据（info 档 + 3 条）原样保留，只改被匹配的卡名**。

#### 五、验证

- 布局探针 **172/172**、Token 探针 **125/125**；
- 本次改的是文档与 annotation 文案，**未动任何布局结构或 Token 取值**，故无需重跑 Figma 批次；下次重跑批次 3/5 时画布文案会自动更新。

**新增沉淀原则**：
- **原则 64 · 待补项一旦补上，必须同时清掉所有「此处待补」的标记，否则等于制造矛盾口径**。本轮画布上有两处写着「PRD 待补」，只补 PRD 不动它们，评审读画布会得到与 PRD 相反的结论。**「待补」是一种有到期日的记录，补上之日必须回收** —— 且回收范围要按标记搜一遍（本轮靠 grep `待补` 找齐），不能凭记忆。
- **原则 65 · 规格缺口分三类，只有第一类能自行补**。① **有客观判据的**（sticky 是系统行为、小字先于图形不可读）—— 直接补，写清依据链；② **无客观正解但有体系约束的**（动效时长）—— 可拟值，但须先摆明它推翻了什么、依据是哪条现有档位，请用户拍；③ **判据尚不存在的**（Lottie 时长取决于未制作的素材）—— **不得拟值，只能冻结约束条件 + 标明回填时机**。把第三类硬写成数字，是用一个假规格换取「文档看起来完整」。

---

### [61] 2026-08-27 · 像素精修（限定「全局排版规律」一档）——A1/A2/A3 + 间距裸值收口

**触发**：条目 [51] 划定的 M3 范围（冻结契约、七步）已全部完成，但「UI 设计阶段」并未结束 —— 逐页视觉精修从未启动。经用户批准，把条目 [51] 的范围红线**放开一档**（见 `:1290-1292`）：只做**全局排版规律**（文本左边缘对齐 / 卡内间距节奏 / 模块间留白 / 行高与扫读性四类），**逐页美感精修仍不做**。验收标准同时定死：**先列问题清单、由用户勾选取舍，改完即结**，避免「像素精修无探针可验」导致无限期往下修。

按规则 4.2「绝不允许超出计划」，**先改计划再执行**：本轮首个动作是更新条目 [51] 红线与 M3 里程碑的过期数字（「14 页视觉稿」→ **18 个主态 + 23 个变体 = 41 框**），之后才动代码。

#### 一、四类排查结论

| 类 | 排查项 | 结论 |
|---|---|---|
| ① 对齐 | 文本左边缘 | **查出 3 组参差**（A1/A2/A3，见下） |
| ② 间距 | 取值是否都在 SPACING 阶梯上 | **查出 9 处裸值 `2`**（B 类） |
| ③ 字号字重 | 是否都走 `TYPE_SCALE` | **零违规** —— `text()` 是唯一出口，非法字阶名静默降级为 body |
| ④ 行高 | 是否显式设置 | **零违规** —— `text():1216-1229` 全量显式写 `lineHeight` |

清单交用户勾选，拍板：**A1+A2+A3 全改**、**B 类保留 2 + 加豁免声明与断言**。

#### 二、A 类三处：全是结构性修正，不是挪像素

**A1 · marker 信息卡四行统一起笔线**。改前四行文字左缘实测 **36 / 0 / 18 / 0** —— 标题被 28 圆标 + 8 间距推到 36、摘要直接挂在卡上是 0、完整度被 10 色点 + 8 间距推到 18、动作行又回到 0。同一层级的四行读成四个层级。改法是**改结构而非补 padding**：`_mi-head` 拆成左列圆标 + 新增 `_mi-body` 右列（`textColW = 358 - 24 - 28 - 8 = 298`），四行全落右列，左缘由结构保证。

**为什么刻意不写 `padLeft: 36`**：36 不在 SPACING 阶梯上，`bindNum()` 对非阶梯值是**静默不绑变量也不报错**（`code.js:672`），三处会一齐脱离 Variables 且毫无征兆。两列结构里这个 36 是 `28 + SPACING.sm` 现算出来的副产物，本就不该是一个间距 Token。

**A2 · 树内说明文字与勾选项同左缘**。说明文字原先直挂列表容器（左缘 0），而紧随其后的 `checkRow` 文字左缘是 `CHECKBOX_SIZE + SPACING.sm = 26`，说明与它所说明的那批项差 26px。新增 `listHintRow()`：**等宽空占位 + `SPACING.sm`**，而不是 `padLeft: 26`（同上，26 也会静默失绑）。顺带把复选框边长提成常量 `CHECKBOX_SIZE = 18`，让「与复选行文字对齐」有单一来源。

**A3 · `catTreeSheet` 左内缘提到父级**。改前 `padLeft: SPACING.lg` 由 `_sheet-title` / `_tree-lv1` / `_tree-lv2` / `_sheet-action` **四处各自重复声明** —— 任一处漏写即整行错位，**而错位在结构断言里零征兆**。提到 `_cat-tree-sheet` 后左缘只有一个来源，新增子行默认就对齐；五个子容器改传 `innerW = 390 - 32 = 358`。`_tree-lv3` 保留 `padLeft: SPACING.xl`（三级相对二级的**层级缩进**，属刻意差异，非重复声明）。

**这里踩了个 mock 能力边界**：子容器**刻意用固定宽而非 `layoutAlign: STRETCH`** —— 离线布局探针的 Auto Layout mock 不实现 STRETCH（`probe-layout-offline.js:22` 文件头已声明），用 STRETCH 会让探针量到 HUG 宽，这一屏的宽度断言从此失去意义。

#### 三、B 类：视觉不动，只消除「静默失绑」

9 处间距写字面 `2`（圆标与其标签、色块名与其色值、时长行与其 PRD 出处等——**均为一个语义单元内部的两行**，用阶梯最小值 `xs=4` 会把它们拉成两条独立信息）。视觉本身没问题，问题是 `bindNum()` 对非阶梯值静默不绑，这 9 处**画面正常但已脱离 Variables**，是最难人工发现的一类漂移。

处理：新建常量 `TIGHT_GAP = 2`，写 21 行豁免说明，9 处全部替换。**刻意不加进 `SPACING`** —— PRD §1.4.5 的阶梯是 4 的倍数，塞进 2 会破坏该规律，且会让它变成「可随意使用的通用档」。具名化换来两件事：① 豁免理由只写一遍；② 探针能钉住「全文只有这一个非阶梯间距值，且用量恰为 9 处」。

#### 四、探针新增 7 条断言，首跑 176/5 —— 5 条全是我自己的口径缺陷

三处改动全是结构性的，**改对了和改错了画面都可能看着正常**（原则 58：结构断言查不出可读性，反过来「看图」也查不出结构退化）。故新增「五之二、像素精修的三条不变量」小节，并把 `markerInfoCard` / `listHintRow` / `TIGHT_GAP` / `CHECKBOX_SIZE` 加入探针导出清单 —— **要真跑函数去量左缘，不能只做源码正则**：写了容器却忘挂子节点时，正则全绿而画面照旧参差。

首跑 **176 通过 / 5 失败**，逐条查完**无一条是代码缺陷**，5 条全是新写断言的取样口径缺陷：

| 失败项 | 真实原因 | 修法 |
|---|---|---|
| `padLeft:36@L2981` / `padLeft:26@L3218` | 这两处**是注释里的反例文字**（「为什么不用 padLeft: 36」），正则扫 `raw` 把注释也算进去 | 新增 `stripJsComments()` 逐字符剥注释后再扫。**不能用 `replace(/\/\/.*$/gm,'')`** —— 文件里有 `'http://www.w3.org/2000/svg'`（`:1030`）会被从 `//` 处截断；用等长空白替换以保持行号不漂 |
| `TIGHT_GAP 实测 11 处` | 数的是**字符出现次数**；`_radius-<档>` 与 `_ai-tag` 各在 padTop + padBottom 里用了 2 次 | 口径改为**按行计数**（一处 `box()` 调用写在一行内）= 9 |
| 左缘取值 `48 / 66 / 121` | 66 是完整度行里**色点之后**的文案、121 是动作行里 **spacer 之后**的「查看详情 ›」，都是合理的行内偏右元素 | 口径收窄为量 `_mi-body` 各**行容器**的起笔线，另加一条「行首文本未被行内 padding 二次推开」 |
| `实测 48，期望 36` | 期望值**漏算卡自身 `pad: SPACING.md`** | 改为 `SPACING.md + 28 + SPACING.sm = 48` |
| `仍有 _lv1-, _sheet-action` | `_lv1-<cat>` 是**胶囊自身的左内边距**（合理）；`_sheet-action` 是定长 200 字符窗口**跨过本次调用结尾**匹配到了紧随其后的 `_sheet-reset` | 窗口由 `{0,200}?` 改为截到本次调用的 `});`；`_lv1-` 与 `_sheet-reset/confirm` 列入 `OWN_PADDING_OK` 白名单 |

修完 **布局探针 182/182、Token 探针 125/125**（以探针自报数为准 —— PowerShell 的 `Select-String '^PASS '` 计数不可靠，此前实测得 0 而实际 163）。

#### 五、实机复验（2026-08-27，Figma 批次已重跑）

用户重跑批次后，**我直接在画布上量绝对几何**（`figma_execute` 读 `absoluteBoundingBox`），而不是靠肉眼比对截图 —— 参差 2px 肉眼看不出，坐标看得出：

| 项 | 实测 | 判定 |
|---|---|---|
| A1 `_mi-body` 5 行起笔线 | 全为 **x=2274**；行首文本亦全为 2274 | ✅ 改前 36/0/18/0 已消除 |
| A1 分隔线 / 动作行宽 | 均 **298** = 右列宽 | ✅ 同左缘不是靠缩窄内容换来的 |
| A3 三个弹层子容器 | `padLeft` 全 **0**、宽全 **358**、左缘全 = sheet + 16 | ✅ 四处重复声明已消除 |
| A2 说明行 vs 其下所有 `_check-row` 标签 | 同 x（**832 / 1542 / 1566**，三处弹层各自一致） | ✅ 改前差 26px 已消除 |
| A2 `_checkbox` 边长 | 全 **18** = `CHECKBOX_SIZE` | ✅ 单一来源生效 |
| A3 `_tree-lv3` 层级缩进 | `padLeft` **24** = `SPACING.xl`，文字 x 比二级 +24 | ✅ 刻意差异保留 |

**离线探针与实机的口径一致性得到交叉验证**：探针（mock）算出的起笔线 `SPACING.md + 28 + SPACING.sm = 48`，实机实测 `cardX = 2226 / bodyX = 2274`，**差值恰为 48**，且逐项对得上（`paddingLeft 12 + _mi-disc 宽 28 + itemSpacing 8`）。这说明 `offsetOf()` 那套「沿父链累加 padding + 前置兄弟宽」的替代口径是可信的 —— mock 不做真实坐标分配（`x` 恒为 0）这一限制没有影响结论。

（写这段时我差点把 `2274 - 2226 = 48` 当成实测写进来，而 `2226` 当时并未量过、只是我倒推出的数 —— 随即补量了一次才敢写。**推算与实测必须区分**，这正是原则 63 的同类错误。）

截图工具连续两次 `CAPTURE_SCREENSHOT timed out after 30000ms`，未强行第三次重试（几何数据已足以判定，且比截图更精确）。**整幅观感仍建议你自己扫一眼** —— 那是坐标查不出的部分（原则 58 的另一半）。

**新增沉淀原则**：
- **原则 66 · 源码正则断言必须先剥注释**。注释里写反例是好习惯（「为什么不用 X」），但它会被正则当成真代码抓出来 —— 本轮 5 条误报里有 2 条源于此。且剥注释不能用行正则，URL 里的 `//` 会误伤；要逐字符跟踪字符串状态，并用等长空白替换以保持行号。
- **原则 67 · 「用量锁定」类断言要数调用处，不要数字符出现次数**。同一处调用可能引用常量多次（`padTop` + `padBottom`），字符数与调用处数不是一回事，前者会让阈值莫名其妙地不准。
- **原则 68 · 量「对齐」要量行容器的起笔线，不是量所有文本的 x**。行内本就该有偏右元素（色点后的文案、spacer 后的动作），把它们混进「左缘一致」的比对里，断言必然假红。正确口径是两层：行容器起笔线一致 + 行首文本未被二次推开。
- **原则 69 · 断言首跑全红时，先假定断言错、而不是代码错**。本轮 5 条 FAIL 全是断言口径缺陷。真正危险的是反过来 —— 为了让断言变绿而去改代码，那等于用假绿掩盖真问题。判断标准是：**能说清这条 FAIL 对应的画面是什么样吗？** 说不清就是断言的问题。

---

### [62] 2026-08-27 M4 启动：四个阻塞点定案（版本 / 状态管理 / POC 机型 / 高德 Key）

**背景**：用户指令「进入 M4 阶段」。按规则 1.3 先读计划真源（派 search subagent 全量检索 M4 范围），再实测本机环境，**查出四个必须先定才能动手的阻塞点**。四项均已由用户拍板，本条目记录决策与依据；按规则 4.2「绝不允许超出计划」，**先改计划文档再动代码**，PRD 与本文档的改动已在本条目落地后提交。

**一、四项决策**

| # | 阻塞点 | 文档口径 | 实测现实 | 定案 |
|---|---|---|---|---|
| 1 | Flutter 版本 | PRD:1229 锁 3.41.9 / Dart 3.11.5 | 本机 3.41.4 / Dart 3.11.1 | **升到 3.41.9**，与 PRD 一致 |
| 2 | 状态管理 | **文档完全没写**（全库检索确认）| — | **Riverpod** |
| 3 | POC 机型 | PRD:1324 三台真机（含 iPhone）| 只有 Windows/Chrome/Edge + 2 个模拟器，无真机无 iPhone | **拆两级 + iOS 延后** |
| 4 | 高德原生 Key | 说明文档:125 称「v1 已接入 key」 | 全库只有 Web 端 JS key，原生 Key 一个都没有 | **先申请开发 Key** |

3.41.9 的存在性经官方 `releases_windows.json` 核实（Dart 3.11.5，2026-04-30 发布），**不是凭记忆写的版本号**；最新 stable 已到 3.47.1，此处刻意不追新。

**二、POC 为什么要拆两级**

PRD §6.10.1 原口径把两个不同问题捆在一个 POC 里，这才是它需要三台真机的原因：

- 「Dart 侧网格聚合有没有数量级缺陷」→ 这是**去/留决策**，模拟器就能答，且它才是 M4 第 1 天真正要的东西；
- 「P95 到底是多少」→ 这是**对外承诺的数字**，必须真机。

拆开后 POC-A 立即可开工，M4 不被设备阻塞；POC-B 单独排期。**关键设计：POC-B 不需要把手机连在开发机上** —— 帧耗时用 `SchedulerBinding.addTimingsCallback` 内置采集，应用内直显 P95 与直方图并落 JSON，于是「任何能装 APK 的安卓设备都能自行出数」。这套埋点与 PRD §6.10.1 兜底方案第 1 条（性能日志埋点）是同一份代码，不重复实现。

**三、iOS 这条是硬墙，必须明示而不是含糊过去**

iOS 安装包必须由 Xcode 签名，Xcode 只有 macOS 版；云真机同样要先有 `.ipa`，所以云路径也堵。**这是平台限制，不是配置问题，不存在「再试一下就能通」**。

因为 PRD §14.6 把聚合 POC 标为「🔴 阻塞 SLA 对外承诺」，如果不写清缺口，就等于**用 Android 的实测数据替 iOS 做了一个假承诺**。故在 PRD §6.10.1 与 §14.6 两处同时写明：本期 SLA 仅覆盖 Android；iOS 数据回填前不得以「已通过 POC」表述 iOS 端性能。§14.6 由 1 行拆成 3 行，iOS 那行标 ⚪ 不阻塞并注明原因。

**四、Key：绕开一个行政死锁**

原生 Key 申请要填 Package，而正式包名按上架前置手册 `:132` 须为 `com.<企业域名主体>.zhaoyazhao`，即**依赖 P2 域名主体** —— 而 P2、P3 实测都还是 ⬜ 未办（手册 `:142` 包名栏仍是 `com.____.zhaoyazhao`，`:485` P3 状态 ⬜）。若等行政流程，M4 地图整条线停摆。

解法：**先用开发包名 `com.s2s.zhaoyazhao.dev` 申请开发 Key 跑通 M4，P2/P3 落地后再申请正式 Key 替换**。代价是多申请一次 Key、多改一处配置。

申请口径已按高德官方文档（2026-08-25 版）写入 PRD §6.7，三条踩坑点：

1. 需填 **Package + 发布版 SHA1 + 调试版 SHA1** 三项，**缺调试版 SHA1 会导致 debug 包地图白屏**（最容易漏）；
2. **1 个 Key 只能对应 1 个应用**，跨应用复用会服务调用失败；
3. Package 须与 `build.gradle` 的 `applicationId` 完全一致，否则报 `INVALID_USER_SCODE`。

调试版 SHA1 取法（默认调试证书，密码固定 `android`）：

```powershell
keytool -list -v -keystore $env:USERPROFILE\.android\debug.keystore -alias androiddebugkey -storepass android -keypass android
```

**五、顺带修掉三处旧口径残留**

这三处都是「文档副本与真源不一致」，留着就会有人照着旧值实现（原则 63 同类风险）：

| 位置 | 旧口径 | 处理 |
|---|---|---|
| `:124` | 「Taro / RN / Flutter / H5 均可」 | 已被 PRD:1229 锁定取代，改为指向 PRD |
| `:125` | 「仅接 MarkerCluster 插件」+「v1 已接入 key」 | 前者被 PRD:1235 取代（改 Dart 侧自实现）；后者点明那是 Web JS key、**不能用于原生** |
| `:135` | 完整度加权公式（50%/30%/20%）| 已作废，改指向 PRD §9.8 三条件计数 |

**六、M4 待办由 4 项拆成 7 项**

新增 **M4-0（环境与阻塞项就位）** 与 **M4-5（出带签名 APK 的能力）** 两项。M4-5 容易被漏：M4.1 的 P7 工信部备案需要 APK 签名信息（MD5/公钥/SHA-1），所以 M4 不止要「跑通」，还必须**能出正式签名包**，且正式签名证书一旦启用不可更换。

**七、本轮新增原则**

- **原则 70 · 规格要求的设备/资源拿不到时，先问「这条规格在回答哪几个问题」，而不是先降级规格**。三台真机看似是一条要求，实际捆了「算法可行性」与「SLA 定标」两个独立问题，前者根本不需要真机。拆开后阻塞消失且规格没有被削弱 —— **降级规格是最后手段，拆解口径是第一手段**。
- **原则 71 · 平台级硬限制必须写成显式缺口，不能靠沉默掠过**。iOS 在 Windows 上出不了包，这件事如果只是「没做」而不是「写明没做」，那么 §14.6 的 🔴 阻塞项就会被当成已闭环，Android 的数字会被默认当成全平台承诺。**缺口写明了是已知风险，不写就是事故**。
- **原则 72 · 行政前置卡住工程时，找可替换的临时标识，而不是等或硬编**。包名依赖域名主体这类链式依赖，等下去会让整条开发线停摆；用 `.dev` 包名换一个可替换的开发 Key，代价是一次替换配置，换来的是不被行政流程绑住。前提是**替换点必须单一且写进文档**，否则将来会漏换。

---

### [63] 2026-08-27 M4-0 / M4-1 / M4-2：环境就位、POC-A 通过（D2 = 留）、脚手架与路由骨架落地

**一、Flutter 升级完成（M4-0 ①）**

`D:\developer\soft\flutter` 是 git 仓库，故**不能用 `flutter upgrade`**（它会跳到最新 stable 3.47.1，越过 PRD 锁定值）。改为按 tag 精确 checkout：`git checkout 3.41.9`。实测结果 **frameworkVersion 3.41.9 / dartSdkVersion 3.11.5**，与 PRD:1229 完全一致。

**二、发现并纠正 PRD 的一处不可实现规格（minSdk）**

这是本轮最有价值的发现。PRD 原写 **minSdk 21**，但从 Flutter SDK 源码实测：

| 项 | 值 | 出处（SDK 源码，非记忆）|
|---|---|---|
| Flutter 3.41.9 默认 minSdk | 24 | `gradle_utils.dart:54` |
| 低于此值仅警告 | < 24 | `DependencyVersionChecker.kt:112` |
| 低于此值**直接报错、构建失败** | < 23 | `DependencyVersionChecker.kt:115` |

**21 < 23，在锁定的 Flutter 版本下根本构建不出来**。根因是 PRD 把「高德 SDK 自身的下限（≥21）」直接抄成了「本项目的 minSdk」—— 两者不是一回事，正确做法是取两侧下限的交集。用户拍板 **抬到 24**，代价是放弃 Android 5.0–6.0（存量极少）。已同步修正 **PRD §6.7、BRD:312、MRD:480** 三处，并在 PRD 写明「为何是 24 而不是 21」，防止后人照高德文档改回去。

**这类错误的通用形态：把依赖方的下限当成自己的取值**。依赖越多，交集越窄，必须逐个核实而不是抄单一来源。

**三、POC-A 算法级基准：通过，D2 = 留（M4-1）**

产出 [tool/poc_a_cluster_benchmark.dart](./tool/poc_a_cluster_benchmark.dart)，纯 Dart（不 import Flutter），`dart run` 直接跑，排除渲染与 widget 生命周期干扰，测到的是算法本身。

实测（画布 1080px / 网格 80px / 7 轮中位数）：

| 分布 | 1 万点 | 5 万点 | 簇数(5万) | 点数×5 → 耗时× | 判定 |
|---|---|---|---|---|---|
| 均匀 | 1.48 ms | 4.73 ms | 980 | **×3.21** | 线性 PASS |
| 聚集（20 热点）| 0.63 ms | 3.61 ms | 131 | **×5.72** | 线性 PASS |

**为什么设两档分布**：均匀分布是算法的最好情况（桶多而浅）；真实城市数据扎堆，单桶会很深，**若实现里藏着桶内两两比对，只有聚集档能暴露**。两档都线性，才敢说没有数量级缺陷。

**判据不靠人眼看毫秒数，而是看比值**：理想线性为 ×5，平方复杂度会到 ×25 附近，阈值设 ×10 区分度足够且容得下哈希扩容与 GC 抖动。脚本直接打印比值与 PASS/FAIL。

**⚠️ 最重要的衍生结论：5 万点聚合仅 4.73ms，占 300ms 预算的 1.6%。** 这说明**性能瓶颈几乎必然在 Pin 渲染而不在聚合算法**。它直接改变 POC-B 不达标时的处置方向 —— 该优化渲染（降 Pin 上限、简化 Marker 图层），而不是去换聚合算法。若没做 POC-A 就直接上真机测总耗时，很可能会把时间花在优化一个只占 1.6% 的环节上。

另一个反向验证：聚集分布**比均匀分布更快**（桶少、哈希写入更集中）。若实现里有桶内两两比对，聚集档必然更慢 —— 实测更快，说明确实是 O(n)。

**四、聚合算法实现要点**（[lib/features/map/clustering/grid_cluster.dart](./lib/features/map/clustering/grid_cluster.dart)）

- **不 import Flutter**：聚合是纯计算，独立后可 `dart run` 跑基准、可单元测试；
- **分桶键含分类**（`gx:gy:categoryId`）：PRD §6.15 要求分类差异化聚合，且不同分类 Pin 颜色不同，混聚会导致聚合圈无法配色；
- **用 `floor` 而非 `round`** 定格号：`round` 会让边界点跳到相邻格，导致同一点在不同缩放下**归属抖动**；
- **簇中心取成员平均，不取网格中心**：点偏在格子一角时，网格中心会把聚合圈画到空白处；
- **累加坐标和后再除**，不边累加边求平均：避免浮点误差累积；
- **`memberIds` 保留全量且不可变**：点击聚合圈要能展开列表（PRD §6.9）；不可变防调用方误改结果。内存随点数线性增长，5 万档需在 POC-B 观察内存峰值。

配套 [test/grid_cluster_test.dart](./test/grid_cluster_test.dart) 7 条断言，钉住上述行为。**POC-A 只证明「不慢」，不证明「算得对」** —— 聚合算错的表现是 Pin 数量或位置不对，这类错误看图时极易被当成「数据就是这样」而漏掉，必须靠断言。其中「同格但分类不同不聚合」「边界点 floor 归属」两条是专门防重构改坏的。

**五、脚手架与路由骨架（M4-2）**

`flutter create --project-name zhaoyazhao --org com.s2s --platforms android,ios,windows`，89 个文件。**带 windows 平台是刻意的** —— POC-A/desktop 调试用得上。`lib/design_tokens.dart` 未被覆盖，`README.md` 是新增（`git status` 显示 `??` untracked，非覆盖）。

三处必配已落地：

1. `applicationId = "com.s2s.zhaoyazhao.dev"`（开发 Key 绑定用，替换点仅此一处）；
2. `minSdk = 24`（不再走 `flutter.minSdkVersion`，值与理由写在注释里）；
3. `AndroidManifest.xml` 加 `android:allowNativeHeapPointerTagging="false"` + 四项地图权限。**第 3 项是 🔴 上架驳回点**（PRD:1232：targetSDK ≥30 时不声明会导致从地图页返回闪退），注释里写明「不得删除」。

**路由清单从 11 项修正为 12 项** —— 我先前统计漏了 `ai-confirm-screen`（PRD:1819，P0 且属发布闭环）。这是**没逐行核对清单就报数**的错误，与原则 63 同类。路由表只注册 M4 的 12 项，**刻意不注册收藏/设置/我的发布等非 M4 页面**：注册空壳会让人误判已实现。

占位屏 [placeholder_screen.dart](./lib/features/placeholder/placeholder_screen.dart) 显著显示「未实现」+ 页面 ID + 关键特征。**不用空白 Scaffold 占位**：白屏分不清是「没实现」还是「实现了但渲染失败」。它同时是 Token 的第一个消费位，Token 接错会立刻看出来。

`main.dart` 里**刻意不初始化高德 SDK**，并把原因写进文件头注释：PRD §6.7 要求隐私协议同意后才可调 `AMapInitializer.updatePrivacyAgree`，在 `main()` 里图省事初始化会直接导致上架驳回。初始化点留到 M4-3 的隐私协议同意回调。

Token 接入集中在 `_buildTheme()`，不散落各页 —— 否则 Token 改动无法一处生效，等于白做 Token 层。`ColorScheme` 里 `surface`/`error` **显式指定而非让 `fromSeed` 推导**：那些色值是经 WCAG 实测定稿的（条目 [43]），不能被算法改写。

**六、依赖安装与验证结果（已闭环）**

- **依赖安装**：沙箱拦住 Pub Cache 写入（`hosted-hashes\*.sha256` 拒绝访问），`flutter pub add` 失败。但**版本解析已完成**，取实测值写入 `pubspec.yaml`：`flutter_riverpod ^3.3.2` / `go_router ^17.5.0` / `amap_map ^1.0.15` / `x_amap_base ^1.0.3`。改用 `flutter pub get` 后**安装成功**（`Got dependencies!`，`pubspec.lock` 已生成入库）。附带提示「15 个包有不兼容的更新版本」，属正常约束提示，**不处理** —— 逐个抬版本会连带引入未验证的破坏性变更，与本期范围无关（规则 4.2）。
- **三项验证全部通过**：
  1. `flutter test test/grid_cluster_test.dart` → **7/7 断言通过**；
  2. `flutter analyze` → 首轮 **1 error + 13 info**，修完后 **No issues found**；
  3. `dart run tool/poc_a_cluster_benchmark.dart` → 改导入路径后复跑仍 PASS（均匀 ×2.86、聚集 ×5.17；与首轮 ×3.21/×5.72 的差异是 JIT 与 GC 抖动，**判定不变**）。
- **analyze 修的三类问题**（都不是笔误，各有一条可复用的判断）：
  1. `main.dart` 的 `const TextTheme` **报 error**：`AppTypeScale.h1.size` 是自定义类属性，Dart 常量表达式不允许访问（`const_eval_property_access`）。**去掉 `const` 而不是把字号字面量抄进来** —— 抄字面量会让 Token 失去单一真源，用编译期常量换掉真源是赔本的；
  2. `test/` 与 `tool/` 用 `../lib/...` 相对路径导入触发 `avoid_relative_lib_imports`，改为 `package:zhaoyazhao/...`。同时把测试的 `package:test` 换成 `package:flutter_test`（`test` 只是传递依赖，直接 import 触发 `depend_on_referenced_packages`）；
  3. 基准脚本 10 处 `print` 触发 `avoid_print`，加文件级 `// ignore_for_file: avoid_print` 并注明理由（命令行脚本，print 是唯一输出手段）。**不是逐行加 ignore** —— 逐行加会淹没真正需要关注的告警。
- `pub add` 被拦前已往 `pubspec.yaml` 写过一次依赖，导致**重复声明**，已清理。教训：命令失败不等于没有副作用，**失败后要检查文件是否已被部分修改**。
- 删除 `test/widget_test.dart`（模板 counter 测试，`main.dart` 一改就编译失败）。
- `.gitignore` 补 Flutter 段：构建产物、`.dart_tool/`、**签名密钥（`*.jks`/`*.keystore`/`key.properties`）**。签名证书泄露等于应用可被伪造，且证书一旦启用不可更换（上架前置手册 P7）。`pubspec.lock` **刻意不忽略** —— 应用（非 library）应入库锁定依赖版本。

**七、本轮新增原则**

- **原则 73 · 把依赖方的下限当成自己的取值，是规格错误的高发形态**。高德标 minSdk 21、Flutter 要求 ≥23 才能构建，正确值是交集 24。依赖越多交集越窄，**每个下限都要回到各自真源核实，不能抄单一来源**。判断信号：规格里出现「某某 SDK 要求 X」时，问一句「其他依赖要求多少」。
- **原则 74 · 性能优化前先测各环节占比，否则很可能优化到不占比重的那一环**。POC-A 测出聚合只占 300ms 预算的 1.6%，直接推翻了「聚合是瓶颈」这个隐含假设，也改变了 POC-B 不达标时的处置方向。**先分解再优化，不要凭直觉认定瓶颈位置**。
- **原则 75 · 性能基准要设「最坏分布」档，均匀分布测不出藏着的坏实现**。均匀分布下桶多而浅，桶内两两比对的代价被摊薄看不出来；聚集分布单桶极深，问题立刻放大。**基准的价值取决于它能否暴露最坏情况**。
- **原则 76 · 命令失败不等于没有副作用**。`flutter pub add` 报错退出，但依赖已被写入 `pubspec.yaml`，我再写一遍就成了重复声明。**失败后要先检查文件状态，再决定重试还是修复**。
- **原则 77 · 编译期常量与单一真源冲突时，让 `const` 让路**。`const` 省的是一次运行时对象构造，代价却是把 Token 值抄成字面量、真源从此有两份。**性能收益要与可维护性代价放在一起比，不能因为编译器提示就顺手改数据**。
- **原则 78 · 静态分析必须清到零告警才算通过**。留着 13 条 info 的后果不是这 13 条本身，而是**下一条真正要命的告警会混在里面被忽略**。豁免要写在文件级并注明理由，让「为何豁免」可被追问。


### [64] 2026-08-27 隐私协议门补规格并落地 + M4-3 首页地图页（降级底图 + 筛选态 + 聚合渲染）

**一、读 SDK 源码推翻了自己的实现假设（本轮最重要的一条）**

我先按记忆写了 `amap_init_guard.dart`，假设「调 `AMapInitializer.updatePrivacyAgree()` = 初始化高德 SDK」，于是守卫只要拦住这一个调用即可。读包源码后发现两处都错：

| 我的假设 | 真源 | 出处 |
|---|---|---|
| `AMapInitializer` 在 `x_amap_base` 包 | 在 `amap_map` 包；`x_amap_base` 只有 `AMapPrivacyStatement` / `AMapApiKey` | 两包 `lib/src/` 全文 |
| `updatePrivacyAgree()` 会触发原生调用 | **它只给 Dart 静态变量赋值，不碰 platform channel** | `amap_initializer.dart:41-43` |
| — | 真正拉起原生 SDK 的是 **`AMapWidget` 构建**（读该静态变量塞进 `creationParams`） | `amap_widget.dart:179` |

**由此合规判据变了**：不是「未同意时不调 `updatePrivacyAgree`」，而是**「未同意时不得构建 `AMapWidget`」**。只做前者会在抓包实测中当场暴露 —— 因为它压根不产生网络行为，而 `AMapWidget` 一建就发起 SDK 初始化。守卫据此重写为两层：`applyConsent()` 写声明、`canRenderMap()` 判定能否构建控件，地图页消费的是后者。

另记一条排查陷阱：`hasContains`/`hasShow`/`hasAgree` 三者任一为 false 即白屏（`amap_initializer.dart:37`），**白屏表现与「Key 未配置」完全一致**，极易误判成 Key 问题。

**二、发现 PRD 的规格缺口，先补文档再写码（规则 4.2）**

两处：① §10.1 页面清单 16 页里**没有隐私协议门**；② §6.5 用户旅程第 1 步是「首次冷启动 → 弹位置权限」，这**违反上架手册 `:330`**（协议同意前不得申请任何权限）。已补 §10.1 一行（16→17 页）、§6.5 新增第 0 步、新增 §6.5.1 整节。

隐私同意状态层的四个非显然决定：

- **三态而非两态**：`unknown`（本地存储还没读完）≠ `notAgreed`（读完发现没同意）。合并的话，已同意的用户冷启动瞬间会闪一下协议门；
- **连版本号一起落盘**：只存布尔值则隐私政策改版后无法重新征得同意，不符《个人信息保护法》。存 `agreedVersion` 与当前版本比对；
- **拒绝不落盘**：拒绝不是需要记住的授权，下次冷启动应再问；落盘反而让用户失去改主意的入口；
- **门控放路由不放各页**：各页自查等于每加一页多一个可能漏判的地方，漏判后果是上架驳回。故 `appRouter` 全局常量改为 `routerProvider`，`redirect` 统一拦截。

协议门三条不可改约束：不可绕过（用 `PopScope(canPop: false)`，**不是隐藏返回按钮** —— 安卓物理返回键与手势返回不受 AppBar 影响）；不同意不退出应用（工信部禁止「不同意就不给用」，故有受限态）；协议文本走在线 URL（手册 P6 `:324`）。协议门与定位权限**必须分两步**，合并会被判为捆绑授权。

**三、发布版 SHA1：撤回我自己上一轮的建议**

我曾建议「先生成临时证书应付高德」。查完渠道清单后**这条是错的**，撤回：

- 网上「发布版 SHA1 要去商店后台拿」的说法**只适用 Google Play**（它有 Play App Signing 会重签名）。本项目渠道清单（手册 `:385-390`：华为/小米/OPPO/vivo/应用宝/App Store）**不含 Google Play**，均为上传自签名包、签名不被改写。**自己证书的 SHA1 就是最终发布版 SHA1**；
- 我错在把**证书**与**包名**当成同类事。包名依赖 P2 企业域名主体，现在定不了；**证书不依赖任何行政前置，现在就能定终身**。高德是 1 Key 绑 1 Package，正式包名落地后无论如何要重申 Key，但那时**证书不换、SHA1 照填**。生成临时证书等于让自己重做两遍。

调试版 SHA1 已取到（`keytool` 在 `D:\developer\soft\jdk\jdk-21.0.7\bin`）：`09:B9:EB:30:F1:F1:1A:6A:E5:F5:D2:EA:A0:98:A8:CD:A1:8D:3C:8A`。正式证书生成命令**刻意不带 `-storepass`**，让 keytool 交互提示，口令不落进 PowerShell 历史。`-validity 10000` 是硬要求：证书过期即无法发更新且不能换。

**四、M4-3 首页地图页落地**

数据流一条线：`Listing`（经纬度）→ `MapProjection.toPixel`（经纬度→像素）→ `clusterByGrid`（O(n) 网格聚合）→ `buildMarkers`（阈值判定 + 尺寸分档）→ `MarkerLayer`（单层 CustomPaint）。

几处非显然的取舍：

- **投影自己写，不等高德的 `toScreenLocation`**：后者是异步的（走 platform channel），而聚合要在每帧布局时同步出坐标，异步返回会让 Pin 晚一帧、拖动时明显拖影；且 Key 到手前根本没有 SDK 可问；
- **阈值判定单独一层，不并进 `clusterByGrid`**：聚合是纯算法要跑 POC-A 基准，阈值是会随 PRD 变的产品规格。混在一起会让每次调阈值都要重跑性能基准去确认没拖慢算法；
- **`buildMarkers` 必须同时收 `points` 与 `clusters`**：初版只收 `clusters`，未达阈值拆回单点时只能用簇中心 —— 那会让同格两条信息精确重叠，**第二个 Pin 永远点不到**。改为从 `points` 查回原坐标；
- **Marker 用一层 CustomPaint 而非每个一个 Widget**：5 万点档位下 Widget 方案要创建 5 万个 RenderObject，仅布局阶段就超预算。条目 [63] 已测出瓶颈在渲染侧不在算法，故渲染层从一开始就按画布设计，不留「先跑通再优化」的债。命中判定**倒序遍历**，与绘制顺序相反，否则点到的是被压在下面那个；
- **`MapProjection` 必须实现 `==`**：`shouldRepaint` 与 Riverpod 都靠它判断「变没变」。用默认引用相等则每帧全量重绘底图，这正是低端机掉帧的来源；
- **降级底图刻意画成示意图并加明示条**：画得像真地图，用户会按真实地理去理解 Pin 落点，而这里路网是程序生成的、与现实无关，**误导比空白更糟**。路网间距按**米**定义故会随缩放疏密变化，按像素定义则无论放大多少倍都一样密，一眼看出是贴图；
- **筛选态放 `features/discovery/` 而非 `features/map/`**：PRD §10.1 要求列表页与地图共享同一套筛选。挂在地图页下，列表页要么反向依赖地图模块、要么自存一份，后者会导致「地图 3 个 Pin、列表 12 条」这种用户一眼可见的不一致；
- **分类的色/图标/阈值挂同一个枚举**：PRD 里它们分散在 §1.4.3 / §6.4.2 / §6.15 三节，但对代码是同一实体的三个属性。分三张表则新增分类要改三处，漏一处的表现是「颜色对了但永远不聚合」这类难查的错。

新增 `test/marker_builder_test.dart` 11 条断言，覆盖三类边界：阈值「达到即聚」而非「超过才聚」（工作类恰好 3 条须聚）、拆回单点用原坐标（断言两点 x 不相等，取簇中心会都是 0.5）、尺寸三档边界（9/10/99/100）。**全量 18/18 通过，`flutter analyze` 零告警。**

**五、提交前发现：`dart format` 把 Token 校验机制悄悄废掉了**

`git add` 后核对 `git diff --cached --stat`，发现 `lib/design_tokens.dart` 有 30 行变更 —— 但本轮我没碰过这个文件。追下去是 `dart format lib test` 干的：产物里 `AppTextStyleToken(size: 24, weight: 'Bold', lineHeight: 1.3);` 单行超 80 列，被拆成了四行。

表层后果只是噪音：这文件每次 format 后都显示被改过，真正的 Token 变更会淹没在里面。**但我随后对深层后果的判断是错的，值得记下来** —— 我看到 `probe-token-vars.py:318` 的字阶正则按单行写死，直接推断「格式化后 `re.findall` 全部落空，空字典遍历零次，校验会静默通过」，并已按这个结论写进了本条目。写完才去实测：用旧正则跑当前产物，匹配 **0 条**；但该处断言用的是**整表相等**（`dart_styles == want_styles`），空字典 ≠ 6 项字典，**它会响亮失败而非静默通过**。也就是说这套校验本来就守住了，我预警的漏洞不存在。

真实的问题因此只剩一个：脚本产物与 `dart format` 排版主张不一致，导致 `git status` 长期有假变更。修法仍是两边一起改 —— `export-dart-tokens.js` 的 `typeScaleBlock()` 改为直接输出 format 后的多行形态（重跑输出 `unchanged lib\design_tokens.dart`），校验正则改为 `\s*` 容忍换行与缩进。**重跑 `probe-token-vars.py`：125 条通过 0 条失败**，反向用例「只改字重也应被检出」造假后确实失败。

**这次误判本身比修复更值得记**：我凭「findall 失配返回空列表」这条通用知识就下了结论，没往下看一行断言是怎么写的。通用知识用来生成怀疑是合适的，用来直接下结论就会造出根本不存在的缺陷 —— 而它写进文档后，会被后来的人当成事实。故原则 82 按实测结论重写。

**六、本轮新增原则**

- **原则 79 · 合规判据要落在「会产生外部行为的那一行」，不是名字看起来最像的那个 API**。`updatePrivacyAgree` 名字最像合规开关，实际只赋值 Dart 变量；真正发起 SDK 初始化的是 `AMapWidget` 构建。**守错了位置，静态代码看着合规，抓包实测当场暴露**。判断信号：写「未 X 不得 Y」这类守卫前，先确认 Y 到底在哪一行发生。
- **原则 80 · 撤回自己的建议要连同错在哪一步一起写**。上一轮我建议生成临时证书，错因是把「证书」与「包名」当成同类的行政依赖项。只说「这条撤回」下次还会犯，写清「错在归类」才不会。
- **原则 81 · 精简数据结构省下的字段，会在下游变成功能缺陷**。`Cluster` 为省内存只留成员 ID 和中心点，看起来合理；到了渲染层，未达阈值的簇拆回单点时没有原坐标可用，同格两点重叠、第二个点不可点击。**判断信号：某层丢弃信息前，先问下游有没有人要用原值。**
- **原则 82 · 「代码有缺陷」这个结论必须实测过才能写下来**。本轮我从「`re.findall` 失配返回空列表」推出「字阶校验会静默通过」，通用知识没错、推理链看着也通，但实测发现那处断言是整表相等，空字典会响亮失败 —— 缺陷不存在。**通用知识只够生成怀疑，不够下结论**；一个凭推理写进文档的假缺陷，后面的人会当事实读，比不写更糟。判断信号：要写「这里有问题」之前，先问「我跑过了吗」。
- **原则 83 · 校验若是「先提取再逐项断言」，比对方式要选整表相等而非逐键循环**。整表相等在提取落空时会因条数不符而失败，逐键循环则遍历零次直接通过 —— 同一份提取逻辑，前者能扛住正则失配，后者不能。本轮 `probe-token-vars.py` 恰好写的是整表相等，才没被 `dart format` 掐断。
- **原则 84 · 代码生成的产物，要按下游格式化工具的输出形态来生成**。生成脚本与 `dart format`（或 prettier、gofmt）对同一段代码有各自的排版主张，谁后跑谁赢，于是产物永远显示被改过、真变更淹没在噪音里。不是加 `// dart format off`，而是让生成脚本直接产出对方认可的形态 —— 前者是把冲突藏起来，后者是消掉冲突。

### [65] 2026-08-28 解决地图 POC 卡点：POC-B 埋点落地 + 绘制层超线性缺陷修复（19.6 倍）

**一、先查清 POC 到底卡在哪**

POC-A（算法级）上一轮已通过。查 POC-B 状态时发现一个被口径掩盖的事实：PRD:1371 写明「任何能装 APK 的安卓设备都能自行出数，无需在该设备上搭 Flutter 环境」，前提是**内置埋点**。而全代码库对 `addTimingsCallback` **零命中** —— 埋点根本没写。

也就是说，**POC-B 的卡点不是「没有真机」，是「真机今天到位也出不了数」**。这两件事的处理方式完全不同：前者只能等，后者现在就能做。此前一直把它记成设备依赖，等于把一件可做的事挂在一件不可做的事后面。

**二、埋点三件套（与上线后性能日志埋点同一套代码，PRD:1371/1383 要求不重复实现）**

- `lib/features/perf/frame_metrics.dart` —— 采集 `FrameTiming.totalSpan` 而非 `buildDuration`：本项目 Marker 走画布绘制，开销主要落在光栅化，只看 build 会漏掉真正贵的那段。
- `lib/features/discovery/stress_data.dart` —— 四档（关闭 / 500 / 1 万 / 5 万），固定种子可复现，**聚集分布**而非均匀（POC-A 已证聚集是最坏情况，×5.72 vs 均匀 ×3.21）。数据走 `allListingsProvider`，与真实数据同一条链路，不另建 demo 页 —— 绕开筛选与信息卡会让数字偏乐观。
- `lib/features/perf/perf_panel.dart` —— 应用内面板，`frameMetricsProvider` 为单例（`addTimingsCallback` 是全局的，随页面注册会重复注册）。

三处口径决定这些数字能不能用：

1. **压测入口不能藏在 `kDebugMode` 后**。POC-B 要在 release 包上测 —— debug 的 JIT 与断言会让帧耗时严重失真 —— 所以档位入口必须存在于 release 构建中。
2. **面板自身开销要可忽略**：每秒刷新一次，且用「上次刷新时间」节流而非 `Timer`。Timer 在页面不可见时仍会触发 setState，白白产生帧，污染的正是要测的数。
3. **导出走剪贴板不写文件**：Android 10+ 写外部存储要做分区存储适配，剪贴板零权限。

`test/frame_metrics_test.dart` 8 条，全过。之所以给一个「只是统计一下」的类配测试：**POC-B 的产出是要写进 SLA 对外承诺的数字，分位算错的表现是「P95 看起来达标」，没有任何异常现象提示它错了。**

三处被测试逼出来的口径细节：

- **分位数用最近秩法 `ceil(p/100 × N) - 1`**，不是 `round`。`round` 会让 P50 在偶数样本下取到偏高的那个（100 样本时 49.5 → 50，即第 51 个值），与「P50 是中位数」的直觉不符，读数时容易被当成实现出错。
- **只报分位数不够，必须同时看 Max 与直方图**。99 帧 8ms + 1 帧 500ms 的场景下，P95 与 P99 都是 8ms —— 那一帧只占 1%，分位数按定义就该排除它。若只看 P99，「达标」会盖住一次 500ms 的真实卡顿。
- **累计计数不受环形缓冲上限影响**。样本队列有 capacity（2000 帧 ≈ 33 秒），但 `_totalFrames`/`_totalJankFrames` 全程计数，否则「跑了 5 分钟，掉帧率 x%」只反映最后几秒。

**三、跑真机前先做一次开发机基准 —— 结果拦下了一次错误结论**

本想直接 `flutter run -d windows --release` 点档位取信号，但该路径抛 `PathAccessException`（沙箱写入限制），且此环境无法操作 GUI。改写 `tool/poc_b_paint_benchmark.dart`：`WidgetTester` 里直接画 Marker，可自动化、可复现。

首跑结果：

| 点数 | 重绘均值 | 每点 µs |
|---|---|---|
| 500 | 42.5ms | 85.08 |
| 2000 | 78.8ms | 39.39 |
| 10000 | 257.3ms | 25.73 |
| 50000 | **2483.9ms** | **49.68** |

关键不在绝对值而在**形状**：500→10000 每点耗时下降是正常的（固定开销被摊薄），但 10000→50000 从 25.73 **反弹**到 49.68，近乎翻倍 —— 这是超线性信号。绝对值同样刺眼：5 万点单帧 2.48 秒，离 16ms 预算差 150 倍。

根因两处，都在 `marker_layer.dart`：

1. **`supplyDemandOf` 回调是 O(n²) 陷阱**。它被实现为对原始列表 `firstWhere`，而 `_paintSinglePoint` 每个 Marker 调一次 —— 5 万点即 5 万 × 5 万次线性查找。改为在 `map_screen.build` 预建 `Map<String, SupplyDemand>` 传入。
2. **`TextPainter` 每个 Marker 每帧重建**。`layout()` 是完整的文字整形（字体查找、字形映射、度量），成本远高于 `paint()`，5 万点即每帧 5 万次整形；大量短命对象还带来 GC 压力，这正是超线性的来源。而实际组合极少：图标只有 5 分类 × 2 配色 × 2 尺寸。加顶层缓存。

修复后：

| 点数 | 重绘均值 | 每点 µs | 修复前每点 |
|---|---|---|---|
| 500 | 16.3ms | 32.51 | 85.08 |
| 2000 | 14.2ms | 7.08 | 39.39 |
| 10000 | 32.1ms | 3.21 | 25.73 |
| 50000 | **126.9ms** | **2.54** | 49.68 |

5 万点 **2483.9ms → 126.9ms，19.6 倍**；每点耗时转为单调下降，超线性消失。`flutter analyze` No issues，`flutter test` 26/26。

**开发机的绝对毫秒数不得写进 SLA** —— 低端安卓 CPU 单核通常只有开发机的三到五分之一。这份基准只回答「形状对不对」，不回答「达不达标」，后者仍要真机。

**四、为什么这两处必须在真机测数之前修**

两处开销都发生在 `paint()` 内，真机上的**表现形式**是掉帧 —— 与「渲染层画不动」一模一样。若带着它们上真机，最可能的结论是「CustomPaint 撑不住 5 万点，下调 Pin 上限」，而那恰是 PRD §6.10.1 兜底方案第 2 条的降级动作。结果会是：降级已生效、指标看着回到线内、根因原封不动。**一个被误读的性能数字，比没有数字更贵。**

**五、本轮新增原则**

- **原则 85 · 「缺设备」和「缺代码」要分开记，否则可做的事会被挂在不可做的事后面**。POC-B 一直记作「等安卓真机」，实际埋点从没写过，真机到位当天也出不了数。**判断信号：一项任务标记为「等外部条件」时，先问「条件到位的那一刻，我能立刻开跑吗」** —— 答不上来，说明真正的阻塞在自己这边。
- **原则 86 · 性能基准先看曲线形状，再看绝对值**。绝对值受机器影响、不可直接外推；而「每点耗时随规模上升」这个形状与机器无关，它唯一的解释就是代码里有超线性操作。本轮 5 万点 2.48 秒很扎眼，但真正定位根因的是 25.73→49.68 那次反弹。
- **原则 87 · 发生在 `paint()` 里的 CPU 开销，会伪装成渲染瓶颈**。`firstWhere` 与 `TextPainter.layout()` 都是纯 CPU 计算，但因为身处绘制路径，症状与「画不动」无法从表象区分。**把它们带进真机测试，会把优化引向降低渲染量这类错误方向** —— 而降级动作生效后指标确实会好看，根因却被永久掩盖。判断信号：每帧执行、且次数与元素数成正比的操作，都要先问「它能不能提到帧外」。
- **原则 88 · 帧耗时只报分位数会漏掉最严重的那一帧**。分位数按定义就排除尾部，99 帧 8ms + 1 帧 500ms 的 P99 仍是 8ms。**Max 与直方图不是补充信息，是判据的一部分。**

### [66] 2026-08-28 M4-3 列表页 list-screen 落地（PRD §6.4.3）

**一、先写范围再写代码**

按规则 4.2，动手前先把本期「做什么 / 不做什么 / 为何不做」写进 M4-3 待办（见下方 🟠 第 1-2 周条目）。不做的五项全部**卡在依赖上而非工作量上**：收藏依赖账号、信任小标依赖 §6.9、「联系 TA」依赖联系中转页、位置文案依赖高德 Key。**先做会返工，且卡片布局已定，届时只是填充字段。**

**二、域模型扩两个字段**

| 字段 | 类型 | 为什么这么定 |
|---|---|---|
| `createdAt` | `DateTime` 必填 | 「最新」排序无法为缺失时间的条目定义位置。允许为空就得在排序里造一个假时间，那等于**让数据缺陷伪装成排序结果** |
| ~~`priceValue`~~ | ~~`double?` 与 `priceLabel` 并存~~ | 不从文案解析 —— 文案含单位且格式不固定（「面议」「50 元/小时」「1500-2000 元/月」），解析必然歧义，而**歧义在排序里的表现是「顺序看着差不多但就是不对」** |

> ⚠️ **上表第二行已于同日作废，见条目 [67] 一**。「数值与文案分开」这个结论没错，但 PRD §13.2 早已把字段定为 `price` + `price_unit`，我未读契约就自创了 `priceValue`/`priceLabel` 这对新名字，属重复发明且与契约冲突。现已改为 `price`（double?）+ `priceUnit`（String?），`priceLabel` 降级为 getter。

**已知局限**（已写进代码注释）：单位不可比，50 元/小时与 1500 元/月放在一起排没有实义。PRD §6.4.3 只写了「价格升序/降序」未定义跨单位口径，本期先按裸数值排，待 §7.4.1 模板字段落地、单位随分类固定后再定归一化规则。

**三、排序模块 `listing_sort.dart`（五档）**

四个决定：

1. **排序态独立于 `DiscoveryFilter`**。排序是列表页专属，地图页没有「顺序」概念。塞进共享筛选态会让地图页也 watch 到它，改排序时地图跟着重建 —— 白白多一次全量聚合与重绘。
2. **无价格条目在升序和降序里都沉底**。当 0 会让「免费送猫粮」霸占升序首屏（它不是最便宜，是没有价格）；当极大值则降序霸屏。
3. **综合排序两个量纲先归一化再各占一半**。米数与分钟数直接相加，10km(10000) 与 7 天(10080) 数值接近纯属巧合，换成秒作时间单位就会完全压过距离。
4. **归一化上界取业务边界而非样本极值**（距离 20km = 10km 最大档留一倍余量，时间 7 天 = 与 §6.4.4 兜底口径一致，超界 clamp 不外推）。用样本极值会让同一条信息的排名随其他信息增删而跳动，用户看到的是「我什么都没做，顺序自己变了」。

`sortListings` 的 `now` 与 `distanceOf` 由调用方注入：前者让单测可固定输入，后者让排序不必知道基准点是用户定位还是默认中心。返回新列表不改入参 —— 就地排序会把共享同一份数据的地图页顺序也改掉。

**四、`FilterPanel` 从 `map/` 迁到 `discovery/`**

**「谁先用」不是归属依据。** 它先在地图页用上，但放在 `map/` 下会逼列表页 import 地图模块（反向依赖）；复制一份则会让两页筛选样式各自漂移。新增 `elevated` 参数区分两处宿主：地图页浮在底图上需白底 + 圆角 + 阴影，列表页嵌在已是白底的顶部，再叠一层会出现「卡中卡」。

**五、页面实现的几个取舍**

- **地图↔列表用 `context.go` 而非 `push`**：两个视图同层级，push 会让返回栈累积成「地图→列表→地图→列表」。
- **筛选面板与排序条固定顶部、不随列表滚动**：它们是当前结果集的控制器，滚到第 50 条想换排序还得先滚回顶部。
- **排序选中态同时改颜色与字重**：只改颜色在色觉障碍下不可辨，只改字重在小字号下差异过弱。
- **排序 chip 视觉高 28，用垂直 padding 补到 44 命中区**。
- **`ListView.builder` 而非 `ListView(children:)`**：压测档位下有 5 万条。
- **AppBar 不放搜索图标**：搜索框已在地图页顶部常驻且关键词属共享筛选态，两处输入框要同步光标与内容，收益不抵复杂度。
- **样例数据用固定时间基准 `DateTime(2026,8,28,12)`**：随时间漂移的数据让「最新排序对不对」每天得到不同结果，截图与实测也无法对照。发布时间**分钟级错开** —— 同分钟会产生并列，并列项相对顺序取决于原始顺序，看起来像排序不稳定。
- **距离 1km 内用米（取整到 10m）**：「368m」比「0.37km」更符合步行距离直觉；相对时间超 7 天回落到日期，「23 天前」不如日期直观。
- **空态只做静态版**：回显当前筛选摘要 + 仅在非全城时给「扩大到全城」按钮。§6.4.4 的 S2 动态蜂窝半径引擎属 Batch2，**做半套会让用户看到范围莫名其妙变了却没有任何解释**。

**六、验证**

新增 `test/listing_sort_test.dart` 9 条（距离 / 最新 / 价格升降与无价沉底 / 全部无价不抛异常 / 综合两量纲权衡 / 超界 clamp / 不改入参）。`dart format` → `flutter analyze` No issues → `flutter test` **35/35**（原 26 + 新 9）。

**七、本轮新增原则**

- **原则 89 · 共享状态里不要塞只有一个消费者需要的字段**。判断信号：**新增一个字段时，问「另一个订阅方会因为它重建吗」** —— 会，而它又用不上这个字段，就该拆出去。本轮排序态若并进 `DiscoveryFilter`，改排序会触发地图全量重聚合。
- **原则 90 · 可比较的数值和给人看的文案要分两个字段存，不要从文案里解析**。文案的职责是表达（含单位、含「面议」这类非数值），数值的职责是比较。**从文案反解出的歧义不会报错，只会让顺序悄悄错掉。**
- **原则 91 · 归一化的上下界取业务边界，不取样本极值**。样本极值会让个体排名随集合增删而漂移 —— 用户的感受是「我什么都没做，顺序自己变了」，这类问题极难复现和归因。
- **原则 92 · 缺失值在双向排序里应当同向沉底，而不是映射成某个极端值**。缺失不是「很小」也不是「很大」，是「不参与」。映射成 0 或极大值必然在某一个方向上霸占首屏。
- **原则 93 · 控件归属看依赖方向，不看谁先用它**。放在先用它的模块下，会逼后来者反向依赖；各复制一份则会各自漂移。**共用控件用参数吸收宿主差异**（本轮 `elevated`），而不是分叉成两个控件。
- **原则 94 · 写 import 前先看同目录现有文件怎么写**。本轮按目录名 `s2s` 写了包名，实际是 `zhaoyazhao`，全量编译失败。**目录名与包名没有任何强制关系** —— 这类「想当然」的成本是整个测试套件跑不起来。

### [67] 2026-08-28 用户叫停：架构偏差核实 + 价格字段对齐 §13.2 + M4-3 详情页 detail-screen 落地

**一、被叫停的那次偏差（本条目最该记的部分）**

用户在列表页提交后叫停：**「立刻停止，还没有进行架构设计怎么能进行开发呢？」** 核实结果分两半：

- **不成立的一半**：PRD 第十部分早在 8/24 就写了 §12 接口约定、§13 数据字典、§14 非功能需求，工程契约并非空白。
- **成立且更严重的一半**：那三章是**面向服务端与接口**的，而我写客户端代码时**一次都没有打开过 §13.2**。逐字段比对后，已产生三处实际偏差：

| # | 我写的 | PRD §13.2 定的 | 后果 |
|---|---|---|---|
| ① | `priceValue` + `priceLabel` | `price` + `price_unit` | 联调时每个价格字段都要查映射表 |
| ② | 五大类扁平枚举 | §2.4 三级分类树 | 详情页面包屑无处可取，只能另造 `categoryPath` |
| ③ | 无 | `status` / `expire_at` / `completeness_level` / `restricted` | 已下架、完整度、受限品类三类状态在客户端根本无法表达 |

最刺眼的是 ①：我在上一轮把「数值与文案要分两个字段」写成**原则 90**，当作一条新发现 —— 而 PRD 半个月前就定完了，我却发明了第三个字段名。**自以为在解决问题，实则在制造分歧。** 这比字段名错了本身更值得记：**当你觉得自己发现了一条通用原则时，先去看契约里是不是早就写了。**

用户随后拍板 **「先完成UI，交付UI优先」**，故不做完整客户端架构设计，把范围压缩为「**顺手对齐字段契约**」：① 立即修（详情页要复用同一模型，不改则偏差被继承放大）；② 通过 `ListingDetail.categoryPath` 部分缓解；③ 把 `expire_at` / `completeness_level` 补进 `ListingDetail`。**② 的分类树、`status`/`restricted` 仍未建模，是已知欠账，不是已解决。**

**二、`price` + `priceUnit` 对齐，`priceLabel` 降级为 getter**

字段名直接取数据库列名，不做「更好听」的重命名 —— 各叫各的会让联调时每个字段都要查映射表，而映射表是最容易过期的文档。

`priceLabel` 从 `final String?` 改为 `String? get`，由 `price` + `priceUnit` 现拼。**展示文案不落库**：落库会产生两份可能不一致的真相（改了 price 忘了改 label，页面照常渲染，只是数字对不上）。整数去掉小数尾巴（「50 元」而非「50.0 元」），非整数保留两位对齐 `decimal(12,2)`。改动波及 5 处（repository / sort / stress_data / test fixture / 模型本体），`list_screen.dart` 读的是 getter，对它完全透明。

**三、`ListingDetail` 与 `Listing` 分开，而不是给 `Listing` 加字段**

列表一次拉几百条，详情一次拉一条。合并会让列表接口被迫返回描述正文与模板字段，**直接违反 §12.1「地图取数接口单次响应 ≤10KB」**。`ListingDetail` 内嵌 `Listing` 复用共同部分，新增 `description` / `publisher` / `completeness` / `expireAt` / `contactChannel` / `contactMasked` / `categoryPath` / `templateFields` / `address` / `negotiable`。

三个刻意的不做：

- **完整联系方式不进详情模型**，只放脱敏值。§14.3 规定全系统仅 `/contact` 一个接口返回完整值，§7.7 要求完整号码不写入前端初始状态。**详情接口顺手带上，前端不显示也照样能被抓包批量采集。**
- **`Publisher` 不做资质等级、不合并成信誉分**。§2.5 是产品红线。
- **`templateFields` 用有序键值对 typedef 而非 Map**。§7.7 要求按序显示，Dart 的 Map 顺序虽稳定但语义上不承诺。

两个派生方法的判断：`daysUntilExpire` **向上取整**（剩 25 小时显示「2 天后下架」—— 宁可让用户以为还早，也不要让人以为已经没了而放弃联系），**已过期返回 0 而非负数**（负数会渲染成「-1 天后下架」）。

**四、详情样例数据由列表条目派生，随机种子取自 `id.hashCode`**

两处各自造数据会出现「列表显示 50 元/小时，点进去 120 元/月」；用全局 `Random` 则同一条信息来回切换时模板字段会自己变。种子取自 id 后，同一条信息永远派生出同一份详情。

`Provider.family` + `where().firstOrNull`，**返回 null 表示信息不存在**（页面显示「已下架或不存在」）。用户点开一条刚被删的信息属正常场景，不是错误，不该抛异常。

**样例必须覆盖全部状态分支**：完整度按 `seed % 3` 均分三档、实名按 `seed % 3 != 0` 约占 2/3、可议价约 1/3。**只造 🟢 和已实名，会让其余状态的排版直到线上才第一次被看见。** 描述文案刻意写到接近三行，用于验证段落排版。

**五、页面实现的几个取舍**

- **媒体区用分类色渐变而非灰块占位**：灰块会被误认为「图没加载出来」，渐变一眼看出是刻意占位；且顺带验证分类色在大面积铺色下的观感 —— Pin 上只有 40px，饱和度问题不暴露。
- **`ListView` 底部 padding 96**：为 `bottomNavigationBar` 的固定主按钮让位，否则最后一段内容被永久遮住，且用户无法察觉自己没看全。
- **模板字段标签固定宽 84**：不固定的话「工具」与「可服务面积」会让 value 参差不齐，扫读成本明显上升。
- **正文行高 1.6 / 标题 1.35**：详情描述是需要连续阅读的段落，标题是一眼扫过的短句。
- **未实名不显示灰色「未实名」标**：那是在替发布者做负面背书。§9.8 的设计是「有认证才展示」，无认证靠完整度档位说话。
- **无价格显示「面议」而非留空**：§5.8 允许价格为空，但空白会让用户以为页面没加载完。
- **完整度圆点用图形色、同行文字用 `*-text` 深色变体**：`success`/`warning`/`error` 配 `successText`/`warningText`/`errorText`；`_SupplyDemandTag` 承载白字必须用 `deepColor`（条目 [43] 实测：原色配白字全部不过 WCAG AA）。
- **地图信息卡整卡可点进详情，而非只给一个小按钮**（§7.5 旅程第 1 步）：卡片本身就是「这条信息」的代表，用户的直觉是点它；同时卡内补一行「查看详情 ›」明示可点。

**需求态三处差异（§7.4.3）**：标题自动加前缀（房屋=求租 / 车辆=求搭 / 生活=求购 / 工作·服务=求助）、主按钮改「响应需求」（**对需求发布者说「联系 TA」语义是反的**）、增「我能帮忙」入口。

**六、本期不做的四项及依赖**

① 媒体轮播（`post_media` 未建模 + 需 CDN）；② 详情小地图（依赖高德 Key）；③ 收藏/分享（依赖账号 + 已备案域名）；④ 举报（属联系中转页）。

**「联系 TA」按钮做了但只做到跳转中转页占位屏** —— 不放会让页面缺掉最主要的行动点，**验收时看不出真实的视觉重量**，底部留白的比例判断会整个失真。

**七、验证**

新增 `test/listing_detail_test.dart` 13 条，只覆盖三处「算出来的值」—— 这类错误不会崩溃、页面照常渲染，但内容是错的：价格文案 4 条（整数无小数尾巴 / 无单位不能拼出「50 元/null」/ 无价格返回 null / 非整数保留两位）、剩余有效期 3 条（整天 / 25 小时向上取整为 2 / 已过期返回 0）、需求态前缀 6 条（资源态无前缀 + 四类映射 + 遍历全分类断言都有前缀）。

`dart format` → `flutter analyze` **No issues** → `flutter test` **46/46**（原 35 + 新 13，另 sort 测试 fixture 同步改造）。

**八、本轮新增原则**

- **原则 95 · 动手写模型前，先打开已有的数据字典逐字段比对**。契约在别人手里写好了，不看就写等于开第二套标准。**判断信号：当你为一个字段起名时感到「这个名字挺好」，就是该去查契约的时刻** —— 好名字大概率别人已经起过一个不同的。
- **原则 96 · 当你觉得自己总结出一条通用原则时，先确认它不是契约里的已知条款**。本轮原则 90「数值与文案分开」正是 §13.2 的既定设计，我把它当新发现记了下来，还顺手造了个与契约冲突的字段名。**重复发明的危害不在浪费时间，在于它带着自信绕开了已有共识。**
- **原则 97 · 列表模型与详情模型分开，依据是接口响应体量而非字段重合度**。重合度高会诱使合并，但列表一次几百条、详情一次一条，合并等于让列表为详情的字段付流量。
- **原则 98 · 敏感字段的最小暴露看接口而不看页面**。「前端不显示」不是保护 —— 抓包照样能拿。**判断信号：任何一个字段，问「它出现在这个响应里是必需的吗」，答否就不该下发。**
- **原则 99 · 派生型样例数据的随机种子必须取自实体 ID**。全局 `Random` 会让同一条数据每次进入都不一样，两处独立生成则会互相矛盾；这两类问题在真实数据接入后自动消失，因而极容易被当成「先这样吧」拖到很晚才发现已经影响了判断。
- **原则 100 · 样例数据要按分支均匀铺开，不要只造「正常」那一档**。只造完整度 🟢 + 已实名，等于其余状态的排版从未被人眼看过。**判断信号：模型里每多一个枚举，样例里就该出现它的每一个取值。**
- **原则 101 · 缺省文案优先于空白**。§5.8 允许价格为空，但空白与「加载失败」在用户眼里没有区别。**留白是设计，空白是故障** —— 二者视觉上一模一样。

### [68] 2026-08-29 打通 release APK 构建：中文用户名致 Kotlin daemon 路径转义丢失

**一、为什么在「UI 优先」期间去碰构建**

用户问「地图 POC 呢」。查状态时看到条目 [65] 记的是「剩余依赖只有设备本身」—— 而这句话隐含一个从未有人验证过的前提：**PRD:1371 承诺「任何能装 APK 的安卓设备都能自行出数」，前提是这个项目能构建出 release APK。**

这正是原则 85 的同一类问题再演一次：*一项任务标记为「等外部条件」时，先问「条件到位的那一刻，我能立刻开跑吗」*。上一轮我用这条原则发现了「埋点根本没写」，却没把它用到底 —— 埋点写完就宣布「只剩设备」，**没有验证过出包本身**。

实测结果：**构建 100% 失败**。真机今天到位也装不上包。

**二、根因：转义丢在 Gradle → Kotlin daemon 的进程边界**

```
error: source file or directory not found:
  C:\Users\u5218u6770\...\SharedPreferencesPlugin.kt
```

`u5218u6770` 是「刘杰」两字的 Unicode 码点。本机用户目录含中文，`PUB_CACHE` 未设置故 pub cache 默认落在其下。Gradle 把待编译源文件列表传给 Kotlin daemon 时，中文被转义成 `\u5218\u6770` 且**反斜杠丢失**，daemon 收到的是一个不存在的路径。

**这个报错的欺骗性极强**：它只说「文件找不到」，一个字不提编码。不知情时第一反应必然是依赖损坏 → 清 pub cache 重装 → 白折腾一轮且照样失败。唯一的线索是路径里那串 `u5218u6770`，而它长得很像一段正常的哈希目录名。

**三、排查过程中我自己制造的一次假阴性（这是本条目最该记的）**

第一次我猜是 daemon 编码问题，加了 `kotlin.daemon.jvmargs=-Dfile.encoding=UTF-8`，跑完仍失败 —— 结论「无效」，正确。

但接着验证 `in-process` 策略时，我写成 `-Pkotlin.compiler.execution.strategy=in-process`，**PowerShell 把 `-P...` 当成了自己的参数缩写**，Gradle 收到的是个不存在的任务名，6 秒就失败。当时的输出里既没有「文件找不到」也没有 BUILD SUCCESSFUL，**如果我当场判读为「这个方案也不行」，就会去动插件版本或 Kotlin 版本，那是完全错误的方向，且每轮成本远高于 40 秒**。

是「6 秒」这个异常时间救了我 —— 之前每次失败都要 1 分 39 秒。一个本该跑编译的任务不可能 6 秒结束。加引号后同一个任务 **19 秒 BUILD SUCCESSFUL**，根因当场定死。

**四、两条修法与取舍**

| 方案 | 效果 | 为何没选 / 选它 |
|---|---|---|
| 迁 `PUB_CACHE` 到纯英文路径 | 根治，且对所有项目生效 | **没选**：属机器级改动，影响本机全部 Flutter 项目、需重下全部依赖。**这是开发者本人的环境决策，不该由项目代劳，更不该由我悄悄替用户做** |
| `kotlin.compiler.execution.strategy=in-process` | 已验证有效 | **选它**：项目内一行配置，随仓库走，新机器 clone 下来即可用 |

`in-process` 的代价是 Kotlin 增量编译变慢、Gradle daemon 内存压力增大。**本项目 Kotlin 仅有插件胶水与 MainActivity，主体是 Dart，故这个代价接近于零** —— 同样一行配置放在 Kotlin 为主的项目里就是错的决定，所以理由必须写进配置注释而不只是文档。

**五、验证**

- `flutter build apk --release` → **`app-release.apk` 102.1MB**（227.7s）
- `flutter test` → **46/46**，无回归

**102MB 这个数字不是问题，但会误导结论**：默认包含 armeabi-v7a / arm64-v8a / x86_64 三套引擎，真机只用其中一套。安装包大小不影响帧率，故不阻塞 POC-B；但**若哪天用它评估「首次启动耗时」或安装转化率，必须先 `--split-per-abi`**，否则测的是一个真实用户永远不会下载的包。已挂进 M4-1b 待办。

**六、本轮新增原则**

- **原则 102 · 「万事俱备只欠 X」这句话本身就该被验证一次**。条目 [65] 已用原则 85 挖出「埋点没写」，却在补完埋点后直接宣布「只剩设备」，没有验证出包 —— **同一条原则只用了一半**。判断信号：当你写下「现在只差某个外部条件」时，立刻去把那个条件之外的全链路**实跑一遍**，而不是在脑子里推演一遍。
- **原则 103 · 排查时先确认自己的实验有没有跑起来，再判读结论**。我把 `-P` 参数写漏引号，PowerShell 截走了它，Gradle 报的是「任务不存在」，与「方案无效」在结论上不可区分。**若当场采信，会朝完全错误的方向走下去。** 判断信号：**耗时反常就是实验没跑起来的信号** —— 编译任务 6 秒结束，比任何报错信息都可靠。
- **原则 104 · 项目级配置修复优先于机器级环境修复，即便后者更彻底**。改 `PUB_CACHE` 一劳永逸，但它改的是用户的机器；`in-process` 只改仓库，新成员 clone 即生效。**凡能写进仓库的修复就写进仓库** —— 环境类修复无法随代码传递，会在每台新机器上重演一次同样的排查。
- **原则 105 · 带取舍的配置项，理由必须写在配置文件里而不只写在文档里**。`in-process` 在本项目几乎零代价，在 Kotlin 为主的项目里则是错的选择。**改配置的人看的是配置文件，不是几千行外的决策记录。**
- **原则 106 · 只在开发路径上跑测试，会漏掉整条发布路径的问题**。`flutter test` 与 `flutter analyze` 长期全绿，掩盖了「release 构建 100% 失败」。**判断信号：日常验证命令与最终交付物的产出命令不是同一条时，后者必须被单独跑过至少一次。**

### [69] 2026-08-29 M4-3 联系中转页 contact-screen 落地（PRD §7.4.2 / §7.7 / §7.8）

**一、动手前先把契约读完，不凭记忆写**

这是对条目 [67]「未读 §13.2 就自创价格字段」被叫停的直接矫正。本轮先完整读了 §7.4.2（含 ASCII 线框）/ §7.4.3 / §7.5 旅程 / §7.7 反爬 / §7.8 边界表 / §12.3 接口与错误码 / §13.2 表结构 / §14.3 出口收敛 / §14.6 隐私红线，**再写第一行代码**。收获是两处若靠推演一定会写错的地方：

- §14.6 把「联系方式出口审计」列为 🔴 隐私红线，§14.3 明确「全系统仅 `/contact` 一个接口返回完整联系方式」。若把完整号码顺手塞进详情接口，功能表现完全一致，但直接踩红线；
- §12.3 的 `42902`（限频）与 `42903`（熔断）**是两个错误码**，后者会「进运营风控待审列表」。混成一句「操作过于频繁」，被误判的正常用户会以为等到明天就好，而他实际需要走申诉。

**二、这一页存在的理由不是「多一步确认」**

§7.4.2 要求「号码中间 4 位脱敏，拨出/复制时才显示完整」。若详情页直拨，完整号码必须提前下发到客户端，§7.7「完整号码不写入前端初始状态」当场失效。**这一页的价值在于让「拉取完整值」成为一个可限频、可埋点、可熔断的独立动作** —— 少了它，三维限频只能限住列表请求，限不住已经到手的号码。

据此定下三条实现约束：

| 约束 | 若不这样做 |
|---|---|
| `fetchFullContact` 写成**模拟服务端接口**（含 600ms 往返、会抛异常），而非本地字符串加工 | 接后端时要改的不只一个函数，页面的加载态/错误态全都没有位置放 |
| `FullContact.value` **只放 State，不进 Provider**（§9.6 数据最小化） | Provider 跨页存活 = 「点击时才拉取」退化为「拉一次就永久持有」 |
| 失败建成**具名枚举 + 异常**，不返回 null | null 只能表达「没拿到」，区分不出 42902 与 42903，而两者文案完全不同 |

600ms 延迟不是装饰：没有加载态，按钮点下去到号码出现之间没有任何反馈，用户会重复点击 —— **而重复点击在真实环境下直接撞熔断**。

**三、为支撑 §7.8 一条边界，改了域模型**

§7.8 有「对方联系方式未填 → 主按钮禁用」这条边界，而 `ListingDetail.contactMasked` 原为必填 `String`。改为可空并加 `hasContact` getter。**没用空串代替**：空串下「未填」与「填了空串」在类型上无从区分，判空逻辑会散落到每个使用处，漏判的表现是按钮可点但拨出一个空号。

同时给样例数据补出这一档（`seed % 7 == 0`）—— 不出现，该分支的排版与禁用态就只能靠想象验收。**用 % 7 而非 % 3 是因为它是异常态，不该在样例里占三分之一。**

**四、降级必须自动，且不是每个「能跳」都值得跳**

- **外呼失败自动降级为复制**（§7.8）：用 `canLaunchUrl` 先探测，不用 try-catch —— `launchUrl` 抛异常时已在跳转过程中，无法再安静降级。**降级不弹错误**：用户的目标是联系上对方，报错等于把「换个办法」的责任推回给他，而他手上已经没有别的办法；
- **微信不做「跳转搜索」**：微信不提供带参搜索 scheme，能跳的只是首页，用户还得自己找搜索框粘贴 —— 多一次跳转、少一次可见的复制反馈，反而更难用。

**五、几处刻意的视觉决定**

- **举报入口不用 Danger 按钮**（§2.6 规定 Danger 用于删除/退出登录）：举报是低频辅助动作，醒目红按钮会与主行动按钮抢注意力，也暗示「这条信息可疑」；
- **温馨提示用 warning 而非 error 配色**：这是善意提醒，error 色会让用户以为这条信息本身有问题；
- **主按钮未填时禁用而非隐藏**：消失会让用户以为页面没加载完，禁用态配合下方举报入口才能表达「这条信息缺东西，你可以让他补」；
- **举报五原因照抄 §7.7 原文不增删改写**：原因会进 §9.10.3 风险分权重表，改一个词就对不上运营配置。

**六、验证**

`dart format` 零改动 → `flutter analyze` 零告警 → `flutter test` **56/56**（新增 `test/contact_test.dart` 10 项）→ **release APK 复验通过**（324.9s，102.7MB）。**这次主动跑了 release**：本轮新增的 `url_launcher` 含原生代码，条目 [68] 的原则 106 刚说过「日常验证命令与交付物产出命令不是同一条时，后者必须单独跑过」，加原生依赖正是它的适用场景。

**七、新增原则**

- **原则 107 · 把「服务端才能做的防护」在客户端建成可展示的状态，而不是留空**。三维限频与熔断本期都无法真做，但把 42902 / 42903 建成具名错误态后，文案、排版、恢复路径全都能被验收；留空则等后端到位时才发现「限频提示没地方放」。**判断信号：某条规则本期做不了，先问「它失败时用户会看到什么」，那部分通常现在就能做完。**
- **原则 108 · 「不做某功能」和「不做某功能的入口」是两件事，后者会让已有文案失效**。举报提交依赖 §9.10.3 无法本期实现，但面板必须做 —— §7.8 的提示文案「尝试举报让其补充」若指向一个不存在的入口，等于在页面上写了一句谎话。**判断信号：砍功能前先搜一遍现有文案有没有指向它。**
- **原则 109 · 样例数据必须覆盖异常档，且出现频率要与它的真实性质相称**。`seed % 7 == 0` 造出「未填联系方式」这一档，让禁用态能被眼睛验收；用 % 3 会让异常态在样例里占三分之一，反过来扭曲对排版常态的判断。**判断信号：写完边界分支后，问一句「样例数据里点得到它吗」。**

---

### [70] 2026-08-29 从代码开发折返回 UI 设计：Figma 稿 11 页留白横扫 + 五类缺陷修复（M3 收口）

**一、起因：用户圈图指出的是「排版丑」，实测下去是「内容没画全」**

用户回传 5 张 Figma 渲染图，圈出五类问题（下称 P1–P5）。我按图逐页量下去，量出的第一个数就把问题重新定义了：**trust 页可见内容只到 293px，底下空着 551px**。这不是「留白比例没调好」，是 PRD §4.4 稿图里明明白白列着的东西没画。

五类问题的实际归属：

| 编号 | 用户看到的 | 实测根因 |
|---|---|---|
| P1 | 多页底部大片空白 | 一半是没贴底（排版），一半是内容缺失（契约） |
| P2 | 主按钮悬在半空 | 无 `pushToBottom`，Auto Layout 下按钮紧跟前一元素 |
| P3 | ✅🟡❌🟢 emoji | 11 处，全部改矢量 |
| P4 | 详情页没有正文 | §7.4.1 稿图有 `_description`，实现直接跳过 |
| P5 | 发布页字段两种形态 | 两个字段用了 `listRow`（宽 390），另两个用 `field`（宽 358） |

**P3 单独说一句**：emoji 的害处不是「不好看」。它由系统字体渲染，三端字形与配色都不一致；且色值不受 `paintOf()` 控制，意味着 §1.4.2 那套实算过的对比度对它**完全无效**。改矢量后每档都做成「色点 + 文字档名」双通道 —— 颜色单独承载状态时，色弱用户读不出来。

**二、本轮最贵的一课：只查用户圈出的页面，等于让缺陷自己挑选被发现的概率**

修完 5 页、探针 208 全绿之后，我做了一次**11 页全量横扫**（量每页最底部可见叶子节点的底边）。结果是四页比 trust 更空：

| 页面 | 内容底边 | 空白 |
|---|---|---|
| notification | 267 | **577** |
| my-favorite | 303 | **541** |
| my-publish | 365 | **479** |
| settings | 384 | **460** |
| trust（已修） | 549 | 295 |
| profile / detail / publish / contact | 807–833 | 12–37 |

这四页用户一张图都没圈 —— **因为他圈的是他打开过的页面**。若本轮就此收口，M3 验收时它们必然被逐个提出来，而那时的返工代价是现在的数倍。已如实向用户报告并取得同批修复授权。

**三、四页的病根统一是「PRD 逐项列举的条目，实现只画了个头」**

按 PRD 逐条比对后补齐（不是往里塞装饰）：

- **notification（§8.3.3）**：3 条 `listRow` → 5 条新建的 `notifyRow`。§8.3.3 明列单条为「图标 · 标题 · 摘要 · 时间 · 未读红点」**五段**，`listRow` 只给得出两段，**摘要与未读态整个缺失，而未读态是本页唯一的交互状态**。条数补到 5 是为覆盖系统类全部触发种类 —— 原先 3 条里没有「违规下架」，即负向通知长什么样在整份稿子上无处可看。
- **my-favorite（§8.3.2）**：2 张卡 → 5 张，五大分类各一张。收藏页是**全稿唯一「五种分类色同屏」的真实语境**，两张卡只验得到两种色，其余三种只在 T6 规格板上以脱离语境的纯色块出现过。页签补「全部」（§8.3.2 明列三个）。
- **my-publish（§8.3.1 / §8.6）**：补「全部」页签后四张卡才能同屏覆盖在架/已下架/草稿三态 —— **原先页签停在「在架」，§8.6 那张发布状态机图在稿面上只落地了一种状态，等于没落地**。操作组从「整页底部一组游离按钮」下沉到每张卡下：§8.3.1 写的是「右：操作组」，是每行一组；整页一组会被读成「作用于全部条目」。
- **settings（§3.4.3）**：5 条平列 → 四组 11 条。缺掉的「注销账号 / 默认范围 / 联系方式展示策略」恰好都是**需要单独确认口径的敏感项**。补分组标题的理由是 §3.4.3 本身就分四组写，十余项平列是一堵没有落点的列表墙。

**四、一次「照契约删除」反而删出了新缺口**

§3.4.3 尾注写「退出登录个人中心放，设置页不放重复」，我据此从设置页删掉了那个 Danger 按钮。**但个人中心当前 5 行列表里也没有它** —— 于是整份稿子丢了 §3.4.2 稿图明列的元素。经用户确认，在个人中心按稿图补回「帮助与反馈」行 + 独立的 Danger 退出登录按钮。

由此定了断言写法：**「不该在 A」这类判据必须连「该在 B」一起验**。只验设置页没有，就漏掉了「两边都没有」这种更糟的情况 —— 而它本轮真实发生了。

**五、探针从 200 → 220，其中一条专治探针自己的盲区**

trust 页第一版修法是 `pushToBottom(body, [annotation(...)])`，离线断言全绿，**实机仍是原来那 551px 空白**。查明机制：`detachAnnotations()` 会把 annotation 卡从画框里**移出去**、改挂到所在 SECTION 上（实机查得两张卡 parent 均为「02 · 核心流程」SECTION）。于是 spacer 后面什么都不剩，页面回到改前。**离线看不出的原因是探针在 `layout()` 之前取节点树，那时 annotation 还在容器内。**

修法有两层：① trust 页改为按 §4.4 补足内容让页面自然填满，不再用 spacer；② 新增一条**全量扫描**断言，扫所有 `build*` 构造器，凡「spacer 之后只剩 annotation」的一律报红（现扫 11 个构造器）。

新增 20 条断言的取舍原则：**只把「PRD 逐项列举过、且删掉后页面照样能渲染」的那些点硬编成判据**。譬如「五种触发种类是否齐备」「操作组是否随状态变化」「五张卡是否覆盖五种分类色」—— 这类语义完整性源码正则数不出来，必须真跑构造器去数。

**六、三次「取值路径错，却报成内容问题」**

新断言首跑 206/208，两条红的都不是它要拦的问题：

- **三行认证色点全报「无色点」**：我写了 `if (!want || !want.length)`，而 `paintOf()` 返回的是**单个 Paint 对象、不是数组**，`want.length` 恒为 undefined，故永远命中「无色点」分支。修法是 `pj(got) !== pj([M.paintOf(role)])`。**这是同型错误的第三例**（前两例：`CARD_SIG`、`iconFills` 外框 fills）。
- **T4 红线断言报红**，命中的是**我自己写在卡里的那句「不含发布记录数与举报率（T4 红线）」**。断言没错，是文案错 —— **卡里不该把不许出现的指标名字印在页面上**。改为直抄 §4.2 定性句「只看信息真实性与完整性，不做人的信誉评级」。

**七、PRD 反向回写（代码与契约双向对齐）**

§4.4 稿图原为两层概括卡 + 「我发布的 5 条 / 🟢3 🟡1 🔴1」。本轮改写为二层结构 + 三类逐条状态行 + 信任指标卡，并补两段口径说明：① 状态与完整度一律「矢量色点 + 文字档名」双通道，不用 emoji（理由同上）；② **条数口径：本页「我在架的 N 条」与 §3.4.2 个人中心的「我的发布 N 在架」必须同一个数**（取 3）—— 同一份数据在两页互相矛盾，评审第一眼会去追哪个对，而它其实没有对错。这条已写成断言。

**八、验证**

离线布局探针 **220/220**、Token 探针 **125/125**（含 Dart 产物一致性三条反向验证）。查明 `FLOW_LINKS` 无需改动：四页均只作跳转**目标**，不作触发源（本轮特意查了 —— 上一轮 `row/*→field/*` 改名正是因漏改 `FLOW_LINKS` 而亮红）。

⚠️ **trust 页本轮只有几何 + 文本数据核验，没有肉眼图核验**：`download_figma_images` 返回 403 Token expired，`figma_capture_screenshot` 桥两次 30s 超时。已报告用户，用户认领换 token。**四页与个人中心的改动尚未实机跑过。**

**九、新增原则**

- **原则 110 · 用户圈出的缺陷页是抽样，不是全集；同类缺陷必须做一次全量横扫**。本轮用户圈 5 页，横扫 11 页后发现另有四页空白**更大**，只因他没打开过。**判断信号：修的是「某页某处」，但成因（少画条目 / 没贴底 / 用错构造器）适用于全部同类页面时，横扫的成本远低于验收时被逐页退回。**
- **原则 111 · 页面大片空白优先当「契约条目没画全」查，而不是「排版参数没调好」**。留白是症状，缺内容与没贴底都会表现成它，而两者的修法完全相反（补条目 vs 加 spacer）。**判断信号：先量可见内容底边，再拿 PRD 对应节逐条点数 —— 数得出缺项就不是排版问题。**
- **原则 112 · 「照契约删除」也要走一遍反向检查：删掉的东西在契约指定的新位置真的存在吗**。按 §3.4.3 尾注从设置页删掉退出登录，而个人中心本来也没有，结果整份稿子丢了这个元素。**判断信号：契约说「A 不放、B 放」时，删 A 之前先确认 B 有；断言也要两边一起验。**
- **原则 113 · 离线探针在真实副作用（移动/删除节点）之前取树，凡依赖「节点最终位置」的判据都可能全绿而实机为假**。`detachAnnotations()` 移走 annotation 使 `pushToBottom` 失效，离线绿、实机原样。**判断信号：判据涉及「某节点排在最后 / 在某容器内」时，先问一句「后续流程会不会把它挪走」。**
- **原则 114 · 断言首跑报红时，先怀疑取值路径，再怀疑被测内容**。本轮两条红的都不是内容问题：一条是把单个对象当数组取 `.length`（同型第三次），一条是断言正确而我的文案确实违规。**判断信号：多个用例同时报「取不到」，几乎一定是取值路径错 —— 真的内容缺陷少有这么齐整。**

---

#### [70-c] 2026-08-30 · 条目 [70] 第三段：list 页 Tab 贴底 + 四页补分页收尾条

**一、起因：原则 110 在同一条目内被命中第二次**

上一段我按用户圈的页修完四页留白、又横扫 11 页补齐内容，自认为「同类缺陷已全量横扫」。实机复验时改从**另一个维度**扫（不是量页面底边，而是量底部 Tab 的底边），当即发现 `list` 页的 `shell/bottom-tab/鸭圈` 悬在 693px、离底 151px —— 而 `profile` 页的同一个 Tab 贴在 844。

**这是同型缺陷的第二次漏检，成因值得记**：上一段修 profile 页时我用了 `pushToBottom`，却没顺手查「`bottomTab()` 一共被调用了几次」。实测 `code.js` 里共 **13 处**调用（`:1906` 定义；3730/3754/3766/3785/3814/3850/3868/3912/3930/3948/3985/4560），只有 profile 那一处（4560）走了 `pushToBottom`。横扫维度选错了 —— 我扫的是「页面」，而缺陷的单位是「构造器调用点」。

**二、实机横扫：24 处 Tab 落点，缺陷全集恰为 1 处**

从 Tab 反向往上找画框（不能从 page 往下找 —— 画框在 SECTION 下，`page.children` 只有 3 个 SECTION，首版这么写返回 `count: 0`）：

| 落点 | gap | 贴底机制 |
|---|---|---|
| `list-screen` | **151** | 无 —— 裸 `appendChild` |
| `home-screen` ×22 | 0 | `_map-canvas` 固定高填满 |
| `profile-screen` | 0 | `_spacer` grow=1 |

所以缺陷全集确实只有 1 处，其余 23 处两种正当写法各自成立。这个数据后来直接决定了探针判据的写法（见第四节）。

**三、四页补「没有更多了」收尾条（PRD §6.7 :1291）**

用户裁定同批补。PRD 原文：「列表滑到底部 | 加载下一页，每次 20 条；"没有更多了"→ 显示鸭子空状态」。§7 接口表里 `/notifications`、`/favorites`、`/posts/mine` 三条 GET 均标「分页」，与 `list` 页同源，故四页统一补。

新增构造器 `listEndRow(width)`，鸭子取 40px（落 `duckSymbol` 的 mini 档）+ 「没有更多了」文案。

**`width` 参数不是过度设计，是几何必需**：`notification` 的 `_body` 无内边距（满宽 390），另三页 `_body` 带 `pad: SPACING.lg`，可用宽只有 358。满宽条塞进后者会把容器顶到 422、画框随之变形。实测四页收尾条宽分别为 390/358/358/358，各自齐边无溢出。

**四、探针 +4 条（220 → 227，含 2 条 batch 内断言 ×5 批次）**

其中「底部 Tab 贴底」这条特意做成**全批次横扫**而非点名式 —— 点名式下一次新增页面照样会漏，这正是本段被命中两次的成因。

判据设计踩了一次坑，值得记：首版只认「Tab 之前存在 grow>0 的兄弟」，结果 **home 系列 22 个画框全报失败**。查证后确认是判据错而非内容错 —— home 系列靠 `_map-canvas` 撑满，而它是 `stack()` 建的非 Auto Layout 容器（`layoutMode: NONE`），**根本不可能有 layoutGrow**。改为「排末位 + (前有 grow 兄弟 **或** 前面兄弟固定高合计已填满画框)」后全绿。

另用「撤掉 `pushToBottom` 再跑」做反向验证：精准报出 `list-screen … 内容也只到 822 < 844`，且**只报这一处**，证明判据有区分力而非恒绿。

**五、一次误判：`✓` 不是漏网的 emoji**

实机扫查时我手写了码点区间 `U+2600-27BF`，扫出 `publish-success` 页有一个 `✓`(U+2713)，当即判为「上一轮 emoji 换矢量的漏网」并改成了矢量勾。

**改完才去读探针的对应注释，发现那里早已写明**：该区间「也含 ✓(U+2713)、→、‹›，它们是标点/几何符号，默认黑白呈现、受 paintOf 控制，**正是我们用来替代 emoji 的东西**」。探针用的是 Unicode 属性 `\p{Emoji_Presentation}`（恰好捕获 ✨🟢✅📞、放过 ✓→‹›），而我手写区间把这条线画错了。已撤回改动，改用与探针同一口径复扫，实机 **0 命中**。

**这次误判的教训比它本身重要**：探针注释里已经把「为什么不能手写码点区间」连同两个反例写清楚了，我却在实机扫查时另起一套判据。**同一件事在项目里已有既定判据时，临时另写一套等于把已经踩过的坑再踩一遍** —— 故新增原则 115。

**六、出图三条路仍全断，根因已锁定**

按用户要求给出排查方法，结论是**已排除 token 与画框大小**：
- `figma_diagnose` 报 `✅ Figma access token detected (source: env)`，且截图桥根本不走 REST；
- 导出 **10×10 空矩形**同样超 25s；
- 同一次 `figma_execute` 里 `createRectangle + remove` 只花 **4ms**，`figma.root` 读取 **0ms**。

即：**`exportAsync` 这一个 API 在该 Figma Desktop 实例里挂死（Promise 永不 resolve），其余插件 API 全部正常**。`figma_capture_screenshot` 内部即调它，故两条路是同一个病。用户重连 bridge 后仍未恢复。

排查步骤（代价从低到高）：① 关掉 9224 端口上的僵尸插件实例（`figma_get_status` 曾报 pid 115280）；② 退出 Figma Desktop 进程重启；③ 手动 `Ctrl+Shift+E` 导出——也卡则是客户端问题（GPU 加速 / 清 `%LOCALAPPDATA%\Figma\` 缓存），正常则重装插件；④ `download_figma_images` 的 403 是另外两个 MCP 服务器（`mcp_Figma` / `mcp_Figma_AI_Bridge`）各自读自己配置里的 token，须写进那两个服务器的配置，贴在对话里无效。

**七、本段全部复验数据（实机）**

- 24 处 Tab 底边间隙**全部 = 0**；`list` 页直子层为 `status-bar(44) → _nav-bar(48) → _list-filter(165) → _list-body(493) → _spacer(1×30 grow=1) → bottom-tab(64)`，底边 844；
- 四页收尾条全部落地：`_duck-symbol-mini` + 「没有更多了」，宽 390/358/358/358，底边 593/704/820/734；
- 15 个主态画框可见内容底边 522–833px；
- 画布正式内容 emoji **0 命中**（同口径复扫）；
- 离线探针 **227/227**、Token 探针 **125/125**。

**八、新增原则**

- **原则 115 · 项目里已有既定判据的事，临时另写一套判据等于把踩过的坑重踩一遍**。实机扫 emoji 时我手写码点区间 `U+2600-27BF`，把 `✓` 误报成漏网并动手改了代码；而探针早已改用 `\p{Emoji_Presentation}`，注释里连「✓ 是我们用来替代 emoji 的东西」都写明了。**判断信号：要在新场景做一件「探针/脚本里已经做过」的检查时，先去读那段判据的注释 —— 它往往记着首版为什么不能那么写。**
- **原则 116 · 横扫要选对单位：缺陷的单位常是「构造器调用点」而不是「页面」**。上一段横扫 11 个页面自认已尽，却漏了 `list` 页的 Tab —— 因为该缺陷的单位是 `bottomTab()` 的 13 处调用点。**判断信号：修完一处后先 Grep 出该构造器/该模式的全部调用点并逐一核对，页面维度的横扫覆盖不了它。**
- **原则 117 · 断言首跑大面积报红时，先算「缺陷全集应该有多大」再定是判据错还是内容错**。Tab 贴底断言首跑报 22 处失败，而实机横扫早已测明缺陷全集只有 1 处 —— 两个数差一个数量级，必然是判据错。**判断信号：报红数远超实测的缺陷数时不要逐条去修内容，先回头审判据是否漏了某种正当写法（本轮漏的是「非 Auto Layout 容器不可能有 layoutGrow」）。**








#### [70-d] 2026-08-30 · 条目 [70] 第四段：出图链路打通（exportAsync 挂死根因锁定）+ 肉眼验收查出 3 处探针盲区缺陷

**一、`exportAsync` 挂死的根因：不是 API/桥/token/尺寸，是 GPU 光栅化管线**

上一段（[70-c] 第六节）我把结论停在「`exportAsync` 这一个 API 挂死」，没再往下追。本段追到底了。

难点在 MCP 调用有 30s 上限，超时后无法区分「Promise 挂死」与「只是慢」。解法是 **fire-and-forget + `pluginData` 回传**——发起导出后立刻同步返回，结果写进 `figma.root` 的 pluginData，隔几分钟再来读：

```js
r.exportAsync({ format: 'PNG' }).then(function (b) {
  p.setPluginData('expProbe', 'OK len=' + b.length + ' ms=' + (Date.now() - t0));
  r.remove();
}).catch(function (e) { p.setPluginData('expProbe', 'ERR ' + ...); });
return { fired: true, syncMs: Date.now() - t0 };   // 同步 2ms 返回
```

四格式对照（**同一 10×10 矩形、同一 API、同一会话**，唯一变量是 `format`）：

| 格式 | 结果 | 内部路径 |
|---|---|---|
| SVG | **OK 149B / 31ms** | 矢量序列化 |
| **PDF** | **OK 5388B / 21ms** | 矢量 |
| PNG | 40s+ 永久 pending（既不 resolve 也不 reject） | **GPU 光栅** |
| JPG | 40s+ 永久 pending | **GPU 光栅** |

390×844 完整画框走 SVG 也 **OK 297581B / 133ms**，证明复杂度不是变量。

**结论：挂死的是 Figma Desktop 的 GPU 光栅化管线**。这同时纠正了我上一段的一处误判——我曾把 `figma_capture_screenshot` 的 30s 超时判为**独立**问题，实为同一根因的第二个症状（截图也要光栅化）。

**二、用户执行的「杀 Figma 进程」为何没生效**

用 `Get-CimInstance Win32_Process` 查各进程的 `--type=` 参数与启动时间：

| 进程类型 | pid | 启动时间 |
|---|---|---|
| **main** | **25920** | **8/28 15:13（未重启）** |
| gpu-process | 115788 | 8/30 12:04 |
| renderer ×6 | 10940 等 | 8/30 12:07 |

窗口 `sds-app - Figma` 挂在 25920 上。**GPU 与渲染进程都换了新的，主进程没换** —— 导出请求由主进程中转，挂死队列没清掉。这解释了为什么用户「已经处理」了却毫无变化。

**三、绕行方案（已生效）：REST 云端渲染**

`figma_get_component_image` 走 **Figma 云端 REST 渲染**，完全不碰本地 GPU。七张图全部落盘（`scale: 2` → 780×1688），文件名 `r64-*.png`。

一个操作细节值得记：**并发请求 4 张以上会打断桥的 fileUrl 缓存**（报 `No Figma file URL available`）。修法是显式传 `fileUrl` + 串行请求。

**四、七张图肉眼验收：227 项探针全绿，却查出 3 处缺陷**

| # | 缺陷 | 严重度 | 页 |
|---|---|---|---|
| 1 | 三行认证文本**互相重叠**、meta 被截断 | **阻断** | trust |
| 2 | 退出登录按钮浮在页面中部、下方空 250px | 高 | profile |
| 3 | 操作组游离卡片之外，看起来像属于**下一张**卡 | 中 | my-publish |

其余四页（notification / list / my-favorite / settings）通过。

**这是本轮最值得记的一件事**：227 项机读断言全绿，而肉眼一看就有一处**阻断级**缺陷。原因见第五节。

**五、缺陷 1 的根因：hug 容器 × 子节点 grow —— 项目里第三次踩同一个坑**

`certRow` 的容器写成 `box(..., 'HORIZONTAL', { gap, align })`，**没写 `w`** → 宽度是 hug → hug 容器没有「剩余宽」可分 → 内部 `main.layoutGrow = 1` 拉不动 → main 塌成 0 宽 → 「企业认证」与「查看 ›」坐标重合、meta 整段被挤出可视区。

**坑早有定论，只有这一处漏了**：[code.js:1514-1518](file:///d:/developer/code/aicoding/s2s/prototype-figma/code.js#L1514-L1518) `searchBar` 的注释写明「宽度既然由内容决定，layoutGrow 就拉不动它」；`notifyRow(:3380)`、`card(:1717)` 两处都在容器上显式写了 `w`。这正是**原则 116**（缺陷的单位是构造器调用点）的又一次印证，只是这次单位更细——是「同一类容器的每一次构造」。

修法：`certRow` 增 `width` 参数，由调用方传 `cw - SPACING.md * 2`（行宽取决于宿主卡的可用宽，构造器无从自知）。

**六、探针 +10 条（227 → 237），其中新判据改了三版**

新增两条批次内断言（×5 批次）+ my-publish 两条既有断言改严：

- **⑭ 凡设 `layoutGrow` 的节点，其父容器主轴方向不得是 hug** —— 抓缺陷 1 的**成因**；
- **⑮ 同一横排内子项宽度合计不得超出行宽** —— 抓**现象**（真压字了）。两条都留，因为重叠成因不止 hug 一种（负 itemSpacing、固定宽合计超出、ABSOLUTE 失手都会压字而父级并非 hug）。

**为什么此前 227 项全绿却拦不住**：既有断言量的全是「节点自身的几何与层级」，而这里错的是**父子两级属性的组合**（父 hug × 子 grow）——单看任一级都合法。**「组合非法」是探针的结构性盲区**，故新增原则 118。

**⑭ 的判据改了三版，第二版是「假的绿」**：

| 版本 | 判据 | 结果 |
|---|---|---|
| ① | `layoutSizingHorizontal === 'HUG'` | 误报 77–102 处（mock 里该字段初值恒为 `'HUG'`，`box()` 传 `w` 不改它，见 [probe:187](file:///d:/developer/code/aicoding/s2s/prototype-figma/probe-layout-offline.js#L187)） |
| ② | 量「父宽 − 固定子项宽是否还有剩余」 | **237 全绿，但反向验证一个都报不出** |
| ③ | `primaryAxisSizingMode === 'AUTO'` | 首跑 234/3，排除一种正当形后全绿 |

第 ② 版的假绿是本段最重要的自查：hug 父级的宽本就等于内容合计、grow 子节点的内容宽也计入父宽，算下来**恒有剩余**。我撤掉 `certRow` 的 `w` 重跑，仍 237 全绿——**若不做反向验证，就会带着一条无效断言收工，还自以为补上了防线**。

第 ③ 版的正确信号来自读 mock 源码后对照 [code.js:783](file:///d:/developer/code/aicoding/s2s/prototype-figma/code.js#L783)：`f.primaryAxisSizingMode = (horiz ? opt.w : opt.h) ? 'FIXED' : 'AUTO'` —— 这才是 `box()` 真会写的字段。首跑报 3 处 `detail/publish/contact` 的 `_spacer`，按**原则 117**（先算缺陷全集该有多大）判为误报：这三页 `_body` 是纵排且只传了 `w`（走 counterAxis），primaryAxis 停在 AUTO，但随后靠 `body.layoutGrow = 1` 从画框取高，属 `pushToBottom` 的正当用法。加排除条件「父级自身沿同一轴向 grow」后全绿。

**⑭⑮ 各自都做了反向验证**：撤 `certRow` 的 `w` → 精准报出 3 行 `cert/*` 且仅此；人为塞一个 40 宽容器装两个 30 宽子项 → 精准报出 `_ov-test` 且仅此。

**七、另两处修复**

- **缺陷 2**：`_quit` 与 `bottomTab` 一并交给 `pushToBottom`。不给 `_quit` 单独插 spacer 是因为 `pushToBottom` 一次只造**一个** spacer，两个 spacer 会把剩余空间对半分、按钮反而卡在中间偏下。
- **缺陷 3**：操作组与分隔线追加进 `card()` 返回的 Frame，卡片由横排改纵排容纳两行，取消透明的 `_pub/` 中间层。可行的前提是 `card()` 是**纯原生 Frame**（未走 `COMP_CACHE`）—— 若它是 Instance，`appendChild` 会直接报错，那就只能改 master。`button()` 就属于后者。

**八、状态与遗留**

- 离线探针 **237/237**；三处修复的**实机复验未完成**——桥在清理探针残留时再次掉线（MCP 服务在 9224 等待、无插件连入，另有 9223 实例 pid 115440）。故本段先入库代码与文档，`r65-*` 渲染图与实机复验顺延到下一段。
- Figma 文件里遗留探针残留待清：`_export-probe2` 矩形 + `pngProbe2`/`pdfProbe` pluginData。
- **原始 GPU 故障未修**，需真正杀掉主进程 **pid 25920**（不是 GPU/renderer 子进程）后重启 Figma Desktop。

**九、新增原则**

- **原则 118 · 探针的结构性盲区是「父子两级属性的组合非法」，单级断言永远拦不住**。227 项断言量的全是节点自身的几何与层级，而 trust 页压字错在「父 hug × 子 grow」——两级各自都合法。**判断信号：某个属性的效果依赖于父级或兄弟的某个属性时（layoutGrow 依赖父级 sizing、ABSOLUTE 依赖父级 clipsContent、baseline 对齐依赖兄弟高度），断言必须成对写。**
- **原则 119 · 新写的断言必须做反向验证：人为造出它该抓的缺陷，确认它精准报出且不扩散**。本段 ⑭ 的第二版判据在 237 项里全绿，撤掉被修的那个参数后**仍然全绿**——是一条无效断言。**判断信号：断言首跑就绿时最危险（首跑报红反而会迫使你审判据）。绿的那一刻立刻反向验证，成本是一次 Copy-Item + 一次重跑。**
- **原则 120 · 挂死类故障要用「同一 API × 唯一变量」的对照实验定位，不要用「换个工具试试」**。`exportAsync` 挂死时，改 SVG/PDF/PNG/JPG 四种 format 跑同一矩形，两通两挂，一步定位到 GPU 光栅路径；而此前换截图桥、换 token、换画框大小试了一整段都没结论——因为那些实验同时动了多个变量。**判断信号：MCP/网络类超时无法区分「挂死」与「慢」时，用 fire-and-forget 把结果写进旁路存储（pluginData / 文件 / 日志）再回来读，就把超时问题变成了可观测问题。**
- **原则 121 · 机读全绿不等于可交付，必须有一道人眼过图**。227 项全绿的稿子上有一处阻断级压字。**判断信号：凡产出物最终由人眼消费（UI 稿、图表、文档排版），断言只能覆盖「可形式化的约束」，交付前的人眼验收是不可省的独立关卡——而且要按页逐张看细节，不能只扫缩略图。**

#### [70-e] 2026-08-30 · 条目 [70] 第五段：绕过插件 UI 直驱批次的通道打通 + 三处修复实机收口（含 fix3 一次返工）

**一、原问题：批次函数在另一个沙箱里，MCP 摸不到**

三处修复要实机验证就得重跑批次，而 `code.js` 无 `module.exports`，六个批次函数只由 `figma.ui.onmessage`（:5743）按 `msg.step` 分发。`figma_execute` 跑在 **Desktop Bridge 插件自己的沙箱**里，实测 `typeof batchSetup === 'undefined'` —— 两个插件的模块作用域互不可见。原以为只能请用户在插件面板上手点五个按钮。

**二、打通的通道：本地服务 → 沙箱 fetch → `new Function` 注入**

先探明 bridge 沙箱的三项能力（每项都用最小实验单独确认，不靠推测）：

| 能力 | 实验 | 结果 |
|---|---|---|
| `globalThis` 是否跨调用持久 | 一次调用写 `globalThis.__s2sProbe`，下一次调用读 | 持久，同一个时间戳读回 |
| 能否动态编译 | `(0,eval)(...)` 与 `new Function(...)` 各跑一次 | 两者均可用 |
| 能否取到本地源码 | `fetch('http://127.0.0.1:59999/')` | `Failed to fetch`，但读 bridge 的 `manifest.json` 发现 `networkAccess.allowedDomains` 放行 `http://localhost:9223`–`9232` |

于是：`prototype-figma/.serve-code.tmp.js` 起一个 9225 的 CORS 静态服务只吐 `code.js` → 沙箱 `fetch('http://localhost:9225/code.js')` 拿到全量 398360 字符 → 剥掉两段后 `new Function` 包起来 `return` 出六个批次函数。

必须剥掉的两段（否则会断桥）：
- `figma.ui.onmessage = ...`（:5743 起）—— 会覆盖 bridge 自己的消息通道；
- `figma.showUI(__html__, ...)`（:5769）—— 会把 bridge 的 UI 顶掉；`__html__` 在 bridge 沙箱里也不是我们的 ui.html。

还要改写一处：`log()` 里的 `figma.ui.postMessage`（:398）改为 push 进 `globalThis.__s2sLogs`，因为 postMessage 的对象是 bridge 的 UI。

三处锚点都先 `indexOf` 校验再动手，缺任一即返回 `ANCHOR-MISS` 而不是硬改。

**三、批次结果回传：沿用 [70-d] 的 fire-and-forget + pluginData**

`figma_execute` 的 `timeout` 上限 30s，而批次动辄数分钟。故 `(async function(){...})()` 触发后立刻 `return { fired: ... }`，批次自己把结果 `JSON.stringify` 写进 `figma.root.setPluginData(key, ...)`，我隔一段时间来读一次。这正是 [70-d] 定位 `exportAsync` 挂死时用的同一套手法（原则 120）。

**四、实机跑批结果**

| 批次 | 耗时 | 结果 |
|---|---|---|
| `batchSetup` | 60.8s | 字体降级 Inter；Variables 新建 0 / 沿用 43 / 改值 0；Component master 20 个；3 个画板 |
| `batchMap` | **1202.5s（20 分钟）** | 24 个画框全部生成；清空旧内容 53 个；清扫游离零件 0 个 |
| `batchCore` | 14.8s（重开插件后重跑） | 14 页；清空旧内容 26 个；游离零件 0 个 |
| `batchModal` | 63.1s | category-selector / map-selector / cert-modal + T6 五组规格板；清空旧内容 5 个 |
| `batchFlow` | 0.35s | **成功连线 23 / 23 条** |

**五、`scrollAndZoomIntoView` 是 GPU 故障的第三症状**

批次 2 为什么比批次 1 慢 20 倍：中途轮询显示 414s 时 24 帧已全部铺完、871s 时全部坐标已定位到位，但函数迟迟不返回。卡点就是 [code.js:4040](file:///d:/developer/code/aicoding/s2s/prototype-figma/code.js#L4040) 的 `figma.viewport.scrollAndZoomIntoView(frames)` —— 它要把跨 5 列 4 行的 24 个 390×844 大画框一次性缩放进视口，触发的正是 [70-d] 锁定的那条 GPU 光栅化路径。

批次 1 只缩放 3 个 Token 画板故 60s 就过；批次 2 的 24 个大画框耗掉约 800s。

这条症状还纠正了 [70-d] 的一处措辞：**该故障不是「永久挂死」而是「极慢」**。1202s 后批次 2 正常返回了完整摘要。之前 PNG 导出「40s+ 永久 pending」大概也只是没等够。

绕行办法：注入时把 4 处 `figma.viewport.scrollAndZoomIntoView(` 全部替换成 noop。它只影响视口位置，对节点产出零影响。改后 `zoomLeft: 0`，无残留。

**六、犯的错：在前一次未返回时又 fire 了一次 batchCore**

第一次 fire 的 core 与批次 2 的 20 分钟 zoom 并发，主线程被占，迟迟无进展。我误判为「结果被 map 的同名 pluginData key 覆盖」，于是换独立 key 又 fire 了一次 —— 变成两个 `batchCore` 同时在跑。两者都会 `resetSection` 删掉同一个 Section 再重建，互相破坏。

轮询证据：15.6 分钟后 trust 页的 `cert/*` 行宽仍是修复前的 100（源码里 `certW = 358 - 12*2 = 334`），`_pub/` 透明层残留 4 个、`_pub-sep` 0 个 —— 全是旧结构，两个实例互锁在重建之前。

第二次 fire 时其实已经加了 `if (globalThis.__coreRunning) return { skipped }` 守卫，但守卫是**和这次 fire 一起写进去的**，对上一次已在跑的实例无效——守卫必须在第一次触发时就存在。

处置：请用户重开 Bridge 插件清空沙箱（含两个卡住的 async 实例），重新注入后**串行**跑 core/modal/flow（setup/map 已成功，无需重跑）。

**七、三处修复的实机验证结果（全部生效）**

五个批次跑通后逐处核对几何：

```
fix1（certRow 行宽）：
  cert/企业认证  rowW=334  mainW=294  actX=302  actRight=334
  cert/家政资质  rowW=334  mainW=282  actX=290  actRight=334
  cert/车辆认证  rowW=334  mainW=294  actX=302  actRight=334
  → 334 = cw(358) − SPACING.md(12)×2；三行动作文案右缘齐平；压字消除

fix2（profile 退出登录贴 Tab 上方）：
  screenH=844  btn/danger/退出登录 y=719 h=45  shell/bottom-tab/我的 y=780 h=64
  → 719+45=764，780−764=16=SPACING.md；Tab 780+64=844 严格贴底

fix3（my-publish 操作组并入卡片）：
  legacyPubLayer(_pub/)=0  pubSep=4
  → 透明中间层已消除、4 条分隔线已下沉进 card()
```

`r65-trust.png` / `r65-profile.png` 人眼过图确认 fix1、fix2 生效。

**八、fix3 的返工：机读全绿而渲染图上操作组根本看不见**

`r65-my-publish.png` 上四张卡各只剩标题一行 —— 分隔线与操作按钮全不见了，可上面那组机读数据（`legacyPubLayer=0`、`pubSep=4`）却是绿的。

读实机节点树才看出：三行都在卡内，但**卡片高度只有 45**，而子内容合计约 130，两行被裁掉了。

根因在 `box()` 的轴向映射（[code.js:783](file:///d:/developer/code/aicoding/s2s/prototype-figma/code.js#L783)）：`primaryAxisSizingMode` 指的是**主轴**（= `layoutMode` 方向）。`card()` 建时是 `HORIZONTAL` 且只传了 `w`，于是 primary(横)=FIXED、counter(竖)=AUTO。`pubItem` 把 `layoutMode` 翻成 `VERTICAL` 后两轴语义随之互换 —— 竖轴接过 FIXED，并保留了原横排 hug 出来的 45px；横轴反而变成 HUG。

修法：翻轴向后同步纠正两轴 sizing 并显式 resize 回满宽。实机复验 45→140、宽 358、三行齐备（`_pub-top:44` + `_pub-sep:1` + `_pub-actions:47`）。

这是同一个坑的第二次：条目 [70] 的 `box()` 轴向映射修复（:770-786 那段注释）修的是**建时**传参，这次栽在**建成后改 `layoutMode`**。全稿只此一处改轴向。

探针补两条（237→239）：
- 正向 —— 卡高必须 ≥ 子节点高之和 + itemSpacing + padding，且宽 = 358；
- 反向 —— 直接构造一张只翻 `layoutMode` 不纠正 sizing 的卡，断言它确实装不下（实测 `h=76 < need=136`），证明正向断言测的是真几何而非恒真式。

`r66-my-publish.png` 人眼过图确认：四张卡各含信息行 + 分隔线 + 操作组，操作随状态变化（下架 / 刷新重发 / 继续编辑），归属清晰。

**九、遗留**

- `.serve-code.tmp.js` 是临时件，已删，不入库；
- 原始 GPU 故障仍未根治（需杀主进程 pid 25920 重启 Figma Desktop）。本段末 `exportAsync` 又挂了 88s 未返回，只能改走 REST 出图 —— 而 REST 需等本地改动同步到云，`saveVersionHistoryAsync` 后约两分钟才读到新节点；
- ✅ M3 里程碑「实际日期」已于 2026-08-31 经用户正式验收后回填（见 §1.3）。

**十、新增原则**

- **原则 122 · 「函数在别的沙箱里」不等于「调不到」，先逐项探明目标运行时的能力边界**。本段把「请用户手点五个按钮」变成全自动，靠的只是三个各一行的探测实验（globalThis 是否持久、能否 `new Function`、fetch 放行哪些域）。**判断信号：判定某条路走不通之前，先问「这个结论我实测过吗」。运行时能力（作用域、动态编译、网络白名单、存储）都是一行代码就能测的，读它自己的 manifest/配置往往直接给出答案。**
- **原则 123 · 往别人的运行时里注源码，必须先剥掉所有会抢占宿主资源的语句**。`figma.ui.onmessage` 与 `figma.showUI` 一旦跟着注进去，就会顶掉宿主插件的消息通道和 UI，等于把自己的桥拆了。**判断信号：注入前搜一遍源码里所有「注册全局处理器」「占用单例资源」的调用（onmessage / showUI / addEventListener / process.on / 全局单例赋值），逐一决定是剥除还是改写，并且每处都先 indexOf 校验锚点再动手。**
- **原则 124 · 异步任务的并发守卫必须在第一次触发时就存在，事后补加对已在跑的实例无效**。本段第二次 fire 时带上了 `__coreRunning` 守卫，但第一次 fire 的实例根本不知道这个变量，于是两个 batchCore 同时改同一个 Section。**判断信号：凡「触发后不等返回」的调用（fire-and-forget、后台任务、定时器），第一版就要把幂等/互斥写进去；补救时不要再 fire，先设法确认前一个实例的真实状态。**
- **原则 125 · 「一直不返回」要先排除「非常慢」，判据是看副作用有没有在推进**。批次 2 卡了 20 分钟最终成功返回，靠中途轮询「画框数 / 坐标是否在变」区分出了「慢」而非「死」；[70-d] 里 PNG 导出的「永久 pending」结论也因此要打折。**判断信号：怀疑挂死时，不要只盯返回值——找一个会随进度变化的可观测量（已生成节点数、文件字节数、日志行数）连续采样两次。在动就是慢，不动才是死。**
- **原则 126 · 「节点存在」验不出「节点被裁」，容器类断言必须连尺寸一起查**。fix3 的判据查了「操作组的父节点是 card」和「4 条分隔线都在」，两条全绿，可渲染图上它们一个都看不见 —— 卡高被锁死在 45px，三行内容里两行被裁在容器外。**判断信号：凡断言写成「X 挂在 Y 下面」，就再补一条「Y 的尺寸容得下它的子节点之和」。尤其是改过 layoutMode / display / overflow 这类布局语义字段的容器 —— 改轴向会让两轴的 sizing 语义互换，建时正确的设置在改后恰好反过来。**

#### [70-f] 2026-08-31 · 条目 [70] 第六段：contact 页按 §7.4.2 稿图整页重写 + 两页贴底 + detail 两态同形（三块内容抽共用构造器）

**一、本段修的四处缺陷（前三处由 r67 图人眼查出，第四处由 r68 图查出）**

| # | 页 | 缺陷 | 修法 |
|---|---|---|---|
| A | contact | 中部约 600px 纯空白，只有两个居中按钮 | 按 §7.4.2 稿图四段整页重写 |
| B | detail-offline | 禁用按钮悬在页中间（内容止于 555px） | 改走 `pushToBottom` |
| C | ai-confirm | 「确认发布」悬在 795px | 同上 |
| D | detail-offline | 中部仍空约 400px —— 缺「描述区 + 信任卡」两块 | 与 `buildDetail` 共用构造器 |

**二、contact 页整页重写：四段逐字落地 §7.4.2 稿图**

| §7.4.2 稿图 | 落地节点 |
|---|---|
| 导航「← 联系 王师傅」 | `navBar('联系 ' + CONTENT.job.publisher, { back: true })` |
| 🛡️ 隐私保护说明（两条要点） | `_privacy-note`（primary-light 底 + `RADIUS.lg`，与 detail 信任卡同档） |
| 📞 联系方式卡 + 脱敏号 + 👁️ 主操作 | `card/contact-channel` → `_phone-row`（「手机号」+ `138 **** 8888`）+ caption + `button('查看完整号码并外呼', 'primary')` |
| ⚠️ 温馨提示 + ❌ 举报 | `_safety-tip`（`stroke: 'color/warning'`）+ 贴底 `button('举报本次发布', 'secondary')` |

同时删掉三处「稿子与红线自相矛盾」的东西，并把矛盾本身写进红线卡而非静默消化：

- **删「复制微信号」**：§7.4.2 明注「用户发布时只填一种联系方式，中转页只显示该一种，**不做双卡片并列**」，而改前正是两键并列 —— 稿面上并列引导，同页红线卡上写着不许并列，两句话贴在同一张画框里；
- **删「选择一种联系方式」**：只剩一种通道后已无「选择」可言；
- **删「本产品不做站内 IM」**：Scope 口径已由红线卡第一条承载，正文再写一遍是同一句话两处维护；
- **红线卡新增两条**：`完整号码不写入前端初始状态，点击时走 API（PRD §7.7）`、`⚠ 稿图两句待裁决：…与 §7.2 红线「不做撮合结果追踪」相矛盾，本稿按 §7.4.2 稿图逐字落地`（后一条已于同日裁决并撤下，纠偏结论见下方「十、隐私说明两句纠偏」—— 当时那句标注对病因的判断本身也是错的）。

三处形制决策（均已写进代码注释）：

- **五个 emoji 一律不落地、也不新增五条矢量路径**：`ICON_PATHS` 现有 13 个 key 无这五个语义图标，而「隐私保护说明」「手机号」「查看完整号码并外呼」「温馨提示」「举报本次发布」每处文字本身已说清是什么，图标是重复编码；
- **`pad` 从 `xl` 改 `lg` 并去掉 `align: 'CENTER'`**：本页从「两个居中按钮」变成三块卡片，卡片可用宽须与 detail/publish 一致（358），居中排列会让说明块里的多行左对齐正文看起来像被随机缩进；
- **主操作留在卡内不贴底**：完整号码是这张卡的内容，拉到底沿会与它所属的号码脱开，稿图里也画在卡的第三行；
- **举报走 secondary（用户裁定）**：ghost 无描边无底色，贴底后渲染成一行页脚文字链，举报是安全兜底入口，「看不出能点」的代价大于权重略高；也不用 danger（红底白字属删除/下架这类不可逆操作，会盖过主操作）。

**三、缺陷 D 暴露的判据缺陷：只比尾部序列，缺在 spacer 之前的内容一个都看不见**

我为 detail-offline 首版写的同形断言只取 `_spacer` 之后：

```js
const i = b.children.findIndex((c) => c.name === '_spacer');
return b.children.slice(i + 1).map(...);   // 只看尾部
```

实测「正常态 `anno>btn`；失效态 `anno>btn`」全绿，可 `r68-detail-offline.png` 上中部仍空约 400px —— 缺的 `_description` 与 `_trust-card` 都在 `_spacer` **之前**。已升级为取 `_body` 全部子节点，按钮折成 `'btn'`（容许 primary/disabled 这项**允许的**差异）、TEXT 折成 `'text'`（`text()` 产出的节点名等于文案本身，直接比名字会把「文案一致」误判成内容差异）：

```js
const bodyShape = (frame) => {
  const b = frame.children.find((c) => c.name === '_body');
  if (!b) return ['无 _body'];
  return b.children.map((c) =>
    c.name.indexOf('_annotation/') === 0 ? 'anno'
      : (c.name.indexOf('btn/') === 0 ? 'btn'
        : (c.type === 'TEXT' ? 'text' : c.name)));
};
```

修后实测两态序列全等：`text>_tags>_template-fields>_description>_trust-card>_spacer>anno>btn`。

**四、结构性改动：三块内容抽成共用构造器**

原先 `buildDetail` 与 `buildDetailOffline` 各内联一份模板字段循环，且失效态注释里专门写着「失效态要与正常态逐字一致才能作为对照」—— 那正是该由同一份代码保证、而不是靠注释叮嘱的事。已抽出 `detailTemplateFields()` / `detailDescBox()` / `detailTrustCard()`，两个 build 现在都是同一组三连调用。`CONTENT.job` 同步补 `publisher: '王师傅'` 与 `phoneMasked: '138 **** 8888'` 两个 fixtures（contact 导航的「联系 王师傅」与 detail 信任卡的「王师傅」是同一个人，直写会立刻分叉；号码取稿图的带空格格式而非 §7.7 的 `138****8888` —— 后者是写给后端的字段口径，前者是给排版看的分段形态，两者不是同一件事）。

**五、探针 239 → 246**

- `tailPages` 从四页扩到六页（补 detail-offline / ai-confirm）：缺陷 B、C 之所以全程零告警，就是因为这条断言只点了它没漏的四页。已加注：**凡新增一页带底部主操作就必须进这张表，否则这条断言的覆盖面会静默缩水**；
- contact 四条：导航标题带发布者名、三块内容齐备、脱敏号存在且含 `/\*{4}/`、**单轨反向断言**「联系通道操作有且仅有 1 个」；
- detail-offline 两条：`_body` 全序列同形 + 三块内容卡点名。

**六、实机批次本段卡死，改由用户手动跑插件（用户裁定）**

`batchCore` 注入后 `__busy` 持续 true 约 15 分钟，期间画框 ID 与 contact 内容均无任何变化（仍是上轮 r68 的 `390:1352` + `btn/ghost/举报本次发布`）—— 写入零字节，是死不是慢。上一轮同批次实测 360s，本段超基线 2.5 倍仍无副作用推进即应判死，而我连等了四轮才回报，这是本段的流程失误。用户裁定后续实机由其在 Figma 插件面板手点批次 3 + 批次 5。

**七、实机复验（r69 / r70）**

用户手动跑批次 3 + 5 后出图人眼过：

| 图 | 结论 |
|---|---|
| r69-detail | 三块内容齐备（信任卡首行「信任卡 · 王师傅」），`联系 TA` 青底贴底 ✅ |
| r69-detail-offline | 与正常态**逐块同高**（164/140/85），差异恰好只剩 §7.8 允许的三项：红条 + 灰底 disabled + 全身 60% 透明 ✅ |
| r69-contact | 四段齐备、脱敏号、举报白底青描边可点 ✅，但**温馨提示到贴底举报之间空约 400px** ❌ |
| r70-contact | 空白 331→214px，四条安全提示 + 举报附注落地 ✅ |

r69-contact 那处空白的病因与本段缺陷 A、D 同族：**空着永远不能自证是有意留白还是稿子没画完**。§7.4.2 稿图这块原本只写一行「建议白天联系，交易请走线下」，落地后自然填不满。用户裁定「补安全提示内容」而非拉大间距，故按 §7.7 举报五原因反推补三条（不预付费用 / 公共场所面谈 / 遇到何种情形可举报），并在页脚按钮上方补 `_report-note`（§7.7 五原因 + §7.8 24h 时限）—— 孤立的举报按钮说不清「什么情况该点、点了会怎样」。后三条 PRD 正文无逐字原句，已写进本页红线卡待回写 §7.4.2，不静默造词。

探针再补两条（246→248）：温馨提示 ≥4 条（只锁下限不锁逐字文案）、举报附注含「诈骗」与「24」。

**八、遗留**

- ✅ 温馨提示四条已回写 PRD §7.4.2（含每条来源表 + 举报附注两行 + 稿图同步更新），`code.js` 里那条「待回写」红线卡标注同步撤下 —— 回写完还留着待办标注，稿面上就多一条假待办；
- ✅ contact 隐私说明两句已纠偏（详见下方「十、隐私说明两句纠偏」）；
- ✅ 同尺回扫 14 页，另查出并删除个人中心「帮助与反馈」越界入口（详见「十一」）；
- ✅ 四个模态首次出图，挖出并修掉五处缺陷（T6 溢出 / 确认选择按钮 / 三列 ✓ / 门牌两条通路 / map 竖向溢出，详见「十二」）；
- ✅ 24 个变体框首次逐张出图，挖出并修掉四处缺陷（过程态六档缺元素 / 半径摘要不随档 / 权限 B 态缺画框 / 地名与底图不符，详见「十四」）；
- ✅ **M3 里程碑已于 2026-08-31 经用户正式验收通过，实际日期已回填（§1.3）**。**验收材料**：14 页普通页实机渲染图（`prototype-figma/assets/render/` 下 r67–r70 共 20 张）+ 四模态实机渲染图（r71 五张含一张缺陷留证 + r72 三张修复后）+ **24 个变体框实机渲染图（r73 二十三张 + 修复后 r74 十一张）** + 离线布局探针 **256 项**（**2026-08-31 条目 [72] 加 4 条对数断言后为 260 项**）+ Token 探针 125 项（`probe-token-vars.py`）。M3 完成标志 **18 个主态 + 24 个变体 = 42 框**，已全部出图。
  > ⚠️ **此「18 主态」口径缺 `privacy-gate` 一页**（2026-08-31 条目 [72] 核出）：PRD §10.1 实为 19 行（16 页 + 3 模态），稿内 18 框，差的正是 🔴 上架驳回红线那一页。**该缺口已于 2026-09-01 M4-3d 补齐（条目 [76]）：稿内现为 19 主态 + 25 变体 = 44 框（探针实测批次 5 后 44 个画框）**，并已加对数探针防复发。**新增两框尚未实机出图**（M3 出图轮次在补稿之前），下次跑插件时一并补渲染图。此处不追改 M3 验收结论 —— 验收当时按当时清单是过的，改口径不如把缺口与它的去处一起挂在明处。

**九、新增原则**

- **原则 127 · 「两态必须同形」的判据要比的是整个内容序列，不是其中一段**。detail 正常态/失效态的同形断言只比了 `_spacer` 之后的尾部，缺在 spacer 之前的两块内容一个都看不见，于是「全绿 + 渲染图空 400px」同时成立。这是原则 126 的同族第二变种：**判据的取样范围比判据本身更容易出错**。**判断信号：写「A 与 B 应当一致」的断言时，先问「不一致可能出现在哪些位置」，取样范围必须覆盖全部位置；同时把「允许的差异」显式折叠（如按钮 variant 折成 `btn`）而不是缩小取样范围来回避它。**
- **原则 128 · 等待远程/异步任务必须设死线并带进度证据，超基线 2 倍即判死回报，不许累加等待**。本段 `batchCore` 卡死 15 分钟，我分四次 `Start-Sleep` 累加等待，每次只读返回值不读副作用；而画框 ID 从头到尾没变过 —— 第一次轮询就该判死。**判断信号：等待前先定基线（同任务上一次实测耗时）与死线（基线 ×2）；每轮轮询必须同时采一个会随进度变化的可观测量（节点 ID / 计数 / 字节数），连续两轮零变化即判死、停手、回报，不要用「再等等看」替代判断。这条与原则 125（先排除「非常慢」）配套：125 管的是别误判死，128 管的是别无限期等。**
- **原则 129 · 上游稿子本身写得薄，落地后就会以「留白过多」的形态暴露，此时该补的是内容而不是间距**。contact 的温馨提示按 §7.4.2 稿图只有一行，落地后页面下半空 400px；拉大间距能让版面「看起来匀」，但用户在这一页真正缺的是线下接触的安全提示。**判断信号：见到大片留白先问「这里本该有什么」，而不是先问「间距该调多少」。若补的内容超出上游文档的逐字口径，就地标注来源与待回写项，让缺口停留在稿面上而不是消失在我的措辞里。**
- **原则 130 · 「稿子承诺了产品没有的机制」这类缺陷读起来完全通顺，只能靠机读锁词，不能靠复审的记性**。contact 隐私说明那两句（「记录在双方联系记录中」「对方回复后可互加」）语义顺畅、语气专业，人眼过图一百遍也看不出问题 —— 它只有对着 §8.3.3 的通知类型表（唯一联系痕迹只有「被联系 × 次」且仅发布者可见）与 §7.2 的「不做站内 IM」（故无「回复」这个动作）才查得出。**判断信号：凡界面文案里出现「记录」「历史」「回复」「双方」这类隐含某个功能实体存在的词，一律回查该实体在页面清单/接口清单里是否立项；查实不存在就改写，并把关键词写进探针反向断言 —— 让下一个人写回去时立刻报红，而不是指望复审时还记得这回事。**

- **原则 131 · 上游文档内部打架时，范围约束优先于稿图；稿图只是排版参考**。§3.4.2 稿图画着「帮助与反馈」，同节 §3.3 却写「不含帮助中心」—— 照稿图落地就等于在界面上承诺一个未立项的功能。**判断信号：稿图与正文冲突时，先分清两者是不是同一层 —— 范围约束（做不做）、字段口径（存什么）、稿图（怎么排）分属三层，冲突时按「约束 > 口径 > 稿图」取值，并把裁决写回上游，别让下一个人再撞一次。守门断言也要照这个层次设计：只扫入口与操作（会向用户承诺功能的地方），不扫正文（正文里出现越界词往往是红线卡在声明「不做什么」，扫它反而会逼人删掉边界声明）。**

**十、隐私说明两句纠偏（2026-08-31 收尾）**

摊开查真源后发现，我此前给这两句下的标注本身就是错的：我写的是「与 §7.2『不做撮合结果追踪』相矛盾」，但 `contact_event` 表确实记录联系动作、且明确禁止 `contacted_success` / `deal_done` 字段（§9.5），**记录本身不违红线**。真正的病灶是另外两处，且两句病因不同：

| 原句 | 真实病因 | 裁决后 |
|---|---|---|
| 本次联系会记录在双方"联系记录"中 | ①「双方」不成立：全站唯一能看到联系痕迹的是 §8.3.3 互动通知「你的『发布』被联系了 × 次」，**仅发布者可见且只有次数**，主动联系方什么也看不到；②「联系记录」页在附录 B 的 14 页清单与 §9.4 API 清单里都不存在 —— 原句在向用户承诺未立项的功能 | 本次联系会计入对方的"被联系"通知 |
| 对方回复后可互加联系方式 | 「回复」在本产品内无处发生：§7.2 / §8.3.3 明确不做站内 IM、无消息列表、无聊天详情，联系是单向的（拿号 → 线下拨打）；互加真实发生在通话里，是线下行为，却被写成平台功能口吻 | 通话中可自行互加微信等联系方式，平台不留存 |

三处同步：PRD §7.4.2 新增纠偏对照表 + 稿图改写；`code.js` 两条要点改写 + 12 行注释记录判据 + 红线卡那条「⚠ 待裁决」换成结论；探针新增反向词断言（锁 `联系记录` / `双方「联系` / `回复后`），**已做反向验证**：把原句塞回去确实报红（检出两个禁用词），恢复后 249 全绿。

这条断言的必要性在于：两句读起来完全通顺，人眼过图一百遍也看不出问题 —— 只有对着 §8.3.3 通知类型表与 §7.2「不做 IM」才查得出。

**十一、把这把尺子扫过全部页（同日续）**

按原则 130 回扫 14 页，又查出一处**同类且更明显**的问题，且这次是 PRD 自身内部打架：

| 位置 | 冲突 | 处置 |
|---|---|---|
| 个人中心「❓ 帮助与反馈 ›」 | §3.4.2 稿图画了这一行，而**同节** §3.3 范围约束明写「不含结算记录、帮助中心等超范围项」；附录 B 的 14 页清单与 §9.4 API 清单里都无对应页与接口 | 删入口（用户裁定）。PRD §3.4.2 稿图同步删除并加注 |

由此定下一条上游冲突的裁决口径：**稿图与范围约束冲突时以范围约束为准** —— 稿图是给排版看的参考，§3.3 是范围契约。这与 [70-f] 里「稿图脱敏格式带空格 vs §7.7 字段口径不带」那次判断同源：两份文字不同层，不是同一件事。

设置页 12 行逐条比对 §3.4.3 四组正文，无越界项，未动。

**断言从「锁一页」升级为「扫全站」**：新增一条扫 8 页所有 `row/*` 入口与 `btn/*` 的断言，锁 13 个越界词（帮助与反馈 / 帮助中心 / 结算 / 钱包 / 余额 / 订单 / 退款 / 信誉 / 评价 / 成交 / 聊天 / 私信 / 客服），每词括注越界依据，报红时直接点名「哪页的哪个节点、越了哪条」。

**只查入口与按钮、不查正文**是关键取舍：正文里出现这些词往往是红线卡在声明「不做什么」（如详情页信任卡末行「不含信誉评价、不含交易记录（Scope 红线）」），那恰恰是对的 —— 连它一起报红就会逼着人把边界声明删掉，守门器反而成了破坏边界的力量。

**已做反向验证**：把「帮助与反馈」塞回个人中心确实报红（`buildProfile 的 row/帮助与反馈（越界：§3.3 不含帮助中心）`），恢复后 250 全绿。Token 探针 125/125 未受影响。

顺带纠正一处我自己的记错：Token 探针文件名是 `probe-token-vars.py`（Python），我此前按 `probe-tokens.js` 去跑，exit 1 且无输出 —— 规则 1.3「不凭记忆、先读真源」的一次现场复现，代价是一次无效执行。

**十二、四个模态首次出图，一次挖出四处缺陷（同日续）**

M3 验收标准写的是「18 主态 + 23 变体 = 41 框」，而我列的验收材料只有 14 页图 —— 对不上。用户裁定先补图。四个模态从建成起从未出过任何渲染图（`Get-ChildItem` 按 `categor|map-sel|cert|t6|board|modal|selector` 过滤返回空），补图后一次挖出四处，**没有一处是 250 项探针能报出来的**：

| 模态 | 缺陷 | 实测量 | 病因层 |
|---|---|---|---|
| board/T6 | 三条长注释把两个 AUTO 宽容器撑破画框，右侧内容整片被裁 | `_t6-3` 宽 1290（溢出 834）、`_t6-5` 宽 736（溢出 280），板宽仅 480 | 机读判据取样点错了 |
| category-selector | 缺 §5.4.2 第 5 条「底部确认按钮『确认选择』」 | 画框 844，内容底沿只到 612，空 232px | 稿图漏画 |
| category-selector | 缺 §5.4.2 第 3 条「选中态：Primary 色 + ✓」的 ✓ | 三列选中项一个勾都没有 | 稿图漏画 |
| map-selector | 缺 §7.9「地图选点页提供『取当前定位门牌』+ 手动门牌框」 | 底沿 727，空 117px | 稿图漏画 |

**T6 那处为什么躲过 250 项探针** —— 探针第⑧条判的是「文字不得超出**最近的 FIXED 宽祖先**」，而 `_t6-3`/`_t6-5` 原本是 AUTO 宽：长文本先把容器自己撑宽，于是「最近 FIXED 祖先」就是被撑大后的自己，量出来当然不超。病灶恰恰在「容器自己也跟着长大了」，**判据必须从画框这个真正钉死的边界回看**。病根在 `text()` 不设 `textAutoResize`（Figma 默认 `WIDTH_AND_HEIGHT`，长文案横向铺成一整行不折行）。修法抽成板内共用的 `wrapNote()`：`textAutoResize='HEIGHT'` 在 `appendChild` **之前**设、`layoutSizingHorizontal='FILL'` 必须在 `appendChild` **之后**（无自动布局父节点时赋值会抛 `requires an auto-layout parent`），两组容器同时从 AUTO 宽改定宽 432（否则 FILL 无依据可算）。

**补内容时又连带出第五处**：map 的 `_addr` 由 75 长到 208 高，而地图占死值 560 —— 实机内容底沿 860 > 画框 844，刚补上的门牌输入框下缘被裁 16px。减地图（560→544）而不减门牌区：门牌是 §7.9 的功能要件、少一像素都影响可点性，地图只是背景画布。

**探针 250 → 252**，两条新断言均经反向验证：
- **画框级横向溢出守门**（18 个构造器扫 FRAME 宽 vs 画框宽）：反向验证把 `_t6-3` 退回 AUTO 宽，确实报红 `_t6-3 宽 1296 > 画框 480`；
- **两模态 PRD 逐字要件 + 三模态内容高度**（确认选择按钮 / 三列 ✓ / 门牌两条通路 / 内容总高不超画框）：反向验证分两次，去掉 ✓ 报红「只有 0 个」、地图高退回 560 报红「内容总高 854 > 画框 844」。

**这里有一条自造的弯路值得记**：竖向溢出我起初写成独立断言，用「子节点 y + height 取 max」判底沿，跑出来全绿 —— 反向验证把地图高退回 560 也**报不出来**。加临时打印才查明：离线桩不实现 Auto Layout 排流，所有子节点的 `y` 恒为 0，那个判据实际只等于 `max(child.height)`。于是删掉整条，改成「直接子节点高度求和 vs 画框高」并入要件断言。**测不到病灶的守门断言比没有更坏 —— 它给的是虚假的安全感。**

出图七张（`r71-t6-board-BAD` 留证缺陷态 + `r71-category-selector`/`r71-map-selector`/`r71-cert-modal`/`r71-profile` + 修复后 `r72-t6-board`/`r72-category-selector`/`r72-map-selector`），逐张人眼过图。cert-modal 与 profile 无缺陷（后者删「帮助与反馈」后五行排布正常）。

**注入通道判死**：按原则 128 试了一次，注入成功（`{len: 404535, zoomN: 4, ok: true}`）、基线 ID `395:12052`、120s×2 两轮采样，画框 ID 与 Section 计数均零变化、`pluginData` 零回传 → 判死停手。**这台机器 bridge 沙箱注入路径已稳定失效，后续实机重跑一律走用户手点插件**；但实机**几何校正**走 `figma_execute` 是通的（本轮 T6 两组定宽、map 地图改高都是这么落的）。

**十三、新增原则（承第九节）**

- **原则 132 · 机读的盲区与人眼的盲区重叠处，就是缺陷长期藏身的地方**。T6 那处溢出在稿面上肉眼可见（长注释齐刷刷切断），却活到第 71 轮：机读那边判据取样点错了（从 AUTO 宽容器往上找 FIXED 祖先，找到的是被撑大后的自己），人眼那边这四个模态从建成起一张图都没出过。两条路各自的覆盖面都不小，但它们的**空隙是重合的**。**判断信号：定期列一张「哪些产物从未经过哪种检查」的清单 —— 不是问「探针覆盖率多少」，而是问「有没有哪块东西既没被机读量过、也没被人眼看过」。凡答案是「有」的，先给它出一张图，再谈补断言。**
- **原则 133 · 守门断言必须经反向验证；测不到病灶的断言比没有断言更坏**。本轮竖向溢出断言首版全绿且反向验证也不报红，查明是离线桩不实现 Auto Layout 排流（子节点 `y` 恒为 0），判据实际只等于 `max(child.height)`。若不做反向验证，它会以「已守门」的名义永久掩盖这一族缺陷。**判断信号：每写一条断言，立刻用「把病灶塞回去」的方式跑一次，必须看到它报红且报的是那句话；反向验证要改参数（如把定宽退回 AUTO、把高度退回原值），不要改代码结构 —— 本轮用正则替换改结构，丢了闭合括号造出语法错，白跑一次。**

**十四、23 个变体框首次逐张出图（r73）：挖出四处缺陷并全部修掉（2026-08-31，条目 [70-h]）**

按原则 132 的判断信号列「从未经过哪种检查」的清单，答案落在**变体框**上：18 个主态已在 r67–r72 逐张出过图，23 个变体框（同页状态差）除 `detail-offline` 外**一张都没出过**，而它们同样被 252 项探针机读为全绿 —— 正是「机读盲区 × 人眼盲区」的重叠处。故补出 r73 二十三张，逐张人眼过图，查出四处缺陷：

| # | 缺陷 | 探针为何全绿 | 修法 |
|---|---|---|---|
| 1 | **兜底过程态六档画面完全等于主态**，PRD §6.4.4 六行表里写明的骨架屏 / 进度条 / 结果卡 / 按钮一件没画 | 探针只核了「六档的 `ui` 文案与表一致」，文案确实逐字对得上 —— 对齐的是**表**，不是**画面** | `EMPTY_FALLBACK_TIMELINE` 从「文案表」扩为**元素级表**（`skeleton`/`progress`/`results`/`buttons`/`terminal`），新增 `skeletonCard()`／`progressBar()` 两个构造器，六档按字段画；终态档走无地图版式 |
| 2 | **半径四档的摘要胶囊全写「5km」** | `mapCanvas` 第四参不传 `summary` 时取默认值，探针没有任何断言要求它随档位变化 | 第四参补 `{ summary: tier.tier + ' · 资源+需求 · 全部分类' }` |
| 3 | **PRD §6.4.4 权限三分表的 B 态「曾授权后被关」根本没有画框** | 纯缺失：没有画框，就没有节点被扫到 | `COVERAGE_STATES` 补 `permission-reopen` 键，引导页改 `permStates` 表驱动两态循环（PRD :1213 明写 A/B 共用同一套布局，只差标题/说明/主按钮文字与动作四处） |
| 4 | cell-fallback 标注写「朝阳门商圈附近」而底图是合肥 | 探针不认识地名，更不知道底图在哪座城 | 改为「当前商圈附近 · 全部类 · 供需全开」，不再写死具体商圈 |

**缺陷 3 是这四处里唯一会让用户认为「App 坏了」的一处**：系统拒绝是 sticky 的（PRD :1209），B 态若沿用 A 态的「开启位置权限」按钮，点了不会有任何反应。这一态从 PRD 8/27 写下就没有过画框，也没有过图。

**修的过程中离线探针又替我捞出两处真缺陷**（这两处都是「补内容连带出的新病」，同族于 [70-g] 的第五处）：
- **B 态标题在 h1 下 hug 到 360，超出 `_body` 可用宽 342**。「A/B 共用一段代码」正是为防这种事 —— 只按 A 态的 7 字短文案写死，B 态的 13 字必被截断。修法沿用 [70-g] 的 `wrapNote()` 手法：`textAutoResize='HEIGHT'` + `layoutSizingHorizontal='FILL'`（后者必须在 `appendChild` **之后**赋值）；
- **终态档的 Tab 被「贴画框下沿」断言判成悬空**（内容只到 407 < 844）。根因是我用了 `layoutSizingVertical='FILL'`，而该断言认的路径是「前有 `layoutGrow > 0` 的兄弟」。两者在实机等价，但稿内其余贴底页（profile / trust 的 `_body`）一律写 `layoutGrow = 1` —— 改成同一写法即通。**同一件事在一份稿里只该有一种写法，否则守门断言必然只认得其中一种。**

**探针 252 → 256**，新增 3 条断言（另 1 条为「三张真源表已导出」前置），**四轮反向验证全部成功报红**（原则 133）：

| 断言 | 反向验证（只改参数不改结构） | 实际报红 |
|---|---|---|
| 过程态六档 PRD 逐字元素（骨架卡 = 3 条 / 进度条存在 / 结果卡 / 按钮文案逐字 / 缺省图只在首末档 / 终态档不得含 `_map-canvas`） | 骨架卡循环 `sk < 3` 改成 1 | 「骨架卡 1 条 ≠ PRD 的 3 条」 |
| 同上（终态档无地图） | `if (step.terminal)` 改 `if (false)` | 「终态空页仍含 `_map-canvas`」 |
| 半径四档摘要胶囊须含本档档位值 | 第四参改回 `{}` | 「3km 档胶囊写的是「5km · …」，不含本档半径」（3 处） |
| 权限 A/B 两态齐备 + B 态主按钮跳设置 + 不得残留「开启位置权限」+ 两态结构同形 + 两态均有「不用了」出口 | B 态 `primaryLabel` 改回「开启位置权限」 | 报两条（主按钮不对 + sticky 拒绝下点了无反应） |

**新断言首跑四处假红，根因值得记**：「缺省图只在首末档出」判据扫的是**整个画框**，于是把底部 Tab 的「鸭圈」页签（它本身就是一个 24px `duckSymbol`，[code.js:1662](file:///d:/developer/code/aicoding/s2s/prototype-figma/code.js#L1662)）当成了缺省图，六档里的四个中间档全部误报。改成只扫 `_proc-panel` 内部后全绿。这是原则 127「判据的取样范围比判据本身更容易出错」的第三个变种 —— **判据的作用域错了，就等于判据错了**，且它报的是假红而不是假绿，比假绿好在会逼你查，坏在会诱你去改本来正确的代码。

**实机重跑与 r74 复验**：按 [70-e] 判死的注入通道，实机重跑仍走用户手点插件（批次 1→5 全量）。按原则 128 走桥先采基线作可观测量 —— `figma_execute` 实测 `sections: [地图与列表:55, 核心流程:26, 模态与T6:5]`、`total:95`、`variantCount:24`、`hasReopen:true`、六个过程态框齐，即判定重跑已完成，不再空等。随后走桥核实机几何，四项修复全部通过：六档均 390×844 不变形、面板底沿 676 < 844、终态档 `map:false`、半径四档胶囊分别为 3km/5km/10km/全城、权限 A 态主按钮「开启位置权限」B 态「去系统设置打开」且两态均有「手动选择城市」出口、cell-fallback 胶囊为「当前商圈附近 · 全部类 · 供需全开」。

出 r74 十一张（六档过程态 + 半径 3km/全城 + 权限 A/B + cell-fallback）逐张人眼过图，全部通过：骨架卡三条读得出是「列表在加载」、进度条 60% 青色填充读得出在动（两端极值都会被读成静止，故不画 0% 或 100%）、终态空页确实无地图、B 态 13 字标题正常折两行未截断。**顺带记录一处非缺陷**：半径 3km 与全城两档底图相同 —— 静态占位底图的既有限制，胶囊与标注已随档位变化，不属缺陷。

**出图链路本轮换了一次通道**：`download_figma_images` 先报路径越界（server 镜像目录为 `C:\Users\刘杰`，须传相对路径）、再报 403 `Token expired`（REST token 过期）；`figma_capture_screenshot` 两次 30s 超时。最终走 `figma_get_component_image` 逐个取 S3 URL（30 天有效）再用 `Invoke-WebRequest` 落盘。**这条通道是目前最稳的一条，记在此处备下一轮直接用。**

**一处口径同步**：M3 完成标志与条目 [51] 原写「23 个变体 = 41 框」，补出权限 B 态后改为 **24 / 42**。`prototype-figma/ui.html` 的首屏计数由 `planStats()` 运行时回填**主态数**（不含变体），无需改。

### [71] 2026-08-31 M4-3 三级分类树建模 + 登录页 login-screen 落地（PRD §2.4 / §3.4.1 / §12.2）

本轮范围由用户裁定为「登录页 + 三级分类树建模」两件同批，会话实现方式裁定为「本地会话 + 具名失败态」。**同批的理由是依赖关系而非工作量**：分类树是下一轮发布页的硬前置（没有叶子类目就取不到 §5.7 的模板 Schema，§5.4.2 的三列级联选择器也无数据源），同时它正是条目 [67] 记下的**已知架构欠账**（详情页至今靠 `categoryPath` 字符串字面量临时缓解）；登录页则是三处已埋 TODO 的共同前置（list 收藏 / detail 收藏 / contact「未登录」失败态）。

**一、分类树建模（`lib/domain/category_tree.dart`，5 一级 / 21 二级 / 48 叶子，§2.3 上限 60）**

关键设计决策（均已写入代码注释，此处只记「为什么不选另一条路」）：

| 决策 | 舍弃的替代方案及其代价 |
|---|---|
| 单一 `CategoryNode` 自引用结构 | 三个层级类会让「按 `parent_id` 回溯面包屑」退化成三段 switch；§13.2 的 `category` 表本身就是自引用单表 |
| **ID 编号规则即层级**（一级 `N` / 二级 `N*100+M` / 三级 `N*10000+M*100+K`） | `10101` 可整除推出二级 `101`、一级 `1`，回溯无需深搜。该 ID 即 §13.2 的 `post.leaf_category_id`，**属契约，已发布编号不得重排** |
| `topCategory` 由 id 首位推导 | 另存一个 `topCategory` 字段等于同一事实记两份，挪节点漏改一处就会「房屋分支显示成蓝色」 |
| `RequiredCert` 用枚举而非 bool | §5.8 的拦截动作要跳**对应**认证页，bool 给不出跳哪一个 |
| 名称逐字照抄 §2.4 | 「二手闲置转让」不简写、「家政/保洁」保留斜杠 —— 顺手规整会让稿图与代码对不上，而这种偏差没有任何一条断言会报 |
| 非法 ID 返回空列表而非抛异常 | 面包屑是展示位，为一个展示位崩掉整页不成比例 |

**高敏标记取向偏保守**：只按 §4.3 明写场景打标。求职（10301）/ 求租（20201）/ 二手车转让（30301）/ 求搭车（30401）**刻意不打标**，理由已写进注释 —— 这四类是个人自用行为，打标会把普通用户挡在门外。

**详情页欠账已还清**：`listing_detail.dart` 的 `categoryPath` 从字段改为 getter（`categoryPathOf(leafCategoryId).map(...)`），`leafCategoryId` 升为 required；`listing_detail_repository.dart` 删掉手写 `_samplePath()`，改为按分类映射叶子 ID。**手写的那版曾同时存在「房屋 > 整租 > 一室一厅」与一级写成「家政」两处错，而这两处错没有任何一处会报错** —— 这正是下面守门测试存在的直接理由。

**二、守门测试 16 项 + 反向验证两轮（原则 133）**

测试文件头部写明存在理由：**分类树是一大坨手写常量，抄错不会报错**。重点在「把 ID 编号规则这条隐式约定变成会失败的断言」，属机读盲区 × 人眼盲区的重叠处（原则 132）。

| 注入（只改参数不改结构） | 实际报红 |
|---|---|
| A：`20103` → `20903`（叶子挂错父级） | 4 条（父级不匹配 / 回溯失败 / §2.4 原文路径 / 企业认证） |
| B：`50103` 开荒保洁的 `personalQualification` → `none` | 1 条（服务类叶子未标个人资质） |

**三、登录会话（`lib/features/auth/auth_repository.dart`）**

| 决策 | 若反着做会怎样 |
|---|---|
| `sendCode` 返回 `Future<AuthFailure?>` 而非 `AuthResult` | 发码不产生会话，硬塞进 `AuthResult` 会出现「成功了但 session 是 null」，而 `isSuccess` 正是靠 session 判定 —— 每个调用点都得记住「发码这里别看 isSuccess」 |
| 成功/失败共用一个结果对象而非抛异常 | 输错验证码是**主要流程之一**不是异常，且失败要带回 `remainingAttempts` / `lockedUntil` |
| `AuthRepository` 必须全局单实例 | 待校验码与失败计数存实例字段，每次新建会重置计数、风控形同虚设 |
| `kSmsCodeTtl`(5min) 与 `kSmsResendCooldown`(60s) 分开 | 合成一个值会导致倒计时结束的同时码也失效，用户按 §3.5 节奏操作反而永远登录不上 |
| 锁定期内**连发码都不给** | 只拦登录不拦发码，攻击者仍能持续消耗短信费用（短信按条真实计费） |
| 成功登录清空失败计数 | 否则「昨天错 4 次、今天成功」的用户下次错一次就被锁 |
| 锁到期时连同计数一起清零 | 否则解锁后第一次输错立刻又被锁 15 分钟 |
| `AuthSession.phone` 存完整值 | §3.7 脱敏针对的是「**别人**的联系方式」，本人号码个人中心要显示、改绑要用 |
| `realNameVerified` 与登录态分离 | §3.8 明确未实名仍可登录、可发受限态帖子，合成一个 bool 会让这一大类用户无处可归 |
| 会话用可空 `AuthSession?` 而非额外 `isLoggedIn` 布尔 | 两个字段能表达「已登录但会话为空」这种不该存在的组合，而组合一旦可表达就必有分支忘了同步 |
| `_debugCode = '888888'` 定义为常量 | 接后端时删掉它，所有引用处立刻编译报错，不会有本地后门留在包里 |

**四、登录页（`lib/features/auth/login_screen.dart`）**

**§3.4.1 八条要件中本期不做两条，原因写在文件头部**：① **图形验证码** —— 客户端自绘等于「自己出题自己判卷」，攻击者读内存即绕过，**留位不留假实现**（假实现会让人以为这道防线已经有了）；② **密码登录折叠区** —— §3.4.1 标注为「次方案」且 `/auth/password/login` 无服务端，**做一个点开后只能报错的折叠区比暂时不做更糟**。

其余取舍：**协议不预勾**（§3.4.1 明写「默认不勾，必须手动勾」，属《个人信息保护法》单独同意要求，**预勾在合规检查中属可判定的违规项**）；**登录页可关闭**（与隐私门相反 —— §3.8 明确未登录可只读浏览，把登录做成不可退出的门等于把「先注册后登录」换个位置又装回来）；**成功走 `pop(true)` 而非 `go(home)`**（登录多半从收藏/联系按钮弹上来，跳首页会让用户丢失原本在看的信息）；**60s 倒计时真源在仓库侧**（放页面则退出重进即可绕过冷却）；**错误用常驻横条而非 Toast**（验证码类错误需用户对照着改输入，Toast 会在他抬头看输入框时消失）；**错误文案动态拼接**（写死「15 分钟」会在等了 10 分钟后仍显示 15；剩 ≤2 次时拼出剩余次数，否则第 5 次毫无预警被锁）；**第三方入口灰禁用而非隐藏**（§3.4.1 要求呈现，**看不见的东西无法判断是漏做还是刻意不做**）；**Logo 用几何占位**（工程未配 `flutter/assets` 也未引入 `flutter_svg`，为一个装饰位引入渲染依赖属超出本轮范围，留 `TODO(M4-4)`）。

**一处坏味道自查并修正**：`_codeSent = false` 的复位起初写在 `_errorTextOf()` 里 —— **一个名为「取文案」的方法顺手改状态，是日后排查「谁把 `_codeSent` 改回去了」时最难找的那一类**，已移到 `_submit` 的 `setState` 内。

**五、§7.7「仅登录用户可拉完整号码」接线**

登录前置判定**放页面而非仓库**：仓库扮演服务端，服务端只该返回 401，「弹登录页」是客户端职责 —— **客户端判是为了少一次注定失败的请求，服务端判才是那道真正的门**。`_FailureBanner` **仅 `notLoggedIn` 给「去登录」按钮**（超限与熔断给按钮反而有害，点了也没用只会让人反复点）。登录回来**自动重试拉取**：让用户回到中转页还要再点一次按钮，等于把他刚才已经表达过的意图丢掉；用户直接关登录页则不重试。

**范围守纪的一处主动判断**：Grep 查到 list / detail 两页的「收藏」在注释中标注为「依赖 `favorite` 表建模（§13.2 以 user_id 为键），本期不做」而**非 TODO**，故本轮**不擅自接线收藏**，只接 §7.7 联系方式这一处。

**六、新增测试 39 项 + 三轮反向验证，其中一轮暴露出真实缺陷**

`test/auth_repository_test.dart` 24 项 + `test/login_screen_test.dart` 15 项（**工程首个 widget 测试**）。全量 `flutter test` **111/111 全绿**（72 → +24 +15），`flutter analyze`（含 test 目录）**零告警**。

两个测试文件头部都写明「为什么必须存在」：`AuthRepository` 的风控分支在页面上几乎无法手测触发 —— 「连错 5 次」要真的错 5 次、「锁 15 分钟自动解锁」要真的等 15 分钟、「成功登录清空计数」要跨两次登录才看得出差别；而 §3.8 的「协议未勾 → 按钮 Disabled」在代码里只是 `bool` 的一个与项，**任何一次重构把 `_agreed` 从 `_canSubmit` 里漏掉，编译器不报错、界面照常能用**。时间全部注入而非用 `DateTime.now()`：断言的正是 5 分钟 / 60 秒 / 15 分钟这三条边界，用真实时钟只能靠 sleep，既慢又会在 CI 上偶发失败。

| 轮次 | 注入（只改参数不改结构） | 结果 |
|---|---|---|
| A | `_agreed` 初值 `false` → `true`（模拟违规预勾） | 5 条报红，含「协议默认不勾选」与「其余条件全满足但未勾时主按钮仍禁用」两条关键项 |
| B | `kMaxFailedAttempts` 5 → 6（模拟阈值被改错） | ⚠️ **首跑 23 项全绿 —— 反向验证在此暴露真实缺陷** |
| C | `_lockedUntil` 里 `_risk.remove(phone)` 改为只清锁保留计数 | 1 条报红（锁到期自动解锁，且失败计数一并清零） |

**B 轮这处缺陷值得单记 —— 它是原则 133 的一个新变种**：我把所有循环上界都写成 `kMaxFailedAttempts` 而非字面量 5，动机是「阈值调整时用例不用改」，看起来是好习惯；**代价是这批用例对「阈值本身被改错」完全失明** —— 常量改成 6，它们就老老实实错 6 次然后照样全绿。PRD §3.8 与 §12.2 写死的是**具体数字**，那么至少要有一条断言钉住具体数字。已补「阈值取值本身即契约」一项（`kMaxFailedAttempts == 5` / `kLockoutDuration == 15min` / `kSmsCodeTtl == 5min` / `kSmsResendCooldown == 60s`），重跑 B 轮确认报红后恢复常量。**判断信号：凡断言的右侧取自被测代码自己的常量，它守的就只是「代码自洽」，不是「代码符合契约」；契约值必须至少有一处用字面量钉死。**

**一处环境噪声记录**：`flutter analyze` 退出码为 1 但输出 `No issues found!` —— dart analysis-driver 写 `C:\Users\刘杰\AppData\Local\.dartServer\` 缓存被 TRAE Sandbox 拦（`hit restricted`）。**判定以 `No issues found!` 为准**，退出码在此环境不可信。

**七、新增原则（承条目 [70-h] 第十三节）**

- **原则 134 · 断言右侧若取自被测代码自己的常量，它守的只是「代码自洽」而非「代码符合契约」**。本轮 23 项风控用例的循环上界全写成 `kMaxFailedAttempts`，动机是「阈值调整时用例不用改」—— 看起来是好习惯，实测把常量改成 6 后**23 项一条不红**：它们老老实实错了 6 次，然后照样全绿。而 PRD §3.8 与 §12.2 写死的是**具体数字 5 与 15 分钟**，这条契约在整个测试套件里当时无人守卫。**判断信号：凡一条断言的期望值来自 `import` 进来的常量，就要再问一句「这个常量本身写错了谁会发现」；契约类数值（PRD 明写的次数、时长、上限）必须至少有一处用字面量钉死，且这条断言的名字里要写明「契约」二字，好让日后想改常量的人知道该同步改哪里。** 它与原则 133 的区别在于：133 说的是「断言测不到病灶」，134 说的是「断言随病灶一起变形」—— 后者更隐蔽，因为反向验证若也只改结构不改那个常量，同样发现不了。

### [72] 2026-08-31 UI 设计环节闭环核实：查出 privacy-gate 缺稿 + 加 PRD ↔ Figma 对数探针（PRD §10.1）

**一、起因与核实方法**

用户问「UI 设计这一环节是否闭环了」。这个问题不能凭 M3 验收结论回答 —— 验收通过只说明「当时那份清单全出图了」，不说明「那份清单本身是全的」（规则 1.3：不凭记忆、先读真源）。故取**三份真源逐页对照**：`docs/PRD.md` §10.1 页面清单（契约）× `prototype-figma/code.js` 的 `MAIN_SCREENS`（稿真源）× `lib/features/**`（代码实现）。

**结论：未闭环，差 `privacy-gate` 一页**，而它恰是 §14.6 列的 🔴 上架驳回阻塞项。

| 页面 ID | PRD §10.1 | Figma 稿 | Flutter 代码 |
|---|---|---|---|
| **privacy-gate** | ✅ P0 | ❌ **无**（Grep 零命中） | ✅ `privacy_gate_screen.dart` |
| **splash-screen** | ❌ **无**（本轮补列） | ✅ `buildSplash` | ❌ 无（M4 未排） |
| login / home / list / detail / contact | ✅ | ✅ | ✅ |
| publish / ai-confirm / publish-success | ✅ | ✅ | ⏳ 下一批 |
| profile / my-* / notification / trust / settings + 三模态 | ✅ | ✅ | ⏳ 未排 |

**二、根因：每一环都没错，但验收的是一份已经过期的清单**

时间线摆出来就清楚了：`privacy-gate` 所属的 PRD §6.5.1 是 **2026-08-27** 才补进 PRD 的（它本身就是「M4-3 启动时发现的规格缺口」），而 M3 的完成标志「18 主态 + 24 变体 = 42 框」在那之前就已定下。于是 42 框全部出图、探针 256 项全绿、用户 08-31 正式验收通过 —— **没有任何一步做错，错的是没人回头问一句「清单本身还是最新的吗」**。

**此前 256 项为何一条不报**：既有的 `MAIN_SCREENS` 校验（`probe-layout-offline.js:2663`）判的是「`FLOW_LINKS` 两端都已登记」—— 判的是**稿子内部自洽**。稿里没有的页，既不会出现在 `FLOW_LINKS` 里，也不会出现在登记表里，于是自洽得完美无缺。这正是原则 134 那种失明的画布版：判据右侧取自被测对象自己，它就只守自洽、不守契约；**契约在 PRD 那一侧，判据必须跨到 PRD 去取**。

**三、处置：用户裁定方案 B —— 补稿排进 M4-4，本轮只加防复发断言**

给过三个选项：A 立即补稿（约 1 轮工作量，但打断 M4 发布闭环节奏）；**B 排进 M4-4 与隐私政策同批**；C 只记录不排期。用户选 B。**同批做的理由**：§6.5.1 定了协议文本走在线 URL（`GET /legal/{doc}`）而非内嵌，那么协议页 URL 不落地，门里的「查看完整协议」就指向空 —— 两件事的验收条件本就绑在一起，分批做等于要验两遍。

**四、新增 4 条对数探针断言（`probe-layout-offline.js:2695` 起）**

判据从 PRD §10.1 表格现取，不在探针内抄任何页面清单副本：

| 断言 | 守什么 |
|---|---|
| PRD §10.1 页面清单表解析出 **19 行**（字面量钉死） | 判据源本身有效，且 §10.1 增删页必须有人来改这个数字 |
| PRD 每一页都在 `MAIN_SCREENS` 内（已备案缺口除外） | 契约有、稿里没有 → 报红（本轮病灶的正向守门） |
| 已备案缺口清单无过期项 | 补稿后忘删豁免 → 报红 |
| `MAIN_SCREENS` 无 §10.1 之外的页 | 稿里有、契约没有 → 报红（「画了没人要的页」） |

**两处设计取舍值得记**：

- **「已备案缺口」而非放宽断言**：显式列 `KNOWN_STAGE_GAPS` 字典，key 是页面 ID，value **必须写明去处**（本轮唯一一项：`privacy-gate` → M4-4 补稿）。**放宽等于让缺口从此隐形；备案是「承认它，并让清单外的任何新缺口立刻报红」。** 再配一条反向守门：备案清单必须**恰好**等于实际缺口，多一项即报红 —— 一条永远绿的豁免比没有更坏（原则 133 同族）；
- **只取 §10.1 表格第 2 列，不全文搜页面 ID**：全文搜会把 §6.5.1 正文里提到的 `privacy-gate` 也算进来，那样即使 §10.1 表格漏了一行也照样对得上 —— 而 §10.1 表格才是页面清单的真源。

**五、第 4 条断言当场抓出第二个偏差：`splash-screen` 未列入 §10.1**

写反方向那条时我预判的情形（注释原文：「稿里有而 PRD 没有的页……更容易发生，加个页比改 PRD 容易」）**首跑即命中**。判定为 **PRD 漏列**，四条依据：① §10.1 首列「Tab/模块」本就含「准入」组（`privacy-gate`、`login-screen` 同组），不存在「只列功能页」的口径；② PRD 正文三处已把启动页当**已定交付物**（§1.5 U1 旅程首站、§1.4.1.2 完整版 IP 首个消费位、§1.7 表③ `splash.svg` 标「✅ 已交付」）；③ §10.1「明确不做的页面」六项里没有它 —— 与 §6.5.1 同一形态：漏掉，非砍掉；④ Figma 侧它还是跳转起点（`code.js:6154` `startFrame = index['splash-screen']`）。用户裁定**补进 §10.1**，已补一行并写明补列理由，清单 18 → 19 行。

**顺带修掉 PRD 两处过期页数**（这是「没有对数断言时清单会漂」的现成证据）：§6.4.1 `:1078` 写「17 页」→ 改为不写死数字（写死正是漂移的成因）；§13 `:2164` 写「十四页三模态」→ 改为「十六页三模态（现表 19 行 = 16 页 + 3 模态）」并注明校正日期。

**六、反向验证三轮 —— C 轮再暴露一条永绿断言**

| 轮次 | 注入 | 结果 |
|---|---|---|
| A | `KNOWN_STAGE_GAPS` 清空 | 1 条报红：`未备案缺口：privacy-gate` |
| B | 往备案清单塞 `login-screen`（已在稿中） | 1 条报红：`已补稿但仍挂在豁免清单里：login-screen` |
| C | §10.1 标题指偏（模拟判据源失效） | ⚠️ 报红 2 条，但**最关键那条反而永绿 —— 暴露真实缺陷** |

**C 轮这处缺陷是原则 134 的直接复现**：`prdPageIds` 解析为空时，「PRD 每一页都在稿内」这条**照样通过** —— 空清单里当然找不出缺口。判据源一失效，依赖它的断言集体失明，而这条恰是本轮为 `privacy-gate` 专设的守门。**处方就是原则 134 的处方**：把原先的 `prdPageIds.length > 0` 改成字面量 `=== 19`，断言名写明「契约页数，字面量钉死」。副作用是将来 §10.1 增删页时它会报红 —— **这正是要的效果**：页面清单变动必须有人来改一次数字并留下痕迹，而不是让探针默默跟着新清单走。三轮注入均已恢复。

**七、实测数据**

离线布局探针 **256 → 260 项，全绿**（0 失败）。PRD 19 页 / 稿 18 页，差额 1 项已备案。


### [73] 2026-09-01 M4-3a 发布主链路：模板引擎 + 级联选择器 + 发布页 + 认证拦截浮层（PRD §5.4 / §5.8 / §4.4）

**一、先改计划再动代码：把「发布闭环」拆成 a / b 两轮**

原待办是一行「发布闭环（publish / ai-confirm / publish-success / cert-modal）」。本轮先按规则 4.2 改计划，拆为 **M4-3a 发布主链路**（本轮）与 **M4-3b AI 确认与完成页**（下一轮）。

**拆的理由不是工作量，是判据**：a 段判「表单能不能填完、能不能被拦住」，判据是模板字段与 §5.8 八条校验；b 段判「AI 猜完怎么确认、发完给什么反馈」，判据是完整度档与配额口径（§5.9 末段 + §5.11）。一轮做完四页，**两套判据会互相掩盖 —— 表单校验的缺陷会被当成 AI 没猜准，反之亦然**。且 b 段的完整度档要拿 a 段的字段填充率作输入，先有 a 才谈得上 b。

**二、模板引擎：四份模板挂在「二级类目」而非叶子**

§5.4.3 只给了四行模板示例，逐行核对 §2.4 后确认**四行全部是二级类目**（二手闲置转让 / 家政保洁 / 顺风车拼车 / 全职招聘），而同一二级下的各个叶子字段集合相同。故查表键取二级 ID，由叶子 ID `~/ 100` 推出 —— **按叶子配等于把同一份字段抄三到五遍**，抄的过程就是引入不一致的过程。

三处刻意的克制：

- **只做 §5.4.3 明列的四份，其余 40 个叶子走 `genericTemplate`**：给通用模板比给一份我猜的字段集合更诚实 —— **猜出来的字段会被后来的人当成 PRD 已定的规格**，届时没人知道那是谁定的；
- **脏 ID 走降级不抛错**：旧草稿与分类改版都会产生查不到的编号，抛错等于让一条旧草稿把整个发布页打崩；
- **`requiredCertForLeaf` 对非法 ID 返回 `none`**：宁可放过一条脏 ID，也不要把它拦在一个用户无法通过的认证前面 —— 那是死路，用户既发不出去也不知道为什么。

**三、表单校验：抽成不可变类，九档具名 blocker**

§5.8 那八条边界若散在 `setState` 里，只能靠手点验证，而「未实名每日 1 条」这类根本点不出来。故抽 `PublishFormState`（12 字段不可变 + `copyWith`），校验全部收敛进一个 `blocker` getter。

| 取舍 | 理由 |
|---|---|
| 具名 `PublishBlocker` 而非 `bool canSubmit` | **禁用而不说原因，是把校验规则藏起来让用户去试** —— 与登录页 `AuthFailure` 同一取向 |
| `blocker` 只返回第一个原因 | 一次抛八条提示等于没提示；顺序按 §5.4.1 表单段落顺序，用户自上而下填就是自上而下解锁 |
| `withCategory` 不用 `copyWith` | 换分类=换模板，旧 `templateValues` 的键在新模板里不存在，留着既不报错也不显示，**但会被一起提交入库成脏数据**；价格单位同理，必须重置为新模板的首个单位 |
| `priceText` 存原文不存 `num` | `5.` 这种中间态提前转 `num` 会让输入框跳字 |
| 「面议」放进 `priceUnits` 数组而非单做开关 | 单独做开关会出现「勾了面议又填了数字」这种要额外规则去排除的组合 |
| 描述 30-200 字只作提示不参与校验 | §5.4.1 原文写的是「建议」，叫建议就不是必填校验 |
| 价格填 0 不算已交代价格 | 0 元发布须走面议，否则「0」的语义不明（免费？未定？） |
| `negotiable` 不影响校验 | 「可议价」是有标价可谈，「面议」是不标价 —— 两回事；§5.8 只把面议当免填数字的出口 |

**踩到一个 `copyWith` 的通病**：`?? this.x` 写法无法把可空字段改回 `null`，所以「未选分类」这条用例不能从 `complete()` 基线 `copyWith(leafCategoryId: null)` 得到，改用全新 `const PublishFormState(...)` 构造。这不是缺陷而是 `copyWith` 的固有边界，但**测试若不知道这点，会写出一条永远绿的断言**。

**四、认证浮层的叠字缺陷 —— 由 widget 测试抓出，不是看代码看出来的**

`cert-modal` 标题按 §4.4 句式拼「发布此分类需要 X 认证」。而 `RequiredCert.label` 里「企业认证」「车辆认证」**自带「认证」二字**，直接拼得到「发布此分类需要企业认证认证」。

**这条值得单独记的原因**：它在人眼过页面时会被当成手滑而不是逻辑错，评审会说「文案改一下」，于是同一个拼接处将来换个枚举值又叠一次。修法是抽 `String get title` getter 加 `endsWith('认证')` 判断，测试断言两条 —— 正向 `find.text('发布此分类需要企业认证')`，反向 `find.textContaining('认证认证')` 必须为空。**光有正向那条也不够**：它只证明这一个类目对，不阻止别的类目叠字。

**五、反向验证三轮**

| 轮次 | 注入 | 结果 |
|---|---|---|
| A | 删掉 `if (!agreed) return PublishBlocker.agreementUnchecked;` | 2 条报红：「协议未勾时禁用」+「blocker 顺序逐步补全」 |
| B | 模板键 `501`（家政）改成 `502`（维修） | 2 条报红：「二级 501 每个叶子命中同一模板」+「家政价格单位含 ㎡/小时/次」 |
| C | 撤掉认证标题的 `endsWith` 去重 | 1 条报红：「不出现『认证认证』叠字」 |

三轮注入均如期报红并已恢复。注入用 SearchReplace 而非 PowerShell `-replace` —— 后者遇到 Dart 字符串插值 `${...}` 与反引号需要转义，**首次尝试文件根本没变，Grep 复查才发现「报红为零」是因为没注入成功**，差点记成假绿。

**六、两处高敏资质的提前告知**

§5.8 的拦截发生在**提交前**，那时用户已填完整张表单。故把「需资质」提示前置两处：级联选择器的叶子行挂资质小片、发布页分类行挂提示行。**在提交时才告知，等于把人一路带到墙上**。

**七、实测数据与一处测试环境事实**

- 新增 68 项测试：模板 21（键与 §2.4 对数 / 四份字段文案 / 降级 / 认证判定）+ 校验 34（契约常量 / 基线 / 八段逐项挖空 / 价格五分支 / blocker 顺序 / `withCategory` / 需求前缀五档）+ 页面 13（禁用同步 / 八段呈现 / 胶囊切换 / §4.4 浮层四条）；
- **全量 `flutter test` 179 项全绿，`flutter analyze` No issues found!**；
- **`scrollUntilVisible` 是必须的**：表单用 `ListView` 按需构建，默认 800×600 测试视口里第 4 段以后的 widget **根本没被创建**，直接断言得到「找不到组件」—— 那不是缺陷，是没看到。已在测试文件写明，避免后来的人把它当 bug 去改页面结构。

**八、原则 134 在本轮的两处落点**

① `publish_form_state.dart` 的四个契约常量（`kMaxImageCount = 9` / `kDescriptionSuggestMin = 30` / `kDescriptionSuggestMax = 200` / `kDefaultValidDays = 7`）用**字面量**写死并被测试单独一组直接比对；② `publish_template_test.dart` 的期望表右侧写 §5.4.3 表格首列的**中文类目名字面量**（`401: '二手闲置转让'`），注释写明「若改从 `category_tree` 取名字，这条断言就退化成『代码与自己相等』」。


### [74] 2026-09-01 M4-3b AI 确认页 + 完成页 + 完整度三档判定模型（PRD §5.9 / §5.10 / §5.11 / §9.8 / §8 T6-④）

**一、把完整度三档抽成 `domain/publish_completeness.dart` —— 因为 §9.8 自己写明它是唯一判定处**

§9.8 末条原文：「判定口径唯一：本表是 🟢🟡🔴 的唯一判定处，§5.9 确认页冲刺区与 §5.11 补全引导均引用本表」。三条件（必填 100% / 位置到门牌号 / 三级类目精准命中）满 3 → 🟢、满 2 → 🟡、≤1 → 🔴，权重 ×2 / ×1 / ×0.5。

本轮有**四处消费方**：确认页冲刺区、确认页底部档位、完成页完整度卡、完成页权益文案。**各写一遍的后果不是重复，是自相矛盾** —— 会出现「确认页说补完门牌能升 🟢，发完却显示 🟡」，而这种矛盾用户看得见、测试却各自都绿。故 `CompletenessAssessment` 单独承载，四处只读它。

**条件三按「该类目实际最深层级」判定，不硬要求三级**。兜底口径是 §9.8 原文给的：「若该二级类目下运营尚未配置三级类目，则选到二级即视为精准命中（判定按该类目实际最深层级，**不因运营未配置而惩罚用户**）」。故实现为 `leafCategoryById(id) != null` —— 树上存在这个叶子即命中，而不是去数路径深度是否等于 3。

**表单补了 `doorNumber` 手填框**：本期没有高德 Key，若只留「取当前定位门牌」按钮（禁用态），**🟢 档在本期将无人可达** —— 那意味着三档里最重要的一档在验收时看不到，冲刺区、跳档、权益 ×2 全都测不出来。

**二、§5.10 的四步 Scope 做成可验证代码，而不是塞一批假数据**

无 AI 服务端，但 §5.10 的约束本身是**客户端可验证**的：猜测值只准来自「用户已填」或「分类模板」。故落两个函数：`buildLocalGuess`（Step 2 生成）与 `reverseScopeCheck`（Step 4 逆向比对）。

五处刻意的克制，每处都是「不编造」的具体形态：

| 克制点 | 理由 |
|---|---|
| `reverseScopeCheck` **独立成函数**，不内嵌进 `buildLocalGuess` | 若写在内部，它校验的就是自己刚生成的东西 —— **恒过**（原则 134 同族） |
| `neighborMode`（据附近同类）`_hasSource` **一律返回 false** | 本期无此数据源。留一条「看起来能用」的来源，第一条走它的猜测值就是编造 |
| **不从描述里截一段当标题** | 「截哪一段」是模型的判断，不在 scope 内 |
| **只在模板恰好只有一个价格单位时才补单位** | 「元/次」和「元/月」差一个数量级 |
| **未选分类时不猜分类** | 分类决定模板、决定资质拦截、决定推荐池分组，猜错的代价不是文案不准 |

**`FieldGuess.value` 用 `String?` 而非空串表示未猜出**：空串与「猜出来是空」无法区分，而这两者在页面上是「需你补充」与「AI 猜的就是空」两种完全不同的呈现。确认页里用户把值改空，走 `_edited.remove(key)` **撤回修改回到 AI 原值**，而不是存一个空串。

**三、确认页（§5.9）的取舍**

| 取舍 | 理由 |
|---|---|
| 用户修改存在单独的 `_edited` Map，**不写回 `_form`** | 猜测结果与表单字段不是一一对应（模板字段是动态键），反向解析失败会**静默丢掉用户的修改** |
| **五态全做**（解析中/慢/失败/待确认/已确认） | §5.9 是五行表。「解析中」与「失败」两态接入真 AI 后**最常见**；留到接入时再写，等于把最容易出错的两态放在最赶的时候写 |
| 「全部重填」放**右上**而非底部 | 放弃动作要与「确认发布」拉开距离；不消耗配额，实现为 `pop()` 回发布页 |
| 冲刺区**只放门牌号 + 三级类目**，不放必填项 | 必填没齐时「确认发布」本就是禁用的 —— **那不叫冲刺，叫拦截**。已达 🟢 时整块 `SizedBox.shrink()` |
| 配额**只显示上限、不显示剩余** | 客户端显示自己算的剩余数，清一次缓存就会「恢复满额」，**那比不显示更坏** |

**四、完成页（§8 T6-④）的取舍**

| 取舍 | 理由 |
|---|---|
| `form == null` 时按 **🔴 档**兜底 | 拿不到数据就说「完整」，等于把最坏情况显示成最好情况 |
| `missing` **列出全部差项** | 与发布页 `blocker` 只报第一条**刚好相反**：那里是拦截（一次给一个动作），这里是清单（让人一眼看完还差什么） |
| 按钮文案「回去补齐（约 30 秒）」而非「立即补齐」 | 跳回发布页会丢已填内容。**它不保留现场，承诺了就是骗人** |
| 档名只从 `level.label` 取一次 | 同一档名出现两处，改 Token 时会漏一处 |
| 有效期说明放**完成页** | 用户真正需要记住「7 天后会来问你还在不在」的时刻，是刚发完的那一刻 |

**五、临时「AI 帮我补齐」入口 —— 用户裁定，且已登记移除条件**

§5.9 确认页的正路入口是 §6.13 T6-① 首页 FAB「AI 帮我发」→ 弹四模式，而该 FAB **M4 未排**（地图页 Grep 零命中）。若把手动填表单链路强塞进确认页，页面会显示「AI 已识别」这类不真实文案。三个选项交用户裁定，**用户选方案 A**。

`_TempAiEntry` 刻意**不套 `_SectionCard`**（无段序号、无「已填」勾），副文案直写「临时入口：正式入口是首页「AI 帮我发」（待做）」，并在类 doc 与 `_openAiConfirm` 两处写明「FAB 上线后须删除本类与本方法」。**这么做的原因**：下一个打开发布页的人不会知道它是临时的，会以为 §5.4.1 漏写了这一段，进而把它当既成事实继续加码。

**六、修掉一个 analyze 抓不到的真 bug：`join(' > ')` 拼的是对象**

原有两处写 `categoryPathOf(id).join(' > ')` —— `categoryPathOf` 返回 `List<CategoryNode>`，`join` 接受任意类型并调 `toString`，于是**页面上会显示 `Instance of 'CategoryNode'`**。`flutter analyze` 对此零告警（类型上完全合法）。

修法是新增 `categoryPathLabel(int leafId)` 把面包屑拼法**收口成唯一处**，三处调用点全改。**为什么必须收口而不是各处改对**：AI 猜测生成的分类值与 §5.10 Step 4 的逆向比对必须用**同一拼法** —— 分隔符散在两处时，改一处就会让校验把自己刚生成的合法值判成非法。测试里补 `isNot(contains('Instance of'))` 钉死。

**七、实测抓出的三处缺陷（analyze / 看代码都发现不了）**

- **`AppRoutes.myPublish` 不存在** —— 完成页第二出口原指向「我的发布」列表页，`analyze` 报 `undefined_getter`。该页属 §5.11 补全（编辑态）范围、M4 未排、路由表里没有它。改落 `AppRoutes.profile`，注释写明「指向一个不存在的路由会白屏，比少一个出口更糟」；
- **对话框 `TextEditingController` 提前 dispose** —— 报 `A widget which was marked as dirty and is still active was scheduled to be built` + `'attached': is not true`。根因：`showDialog` 关闭有一段**退场动画**，`await` 返回后输入框仍在树上，此刻 dispose 就炸。改用 `TextFormField(initialValue:..., onChanged:...)` 让它自管控制器。**这条只有 widget 测试能抓** —— 手点时动画一闪而过，异常常被当成偶发；
- **测试断言写错类目名** —— 二级类目真源是「二手闲置转让」不是「二手闲置」。断言写的是我记的名字而不是树上的名字，属规则 1.3 同一类失误。

另有一处不是缺陷但必须记：**确认页 widget 测试要把视口放大到 `Size(800, 2400)`**。默认 800×600 里冲刺区与配额说明都在折叠线以下，而 `ListView` 按需构建 —— 本页要断言「补门牌号后底部档位实时变化」，需两处同屏可见。已在测试文件写明，避免后来的人把它当页面缺陷去改结构。

**八、反向验证三轮：12 条失败横跨四层**

| 轮次 | 注入 | 结果 |
|---|---|---|
| A | `_hasSource` 的 `neighborMode => false` 改 `=> true` | **1 条**报红：「声称 neighborMode 一律判非法」 |
| B | `level` 的 `2 => yellow` 改 `1 => yellow` | **6 条**报红：完整度三档两组 + 权重随档位走 + 权益首条 + 补门牌跳档 + 确认页底部「还差几项」 |
| C | 完成页 `form == null` 默认三个 `false` 改三个 `true` | **5 条**报红：完成页档名 / 差项 / 权益 / 🔴 档兜底用例 |

三处均如期报红并已恢复，恢复后全量复跑回到基线。**B 轮的 6 条横跨 domain / 确认页 / 完成页三层**，正是第一节「唯一判定处」想要的效果 —— 判定改错，四处消费方一起报红，而不是某一处偷偷显示成另一档。

**九、原则 134 落点与实测数据**

- 契约常量全部**字面量**写死并被测试单独一组直接比对：`kWeightGreen = 2.0` / `kWeightYellow = 1.0` / `kWeightRed = 0.5` / `kParsingSlowSeconds = 5`；权益首条断言精确到「`×2` 不能写成 `×2.0`」；
- 新增 **77 项测试**：完整度 24（三权重与三档名 / §9.8 三档全组合 / `missing` 顺序 / §8 T6-④ 权益三条 / §5.10 配额 3·3·10·20 与高敏 +10 / `PublishFormState.completeness` 复用 `canSubmit`）+ 猜测 23（四件套齐备 / 已填照搬 / **不猜不编造** / Step 4 逆向比对 8 条）+ 确认页 17 + 完成页 13；
- **全量 `flutter test` 254 项全绿，`flutter analyze` No issues found!**

### [75] 2026-09-01 M4-3c 稿 ↔ 码对数收口：同一处失明换了一条边再犯（PRD §10.1 / §5.8 / §8 T6-④）

**起因**：用户问「UI 设计闭环结束了吗？」。按规则 1.3 不凭记忆，取三份真源逐页对照 —— `docs/PRD.md` §10.1（契约）× `prototype-figma/code.js` 的 `MAIN_SCREENS`（稿）× `lib/features/**`（码）。核出两层结论：① 稿仍差 `privacy-gate` 一页（与条目 [72] 一致，仍挂 `KNOWN_STAGE_GAPS`，去处 M4-4，本轮无变化）；② **新查出**：M4-3b 刚落地的完成页与稿有两处矛盾，而探针**一条不报**。

**一、三份真源有三条边，此前只守了两条**

条目 [72] 的病灶原文是「判据右侧取自被测对象自己，它就只守自洽、不守契约」。本轮暴露它的**第二种形态**：8/31 补的 4 条断言守的是 **PRD ↔ Figma**，而 **Figma ↔ Flutter 这条边从来没有判据**。于是 M4-3b 有意改的两处只改了码、没回改稿，260 项探针全绿。

> **可推广的判断**：三份真源之间有三条边，补齐其中一条不等于闭环。**每加一份真源，要检查的是新增的那几条边，而不是新增的那一份文件。**

**二、两处矛盾性质不同 —— 这是本轮最关键的判断**

用户第一次裁定「改稿对齐代码」。执行中读 PRD 才发现**第二处的前提不成立**，故中途二次回报并请用户重裁：

| 矛盾点 | PRD 有无规定 | 谁偏了 | 处置 |
|---|---|---|---|
| 「立即补齐」→「回去补齐」 | **无**（PRD 未定此文案） | 稿码分歧 | 改稿对齐代码 |
| 「我的发布」→「去「我的」」 | **§5.8 明文**（`PRD.md:899`「发布成功跳：默认跳"我的发布"」） | **代码偏了** | 稿不动，码侧登记临时降级 |

若照「改稿对齐代码」一律执行，等于**让 Figma 稿去违背 PRD** —— 那会把「M4 未排页导致的临时妥协」洗成看起来正式的设计决定，下一个人会以为 PRD 改过口径。用户改选**「代码侧登记临时降级」**。

> **可推广的判断**：「稿码不一致」不是一种问题，而是两种。**先问 PRD 有没有规定，再决定改哪一侧** —— 没规定的按实现改稿，有规定的说明是码偏了，此时改稿是在销毁证据。
> **同样重要**：发现用户裁定的前提有误时应当**中途回报而非硬执行**（用户认可了这次二次确认）。

**三、第一处：改稿对齐代码，并写明「立即」为何是空头承诺**

`code.js` 补齐按钮改为 `'回去补齐（约 30 秒）'`，注释四行写清理由：该按钮跳回发布页，而发布页**不保留已填内容** —— §5.11 那个能带原内容进编辑态的补全页属「我的发布」范围，本期未做。**「立即」在承诺一件它做不到的事**，用户点进去看到空表单，代价比按钮文案平淡得多。

**四、第二处：码侧登记临时降级，与 `_TempAiEntry` 同一处置**

`publish_success_screen.dart` 第二出口注释重写为：明示「此处偏离**不是**设计决定」+ PRD §5.8 依据 + 偏离原因（`my-publish-screen` 属 §8.3.1、M4 未排、路由表无此项，指向不存在的路由会白屏）+ **移除条件**（该页落地后改回并从探针 `KNOWN_BTN_DOWNGRADES` 删备案）。稿子保持 `button('我的发布', 'ghost', 0)` 与 `FLOW_LINKS` 那条边不动 —— 它忠于 PRD。

**五、补最小一条 Figma ↔ Flutter 对数守门（探针 260 → 262）**

判据两侧**各取自己的真源**，不抄副本：稿侧取 `btn/variant/label` 节点名（`button()` 就是这么命名的，`code.js:1987`），码侧 `fs.readFileSync` 读 Dart 源找字面量。

三处克制，都在注释里写了理由：

- **只守「关键按钮文案」，不做全量元素对数** —— 全量对数需要一套稿↔码元素映射表，而那张表本身会成为**第三份要维护的副本**；判据一旦比被判对象更难维护，它就会在某次赶工里被放宽（原则 133）。按钮文案是稿码分歧里**用户唯一直接读到**的那部分，且两侧都能机械取值；
- **`wiredPages` 只列已实现页**（`buildPublishSuccess` / `buildAiConfirm`）—— 未实现页的码侧没有真源可取，硬列进来只能靠豁免，那等于**自造一批永绿项**；
- **码侧不要求文案长在同一个 `Text()` 里**，只要求字面量在该页源码出现过 —— 判「稿上写的话码里有没有」，不判它长在哪个 widget 上（后者属实现细节，管到那一层会逼人改判据）。

`KNOWN_BTN_DOWNGRADES` 与 `KNOWN_STAGE_GAPS` 同一取向：承认偏差，并让**备案外的任何新偏差立刻报红**；value 必须写明「稿侧原文案 + 偏离理由 + 何时改回」。另配**反向守门**「备案项必须恰好等于实际降级」—— 稿侧改回后备案就成永绿豁免，会替将来真正的同名偏差挡枪（原则 133 同族）。

**六、实测抓出的两处自身失误（都属规则 1.3 同类）**

- **`M[fn] is not a function`** —— `buildPublishSuccess` 不在 `probe-layout-offline.js` 的 `new Function` 导出清单里（`buildAiConfirm` 在）。这条断言**必须真跑完成页**才能取到 `btn/` 节点名，源码正则取不到 Instance 的实际命名（`button()` 拼的是 `btn/variant/label`）。已补进导出清单并注明；
- **`node probe-tokens.js` → MODULE_NOT_FOUND** —— 文件名是我**记的**而非读来的。`Glob probe*.js` 只有 `probe-layout-offline.js`；Token 探针实为 **`probe-token-vars.py`**（Python 脚本）。另记一条工具用法：`Select-Object -Last N` 会吞掉 FAIL 行，应改用 `Select-String -Pattern "FAIL|通过 \d+ / 失败"` 精确取关键行。

**七、反向验证三轮：三次各 1 条报红**

| 轮次 | 注入 | 结果 |
|---|---|---|
| A | 稿侧改回「立即补齐」 | FAIL「publish-success-screen 稿有码无：「立即补齐（约 30 秒）」」 |
| B | 备案 key 改为 `我的发布XX` | FAIL「稿侧已无此按钮但仍挂在备案里：我的发布XX」 |
| C | **码侧**改「马上补齐」 | FAIL「稿有码无：「回去补齐（约 30 秒）」」 |

三轮均 261/1 并已恢复。**C 轮验的正是原始病灶方向**（码改了稿没改），即这条断言若早存在，M4-3b 当天就会报红。

**八、实测数据**

- 离线探针 **262 / 失败 0**（原 260，新增 2 条）；Token 探针 **125 / 失败 0**；
- `flutter test` **254 / 254**（本轮只改注释与稿，未新增测试）；`flutter analyze` **No issues found!**；
- **UI 闭环仍未结束的唯一稿缺口：`privacy-gate`**（🔴 上架驳回红线），备案在 M4-4。
  > 📌 **该缺口已于同日 M4-3d 补齐（条目 [76]）**，此处保留当时结论以存时序，勿据此认为稿仍缺页。

### [76] 2026-09-01 M4-3d 补 `privacy-gate` Figma 稿（PRD §6.5.1 / §10.1）

**起因**：用户问「UI 设计闭环结束了吗？」。按规则 1.3 不凭记忆翻三份真源，稿仍差 `privacy-gate` 一页（条目 [72] 核出、挂 `KNOWN_STAGE_GAPS`、去处 M4-4）。用户指令「本轮就补」—— 但去读 `privacy_gate_screen.dart` 与 `code.js` 的 `FLOW_LINKS`/`screen()` 机制后，发现 8/31 那条 M4-4 子待办有两处前提不成立。

**一、读真源后修正 8/31 计划的两处前提**

| 原计划（8/31 写） | 读真源后的实际 | 依据 |
|---|---|---|
| `COVERAGE_STATES` 补**两档**变体（同意态 / 受限态） | 只补**一档**（受限态） | 同意态**就是主态本身** —— `privacy_gate_screen.dart` 的 `_AgreementView` 是默认视图，`_DeclinedView` 才是分支。单画「同意态」= 把主态抄一遍，与条目 [70] 记的「重复稿」同一个错 |
| `FLOW_LINKS` 补**三条**边 | **改 1 条 + 补 1 条**，第三条不进表 | ① `splash → gate` 与既有 `['splash-screen','_splash-tap','login-screen']` 冲突：第二列是节点精确名，`_splash-tap` 只有一个，`findClickable` 命中唯一节点、`link()` 用 `setReactionsAsync` 覆盖写，一个触发点连不了两个目标。按 §6.5.1 协议门早于登录应**改**该边目标为 `privacy-gate`；② 第三条 `gate 不同意 → 受限态` 目标是变体画框，而探针有断言「`FLOW_LINKS` 全部源/目标画框均在 `MAIN_SCREENS` 内」，变体不在其中，**故不进表**，受限态靠并列出图对照 |

**所以本轮是「改 1 条 + 补 1 条」，不是「补 3 条」**。若照原计划执行，会产出重复稿（同意态变体抄主态）并踩 `FLOW_LINKS` 静默覆盖（第二条边无声覆盖第一条）。规则 4.2「先改计划再动代码」拦下的一次。

**二、六项落地清单**

| 项 | 内容 | 状态 |
|---|---|---|
| ① 主态构造器 | `buildPrivacyGate()`：标题 + 四段正文（引导句 / 三条收集清单 / 高德 SDK 明示 / 不同意的后果）+ 「同意并继续」+ 「不同意」+ 八行红线标注 | ✅ |
| ② 受限态变体 | `buildPrivacyDeclined()`：PRD §6.5.1「受限态不是空白页」—— 产品价值说明 + 「重新阅读协议」主按钮，未同意期间四不得 | ✅ |
| ③ 三表登记 | `MAIN_SCREENS` 18→19、`CORE_PAGES` 14→16、`COVERAGE_STATES` 4→5 项 | ✅ |
| ④ FLOW_LINKS | 改 1 条（`splash-tap` 目标 `login` → `gate`）+ 补 1 条（`gate 同意` → `login`），23→24 条 | ✅ |
| ⑤ 探针五处 | 删 `KNOWN_STAGE_GAPS` 唯一项、`wiredPages` +2 行（主态与受限态各一行）、`tailPages` +1 行、`new Function` 导出 +2、`ui.html` 批次 3 页名清单同步 | ✅ |
| ⑥ 反向验证四轮 | 刻意造错全部报红后恢复 —— 证明受限态那行 `wiredPages` 真的在守 | ✅ |

**三、不做的三项及其依据**

| 不做项 | 依据 |
|---|---|
| 协议正文全文上稿 | PRD §6.5.1 明写「协议文本走在线 URL，不内嵌写死」，稿上放全文等于把「文本可远端更新」这条设计判断画反 |
| 《用户协议》《隐私政策》可点入口 | 码侧要到 M4-4 政策上线后才有真实 URL，现在画可点链接是稿超前于码，正是 M4-3c 刚立起来的稿码对数要防的事 |
| `gate → 受限态` 连线 | 目标为变体画框，不在 `MAIN_SCREENS` 内，探针断言会报红 |

**四、实测数据**

- 离线探针 **262 / 0**（含新纳入的稿码对数 2 页 → 4 页、贴底 6 页 → 7 页）；Token 探针 **125 / 0**；`flutter analyze` **No issues found!**（19.3s）
- **关键 PASS 行**：`PRD §10.1 每一页都在 MAIN_SCREENS 内 [PRD 19 页 / 稿 19 页，缺口 0 项（零缺口）]`、`已备案缺口清单无过期项 [0 项备案全部仍为真实缺口]`、`Figma 稿关键按钮文案…[4 页按钮文案全部对上（备案降级 1 项）]`、`7 页尾部元素贴底 [7 页全部命中]`、`批次 3 core 画框未被内容顶变形 [41 个画框，全部合规]`

**五、反向验证四轮**

| 轮次 | 注入 | 失败条数 | 关键报错 |
|---|---|---|---|
| A | `KNOWN_STAGE_GAPS` 塞回 `privacy-gate` | 1/262 | 已补稿但仍挂在豁免清单里 |
| B | 从 `MAIN_SCREENS` 删 `privacy-gate` | 4/262 | `screen()` 当场抛错 + FLOW_LINKS 两断言 + PRD 断言 |
| C | 受限态按钮文案改为「重新阅读协议（反向验证）」 | 1/262 | 稿有码无，证明受限态那行 `wiredPages` 真的在守 |
| D | `FLOW_LINKS` 触发点名改「同意并继续X」 | 2/262 | 自报 1 条跳过 + FLOW_LINKS 断言命中 0 |

四轮全部恢复，恢复后全量复跑回到基线 262/0。

**六、与 M3 里程碑的关系**

条目 [72] 末那条备注「此『18 主态』口径缺 `privacy-gate` 一页……已备案于 M4-4 补稿」—— 现已于 M4-3d 完成，备注已同步更新（见上）。M3 验收当时按当时清单是过的，补稿是 M4 范围内的事，不追改里程碑结论。

⚠️ **遗留一项**：新增的两个画框（主态 + 受限态）**尚未实机出图** —— M3 的 r67–r74 渲染图轮次在补稿之前，本轮只跑了离线探针。**离线探针管的是结构与文案，管不了观感**（条目 [70] 的四页留白、条目 [74] 的六处缺陷都是出图才看见的）。下次在 Figma 桌面版跑插件时按 1→2→3→4→5 整体重跑并补这两张图，验收看形：主态四段正文不溢出、两键贴底；受限态居中、不是空白页。

### [77] 2026-09-01 M4-3e 设计系统交付形态收口：从「规格散在代码里」到「有库形态、有规范文档、有守门」（PRD §1 / §1.7 / §1.8）

**起因**：用户问「咱们要输出 UI 设计稿（Figma）、《设计系统/组件规范》，当前是否具备」。读四处真源后判定 —— **内容基本具备，交付形态不合格**：Figma 样式面板是空的（设计师点不到「h1 样式」，只能点到 `size/h1` 这个数字变量）、20 个 master 是扁平散件而非带 `variant` 的 Component Set、规范只以「PRD §1 的 351 行 + `code.js` 注释」形式存在。分五段执行（e1–e2 第一层 / e3–e4 第二层 / e5 第三层 / e6 收口），**每段各自验证后单独提交**。

**一、五次提交与各自的验证数**

| 段 | 提交 | 内容 | 离线探针 |
|---|---|---|---|
| e1+e2 | `1946d94` | 第一层设计裁决：`TAG_SPECS` 四族 + `DOT_SIZES` + `card()` 三槽位 + `FIELD_SPECS` 两套五态 + `emptyState()` | 270 / 0 |
| e3 | `70ff9a2` | 6 档 Text Style + 26 档 Paint Style 注册 | 276 / 0 |
| e4 | `50f6867` | 20 个 master 合成 3 组 Component Set | 286 / 0 |
| e5 | `c4d4b71` | `export-spec-doc.js` 生成器 + 规范文档产物 + 单向出口断言 11 条 | 297 / 0 |
| e6 | 本段 | 全量验证 + 修一处三轮未被发现的红 + 回写本条目 | 297 / 0 |

**二、三次「读真源后发现计划前提有误」，均中途回报并经用户裁定后才动代码（规则 4.2）**

这是本轮最值得记的部分 —— 三次都是**若硬按计划执行就会产出错误结果**，且错误在事后极难发现：

| # | 计划原文 | 读真源后的实际 | 裁定 |
|---|---|---|---|
| ① | 标签「41 处写法互不相同、无众数」 | **四族性质不同的东西被混在一起数**：A 可点胶囊 4 处（真不一致，需归一）/ B 静态标记 1 处（样本量 1，只能立规格）/ C 浮层提示条 2 处（**彼此已完全一致**，只需登记）/ D 被我误算成标签的按钮与容器 4 处。我先前举的「圆角 `full` 9 处 vs `sm` 3 处」这条**证据不成立** —— `sm` 那 3 处里 2 处根本不是标签 | 仍照 11 处执行，但 `TAG_SPECS` **按四族分栏承载**，不把语义不同的四族硬塞进同一条胶囊规格 |
| ② | 第三层「只读上游、零改画布」 | **对比度这一项没有真源可读**：`code.js` 里既无算法也无组合清单，26 行实测表只存在于 PRD §1.4.2 的**手写表**里。故「改一个色」与「回算它涉及的所有组合」之间没有任何机械联系 —— Accent 由 `#FF8A3D` 压深至 `#B4531A` 那次就是先漏算、实机核查才发现白字压其上仅 2.345:1 | 补 `CONTRAST_PAIRS`（34 组），**表内刻意零比值** |
| ③ | 同上 | **卡片三态也没有真源**：PRD §1.4.7 `:343` 明写三态，而 `card()` 只实现正常态，选中态与下架态**全稿零实现** | 补 `CARD_STATES` 登记表 + 如实标「本轮未出稿」 |

②③ 若不回报而由生成器自己手写，就等于在产物里放一份**无人守门的副本**（原则㊾）—— 那是本轮全部工作要消灭的东西，却会被顺手造出来一份。

**三、`CONTRAST_PAIRS` 为什么刻意不写比值（本轮最关键的一处设计取舍）**

表只登记 `fg` / `bg`（role 名）/ `threshold` / `verdict` / `note`，**比值一律由生成器现算**。若把 `4.62` 这类数字抄进表里，改色后数字不会变，这张表就成了第二份会静默脱钩的副本。同理 `hexOfRole()` 从 `paintOf()` 里提取出来共用 —— 生成器若自己再写一遍 role→hex 分支，`-deep` 后缀那个坑（查 `CATEGORY_COLORS['cat-house-deep']` 拿到 `undefined` 后静默回退纯黑）就有了第三份实现。

**`hexOfRole` 的关键取舍**：查不到时返回 `undefined`，**不回退黑色** —— 回退策略交调用方决定：画布构造要「查不到用黑色继续画」，规范生成器要「查不到当场抛」。若函数自己回退黑色，生成器就永远抛不出来，写错一个 role 名只表现为文档里多一行 `21.00:1` 的**假达标**。

**四、交叉验证：34 个现算比值与 PRD §1.4.2 手写表逐行完全一致**

`14.68 / 14.03 / 13.21 / 4.83 / 4.62 / 4.91 / 4.42 / 6.70 / 7.44 / 4.35 / 6.47 / 5.02 / 2.28 / 2.15 / 3.76 / 1.24 / 1.18 / 1.11 / 2.29 / 2.54 / 2.05 / 5.01 / 3.68 / 4.23 / 2.80 / 3.53 / 4.85 / 4.87 / 4.83 / 4.82 / 4.81` —— 全部对上。这是很强的双向验证：说明 PRD 那张手写表**当年算得对**，且生成器算法与之同口径。探针另用自己的 `lumOf` 再算一遍去核产物（全项目唯一一处「同一个量由两套独立实现各算一遍」），任一侧笔误立刻现形。

34 组 vs PRD 的「26 行」：PRD 把五个 `-deep`、三档圆点等合并计行，覆盖一致但本表更细，产物内已说明。

**五、e6 查出一条红，且它已经红了三轮（本轮唯一的过程失误，如实登记）**

`probe-token-vars.py` 的 `field() 开放了 width 参数` 一条在 e6 首次跑 Token 探针时报红（124/1）。追因：**不是 e5 引入的，是 e1+e2（`1946d94`）给 `field()` 补第四参 `opt` 时打断的** —— 判据写的是 `"function field(label, placeholder, width)" in code`，钉死了整条签名，加任何参数都断。

**为什么挂了三轮没被发现**：e2e / e3 / e4 三段我都只跑了离线探针，**没跑 Token 探针** —— 而这两个探针守的维度不同（离线管结构与布局，Token 管 Variables 与 Dart 产物一致性），改 `code.js` 的顶层真源表本该两个都跑。

两处修正：
- 判据改为 `re.search(r"function field\(label, placeholder, width\b", code)` —— 只核前三个形参的顺序与名字（这才是它真想守的：`width` 仍在、login 那 16px 溢出的修法没被回退），往后加参数不再误报，删掉或改名 `width` 照旧报红。已反向验证：把 `width` 删掉 → 当场报红；
- 扫了 `probe-token-vars.py` 全文，**已无同类「钉全签名」的脆判据**。

**教训**：判据脆不脆，看的是「它会不会被无关改动打断」。钉全签名等于把「函数参数表从此不许动」也一并断言了 —— 而那不是任何人想守的规格。

**六、e5 的两处实现手法（复用价值较高，记下来）**

- **20 条组件契约用 Proxy 收取，不手抄**：`describeComponents(cache)` 的 `descs` 表本身就是从真源现算的。生成器传一个 Proxy 当 `cache`，让任何键都命中一个假 COMPONENT 节点，用 `set description` 拦截器把 20 条收回来 —— 无需预知键名、无需 mock 整个 Figma 文档，也不构成手抄。并加 `n !== collected.length` 一致性校验；
- **刻意用 eval 取值而非正则**：本文档大量取值是**派生的** —— `TAG_SPECS.chip.padV` 写的是 `SPACING.xs`、`BUTTON_SPECS.primary.radius` 写的是 `RADIUS.md`，正则拿到的是这两个字符串本身而非 `4` 和 `8`。沿用 `export-dart-tokens.js` 已验证的「figma mock + `new Function` 求值」模式；
- **生成器的落盘改到 `require.main === module` 之后**：探针要 `require` 它拿 `render()` 现算一份期望文本去比对产物，若落盘发生在模块求值期，探针一 `require` 就把产物改写掉，「产物与真源同步」那条断言验的就是自己刚写下的东西，**永远绿**。

**七、实测数据**

- 离线探针 **297 / 0**（M4-3d 基线 262 → 第一层 270 → e3 276 → e4 286 → e5 297，本轮共新增 35 条）
- Token 探针 **125 / 0**（修掉上述那条红后恢复基线）
- `flutter test` **254 / 254**（本轮零 Dart 改动）；`flutter analyze` **No issues found!**
- 产物计数自报：`TOKEN 43 + CONTRAST 34 + MOTION 6`，`docs/设计系统与组件规范.md` 共 11 章 22540 字符

**八、反向验证（e5 三轮 + e6 一轮，全部恢复）**

| 轮次 | 注入 | 结果 |
|---|---|---|
| A | `text-secondary` 由 `#6B7280` 调浅至 `#8B93A0` | 3 条红：判定与实测打架（判 pass 实测 3.10）、算值对不上、产物脱钩 —— 即「改了色忘回算」当场现形 |
| B | 手改产物一处措辞（「最常见组合」→「最常用组合」） | 仅同步那条红，鉴别力到单字符 |
| C | 全部 `ban` 档放宽成 `pass` | 反向断言按增量抓出 8 档 |
| D | 删掉 `field()` 的 `width` 形参 | Token 探针那条修好的判据当场报红 |

**A 轮暴露出反向断言本身的一处缺陷并已修**：C 轮判据原写「冲突数 == ban 档数」，真源自身出现别的不自洽时会连带变红，把「反向断言有没有鉴别力」与「真源当前是否自洽」混成一条。已改为取**增量**。

**九、不做的四项及其依据**

| 不做项 | 依据 |
|---|---|
| Pin 补全 30 种组合（现 10/30） | 选中态与完整度角标态属**结构差异**，而 Figma 禁止对 Instance 增删子节点 —— API 限制改不了。规范里如实声明「这两态不走组件、实现侧自行构造」 |
| 暗色模式 | PRD §1.7 明写本期不做 |
| 动效进 Variables | `createVariable` 只收 COLOR/FLOAT/STRING/BOOLEAN，无时间与缓动类型（条目 [48] 已查明），动效仍留 annotation |
| 多行输入（textarea） | 稿内零实现，补它属改产品形态而非补规范 |

**十、遗留项（已在产物 §11「已知缺口」逐条登记，共 8 行）**

① 卡片选中态 / 下架态**稿内零实现**（规格已定，见 `CARD_STATES`）；② 输入框 focus / error / disabled 三态属**本轮新拟规格**而非对现状的归纳，稿内亦零实现；③ 画布既有文本节点**尚未挂 `textStyleId`**（Style 已注册，但画布上的文本仍是直接写属性）；④ `_sheet-reset` / `_sheet-confirm` 是手搓按钮、**绕过 `BUTTON_SPECS` 真源表** —— 改走真源表须给它补一档「满宽 grow + full 圆角」，master 数由 20 变 22，与 Component Set 改动叠在一起风险不该合并，故列遗留。

⚠️ **本轮改动尚未实机出图，且这次的代价比 M4-3d 那次大**：Component Set 合成**牵动全画布 44 框的 Instance 引用**，下次在 Figma 桌面版须按 1→2→3→4→5 **整体重跑**（不可只跑 2/3/4，那样 master 缺失会退化成散 Frame），**已有 121 张渲染图可能有一部分要重出**。离线探针管结构与契约，管不了「合成后 Instance 在画布上还长得对不对」。

⚠️ **优先级如实回报（延续 e0 的判断，未变）**：这两项交付物对 9/30 内测包与商店上架**都不是阻塞项**。而 **P1–P5 行政项当前仍零进度**（软著 8/25 就该提，现提最早 11 月末下证）、M4-4 / M4-5 未动。此项系用户明确裁定要做，故执行，但不改变 P1–P5 的紧急性。


### [78] 2026-09-04 Batch1 数据模型结构性修正：数据库设计文档 + DDL + PRD §13 回写

**起因**：用户要求基于 PRD §13 与架构 §5 经 `ce-ideate → ce-brainstorm → ce-plan` 流程产出《数据库设计文档》、ER 图、数据字典、建表/迁移脚本。经四轮 `AskUserQuestion` 裁定，确认 6 项结构性修正（敏感字段盲索引双列 + 手机号归一化 + 状态归因 + 两个 STORED 生成列 + 写守卫分流 + 整数微度域 grid_id）与 4 项口径定案（表范围扩为 15 张 + 分类树版本号落 `system_config` + 7 张零字段表字段定义 + `user.phone` → `phone_mask`）。

**一、四件交付物与各自真源**

| 交付物 | 路径 | 真源 |
|---|---|---|
| 计划契约（implementation-ready） | `docs/plans/2026-09-04-1630-refactor-batch1-data-model-corrections-plan.md` | 6 项 KTD + 27 条 R + U1-U8 实施单元 + Verification + DoD |
| DDL 迁移脚本（业务库） | `docs/database/ddl/V1__init_schema.sql` | Flyway 命名 `V1`，15 张业务表 + 索引 + 初始数据 + STORED 生成列 |
| DDL 迁移脚本（埋点库） | `docs/database/ddl/V1__init_track_schema.sql` | Flyway 命名 `V1`，架构 R14 独立 database，月表 + 兜底表 |
| 数据库设计文档 | `docs/database/数据库设计文档.md` | ER 图 + 15 张表数据字典 + 索引设计 + 9 条保留策略矩阵 + 6 项关键设计决策 + 与原 PRD/架构的 10 条修正项对照 |
| PRD §13 回写 | `docs/PRD.md:2306-2587` | §13.1 实体清单增 `system_config` + §13.2 全 15 张表字段字典 + §13.3 索引要求 |

**二、6 项 KTD（结构性修正）**

| KTD | 内容 | 覆盖 R |
|---|---|---|
| KTD1 | `l2_category_id` / `completeness_level` 用 `GENERATED ALWAYS AS (...) STORED`，可建索引、不漂移 | R9, R10 |
| KTD2 | HMAC-SHA256 + pepper 在 Java 应用层算，MySQL 只存 `BINARY(32)`；AEAD 仅用于解密展示；`key_version` 支持灰度轮换 | R1, R3, R4 |
| KTD3 | 手机号彻底归一化到 `user_identity`，`user` 表只留 `phone_mask` 明文掩码 | R1, R2, R5 |
| KTD4 | 写守卫按写入来源分流：用户意图写守 `version` 返 `40903`；系统派生写守 `status IN (合法前驱集)` 静默跳过 | R13, R14 |
| KTD5 | `grid_id` 不用 DDL 生成列（避免 SQL 与 Dart 两套实现），改整数微度域实现加固 | R16 |
| KTD6 | `completeness_level` 以 API 映射为档位序真源，落库即 `tinyint`（0=🔴/1=🟡/2=🟢） | R10 |

**三、PRD §13.2 全 15 张表字段字典回写**

| 表 | 状态 | 关键修正 |
|---|---|---|
| `user` | ✅ 已回写 | `phone` → `phone_mask varchar(20)`；增 `key_version` |
| `user_identity` | ✅ 新造 | `identity_value` 拆为 `identity_hash`(BINARY 32) + `identity_value_enc`(VARBINARY) + `key_version` |
| `post` | ✅ 已回写 | 增 `status_reason`/`status_changed_at`/`template_version`/`version`/`key_version`；`l2_category_id`/`completeness_level` 改 STORED 生成列 |
| `contact_event` | （未改）| 红线：禁增 `contacted_success`/`deal_done` |
| `publish_memory` | （未改）| — |
| `category` | ✅ 已回写 | 删除 `version` 列，移至 `system_config` |
| `report` | ✅ 已回写 | 补 `reported_user_id`/`evidence`/`status`/`handled_at`/`handler_id` |
| `template` | ✅ 本轮新造 | 5 字段：`id`/`leaf_category_id`(UK)/`fields`(json)/`template_version`/`updated_at` |
| `post_media` | ✅ 本轮新造 | 8 字段：表名沿用 PRD 不用架构 §5.1 的 `media` |
| `favorite` | ✅ 本轮新造 | 5 字段：唯一性由物理唯一索引 `uk_user_post(user_id, post_id)` 保证（评审改） |
| `cert` | ✅ 本轮新造 | 11 字段：与 `user.realname_status` 职责边界明确 |
| `device` | ✅ 本轮新造 | 6 字段：`fingerprint` 不可信只提高刷量成本 |
| `notification` | ✅ 本轮新造 | 9 字段：三 Tab 按 `type` 分流 |
| `audit_log` | ✅ 本轮新造 | 10 字段：PRD §9.10.1 七项审计字段落地 |
| `system_config` | ✅ 本轮新建 | 6 字段：承载 `category_tree_version` 等系统级 key-value 配置 |

**四、4 项关键边界（不重复 PRD/架构已有内容）**

1. 不写外键约束（架构 §5.1.1：分库分表演进时是负担，读路径信任冗余列、写路径由 service 层同事务维护）
2. `post_media` 表名沿用 PRD 不用架构 §5.1 的 `media`（PRD 是产品口径真源）
3. 三级类目编号即不变量，已发布不得重排（STORED 生成列 `l2_category_id` 派生的前提）
4. Hard Filter 索引 `(city_id, l2_category_id, type, status, expire_at)` 与 `city_id` 列归属待 Batch2 定，Batch1 不建

**五、遗留项（已登记在设计文档 §8）**

`docs/database/数据库设计文档.md` §8 的 7 条 PRD 回写 checklist 已全部完成；本设计文档与 PRD §13.2 全 15 张表字段字典完全对齐。

**六、`ce-doc-review` 评审结论（2026-09-04 同日执行）**

6 个 persona 并行评审计划契约，5 完成 / 1 失败（`scope-guardian` 子代理工具不可用，本轮无该视角覆盖，已在 Coverage 表标记）。共产出 4 项 Apply + 9 项确认修正 + 6 项决策，全部落地。

| 类别 | 结论 |
|---|---|
| Apply（4） | Q1 引用「第 5 条」→「第 6 条」；档位真源从 §13.2 改 §12.3（PRD:2175）；⚪ 统一为 🔴；Preservation Note「7 张零字段表」→「8 张表」 |
| 密钥配置 | `app.crypto.hmac-pepper` 字符串 → `hmac-peppers` **列表**（按 `key_version` 索引）；生产密钥只经环境变量注入，dev/prod 必须不同值 |
| AEAD 加固 | 新增 **AAD 绑定**：`user_identity` 绑 `user_id + identity_type`、`real_name_enc` 绑 `user_id`、`contact_value_enc` 绑 `post_id`，堵住密文跨行搬运 |
| 密钥泄露预案 | AEAD 主密钥泄露可逐批重加密、业务无感；**pepper 泄露须全量重算盲索引 + 重建唯一索引 + 停写窗口**。Batch1 不提供在线吊销能力 |
| 收藏并发 | 「service 层显式查重」→ **物理唯一索引 `uk_user_post` + `ON DUPLICATE KEY UPDATE deleted_at = NULL`**，由 DB 保证幂等（已同步 DDL / 设计文档 / PRD:2494） |
| 审计写入 | `AuditLogWriter` 统一序列化并按白名单剔除 `phone`/`phone_mask`/`real_name`/`id_card`/`contact_value` 及一切 `_enc`/`_hash` 结尾列 |
| 埋点落地 | R8/R18 补入 U1：DDL 同脚本建 `track_event_202609`（含 4 个维度快照列）与 `track_event_fallback` 兜底表，两者**不计入 15 张表口径** |
| 状态机 | 补第五条系统派生写路径 `active → hidden`（24h 未实名自动隐藏，PRD:2392）；`hidden` 仍是 `audit_takedown` 合法前驱 |
| 成功标准 | 由「定时任务 40903 计数为 0」收窄为「**系统派生写路径**产生的 40903 为 0；用户意图写路径的乐观锁冲突属预期，不计入」 |
| 保留策略门禁 | PRD §13.4 与设计文档 §5 表格均新增「保留策略键」列（9 个英文键），作为 `RetentionRuleCoverageTest` 的双向比对键 |
| 演进代价 | 类目编号只允许**新增 + 旧编号置 `forbidden` 并存**，真需重排须生成列退回普通列 + 重建表 + 回刷 + 重建 `idx_pins_cover`；`completeness_level` 三条件硬编码，新增第四条件须 ALTER 生成列（重建表）+ 同步 PRD §9.8 |

**七、掩码一致性覆盖三条写路径**

`user.phone_mask` 与 `user_identity` 的一致性须在注册 / 换绑 / 注销三条路径上全部成立：换绑是「删旧身份行 + 插新身份行 + 改写掩码」三步同事务；注销时须在同事务内清空 `phone_mask`，不可只删身份行。

**八、工程验证：从"纸面正确"到"实际可执行"（2026-09-05）**

用户追问"数据库设计就完成了吗？"，据此如实区分已完成与未完成，随后闭合了前 4 项：DDL 从未在真实 MySQL 执行过、覆盖索引未做 EXPLAIN 实测、脚本不在 Flyway 扫描路径、埋点表与业务表混在同一脚本违反架构 R14。

| 环节 | 动作 | 结果 |
|---|---|---|
| 分库 | 按架构 R14 拆出 `V1__init_track_schema.sql` | 埋点两表落独立 database |
| 脚本落位 | 为跑迁移曾把脚本移进 `server/src/main/resources/db/migration{,_track}/` 并建最小 Maven 工程 | **两者均已删除**；DDL 现归拢在 `docs/database/ddl/`，验证脚本在 `docs/database/scripts/` |
| 迁移跑通 | `mvn flyway:migrate` 与 `mvn -Ptrack flyway:migrate` | 两库均 `Successfully applied 1 migration`；工程虽已删，**Flyway 可执行性已验证的结论仍成立** |
| 结构等价 | Flyway 建库与手工执行 DDL 的库做列定义/索引差集 | 两个差集均为空 |
| 行为断言 | 7 项（生成列不可写/计算、盲索引唯一、收藏幂等、写守卫分流、埋点快照列、兜底表一致） | 全部通过 |
| R15 性能 | 50 万行长尾分布造数 + `EXPLAIN ANALYZE` | `Covering index range scan`，2322 行 p95 = 1.37ms（**2026-09-06 用响应契约的真实 SELECT 列表复测，见第十五节**） |

**九、实测抓出的一处缺陷（看代码发现不了）**

`post.completeness_level` 的列注释写成 `0=🔴/1=🟡/2=🟢`。DDL 文本本身合法、建表也不报错，但 `information_schema.COLUMN_COMMENT` 是 utf8mb3，承载不了 4 字节 emoji——迁移时抛 `Error 1300: Cannot convert string from utf8mb4 to utf8mb3`，注释落库变成 `0=?/1=?/2=?`。只有真正执行迁移并回查元数据才能发现。已改为文字 `0=红/1=黄/2=绿`，全库扫描确认无残留 emoji。

**十、造数分布的教训**

首轮 EXPLAIN 用 `grid_id = CONCAT(n%1000, '_', n%997)` 造数，把 50 万帖子打散到近百万个网格组合，热点组合只有 1 行——EXPLAIN 虽显示命中覆盖索引，但 `rows=1` 的耗时不能证明任何性能结论。改成 70% 集中在 5 个热点网格后，单组合达 2322 行，p95 = 1.37ms 才是有效证据。**造数分布不真实的性能测试，等于没测。**

**十一、database 定名与容量估算（2026-09-05 补）**

用户指出"现在是数据库设计阶段，你是在开发代码吗"，纠正了我的越界（建 Maven 工程骨架、把 DDL 移进 `server/src/main/resources/`）。停止编码后补齐两处纯设计缺口：

**database 定名**：查证发现上游三份文档（架构 §5、可观测性 :151、PRD §13）**只规定「两个 database、同一 MySQL 实例」的拓扑，从未定义库名**。我此前在验证环境里用的 `s2s` / `s2s_track` 是自己起的、未经确认。经用户确认后写入设计文档 §1.4，同时明确三条约束：分库目的是 buffer pool 不互相拖累而非容灾；`audit_log` 必须归业务库（180 天合规下限不能被埋点库 90 天清理误伤）；禁止跨库 JOIN（这正是 R8 设维度快照列的原因）。

**容量估算**：BRD/PRD/架构均无 DAU 与发帖量输入（BRD §7.1 明确 Batch1 冷启动不考核），故不做正向预测，改为**反推承载上限**。

| 项 | 实测/推算 |
|---|---|
| `post` 行宽 | 824B 数据 + 248B 索引 = **1072B/行** |
| 埋点行宽 | 215B + 163B = **379B/行** |
| 40G 盘 → MySQL 可用 | 16G，`post` 约 960 万行、埋点约 1800 万行 |
| 真正的瓶颈 | **不是磁盘，是 256MB buffer_pool** |

> 上表索引行宽已按 2026-09-06 复核后的 10 列 `idx_pins_cover` 重测（原 205B/1030B/990 万行是 8 列索引下的数据），见第十五节。

**十二、一处被推翻的旧结论**

条目 [78] 第八节记录过"索引体积 96.8MB ≈ 数据体积 95.6MB，须评估写放大"。这个观察**是错的**——那批 EXPLAIN 造数的 `desc` 只有 4 字节，行宽约 190B，把数据体积的分母压小了，造成"索引与数据等大"的假象。按真实内容长度重测：二级索引占数据体积 **30%**，`idx_pins_cover` 单独占 12%，属正常范围。

教训与第十节同源：**造数内容长度不真实，测出来的不只是性能数字不准，连"索引 vs 数据"这类比例关系都会得出反向结论。** 两份文档的错误结论已修正。

由此推出的真实约束是：`/map/pins` 走覆盖索引不是优化而是生存条件——100 万行时数据页达 824MB，远超 256MB buffer_pool，一旦回表必然引发磁盘随机读；且 `post` 上禁止净增索引。

**十三、遗留未覆盖项**

- 全部证据取自 MySQL **8.0.46**；`docker pull mysql:8.4` 卡在下载完成但不结束，8.4 LTS 兼容性未验证；
- 盘容量分配（操作系统 8G / 日志 4G / 备份 6G / 余量 6G）按运维常识划定，架构文档未细分，未经实机部署验证；**（口径已于 2026-09-07 条目 [104] 裁定作废：此处四项划分被《可观测性架构方案》§7 的七项分配表取代 —— OS 6G / Docker 镜像层 6G / swap 2G / MySQL 业务 6G / binlog 3G / Redis AOF 1G / 应用日志 3G / 埋点余量 8G / 未分配缓冲 5G。以后者为唯一口径源，理由是它是 SLS 迁移阈值 T1「埋点占盘 8GB」的直接来源，有可验证的推导；本行保留仅作历史留痕，不得再作为配置依据）**
- `buffer_pool` 命中率无实测——需真实流量，冷启动期无数据；
- 越界产物已删除：`server/` 整个目录（含 `pom.xml`、`target/`）、依赖它们的 `verify_flyway.sh`，以及 `explain_pins_index.sh`（造数分布错误，结论已被 `explain_pins_real.sh` 取代）。数据库设计产物现全部归拢在 `docs/database/` 下：设计文档 + `ddl/`（2 份 DDL）+ `scripts/`（5 个验证脚本）；
- U2-U8 实施单元（`CryptoConfig`、`AuditLogWriter` 白名单、`RetentionRuleRegistry` + CI 门禁、`PostStatusTransition`、`grid_id` 整数微度域）全部未动，**属编码阶段，本阶段不启动**。

**十四、知识沉淀（ce-compound）**

数据库设计阶段收尾，沉淀一条学习：`docs/solutions/workflow-issues/test-data-fidelity-for-performance-claims.md`（造数保真度：性能与容量结论的有效性前提）。

选题依据是本轮唯一「静态分析发现不了、且犯了两次」的问题：同一批造数先后用于 EXPLAIN 与容量测算，各错一次且错法不同——分布失真让性能数字无效，内容长度失真让「索引≈数据」这个**反向结论**写进了两份文档。核心论点是两类失真危害不对等：前者数字可疑会引人警觉，后者产出的结论完全合理、会真的被采纳。

`CONCEPTS.md` 新增两条词条与一条歧义澄清：

| 类型 | 条目 |
|---|---|
| 词条 | **保真维度**——按测量目的确定，换目的必须重造 |
| 词条 | **承载上限反推**——无规模输入时的估算方式，回答「先撞哪堵墙」而非「够不够」 |
| 歧义 | 「验证」= 可执行 vs 正确，**建表成功不蕴含设计正确** |

**第二条学习**（交付物归拢后）：`docs/solutions/workflow-issues/phase-boundary-verification-scaffolding.md`（设计阶段的验证越界：为了验证而搭的工程会替下一阶段做决策）。

选题依据是用户的一句质问「现在是数据库设计阶段，你是在开发代码吗」。我没写一行 Java，但 `server/pom.xml` 定死了 groupId、Java 版本、目录结构——这些是后端工程搭建阶段的决策，被「验证需要」的名义提前定死。核心论点是越界与否不看文件后缀，看产物里有没有需要拍板的选择；判据是「删掉它，验证结论还成立吗」。

`CONCEPTS.md` 新增词条 **验证脚手架**（归入「架构交付链」聚落），同时把上一轮误放进 Flagged ambiguities 的同名规则提升为正式词条——它是一条规则而非词义歧义，原先分类有误。

**十五、全文一致性复核（2026-09-06）**

对 PRD §13 / 数据库设计文档 / 两份 DDL / 计划文档 / 说明文档五份做交叉核对。**DDL 与设计文档 §3 在结构层面零差异**（列集合、顺序、类型、NULL、DEFAULT、ENUM 取值、索引列序、生成列表达式全部一致），发现的问题集中在口径与遗漏两类。

**最重要的一处发现：覆盖索引没覆盖真实 SELECT 列表。**

`idx_pins_cover` 原为 8 列，尾部是 `id, user_id, completeness_level`。此前的实测用 `SELECT id, user_id, completeness_level` 跑出 `Covering index range scan`，看着完美。但 `/map/pins` 的响应契约（PRD:2165 `pins[]` schema）明确要求返回 `lng`/`lat` —— **坐标不在索引里，真实查询必然回表**。而 §6.4 已算过：100 万行的数据页 824MB，远超 256MB buffer_pool，回表就是磁盘随机读。也就是说，「走覆盖索引是生存条件」这条设计约束，其证据本身是不成立的。

这暴露的是一条容易踩的判定错误：**覆盖索引的成立条件不是「WHERE 都在索引里」，而是「SELECT 列表也都在索引里」**。前者容易验证也容易满足，后者才是 index-only scan 的真实门槛；而 EXPLAIN 只回答「你给的这条 SQL 是否覆盖」，不会提醒你给的 SQL 与线上契约不符。

修正与代价（已实测）：

| 项 | 改前 | 改后 |
|---|---|---|
| `idx_pins_cover` | 8 列 | **10 列**（尾部补 `lng`/`lat`，`user_id` 移末） |
| SELECT 列表 | `id, user_id, completeness_level` | **`id, lng, lat, leaf_category_id, type, completeness_level`**（响应契约六项） |
| EXPLAIN | `Covering index range scan` | 仍为 `Covering index range scan`（✅ 结论保住了） |
| p95 | 1.71ms | **1.37ms** |
| 索引体积 | 5.5MB/10 万行 | **9.5MB/10 万行** |
| 二级索引占数据体积 | 25% | **30%** |
| 100 万行时索引总量 | 196MB（buffer_pool 77%） | **235MB（92%）** |

代价接受的理由：9.5MB 与 824MB 的数据页不在一个量级。但「100 万行时索引占 buffer_pool 92%」这个时点被提前了，已在设计文档 §6.4 列为 Batch2 容量复核首项。

**其余四项裁定**（用户逐条拍板）：

| 问题 | 裁定 | 落地 |
|---|---|---|
| PRD 两处写「距离＝空间索引」，但 `lng`/`lat` 是 `decimal(10,6)` | 「处理方案不能影响目标达成」 | 查 MySQL 8.0 官方文档核实：`SPATIAL` 索引只支持 `GEOMETRY` 系列 + 要求 `NOT NULL` + 显式 SRID，且只对 `MBRContains()` 这类空间谓词有效，**对 ORDER BY 距离本就不加速**。改为 `grid_id` 集合过滤 + 应用层 Haversine 排序；实测 9 格聚合 8294 行、索引扫描 2.44ms，候选集规模在应用层可排序范围内，150ms 验收线不受影响 |
| `id_card_hash` 是 HMAC 确定性列却只是普通索引 | 补唯一索引 | `uk_id_card_hash` 实现「一证一号」；**未实名用户该列为 NULL，MySQL 唯一索引允许多个 NULL，故不与「大多数用户未实名」冲突** |
| PRD `report.reason` 写中文字面量，DDL 是英文标识符 | 取英文标识符，PRD 补中文对照 | PRD:2442 改为 `false_info`＝不实信息 等五项对照，明确中文仅为展示文案不入库 |
| 埋点两表在设计文档 §3 零覆盖；`grid_id` 跨库不同宽 | 补字典 + 对齐宽度 | 新增 §3.16/§3.17（12 列 × 2 表 + 索引 + 边界条款）；埋点库 `grid_id` 由 `VARCHAR(32)` 改 `VARCHAR(24)` 与业务库一致。`device_id CHAR(36)` 与 `device.fingerprint VARCHAR(64)` 语义不同（客户端 UUID vs 服务端指纹），差异保留 |

**另修 8 处纯口径不一致**：3 处 emoji 残留（元数据列是 utf8mb3，落库会变 `?`）；ER 图 `publish_memory` 错挂 `post` 应挂 `user`；`bool restricted` → `tinyint`；兜底表「无索引」表述不准（有主键）；§1.3 与 §5 的「批次」二义（表上线批次 vs 清理任务落地批次）；§3.5 生成列表达式用省略号占位（改为可读缩写 + 完整子表达式两段式）。

**章节引用脆弱性**：设计文档 §6 关键设计决策重排为 §7 后，PRD 有 4 处「数据库设计文档 §6.x」全部失效；另有一处 `PRD §1451` 是把行号误写成章节号。**跨文档引用章节号，等于给自己埋一批只有全文核对才能发现的失效链接。**

**遗留未处理**（低优先级，已记录不再展开）：C1-C13 共 13 处 DDL 注释信息量大于文档（如 `post.desc` 的「过敏感词双重校验」、坐标的「不存轨迹」红线）；PRD 中 `publish_memory`/`contact_event`/`report` 三表字段字典缺主键列 `id`；`category.level` PRD 写 `int` 而 DDL 为 `TINYINT`；PRD 用 `FK` 标注而设计文档明确不写外键约束。


### — 待启动（按 9/30 内测包基线排序） —

**🔴 本周必做（8/24-8/28）——行政流程，不做则商店上架无限期顺延**

> 📖 **逐项操作指导见 [上架前置工作指导手册](./docs/上架前置工作指导手册.md)**（办理入口 URL / 操作步骤 / 材料格式硬指标 / 驳回雷点）。下列为待办摘要，**勾选状态以手册为准**。

- [ ] **P1 核对已有执照经营范围**：须含「软件开发 / 信息技术服务 / 互联网信息服务」，不含需先变更；
- [ ] **P2 购域名 + 境内云服务器**（约 131 元/年）：APP 备案必填连接域名，隐私政策必须挂自有域名；
- [ ] **P3 锁死 App 正式名称**：软著名 = APP 备案名 = 商店上架名 = 桌面显示名，**一字不差**，之后改名等于全流程重做；同时冻结包名与版本号格式（统一 `V1.0`）；
- [ ] **P4 提交 ICP 域名备案**（7-20 工作日）；
- [ ] **P5 提交软著申请**（8/25，保守 60 工作日 → 预计 11/17 下证）：**不需 app 做完**，仅需 60 页源码 + ≥10 页说明书 + 500-1300 字功能介绍；提交前须过手册内「三重一致性自检表」；
- [ ] **P8 申请 Apple D-U-N-S 编号**（免费，但可能 1-2 周）：组织账号前置项，晚申请会被动卡住 iOS；
- [x] **在 Figma 桌面版实机跑通 `prototype-figma/` 插件**：Plugins → Development → Import plugin from manifest 选 `prototype-figma/manifest.json`。**已于 8/25 跑通三轮**（条目 [44] 逐条 MCP 实测：41 框齐、22 条连线全通、21 个 Variables、色板 16 项在画布）。
      ⚠️ [27] 改了页面结构（三页合并为一页 + Section 分区），**若此前已生成过内容，须先跑批次 6 清空**（它会连带删除旧版三个 Page，否则旧页重名画框会让跳转全判跨页失败），再按 **1→2→3→4→5** 顺序整体重跑；不可只重跑 2/3/4（那样 Component master 缺失会退化成散 Frame）。
      🔁 **条目 [45] 后需再跑一轮**：新增启动页 + 三处 IP 符号接入，验收 **42 框 / 23 条连线 / 起始点 = `splash-screen`**；
      ✅ **条目 [47] 已于 8/26 跑完并验收通过**：IP 几何已从「另编的舌状块 + 半圆弧」全量换成源图实测的「圆盘 + 挖空鸭头剪影 + 眼点 + 完整同心圆环」；底部 Tab 三键去 emoji 改矢量（`map` / 圆盘鸭 / `person`）。**验收看形不看数**：三处符号（启动页 160px / 登录页 96px / Tab 中键 24px）均可认出鸭头，前两处带两道环、24px 处无环眼点偏大；Tab 切换时图标颜色随文字变主色；
      🔁 **条目 [47-b] 后需再跑一轮**：mini 档鸭头由占画板 72% 抬到 **80%**（用户「镂空的更好看」= 希望鸭头更大）。**验收看形不看数**：底部 Tab 中键鸭头明显比上一轮更满，但外圈主色环带仍连续不断（这是 80% 的上限依据，24px 下环带 1.9px）；喙尖不得触到或越过圆盘边缘；
      ✅ **条目 [70] 已于 8/30 分三轮跑完并全部实机复验通过**：P1–P5 五类修复 + 四页留白补齐 + 个人中心补退出登录 + list 页 Tab 贴底 + 四页补分页收尾条。**末轮实测数据**：24 处底部 Tab 底边间隙**全部 = 0**（改前 list 一处悬在 693px、离底 151px）；四页收尾条全部落地（`_duck-symbol-mini` + 「没有更多了」，宽 358/390 各按宿主可用宽取值，无溢出）；15 个主态画框可见内容底边 522–833px（notification 569 / my-favorite 680 / my-publish 796 / list 833 / profile 833）；**画布正式内容零 emoji**（用与探针同口径的 `\p{Emoji_Presentation}` 复扫，0 命中）。离线探针 **227 项全通**、Token 探针 **125 项全通**；
      ✅ **条目 [76] + [77] 已于 2026-09-01 实机复验通过**（用户在 Figma 桌面版跑完批次 1→5，我通过 figma-console-mcp 的 Desktop Bridge 逐项核验，非看图核验，见下）：
        - **Component Set 合成正确**：3 组 + 1 独立 = **20 master**（`shell/bottom-tab` 3 / `ui/button` 6 / `ui/pin` 10 双轴 `category × supply` + `shell/status-bar`），与 e4 的 `planStats` 自报一致；
        - **首要风险已排除 —— Instance 没散**：主页面 **241 个 INSTANCE 全部引用有效**（`brokenCount: 0`），且引用的都是合成后的 Set 变体形态（`ui/pin :: category=cat-work, supply=resource` 这种）。合成没有打断任何一个 Instance，**121 张渲染图无需重出**；
        - **样式面板已填上**：6 档 `字阶/*` Text Style + 26 档 Paint Style，名字与 `code.js` 真源逐一吻合（此前该面板**全空**，这是对外交付时最先被发现的缺口）；
        - **新增两框验收通过**：主态四段正文底边 153/249/324/378 无溢出，`_spacer` 撑开 316px 把两键推到 763/820（距底 24px）；受限态三元素中心 x **全 = 195 = 画框中心**、纵向 377–511、带「重新阅读协议」返回入口，**不是空白页**；
        - 附带核到：Variables **43 项**（COLOR 26 + FLOAT 17，与 `design_tokens.dart` 产物同数）、**画布正式内容零 emoji**（`\p{Emoji_Presentation}` 复扫 0 命中）、原型连线 24 处、90 个画框分三 Section。
        ⚠️ **两处「看着像问题、查证后不是」**：① `shell/bottom-tab :: variant=发布` 零引用 —— 查 24 处 Tab 分布（鸭圈 23 / 我的 1）后确认**发布页是全屏推入页、不带底部 Tab**，该变体为激活态备用，符合设计；② 1076 个画布文本 `textStyleId` 全为空 —— 这是 e3 裁定「只注册样式、画布不挂」的**已知状态**（本条目遗留项 ③），不是回归。
        ⚠️ **`figma_capture_screenshot` 通道故障，改走 REST 渲染绕过（已解决）**：该工具连续超时，追因确认 **`exportAsync` 在插件侧整体失效** —— 判据是「20×20 空白矩形的 `exportAsync` 也卡死，且不 await 时其 Promise 1500ms 后仍 `pending`，而同一段代码里 `setTimeout` 正常触发」，说明事件循环健康、断的是**插件沙箱→主进程的栅格化回路**。重启 Figma 桌面版无效（进程确认为新起，故障照旧），字体假设亦排除（9239 款可用、1076 个文本全 Inter 零缺失）。**改用 `figma_get_component_image`（走 REST API 渲染、返回图片 URL）成功绕过** —— 与本项目的稿、与 Component Set 合成均无关。往后实机看图一律走这条：它还支持 scale 到 4，质量更好。
        ⚠️ **看图当场查出两处坐标断言测不出的问题**（这正是「离线探针管不了观感」的实证）：
          - ① **产品名写错**：`docs/设计系统与组件规范.md` 标题误作「找**呀**找」，PRD 第 1 行是「找**鸭**找」。同音异形，**坐标断言一条都不会红** —— 字数、布局、对比度全一样，产物与真源也「同步」（因为错的就是真源）。已改生成器并补探针断言，判据是**与 PRD 第 1 行对表**、报红时打印两边的值；已验证鉴别力（修前当场报红）。离线探针 297 → **298**；
          - ② **受限态「正文↔按钮」间距只有 12px**，视觉上按钮像正文的一部分。根因：`box()` 用统一 `gap: SPACING.md`，于是「标题→正文」与「正文→按钮」同为 12px（主态没这问题是因为它用 `_spacer` 把按钮推到底）。经用户裁定按下方 ⑫ 修。
        **✅ 看图新增的一处扩充已于 2026-09-01 完成（用户裁定「按建议给操作区加一档更大间距」）**：
          - ⑫ 给「操作区与正文之间」立一档独立于统一 `gap` 的更大间距。**刻意不改统一 `gap` 本身** —— 那会牵动全部空态与所有用 `box()` 的纵排容器；只在「正文之后紧跟主操作」这一处形态上加间距。改动面：受限态一处画面（`buildPrivacyDeclined`）+ `emptyState()` 的 `opt.action` 分支。后者**当前零调用点**（是给 PRD :1686「我的发布」空页预留的），故本次实际只有一张画面变化。
          - **落法**：新增常量 `ACTION_GAP = SPACING.xxl`（32）+ 唯一构造器 `appendActionWithGap(container, action)`。值取自阶梯而非再写一个字面 32（否则多一份会与阶梯静默脱钩的副本，原则㊾）。手法是**插一个定高隔块**（`_action-gap`，高 = `ACTION_GAP − 容器 gap × 2`，容器会在隔块两侧各排一段 gap），与 `flexSpacer` 同一路子 —— 三条备选各有硬伤：改容器 `gap` 会连标题↔正文一起拉开；`marginTop` 在 Figma Auto Layout 里**不存在**（只有 `itemSpacing`）；给按钮包一层带 `paddingTop` 的壳则要把 FILL 与宽度约束再透传一次，更易错。取 xxl 而非 xl 有硬理由：`md` 档容器下 24 会算出**0 高隔块**，而零尺寸节点会被体检报成异常节点。
          - **三条新断言（离线探针 298 → 301，Token 探针 125 不变）**：① 受限态「正文→按钮」实测间距 = `ACTION_GAP`（判据是**累加中间每段高与每段 gap**，不是核源码有没有调构造器 —— 隔块高由容器 gap 派生，容器 gap 一改源码一字未动而实际间距就变了）；② 隔块高 = `ACTION_GAP − gap×2` 且为正、宽 1；③ [反向] 容器 gap 已 ≥ `ACTION_GAP/2` 时构造器必须**当场抛错**，不许静默塞 0 高隔块（那种退化「源码看着加了、画面等于没加」，零征兆）。实测 32 / 隔块 1×8 / 反向已抛错。
          - **附带一条方法论**：这处缺陷改前**没有任何断言会红** —— 12px 不溢出、不重叠、在 `SPACING` 阶梯上、触控区也够。间距的「层级表达」属观感，只能靠人看出来；但**一旦定了值就该由断言钉死**，不能留给下一次看图（同 ① 产品名那条的取向：观感发现问题 → 立刻转成机读判据）。
          - 两份产物重跑均为 `unchanged`（`docs/设计系统与组件规范.md` 与 `lib/design_tokens.dart`）：本次动的是版式，未触 Token 表。
          - **✅ 已于 2026-09-01 实机复验通过（用户重跑批次 + 我看图 + Bridge 量坐标）**。实测纵向：标题 323→346、正文 358→400、`_action-gap` 412→420（1×8）、按钮 432→477。**正文底 400 → 按钮顶 432 = 32px**（改前 454→466 只有 12px），标题↔正文仍是 12px —— 层级差已被间距表达出来。看图印证：按钮明显自成一档，不再读成正文续行。附带确认**加隔块没打断组件引用**（按钮仍是 INSTANCE `btn/primary/重新阅读协议`）、`_body` 高 800（844 − 44 状态栏）、内容纵向 323–477 重心居中。
          - ⚠️ **两条实机通道的操作教训**：① **跑批次会顶掉 Desktop Bridge** —— Figma 同时只运行一个插件，用户点完 `prototype-figma` 的批次按钮后 Bridge 即断（报 `Cannot connect to Figma Desktop`），需 `figma_reconnect` 重连（一次即通，`probeVerified: true`）。这是常态而非故障，往后「用户重跑 → 我核验」之间应默认先重连；② **重跑后全部节点 id 会换新**（受限态由 `413:32919` 变为 `418:1045`），任何跨轮记下的 id 一律作废，必须重新走 `figma_get_file_data`（`depth: 3` + `nodeIds` 指定 Section）现查。

        **⚠️ 2026-09-02：为交付重出全量渲染图，看图又查出问题（用户裁定「先修 ① 和 ③」）**：
          - **起因**：判断「UI 设计可否交付」时查真源发现 —— 页面覆盖零缺口、组件与样式齐、301+125 断言全绿、规范文档 11 章，**但 `assets/render/` 的 121 张图停在 8/31 17:22**，而此后画布改过两轮（e1–e4 的标签四族/卡片三槽位/组件化回改 + ⑫ 间距）。交付时设计师拿到的图与稿对不上是最刺眼的一类问题，而这**恰是探针管不了的**（它管结构，管不了「图是哪一版出的」）。用户裁定重出全量。
          - **出图通道的取舍**：`figma_capture_screenshot` 仍坏（见上文追因），`figma_get_component_image` 走 REST 但**单次只接一个 node id**，45 框 = 45 次工具调用；Exec（code mode）里 `run_mcp` 报 `MCP server is not found`，编排不通。故请用户提供 Figma PAT，走 REST `/v1/images/:key?ids=a,b,c` **单次可传多个 id**，分 15 个一批共 3 批拉完。token 只经环境变量传入、脚本跑完即删、未落任何文件与 git，用完请用户 revoke。
          - **实施**：临时脚本 `fetch-render-tmp.js`（已删）拉 45 框 @2x → `r75-*.png`，**不覆写历史图**。首跑第 2 张即 `ECONNRESET`（S3 侧偶发），补「退避重试三次 + 已存在则跳过」的断点续跑后 **45/45 全成**。抽看 10 张覆盖全部改动面，确认 e1–e4 与 ⑫ 均落对：⑫ 受限态按钮自成一档、e1 标签四族色条+角标一致、e2 卡片三槽位在「我的发布」四态分明、e2 `FIELD_SPECS` 字段无溢出、e4 组件化各页表现一致、产品名「找鸭找」已正确。
          - ⚠️ **③ 两条看图判断经查证 PRD 后全部撤回（如实登记：我看图判断有误）**：① 「发布」中键那个「读不出语义的带缺口圆」**不是图标选错** —— PRD §1.4.1.2 `:131`、§12.1 `:1936`、`:413` 三处明写它就是鸭子 IP 微缩档（`duck-symbol-mini`，圆盘铺满+白色实体鸭头），是刻意的品牌 IP 消费位之一。真正对得上的是 PRD `:133` 那条硬约束「**24px 下必须可辨**」未达成 —— 但那是 IP 图形自身的辨识度课题，与本次修间距/对齐不同性质，**不并入本次**（否则就是无规划扩展）；② 「全部」分类只有纯色圆无图标**不成立** —— PRD `:1020` 分类栏里「全部」与五类并列，而 §6.4.2 只给五个**分类**定了图标，「全部」本就没有图标规格，纯色圆是合理落法。**教训：看图得出的观感问题，必须先与 PRD 对表再动手；否则会把「刻意的设计决策」当成缺陷改掉。**
          - **⑬ 只做 ①（受限态多行正文居中）**：PRD §6.5.1 `:1251`/`:1256` 只规定受限态「不是空白页、要有价值说明 + 「重新阅读协议」主按钮」，**未规定对齐**，故属可自主裁定的视觉判断。成因是 `align: 'CENTER'` 只让子节点**块**在交叉轴居中，文字自身仍是 LEFT —— 这正是此前记过的「坐标断言判『中心 x=195 = 居中』过于宽松」的实际后果。**边界刻意收窄**：全项目 `textAlignHorizontal` 零使用（所有文字皆 LEFT），单行短文案在居中块里 LEFT 与 CENTER 无视觉差，故**只给「居中收口页的多行正文」加**，不做全局对齐改造。
          - **⑬ 落法与一条新判据（离线探针 301 → 302，Token 探针 125 不变）**：`buildPrivacyDeclined()` 内给 `heading` 与 `lead` 两段文字显式设 `textAlignHorizontal = 'CENTER'`，**不下沉进 `text()`** —— 全项目其余文字（正文段落、表单标签、列表行）都该左对齐。新断言取节点的 `textAlignHorizontal` **实际值**而非核源码有没有写那两行赋值：将来若 `text()` 改了默认对齐、或有人把 FILL 改成 hug 使块居中重新生效，都该由这条如实反映当前画面。反向验证：注掉 `lead` 那一行后精准报「2 段文字中 1 段为 CENTER」。
          - ⚠️ **顺带修掉 mock 的两处失真（不修则新断言测的是假象）**：① `makeText()` 未给 `textAlignHorizontal` 默认值 —— 真 Figma 默认 `'LEFT'`，mock 里是 `undefined`，会把「真的设成左对齐」与「属性根本不存在」混为一谈；② 克隆节点的逐属性搬运清单里漏了 `textAlignHorizontal` —— 不补则 Instance 内的居中文字在探针眼里全变左对齐。**本 mock 是逐属性搬运，往后新增任何 TEXT 属性都得在克隆清单里登记一行，否则克隆出来的是半个节点。**
          - **验证**：离线 302/0、Token 125/0，两份产物重跑均 `unchanged`（只动版式与对齐，未触 Token 表）。

        **⚠️ 2026-09-02：补 §11 登记的两项「规格已定、稿内零实现」（用户裁定「先补那两项零实现」）**：
          - **先读真源查出：两项性质完全不同，不可等同处置**。① **输入框五态** —— `field()` **已能生产全五态**（`opt.state` 五个键均已实现，`error` 态还带「必给 errorText」的强制校验）。缺的只是**画布上没有一处把 focus/error/disabled 画出来**，即无展示位；② **卡片三态** —— `card()` **只有正常态一种形态**，选中态的 `primary-light` 边框与下架态的 `opacity 50%` 在**代码里根本不存在**。缺的是实现本身。故 ① 是「补展示位」，② 是「补实现 + 补展示位」。
          - **落点定在批次 1 的 `00 · Tokens 与组件` 页**，与语义色板/分类色 Pin 矩阵/字阶/动效四板并列，**不进 §10.1 的 19 个页面画框**。理由：这两项是**规范演示**而非产品画面 —— PRD §1.4.7 的选中态在原型内无交互载体（列表卡点击即跳详情，无多选场景），输入框的 focus 更是瞬时态，硬塞进业务页会造出 PRD 里不存在的画面，那是无规划扩展。T6 板第 ⑤ 组「20px 真实尺寸对照条（图标验收用）」已是这一范式的先例。
          - **`CARD_STATES` 与 `FIELD_SPECS` 两张表的 `inStock` / `sample` 字段要跟着翻**：这两张表是《设计系统与组件规范》§11 那 8 条已知缺口的数据源，补了实现却不翻字段，产物文档会继续声明「本轮未出稿」——**规范与画布对不上，比缺口本身更坏**。
          - **✅ 已完成（307 / 0 + 125 / 0，规范文档 `updated`）**。实做与计划一致，另有三处计划外的必要连带改动，逐条记明：
            - **画板 E `board/组件状态实样 [PRD §1.4.7 + §1.8]`（w 480）**：卡片三态逐档现取 `CARD_STATES`、输入框五态逐档现取 `FIELD_SPECS.formField.states` 的键序，**不手写档位数组** —— 表里加一档而板上漏画，新断言会当场报红。三张卡的文案与分类色刻意完全相同，**唯一变量只剩状态本身**，否则读图的人分不清差异来自状态还是来自内容。板尾一张 `spec` 档标注卡写四条验收口径。
            - **`card()` 加第 6 参 `state`**：`selected` 走 `stroke: 'color/primary-light'` + `strokeWeight: 1`，`archived` 设 `c.opacity = 0.5`，**状态名对不上表就当场抛错、不静默退回 normal** —— 三态差异只有一道 1px 边框与一层透明度，静默退回会让「三态画上去了」看起来是真的。查证 `box()` `:1250` 是 `if (opt.stroke)`，传 `null` 走 falsy 分支不描边，正常态与下架态照旧无描边（描边与阴影并存会出双线）。
            - **⚠️ 连带改动一（最要紧）：`probe-layout-offline.js` 那条「未出稿如实声明」断言原写法会因本次改动退化成永真**。原体是 `for (st of CARD_STATES) { if (st.inStock) continue; ... }` —— 只查 `false` 的档。三态补齐后 `inStock` 已无 `false`，循环空转、断言恒绿：**补完实现反而把守门的判据废掉了**。改为逐档**双向**核对：`inStock=true` 的档若被产物误标「稿内实现＝无」同样报红（文档谎报缺口，会让人白做一遍已有的东西）。这类「改真源把守门判据顺手废掉」的形态，比判据写错更难发现 —— 因为它表现为全绿。
            - **连带改动二：产物生成器有四处硬编码措辞不由 `inStock`/`sample` 派生**，只翻字段是不够的：§4.1 标题「其中 N 档本轮未出稿」（改为 `missing === 0` 时输出「全部有实处」）、§7 那段 ⚠️ 样本量注解、§11 表里那两行、§0 第 2 条导读。**措辞口径同时收紧**：`sample` 由「有实处 / 本轮新立」改为「**业务页有实处 / 仅状态实样板**」，§11 两行改为「已补：实样在 Tokens 页；**业务页内仍无实处**」—— 补的是规范演示位，不是产品画面，这两件事不可混说，否则接棒人会去 19 个业务页里找那五个输入框状态。
            - **连带改动三：`CARD_STATES.archived.note` 改写时补记了一条此前没写明的设计决策** —— 「我的发布」页那张已下架卡**刻意保持全亮度、不施加 opacity 折扣**：那一页的卡带「刷新重发 / 删除」操作组，压到 50% 会连**操作入口**一起压暗。原 note 把这写成「折扣未施加」（像是遗漏），实为取舍。
            - **新增 5 条断言**：画板 E 已落地；卡片三态逐档核 selected 独有 1px primary-light 描边 / archived 独有 opacity 0.5 / 三档同阴影（**判据取节点属性实测值，不核源码写没写那几行赋值**）；输入框五档逐档上稿；error 档实样真带 `_field-error` 文案行；板内卡片实样不超出所在行可用宽（358 ≤ 480 − xl×2；Auto Layout 溢出在图上只表现为「卡被裁掉一点」，极易漏看）。**输入框的取值正确性不重复断言** —— 上文 `field()` 五态那条已逐项比 `FIELD_SPECS`，再写一遍就有了第三份口径（原则㊾）。
            - **踩坑一处**：§11 那两行写在 template literal 里，反引号未转义直接把生成器炸成 `SyntaxError: Unexpected identifier 'card'`。探针的「产物与真源同步」断言把它报成「探针自身异常」而非静默通过 —— 这条通道是通的。
          - **⏳ 待用户在 Figma 点批次 1 让画板 E 上稿**，再出图核验形态（三态差异极小，图上仍需人眼确认那道 1px 边框是否看得见 —— 它与 surface 只差 1.11:1）。同时批次 3 需重跑以让 ⑬ 的受限态文字居中上稿。

        **⚠️ 2026-09-02 ⑭：上稿方式的判断失误 + 实机核验查出选中态卡高偏移 2px（用户裁定「全量重跑五个批次」）**：
          - **我上一轮说「点批次 1 让画板 E 上稿」是错的，读代码查实后当场撤回**。`batchSetup()` 开头就是 `resetPage(PAGE_NAMES.setup)` + `COMP_CACHE = {}` + `registerComponents(page)`，而 `resetPage` 是逐个 `remove()` 页面全部子节点 —— **含那个装着 20 个 Component master 的 host**。批次 2–4 的 247 个 Instance 全部指向旧 master，单点批次 1 会把它们一并毁掉。UI 上「必须按 1→2→3→4→5 顺序点」这句警告说的就是这件事，我此前把它读成了「首次生成时的顺序要求」。**教训：凡涉及「只跑某一个批次」的建议，必须先读那个批次的头几行有没有 reset**，批次之间不是彼此独立的。
          - **全量重跑（清空 → 1→2→3→4→5）后实机核验全绿**：20 master / 3 组 Component Set / 6 档 Text Style / 26 档 Paint Style，Section 画框数 55 / 30 / 5 与重跑前完全一致，**247 个 Instance 零断链**（逐个 `getMainComponentAsync()` 核过）。画板 E 已落在 Tokens 页，五板并列。
          - **实机查出一处离线探针查不出的几何偏差**：选中态卡高 **78**，另两档 **76**。根因是 Figma 的 `strokesIncludedInLayout` **默认 true** —— 1px INSIDE 描边被计入 Auto Layout，把内容框上下各挤 1px。这直接违反 `CARD_STATES.selected.spec` 那句「其余一切不变」。**2px 在单张卡上看不出来，但选中态与正常态在列表里必然相邻，卡一被选中整列往下错 2px，看着像列表在抖**。已在 `card()` 里对 selected 档设 `strokesIncludedInLayout = false`。
          - **这条同时补进了 spec 正文与 CSS 侧口径**：`CARD_STATES.selected.spec` 加「硬约束：边框不得占据布局尺寸 —— Figma 侧设 `strokesIncludedInLayout = false`，**CSS 侧须用 `outline` 或 `box-shadow` 而非 `border`**」。只在代码里改掉是不够的：实现侧照着「加 1px 边框」这句话写 `border`，会在浏览器里复现同一个 2px 位移。
          - **mock 两处同步补齐**（同 ⑬ 的 `textAlignHorizontal` 手法）：`makeNode()` 补 `strokesIncludedInLayout: true` 默认值（否则 `undefined` 会把「真的设成 false」与「属性根本没设」混为一谈）；克隆逐属性搬运清单补一行，否则 Instance 内的选中态卡会退回默认 true。**本 mock 逐属性搬运，新增任何属性都得在克隆清单登记一行 —— 这已是第二次踩到。**
          - **断言的能力边界如实记明**：本 mock 不模拟描边对几何的挤压（只保证主轴累加 + 交叉轴取最大），故新断言**只能核 `strokesIncludedInLayout` 的属性值、核不出那 2px 高度差**。这 2px 是实机量出来的，离线断言只负责防回归。**这类「只有真机才量得出」的偏差，是继渲染图过期之后第二个探针盲区**。
          - **emoji 红线又拦下一次**：我在 `spec` 里写的 `⚠️` 会被画板 E 渲染进画布，探针当场报「画布正式内容零 emoji｜发现 1 处 @_card-state/selected」。已换纯文字「硬约束：」。**真源表里的字段不是纯注释 —— 它们会上画布**，写字段时要按画布内容的标准来写。
          - **✅ 307 / 0 + 125 / 0，规范文档已重新生成。⏳ 待再跑一轮全量批次让 `strokesIncludedInLayout` 修复上稿**，随后出渲染图。

        **⚠️ 2026-09-02 ⑮：2px 修复上稿核验，并顺手查出同一病灶的第二处受害者 —— 按钮**：
          - **卡片修复已生效**：三档卡高全为 **76**，`selected.strokesIncludedInLayout = false`。基线不变（20 master / 3 Set / 247 Instance 零断链 / Section 55·30·5）。
          - **顺手做的全稿同类扫描扫出真问题**（本想只是确认「没有别处」，结果确认了有）：判据是「带描边 + Auto Layout + 该轴 AUTO 尺寸 + `strokesIncludedInLayout` 非 false」，命中 110 处。逐类核过后**只有按钮一族是真受害者**：`btn/secondary` 高 **47**，其余五个变体全 **45**（pad 12+12 + body 行高 21）。
          - **它不是理论风险，是画布上肉眼可见的错位**：实测 **5 处 `align: MIN` 的横排容器**里 secondary 与 ghost 顶对齐、底部差 2px —— `_pub-actions` 4 处（「编辑 / 下架」×2、「刷新重发 / 删除」、「继续编辑 / 删除」）+ `_actions` 1 处（「去首页看效果 / 我的发布」）。**一排按钮底线不齐，比单张卡高 2px 更刺眼**。已在 `buttonRaw()` 里无条件设 `strokesIncludedInLayout = false`（不只对 secondary 设：六变体共用一个构造器，将来任何变体加描边都会踩同一坑，条件判断只会漏）。
          - **`row/*` 16 处刻意不动**：同样带描边高 47，但它们彼此同高、垂直堆叠、无「同构无描边兼生」的对照，改了纯属超出计划（规则 4.2）。`_annotation/*` 50 处是标注框，不上产品画面，同理不动。
          - **mock 的逐属性搬运清单第三次咬人**：新断言写完当场报红「六个变体全 true」，而源码明明设对了 —— 根因是 `createComponentFromNode` 的 mock **没搬 `strokes` / `strokeWeight` / `strokesIncludedInLayout`**，Frame 上设的值到不了 master。已补。**这轮一共在三处搬运清单上栽了跟头（克隆节点、`makeNode` 默认值、`createComponentFromNode`）—— 结论是：往后新增任何节点属性，必须同时在这三处登记，否则探针测的是自己造出来的半个节点。**
          - **假红比假绿好，但仍是缺陷**：这次是报红引我查出 mock 漏搬；若方向反过来（mock 默认 false），断言会在源码没设时也全绿，那就是真正危险的假绿。
          - **✅ 308 / 0 + 125 / 0**（新增按钮那条），规范文档 `unchanged`（`strokesIncludedInLayout` 属于 Figma 侧实现细节，spec 正文里已由卡片那条的「CSS 侧须用 outline/box-shadow」统一覆盖）。**⏳ 待再跑一轮全量批次让按钮修复上稿**，随后出渲染图。

        **✅ 2026-09-02 ⑯：按钮修复上稿核验 + 重出全量渲染图 50 张（r76）**：
          - **按钮修复已生效**：46 个 `btn/*` 全为 **45 高**（六变体无一例外），5 处横排容器底部**完全对齐**（如 `_pub-actions` 内「编辑」与「下架」同为 5521）。三档卡高全 76。基线不变：20 master / 3 Set / **247 Instance 零断链** / 6+26 Style / Section 55·30·5。
          - **残留 28 处带描边 AUTO 容器逐个核过、确认不改**：全部**同名同高**（`_sheet-reset` 三处均 36、`notify/*` 五处均 69、`_seg-tab` 三处均 47、`_template-fields` 两处均 164……），且都是独立容器、无「同构无描边兼生」的对照，不构成错位。与 `row/*` 同理。
          - **渲染图重出 50 张 @2x（`r76-*`）**，比上轮 r75 的 45 张多 5 张规格板（含新增画板 E）。通道仍走 REST `/v1/images?ids=a,b,c&scale=2`（`figma_capture_screenshot` 通道坏、`figma_get_component_image` 单次只接一个 id、Exec 里 `run_mcp` 不可用，三条替代路径均已在前段验证过）。分 12 个一批共 5 批，50/50 全成，无 ECONNRESET（上轮补的退避重试 + 断点续跑起了作用）。token 只经环境变量传入、脚本跑完即删、未落盘未入库。
          - **图上逐一确认三处修复**：`r76-b05` 画板 E 三态五态齐备，且**那道 1px `primary-light` 边框在白底上确实可辨**（此前只能靠 1.11:1 这个数字推断，属「规格能读懂、观感只能看」的典型）；`r76-c12` 我的发布四张卡的「编辑 / 下架」底线已齐；`r76-c03` 受限态标题与正文居中。
          - **看图另发现一处规格开放点，登记不改**：下架态卡的 `opacity 50%` 会把「已下架」标签一起压浅，**而它恰是这张卡最该读清的信息**。PRD `:344` 原文「Opacity 50% + 已下架标」两者并列，未说标要不要豁免 —— 属规格本身的开放点，不是实现偏差，交由用户决定是否立议题。

        **⚠️ 2026-09-02 ⑰：交付结论 = 可交付，但查出一条须随稿声明的触控线破线（用户裁定「按现状交付，把这条补进 §11」）**：
          - **为下交付结论去核 §11 时查出一条此前没被记录的破线**：`_sheet-reset` 高 **36**、`_sheet-confirm` 高 **34**，各出现 3 次（T3 三级筛选树三层弹层的底部动作行），**均破 PRD `:422` 的「按钮最小触控区 44×44 px」**。§11 原先只登记它们「手搓、绕过按钮真源表」，没意识到连带破了触控线 —— **登记了症状，漏了后果**。
          - **探针为何没拦住（第三个盲区，且我一度说错了原因）**：我先前判断那条 44 断言「按有没有挂 reaction 筛选」，读源码后确认是**逐个点名**（只查 `_tab-` 单元格与 `_nav-bell` 两处，靠正则抠 `code.js` 源码里的字面数字）。`_sheet-*` 从未进入检查范围。**「逐个点名」的判据天然只覆盖点到的名字**，新增可点元素不会被自动纳入 —— 这与「渲染图版本」「描边挤压几何」并列为第三类盲区，但性质更普遍。
          - **按现状交付、只补声明的理由**：改法要先给 `BUTTON_SPECS` 补一档「满宽 grow + full 圆角」（现有 capsule 档宽度写死 312），master 数由 20 变 22，牵动 Component Set 与全部断言基线 —— 等于交付前夜重开一轮全量重跑 + 重出图。**设计稿的作用是传达规格：一条被如实标注的偏差不会造成错误实现，一条没被发现的偏差才会。**
          - **本轮三处改点（不动画布，无需重跑批次）**：① 生成器 §11 那行措辞由「绕过按钮真源表」升级为写明破线实测值与实现侧须补到 44；② `code.js` 内 `TAG_SPECS.notTag.usage` 与 `buildFilterSheet()` 处的注释同步写明破线（真源与产物两头都要能查到，否则读代码的人仍以为只是「旁路真源表」）；③ **探针补一条遍历全稿的触控线断言**，把「逐个点名」改为「按名单遍历所有语义为按钮的容器」，并**把这两个已知破线者列为具名豁免、附豁免理由与目标值** —— 豁免必须是显式的，否则下一次有人新建矮按钮时断言依旧全绿。
          - **✅ 已完成（310 / 0 + 125 / 0，规范文档 `updated` → 复跑 `unchanged`）**。产物两处落地：§4 标签表 `:266`（notTag 的 usage）与 §11 缺口表 `:595`。
          - **⚠️ 新断言当场逮到一处我差点写错的东西：离线值与实机值必然不同，豁免值只能记离线值**。新断言首跑即报「`_sheet-reset` 高 33 < 已声明的 36」——我原本按实机量到的 36 / 34 填豁免值，而 mock 按 `fontSize * 1.4` 估行高（small 12 → 17，加 pad 8+8 = 33），真机 small 行高是 20（→ 36 / 34）。**两侧都没错，但混用会让这条断言在离线永远报红**。故 `TOUCH_WAIVED` 记 `at`（离线值，用于防回归）+ `live`（实机值，与文档口径一致），注释写明二者为何不同、以及「共同结论一致：都远低于 44」。若当时图省事直接把 `TOUCH_MIN` 调低或把这两个名字无条件跳过，就会造出一条永久免检的口子。
          - **配套第二条断言「豁免名单无僵尸条目」**：若某个豁免名在稿内已找不到（被改名或删掉），这条豁免就成了替不存在的节点永久开口子。反向验证：把 `_sheet-reset` 改成 `_XX-reset` 后精准报「稿内已无：_XX-reset（应删除对应豁免）」。**豁免机制本身也需要被守门 —— 这是此前八条 §11 缺口都没有做到的**。
          - **判据从「点名」升级为「遍历」的收益边界**：新断言遍历 `btn/*` 全族加两个手搓件，并整体排除 `_annotation/*` 子树（标注卡是旁注，不上产品画面）。它能自动纳入将来新建的矮按钮 —— 但**仍只覆盖「名字像按钮」的节点**。真正彻底的判据应是「凡挂了 reaction 的节点」，那需要连线数据齐备，属另一个课题，此处不扩。

        **⚠️ 2026-09-02 ⑱：整理交付目录（用户裁定「新建 delivery/ 目录，旧图原封不动」）**：
          - **动因**：`prototype-figma/assets/render/` 下累计 **216 个文件**，只有 `r76-*` 那 50 张是当前版，其余 `r62`–`r75` 与十来种散图（`duck-*` / `tabicon-*` / `stress-*` 等）是过程留痕。交付物与过程留痕混在一个目录里，收件人无从判断该拿哪些 —— **这与「渲染图过期」是同一类问题的另一面**：前者是图不对版，后者是对版的图找不出来。
          - **落法**：新建 `delivery/` 汇总当前版四件核心交付物（50 张渲染图 / 规范文档 / Flutter token / 交付说明），**`render/` 原封不动** —— 历史轨迹是排查依据（如「按钮 2px 错位是哪一轮引入的」要靠 r73 与 r76 对比），移走或删掉等于砍掉可追溯性。代价是仓库体积增约 28 MB，用户已裁定接受。
          - **`delivery/` 是产物而非真源，故必须可一键重建**：写一个 `build-delivery.js` 负责拷贝与清单生成，**不手工拉文件**。手工拷的目录下一轮改稿后必然与真源脱节，而脱节了看不出来 —— 正是 r75 过期那次的成因。脚本自报 `created/updated/unchanged`，与另两个生成器同口径。
          - **交付说明写「收件人视角」而非开发日志视角**：§11 的 8 条缺口里有 2 条是**实现侧行动项**（`_sheet-*` 破 44 触控线须补足；卡片选中态与输入框五态业务页内无实处，须按登记表构造），其余 6 条是已裁定不做或 API 限制。这两类混在一张表里，读的人会把行动项当免责声明滑过去，故交付说明里**单独拎出「必须处理的 2 条」**。
          - **✅ 已完成**。`delivery/` 共 53 个文件 28.3 MB：`01-页面渲染图/` 四个子目录（规格板 5 / 地图与列表 25 / 核心流程 16 / 模态与组件 4）+ `02-设计规范/设计系统与组件规范.md` + `03-开发资产/design_tokens.dart` + `README.md`。两套探针 310 / 0 + 125 / 0 不受影响。
          - **生成器内建两道自检，都已反向验证过**：① **归组遗漏检查** —— 若 `RENDER_GROUPS` 缺登记某个区段字母，那批图会被静默丢掉，而「交付目录里少了几张图」几乎不可能被发现（没人会去数 50 个文件）。反向测试把 `'t'` 改成 `'zz'`，精准报「有 4 张图未被归组」并列出四个文件名；② **README 作指纹** —— README 内含图张数与分组明细，图有增减必然反映到它上面，故用它比对来判定 `created / updated / unchanged`，与另两个生成器同口径。
          - **⚠️ 反向测试当场暴露一个真缺陷并已修**：首版是「先 `rmrf` 清空、再边拷边验」，于是校验失败时**目录已被清空且只拷了一半** —— 交付目录处于半空状态而脚本已退出，下次跑又报 `created`，看不出上一次是失败中断的。改为**先分组、先验完整性与文件存在性、全部通过后才动目录**。修后重测：报错时目录仍完好保留 53 个文件。**这类「失败路径留下半成品」的缺陷，只有真的把它跑失败一次才会现形**。
          - **README 里的画框数改为现算**：原先手写「45 个画框 / 5 块规格板」，虽当时正确，但它与图张数无任何关联 —— 改稿后数字不会自己变，而「文档说 45 个、目录里 50 张」这种对不上，读的人只会怀疑整份文档。改为从分组结果派生（`r.total - boardCount`），现算得 45 / 5 与手写值一致，印证原值没错，但从此它会跟着变。

        **⚠️ 2026-09-02 ⑲：重出过期的 PRD.docx（用户裁定）+ 修掉一条永远报红的判据 + 新建源–派生比对探针**：
          - **滞后程度远超预估**：docx 停在 **8/24 13:31**，而 `PRD.md` 此后被 **16 次提交**改过。缺失内容包括隐私协议门规格、鸭子 IP 全量矢量化、contact 隐私说明两句纠偏、「帮助与反馈」越界入口删除等。重生后 **130,048 → 158,060 字节**（+21.5%），目录 24 → **28 条**，段落 815 → **1101**，表格 48 → **91**（几乎翻倍）。
          - **按已固化的两步操作执行**：`gen_prd_v2.1_docx.py` 产 `_tmp.docx`（避 Word 文件锁）→ `Move-Item -Force` 改名。**漏第二步是静默失败** —— 脚本会打印「已生成」，但正式交付件文件根本没被替换。两个顺带收获：① 遗留项「删除被 Word 占用的旧 `docs/PRD.docx`」**其实早已解决**（文件已不存在），说明文档里那条待办是过期的；② `~$找鸭找-市场需求文档-v2.1.pptx` 是 pptx 的锁文件，与 docx 无关（此前记录已辨明）。
          - **五段闸门报两条 FAIL，查证后确认是判据缺陷而非文档问题**：`verify_prd_docx.py:105` 把「替代」「替换」当**裸词**列入版本痕迹禁用词，于是五处正常技术用词全部误报 —— 4B 方案的「唯一不可替代优势」、占位矩形的「转曲后替换」、换底图流程的「替换 `map-bg.png`」、开发 Key 的「替换配置」。**同一份清单第一组早已带上下文限定（如「替代 v1」「原 v2.0」），说明原作者本就懂这个道理，裸词那两个属过度收紧。** 已改为逐个列举带版本语境的组合（`替代 v2` / `替代原` / `替换旧` / `替换为 v` 等七项）。
          - **为什么这类误报比漏报更坏**：闸门每次跑都亮两条红灯，下次跑的人无从区分「已知自伤」与「新出的真问题」，最终会把整段校验当噪音跳过 —— **一条永远报红的判据等于没有判据，还额外消耗每次读它的人的注意力**。放宽后立即反向验证：往 PRD 临时插「本节替代原方案，并替换旧版口径」，新判据精准报出 `替代原` / `替换旧` 各 1 次，**拦真病火的能力未失**。
          - **新建 `docs/verify_prd_docx_sync.py`（11 条特征句的源–派生比对）**。动因：`verify_prd_docx.py` 验的是固定关键词表，**管不了「MD 改了但 docx 没重生」** —— 8/24 那次整章缺失，两个文件各自合法、闸门全绿、无任何工具报错，唯一检出手段是主动做源–派生比对。本脚本把那个手工动作固化下来，双向确认后 11 条全 OK。反向验证：插入锚句后精准报 `[MISS]` 并给出正确处置（重跑生成器），退出码 1。
          - **⚠️ 人工比对时我连续三次栽在「特征句脑补」上**，每次都被脚本的双向确认逮到：① `Figma Variables` —— PRD 里根本没有这个词（Design Token 落地是 Figma 侧的事，未写进 PRD），凭印象编的；② `24px 下必须可辨` —— 原文是「`**24px**` 下必须可辨」，**跨 `**` 标记边界**，在 md 里匹配不上却在 docx 里能匹配（docx 提取的是纯文本，标记已被消化）；③ `44px 最小触控` —— 原文是「按钮最小触控区 44×44 px」，措辞脑补。**故探针在 md 侧比对前统一剥掉 `**` 与 `` ` `` 标记，且特征句一律先在 md 侧验明存在（报「脑补」）再查 docx（报 MISS）** —— 把「我记错了」与「docx 真缺内容」分开，混为一谈时假 MISS 会淹没真 MISS。这条教训此前只以「必须从权威源复制原句」的形式留在记忆里，**明知却仍犯三次，说明它必须固化进代码**。
          - **⚠️ 新探针差点重犯我刚修掉的错**：首版把「docx 的 mtime 早于 md」作为**成败判据**，结果还原测试文件后立即假红 —— `mtime` 对无关操作敏感（Git 检出、编辑器保存、脚本还原都会把 md 的 mtime 推到现在），内容一字未改也报滞后。**我刚从 `verify_prd_docx.py` 删掉两个「经常假红的裸词」，转头就在新脚本里造了一条同类判据。** 已改为时间序只作提示、不参与退出码，成败一律由逐句比对实证决定。

        **⚠️ 2026-09-02 ⑳：撤销「鸭子 24px 辨识度未达成」这条遗留项 —— 系我记错，PRD 明写全档达标**：
          - **查证结果**：PRD `:141` 原文「眼点半径：随鸭头同一仿射变换得 **r=45.37**（24px 下 2.13px），**已自然超过 2px 底线，无需再抬**。喙颈开口实宽占画板 13.07%，24px 下 3.14px，**全档达标无需处理**。」`:143` 另给主色环带硬约束 ≥1.5px，实测 1.90px 达标。**三项指标全部达标，且早在 2026-08-26 那轮就已收口。** 我把 `:133` 那句硬约束「24px 下必须可辨」当成了未完成项，实为已满足的验收标准。
          - **不凭 PRD 单方声明结案，独立核算了 SVG 源文件**：`duck-symbol-mini.svg` 实读 `<circle cx="501.03" cy="341.89" r="45.37" fill="#0B7C8C"/>`，viewBox `0 0 1024 1024`。折算 `45.37 × 2 ÷ 1024 × 24 = 2.127px` ✓ 超 2px 底线；圆盘 `r=504.01` 铺满 98.4% ✓ 与 `:137` 一致；眼点填色 `#0B7C8C` 是主色 ✓ 与 `:139` 一致。**PRD 的数值与 SVG 真源逐项对上，结论可靠。**
          - **⚠️ 顺带查出一个真问题：`probe-assets-verify.py` 跑不了**。缺 `cv2` 与 `cairosvg`，且仓库**没有任何 requirements 文件**。而 PRD `:125` 明写「下表阈值由 `probe-assets-verify.py` 实测反推，非拍定值」、`:143` 说环带约束「已固化为该探针断言」—— **依赖不成文，等于那些被 PRD 引为权威的阈值无人能复核**。这与「渲染图过期」「docx 滞后」是同一族问题：产物或判据脱离了可执行链条，却看不出来。
          - **装依赖两次被沙箱拦下**（`pip install` 与 `pip install --user` 都会碰 `C:\Python314\Lib\__pycache__` 与 `numpy` 目录）。**未试第三次** —— 按红线不硬执行，改为两项补救：① 新建 `prototype-figma/requirements-assets.txt` 登记 `opencv-python` / `numpy` / `cairosvg`，并注明 cairosvg 在 Windows 需 GTK 原生库、可能装完仍导入失败；② 给探针的 import 段加 `try/except ModuleNotFoundError`，缺依赖时打印安装命令**并指明替代核验路径**（SVG 几何常量可直接读文本核算，无需光栅化），退出码 2 与断言失败的 1 区分开。
          - **为什么替代路径必须写进代码而非只留在文档**：本轮我正是靠「直接读 SVG 算 `r × 2 ÷ 1024 × 24`」绕过缺失的光栅化链完成了核验。这条路子不写在探针里，下一个碰到缺依赖的人只会看到 `ModuleNotFoundError`，然后要么放弃核验、要么花时间重走一遍我刚走过的弯路。

        **⚠️ 2026-09-02 ㉑：UI 设计耗时复盘沉淀为 learning（`ce-compound`，lightweight 深度）**：
          - **深度选择理由**：会话已经过上下文压缩，核心证据（三轮重跑的实机数据、六起假信号、四起记忆失真）只存在于主上下文里，派 subagent 去研究反而要先把这些重述一遍 —— 属 skill 明示的「real context pressure」情形。跳过的只有 subagent 交叉检索与语义 grounding 校验，两项确定性校验脚本照跑。
          - **新建 `docs/solutions/workflow-issues/visual-feedback-loop-latency.md`**（knowledge track，`problem_type: workflow_issue`，`root_cause: missing_workflow_step`）。目录与 `component: frontend` 均按 corpus-first 规则取自既有三篇 learning 的用法，未新造词。
          - **复盘数字全部现场重算，未沿用记忆**：`git log --since=2026-08-25` 实测 **67 次提交**，含「修/补/纠/撤」任一字者 **31 次（46%）**。此处修正了我先前口述的「36 次 / 54%」—— 那个数字是凭印象给的，实测并集是 31。`code.js` 7340 行改 36 次、探针 5616 行改 **33 次**（近一比一），探针现跑 **310 / 0**。
          - **核心结论（写进文档第一条）**：耗时的根因不是需求变更也不是代码量，而是**「出图 → 挖缺陷 → 补断言」回路启动得太晚**。提交信息自证：「四模态**首次出图**挖出五处缺陷」「24 个变体框**首次逐张出图**挖出四处缺陷」都发生在第 6–7 天，而 8/30–8/31 两天断言数链式增长 `200→220→227→237→239→246→248→249→252→256`，每一步背后都是一次出图挖出的缺陷。**前 5 天在零视觉反馈下堆了几千行生成代码，那段时间写下的缺陷全部在第 6 天以后集中付账。**
          - **必须澄清的一点也写进了文档**：不是那 310 条断言浪费了时间。按钮底线 2px 错位、卡片选中态被挤高 2px、弹层按钮破 44px —— 这几类在缩略图上肉眼看不出来，只有断言能守住。问题在**时序**：同一条断言写在画布之前只值几行代码，写在画布之后要额外付一次全量重跑与一次回改。
          - **`CONCEPTS.md` 词汇捕获（update-only）**：新增「视觉稿实现链」一簇 3 个词条 —— **无反馈期**（返工量与它成正比而非与代码量成正比）、**实机核验**（与离线 mock 互补而非替代，两侧数字都对但不可互换）、**单页闭环**（同时压掉「批量看图注意力摊薄」与「生成器只支持全量重建」两项成本）。按 lightweight 规则只做增补、不做 bootstrap。
          - **两项确定性校验均通过**：`validate-doc-claims.py` 0 flags、`validate-frontmatter.py` OK。首跑曾报一条 FLAG —— 我在文中写 `assets/render/` 而工作区实际路径是 `prototype-figma/assets/render/`，**又是一次「凭印象写路径」**，已按脚本提示补全前缀。这条 flag 恰好印证了文档第七条「记忆挡不住的规则要变成脚本」。
          - **未做的两件事及理由**：① 未编辑 `AGENTS.md` / `CLAUDE.md` 补可发现性行 —— 本仓库根本没有这两个文件（已 Glob 确认），无对象可改；② 未触碰既有三篇 learning —— 交叉引用只在新文档里单向指向它们，改既有文档属 `ce-compound-refresh` 的职责范围。

        **⚠️ 2026-09-02 ㉒：`strokesIncludedInLayout` 缺陷沉淀为第二篇 learning（`ce-compound` 第二次运行）**：
          - **为什么另起一篇而非并入㉑**：skill 硬约束是一次只沉淀一个 learning。我原本打算写「豁免机制守门」，自查 overlap 后否掉 —— 它与㉑第四、五条在问题域、解法、引用文件三个维度重合（moderate），独立成篇后两篇会互相稀释。改选 `strokesIncludedInLayout`：与既有四篇 workflow-issues **零重叠**，且它是 bug track（首次落 `docs/solutions/ui-bugs/`，此前该目录不存在），并带出 CSS/Flutter 实现侧硬口径。**用户裁定采纳此方向。**
          - **新建 `docs/solutions/ui-bugs/stroke-included-in-autolayout-2px-shift.md`**（bug track，`problem_type: ui_bug`，`root_cause: wrong_api`，`resolution_type: code_fix`）。`component: frontend` 沿用 corpus 既有拼写；目录按 schema 的 `ui_bug → ui-bugs/` 映射新建，因既有四篇全在 workflow-issues 下、无一覆盖 UI 缺陷这个域。
          - **⚠️ 写作时核出一处我先前的口述错误**：我此前说按钮受害位置是「`_pub-actions` ×3 + `_actions` ×1 = 4 处」，实读 `code.js:6474-6489` 后修正为 **`_pub-actions` ×4 + `_actions` ×1 = 5 处** —— `items` 数组有 4 条（编辑/下架 ×2、刷新重发/删除、继续编辑/删除），每条各生成一个 `_pub-actions`。文档里按实测的 5 处写。
          - **文档给出一条此前没显式说过的判断依据**：按钮是**无条件**设 `false`、卡片是**按档**设，两种处理相反且都对。依据不是「现在有几个成员带描边」，而是「**这个集合会不会长出新的带描边成员**」—— 按钮六变体是开放集合（随时可能加 outline 变体），卡片三态是闭集合。
          - **Prevention 四条**：① 这类「引擎默认行为」缺陷只有实机核验能查出，故出图通道要早通（指向㉑那篇）；② 离线断言核属性值而非高度差，**且必须在注释里写明它测的是什么** —— 否则下一个人以为高度已被守住，从此不做实机核验；③ mock 逐属性手抄搬运漏一个就是一条假信号，本次靠假红引出**纯属运气**（默认值方向反过来就是假绿，缺陷会被探针背书为已修复）；④ 检索同类风险按「属性组合」扫而非按症状扫，命中 110 处逐类核后只有按钮一族是真受害者。
          - **`CONCEPTS.md` 词汇捕获**：新增 **相邻对照** 一条 —— 小位移缺陷成立的唯一条件是同规格实例边界相接，故判断某处偏差要不要修看的是「附近有没有对照物」而非「偏差多大」。这条正是本次 110 处命中里只修按钮一族的裁定依据，也解释了 `row/*` 16 处与 28 处杂项为何刻意不动。
          - **两项确定性校验均通过**：`validate-doc-claims.py` 0 flags、`validate-frontmatter.py` OK（本篇一次通过，未像㉑那样先报路径 flag）。







- [x] ~~删除被 Word 占用的旧 `docs/PRD.docx`~~（2026-09-02 核查：文件已不存在，此条为过期待办）；

**🟠 第 1-2 周（8/31-9/11）**

- [x] ~~UI/UX 设计稿~~ **已按用户 08-26 决策提前至 M3 执行，不排在本阶段；M3 已于 08-31 经用户正式验收通过（42 框全部出图 + 探针 256/125 全绿）** —— 视觉稿自己做，无外部设计师接棒，故顺序为「先 Token 后像素」：Design Token 落地已于 8/26 完成（条目 [49]，17 项数值升为 Figma Variables，探针当时 58 项全通；**Variables 现为 43 项 = COLOR 26 + FLOAT 17，Token 探针 125 项**），像素精修已于 08-31 收口（限定为「只做全局排版规律」，逐页美感精修按用户批准不做，条目 [70] 至 [70-h]）。**本阶段须一并定论**的四项均已结案：① ~~Accent `#FF8A3D` 与车辆色 `#F97316` 两橙冲突~~ **已于 8/25 结案（条目 [41]）：不换色，限定 Accent 只用于 AI 控件，两橙不同层出现**；② ~~鸭子 IP 造型与主色 `#0B7C8C` 的适配~~ **已于 8/25 定稿（条目 [42]）：方向 4B「负形留白 + 同心波纹」，规格见 PRD §1.4.1.1–§1.4.1.4**；③ ~~其余中性/语义色定稿~~ **已于 8/25 定稿（条目 [43]）：全色板首次实测对比度，测出并修完 6 处违规，新增 `*-text` 三个深色文字 Token，语义色板 13 项→16 项**；④ ~~IP 素材交付~~ **已于 8/25 交付（条目 [45]）：`prototype-figma/assets/` 12 个 SVG，5 件素材全齐，含 App 上架图标；三处代码消费位已接入**；
- [x] **M4-0 环境与阻塞项就位** —— ① Flutter **3.41.9 / Dart 3.11.5 已升级并实测核实**（按 tag checkout，非 `flutter upgrade`）；③ Riverpod 已声明。② 高德开发 Key **待用户申请**（包名已定 `com.s2s.zhaoyazhao.dev`，`build.gradle.kts` 已配，取 SHA1 命令见条目 [62]）；
- [x] **M4-1 聚合性能 POC-A 算法级 → 通过，D2 = 留**（条目 [63]）：均匀分布 1万→5万 = 1.48→4.73ms（×3.21），聚集分布 = 0.63→3.61ms（×5.72），两档均线性。**关键衍生结论：5 万点聚合仅占 300ms 预算的 1.6%，瓶颈在 Pin 渲染而非聚合算法** —— POC-B 若不达标应优化渲染，不要换算法；
- [ ] **M4-1b 聚合性能 POC-B 定标级（安卓真机到位后）**：低端 2GB + 中端各 1 台，装 APK 自行出数（不需连开发机）；结论回填 SLA 实测值。**iOS 侧延后**（Windows 无法出 `.ipa`，属平台限制，已在 PRD §6.10.1 与 §14.6 明示，SLA 本期仅覆盖 Android）；
      ✅ **埋点侧已于 8/28 全部就位（条目 [65]）**：`frame_metrics.dart`（P50/P95/P99 + Max + 六桶直方图 + JSON 导出）、`stress_data.dart`（四档聚集分布压测数据，走真实 Provider 链路）、`perf_panel.dart`（应用内面板，档位入口不受 `kDebugMode` 限制故 release 可测）。**此前该项一直记作「等真机」，实为埋点从未实现，真机到位当天也出不了数** —— 现在剩余依赖只有设备本身；
      ✅ **跑真机前的开发机基准已拦下一处必修缺陷**：Marker 绘制每点耗时在 1 万→5 万点从 25.73µs 反弹到 49.68µs（超线性），5 万点单帧 2483.9ms。修掉 `firstWhere` 的 O(n²) 与 TextPainter 每帧重建后降至 126.9ms（**19.6 倍**）。**不修就上真机会把结论误导向「下调 Pin 上限」** —— 恰是兜底方案的降级动作，造成降级生效而根因未除的假象；
      ✅ **「能出 release APK」这个隐含前提已于 8/29 实测打通（条目 [68]）**：此前 release 构建 100% 失败（中文用户名致 Kotlin daemon 路径转义丢失），即真机到位仍装不上包。已加 `kotlin.compiler.execution.strategy=in-process`，实测产出 `app-release.apk` 102.1MB。**至此 POC-B 的剩余依赖才真正只剩设备本身**；
      ⚠️ **102MB 含三套 ABI 引擎**，真机只用其中一套。不影响帧率故不阻塞 POC-B 出数，但**若将来要用它评估「首次启动耗时」或安装转化，必须先 `--split-per-abi`**；
- [x] **M4-2 Flutter 工程脚手架 + Token 落地 + 路由骨架**（条目 [63]）：`flutter create` 89 文件（含 windows 平台备 desktop 调试）；`design_tokens.dart` 43 项经 `main.dart` 的 `_buildTheme()` 集中接入；路由表 **12 项**（原报 11 项漏了 `ai-confirm-screen`，已修正）；三处必配已落地（`.dev` 包名 / minSdk 24 / `allowNativeHeapPointerTagging`）。**依赖已装（`flutter pub get` 成功），验证三项全通过：测试 7/7、`flutter analyze` 零告警、POC-A 复跑 PASS**；
- [ ] **M4-3 五大 P0 闭环开发**：账号 → 发布 → 首页地图 → 详情 → 联系中转。地图页须落实 PRD:1231 隐私顺序（未同意不初始化，`main.dart` 已刻意留空并注明）与 :1232 `allowNativeHeapPointerTagging="false"`（已配），两项均为 🔴 上架驳回点；
      - [x] **隐私协议门 privacy-gate**（条目 [64]）：PRD §10.1 补页（16→17）+ §6.5 补第 0 步 + 新增 §6.5.1；三态同意状态（连版本号落盘）+ 路由 `redirect` 统一门控 + 两层高德守卫（**合规判据是「未同意不得构建 `AMapWidget`」，非「不调 updatePrivacyAgree」**，实测依据见条目 [64] 一）；
      - [x] **首页地图页 home-screen**（条目 [64]）：降级底图（PRD:1207，路网按米定义随缩放疏密变化 + 明示条）+ 共享筛选态（范围/供需/分类/关键词，放 `features/discovery/` 供列表页复用）+ 聚合渲染（§6.15 分类阈值 + §6.4.2 尺寸三档，单层 CustomPaint）。**高德 `AMapWidget` 分支已就位但未接入，待 Key**；
      - [x] **列表页 list-screen**（条目 [66]，PRD §6.4.3，本期范围如下）：
            **做**：复用 `FilterPanel` 控件本体（不做收起，PRD §6.4.1 末条）+ 排序五档（综合/距离/最新/价格升/价格降，默认距离）+ 卡片（分类色块 + 标题 2 行截断 + 价格 + 距离 + 发布时间 + 分类面包屑）+ 与地图页互跳且筛选态保持 + 空态。**已完成，`flutter test` 35/35、`analyze` 零告警。`FilterPanel` 已从 `features/map/` 迁至 `features/discovery/` 以修正依赖方向**。
            **不做且写明原因**：① **收藏星标**依赖账号体系（登录页尚未实现），做了只能存本地，登录接通后要迁移，等于返工；② **实名/资质小标**依赖 §6.9 信任体系，M4 未排；③ **筛选抽屉的「仅看认证」**同 ②；④ **「联系 TA」小按钮**指向联系中转页（本批次内后做），先不放入口，避免点了跳占位屏；⑤ **位置文案**（如「文三路 · 500m」）需逆地理编码，依赖高德 Key，先只显示距离。
            以上五项在账号/信任/Key 到位后补齐，届时卡片布局已定，只是填充字段。
      - [x] **详情页 detail-screen**（条目 [67]，PRD §7.4.1 / §7.4.3 / §7.7 / §9.8，本期范围如下）：
            **做**：媒体占位区（分类色渐变）+ 信息区（供需标 / 实名标 / 三级面包屑 / 需求态标题前缀 / 价格与可议价 / 地址 / 剩余有效期）+ 模板字段区（§7.7 按序 key-value）+ 描述区 + 发布者信任卡（§9.8 完整度三档）+ 底部固定主按钮（资源态「联系 TA」/ 需求态「响应需求」）+ 需求态「我能帮忙」入口 + 已下架/不存在态。**同步对齐 §13.2 价格字段契约**（`priceValue`/`priceLabel` → `price` + `priceUnit`）。**已完成，`flutter test` 46/46、`analyze` 零告警**。
            **不做且写明原因**：① **媒体轮播**（`post_media` 未建模，且需 CDN）；② **详情小地图**（依赖高德 Key）；③ **收藏/分享**（依赖账号体系 + 已备案域名）；④ **举报**（属联系中转页范围）。「联系 TA」按钮做出来但只跳到中转页占位屏 —— 不放会让页面缺掉最主要的行动点，验收时看不出真实视觉重量。
            ⚠️ **已知架构欠账**（条目 [67] 一）：§2.4 三级分类树仍未建模（详情页靠 `categoryPath` 临时缓解）、`status` / `restricted` 两字段全无。用户已定「UI 优先」，此项**延后而非取消**。
      - [x] **联系中转页 contact-screen**（条目 [69]，PRD §7.4.2 / §7.7 / §7.8 / §12.3 / §14.3，本期范围如下）：
            **做**：隐私保护说明区（置顶不折叠）+ 联系方式卡片（单轨，仅展示发布时填的那一种；脱敏 → 点击拉取完整值）+ 五种具名失败态（未登录 / 限频 42902 / 熔断 42903 / 对方未填 / 网络）+ 外呼 `tel:` 及**失败自动降级为复制**（§7.8）+ 微信号复制 + 安全温馨提示 + 举报原因面板（§7.7 五原因）+ 信息不存在态。**已完成，`flutter test` 56/56、`analyze` 零告警、release APK 复验通过（102.7MB）**。新增依赖 `url_launcher: ^6.3.2`。
            **不做且写明原因**：① **真实三维限频**（§7.7 账号 30 / 设备 30 / IP 100 每日）计数必须在服务端，客户端计数可被清数据绕过 —— 只把超限与熔断建成可展示错误态，让文案与排版能被验收；② **联系事件落库**（§7.7 北极星指标唯一统计点）需 `contact_event` 表；③ **举报提交**（§9.10.3 风险分累计）需服务端，面板做出来、提交动作留 TODO —— 面板不做，§7.8「尝试举报让其补充」这条提示就指向一个不存在的入口。
      - [x] **登录页 login-screen + 三级分类树建模**（条目 [71]，2026-08-31 完成，PRD §3.4.1 / §2.4 / §12.2 认证接口清单，本期范围如下）：
            **为什么这两件事同批**：分类树是发布页的硬前置（没有叶子类目就取不到模板 Schema，级联选择器也无数据源），而它同时是条目 [67] 记下的已知架构欠账（详情页至今靠 `categoryPath` 字符串临时缓解）。**先还清它，下一轮发布页才能专注模板引擎本身**，否则发布页会一边建树一边填模板，两件事互相干扰。登录页则是三处已埋 TODO 的共同前置（list 收藏 / detail 收藏 / contact「未登录」失败态），一页解锁三处。
            **做**：① 分类树按 §2.4 全量建模（5 一级 → 二级 → 三级叶子，含大类色绑定与**高敏标记**，§2.9 末条）+ 版本号字段（§2.9「版本号 + 本地缓存」）；② 详情页面包屑改为消费分类树，`categoryPath` 临时缓解退场；③ 登录页按 §3.4.1 逐条落地（鸭子 Logo + slogan + 手机号 + 短信验证码 + 60s 倒计时 + 协议默认不勾必须手勾 + 「未注册手机号验证后自动注册」+ 第三方入口灰禁用 + 主色胶囊 80% 宽主按钮）；④ 本地会话 `AuthSession`（登录态跨页共享，走 Riverpod 不用 `setState`）+ 具名失败态（**§12.2 末条 `40105` 连续 5 次失败锁定 15 分钟** / 验证码错误 / 验证码过期 / 网络失败 / 协议未勾）。**已完成，实测 5 一级 / 21 二级 / 48 叶子（§2.3 上限 60）；`flutter test` 111/111、`analyze` 零告警；顺带接线 §7.7 联系中转页登录门（「去登录」+ 登录后自动重试）**。
            **不做且写明原因**：① **真实短信下发与校验**（`/auth/sms/send`、`/auth/sms/login`）无服务端，验证码校验只能在本地 —— 与前四页同一处置：把**失败态全部做出来**让文案与排版可验收，校验动作留 TODO 指向接口；② **图形验证码**（§3.4.1 主方案含）依赖服务端出图与核验，客户端自绘等于自己出题自己判卷，无防刷意义；③ **密码登录**（§3.4.1 次方案「可折叠切换」）与 `/auth/password/login` 同样无服务端，且短信主方案已能走通闭环，本期不做折叠区；④ **注销/改手机号**（§3.4.3 账号与安全）属设置页范围，M4 未排；⑤ **拼音首字母与同义词映射搜索**（§2.9）本期不做 —— 级联选择器三列直选已能覆盖发布闭环，搜索是效率优化而非闭环必需，且同义词表属运营配置；⑥ **强制资质拦截**（§5.8「叶子类目 need_cert 非空则拦截」）本期只在树上**打好高敏标记**，拦截动作在发布页做（cert-modal 属发布批次）。
            ⚠️ **本轮未接 list / detail 两页的收藏**：Grep 核实两处注释标注为「依赖 `favorite` 表建模（§13.2 以 user_id 为键），本期不做」而非 TODO，故不擅自扩范围（规则 4.2）。登录态已就绪，`favorite` 表建模时直接接。
      - [x] **M4-3a 发布主链路**（条目 [73]，2026-09-01 完成，PRD §5.4.1 / §5.4.2 / §5.4.3 / §5.7 / §5.8 / §4.4 认证浮层）：模板引擎（叶子 → Schema）+ 分类级联选择器 `category-selector` + 发布页 `publish-screen` + 认证拦截浮层 `cert-modal`。前置分类树已于条目 [71] 就绪。
            **做**：① `publish_template.dart` 模板引擎（§5.4.3 四份模板 + 通用降级 + `templateForLeaf` / `requiredCertForLeaf`）；② `category_selector_screen.dart` 三列级联 + 高敏资质提示前置 + 面包屑；③ `publish_form_state.dart` 不可变表单态（九档具名 `PublishBlocker` + §5.8 八条边界 + 四个字面量契约常量）；④ `publish_screen.dart` §5.4.1 八段表单 + 模板附加字段段 + 顶部/底部双主按钮同步禁用 + 禁用原因常驻；⑤ `cert_modal_screen.dart` §4.4 认证拦截半透明抽屉；⑥ 路由三条接线（publish / category-selector / cert-modal）。**已完成，新增 68 项测试（模板 21 + 校验 34 + 页面 13），`flutter test` 179/179、`analyze` 零告警**。
            **不做且写明原因**：① **地图选点与真实定位**（依赖高德 Key）—— 按钮**保留但禁用**，另给「使用当前位置（演示）」走通校验；隐藏按钮会让验收无法判断是漏做还是待前置；② **媒体上传**（依赖 §13.2 `post_media` 与 CDN）—— 只呈现 `0 / 9` 与上传区占位；③ **记忆机制**（§5.7 需云端）—— 模板留 `memoryHint` 展示位但文案写「示例：」而非「记忆带入」，以免误认为已实现；④ **未实名每日 1 条 + 先发后审受限态**（§5.8 首行 / §3.7）—— 判定与计数在服务端，客户端自计清缓存即失效；⑤ **Schema 服务端下发**（§12.3）—— 无服务端时解析代码没有真实响应可测；⑥ **拼音首字母与同义词搜索**（§2.9，延续条目 [71] 同一理由）。
      - [x] **M4-3b AI 确认与完成页**（条目 [74]，2026-09-01 完成，PRD §5.9 / §5.10 / §5.11 / §9.8 / §8 T6-④）：`ai-confirm-screen`（逐字段 AI 猜测值 + 「AI 猜」角标 + 升 🟢 冲刺区）+ `publish-success-screen`（完整度卡）+ 完整度三档判定模型（§9.8 唯一判定处）。
            **做**：① `publish_completeness.dart` 完整度三档判定（§9.8 三条件 → 🟢🟡🔴 + 权重 ×2/×1/×0.5 + `missing` 差项清单 + §8 T6-④ 权益三条 + §5.10 每日配额 3·3·10·20 与高敏 +10）；② `ai_guess.dart` 猜测数据形态（`FieldGuess` 三来源 + `AiParseStatus` 五态承载）；③ `ai_guess_builder.dart` §5.10 Step 2 生成与 **Step 4 逆向比对**（独立函数，非内嵌）；④ `ai_confirm_screen.dart` §5.9 五态 + 逐字段「AI 猜」角标与来源标签 + 逐项修改 + 升 🟢 冲刺区（补门牌号实时跳档）+ 配额说明 + 「全部重填」；⑤ `publish_success_screen.dart` §8 T6-④ 完整度卡 + 差项清单 + 权益三条 + 有效期说明 + 两处出口；⑥ `category_tree.dart` 新增 `categoryPathLabel` 收口面包屑拼法（修掉 `join` 拼对象的真 bug）；⑦ 表单补 `doorNumber` 与 `completeness`；⑧ 路由两条换真实页面 + 发布页临时 AI 入口。**已完成，新增 77 项测试（完整度 24 + 猜测 23 + 确认页 17 + 完成页 13），`flutter test` 254/254、`analyze` 零告警**。
            **不做且写明原因**：① **真实 AI 解析**（§5.10 Step 1/Step 3 需大模型服务端）—— 本期落地的是**可验证的那部分**：猜测值只准来自「用户已填」或「分类模板」，并用 `reverseScopeCheck` 反查越界；`neighborMode`（据附近同类）无数据源，一律判非法；② **语音 / 图片 / 一句话三种输入模式**（§5.9 T2 四模式）—— 依赖 ASR 与 OCR 服务端，且入口在未排的首页 FAB；③ **配额真实计数**（§5.10）—— 服务端事项，客户端只显示上限不显示剩余（自算剩余清缓存即「恢复满额」）；④ **§5.11 补全引导（编辑态回填）**—— 需「我的发布」列表页与草稿持久化，M4 未排；完成页「回去补齐」故意写「约 30 秒」而非承诺保留现场；⑤ **「取当前定位门牌」**（依赖高德 Key）—— 按钮保留但禁用，另给手填框以使 🟢 档在本期可达。
            ⚠️ **入口偏差与用户裁定（2026-09-01）**：§5.9 的确认页是「T2 四模式共用」的强制态，正路入口是 §6.13 T6-① 首页 FAB「AI 帮我发」→ 弹四模式，而**该 FAB 属 §6.13，M4 未排、地图页 Grep 零命中**。若把手动填表单链路也强塞进确认页，页面会显示「AI 已识别」这类不真实文案（手动填根本没有 AI 猜测）。三个选项交用户裁定，**用户选方案 A：发布页加临时「AI 帮我补齐」入口**，手动填完仍直达完成页。**该入口是 §5.4.1 未定义的临时元素，FAB 做出来后须移除** —— 已在 `_TempAiEntry` 类 doc、`_openAiConfirm` 方法 doc 与本条三处登记，避免它变成没人知道来源的既成事实。
      - [x] **M4-3c 稿 ↔ 码对数收口**（条目 [75]，2026-09-01 完成，PRD §10.1 / §5.8 / §8 T6-④）：M4-3b 收口后再核 UI 闭环，查出**条目 [72] 那条盲区换了一条边再犯** —— 新加的 4 条断言守的是 PRD ↔ Figma，而 **Figma ↔ Flutter 这个方向从来没有判据**，于是 M4-3b 有意改的两处（完成页补齐按钮文案、第二出口跳转目标）只改了码没回改稿，探针一条不报。
            **做**：① 改稿对齐代码（补齐按钮「立即补齐」→「回去补齐」，用户裁定）；② 第二出口在代码侧**登记为临时降级**（PRD §5.8 明文「默认跳『我的发布』」，稿子忠于 PRD 故不改，是代码偏了 —— `my-publish-screen` M4 未排、路由表无此项，故暂落 `profile`，与 `_TempAiEntry` 同一处置：写明移除条件）；③ 补**最小一条** Figma ↔ Flutter 对数守门，判据两侧各取真源（`code.js` 与 `lib/`），并配临时降级备案的反向守门（备案项补齐后须删，否则永绿豁免）。**已完成，离线探针 260 → 262 / 失败 0、Token 探针 125/0、`flutter test` 254/254、`analyze` 零告警；反向验证三轮（稿侧改回 / 备案过期 / 码侧改文案）各报 1 条并已恢复**。
            **不做且写明原因**：① **全量元素对数**（不只按钮）—— 需要一套稿↔码元素映射表，那张表会成为第三份要维护的副本，判据一旦比被判对象更难维护，就会在赶工里被放宽（原则 133）；② **未实现页的对数**（`wiredPages` 只列已实现两页）—— 码侧没有真源可取，硬列进来只能靠豁免，等于自造永绿项；③ **`privacy-gate` 补稿**仍在 M4-4（与隐私政策同批，条目 [72] 三的理由未变）。
            ⚠️ **第二处矛盾的性质与第一处不同**（读 PRD 时才发现，故中途二次向用户确认）：`docs/PRD.md:899` 明文规定「发布成功跳：默认跳『我的发布』」，稿标注也写了该依据。若照「改稿对齐代码」一律执行，等于让 Figma 稿去违背 PRD —— **那会把「M4 未排页导致的临时妥协」洗成看起来正式的设计决定**，下一个人会以为 PRD 改过口径。故只改文案那一处的稿。
      - [x] **M4-3d 补 `privacy-gate` Figma 稿**（条目 [76]，2026-09-01 完成，用户指令提前执行；条目 [72] 备案的唯一 UI 缺口，🔴 上架驳回红线）：`code.js` 加 `buildPrivacyGate()` + `MAIN_SCREENS` 登记 + `COVERAGE_STATES` 补受限态条目 + `FLOW_LINKS` 补两条边，**并从 `probe-layout-offline.js` 的 `KNOWN_STAGE_GAPS` 删除该项**（不删则「已备案缺口清单无过期项」那条断言会报红，这是刻意设计的提醒机关）。
            **⚠️ 读真源后修正 8/31 那条待办的两处前提**（原文写的是「`COVERAGE_STATES` 两档变体 + `FLOW_LINKS` 三条边」，逐条核对后不成立）：
            ① **变体只补一档，不是两档**。原计划写「同意态 / 受限态两档变体」，但**同意态就是主态本身**（`privacy_gate_screen.dart` 的 `_AgreementView` 是默认视图，`_DeclinedView` 才是分支）。再单画一张「同意态」变体等于把主态抄一遍，与条目 [70] 记的「重复稿」同一个错；
            ② **连线补两条，不是三条**。原计划的第一条 `splash → gate` **与既有的 `splash-screen → login-screen` 冲突** —— `FLOW_LINKS` 第二列是节点精确名，`_splash-tap` 只有一个，一个触发点不能连两个目标（`findClickable` 命中唯一节点、`link()` 用 `setReactionsAsync` 覆盖写）。按 §6.5.1 冷启动顺序，正确做法是**改**这条边的目标为 `privacy-gate`（协议门早于登录），再补 `gate 同意 → login`。故本轮是「改 1 条 + 补 1 条」而非「补 3 条」；第三条 `gate 不同意 → 受限态` 的目标是变体画框，而 `FLOW_LINKS` 两端必须是 `MAIN_SCREENS` 登记的主态（探针有此断言），**故这条不进 `FLOW_LINKS`**，受限态靠变体画框并列出图供评审对照。
            **做**：① `buildPrivacyGate()` 主态（标题 + 四段正文：引导句 / 三条收集清单 / 高德 SDK 明示 / 不同意的后果 + 「同意并继续」+ 「不同意」+ 八行红线标注）；② `buildPrivacyDeclined()` 受限态变体（PRD §6.5.1「受限态不是空白页」）；③ 三表登记（`MAIN_SCREENS` 18→19、`CORE_PAGES` 14→16、`COVERAGE_STATES` 4→5 项）；④ `FLOW_LINKS` 改 1 补 1（23→24 条）；⑤ 探针四处（删 `KNOWN_STAGE_GAPS` 唯一项、`wiredPages` +2 行、`tailPages` +1 行、`new Function` 导出 +2）；⑥ `ui.html` 批次 3 页名清单同步。**已完成，离线探针 262/0（含新纳入的稿码对数 2 页 → 4 页、贴底 6 页 → 7 页）、Token 探针 125/0、`analyze` 零告警；反向验证四轮（删 `MAIN_SCREENS` 登记 → 4 条报红含 `screen()` 当场抛错 / 留备案 → 1 条 / 改稿侧受限态按钮文案 → 1 条 / 改连线触发点名 → 2 条），全部恢复**。
            **不做且写明原因**：① **协议正文全文上稿**（PRD §6.5.1 明写「协议文本走在线 URL，不内嵌写死」）—— 稿上放全文等于把「文本可远端更新」这条设计判断画反；② **《用户协议》《隐私政策》可点入口**—— 码侧要到 M4-4 政策上线后才有真实 URL，现在画可点链接是稿超前于码，正是 M4-3c 刚立起来的稿码对数要防的事；③ **`gate → 受限态`连线**（理由见上 ②）。
            ⚠️ **两个新框尚未实机出图**（M3 的 r67–r74 轮次在补稿之前）：下次跑 Figma 插件时按 1→2→3→4→5 整体重跑并补图，验收看形（主态四段正文不溢出、两键贴底；受限态居中、非空白页）。离线探针管结构与文案，管不了观感。
      - [x] **M4-3e 设计系统交付形态收口**（条目 [77]，2026-09-01 完成，用户裁定「全部做完含 Component Set」+「独立文档 + 生成器」）：用户问「UI 设计稿（Figma）、《设计系统/组件规范》当前是否具备」，读四处真源后判定 —— **稿的内容具备（19 页零缺口）、规范的内容也基本具备（Token 43 项 + 20 条组件契约 + PRD §1 共 351 行），但两者的交付形态都不合格**。
            **✅ 已完成，分五次提交**：`1946d94`（第一层设计裁决，探针 270/0）→ `70ff9a2`（e3 样式注册，276/0）→ `50f6867`（e4 Component Set，286/0）→ `c4d4b71`（e5 规范文档生成器，297/0）→ e6 收口（全量验证 + 回写条目 [77]）。**末轮实测：离线探针 297/0（M4-3d 基线 262，本轮新增 35 条）、Token 探针 125/0、`flutter test` 254/254、`analyze` 零告警；反向验证四轮全部报红并恢复**。产物 `docs/设计系统与组件规范.md` 共 11 章，计数自报 `TOKEN 43 + CONTRAST 34 + MOTION 6`，**34 个现算比值与 PRD §1.4.2 手写表逐行完全一致**。
            ⚠️ **e6 查出一条已红三轮的断言**（`field() 开放了 width 参数`）：不是 e5 引入，是第一层给 `field()` 补第四参 `opt` 时打断的（判据钉死了整条签名），而 e2e/e3/e4 三段我都只跑离线探针没跑 Token 探针。已把判据改为只核前三个形参并反向验证，且扫过全文确认无同类脆判据。详见条目 [77] 五。
            ⚠️ **本轮改动尚未实机出图**：Component Set 牵动全画布 44 框的 Instance 引用，下次须按 1→2→3→4→5 **整体重跑**（不可只跑 2/3/4，master 缺失会退化成散 Frame），**121 张渲染图可能有一部分要重出**。
            **⚠️ 先修正我自己上一轮的判断**：我最初答「内容已齐，只差换个壳」。读真源后此话**只对 Token / 按钮 / 卡片 / 骨架屏成立**，对 PRD §1.7 点名的另三个组件不成立：
            ① **标签/徽章不存在事实上的规范** —— 41 处写法互不相同：上下内边距 4 种（2/4/8/12）、左右 5 种（4/8/12/16/24）、圆角 `full` 与 `sm` 混用、小圆点 8/9/10/12 四种边长、「一排选一个」的非选中态有三种画法（无填充无描边 / 无填充+border / background 填充）。**不存在能覆盖多数用例的众数**，故从这 41 处「整理」出的任何单一规格都是**新拟的设计**，不是对现状的归纳；
            ② **`card()` 的 `tag` 槽位一位三用** —— 同一个 `caption/primary` 无底文字在三处承载三种语义：供需属性（`资源`）、生命周期（`已下架`）、时间新鲜度（`今天更新`），稿上无任何手段区分；
            ③ **输入框是两套自洽规格而非一套** —— `field`/`selectField`（44 高 / R-md / surface / body）与 `navSearchBox`（32 高 / R-full / background / small）在 5 个维度不同，且**聚焦态 / 错误态 / 禁用态全稿皆无**；
            ④ **空态样本量为 1** —— `listEndRow` 4 处参数自洽可直接成规格，但「真正一条都没有」的空页只有 1 个内联实例，且鸭子插画有 40 / 64 / 96 三种体量。
            **⚠️ 再修正一次（2026-09-01 e0 逐字读完 11 处后）**：上条 ① 里「41 处写法互不相同、不存在众数」这个结论**举证有误**，须以本条为准。11 处并非同类，而是**四族性质不同的东西被混在一起数**：
            - **A 可点选择胶囊 4 处**（`_radius-*` / `_sd-*` / `_lv1-*` / `_mode-*`）：内边距 3 种（`TIGHT_GAP+sm`、`xs+md`、`xs+sm`）、非选中态 **3 种画法**（无填充无描边 / `background` 底 / 无填充 + `border` 描边）—— **只有这一族是真不一致，也只有它需要「归一」**；
            - **B 静态标记 1 处**（`_ai-tag`：`TIGHT_GAP`+`xs` / `RADIUS.sm` / `accent` 底）—— **样本量 1**，只能「立规格」，无从归纳；
            - **C 地图浮层提示条 2 处**（`_map-caption` / `_hint`：均为 `xs`+`sm` / `surface` 底 / `RADIUS.sm` / `primary-dark` 字）—— **彼此已完全一致**，只需登记，不需改一个字；
            - **D 被我误算成标签的按钮与容器 4 处**（`_sheet-reset` / `_sheet-confirm` / `_filter-summary` / `_overlay-cats`）—— 圆角虽是 `full`，语义却是按钮与容器。其中 `_sheet-reset` / `_sheet-confirm` **是手搓的按钮、绕过了 `BUTTON_SPECS` 真源表**，这是另一个问题（真源表被旁路），不是标签不统一。
            据此，我先前举的「圆角 `full` 9 处 vs `sm` 3 处」这条证据不成立 —— `sm` 那 3 处里有 2 处（C 族）根本不是标签。
            **用户 2026-09-01 裁定：仍照 11 处执行**（而非只收 A 族 4 处）。为避免规范文档里出现自相矛盾的定义，`TAG_SPECS` **按上述四族分栏承载**，而不是把语义不同的四族硬塞进同一条胶囊规格：A 族归一取值、B 族立规格、C 族原样登记、D 族在表内标注「此族指回 `BUTTON_SPECS`，本表只登记不定义」。
            **做（分三层，每层各自验证后提交）**：
            **第一层 · 设计裁决（改画布，必须先做，后两层依赖其终值）**：① 建 `TAG_SPECS` **四族分栏**收口 11 处 —— A 胶囊型（可点、一排选一个：上下 `xs` / 左右 `sm` / `RADIUS.full` / `caption`，选中 = 主色或 `cat-deep` 底白字、未选中统一为 `background` 底 `text-secondary`，**三种非选中画法归一**）、B 静态标记型（不可点：上下 `TIGHT_GAP` / 左右 `xs` / `RADIUS.sm`）、C 浮层提示条（登记现状，零改动）、D 按钮族（**本表只登记不定义**，规格一律指回 `BUTTON_SPECS`；其中 `_sheet-reset` / `_sheet-confirm` 是绕过 `BUTTON_SPECS` 的手搓按钮，**改走真源表列为遗留项、本轮不做** —— 用户未选「顺带改走」那档，且改它须给 `BUTTON_SPECS` 补一档「满宽 grow + full 圆角」从而使 master 数由 20 变 22，会与第二层 Component Set 的改动叠在一起，风险不该合并）；② 建 `DOT_SIZES` **显式登记**四档圆点及各自用途（8 未读 / 10 状态与完整度 / 12 Pin 角标）—— 是「登记」不是「硬统一」，8px 未读点与 12px Pin 角标本就不该同尺寸，但把 `completenessTag` 派生的 9px 并入 10px 一档（同页两种色点一个 9 一个 10 会被当成两套语义，`code.js:3535` 注释自己承认过且只修了一处）；③ 拆 `card().tag` 三语义为三个具名槽位（**用户 2026-09-01 裁定：仍保持纯文字、不套容器** —— 这三处传的是「这条信息的元数据」而非可点标签，套上 `_ai-tag` 那种静态标记容器会把它提到与标题同级的视觉权重，且列表卡是 M3 验收面积最大的一处。故本项**只拆参数、画面零变化**，规范里写明它们属「卡片右侧元数据」而非标签组件）；④ 补输入框 `focus` / `error` / `disabled` 三态并把两套规格显式化为 `FIELD_SPECS`；⑤ 建 `emptyState()` 构造器并显式登记鸭子三档体量各自的理由。
            **第二层 · Figma 库形态（改组件体系，牵动全画布 Instance）**：⑥ 注册 6 档 Text Style 与 Paint Style（**当前样式面板是空的** —— 设计师点不到「h1 样式」，只能点到 `size/h1` 这个数字变量，这是对外交付时最先被发现的缺口）；⑦ 20 个扁平 master 合成 Component Set 带 `variant` 属性（按钮 6 档 / Tab 3 态 / Pin 10 种）；⑧ 探针 mock 补 `combineAsVariants` 与 `componentPropertyDefinitions`（**离线探针现在是这一项的盲区**，不补则做完无从验证）。
            **第三层 · 规范文档（只读上游，零改画布）**：⑨ 写 `export-spec-doc.js`，从 `code.js` 真源现算出 `docs/设计系统与组件规范.md`（Token 43 项 + 字阶 6 + 按钮 6 态 + 卡片 3 态 + 骨架屏 + 动效 6 档 + 20 条组件契约 + 26 行对比度实测表 + 第一层新增的标签/输入框/空态规格），沿用 `export-dart-tokens.js` 已验证的「figma mock + eval 取真源表」模式；⑩ 产物头部标「勿手改」+ 探针断言比对，与 `design_tokens.dart` 同为**单向出口**。
            **⚠️ 第三层开工前扩了两处（2026-09-01 e5 读真源后发现计划前提有误，用户当场裁定，两项均取推荐档）**：本层原写「只读上游、零改画布」，但逐项核真源后查出**两处根本没有真源可读**，若硬按原计划执行，生成器就得自己手写这两处内容，等于在产物里放一份无人守门的副本（原则㊾）：
            - **⑨-a 对比度 26 行**：`code.js` **无任何对比度算法与组合清单**（唯二的 `lumOf` 实现在 `probe-layout-offline.js:1151` 与 `color-preview.html:152`，两处都只算自己那几组）。清单只存在于 PRD §1.4.2 的手写表里，改色时不会有人回算。**裁定：在 `code.js` 补 `CONTRAST_PAIRS` 登记表**（只登记「前景 / 背景 / 阈值 / 判定 / 豁免理由」，**比值一律由生成器带 `lumOf` 现算，表内不写任何数字**），探针加断言比对生成器算值与 PRD 表一致。这样将来任何调色都会让比值自动变、并被探针打红；
            - **⑨-b 卡片三态**：PRD §1.4.7 `:343` 写三态，`card()` 只实现了正常态，**选中态与下架态稿内零实现**。**裁定：在 `code.js` 补 `CARD_STATES` 登记表**并如实标注「这两态本轮未出稿、实现侧按此表构造」，与 Pin 那两态的声明同一体例，不留静默缺口。
            两项都是**登记表**（约 50 行），不改任何画面、不动 Instance，故不牵动 121 张渲染图；本层「零改画布」这一条仍然成立。
            **不做且写明原因**：① **Pin 补全 30 种组合**（现 10/30）—— 选中态与完整度角标态属结构差异，而 **Figma 禁止对 Instance 增删子节点**，这是 API 限制改不了，规范里如实声明「这两态不走组件、实现侧自行构造」；② **暗色模式**（PRD §1.7 明写本期不做）；③ **动效进 Variables**（`createVariable` 只收 COLOR/FLOAT/STRING/BOOLEAN，无时间与缓动类型，条目 [48] 已查明），动效仍留 annotation；④ **多行输入（textarea）**—— 稿内零实现，且发布页「一句话发」模式当前就是四个 44 高单行框，补它属改产品形态而非补规范。
            ⚠️ **两处已知代价**：① Component Set 会牵动全画布 44 框的 Instance 引用，须整体重跑 1→2→3→4→5 并复验，**已有 121 张渲染图可能有一部分要重出**；② 第一层回改旧调用点会改动**已经用户验收过的画面**（M3 于 8/31 验收）—— 故每一处回改都须在注释里写明「改前值 → 改后值 + 依据」，让验收过的画面变更可追溯，不能静默改掉。
            ⚠️ **优先级如实回报**：这两项交付物对 9/30 内测包与商店上架**都不是阻塞项**。而 P1–P5 行政项当前零进度（软著 8/25 就该提，现提最早 11 月末下证）、M4-4 / M4-5 未动。此项是用户明确裁定要做，故执行，但不改变 P1–P5 的紧急性。

- [ ] **M4-4 P6 隐私政策上线**（挂自有域名，须列明高德 SDK）；**`privacy-gate` 补稿已提前于本轮 M4-3d 完成**（用户 2026-09-01 指令），此处只剩政策文本本身上线与在稿/码里接上真实 URL；
- [ ] **M4-5 出带签名的 APK 能力**：M4.1 的 P7 备案需 APK 签名信息（MD5/公钥/SHA-1），故 M4 不止要跑通、还须能出**正式签名包**；正式签名证书一旦启用不可更换（见上架前置手册 P7）；

**🟡 第 3-6 周（9/14-9/30）**

- [ ] **P7 工信部 APP 备案**（需 APK 签名信息，依赖可出包）；
- [ ] **P8 注册各渠道开发者账号**；
- [ ] 后端服务：发布/地图/分类/认证/通知/合规；实名认证供应商接入；
- [ ] 我的模块（降级版：列表 + 基础操作，无双向推送）；
- [ ] **9/30 内测包交付**：蒲公英/TestFlight 可安装、五大 P0 闭环可用；

**⏸ 上架前补齐（10-11 月）**

- [ ] 运营治理后台 7 页 + 4 角色 RBAC + 审计留痕（§2.7 本期不做，上架前必须补）；
- [ ] **P9 公安联网安全备案**（上架后 30 日内，法定强制）；
- [ ] **P10 安全评估报告**（华为对社区类强制）；
- [ ] 软著下证后提交各商店审核（预计 11/25）；

**📋 尚未安排（Batch2/Batch3，上架后）**

- [ ] 提交三大文档进行评审（BRD 9 处 + MRD 7 处 + PRD 15 处 + D1/D2/D3 决策 + 11 处一致性偏差均已落地）；
- [ ] LR+GBDT 匹配（**须先评估《互联网信息服务算法备案》**）+ S1 双向推送；
- [ ] AI 智能发布 3B 小模型（`ai-confirm-screen` 骨架已备，待接模型）；
- [ ] 动态蜂窝半径引擎；
- [ ] 单城试点：种子内容导入、种子用户招募、密度监控；
- [ ] 迭代：首次联系触发率 < 30% 则调权重/调分类/调模板。

### 2026-09-03（服务端架构设计定案 · 三份架构文档交付）

- [x] **PRD 缺陷 14 条逐条定案并落地**（条目 [78]，2026-09-02 至 09-03）：上一轮 ce-ideate 交付的架构工件被用户判为「比较难理解」，追因时发现根因不在表达而在 PRD 口径本身存在缺陷。14 处缺陷分四类逐条讨论、当场落地，**累计 21 处 PRD 编辑**。其中三条影响面最大：
      ① **两 ID 分工写死**（第 2 条）：`interaction_id` 客户端生成、覆盖缓存→网络→聚合→渲染四段、经 `X-Interaction-Id` 透传，服务端**禁止**用于鉴权/限流/幂等；`request_id` 服务端生成、是 UI 报错唯一回显值。并明文声明「**缓存命中的会话无 `request_id` 是预期行为，不是数据缺失**」—— 不写这句，它必然被当 bug 上报；
      ② **Pin 响应格式改紧凑数组**（第 4 条）：原文「500 Pin 上限」与「单响应 ≤10KB」**无法同时成立**（对象式 JSON 单 Pin 约 118 字节 × 500 = 57.6KB，是 10KB 的 5.8 倍）。改为 `schema` 字段序字典 + 二维数组、枚举转 int、坐标 5 位小数后，原始约 22KB、gzip 后 4-5KB，对 10KB 有一倍余量。同时**聚合切换点由「点数 >500」改为「zoom 层级」** —— 按点数切换恰好在体积临界点翻转，两个约束互相抵消，达标区间为空；
      ③ **Hard Filter 粒度放宽为二级类目**（第 14 条）：三级放宽为二级（同城/同二级/同供需态），三级降为精排软特征（同三级 ×1.0 / 同二级不同三级 ×0.7）；配套给 `post` 加 `l2_category_id` 冗余列，避免读路径 join `category` 回溯父级。
      **一条主动纠错**：我原把「日志保留期 30 天 vs 90 天」列为阻塞项，读 `docs/PRD.md:2389-2401` 后发现 `:2394` 的 30 天指**中转脱敏日志**、与埋点事件表是两个不同对象，冲突不成立，改为增补一行而非改写既有行。

- [x] **服务端架构 10 项决策定案**（条目 [79]，2026-09-03，ce-brainstorm 全流程）：产物 `docs/plans/2026-09-03-1051-feat-server-architecture-baseline-plan.md`（需求态统一计划，Ready for Planning 四检全过、阻塞项为空），18 条 R-ID 分四组承载。
      **选型七项**：Java 17 + Spring Boot 3 单体（不拆微服务）**［注：JDK 已于 2026-09-04 升至 21，见条目 [85]，本行保留当时原貌］** / MySQL 8.4 LTS 唯一持久化 / Caffeine + Redis 7 两层缓存 / GBDT 本地 LightGBM 自训 + 文本解析调外部 API / 阿里云 ECS 经济型 e 2 核 2G 99 元档 + OSS / Docker Compose 三容器 / 可观测性先落 MySQL 并预置迁移条款。
      **架构组织四项**（这一组是用户纠偏后补的，见下）：按功能域垂直切 `auth`/`post`/`map`/`category`/`contact`/`ai`/`cert`/`notify` + `common`，与 `lib/features/` 同构 / MyBatis-Plus（网格集合查询写 XML 附 `EXPLAIN`）/ 响应包装器 + 全局异常处理器 + 错误码枚举 / Spring `@Scheduled` 单机调度。
      ⚠️ **用户三次纠正我的不足，逐条记下以免再犯**：
      ① `讨论完了吗？各种技术选型，采用什么架构，貌似都不太知道` —— 我前 8 项**全是「用什么产品」（选型），完全没碰「怎么组织」（架构）**。选型清单不等于架构设计，这是本轮最大的方法错误，据此补出上述架构组织四项；
      ② `你需要提供成本和实现复杂度的评估，我才能决策` —— 我原选项只给方案不给数字，无法支撑决策。补齐「一次性投入 / 月度运行成本 / 实现复杂度 / 卡点」四栏对比表后才定案；
      ③ `模型这块如果自己训练会怎么样` —— 我原选项未覆盖自训路径。查证后拆为两件性质相反的事：**GBDT 只能自训且成本几乎为零**（标签来自 `contact_event` 客观行为，无需标注；CPU 推理无需 GPU），**文本解析自训比调 API 贵十倍且存在鸡生蛋问题**（语料要从上线后真实数据来，但发布功能又需要解析能力才好用，Batch2 时点物理上不可行）。
      **两处查证纠正了我的错误前提**：① 我原假设服务器是 1 核 2G，实为 **2 核 2G 且 99 计划新老同享续费同价**；② 我原以为「MySQL 做不了向量」，实为三条路径都存在但各有当期迈不过的门槛（9.x 有 `VECTOR` 类型但**无 ANN 索引**只能全表暴力比对 / MyVector 插件有 HNSW 但需自编译且**不支持 Windows 构建**、开发环境跑不起来 / 云厂商增强版需买托管 RDS、与 131 元/年不在一个量级）。故 R2 是「推迟到 Batch3」而非「做不到」。
      **内存预算贴顶是本轮最硬的约束**：MySQL 450MB（buffer pool 256MB）+ Redis 150MB（maxmemory 128MB）+ JVM 800MB（`-Xmx640m`）+ 系统 600MB ≈ 2000MB，**必须配 swap**。这份预算是配置纪律不是估算 —— 任何一项调大必须同时调小另一项，`-Xmx` 不能按「Java 应用一般给 1G」的习惯设。

- [x] **三份架构文档交付**（条目 [80]，2026-09-03）：以上述统一计划为唯一依据源，全部用中文撰写、每个组件标注 Batch1/2/3。
      ① **`docs/architecture/系统总体架构设计文档.md`** —— 架构总览图 / 9 功能域切分表（含对应客户端目录与主要接口）/ 部署形态图与内存预算表 / 13 张表数据架构 + 两个冗余列（`grid_id`、`l2_category_id`）的理由 / 接口契约落地（三个共享组件 + 两 ID 分工 + 幂等与 gzip）/ 四条关键链路（地图图层切换四段、联系出口五重防护、AI 发布、匹配排序分阶段）/ 定时任务 6 项 / 38 接口批次矩阵 / 性能与降级次序 / 决策依据索引；
      ② **`docs/architecture/技术栈选型说明.md`** —— 12 项选型总表（含月度成本）+ 逐项「结论 / 被否方案与理由 / 退出条件」三段式。开篇先摆压缩全部选择空间的三条约束（1 人 + 38 自然日无缓冲 + 131 元/年），后文每处「否掉」都能追回这三条。AI 一节按「两件不同的事、两个相反的答案」写；末尾列四条需定期复核的外部假设；
      ③ **`docs/architecture/可观测性架构方案.md`** —— 三类数据三套保留规则（埋点 90 天由盘容量决定 / 审计 180 天由合规下限决定 / 中转脱敏日志 30 天，三者不共用配置）/ 四个必埋事件 + 三条易实现错的口径 / 缓存命中无 `request_id` 的对照表 / 北极星三轴 + 轴① 两项反向哨兵 / 三阶量级测算 / 第一天就做的三条防线 / 三条迁移阈值与动作次序。
      **交叉校验查出并修正 4 处**：错误码数量（我写 21，实为 20 个 code 含成功码 `0`）、五大 P0 闭环表述（我写「登录/发布/找到/联系/举报」，`docs/PRD.md:2440` 实为「登录/发布/地图/详情/联系」）、降级触发条件缺失（补连续 3 次帧渲染 >16ms 或 P95 >800ms，并补「第 4 级降 Pin 指降到 500 以下」—— `docs/PRD.md:2426` 已明确删去「上限降 500 Pin」这个动作，500 就是常态上限，降到 500 等于没降）、把我自己扩展的「日志文件纳入 grep 范围」误混入 PRD 原文引用（已改为明确标注是本方案的补充要求）。
      ⚠️ **两处未授权未动的既有冲突**（不阻塞服务端落笔，动工前须裁定）：① `docs/PRD.md:1275` 写 minSdk 24、`docs/PRD.md:2465` 写 21，属客户端口径；② `docs/BRD.md` 的 M3 指 2027-04 三轴联动、本说明文档的 M3 指 2026-08-31 视觉稿冻结，同名不同义。
      ⚠️ **架构设计到此为止是「文档态」，不是「可开工态」**：三份文档只解决了「选什么、怎么摆、为什么」，尚未产出工程脚手架（`pom.xml`、Compose 编排文件、13 张表 DDL、`common` 三组件代码）。`pubspec.yaml` 当前**无任何后端/网络库**，客户端接口层也未开始。9/14 起的后端服务窗口若按本文档 §2.7「无缓冲」排期执行，须先把这批脚手架落地。

- [x] **架构评审并落地 24 处修正**（条目 [81]，2026-09-03，ce-doc-review 六 persona 评审）：对上述三份文档做多视角评审，6 个 persona（一致性/可行性/安全/产品/范围/对抗）回收 **43 条原始 findings**，综合为 **34 条**，其中 6 条自动应用、18 条经用户批准「全部应用」后落地、4 条决策项逐项处置、7 条降为 FYI。
      **本轮补出的四类实质缺口**（这些不是表达问题，是文档确实没写的东西）：
      ① **安全底座整片缺失** —— 三份文档原先都没写 TLS 终止点在哪、密钥放哪、哪些端口对公网开。补出 `docs/architecture/系统总体架构设计文档.md` §4.2 至 §4.5 四个新小节：Caddy 承载 TLS（内置 ACME 自动续期，故不用 Nginx）/ 网络暴露面（仅 443 公网、22 走白名单、MySQL 与 Redis 不写 `ports:` 映射、Redis 必设 `requirepass`）/ 密钥五类走宿主机 `.env` 权限 600 / 每日 `mysqldump` 上 OSS 保留 7 份且**上线前必须演练恢复**。**代价是内存预算重排**：Caddy 占 50MB，`-Xmx` 从 640m 下调到 600m（JVM 800→750MB）才能贴回 2000MB；
      ② **图片链路的两条阻塞红线没有执行点** —— PRD `:2401` 要求 EXIF 剥离、发布前需机审，但原文档写的是「图片不经服务端」，直接与这两条冲突。改为「不经服务端**传输**，但必须经服务端**登记与审核**」，并新增 §2.1 用五行表把申请上传→上传→元数据剥离→内容审核→可见性逐段落到具体执行方；配套两条约束：客户端不持有 OSS 长期凭证（只拿一次性直传凭证）、存储桶不开公共读；
      ③ **三类到期数据没有执行者** —— PRD `:2396`（收藏 30 天）、`:2397`（发布记忆 180 天）、`:2398`（注销 7 天冷静期）在保留期表里有、在定时任务表里没有，等于写了义务但没人执行。§8 定时任务表由 6 项**扩到 11 项**，补齐这三项外加「埋点月表预建」与「可观测性阈值巡检」；R12 已同步回改为这个完整口径，两处不再有差异；
      ④ **成本估算漏了全部外采服务** —— 原总表 12 行全是自建组件，短信、内容安全、实名核验、大模型 API 四项按量付费服务一个都没登记，导致「Batch1 约 8 元/月」这个数字不成立。补出四行后**修正为 20 至 45 元**，并写明它与 131 元/年**不是同一口径**（131 元是一次性与固定资源，短信机审是随用量线性增长的可变成本，不能相加后比较）。
      **一类被评审揪出的系统性问题：Batch1 批次倒挂。** 四处都是同一个错误的不同表现 —— 用 Batch2 才存在的东西去卡 Batch1 的验收：北极星轴① 的 `≥0.216` 被当成 Batch1 验收线，但 `demand_push_sent` 与 `resource_detail_click` 由 AI 推送产生、`ai` 域整体在 Batch2，**Batch1 采不到这个数**（已改为按批次分档：Batch1 只验采集链路就位，Batch2 冷启动期只记基线，观察期后才用 0.216）/ Hard Filter 索引首列 `city_id` 在 13 张表里根本不存在（已标 Batch2 并注明不要提前建）/ 降级次序前两级在 Batch1 是**空动作**（关 AI 发布与关双向推送都属 Batch2，意味着 Batch1 一旦 P95 超 800ms，服务端侧无任何可降级项，唯一动作是改实现或升配）/ `contact_event` 同名两义（埋点表里的事件 90 天允许丢，`audit_log` 里的记录 180 天不允许丢且是唯一合规举证来源，不能因字段重复就复用同张表）。
      **四条运行时假设从「隐含」变为「显式登记」**：经济型 e 是**共享 CPU 无性能 SLA**（PRD 的 P95 数字在这种机型上不是可稳定复现的承诺，故复测必须同时记 CPU steal，steal 持续 >10% 的实测结果不算有效数据）/ 40G 盘九项分配表（**T1 阈值取 8GB 就是从这张表算出来的**，此前是凭空数字；并硬性要求 binlog 设过期天数、应用日志配轮转，任一漏配都会绕过分配表把盘吃满，而盘满的表现是 MySQL 拒写、即全站不可用）/ Redis 必须显式设 `maxmemory-policy volatile-lru`（默认 `noeviction` 在内存打满时让写命令直接报错、等于登录与限流全挂；`allkeys-lru` 又会淘汰无 TTL 键）并开 AOF `everysec`/ T2 阈值给出可直接执行的分钟级聚合 SQL 并绑定周巡检任务。
      **一处安全口径改动影响接口契约**：`/track/events` 由免鉴权**改为要求有效登录态**，`user_id` 取自登录态而非请求体（否则任何人都能伪造他人埋点、污染北极星指标）；同时新增 §9.1 免鉴权接口限频表（`/auth/sms/send` 同手机号 1 分钟 1 条、1 小时 5 条、1 天 10 条，同 IP 1 小时 20 条；`/auth/sms/login` 连续 5 次失败锁 15 分钟；超限统一回 `429` 不新增错误码）—— 短信是按条付费的，无限频等于把预算暴露给刷量。
      **一条方法论纠正记下以免再犯**：评审 persona 声称「PRD 行号整体偏移一行」，我逐条实测后发现 `:2478`、`:2479`、`:2440` **本来就是正确的**，只有 5 处确实错。**未盲从 persona 的整体性判断，只改实证有误的那 5 处** —— 批量结论要抽样验证，否则会按错误前提引入新错误。
      ⚠️ **两项延后（已在文档内登记，非遗漏）**：① Batch1 全员未实名导致联系闭环采不到数，属产品策略问题、文档改不了，仅登记提醒；② 告警「通知」的承载组件本期不定，定案时点与 SLS 迁移绑定（届时用 SLS 自带告警能力），在此之前靠 §9.1 周巡检保证有人真的去看日志。

- [x] **前端视角复评审并落地 20 处修正**（条目 [82]，2026-09-03）：条目 [81] 的六 persona 评审全部站在服务端与治理视角，用户追加「从前端角度再次评审一波」。派发 3 个前端 persona（契约可实现性 / 状态与错误处理 / 前后端接缝对抗）加 1 个客户端现状探索 agent，回收 **36 条 findings**，合并为 **18 条**三层清单（阻塞 5 / 严重 7 / 中等 6）。用户随后指示「先复核再完善」，故本轮**先逐条实证核实、后落地**。
      **核心判断（一句话）**：三份架构文档全部以服务端为中心书写，19 个 Batch1 接口的**客户端对侧一条都没登记**，而客户端当前 6 个运行时依赖里**没有任何 HTTP 客户端**、18 个 Provider **全部是同步 `Provider`**。也就是说客户端跑在本地假数据上，接服务端不是「加几个请求」，而是先建一层不存在的网络层、再把整条数据链从同步改成异步。这不是实现细节而是排期风险 —— 按「服务端做完客户端接上就行」估工，会在 M6 内测包前才发现状态层要整体改写。
      **三处事实性错误已修**（均有代码或 PRD 双证）：① 架构图 `C2` 节点写「三级本地缓存 大类 1h / 二级 15min / 三级 5min」，但 `lib/nfr_constants.dart:134` 早在 2026-09-02（缺陷第 7 条）已定案 `pinSetTtlSec = 60`，1h/15min/5min 只保留给静态数据（`staticL1/L2/L3TtlSec`）—— 按类目层级分档会让新发布最长 1 小时看不见，已改为「Pin 集合 60s / 静态数据 1h·15min·5min」；② §9.1 埋点单请求上限写「20 个事件」，而 PRD `:2216` 与 `nfr_constants.dart:180` 双证是 **50**，20 是全项目唯一异类，已改；③ §9.1 标题写「有**两个**接口不要求登录态」但表实有三行、且第三行 `/track/events` 已在上轮改为要求登录态，标题已改为「三个高风险接口的限频与防刷」。
      **本轮补出的五类实质缺口**：
      ① **`grid_id` 没有算法** —— PRD `:2312` 只写「约 500m 网格 ID，由 `(lng,lat)` 写入时计算」。但这个 ID 被三方独立计算（服务端写入、服务端解析入参、客户端拼缓存键），三方算得不一致的后果是**缓存永不命中且地图取不到 Pin，而且不报任何异常**。新增 §7.1.1 定死六项（步长 0.0045°、原点 (0,0)、`floor` 取整、`"{gx}_{gy}"` 编码、5 位小数先截断、正则 `^-?\d+_-?\d+$` 校验），并要求三方共用同一组测试向量（含正负跨零与边界跨格）；
      ② **19 个错误码只有服务端一半** —— PRD `:2235-2257` 那张表**本身就带「客户端处置」列**，说明它是双边契约。原文档只落了服务端一半，结果是服务端老实回 `42902`、客户端弹「网络异常」。新增 §6.4（客户端集中枚举 + PRD §12.5 为唯一口径源 + 未知码按 `5xxxx` 兜底）、§6.5（`40101` 的**单飞 refresh** 时序：暂停 in-flight → 单飞续期 → **沿用原 `X-Interaction-Id` 与 `Idempotency-Key`** 重放 → 失败清会话；并发续期是这块最常见的实现错误，后果是随机掉登录）、§6.6（可重试码白名单 + 指数退避加抖动 + **总共 2 次而非每层 2 次** + `/map/pins` 超时收紧到 3s）、§6.7（时钟不一致：登录态与幂等窗口一律以服务端为准）；
      ③ **两个 429 业务码无处可归** —— PRD `:2249-2252` 四个 `429` 码语义**全被占满**（AI 配额/联系超限/熔断/推送频控），本轮新增的短信限频与埋点限频一个都对不上。硬塞 `42902` 会让用户看到「今日联系次数已用完」而他其实只是短信发太快。新增 §9.1.1 裁定 `42905`（验证码频控）与 `42906`（埋点频控），错误码总数 20 → **22**（21 个错误码），§6.1 与 §6.4 已同步；`42906` 是**唯一不向用户呈现**的错误码（埋点失败不是用户能解决的问题），这条例外必须写进映射表否则会被兜底逻辑弹成 toast。同时定义 `Retry-After` 响应头 —— PRD 原先根本没有这个头，客户端拿不到剩余秒数，只能显示「请稍后再试」；
      ④ **图片链路与 PRD 两处对不上** —— 上轮定的直传方案与 PRD `:2096`（`/media/upload` 入参 `in: file`）直接冲突（`file` 入参必然过服务端、与 3M 带宽约束不共存），新增 §2.1.1 裁定改为两步式（`/media/upload/ticket` + `/media/{id}/commit`）。另一处更隐蔽：上轮写「`audit_status != pass` 的 `media_id` 一律不出现在任何响应里」，与 PRD `:2247` 要求「图位显示『该图未通过审核』」矛盾 —— 若本人也看不到，他无法知道该换哪张图。已精确为「不出现在任何**非本人**可见的响应中」，并给出三态双视角展示表（`pending` **允许提交发布**，因为审核是带外异步的，要求等审核完再提交会毁掉发布转化率）；
      ⑤ **`layer_switch` 只报总耗时** —— `nfr_constants.dart:176-178` 已在 2026-09-02（缺陷第 9 条）定案要四段分列加 `result`/`fail_reason`/`err_code`，但可观测性文档 §4.1 只写了 `duration_ms` 与 `net_rt_ms`。只有总耗时的话，P95 超标时无法知道是缓存、网络、聚合还是渲染慢，而这四段优化手段完全不同。新增 §4.1.1 十二字段表，并加一条**可自动执行的数据质量校验：四段之和必须等于 `duration_ms`（±1ms）** —— 没有它，客户端漏计一段的 bug 会表现为「P95 突然变好了」，这是最难发现的一类指标错误。
      **三项客户端才能采、但客户端一项都没有的数据**（新增可观测性 §4.5）：`network_type`（Flutter 标准库不提供，需 `connectivity_plus`；取不到时**上报 `unknown` 而非猜 `wifi`**，猜错会把弱网会话混进 P95 分母、正好破坏排除规则）/ `mem_peak_mb`（需平台通道，Batch1 只采 Android、iOS 上报空值）/ **设备指纹**（PRD `:1695`、`:2173`、`:2181` 三处限频规则都依赖它，但 PRD 从未定义怎么生成怎么传 —— 已定为客户端首启生成 UUID v4 持久化、走 `X-Device-Id` 头、**明确标注不可信**、且不采集 IMEI/MAC/广告 ID）。缺前两项是指标质量问题，缺第三项是**三维限频塌成两维的安全问题**。
      **两条 P95 排除规则从「口径」变为「SQL 可判定」**（新增 §4.2.1）：弱网靠 `network_type IN ('3g','2g','none')` 直接过滤；重启窗口新增服务端 `restart_window` 两列表由应用启动时自写，否则「重启后 5 分钟不计入分母」这句话每次算 P95 都要靠人回忆当时有没有发版。
      **轴② 补出阶梯目标与单次布尔口径**（新增 §6.1.1）：`nfr_constants.dart:195-217` 的 `layerLoadTargetByPhase` 定案是 Batch1 ≥0.60 / 挂牌期 ≥0.70 / Batch2 ≥0.80 / M3 ≥0.90，而文档原先无此列 —— 用 0.90 卡 Batch1 会让首个内测包必然「不达标」且这个不达标传达不出任何信息。分母排除四类（`cancelled` 用户改变意图 / 弱网 / 四段和校验不过 / 落在重启窗口内），且**不做「重试 3 次成功算成功」的折算**：一次交互就是一次布尔判定，重试本身已意味着用户等了更久。
      **客户端依赖选型从零到定死**（新增《技术栈选型说明》第 12 节 + §2 总表六行）：`dio`（否 `http` —— 单飞续期、退避重试、竞态取消**全依赖拦截器与取消能力**，用 `http` 等于自己实现半个 `dio`）/ 手写 `fromJson`（否 `json_serializable` 与 `freezed` —— `lib/domain/` 1424 行已是手写风格，19 个接口的手写成本低于改造成本，**刻意接受的技术债**，模型超 20 个时重评）/ `uuid` / `connectivity_plus` / `image_picker` / `cached_network_image`（自带 `Image.network` 无磁盘缓存层，3M 带宽下滚动回来重新下载是实打实的损失）/ 定位复用 `amap_map` 自带（否 `geolocator` —— 两套权限逻辑加 GCJ-02 与 WGS-84 双坐标系转换）/ 不引 `device_info_plus`（指纹已定为自生成 UUID）。依赖数 6 → 11。
      **计划文档回补 R19 至 R22**：原 18 条 R-ID **全部是服务端条款**，这是客户端侧在任何文档里都没落点的根因。新增客户端依赖（R19）、统一网络封装（R20）、**Provider 链异步化含请求竞态**（R21，明确「这项改写是 Batch1 的工作量而不是 Batch2 的优化」）、埋点采集与队列（R22）。同时新增架构 §9.2 客户端配套交付物十行矩阵，其中含一条**发内测包前的阻塞项**：`auth_repository.dart:338` 硬编码验证码 `_debugCode = '888888'` 必须加 release 构建期断言。
      **方法论纠正再次生效（第二次记录）**：条目 [81] 已因 persona 误判「PRD 行号整体偏移」而立下抽样验证的规矩，本轮继续执行并**又抓到两处 persona 不实**：① 报告引用 `lib/features/publish/publish_completeness.dart`，Grep 报 `os error 2`，实际路径是 `lib/domain/publish_completeness.dart`；② 报告称「`CompletenessLevel` 有两个不同定义」，实测 `listing_detail.dart:16-24` 只有一个枚举、另一文件是 `import` 它而非重复定义，真问题只是「三值枚举 vs 文档 `0=⚪/1=🟡/2=🟢` 四态标记」的映射缺失。**结论：subagent 的目录清单可信度高于其结论性判断，凡涉及具体行号与文件路径的断言必须实测。**
      ⚠️ **本轮登记但未动的项**：`CompletenessLevel` 三值枚举与文档四态标记的映射消歧（需先定 `⚪` 档是否真实存在）/ `publish_form_state.dart:84` 与 `listing_detail_repository.dart:49` 的 7 天字面量改引 `NfrPostLifecycle.validDays`（属代码改动，未获授权）/ iOS `Info.plist` 三项补齐（同上，已登记进 §9.2 矩阵）/ PRD 本体的同步更新（`42905`/`42906` 入 §12.5、`/media/upload` 改两步式、设备指纹定义、`Retry-After` 头）—— 架构文档已给出裁定，PRD 回写待用户确认后执行。

- [x] **QA 视角复评审并落地 21 条修正**（条目 [83]，2026-09-04）：条目 [81] 是服务端与治理视角、条目 [82] 是前端视角，两轮都在回答「要做什么」和「怎么做」。用户追加「继续评审」并选定**测试与验收视角（QA）**，派发 4 个 persona（验收可执行性 / 测试用例覆盖 / 验收对抗 / 文档回归核查），回收 **48 条原始 findings**，剔除 3 条 persona 误判后合并为 **21 条**三层清单（阻塞 4 / 严重 9 / 中等 8）。用户指示「需要再次核实，并完善」，故本轮同样**先逐条实证、后落地**，11 条待验条目实测全部为真。
      **核心判断（一句话）**：前两轮补齐了「要做什么」与「客户端怎么做」，这一轮暴露的是**「做完怎么证明它对」几乎是空的** —— 三个 🔴 阻塞级验收项（读接口 P95、埋点四事件、隐私红线 grep）没有一条能被第二个人独立执行出相同结论，而 Batch1 唯一能卡出数字的轴② 其**分子定义与代码真源冲突、且判定权完全在被考核方（客户端）手里**。验收清单写成这样，等于把「验收」变成「验收剧场」：每一项都能签字通过，但签字不代表功能真的达标。
      **四条阻塞级已修**：
      ① **轴② 分子口径与代码真源冲突** —— 可观测性原写分子为 `result = 'success'` 的事件数，但 `lib/nfr_constants.dart:202` 定案的是**单次布尔口径**：`duration_ms ≤ 300ms` 才算成功。只按 `result` 算，等于「没崩就算成功」，一次 3 秒才切完的图层也计入分子，指标必然虚高且永远达标。已改为 `result = 'success' AND duration_ms <= 300` 双条件，并补一条**服务端交叉核对**：客户端 `t_net_ms` 与同 `interaction_id` 的服务端 `rt_ms` 相差须 ≤50ms —— 否则整条判定链都在被考核方进程内，客户端少计一段耗时就能把成绩改好，且不留痕迹；
      ② **release 断言在 Dart 里是无效兜底** —— 条目 [82] 给 `auth_repository.dart:338` 的 `888888` 定的方案是「加 release 构建期断言」，但 Dart 的 `assert` 在 release 模式**整体不参与编译**，`assert(!kReleaseMode)` 这行代码在正式包里根本不存在，写了等于没写。已改为三层可执行门禁（新增架构 §9.2.2）：源码用 `if (kDebugMode) { }` 包裹让编译期 tree-shaking 真正剔除 / 出包后对产物执行 `strings ... | grep -c '888888'` 与 `grep -c 'debugCode'`，**两者必须为 0 否则 `exit 1`** / CI 加 `grep -rn "888888" lib/` 拦截新增。留存 `build/release-gate.log`；
      ③ **`/media/upload` 残留导致接口计数错** —— §2.1.1 已把上传拆成 `/media/upload/ticket` + `/media/{id}/commit` 两步，但 §9 的接口表、发布分组、`post` 域描述三处仍写单个 `/media/upload`。这不只是笔误：**接口总数由 38 变 39、Batch1 由 19 变 20**，而 19/20 是排期与「客户端要接多少个」的基数。三处已替换、计数已修正，技术栈 §12 的「Batch1 只需接 20 个接口」同步；
      ④ **乐观锁 `version` 从未进入接口契约** —— §5.2 写了「`PATCH /posts/{id}/status` 带 `version`，冲突回 `40901`」，但**没有任何接口的响应下发 `version`**，客户端拿不到这个值，这条并发保护实际上无法实现。新增 §5.2.1 定死：`GET /posts/{id}` 与 `GET /posts/mine` 响应必须下发 `version`、`PATCH` 请求体必带（缺失回 `40001`，**明确禁止服务端按当前值兜底** —— 一兜底乐观锁就彻底失效且没人会发现），并定义 `40901` 语义为「不可自动重试但必须强制重取」，在 §6.6 表里从「确定性失败」行**单独拆出一行**（混在里面会被客户端实现成静默失败）。
      **本轮补出的五类实质缺口**：
      ① **读接口 P95 没有测量协议** —— 原文只有「P95 ≤ 300ms」一个数字，没说测哪个数、谁来发压、并发多少、跑多久、不过怎么办。新增 §10.3.1 至 §10.3.5 五个子节：P95 一律取**服务端 `rt_ms`**（不用客户端 `duration_ms`，后者含缓存与渲染、属轴② 口径，两者不可互相顶替）、逐接口单独出数（混算会让慢接口被快接口稀释）、压测机与被测机分离（同机发压会抢那 2 核）、并发 20 跑 5 分钟丢弃前 30 秒、**任一接口不过即整项不过**、无留存视为未验收；配套索引造数规格（10 万有效行 + 5 万过期行、`grid_id` 覆盖 200 网格长尾、判据是 `EXPLAIN` 的 `type != ALL` 且 `key` 命中且 `rows < 5%`、造数脚本固定随机种子）、并发写四用例、响应体积验收（必须**打满 500 条**再量，不打满就是在量空数组）；
      ② **CPU steal 免责条款可以无限重跑** —— 上轮定的「steal 持续 >10% 的结果不算有效数据」有个漏洞：没写重跑上限。经济型 e 是共享 CPU，理论上可以一直重跑到某次刚好没有邻居抢占，然后拿这次的数据签字。新增 §10.3.4：**最多 3 次重跑，3 次全废即判定机型不满足 SLA 并进入升配评估**，不得记作「待复测」；`vmstat 10` 采样取**全程均值**（不是挑低点）；全部重跑记录含作废的都要留存；结论必须写成「在 steal 均值 X% 条件下 P95 为 Y ms」而不是裸一个数字；
      ③ **四段和恒等式可以被减法绕过** —— 条目 [82] 新增的「四段之和 == `duration_ms`（±1ms）」是好校验，但**它单独存在时是自我满足的**：只要实现成 `t_render_ms = duration_ms - t_cache_ms - t_net_ms - t_parse_ms`，恒等式 100% 通过而四段数据全是错的，而这正是最省事的写法。新增可观测性 §4.2.1 四条纪律：四段各自独立 `Stopwatch`、**代码评审出现这类减法即打回**、`duration_ms` 也独立测、服务端 `rt_ms` 作为客户端无法自证的第二重校验；并明确恒等式只是必要条件不是充分条件；
      ④ **分母可以被悄悄缩小** —— 轴② 的四类排除项（`cancelled`/弱网/校验不过/重启窗口）没有封闭声明，实现时多排除一类就能让成功率变好，且从看板上看不出来。§6.1 新增「分母不得缩小的纪律」：排除项**只有这四类、不允许新增**，若需新增须登记并附实测占比，未登记的排除视为该期指标无效；同时 `network_type` 补 `unknown` 枚举并明确 **`unknown` 计入分母、不属于弱网排除集**（否则取不到网络类型就成了免罪符）；埋点队列补 `dropped_count` 随下一批上报 + 告警线（单用户单日 >200 或全体 >上报总量 5%）—— 静默溢出会让轴② 系统性偏好，因为**慢交互恰好更容易在队列压力大时被丢**；
      ⑤ **两条隐私红线的审计不可执行** —— 「联系方式不出现在任何列表接口」与「不存储身份证」原文只有结论没有执行方式，验收时只能写「已确认通过」。新增可观测性 §10.1 给出可执行 grep 定义：联系方式出口搜 `*.java` + mapper XML + `resources/`，模式覆盖 `phone`/`mobile`/`wechat`/`contact`/`tel` 含驼峰，判据是完整值出口**只允许 `/contact` 一处**；身份证查 DDL（`id_card`/`idcard`/`id_no`/`identity`）+ 数据 `WHERE col REGEXP '^[0-9]{17}[0-9Xx]$'` + 代码写入路径；**要求留存完整 grep 输出，只写「已确认通过」视为未执行**。同时指出「身份证存储审计」在 Batch1 是**真空通过**（实名功能整体在 Batch2，此时扫描必然为空、签字毫无信息量），已标注为「Batch1 不适用 / Batch2 验收」而非「已通过」。
      **三处「正确但不可测」的描述改为断言点**：① `grid_id` 的「5 位小数截断」有歧义 —— 多数语言 `toInt()` 是向零取整、`floor` 是向负无穷，`-0.000015` 分别得 `-0.00001` 与 `-0.00002`，而这个 ID 由三方独立计算、算错**不报异常只是永远不命中**。已明确为「向下取整到 5 位小数（`floor(x * 100000) / 100000`）」，并把原「至少覆盖三种情况」替换为 **10 条带期望值的测试向量表**（含跨零、负边界、精度方向探针），且要求同时断言中间值 —— 因为网格步长比 5 位小数粗 450 倍，精度方向错了会被网格吸收、只在极少数坐标上暴露；② 新增 §6.3.1 幂等的四类必测输入（缺失回 `40001` 不得放行 / 格式非法含大写 UUID 与 v1 / 重复但请求体不同须返回首次结果 / **首次失败后重放必须重新执行**，只有成功响应进幂等缓存）—— 后两条常被实现成相反行为，且**两者都不会在正常使用中暴露**；③ 新增 §6.5.1 单飞的三个断言点（refresh 实际调用次数 `== 1` / 5 个挂起请求全部重放一个不少 / 每个重放请求的 `X-Interaction-Id` 与 `Idempotency-Key` 逐字相同）—— 「单飞」实现错了在正常使用中完全正常，只在并发时随机掉登录。
      **五处事实性矛盾已修**：`-Xmx` 双值（技术栈 §2 仍写 800MB/640m，是 Caddy 未承担 TLS 时的旧口径，已废弃并在四处统一为 **JVM 750MB / `-Xmx600m`**，含计划文档 `:129` 与 `:156`）/ §4.2 标题写「三条」实有四条 / §2.1 可见性行漏「非本人」限定 / 可观测性误引技术栈 §13（实为 §12）/ §9.2 仍写「原 18 条 R-ID」（现为 22 条）。另修 T1 阈值判据由 `df` 改为 `SELECT SUM(DATA_LENGTH+INDEX_LENGTH) ... WHERE TABLE_SCHEMA='track'`（`df` 量的是整盘、埋点表涨到 8GB 之前盘可能先被别的东西吃满，方向正好相反）；T2 由 `MAX` 峰值口径改为**持续判据**（`HAVING c > 3000` 且高位分钟数 ≥60 才命中，单分钟尖峰会天天误报、告警很快被无视）；新增 §9.1.1 主看板 Q1-Q7 SQL 清单作为 T3 的判定对象并要求落单一文件。
      **验收清单三列扩五列 + 新增 R23**：可观测性 §10 表原为「验收项 / 判定标准 / 关联章节」三列，只说「要什么」不说「谁执行、用什么工具、什么情况算失败」。已扩为五列（加「执行者与工具」「失败判定」），埋点四事件的失败判定含「四段和校验不过占比 >1%」「`interaction_id` 缺失率 >0」，并与 §4.5 的平台分档对齐（iOS `mem_peak_mb` 为空不算失败）。新增 **R23「验收可执行化」**（登记在可观测性 §11 索引表），说明本轮补的**全部是判定协议与防自证条款、不新增任何采集项** —— 即不增加客户端埋点工作量。
      **方法论纠正第三次生效**：条目 [81]、[82] 已两次因 persona 误判立规，本轮继续抽样实测，**又抓到 3 条不实并全部剔除**：① `qa-regression` 称 `categoryTreeVersion` 应在 `:22`，实测 `:22` 是 `/// TODO(M5)` 注释、`:23` 才是常量，文档原本正确，且该 persona 自己的 `suggested_fix` 前后自相矛盾；② 把 `cupertino_icons`（图标字体资源）算作第 7 个运行时依赖，判定为非功能性依赖后剔除，但仍在三处文档补了「**功能性**运行时依赖」限定词以消除歧义；③ `qa-acceptance` 称架构 §4.1 内存表与正文冲突，实测 `:181` 与 `:186` 均为 600m 一致，真冲突在部署图 `:152` 与技术栈 `:85`，已修正 finding 指向后落地。**结论沿用并加强：凡涉及具体行号的断言必须实测，且 persona 内部自相矛盾的 finding 优先怀疑 persona。**
      ⚠️ **本轮登记但未动的项**：PRD 本体同步（`version` 入 §12.2 三接口、`:2096` `/media/upload` 改两步式、接口总数改 39、`42905`/`42906` 入 §12.5、设备指纹定义、`Retry-After` 头、`:2247` `40902` 客户端处置表述）—— 架构文档已裁定，PRD 回写待确认；工程性缺口整体未获授权（`pom.xml` / Docker Compose 编排 / 13 张表 DDL / `common` 三组件 / 客户端网络层脚手架 / `scripts/seed-perf.sql` 造数脚本 / Q1-Q7 看板 SQL 文件）—— 其中造数脚本与看板 SQL 是本轮新增验收协议的直接依赖，不落地则 §10.3.2 与 §9.1.1 仍只是文字。

- [x] **PRD 回写完成，架构基线闭合**（条目 [84]，2026-09-04）：条目 [81] 至 [83] 三轮评审在架构文档里做了大量裁定，但**PRD 本体一直没同步**。这是最危险的一类不一致 —— 开发时真正会被翻的是 PRD，两份文档打架时人会信 PRD 而不是架构文档。本轮把 6 项裁定全部回写，并在回写过程中**发现并修掉一处我自己引入的错误码冲突**。
      **回写前发现的一处真冲突（本轮最重要的收获）**：`40901` 被两份文档赋予了不同含义 —— PRD `:2246` 早已定义为「命中敏感词」，而我在条目 [83] 新增 §5.2.1 时把乐观锁版本冲突也写成了 `40901`，**并未先查 PRD 是否已占用该编号**。架构文档 §6.4 自己立的规矩是「PRD §12.5 是错误码唯一口径源」，所以是架构文档错。`409` 段已占 `40901` 敏感词、`40902` 图片未过审，故版本冲突改用 **`40903`**，错误码总数 21 → **22**（含成功码 `0` 共 23 个 code）。架构文档四处（§5.2.1、§6.6 重试表、§10.3.3 并发写用例、§6.1/§6.4/§9.1.1/§9.2 四处计数）已全部改正。**这个错误的性质值得记下**：新增枚举值前必须先查唯一口径源是否已占用，否则「唯一口径源」这条规矩就是自己先破的。
      **六项 PRD 回写明细**：① §12.3 发布表把 `POST /media/upload`（入参 `file`）替换为两步式 `/media/upload/ticket` 与 `/media/{media_id}/commit`，并补一段说明（含「单张 2MB 图上行占满 3M 带宽约 5.5 秒、多图必然超时」这个废弃理由、接口总数 38 → 39 / Batch1 20、以及客户端不持长期密钥与存储桶不开公共读两条红线）；② §12.1 编码行由「媒体上传用 `multipart/form-data`」改为「**媒体不走本 API**，本 API 无 `multipart` 接口」—— 原文会让开发以为要写一个文件上传接口；③ `version` 入三个接口（`GET /posts/{id}` 与 `GET /posts/mine` 出参、`PATCH /posts/{id}/status` 入参必带），并补一段说明三条并发写路径与两条纪律（缺失回 `40001` 且**服务端不得按当前值兜底**、客户端收到 `40903` **不可自动重试**必须重取）；④ §12.5 新增 `40903`、`42905`、`42906` 三行，并把 `40902` 的客户端处置补上「**仅本人可见**」限定（原文没写，会被实现成所有人都看到「该图未通过审核」）；⑤ §12.5 末尾新增 `Retry-After` 响应头段落，规定 `42901`-`42906` 与 `40105` 必须带剩余秒数、客户端**不得自行猜测时长**、`42906` 只用它调度队列不呈现给用户；⑥ §12.5 末尾新增设备指纹四行定义表（首启 UUID v4 持久化 / `X-Device-Id` 头 / **明确不可信**且账号维与 IP 维不可因它放宽 / 禁采 IMEI·MAC·IDFA·OAID），同时 §12.1 新增「设备标识」请求头行 —— 此前 §7.7 三维限频与 `device` 表都依赖它，但 PRD 从未定义。
      **三处「需同步」状态标记已翻转**：架构 §9 开头、§5.2.1 两处、可观测性 §4.5 设备指纹段，原本都写「PRD 需按此同步」，现已改为「PRD 已于 2026-09-04 完成同步」并注明落点章节。这样下次读文档的人不会以为还有待办。
      **架构基线至此闭合**：四轮评审（服务端治理 / 前端 / QA / 本轮回写核对）累计落地约 **90 处**修正，23 条 R-ID 全部登记，PRD 与三份架构文档之间**不再有已知的口径差异**。剩余工作全部是工程性产出而非设计问题。
      ⚠️ **下一阶段的工程缺口（规格已完整，缺的只是文件）**：`pom.xml` / Docker Compose 编排 / 13 张表 DDL / `common` 三组件（响应包装器·全局异常处理器·错误码枚举）/ 客户端网络层脚手架 / `scripts/seed-perf.sql` 造数脚本 / Q1-Q7 看板 SQL。其中**后两项是条目 [83] 新增验收协议的直接依赖** —— 不落地，§10.3.2 的索引验收与 §9.1.1 的阈值巡检仍然只是文字，验收还是做不了。

- [x] **OSS 直传安全复核 + JDK 升级至 21**（条目 [85]，2026-09-04）：用户提出两个问题 —— 「oss 走前端会出现安全问题吗」（设计审查）与「jdk 要升级到 jdk21」（变更指令）。两者性质不同，分别处理。
      **OSS 直传：方案不变，但补七条硬约束。** 原架构 §2.1 只有两条配套约束（不持长期凭证、桶不开公共读），方向正确但**只写了目标没写实现约束**。核实阿里云文档后发现三个此前未覆盖的真实风险：① `x-oss-process=image/info` 能直接读出 `GPSLatitude`/`GPSLongitude`/`GPSMapDatum`，说明**原图落桶那一刻 GPS 是完整可读的** —— 原文只写「原图立即删除」，如果实现成异步任务或定时清理，窗口期内任何拿到对象名的人都能读出发帖人的家庭住址坐标，这是全项目唯一一条会造成**人身安全**风险的漏洞，已明确要求**与 commit 同事务同步删除，任一步失败整体回滚**；② 阿里云文档明确 CDN 开启「过滤参数」后 URL 问号之后参数被全部去除、**请求直接命中原图**，`x-oss-process` 处理规则被整体绕过 —— 已要求图片域不挂 CDN，若必须挂则禁开过滤参数；③ 客户端传完不调 `commit` 时图片已在桶里却无审核记录，成为既不可见也不会回收的孤儿对象 —— 已改为**以 OSS 上传回调作为 commit 触发源**，客户端主动调只作补偿，并对 24 小时仍 `pending` 的记录连同 OSS 对象清理。另补两条：`media_id` 与对象名一律服务端生成、凭证路径精确到单个对象（否则可传他人 `media_id` 覆盖别人已过审的图，等于用直传做越权写）；直传 Policy 必须由 OSS 侧强制 `content-length-range` 与 `content-type` 白名单、`commit` 时二次校验副本真实格式（只在签发接口校验入参等于没校验，客户端可声明 2MB 实传 200MB 刷存储费）。后两条合起来还堵住「先传合规图过审、再用同一签名 URL 覆盖为违规图」这类审核绕过，约束是 **commit 成功后立即设为不可覆写，签名有效期不作为唯一防线**。七条落在架构新增的 **§2.1.0**，并新增 **R24** 作为依据。
      **JDK 17 → 21：升，但不开虚拟线程。** 原定案理由是「17 是 Spring Boot 3 生态验证最充分的 LTS，21 的虚拟线程对本项目没有实际收益」。核实后判定：**后半句仍然成立，前半句时效性已弱化**（Spring Boot 3.2+ 已一等支持虚拟线程，4.1 文档甚至推荐 Java 24+）。故按用户指令升到 21，但虚拟线程明确**不开启**，理由三条：本项目瓶颈在内存不在线程数（2 核 2G、并发 20）、CPU 密集场景虚拟线程无收益、Java 21 的 `synchronized` 会导致载体线程 pinning 而 MyBatis-Plus 与 HikariCP 链路上 `synchronized` 块不少（Spring Boot 官方原文「for the best experience, Java 24 or later is strongly recommended」），在 21 上贸然开启可能比平台线程更差。用户选择「先不开，Batch1 后压测再定」，已写入退出条件。改动落点：技术栈 `:38`/`:73`/`:75`/`:89`（§3 整节重写并拆出「虚拟线程退出条件」与「换语言退出条件」两条）、计划文档 `:27`/`:40`/`:54`（含 **R1 本体**）、架构 §11 索引表。内存预算不变（JVM 750MB / `-Xmx600m`），JDK 21 相比 17 在本项目负载下无显著堆外差异。
      **R-ID 增至 24 条。** R24 是首条纯安全性质的条款，也是首条由「用户对既有设计提出质疑」而非评审 persona 触发的条款 —— 值得记下的是：三轮 persona 评审（服务端治理 / 前端 / QA）**没有一个 persona 提出 OSS 直传的安全问题**，而用户一句话就问到了。persona 评审覆盖的是「文档内部是否自洽」，覆盖不了「这个方案在真实攻击面下是否成立」。

- [x] **微信快捷登录可行性核实 + 登录身份归一化预留**（条目 [86]，2026-09-04）：用户提出「登录注册需要增加微信方式进行快捷注册和登录，这个下你分析一下是否可行」。这是一个**新需求评估**，不是变更指令，故先核实再给选项，未直接动手。
      **核实结论：技术可行，但成本不在本期预算内。** 微信开放平台移动应用登录本身接入不复杂（拉起授权 → code 换 access_token → 取 unionid），真正的成本在两处：① **资质与审批** —— 微信开放平台移动应用需企业主体、需提供已上架的应用包并等待审核，这段等待期不可压缩，而本项目 38 自然日无缓冲；② **账号合并语义** —— 微信登录进来的新用户没有手机号，而 §3.7 的实名、§7.4 的联系方式、§9.6 的注销清理全部围绕手机号建模，一旦上线就必须同时回答「微信用户如何实名」「已有手机号账号如何绑微信」「绑定冲突如何裁定」三个问题，每一个都会牵动既有流程。故**微信登录整体延到 Batch2**。
      **但本期必须做一件事：预留数据模型。** 如果 Batch1 继续把手机号当作账号唯一标识（`user.phone` 上的 UNIQUE 就是登录入口），Batch2 上微信时就要做一次**账号数据迁移** —— 在已有真实用户数据的库上改主标识，是所有迁移里风险最高的一类。本期只需引入 `user_identity` 表（`user_id` + `identity_type` + `identity_value`，双唯一索引），Batch1 只写 `phone` 一种类型，Batch2 加 `wechat` 就只是多插一行，迁移成本降到零。用户选择「Batch2 再做，本期只预留数据模型」。
      **发现一条 PRD 此前遗漏的连带约束：Apple 登录不能与微信拆批。** PRD §3.4.1 把「微信/QQ/Apple」三个第三方入口并列灰禁，读起来像三项独立可选功能。实际上 App Store 审核指引 4.8 规定：iOS 端只要上线了任一第三方社交登录（微信即属此列），就**必须同时提供 Sign in with Apple**。即 Batch2 打开微信登录时 Apple 登录必须同批交付，否则 iOS 版直接拒审。这条已写进 PRD，避免 Batch2 排期时按「一项功能」估工作量。
      **改动落点（五处，只落模型不落功能）**：PRD §3.4.1 新增「登录身份归一化」段落 + Apple 连带约束、§12.2 登录接口补查表路径说明、§12.2 登录风控与注销清理补 `user_id` 口径、§13.1 实体清单新增 `user_identity`、§13.2 新增字段字典并修订 `user.phone` 说明；架构 §5.1 表清单 13 → 14 张、§8 注销清理改按 `user_id` 删全部身份行、§9.1 新增「风控账号维统一为 `user_id`」口径（三条 → 四条）、§11 依据映射表同步；计划文档新增 **R25**。**R-ID 增至 25 条，接口数与错误码数均不变。**
      **两条容易踩的坑已写死在文档里**：① 账号级风控（联系次数、AI 配额、举报频次、熔断冻结）必须改以 `user_id` 为键，但**渠道级**短信限频保留手机号维 —— 两者性质不同，一刀切改成 `user_id` 会让短信通道失去保护；② 注销清理必须按 `user_id` 删**全部** `user_identity` 行，只删 `phone` 行会导致 Batch2 起「微信身份残留、下次微信登录复活已注销账号」，这是合规事故。

- [x] **架构设计方法论沉淀（方法论文档第四章）**（条目 [87]，2026-09-04）：架构设计阶段收口后，用户要求「总结方法论供后期复用，追加完善方法论文档」。产出分两处。
      **其一，`docs/solutions/workflow-issues/data-model-reservation-for-deferred-features.md`（新建）**：把条目 [86] 的推导抽象成一条可复用原则 —— **延期的是逻辑，不是数据**。功能可以延后，但该功能会引入的**身份维度**必须在当期建模，否则延期不是省了成本，而是把成本换成了「在有真实用户数据的库上改主标识」这种最高风险形态。附两条连带纪律：延后清单里的多项第三方接入要先查是否存在**平台规则层面的强制捆绑**（如 App Store 4.8）；数据模型一旦归一化，**所有以旧标识为键的下游逻辑都要重新分维**（渠道维 vs 账号维）。两个校验脚本均 0 flag 通过；`CONCEPTS.md` 同步补入「架构交付链」词群 4 条（批次 / 数据模型预留 / 渠道维·账号维 / R-ID）。
      **其二，`AI工程化方法论.md` 追加第四章「架构设计与多轮评审」**：这是继第一章（产品文档交付链一致性）、第二章（低保真骨架生成）、第三章（高保真原型设计）之后的第四类业务沉淀，沿用同样的六段骨架并扩展为 11 小节。核心主张是一句在本轮反复被验证的话 —— **架构文档的失效方式与代码相反：代码写错会编译失败，架构文档写错完全静默**，所以架构阶段的全部方法论本质上都是在人工补一套「编译器」。七条准则各自都有本项目的实证出处：4.2 依据源模式（R-ID 体系，25 条）/ 4.3 多视角评审要换**视角**不是换 persona 数量（四轮评审对比，含「三轮 persona 都没问出 OSS 直传安全问题、用户一句话问到了」这条反证）/ 4.4 subagent 的结论性判断必须实测、目录清单可以信 / 4.5 新增枚举值前先查唯一口径源是否已占用（`40901` 冲突事故）/ 4.6 验收项必须能被第二个人独立执行出相同结论（五问表 + 五条衍生）/ 4.7 跨端共算的值必须给测试向量 / 4.8 延期的是逻辑不是数据（引用上述 solution 文档为详细出处）/ 4.9 架构裁定必须回写规格文档。另附反模式 15 条、落地检查清单 19 项（五组）。
      **值得单独记一笔的元观察**：4.5 那条准则的来源是「立了 R-ID 依据源规矩之后，第一个违反规矩的是立规矩的人自己」—— 新增乐观锁冲突码时没查 PRD §12.5 就用了 `40901`，而它早已被「命中敏感词」占用。这类错误不会报错、只会在联调时以「客户端提示文案对不上」的形式浮现，正是「静默失效」的典型样本，也是把它写进方法论而非仅作为一次修复记录的原因。

- [x] **架构设计验收与三项缺陷修复**（条目 [88]，2026-09-04）：执行此前多次推迟的架构设计验收。验收方式严格遵循刚沉淀的方法论 4.4「结论性判断必须实测」—— 全部判据用 Grep/Read 对文档与代码取证，不做目测。
      **通过 9 项**：① R-ID 全覆盖（25 条在架构 §11 索引表逐条有落点，无孤儿条款）；② 接口数一致（39 全量 / Batch1 20，架构 §9 与 PRD §12.2 相同）；③ 错误码一致（PRD §12.5 **实点算 23 行** = 成功码 `0` + 22 个错误码，含 `40903`/`42905`/`42906`，与架构 §6.1 相符）；④ 表数一致（PRD §13.1 与架构 §5.1 **各实点算 14 行**）；⑤ 内存预算一致（450+150+750+50+600=2000MB，技术栈与架构 §4.1 逐项相同）；⑥ JDK 21 已全量同步（技术栈全文无 `Java 17` 残留）；⑦ **架构裁定确已真回写 PRD**（非仅声称：`version` 必带、七条安全约束、`user_identity`、注销按 `user_id` 四处均实测落地）；⑧ 代码行号引用抽查 7 处全部命中；⑨ 可观测性文档 R23 三个落点齐备。
      **不通过 3 项，已全部修复**：
      **P1（🔴 阻塞）`grid_id` 类型冲突** —— PRD §13.2 写 `int`，架构 §7.1.1 却定死编码为字符串 `"{gx}_{gy}"`（如 `"26700_6727"`）。字符串存不进 `int` 列，而这是**静默型冲突**：建表按 PRD、写入按架构，要到第一次插数据才报错；若有人「顺手」把字符串哈希成 int，则 §7.1.1 那 10 条测试向量全部作废。用户裁定按 `varchar(24)` 方案修，PRD §13.2 字段类型改为 `varchar(24)` 并补写「为何不是 int」的理由与算法出处链接。
      **P2 引用行号失效** —— 架构 §2.1 称 EXIF 剥离在 `PRD.md:2496`、图片内容审核在 `PRD.md:1834-1835`，实测两处分别是「缓存 TTL 表」与「自动重发开关」，全指错。修正为 `2556`（§14 验收清单）与 `1855`（§9.6 内容审核），并**在行号后补加章节名** —— 因为行号会随文档增删漂移，章节名是漂移后仍可定位的锚。修复中另发现两处同类失效（§5.1 的 `PRD.md:2371`、§7.1.1 的 `PRD.md:2312`），一并修正为 `2432`/`2372`。
      **P3 R-ID 计数陈旧** —— 架构 §9.2 仍写「现为 24 条」漏掉 R25，而同文档 864 行已含 R25，自相矛盾。改为「现为 25 条」并补 R25 说明。
      **本轮的方法论回环**：五处修改全部回读 Grep 验证，确认新内容落盘且旧内容零残留（`PRD.md:2496`/`1834`/`2371`/`2312`/`现为 24 条`/`grid_id | int` 六个模式均已 No matches）。P1 恰好印证方法论 4.7「跨端共算的值必须给测试向量」的一条**未尽之处**：给了测试向量还不够，**承载该值的存储类型也必须与向量的形态对齐** —— §7.1.1 把算法和 10 条向量写得极细，却没人回头核对 PRD 里那一列的类型能否装下向量的期望值。这条已构成第四章 4.7 的补充素材。

- [x] **架构文档归类到 `docs/architecture/`**（条目 [89]，2026-09-04）：架构设计阶段收尾整理。三份架构产出此前平铺在 `docs/` 根目录，与 PRD/BRD/MRD 等**需求类**文档、以及十余个 `gen_*.py`/`verify_*.py` 脚本混在一起，检索时难以一眼分辨哪些属于架构基线。本次归入统一目录：
      `docs/architecture/系统总体架构设计文档.md`（主文档）/ `docs/architecture/技术栈选型说明.md` / `docs/architecture/可观测性架构方案.md`。
      **移动方式**：先试 `git mv` 保留历史，返回 `not under version control` —— 三份文档均为未跟踪状态（首次提交尚未发生），故改用 `Move-Item`，无历史损失。
      **引用同步是本次的实际工作量**。全仓库扫描后共 9 处需改，分两类：① **架构文档之间的互引** 2 处（`技术栈选型说明.md:6` 与 `可观测性架构方案.md:6` 的「前置阅读」行都指向 `docs/系统总体架构设计文档.md`）；② **说明文档中的路径登记** 7 处（§文档索引表 3 处 + 进度记录条目 [80] 正文 4 处），用 Python 脚本批量替换以避免手工遗漏。
      **一个本可以踩到但没踩的坑**：三份架构文档内部共有 109 处 `docs/` 字样引用（主文档 65 / 可观测性 28 / 技术栈 16），初看以为层级变化后全都要改。实际核查发现这些引用一律采用**仓库根相对路径**写法（`docs/PRD.md:2432`、`docs/plans/....md`），而非 `./PRD.md` 这种依赖自身位置的写法 —— 所以文件挪到子目录后**全部仍然有效**，一处不用动。这印证了一条值得记下的约定：**跨文档引用统一用仓库根相对路径，文档就可以自由重组目录结构而不产生引用债**；反之若当初写成 `./PRD.md`，本次归类将连带修改上百处。
      **验证**：`docs/系统总体架构设计文档`/`docs/技术栈选型说明`/`docs/可观测性架构方案` 三个旧路径模式全仓库 Grep 均为 No matches；`docs/architecture/` 新路径命中 10 处（说明文档 7 + 可观测性 2 + 技术栈 1），与预期完全一致。

- [x] **方法论补充「跨文档引用路径约定」（1.15 新节）**（条目 [90]，2026-09-04）：条目 [89] 归类时发现「109 处引用零改动」这个结果并非运气，而是依赖了一条**此前从未写下来的隐性约定**。用户要求补充说明，遂在 `AI工程化方法论.md` 第一章新增 **1.15 跨文档引用的路径约定（全书通用）**，原 1.15 落地检查清单顺延为 1.16。
      **核心约定**：跨文档引用**一律用仓库根相对路径**（`docs/PRD.md:2432`），不带 `./`、`../`、盘符。给出三种写法的代价对照表 —— 根相对路径在「移动引用方文件」时完全不受影响，文档间相对路径每一条都要重算层级，绝对路径则换机器即全数失效。
      **立规的论据是本项目的实测数字**：三份架构文档内部 109 处 `docs/` 引用（主文档 65 / 可观测性 28 / 技术栈 16）在下移一层后全部仍有效，整次归类实际只改 9 处。关键的一句判断写进了方法论：**若当初写成 `./PRD.md`，这类重构不是「工作量大一些」，而是会因代价过高而根本不会发生** —— 文档目录于是永远停在最初随手摊开的状态。这是「隐性约定值得显式化」的典型：它的收益不体现在日常，只在需要重组时一次性兑现。
      **两条衍生纪律**：① **行号引用必须带章节名** —— 行号是会漂移的锚，被引文档前面增删任何一行，后面所有行号同时失准且**不报任何错**；举证用条目 [88] 的 P2（`PRD.md:2496` 实为缓存 TTL 表、`1834-1835` 实为自动重发开关）。正确形式是 `docs/PRD.md:2556`（§14 上线前验收清单），行号负责精确定位、章节名负责漂移后找回。② **移动文件是一次「引用同步」作业，不是一次 mv** —— 移动前先 Grep 旧路径计数，移动后 Grep 确认归零、再 Grep 新路径确认命中数符合预期，两次计数对不上即为遗漏或误伤；批量替换用脚本，手工改 7 处以上必然漏。
      同步扩充 §1.13 反模式表（21 → **24 条**）与 §1.16 落地检查清单（19 → **22 项**）。回读验证 1.10–1.16 编号连续无冲突、无残留旧编号引用。

- [x] **详细设计阶段：产出《API 接口契约》与《前后端详细设计文档》**（条目 [91]，2026-09-06）：架构设计与数据库设计完成后的下一层落地物，是编码前的最后两份设计交付物。依据源为三份架构文档 + 数据库设计文档与 DDL + PRD §12 + `lib/nfr_constants.dart`，**全程不新造口径**。
      **产出一：`docs/api/openapi.yaml`**（OpenAPI 3.0.3，约 2500 行）。自检脚本实测：`operations total: 41`（含 Batch2/Batch3 占位）、**Batch1 业务接口精确 20 个** + `/track/events` 另计、`$ref` 断链 0 处、`x-batch`/`x-domain` 扩展字段 100% 覆盖、`components` 含 schemas 31 / parameters 6 / responses 8 / headers 1。22 个错误码全覆盖且无多余。`info.description` 内嵌 6 大节全局约定（基础约定表、统一响应包、错误码表、客户端错误码行为四分类、`40101` 单飞续期时序、时钟不一致口径）—— 把跨接口约定写在契约文件内部，避免读者需要同时开两份文档。
      **产出二：`docs/design/前后端详细设计文档.md`**（**已于 2026-09-19 拆分为 [后端详细设计文档.md](file:///d:/developer/code/aicoding/s2s/docs/design/后端详细设计文档.md)（§0–§9 + §20–§21）与 [前端详细设计文档.md](file:///d:/developer/code/aicoding/s2s/docs/design/前端详细设计文档.md)（§10–§19），原名已不存在**）（20 节，后端 §1–§9 + 前端 §10–§18 + 自检与缺口 §19–§20）。后端部分含 `ErrorCode` 枚举全量 Java 代码、`GridIdCalculator` 完整实现、`/map/pins` 带 EXPLAIN 结论注释的 SQL、九域分层、11 项定时任务表；前端部分含 dio 五拦截器执行顺序、`ApiErrorCode` 全量 Dart 枚举、`40101` 单飞续期时序图与三个断言点、Provider 异步化改造链与竞态处理、缓存键五要素、埋点四段计时纪律、出包门禁三层。
      **两处口径冲突已裁定并同步回写**（经用户确认）：① `category_version` 统一为 `YYYY-MM-DD.N` **字符串**（此前 PRD 与架构一处写整数），已同步 DDL 初始数据、数据库设计文档 §3.15/§7.5、OpenAPI `CategoryVersion` schema；② `40301` 未实名详情额度 **Batch1 关闭**，新增 `system_config.detail_quota_enabled='off'` 配置行控制，代码路径完整实现仅由开关短路 —— 理由是 `cert` 域延后到 Batch2，Batch1 全员 `realname_status='none'`，额度生效会把「地图→详情→联系」P0 主闭环卡死。
      **本次刻意做的一件事：把「Dart 不能照抄 Java」写进设计文档。** `grid_id` 是全项目唯一需要三方（Dart / Java / SQL）逐位一致的算法，而 Dart 的 `~/` 与 `.toInt()` 对负数是**向零截断**、Java 的 `Math.floorDiv` 与 `(long) Math.floor` 是**向负无穷取整** —— 语义不同但都能编译通过、正坐标下结果还完全相同。若不显式写下这条对照，客户端极可能直接把 Java 实现翻译过来，然后在西半球/南半球坐标上静默错一格，表现为「缓存命中率长期偏低且查不出原因」。同一逻辑下，测试向量第 9 条 `(-0.000015,-0.000015)→"-1_-1"` 被标注为**唯一能暴露截断 bug 的向量，但它的最终结果偶然正确**（截断得中间微度 -1、floor 得 -2，除以 450 向下取整后都是 -1），因此该向量**必须额外断言中间微度值 == -2**，只断言最终 `grid_id` 属于白测。
      **登记 5 项已识别缺口**（写入文档 §20，不在本阶段解决）：① `restart_window` 表未在 `V1__init_schema.sql` 建表，导致可观测性 §4.2.2 的「重启窗口排除」是一句无法执行的话，建议由**服务端启动时自写**（客户端不知道服务端何时重启）；② `NfrPerf.clusterModeSwitchMetersPerPixel` 待 POC 实测校准，不在编码期猜；③ `post_media` 与架构 §5.1 `media` 表名差异（已裁定沿用 PRD 口径，此处仅登记以免再起争议）；④ 客户端 `mem_peak_mb` 的 Android 平台通道未实现；⑤ `track_drop_stat` 落表方式未定，Batch1 选写日志。
      **过程中踩到的工具坑（已固化为做法）**：`Write` 工具单次写入过长内容会间歇性丢参数（报 `missing field 'content'`/`'file_path'`，累计 5 次）。改为**分段写入**——先 `Write` 写骨架并留 `<!-- SECTION-MARKER-XXX -->` 占位注释，再用 `SearchReplace` 逐段把标记替换为正文并在末尾留下一个标记。两份长文档都用这个模式一次通过。另：PowerShell 下 `python -c @'...'@` here-string 会被 shell 吞引号，正确做法是先落临时脚本文件再执行、验完删除。

- [x] **前端视角审查两份设计交付物并落地 11 条修复**（条目 [92]，2026-09-06）：条目 [91] 产出的 `openapi.yaml` 与《前后端详细设计文档》完成后，用户要求「前端视角审查一下」。审查以**实现者视角**进行 —— 判据不是「文档写得全不全」，而是「照这份文档写代码会不会出错」。全部结论用 Grep/Read 对契约与现有 `lib/` 代码取证，不做目测。共发现 **3 个 P0 + 4 个 P1 + 4 个 P2**，用户裁定「全部 11 条一次改完」，已全部落地。
      **P0-1：我自己在 §11.1 写下的论证是错的。** 原文写「`RetryInterceptor` 在最内层，重发时复用原始 `RequestOptions`，因此 `Idempotency-Key` 天然沿用」。实际上 dio 拦截器**不是嵌套调用栈**，而是一个列表（发出正序遍历、返回倒序遍历）；重发的标准写法 `dio.fetch(err.requestOptions)` 会**重走完整 onRequest 链**，`HeaderInterceptor` 因此再执行一次 `uuid.v4()`，幂等保护当场失效。「位置」保护不了任何东西。新增 §11.1.1，改为靠**幂等注入**（写头前先判 `containsKey`），并给出决策矩阵证明这条纪律**不依赖 dio 实际行为**：加了在两种行为下都对，不加则至少一种错 —— 所以它不是待验证项而是无条件纪律。另补三条单测断言，第三条是「`uuid.v4()` 全链路只被调用 1 次」，因为前两条在「两次都恰好没生成」的假实现下也能通过。
      **P0-2：整段漏写 —— `category_id` 的语义迁移，会在 release 包直接崩溃。** 契约 `PinsCompactResponse` 的 `category_id` 是**叶子类目 ID**（example `10101`），而现有 [listing_category.dart](file:///d:/developer/code/aicoding/s2s/lib/domain/listing_category.dart#L94-L112) 的 `id => index` 取值 `0..4`、`listingCategoryFromId` 直接拿它做 `values[id]` 下标。debug 下 `assert` 拦住（但发现点在 UI 渲染，离根因很远）；**release 下 `assert` 不参与编译，直接 `RangeError`**。同时 `grid_cluster.dart:102` 的分桶键 `'$gx:$gy:${p.categoryId}'` **不会崩但会静默变味** —— 分桶粒度从「5 个一级大类」变成「数十个叶子类目」，桶数暴增、聚合失效，地图上该聚成一圈的地方散成一片点，不报错只是「效果不对」，是最难归因的一类缺陷。新增 §10.4.1 给出四步迁移：拆成 `leafCategoryId` + `topCategory` 两字段、一级大类只能**查表**不许按编码规则算术推导（编码规则是服务端实现细节，且分类树带版本号会演进）、`listingCategoryFromId` 换成返回 `null` 的查表函数（查不到是 §16.4 规定的**预期状态**，不是异常，调用方降级为中性配色而非丢弃 Pin）、删掉 `id => index` 这个**名字本身在诱导误用**的 getter。验收要求明确写「必须跑 release」—— debug 下 `assert` 会拦住，反而掩盖了真问题。
      **P0-3：两条既有规则叠在一起会静默丢埋点。** §17.4 规定「上报失败退回队列**头部**」，§11.1.1 规定「重试沿用原幂等键」—— 二者叠加后：首批返 `42906` 一条未落库 → 退回队列头部 → 期间新采集 20 条 → 重新组批（内容已变）→ 沿用旧 Key → 服务端按幂等语义**直接返回首次的 `code=0`，业务逻辑一行不执行** → 客户端判成功并把 70 条全部出队。**70 条事件一条未落库，且不产生任何错误日志**，`dropped_count` 也不会 +1（它只统计容量溢出）。可观测性系统自己成了盲区，这是最坏的一类缺陷。且服务端「重复 Key 但请求体不同须返首次结果」是幂等的**正确语义**（后端 §3.3 明文），所以防线只能建在客户端。新增 §17.4.1 定规则：**埋点批次每次组批换新 `Idempotency-Key`**（批次是「一次传输动作」，不是「一笔业务意图」），并给出与普通写接口的五行对照表说明两者为何相反；换新键带来的重复写入风险由服务端按**事件维度**去重兜住（键 `(user_id, event, interaction_id, ts)`，不依赖幂等键），取舍方向写死为「**宁可重一条，不可丢一条**」—— 重复可在分析侧去重，丢失永久不可恢复。此项连带登记新缺口 #7：该去重唯一约束尚未在 DDL 中体现，**必须在客户端换新 Key 上线前完成**。
      **P1 四条**：④ `shared_preferences` 装不下埋点队列（2000 条约 400KB～1MB）与 Pin 缓存 —— 不是容量硬限制，而是**性能形态不匹配**：Android 侧启动期全量同步加载 + 每次 `setString` 全量重写，写 n 条的总代价是 **O(n²)**，而队列变满恰恰发生在弱网/离线积压时（最不该雪上加霜的时刻）。新增 §10.2.1 分三层存储（键值层 `shared_preferences` / 队列层**追加写文件** JSON Lines / 缓存层内存 LRU + 文件落盘），并说明为何不上 `sqflite`（唯一需求是「追加+顺序读+头部截断」，文件天然擅长，引数据库要付 schema 迁移/异步初始化/平台插件三笔成本），因此新增依赖 `path_provider`。⑤ 供需态**三套表示法且一半同名**（本地 `supply`/`demand`、契约 `"resource"`/`"demand"`、紧凑数组 `0`/`1`）：`demand` 三处同名、`supply` 与 `resource` 不同名，于是 `values.byName` 的错误实现会「**一半能跑**」—— 传 `demand` 通过、传 `resource` 抛异常，比全错难查得多（会让人相信「映射对了、只是某条数据有问题」）。另指出紧凑数组的 `0`/`1` 与 `SupplyDemand` 的 index **当前恰好一致**，`values[code]` 现在能跑但靠的是巧合，故显式 switch「不是啰嗦，是把巧合换成约定」。⑥ `X-Interaction-Id` 契约全局 required，但埋点是全系统**唯一没有交互起点**的调用（定时/进后台/登录后补报/离线回灌四种触发都不属于任何进行中交互，且一批 50 条可能来自几十个交互 —— 沿用其中任一条都会把整批线索指向无关交互）；新增 §17.4.2 把批次级该头的语义收窄为「这次传输动作」、每批新生成，事件所属交互保留在请求体 `events[].interaction_id`，两者互不覆盖。⑦ 两处基础类型不匹配：帖子 ID 契约 `int64` vs 本地 `String`（改 `int`，理由是相等性陷阱、`int64` 精度、校验点收敛到唯一入口）；半径契约**字符串**枚举 `['1','3','5','10','city']` vs 本地 `int?`（`null` 代表全城），必须显式双向映射 —— **若写成 `km.toString()`，全城档会得到字符串 `"null"`**，服务端按「五要素不许兜底」回 `40001`，而客户端报错只说「参数错误」不说是哪个参数，排查成本极高。
      **P2 四条**：⑧ §15.2「取消的请求不上报埋点」与 §17.2「`cancelled` 会话必须上报」字面矛盾，统一以 §17.2 为准并说清「不污染 P95 是靠**分母排除**、不是靠不上报」—— 不上报会一并丢掉「取消频率」这个信号，而取消率升高恰是「首屏太慢导致用户反复重试」的典型征兆。⑨ `pubspec.yaml` 缺 `dio`/`uuid`/`connectivity_plus`/`image_picker`/`cached_network_image` 五项（另加 `path_provider`），且 §10.2 依赖表漏列**已在用**的 `url_launcher`。⑩ 架构 §9.2 的 10 行交付物矩阵**自身漏了「联系外呼与降级」**（PRD §7.8）—— 这是 Batch1 P0 闭环的最后一步（看到帖子 → 拿到联系方式 → **打出这个电话**），只照矩阵施工会做出一个「能看不能联系」的应用；新增 §20.1，含三条降级路径与「`canLaunchUrl` 返 false 不等于报错」的纪律（iOS 未声明白名单的 scheme **恒返 false**，此时根因在配置不在设备，弹「拨号失败」只会误导用户去检查自己的手机，正确处理是降级到复制号码），以及「联系方式用完即弃、不落本地存储」（否则会绕过服务端加解密与 AAD 绑定的整套防护）。⑪ 手动注入 `Accept-Encoding: gzip` 有踩中「显式设置即需调用方自行解压」语义的风险，届时 `response.data as Map` 会在所有接口上抛类型错误；标为**待实测**（§11.5 + 缺口 #6），默认取「**不手动注入**」—— 多余的头收益为零（Dart `HttpClient` 默认已开压缩），踩中的代价是整条网络层解包崩溃。⑫ `ContactInfo.remaining_today` 只回**账号维度**，但限频实际有账号/设备/IP/熔断**四维**，于是「UI 显示还可查看 3 次 → 用户点第 2 次因设备维度超限返 `42902`」是**必然会发生**的场景，应用当场自我否定；新增 §20.2 定文案规则（「以实际请求结果为准」、收到 `42902` 立即把本地剩余次数**刷成 0** 以免「还剩 3 次」与「已达上限」同屏、不解释是哪个维度超限），并明确**服务端不改为四维最小值**（那会把设备维度限额暴露给客户端，可被反推出风控阈值）。
      **改动落点**：《前后端详细设计文档》新增 §10.2.1、§10.4（4 个子节 + 8 条迁移自检）、§11.1.1、§11.5、§17.4.1、§17.4.2、§20（矩阵补项两节），改写 §11.1/§11.2/§15.2/§17.4 与交付物矩阵（10 → **12 行**，补入「联系外呼与降级」和「模型语义迁移」两项漏项），§19 自检清单 **18 → 34 条**并按五组归类，缺口表移至 §21 并 **5 → 7 项**；`openapi.yaml` 改 4 处（`XInteractionId` 与 `IdempotencyKey` 两个 parameter 各加埋点例外说明、`/track/events` 补两头特殊规则与服务端去重口径、`ContactInfo.remaining_today` 补 UI 文案口径）。契约实测复核：`operations 41` / `Batch1 21` / `schemas 31` / `parameters 6` / **断链 0**，与修改前完全一致。
      **本轮最值得记的一点：三个 P0 中有两个是「我自己上一轮写错的」，且都不是笔误而是论证错误。** P0-1 是把「拦截器有顺序」误推成「顺序能保护幂等键」—— 一个听起来自洽、且在多数情况下看起来能跑的结论；P0-3 是两条各自正确的规则（失败退回队列头部 / 重试沿用幂等键）叠在一起产生了第三种错误行为，**任何单独一条都无可指摘**。这印证并扩展了方法论 4.3「多视角评审要换视角不是换 persona 数量」：**换成实现者视角的价值，恰恰在于它会去追问「这行代码实际会怎么执行」，而写作者视角只会检查「这段话是否自洽」。** 另一条可复用的判据也在本轮成形 —— **当某条纪律的正确性依赖于对第三方库行为的假设时，优先选择「在所有可能行为下都正确」的写法**（P0-1 的决策矩阵：加了幂等注入在两种 dio 行为下都对，不加则至少一种错），这比「先去实测再决定」更省，也彻底消除了假设失效的风险。此外本轮又一次撞上「Dart `assert` 在 release 不参与编译」（P0-2），这已是该教训在本项目的第三次现身，说明它需要的不是再记一遍，而是**进静态扫描清单**。

- [x] **关闭缺口 #7：埋点事件去重约束落定到 DDL**（条目 [93]，2026-09-06）：条目 [92] 的 P0-3 定下「埋点批次每次组批换新 `Idempotency-Key`」后，重复写入的防线就完全依赖服务端去重，而该约束当时只存在于文字描述、未在 DDL 中体现，被登记为缺口 #7 并注明「必须在客户端换新 Key 上线前完成」。本轮按用户指令优先处理。
      **两处裁定（经用户确认）**：① 形态取「**唯一索引 + `INSERT ... ON DUPLICATE KEY UPDATE id=id`**」，而非「普通索引 + 落库前 SELECT 判重」—— 后者无法消除并发下两批同时到达的 race（判重与插入之间存在窗口），且每批多一次查询；也不用 `INSERT IGNORE`，理由见下。② `user_id` 列**保留 NULL 定义**，不改 `NOT NULL` —— 契约已规定 `/track/events` 必须登录态且 `user_id` 一律取登录态，入库路径上恒非 NULL；字段表里「未登录事件补报前为 NULL」描述的是**客户端本地队列**状态而非数据库行状态。但这是一个**假设**，故用断言把它变成可执行检查而不是靠记忆维持。
      **索引设计：`uk_event_dedup(interaction_id, event_name, user_id, ts)`，净增索引 0 个。** 列序把 `interaction_id` 置最左有两个收益：它基数最高（每次交互一个 UUID）故判重最快；其最左前缀**完全覆盖**原 `idx_interaction(interaction_id)`，因此该索引被删除 —— 加了一个唯一索引却没有增加任何写放大。这一点是刻意找的：埋点表是全系统写入最频繁的表，任何净增索引都会直接抵扣写入吞吐。
      **严禁 `INSERT IGNORE`，这是本轮最关键的一条实现纪律。** 它与 `ON DUPLICATE KEY UPDATE id=id` 只差几个字符、行为看起来相同，但 `IGNORE` 不只忽略唯一键冲突，还会把**数据截断、非空列插 NULL、类型转换失败**一并降级为 warning。在埋点写入路径上用它，等于关掉这张表全部的数据质量报错 —— 而埋点本就是「异步旁路、允许丢、低关注度」的链路，一旦静默容错，脏数据可以积累很久无人察觉。文档里同时禁掉了另一个更隐蔽的错写法 `ON DUPLICATE KEY UPDATE props = VALUES(props)`：它看起来更"合理"（用新值更新），却会让后到的重复批次**覆盖**首次落库的事件属性，且**任何行数校验都看不出异常**。因此 A8.1 特意断言 `props` 保持首次值，而不只断言行数为 1。
      **发现并写下一条「约束的成立条件」：`ts` 必须是客户端采集时刻。** 这原本只是月表归属口径（PRD 既有规定），本轮才意识到它同时是去重约束的**成立前提** —— 若实现时把 `ts` 改成服务端到达时刻（一个看起来无害甚至更"准确"的改动），同批事件重传会拿到不同 `ts`，唯一键当场失效、去重能力归零，而**表面上一切正常：索引还在、不报错、只是不再挡住任何东西**。同类失效路径共三条（预建任务漏建索引、落库改用 `IGNORE`、`ts` 换成到达时刻），共同特征是**全都不产生错误信号**，只表现为按次数计算的指标逐渐偏高。三条已分别写入后端流程第 [7] 步、后端自检清单、可观测性 §4.4。
      **连带确立的客户端纪律：事件入队后去重键三列冻结。** 去重键四列中有三列（`interaction_id`/`ts`/`event`）来自客户端，因此「在上报前补全字段」这类看似修复的操作（如发现缺 `ts` 就用当前时间补上、重传时刷新 `ts`）会让去重键每次重传都变化。纪律定为：**上报器只做读取与序列化，不做任何加工**。同时把 `TrackEvent.interaction_id` 从可选改为 **required**（它是去重键成员，缺失即无法去重），并要求 `ts` 带毫秒精度（去重列是 `DATETIME(3)`，只到秒会误丢同秒内的多次同名事件）。
      **兜底表刻意不建这个索引，并把理由写进 DDL 注释。** `track_event_fallback` 是缓冲不是终点，去重只需在数据抵达终点时发生一次；搬运任务用 `ON DUPLICATE KEY UPDATE` 写月表，兜底期积累的重复会被月表索引一并消化。在兜底表建唯一索引反而会拖慢降级路径写入 —— 而降级路径恰是最需要写得快、最不能再失败一次的时刻。同时明确 **A7 断言只比对列结构、不比对索引**（两表索引本就应当不同），避免后续有人以为是漏建。另补一条月表预建纪律：**必须用 `CREATE TABLE ... LIKE`**，逐列拼 DDL 会漏掉索引，导致「下个月起去重静默失效」且因为「上个月一直是好的」而极难归因。
      **改动落点**：`V1__init_track_schema.sql`（月表加 `uk_event_dedup`、删 `idx_interaction`、兜底表补「为何不建」注释）；《数据库设计文档》新增 **§3.16.1**（约束的完整论证：为何必须存在、为何是这四列、`ts` 前提、禁 `IGNORE`、反向风险与准入纪律、`user_id` NULL 缺口、预建任务须用 `LIKE`），§3.16 索引表与边界 4→5 条，§3.17 索引说明，§4.4 断言表补 A8 + 5 个子断言预期表，文件树计数 7→8；`assert_behavior.sh` 新增 **A8（5 个子断言）**；《前后端详细设计文档》§5.10 埋点落库补第 [7] 步 + 三条实现纪律、定时任务 #2 补 `LIKE`、后端自检 +4 条、§17.4.1 改写（补三列客户端约束表）、§19 埋点自检 +2 条、缺口 #7 **标记关闭**；`openapi.yaml` 改 3 处（`/track/events` 去重节重写、`IdempotencyKey` 表述对齐、`TrackEvent` 的 required 与两字段描述）；可观测性 §4.4 补去重条目与三条失效路径。
      **实测复核**：`openapi.yaml` 复检 `operations 41 / Batch1 21 / schemas 31 / parameters 6 / 断链 0`，与改前完全一致；定向核验 `TrackEvent.required` 已含 `interaction_id`、旧去重键字符串已全部清除。DDL 静态复检括号配平、反引号成对、月表索引清单为 `uk_event_dedup + idx_event_ts + idx_user_ts`（`idx_interaction` 确认已删）、兜底表无 UNIQUE、**两表列集合完全一致**（A7 的静态预检）。
      **一处必须说明的未完成项**：A8 的 5 个子断言**尚未实测** —— 行为断言依赖 `docker exec` 到 MySQL 容器，而当前环境 `docker` 命令不可用（实测报 `CommandNotFoundException`）。已在《数据库设计文档》§4.4 明确标注「⏳ 待实测」并写明原因，同时列入后端自检清单。**没有把它写成通过**：这条约束的三种失效路径全部不产生错误信号，恰恰是最不能靠「DDL 看起来对」就放行的一类。
      **本轮方法论增量**：新成形一条判据 —— **凡「加约束」的改动，都要同时问它的成立条件是什么、以及它失效时会不会报错**。`uk_event_dedup` 的成立条件（`ts` 取采集时刻）此前散落在另一个上下文（月表归属）里，若不主动追问就会在实现期被一个"更准确"的改动无声破坏。这类「静默失效型约束」与常见的「会报错型约束」需要不同的守护方式：后者靠测试用例，前者必须靠**断言 + 把成立条件写在约束旁边**。另：本轮再次验证了「结论必实测」的价值边界 —— 环境不具备时，**正确做法是标注待实测并说明原因，而不是降低标准或改用目测**。

- [x] **§10.4 模型语义迁移落地 + pubspec 依赖补齐**（条目 [94]，2026-09-06）：条目 [92] 的 P0-2 定下四步迁移方案后，方案只存在于文档；本轮按用户指令把《前端详细设计文档》§10.4 的四个子节全部落到 `lib/` 代码，并补齐 §10.2 依赖表中缺失的 6 项 `pubspec` 依赖，配单测与全量验证。
      **一处架构冲突需先裁定（经用户确认）**：§10.4.1 要求 `ClusterPoint` 持有 `topCategory: ListingCategory`，但该枚举因 `color`/`deepColor`/`icon` 三个成员必须 `import material.dart`，而聚合模块（`features/map/clustering/`）**刻意保持纯 Dart** —— POC-A 基准要在 Dart VM 里直接跑（`dart run tool/poc_a_cluster_benchmark.dart`）。用户裁定「**拆枚举：纯 Dart 本体 + Flutter 样式扩展**」。取证时发现拆分成本比预想低：`design_tokens.dart` **零 import**、色板是纯 `int` 常量，枚举里真正需要 Flutter 的只有色与图标，`label` 与 `clusterThreshold` 本就是纯 Dart。
      **拆分界线定为语义而非技术：`clusterThreshold` 参与计算故留业务侧，色与图标只参与绘制故归样式侧。** 这条界线比「按是否依赖 Flutter 划」更稳 —— 后者是当前实现的偶然结果（今天阈值恰好是 `int`），前者说的是「这个属性在系统里干什么」。前提是否真的成立由基准自身证明：`dart run` 跑通，线性度 ×3.08（均匀）/ ×3.55（聚集）双 PASS。
      **迁移八条自检的取证结论：6 条已证、1 条部分证、1 条为类型检查。** 其中 **#4「`category_id=10101` 在 release 包渲染通过」只能算「逻辑层已证、渲染层待补」** —— 规格原文的判定方式是 `flutter run --release` 实跑，我用的是 `dart --no-enable-asserts run` 临时探针（并用 `assert(() { assertsOn = true; return true; }())` 自证当前确为 release 的 assert 语义），证得 `topCategoryOf(10101)=work`、`999999→null`、分桶 2 桶 2 marker、无 `RangeError`。但探针覆盖不到 `CustomPainter` 里取色取图标那一段，**两种验证不等价，故不写成通过**。
      **三项规格外补充，都是实现期才暴露的规格空洞。** ① §10.4.2/§10.4.3 的代码示例引用了 `ApiException.parse(...)`，但**该类型全项目不存在**（只有 `ContactException`）—— 按最小方案新建仅含 `parseError` 的 `lib/core/api_exception.dart`；同理 §10.4.1 引用的 `CategoryTree` 也不存在，改用 `List<CategoryNode> tree = categoryTree` 可选参数表达同一语义，**不为了对齐文档而新造类型**。② §10.4.2 只给了 `fromApi` 下行方向，**缺了上行的话调用方会就地写 `sd.name`** —— 而 `SupplyDemand.supply.name` 是 `'supply'`，不是契约的 `'resource'`，等于「一半能跑」陷阱的镜像；补 `toApiPostType`。③ `CategoryNode.topCategory` 原先用 `values[top - 1]`，与 `listingCategoryFromId` 是**同一类隐患**（越界 `RangeError`、同样只有 debug 的 assert 挡着），但 §10.4 通篇未提 —— 主动一并修掉并补越界断言。
      **实现期确立 ID 类型分层界线**：§10.4.3 只说「帖子 ID 改 `int`」，但落地要求分层 —— 聚合层与 Marker 层保持 `String`（`ClusterPoint.id`/`SinglePointMarker.listingId` 是本地分桶标识，只做 Map 键与去重），域模型 `Listing.id` 为 `int`，交界处 `.toString()` 对齐，**字符串解析收敛到路由唯一一处** `int.tryParse(...) ?? -1`。取 `-1` 而非抛异常：非法深链语义上等于「这条信息不存在」，走 provider 返回 `null` 显示「信息不存在」，与「已下架」是同一种用户可理解的结果，抛异常只会白屏。迁移中还撞上一个 **info 级不报错的坑**：`supplyDemandById` 的键类型若与查表方 `marker.listingId` 不一致，`containsKey` 永远为 `false`，analyzer 只给提示不报错。
      **降级取向：查不到大类时用明显不属于五色板的中性灰，绝不兜底成任一真实分类色。** 新增 `neutralCategoryColor`（取 `AppColors.textSecondary`）+ `Icons.help_outline`。理由是**兜真实色会让「本地分类树版本落后」这个数据问题伪装成「配色错了」的设计问题**，排查方向当场跑偏。同理 `publish_form_state.dart` 在大类为 `null` 时**不加标题前缀**而非兜一个「【求助】」——前缀会进标题落库，猜错等于替用户改文案。另 `topCategoryOf` 刻意遍历分类树而非按编码规则取前几位：编码规则是服务端实现细节且可能变，而分类树带版本号。
      **改动落点**：新建 `lib/core/api_exception.dart`、`lib/domain/listing_category_style.dart`、`test/contract_mapping_test.dart`；改写 `listing_category.dart`、`marker_builder.dart`、`test/grid_cluster_test.dart`（增 2 条 P0 守卫用例：同大类不同叶子仍聚合 / 大类为 null 自成一桶）；修改 `category_tree.dart`、`grid_cluster.dart`、`marker_layer.dart`、`map_screen.dart`、`app_router.dart`、`listing.dart`、`discovery_filter.dart` 及 discovery/detail/contact/publish 四域连带文件；两个 `tool/poc_*` 基准同步；`pubspec.yaml` 补 `dio`/`uuid`/`connectivity_plus`/`image_picker`/`cached_network_image`/`path_provider`；《前后端详细设计文档》新增 **§10.4.5 落地记录**。
      **全量验证**：`dart analyze` 全项目 **`No issues found!`**（含 info 级）；`flutter test` **256 个用例 All tests passed**；新增/改写的 4 个测试文件单独跑 **55 个用例全通过**；POC-A 基准双 PASS。所有临时产物（`.localcache/`、`.dartcache/`、release 语义探针脚本）已清理，`git status --short` 确认无残留。
      **本轮方法论增量两条。** ① **「工具返回空结果」不等于「没有问题」。** `flutter analyze` 被沙箱拦住（无法写 `%LOCALAPPDATA%\.dartServer` 临时文件）后我改用 `GetDiagnostics`，得到 `[]` —— 但这与「`Listing.id` 刚从 `String` 改成 `int`、调用点必然一片红」的已知事实直接矛盾，故**主动判定该结果不可信、不予采信**，转而用 `$env:LOCALAPPDATA="<项目内目录>"` 把缓存目录重定向，一次拿到完整的 **77 条**错误清单。若当时采信了那个 `[]`，本轮会带着 77 处编译错误宣布完成。判据可复用：**当验证结果与已知事实冲突时，先怀疑验证手段，而不是修正对事实的认知。** ② **文档里引用的类型不一定存在。** 上一轮写 §10.4 时我引用了 `ApiException` 与 `CategoryTree` 两个「听起来理应存在」的类型，实现期才发现都是凭空的。这类错误在写作视角下完全不可见（文字自洽），只有在实现视角下才会暴露 —— 与条目 [92] 那条「换实现者视角的价值在于它会追问代码实际怎么执行」是同一条规律的另一个侧面：**它还会追问「这个名字指向的东西真的存在吗」**。

- [x] **补跑行为断言 A8：缺口 #7 的最后一项遗留清零**（条目 [95]，2026-09-06）：条目 [93] 关闭缺口 #7 时留下一项未完成 —— A8 的 5 个子断言因环境无 Docker 未能实测，当时按纪律标注「⏳ 待实测」而未写成通过。本轮用户告知 **Docker 装在 WSL 里**，实测环境具备，遂补跑。
      **环境探测结果**：WSL2 Ubuntu-22.04 运行中，Docker 28.1.1，`mysql:8.0` 容器 `mysql8` 已 `Up 21 hours (healthy)`。**此前判定「环境不具备」是错的** —— 我只在 Windows 宿主执行了 `docker --version`（报 `CommandNotFoundException`）就下了结论，没有想到 Docker 可能装在 WSL 内。教训见下。
      **A1–A8 共 8 项全部实测通过。** A8 五条实测值与预期逐条吻合：A8.1 `rows=1` 且 **`props_n=1`**、A8.2 `2` 行、A8.3 `2` 行、A8.4 `0` 行、A8.5 `type=ref / key=uk_event_dedup / key_len=144 / Using where; Using index`（覆盖索引、无回表）。另做索引结构静态复核：`uk_event_dedup` 四列顺序与设计一致、`idx_interaction` 确认已删、月表索引恰 4 项、兜底表仅 `PRIMARY` 无 UNIQUE。
      **A8.1 的 `props_n=1` 是这次补跑最有价值的一条产出。** 它是五条中唯一能区分 `ON DUPLICATE KEY UPDATE id=id`（正确）与 `props = VALUES(props)`（看起来更"合理"的错写法）的断言 —— 后者行数断言**依然通过**，只是事件属性被后到的重复批次覆盖，任何行数校验都看不出来。条目 [93] 当时预判「A8.1 是最容易写错又最难发现的一项」，本轮实测证实了这个预判的必要性：如果只断言行数，这条实测等于没做。
      **本轮我犯了一个必须记录的操作错误：污染了含 50 万行压测数据的库。** 脚本签名是 `assert_behavior.sh <容器> <业务库> <埋点库>`，我第一次调用时第二个参数直接传了 `s2s_batch1` —— 那是既有的压测库。A1–A5 随即往里插了 3 条 post、2 个 user、1 条 user_identity、1 条 favorite，并把 `post.id=1` 从 `active/NULL` 改成了 `archived/1`。**发现方式**：跑完后我主动去核对库的行数，看到 `post_total` 从 493857 变成 500003 才意识到。已逐项精确回滚（删 3 条新增 post、`id=1` 状态复位、清 3 张辅助表、`AUTO_INCREMENT` 复位 500001），复核 `post_total=500000`、`status_reason IS NOT NULL` 计数为 0。**⚠️ 但此处的复核不完整，`id=1` 的 status 实际复位错了 —— 详见条目 [96]，已于同日修正为 `draft`。** 随后改用两个独立空库（`s2s_biz_assert` + `s2s_track_batch1`）重跑全部 8 项，验完即 DROP，库列表与操作前一致。
      **根因不是手滑，是我没读脚本就传参。** 脚本头部注释明写了三个参数的含义，而我只关心「埋点库参数传对了」，对业务库参数用了个「看起来是这个项目的库名」就交上去。既有 `s2s_track` 是旧版 DDL（无 `uk_event_dedup`）这件事我查过并特意避开了，却没对业务库做同样的检查 —— **同一个风险，我只在一半的参数上防住了**。已在《数据库设计文档》§4.4 补上留档：「两个库名参数必须传独立空库，脚本内 A1–A5 会插数据并 `UPDATE post WHERE id=1`」，让下一个人不必靠读脚本才发现。
      **改动落点**：《数据库设计文档》§4.4 断言表 A8 改为通过 + A8 子断言表增「实测」列 + 补三段说明（A8.1 不可省的理由、索引静态复核结论、实测方法与空库要求）、文件树注释 `A8 待实测` → `A1–A8 全部实测通过`；《后端详细设计文档》§21 缺口 #7 行去掉「遗留一项」改为「无遗留项」。
      **本轮方法论增量两条。** ① **「命令不存在」不等于「能力不存在」。** 条目 [93] 里我因宿主 `docker` 报 `CommandNotFoundException` 就判定环境不具备，把 A8 挂了起来 —— 但工具其实一直在 WSL 里跑着。当时那个判断在形式上是「实测后得出的结论」，所以我没有再怀疑它。**判据补正：环境探测失败时，要区分「这台机器没有这个能力」与「我没找到调用它的路径」，后者应至少换一条路径再试**（宿主 → WSL → 容器 → 远程）。② **对破坏性脚本，传参前必须先读它对每个参数做了什么。** 这条听起来是常识，但本轮暴露的是更具体的失误形态：我对三个参数中的两个做了尽调（容器名、埋点库），唯独漏掉的那个正是唯一会被写入的库。**风险检查不能只覆盖「我正在关注的那个参数」，要按参数逐个过一遍。**

- [x] **沉淀「破坏性脚本传参审计」学习文档 + 发现并修掉一处残留污染**（条目 [96]，2026-09-06）：调用 `ce-compound` 技能（Interactive + Full 模式），把条目 [95] 的压测库污染事故沉淀为 `docs/solutions/` 下的持久化学习。**本轮最重要的产出不是文档本身，而是接地校验过程中发现条目 [95] 的回滚做错了一半。**
      **语料库重叠判定为 Low，故新建独立篇目。** 逐篇核对既有 8 篇后确认：目标主题的三要素（破坏性脚本 / 传参前逐参数确认 / 污染真实库）从未在同一篇中同时出现，最接近的 `test-data-fidelity` 覆盖约 28%（只有一句「不污染主库」的隔离纪律，缺传参审计这一环）；45 个既有标签中无任何一个指向破坏性操作或数据安全语义。新篇 `workflow-issues/destructive-script-parameter-audit.md`，frontmatter 按 corpus-first 取既有分布值（`module: docs` / `component: database` / `root_cause: missing_validation` / `resolution_type: workflow_improvement`）。
      **接地校验查出条目 [95] 的回滚残留：`post.id=1` 的 status 我复位错了。** 校验员指出 `assert_behavior.sh:56` 还有一条**我此前完全没注意到的无守卫语句** `UPDATE post SET status='active' WHERE id=1`（先于 `:62` 那条带前驱守卫的执行），它丢弃了原值且不留痕迹。据造数脚本 `explain_pins_real.sh:59` 的 `ELT((FLOOR(n/1000) % 4) + 1, 'draft','active','active','archived')` 反推，`n=1` 应落 `draft`，而我当时复位成了 `active`。**实测证实污染确实残留**：`id=1` 为 `active` 而 `id=2..5` 全为 `draft`，status 分布 `draft 124999 / active 250001 / archived 125000` —— 恰好错位一行。已 `UPDATE ... SET status='draft' WHERE id=1` 修复，复核 `125000/250000/125000` + `COUNT(*)=500000` + `MAX(id)=500000` + `COUNT(status_reason)=0`，与造数公式完全吻合。
      **根因是复核不变量选得不全，而不全的方式很隐蔽。** 条目 [95] 的复核查了三项：总行数、`status_reason` 非空计数、`MAX(id)`。**这三项恰好一项都碰不到 `status` 列本身** —— 行数对、`status_reason` 已清空、`MAX(id)` 已复位，全绿。**「复核不变量选得不全」与「没有复核」在观感上完全一致，都是全绿**，这比原事故更值得记：原事故至少有个行数差异能被发现，而错误的复核会主动给出「已复原」的结论并终结排查。正确做法是把不变量由写入面逐项推导（脚本改了哪一列就查哪一列），而不是挑几个「看起来能代表健康度」的聚合数字。
      **另修正校验查出的其余 5 处失真**（全部是文档写作中的失真，非新缺陷）：① 「脚本从头到尾没有任何一处报错」与 A1/A3 的预期报错（ERROR 3105 / 1062）冲突，改为「没有产生任何**指向污染**的错误信号」；② 「A1–A5 写入型 / A6–A8 埋点写入型」忽略了 A7、A8.4、A8.5 是纯读、A1 是被 DDL 拒绝的写尝试；③ 示例代码 `bash <脚本>` 与 `wsl -e docker` 混用，在宿主不可运行，会重演本篇批评的路径失误，统一为 `wsl -e bash`；④ 「s2s_track 是旧版 DDL」是当时实例状态、与仓库现存 DDL 不符，加限定；⑤ 明示三个危险默认值**至今仍在脚本里**，本轮修复只落在文档侧，避免读者误以为已整改。
      **词汇捕获**：`CONCEPTS.md` 新增「数据落库链 / 写入面」条目（一个脚本实际会写到的库表行集合，由写语句决定而非名字决定；含逐参数审计、定点改写危害、默认值即隐式授权、复核不变量须由写入面推导四条行为规则）；Flagged ambiguities 新增一条 ——「不含决策」曾被当作「无害」的同义词，而验证脚手架按「含不含拍板选择」判定、写入面按「会不会写到真实数据」判定，两者正交。
      **改动落点**：新建 `docs/solutions/workflow-issues/destructive-script-parameter-audit.md`（约 145 行，knowledge track 五段结构 + 4 条 Related 反向链接）；`CONCEPTS.md` 增 1 词条 + 1 条歧义澄清；实测修复 `s2s_batch1.post` 的 `id=1` status 残留。
      **验证**：`validate-frontmatter.py` 与 `validate-doc-claims.py` 修正前后各跑一次，均 exit 0、`0 flags`、`checked 1 paths, 0 SHAs, 0 links`（Windows 默认 GBK 读 UTF-8 文档会 `UnicodeDecodeError`，加 `PYTHONUTF8=1` 解决，非文档缺陷）；4 条 Related 链接与 `数据库设计文档.md:693`、`phase-boundary-verification-scaffolding.md:42/:81` 等被引行号全部逐字核实。
      **本轮方法论增量两条。** ① **回滚的复核不变量必须由写入面逐项推导，不能挑代表性指标。** 这是条目 [95] 那条「按参数逐个过一遍」的下一环 —— 我在传参环节学会了逐项检查，却在**复核环节**又犯了同一个形态的错：只查了自己关注的那几个量。**同一个思维缺陷会在流程的不同环节各犯一次，修掉前一次不会自动修掉后一次。** ② **文档接地校验能查出的不只是文档错误，还有被文档固化的操作错误。** 本轮若不写这篇学习、不做接地校验，`id=1` 的错值会一直留在压测库里，且条目 [95] 里那句「与污染前完全一致」会成为永久的错误留档 —— 后续任何人复用这个库测性能，都会站在一个已被静默改动过的基线上。**把结论写成可被逐条核对的文档，本身就是一道校验。**

- [x] **《AI 工程化方法论》追加第六章：详细设计与接口契约**（条目 [97]，2026-09-06）：把条目 [91]–[96] 六轮工作（接口契约与详细设计产出、前端视角审查 11 条、DDL 去重约束落地、模型语义迁移取证、WSL 内断言补跑、破坏性脚本传参审计）抽象为方法论第六章，按文首格式要求四条执行：章节索引表登记 + 章内六段结构 + 准则须「换个项目仍然成立」+ 每条准则给可操作判定动作。
      **章的核心主张（本轮新造的抽象）**：这一阶段的缺陷有一个共同形态 —— **它们在一半的情况下是对的**。契约与详细设计不会编译失败，也不像架构文档那样能被「这里没说怎么做」暴露；它的失效方式是**部分生效**（一半枚举映射正确、一半坐标算得一样、一半重试沿用了正确的键）。**半对的缺陷比全错的贵一个量级**，因为它让人相信「机制是对的，只是某条数据有问题」，从而把排查方向永久带偏。因此全章准则指向同一个动作：**把契约当代码读** —— 写作视角只能验证「这段话是否自洽」，本章缺陷只在实现者视角下现形。
      **十条准则**：6.4 实现者视角的三个追问（这行怎么执行 / 这个名字指向的东西真的存在吗 / 这两条同时生效会怎样）、6.5「一半能跑」的映射比全错更贵（部分同名 + 序号偶然一致两类成因，衍生：可空语义字符串化会造出格式合法的错值）、6.6 两条各自正确的规则会叠出第三种行为（三类优先检查组合 + 取舍方向须在文档写死）、6.7 涉及第三方行为的纪律取「全域正确」写法而非「先去实测」（两行两列决策矩阵 + 反向约束）、6.8 加约束时必须同时写下成立条件与失效信号（静默失效型 vs 会报错型）、6.9 容错与降级的方向要写死（6.9.1 宽容模式不许进旁路链路 / 6.9.2 降级不许兜成真实业务取值）、6.10 展示层不得给出它无权判定的结论（判定维度与展示维度不对称）、6.11 清单与矩阵要验自身的闭环完整性（判定动作必须绕开矩阵口述闭环）、6.12 验证结果与已知事实冲突时先怀疑验证手段（与 3.4 准则二同源但触发方向相反：由「结果好得不该这么好」触发，故更危险）、6.13 破坏性脚本的写入面必须先取（衍生三条：定点改写危害不对称 / 复核不变量由写入面逐项推导 / 「不含决策」与「无害」正交）。
      **抽象化处理**：按格式要求③，全章已去除项目专有名词（类库名 → 「该类库」、字段名 → 「某个新增取值 / 某额度字段」、库名 → 「含五十万行的压测库」、语言与框架名 → 「某语言」），仅保留 `grep` / `containsKey` / `INSERT IGNORE` 类通用技术符号；具体数值（如 77 条错误）一律标注为「某次运行的观测值」。
      **改动落点**：`AI工程化方法论.md` 章节索引表新增第六章行（第 27 行）；文末追加标记前插入第六章正文 324 行（1421–1744 行），含 6.1 适用边界（四行判断项）、6.2 工具矩阵（五行 + 长文档分段写入的实测约定）、6.3 流程骨架（七步，关键顺序是⑤实现者视角复审必须独立于③之后单跑）、6.4–6.13 十条准则、6.14 反模式 22 条、6.15 落地检查清单 26 项（分六组）。
      **验证**：Grep 全文确认六章标题编号连续无冲突；6.14 反模式 22 条与 6.15 清单 26 项的括号引用逐条核对，全部指向存在的小节（6.4–6.13 及 6.9.1 / 6.9.2 / 各衍生）；跨章引用 1.7、3.4 准则一、3.4 准则二、4.6 衍生三、4.7、5.9 对照全文大纲核实全部存在且语义匹配；锚点 `#第六章详细设计与接口契约` 与前五章的去标点形式一致；Grep 项目专有名词表（类库名 / 字段名 / 库名前缀 / 脚本名 / 批次名等 13 个模式）返回零命中。

- [x] **安全与 DevSecOps 方案设计：交付《系统安全设计方案》《DevSecOps 接入方案》**（条目 [98]，2026-09-06）：依据既有三份架构文档 + 《前后端详细设计文档》+ PRD §12/§13/§14 + 《上架前置工作指导手册》P1–P10 + BRD §9.3 红线，输出两份新交付物（`docs/architecture/系统安全设计方案.md`、`docs/architecture/DevSecOps接入方案.md`）。**两项范围裁定经用户确认**：① DevSecOps 门禁「**仅定义规格，本期不落任何自动化**」（不建 GitHub Actions、不建 git 钩子）；② 三个「文档已承诺但仓库不存在」的交付物（出包脚本 + `release-gate.log`、`scripts/seed-perf.sql`、Q1–Q7 看板 SQL）「**只定义规格与验收判据，不写实物**」。
      **《系统安全设计方案》的定位刻意不是「重写」而是「按安全域重组 + 补空白」。** 开工前的取证结论是：安全条款绝大部分已散落在 PRD §14.3 / 架构 §4.3–§4.4 / 可观测性 §10.1 / 详设 §3.3–§3.4 中，此时若按标准目录另写一份，必然产出第二套口径。故全篇每一节都在标题或依据说明处标注「归集自 XXX」，§14 决策依据索引逐节标明「归集，无新增」/「归集 + 补充」/「**本方案新增**」，可被逐条反查。全篇 14 节，含威胁模型 T1–T4（爬虫 / GPS 泄露 / 密文跨行搬运 / 内部滥权，附残余风险）、7 个安全域、**42 项自检**（S2–S10 十组）、PRD §14.6 五项 🔴 安全验收的三列化（执行工具 / 失败判定 / **Batch1 适用性**）、PIPL 与 P1–P10 合规映射、6 项缺口登记 G1–G6。
      **§1.1 定了一条贯穿全篇的判断准则：「这不是一个数据泄露=罚款的系统」。** 联系方式批量可获取性与照片 GPS EXIF 属于**人身安全**问题，不是合规成本问题；由此推出「性能/体验/成本与批量可获取性冲突时一律牺牲前者」。这条准则的作用是给后续实现期的每一次取舍提供预先定好的方向，而不是临场权衡。
      **§5.2 单独展开了唯一一处人身安全级漏洞的时序。** OSS 直传的「原图删除必须与 `commit` 同事务同步完成」若被实现成异步清理，攻击窗口内 `GET https://<桶>/<对象名>?x-oss-process=image/info` 可直接读出 GPS。文中同时驳掉了「上传者自己知道对象名所以无所谓」的辩解 —— 泄露的对象是**上传者自己的住址**，而对象名可能出现在日志、抓包、代理链路中。
      **三处刻意写下的「反直觉但必要」论证**：① `AuditLogWriter` 必须有**字段白名单**剔除 `phone/real_name/id_card/contact_value` 及一切 `_enc`/`_hash` 结尾列 —— 审计表本身若记下明文，就成了新的泄露面；② 「启动校验缺失密钥即快速失败」是**安全条款而非健壮性条款** —— 允许缺失启动等于允许静默降级到无加密状态运行；③ pepper 与备份**必须物理分离** —— 两头都做对很容易，两头都做错（pepper 进备份 / 备份不加密）也很常见，而只要有一头对整链就成立。
      **不越权裁定的三处**：G1（后台 RBAC 越权无 5 位错误码）明写「**不得在本方案或代码中先行占号**」，理由是历史上 `40901` 曾被误占并返工；G2（PRD §12.1 引用「§6.4.3 坐标隐私模糊化」但该节不存在）明写「超出安全方案的裁量权，本方案只做一件事：把悬空引用标记出来，使它不能被当成『已经有要求了』糊过去」；D-1 构建工具只给建议（倾向 Maven），口径源仍是《技术栈选型说明》。
      **《DevSecOps 接入方案》的第一句话是「本期为规格文档、不是实施文档」，紧接着的第二句是「本方案不豁免任何门禁」。** 这两句必须同时存在 —— 否则「本期不落自动化」会被读成「本期可以不做」。为此配了 §9 **十一项落地清单**（按依赖排序 + 优先级标注），明确「规格已定义即编码期必须落地」。不落自动化的理由写实：1 人 + 38 自然日 + 131 元/年 + 2 核 2G + **服务端零代码**（连 `pom.xml` 都还不存在，此时建 CI 是给空仓库配流水线）。
      **§4 出包门禁是全篇最高优先级，因为它闭合的是两处已存在的未闭合阻塞项。** B-1：`lib/features/auth/auth_repository.dart:338` 的 `_debugCode = '888888'` 后门，三层门禁在三份文档里都写了，但**没有任何脚本载体**，等于后门可随包发布；且**不能用 `assert` 兜底**（release 下 `assert` 整体不参与编译 —— 这已是该教训在本项目的第四次现身）。判据定为三层：源码层 grep / 编译期 `kDebugMode` / **产物层 `strings | grep -c` 必须为 0**，并论证「产物层是唯一有效的最终判据」（前两层都可能被绕过或被条件编译改变）。B-2：release 仍用 debug 签名，**阻塞 P7 APP 备案** —— 且签名一经备案不可更换，换签名等于换应用身份，故正式 keystore 被列为落地清单第 2 项、标注「自身极快但阻塞长流程」。
      **§4.2 一处本方案新增：`release-gate.log` 必含 APK SHA-256。** 否则一份 PASS 日志可以被拿去为**另一个包**背书 —— 日志与产物之间没有任何绑定关系，这个漏洞不需要恶意也会发生（重新构建后忘记重跑门禁）。
      **§5.4 归出 6 项必须落成自动化测试的安全断言，共同特征是「静默降级为不安全状态」**：AAD 绑定 / `AuditLogWriter` 白名单 / 审计与业务同事务 / 原图删除同事务 / 注销删除**全部** `user_identity` 行 / 启动校验失败。选中标准不是「重要」而是「**人工检查抓不到**」—— 这六项做错时功能全部正常，没有任何错误信号。其中「注销删全部身份行」附了具体事故形态：只删 phone 行会导致微信登录**复活已注销账号**。
      **§7.2 把 Lint 强制化定为「现状最大的低成本收益点」，同时刻意不加自定义规则集。** 现状 `analysis_options.yaml` 仅 28 行、零自定义规则、无 `analyzer.errors`。本期只要求 `flutter analyze` 0 error；一次性引入严格规则集会产出上百条历史违规，最终被 `// ignore:` 淹没，收益为负。
      **§3.3 列了主动放弃的工具清单并逐条给理由**（SonarQube / Snyk / Dependabot / OWASP DC / Trivy / DAST / KMS），替代做法是人工季度复核 `flutter pub outdated` + `mvn versions:display-dependency-updates`。写清「放弃」比留白重要 —— 留白会在后续评审中被反复重提。
      **§10 给出了本轮最不好看但必须写的结论：42 项自检中可自动化 17 项、半自动 4 项、只能人工实测 **21 项**，5 项 🔴 中有 4 项只能手工实测。** 因此「**DevSecOps 在本项目中不可能替代人工验收**」。隐私协议加载顺序（未同意前禁读设备标识）只有抓包能证明，任何代码审查都证明不了「实际没发出那个请求」。同理安全方案 §13 写了防自证条款：「只写已确认通过而无输出留存的视为未执行」，并把「身份证存储审计」标注为 ⚠️「**Batch1 不适用 / Batch2 验收（不得记为已通过）**」—— Batch1 `cert` 域不存在，此项是真空通过，若记成已通过就是虚假安全感。
      **改动落点**：新建 `docs/architecture/系统安全设计方案.md`（14 节）、`docs/architecture/DevSecOps接入方案.md`（12 节）；`说明文档.md` §1.2 产出物清单 +2 行、§3 进度记录 +本条。
      **过程记录（工具坑复现）**：`Write` 单次写入长内容再次连续 5 次报 `missing field 'content'`，沿用条目 [91] 已固化的做法解决 —— 先 `Write` 落 stub 并留 `<!-- APPEND-ANCHOR -->`，再用 `SearchReplace` 以锚点为 `old_str`、`新内容 + 锚点` 为 `new_str` 分 5 段追加，末段去锚点收尾。第二份文档规模适中，单次 `Write` 一次通过。另：`说明文档.md` 已 4085 行（689KB），超出 Read 的 256KB 上限无法整读，定位靠 Grep 章节标题 + `Read` 带 `offset` 分段 + PowerShell `Measure-Object` 取真实行数；期间发现 **Grep 的 `offset` 是「跳过前 N 条匹配结果」而非「从第 N 行开始」**，直接按其输出行号推断插入点会错位，须以 `Read` 实读确认。
      **本轮方法论增量两条。** ① **写「安全方案」时最大的风险不是漏项，而是造出第二套口径。** 安全条款天然散落在需求、架构、可观测性、详设各处，一份新文档若按标准目录从头写，每一节都会不自觉地重述阈值与规则，而重述必然漂移。正确形态是**归集 + 逐节标注来源 + 只在空白处新增**，并把「本方案新增」的部分显式列出来接受审查。② **「本期不落实物」的方案必须自带反误读装置。** 用户裁定不落自动化是资源判断，但方案若只写规格，读者（含未来的我）极易读成「这些门禁本期不适用」。装置有三个：文档定位处明写「不豁免任何门禁」、落地清单给出优先级与依赖顺序、以及把两处**已存在的**阻塞项（后门可随包发布 / debug 签名阻塞备案）单列一节 —— 让「暂不实施」与「暂不阻塞」区分开。

- [x] **G1 / G2 缺口裁定并全链落盘：新增错误码 `40305`、`42907`，明确不做坐标模糊化**（条目 [99]，2026-09-06）：承接条目 [98] 登记的 6 项缺口，本轮只裁定 G1（后台 RBAC 越权无 5 位错误码）与 G2（PRD §12.1「§6.4.3 要求坐标隐私模糊化」交叉引用悬空）两项，G3–G6 原样保留。两项均**由需求侧在 PRD §12.5 这一唯一口径源定案**，安全方案只记录论证与落点索引，未自行占号。
      **G1 裁定：越权统一返 `40305`「无权限访问该资源」（含读与写，不按资源类型拆码）。** 取证：`403` 段已占 `40301`–`40304` 全为业务语义、无通用越权码；全仓 Grep 确认 `40305` 未被占用。**为何不留到 Batch2 后台开发前再补**（条目 [98] 的原建议）：那恰是成本最高的时点 —— 届时要同时改 PRD + 后台契约 + 客户端码表 + 已在写的代码共 4 处，而现在补只改 PRD 一处。原纪律要求的是「不由安全方案**单方面**占号」，不是「推迟到最贵的时候占」，两者不冲突。
      **G1 的反直觉论证：单一错误码本身就是防枚举手段。** 直觉上「统一返同一个码」像是信息泄露，实际相反 —— 若不定统一码，实现者会各自用 `404` / `40301` / 裸 `403`，**不同资源返回不同响应，反而制造出可枚举的信息差**；统一返 `40305` 才消除了这个差异。另**不做 `404` 掩盖资源存在性**：T4 内部滥权的攻击者本就知道资源存在（他在后台里），这层掩盖只增排障成本、收益为零，真正的防线是 `audit_log`。
      **G2 的取证推翻了问题本身的设定。** 原议题是「坐标该模糊到什么粒度」，但精度换算证明这个前提不成立：6 位小数 ≈ 0.11m（门牌级）/ **5 位小数 ≈ 1m（仍是门牌级）** / 4 位 ≈ 11m（仍可定位楼栋单元）/ 3 位 ≈ 110m（才开始隐藏住户）。**从 6 位降到 5 位是压缩响应体积，不是模糊化**，离真正模糊化差 2–3 个数量级。且产品侧已作出相反的强激励：🟢 完整档以「位置到门牌号」换 **×2 优先展示**（§9.8）、引导文案主动索要门牌号、`address` 明文 `varchar(120)` 由 `PostDetail` 原样返回。
      **G2 裁定：明确不做坐标模糊化 —— 这不是妥协，而是唯一自洽的选择。** 因为 `address` 明文到门牌号照原样返回，坐标即使模糊到 500m 也挡不住任何人。**半吊子模糊化是最坏结果：产品价值受损，隐私一点没保护。** 保护责任转移到两条派生措施：① **位置知情告知**（PIPL 告知同意在位置数据上的唯一落点）—— 选点页与完整度引导文案须写明「门牌号将对所有看到这条信息的人可见」，**不得只写「附近更多人看到」**，隐私政策 P6 须列明；② **未登录详情浏览限频 `42907`**。
      **措施②闭合的是一处此前完全未识别的防线不对称**（G2 取证时暴露，非原始议题）：联系方式有三维限频 + 熔断，**而「精确地址 + 门牌号 + 物品」这份清单对未登录用户完全开放、零限频**。攻击者要的未必是电话号码 —— 「哪里有人在出租房 / 卖车」加精确地址本身就是成品目标清单，正是 §1.1 定义的 P0 保护对象，却原本完全在防线之外。阈值**直接沿用 §7.7 的设备 30 / IP 100，不新造数字**；只约束未登录请求（已登录可按 `user_id` 追溯），是**唯一没有账号维的限频轨**；提示附登录入口且不告知哪个维度超限。
      **顺带裁定一处契约与需求的鉴权口径冲突。** PRD §12.3 写「未登录可只读查看详情」，而 openapi `/posts/{post_id}` 原先无 `security: []`（即需鉴权），两者必有一错，不裁定则实现期会二选一且不留痕。经确认**以 PRD 为准**，openapi 补 `security: []`（免鉴权接口由 4 处增至 5 处），并显式写下「与 `/posts/{id}/contact` 的未登录一律 `40101` **不同，二者不可混同**」以防混淆两个接口的鉴权语义。
      **改动落点（六份文档 30 余处）**：`PRD.md` 7 处（§12.5 新增 `40305` 与 `42907` 两行、§9.10.1 权限隔离验收行、§12.1 坐标行改写删除错配括注、§14.3 新增「位置精度」与「位置知情告知」两行、§12.3 补免鉴权口径与完整限频规则、`Retry-After` 段 42906→42907）；`openapi.yaml` 10 处（顶部错误码表补两行且计数 23→25、`Retry-After` 段、四分类表两行、`/posts/{post_id}` 补 `security: []` + description 两节 + `429` 响应、`Forbidden` 补 `40305`、`TooManyRequests` 补 `42907` 且 6→7 码、`guestDetailLimit` 示例、`Retry-After` 头定义）；`系统安全设计方案.md` 6 处（§11 缺口表关闭 G1/G2 + 新增 §11.1/§11.2 裁定小节、§6.1 新增「第四条限频轨」、§8.1 RBAC 三条纪律补 `40305`、S6 自检 +2 项、§13.1 计数 42→44、§14 索引 +2 行且边界声明加澄清注）；`前后端详细设计文档.md` 8 处（后端 `ErrorCode` +2 码、客户端 `ApiErrorCode` +2 码、§3.4 限频键表 +2 行及两点特殊性、§12.2 行为映射、§20.2 补 `42907` 更严格规则、§9/§19 自检计数、§0 纪律行、目录树注释）；`系统总体架构设计文档.md` 4 处、`DevSecOps接入方案.md` 4 处（自检总数 42→44、只能人工实测 21→23 及其原因说明）。
      **验证（全部实测，非目测）**：openapi 契约计数与改前基线逐项比对全部一致 —— `operations 41 / Batch1 21 / schemas 31 / parameters 6 / responses 9 / 断链 0`（断链用脚本收集 `components` 下全部定义名再逐个校验 `$ref` 目标，230 个 `$ref` 零断链），`security: []` 按预期由 4 增至 5（第 6 处命中为 description 中的文字说明，非声明）；PRD §12.5 表逐行点算 **25 行 = 成功码 1 + 错误码 24**，码序列完整无跳号无重复；后端 `ErrorCode` 与客户端 `ApiErrorCode` 各实测 **24 个服务端码**，与 PRD 表逐码一致；安全方案自检项按 `- [ ]` 实测 **44 项**（S2=4 S3=5 S4=5 S5=6 **S6=7** S7=6 S8=5 S9=3 S10=3），据此同步全仓 5 处「42 项」表述；残留旧计数（`22 个错误码` / `42901–42906` / `42 项自检`）全仓 Grep 返回 **0** 命中（`docs/ideation/` 下的历史发散记录刻意不改，那是当时的观测事实）。
      **本轮方法论增量两条。** ① **「已有措施」必须先验证其手段与目的是否匹配，不能只检查它在不在。** PRD §12.1 那句「§6.4.3 本就要求隐私模糊化」的问题**不是漏写而是错配** —— 它把一个性能优化（5 位小数压缩响应体积）冒充成隐私措施，这比单纯缺失更危险：缺失会被发现，而「已经模糊化了」会让后续每一次评审都跳过这个议题。检查清单类工作的真正风险在此，不在漏项。② **裁定一个缺口时要为它取证，而取证过程本身会暴露原缺口清单没有的东西。** `42907` 不在 G1–G6 里，它是在为 G2 查「未登录能看到什么」时才浮现的；同理那处鉴权口径冲突也是查 `security` 分布时撞见的。所以缺口裁定不该只回答被登记的问题，**应把取证半径内的发现一并处理**，否则下一轮还要重新建立同样的上下文。

- [x] **G3–G6 四项缺口全部裁定并全链落盘：构建工具定 Maven，注册成本纪律转硬门禁，密钥存放双方案冲突消除**（条目 [100]，2026-09-06）：承接条目 [99]，本轮处理剩余四项，**6 项缺口自此全部关闭，安全方案 §11 表转为历史记录、无待处理项**。四项的裁定方式各不相同，恰好覆盖了缺口的四种真实形态：一项是**真定案**（G6）、一项是**纪律不可执行需转判据**（G4）、一项是**问题设定不准确、真正缺的是别的东西**（G3）、一项是**根本不该由本方案登记**（G5）。
      **G6 裁定：服务端构建工具定为 Maven，口径源落在《技术栈选型说明》§3.1（新增小节）。** 该文档此前对构建工具**零提及**，是一处真空 —— DevSecOps 的 D-1 与安全方案的 G6 都指向"待定"，而没有任何文档负责定。判据四条：Spring Boot 官方文档与 start.spring.io 默认 Maven（1 人 38 天工期下「抄得到的样例最多」是首要指标）；`pom.xml` 是纯声明式 XML，而 Gradle 的 `build.gradle(.kts)` 是可执行脚本、易混用 Groovy / Kotlin 两套 DSL 语法，排错成本落在唯一的开发者身上；Gradle 的长处（多模块并行、增量构建）在本项目**为零**，因为架构 §4 已否掉微服务、单体单模块；宿主机直接 build 无独立 CI runner，Gradle Daemon 会与 JVM 750MB 争抢同一份 2G。**这不是「哪个工具更好」，而是「哪个工具在本项目约束下不产生额外工作」** —— Gradle 的收益需要一个本项目不存在的前提（模块数 > 1），成本却立即发生。
      **G6 为何不留到原定的「编码期 Day 1」**：与 G1 同一个道理但更直接 —— DevSecOps §5 全部服务端门禁命令、§3 依赖锁定规格都以它为前置，不定案那些规格只能写成带条件前缀的半成品（实测原文有三处「D-1 定案后」的条件写法）。**现在定案改的是三份文档的文字；Day 1 定案改的是已经写好的门禁脚本。**
      **G4 裁定：原纪律「上线前必须同步评估注册成本」转为四项可逐项打勾的 Batch2 硬门禁**，落点选在《系统总体架构设计文档》§5.1.1（微信登录 Batch2 待做项的**同一处**）而非安全方案 §11。原缺口的实质问题不是没写纪律，而是**纪律不可执行**：「必须评估」四个字没有判据（怎么算「不低于」）、没有卡点（谁在哪个节点检查）、没有失败后果（结论是「偏低」时上不上），在 Batch2 排期压力下必然被解释为「已经评估过了，微信也有频控」而通过。四项门禁：① 微信注册接口有设备维 + IP 维限频、阈值不高于短信 IP 维 20/h，超限返 `42905`（**沿用既有码与既有阈值，不新造**）；② 微信首次注册**必须绑手机号**才具备发布与联系权限；③ 账号级风控与配额在微信会话下仍以 `user_id` 为键（同一 `user_id` 的手机号会话与微信会话须共用同一计数）；④ 注销按 `user_id` 删**全部** `user_identity` 行含 `wechat` 行（绑微信后注销、再用该微信登录须走全新注册而非复活旧账号）。**且明确四项对 Apple 登录同样适用**，不得因「Apple 是 App Store 审核硬性要求」而豁免。
      **G4 的关键判断：②是四项里唯一会被「体验优化」掉的，也是唯一真正承载成本的一项。** ①只限制注册**速率**，攻击者可用时间换量；③④防的是权限与残留，不是成本。只有②让每个具备发布能力的账号都对应一个真实手机号，把成本从零抬到「一条短信资费 + 一个可接码的号」。**若把②改成「手机号绑定可选」以提转化率，①③④全部形同虚设** —— 攻击者要的从来不是账号，是账号附带的发布与联系能力。另外③④与 Batch1 已交付的「风控键改 `user_id`」「注销按 `user_id` 删全部身份行」是同一件事的两端：那份改造的**全部收益都在微信上线这一刻兑现**，③④正是验收它是否真的到账。
      **G3 的取证再次推翻了问题设定（与 G2 同型）。** 原描述是「`key_version` 列存在但无轮换能力，一旦怀疑泄露无平滑更换手段」，但实读《数据库设计文档》§7.1 发现**处置流程已完整成文**：两类密钥的影响面、处置步骤、是否需停写、pepper 须停写的原因（重算期新旧 hash 并存、唯一索引无法同时约束两套值，否则同一手机号在新旧 pepper 下各存一行）、Batch1 无在线吊销能力，全都在。**真正缺的不是流程文本，而是流程能否被执行的三项前置条件** —— 它们的共同特征是「等到真要轮换时才发现缺，就已经来不及了」。因此裁定：轮换的实现（回填任务、旧版本下线）**维持在 Batch2**，但三项前置条件本期补齐。
      **G3 前置条件①：发现并消除一处从未合并的双方案并存。** 安全方案 §4.4 / 架构 §4.4 定密钥放宿主机 `.env`，而 `docs/plans/2026-09-04-1630-refactor-batch1-data-model-corrections-plan.md` 的 KTD5 定放 Spring `application-{env}.yml`（生产用 `HMAC_PEPPERS_JSON` 环境变量覆盖）—— **两套并存、从无任何文档说明取舍**。裁定后认定二者**并非对立，而是同一条链的两段被当成了两个方案**：`.env` 决定密钥值以什么形式到达容器，yml 决定 Spring 以什么形式读取它。唯一链条：`.env`（600、不入 git）→ Compose `env_file` → 容器环境变量 → `application-prod.yml` 的 `${}` 占位 → `CryptoConfig` 配置绑定。并明确两条禁令：**yml 中不得出现任何真实密钥值**（开发环境也走 `.env`，值可弱但形式必须一致，否则生产是唯一没被验证过的路径）；`.env` 不得直写 `app.crypto.*` 带点号键名绕过 yml。另确认 KTD5 的**列表结构**（`hmac-peppers` / `aead-master-keys` 按 `key_version` 索引）是 Batch1 的唯一正确实现，**即使列表当前只有一个元素** —— 若按单值实现，Batch2 轮换第一步就是改三个类的签名并回归全部加解密调用点。
      **G3 前置条件②：`key_version` 三张表 `DEFAULT` 不一致，实测确认是刻意的而非遗漏。** `user` 为 `NOT NULL DEFAULT 0`、`user_identity` 与 `post` 为 `NOT NULL` 无 DEFAULT。这个不一致**编码了一条真实的语义差异**：`user` 行可能在尚无加密列值时先建（注册事务内先建 `user` 再写身份行），需兜底；而后两张表每行必带密文，「忘写 `key_version`」是一个**必须在插入时就失败**的错误 —— 否则密文用 v1 加密、版本记 0，解密取错密钥后 AEAD 抛异常且**无法从数据本身反推真实版本，这行密文永久损坏**。**用数据库约束把「忘记声明密钥版本」变成插入失败，比任何代码 review 都可靠。** DDL 不改，只把语义写进文档。
      **G3 前置条件③：补轮换的触发判据（原文只写了「怀疑泄露时怎么做」，没写「什么算怀疑泄露」，等于预案不可触发）。** 五条：`.env` 曾被提交到 git（哪怕已 `git rm`）/ 备份与 pepper 曾同介质或同时外传 / 宿主机判定失陷 —— 三项**不可延后**；接触过生产 `.env` 的**人员离场** —— 可排期但须 30 日内完成；无泄露迹象的定期轮换 —— **Batch1 不做**（单人运维下停写窗口与重算出错的操作风险高于所防风险）。**第四条是最容易被漏掉的一条**：前三条都是「出事了」，只有人员离场是**没有任何异常信号的常规事件**，因而不触发任何人的警觉，而它恰是密钥泄露最常见的现实路径。同时明确 **pepper 保管强度必须高于 AEAD 主密钥** —— 同样一次泄露，后者是后台任务逐批重加密、业务无感，前者是停服维护 + 全量重算盲索引 + 重建唯一索引，**保管强度应按处置代价分级，不按「都是密钥」一视同仁**。
      **G5 裁定：确认为非安全议题、已在《可观测性架构方案》§6.3 闭环，本方案关闭且不重复登记。** 实读该节确认三点：告警通道在单机形态即日志扫描加通知、明确不引入 Prometheus / Grafana（2G 内存）；「通知」组件的**定案时点已绑定** SLS 迁移，迁移后用 SLS 自带告警能力、不另选组件；**在此之前有替代保障** —— 「有没有人真的去看那个日志」由 §9.1 周巡检承担。四条判断：`P95>300ms` 是性能指标，其告警不可达影响的是性能回归发现速度，与本方案 §1.1 的保护对象（个人信息、位置、联系方式）无交集；「无接收端等于不可达」不成立 —— 落盘日志 + 周巡检就是一条可达路径，只是延迟从分钟级变周级，对内测阶段唯一开发者可接受；**同一议题在两份文档各登记一次，会产生「两处待办、各自以为对方在跟」的落空 —— 唯一口径源原则同样适用于待办项，不只适用于数字**；安全侧留痕靠 `audit_log`（≥180 天）与 `security_event` 走审计而非告警通道，不依赖该组件。**最后一条是这项裁定的关键**：若安全事件也依赖「通知」才能被发现，G5 就是安全缺口而非性能缺口；之所以不是，是因为 §8.2 从一开始就选了**留痕 + 事后可查**而非实时告警 —— 单人运维、无 7×24 值守下这是正确取舍，**一个没人接的告警和一条没人看的日志价值完全相同，但后者不需要额外组件**。
      **改动落点（六份文档 20 余处）**：`技术栈选型说明.md` 2 处（**新增 §3.1 构建工具小节**含四条判据 + 五条命令口径 + 版本号纪律 + 退出条件；§2 选型总表新增「构建工具」行）；`DevSecOps接入方案.md` 9 处（§1 现状表、§2 D-1 行与纪律段、§4 巡检命令去条件前缀、§7 版本锁定表、§9 待办第 1 项关闭、§11 问题登记 D-1 行、§5.4 与 §1 依赖源计数 44→45、**§10 承载映射整表重算**）；`系统安全设计方案.md` 8 处（§11 表四行全部改已关闭 + **新增 §11.3–§11.6 四个裁定小节**、§4.4 轮换段重写为三项前置条件表、§6.4 纪律句改指新落点、S4 自检 +1 项、§13.1 计数 44→45、§14 索引 +4 行、边界澄清段与文末计数）；`系统总体架构设计文档.md` 2 处（**§5.1.1 新增「Batch2 上线前置检查项」四项门禁表** + 顺带修正该节索引名 `uk_type_value` → 实际 DDL 的 `uk_type_hash`）；`数据库设计文档.md` 1 处（§7.1 补轮换触发判据表 + `DEFAULT` 语义表）；`说明文档.md` 3 处（§1.2 两行版本号与计数、本条目）。
      **验证（全部实测）**：安全方案自检项按 `- [ ]` 重新点算 **45 项**（S2=4 S3=5 **S4=6** S5=6 S6=7 S7=6 S8=5 S9=3 S10=3），据此同步 DevSecOps §10 承载映射为 **18 自动 / 4 半自动 / 23 人工**（新增的 S4 项是纯静态文本形态检查，`Grep` + 集合比对即可，故归自动化，是 9 组中自动化比例最高的一组）（实测命令 `Select-String -Pattern '^\s*-\s\[\s\]'` 计数返回 45，与分组明细吻合）；Grep 旧计数 `44 项` / `44 个自检` 在安全方案与 DevSecOps 两份文档中**已实测为零命中**（沿革来历改以「初版 42 → G2 +2 → G3 +1 = 45」的算式表述，不再复述旧总数）；本文档 §3 进度记录中条目 [98]/[99] 仍留有 `44 项` 字样，属**历史记录不回改**的既有做法，不计入遗漏；本文档改后总行数实测 **4127 行**；全仓 Grep `缺口 G3` / `G3–G6` 确认无「待处理」表述残留；DDL 三处 `key_version` 定义逐行实测（`V1__init_schema.sql` L37 / L58 / L184）与文档新增表述逐项一致。
      **本轮方法论增量三条。** ① **缺口有四种形态，混在一张表里会掩盖它们需要完全不同的处理方式。** 本轮四项恰好各占一种：真定案（G6，缺的是决定）、纪律不可执行（G4，缺的是判据与卡点）、问题设定不准确（G3，缺的不是登记项说的那个东西）、根本不该登记（G5，重复登记本身就是风险）。**照着表格逐项「补上」是错的做法** —— 每一项都必须先取证判断它属于哪一形态。② **「必须评估」「须同步考虑」这类措辞是纪律失效的可靠信号。** 一条纪律若没有判据、卡点、失败后果三件套，它在排期压力下的实际效力为零 —— 因为「评估过了」永远可以被声称。G4 的处理方式（四项可逐项打勾 + 明确「任一不满足即不得上线」）是这类纪律的唯一有效形态。③ **落点位置决定纪律是否会被读到，这与内容质量同等重要。** G4 的门禁若留在安全方案 §11「缺口登记」章节，Batch2 开工时没人会翻；放进架构 §5.1.1（与微信登录待做项清单**物理相邻**）才会在排期时被读到。**文档的物理布局是纪律执行链的一部分，不只是排版问题。**

- [x] **沉淀「缺口登记表的四种形态」学习文档**（条目 [101]，2026-09-06）：调用 `ce-compound` 技能（Lightweight 深度，因本会话已压缩过一次、上下文压力真实），把条目 [100] 的方法论增量沉淀为持久学习。选 Lightweight 而非 Full 的代价是跳过了跨引用与语义接地校验，收益是不再消耗已紧张的上下文。
      **新建 `docs/solutions/workflow-issues/gap-register-triage-and-enforceable-discipline.md`**（knowledge track，`problem_type: workflow_issue`，`root_cause: missing_validation`，`resolution_type: workflow_improvement`）。目录、`module: docs`、`component: documentation` 三项均按 corpus-first 规则取自既有 8 篇 workflow-issues 的用法，**未新造任何词汇值**。正文按 knowledge track 五段结构展开，核心是把四种形态各自的**判据与处理动作**分开写 —— 形态三（问题设定不准确）特别注明是四种里**唯一无法从登记表本身识别**的一种，只能靠实读被指原文发现，故「实读原文」这一步不可省。
      **`CONCEPTS.md` 词汇捕获（update-only，归入既有「架构交付链」聚落）**：新增 2 词条 —— **缺口登记项**（四种形态及各自处理方式，以及「清表第一步是判形态而非动手补」）、**可判定门禁**（判据/卡点/失败后果三件套；失败后果一列是将来有人要砍某项时唯一能拦住他的东西；须标出一组门禁里真正承载核心风险的那一项，因为它恰好最容易被「体验优化」掉）。按 lightweight 规则只做增补、不 bootstrap。
      **验证（实测）**：两项机械校验均在强制 UTF-8 后通过 —— `validate-frontmatter.py` 返回 `OK`、`validate-doc-claims.py` 返回 `checked 4 paths, 0 SHAs, 0 links; 0 flags` + `OK`，退出码均为 0。**顺带记录一个环境坑**：这两个脚本在 Windows PowerShell 下默认以 GBK 解码文件，读中文 learning 必抛 `UnicodeDecodeError`（`'gbk' codec can't decode byte 0xae`），须先置 `$env:PYTHONUTF8='1'` 与 `$env:PYTHONIOENCODING='utf-8'` 再执行；**这是脚本自身的编码假设问题，不是文档格式问题** —— 若不辨明就会去改本来正确的 frontmatter。文档中引用的三处行号（安全方案 §11.3 L649、技术栈选型 §3.1 L96、架构 §5.1.1 L314）均由 `Select-String` 实测取得。
      **可发现性检查（只读）**：本仓无 `AGENTS.md` / `CLAUDE.md` 等活动 instruction 文件（已 Glob 实测），故不适用；`docs/solutions/` 与 `CONCEPTS.md` 的可发现性由本文档 §3 进度记录承担 —— 每轮沉淀都在此留一条带路径的记录，这与项目「重新打开项目前必先读本文档」的纪律是同一条链。

- [x] **清掉两处存量偏差：D-2 定案 + 架构 §9 接口口径**（条目 [102]，2026-09-07）：条目 [101] 沉淀的「四种形态」框架**首次用于处理自身的存量待办**，两项各暴露一个新问题。

      **① D-2（服务端测试执行时机）定案 —— 属形态一「真定案」。** 定案为**单测每次提交前、集成测试仅出包前**，口径源为《DevSecOps 接入方案》§5.3（该节早已有完整分层表与理由：单测秒级可承受；集成测试需 MySQL/Redis 容器，而 2 核 2G 的内存预算已排满 MySQL 450 + Redis 150 + JVM 750 + Caddy 50 + OS 600 ≈ 2000MB）。**关键辨别：D-2 与 D-1 分属两类，不能套用同一条纪律。** D-1 是技术栈选型问题，其唯一口径源在《技术栈选型说明》，故 DevSecOps 方案「不自行定案」；**D-2 是门禁分层问题，其口径源就是本方案 §5.3，故可就地定案**。这一区分已写入该方案 §2 纪律段，避免下次误把 D-2 也推给选型侧而无限悬置。清理残留登记共 5 处：DevSecOps §5.3 标题、§11「不做」表（原写「不定案 D-1 / D-2」，已改为「不定案技术栈选型项」并注明 D-2 不属此类）、§11 问题登记表 D-2 行、安全方案 §11.6 落点段（原写「D-2 不在 G6 范围，仍待定」，已补明它为何不能随 G6 裁定）、本文档 §1.2 第 56 行。全仓 `Grep D-2` 实测已无「待定 / 未定案」残留。

      **② 架构 §9 接口计数 —— 属形态三「问题设定不准确」，且实测结论与登记完全相反。** 原登记为「架构 §9 写 39 个 vs 实测 41 个 operations，须校正为实测值」。实读后发现：**39 本来就是对的，错的是我方比对口径。** 架构 §9 的 39 指**不含埋点的业务接口数**（9 个分组求和即 39，埋点单占第 10 行），而 41 是 openapi **含埋点**的 operations 数 —— 拿含埋点的比不含埋点的，差值毫无意义。若照登记「校正为 41」，等于把一个正确的数字改错。**这正是形态三无法从登记表本身识别、只能靠实读原文发现的实例，本轮为该形态提供了第二个独立样本。**

      **③ 但取证过程翻出一个远大于原登记的真问题：PRD 与 `openapi.yaml` 的接口集合大面积分歧。** 实测 PRD 40 operations（表格 39 行，其中 `POST /posts/{id}/favorite / DELETE` 一行含两个 operation）、openapi 41 operations，**交集仅 25**。差异分三类：**A 命名不一致 7 对**（`/certs/*` ↔ `/cert/*` 等）、**B 方法冲突 1 处**、**C 一方独有 15 个**（PRD 7 / openapi 8）。已在架构新增 §9.0 固定四种点算口径并穷举登记三类差异，附验算（25 + 7 + 1 + 7 = 40 与 25 + 7 + 1 + 8 = 41，两侧均与实测吻合，证明无遗漏项）。经用户裁定：**本轮只处理 B，A/C 登记待编码期 Day 0 前处理** —— 依据是 B 是唯一的真规格分歧且落在全系统唯一敏感出口上，留着它编码期必有一方门禁失效；而 C 中 openapi 独有的 8 个是否有需求支撑只能由需求侧逐个裁定。

      **④ B 类裁定过程中我给错过一次前提，须记录。** 我最初把 B 描述为「PRD 的 POST vs openapi 的 GET，二对一」，并按「PRD 是需求侧唯一权威源」建议以 PRD 为准。实测后是 **GET 9 处 vs POST 1 处**：openapi（paths + `operationId: getPostContact` + 两处 schema 描述）、安全方案（§2.1 唯一出口规格、§3.2 威胁 T1）、详设（§5.5.1 完整接口设计、域清单、解密调用点等 5 处）全部写 GET，**PRD L2197 是全仓孤例**。我把这个 1 : 9 的实情连同「按原裁定要改 9 处、含详设 §5.5.1 整节重写」的代价一并报回，用户随即改判为**以 openapi 为准，只改 PRD 1 处**。**教训：「PRD 是需求侧唯一权威源」这条纪律管的是需求内容（是否限频、阈值多少），不管接口契约细节（HTTP 方法动词）—— 用错适用范围会把 1 处的改动放大成 9 处。** 既有先例可对照：安全方案 §11.2 处理过同类冲突并以 PRD 为准，但那处争的是「未登录能否访问详情」，是真需求问题，性质不同。

      **⑤ 顺带在 PRD 定案处补了一条防回改说明。** `/posts/{id}/contact` 是一个**有副作用的 `GET`**（写 `contact_event` + 写 `audit_log`），若不写明，下一个人会以为 GET 是笔误而改回 POST。故在 `docs/PRD.md:2207` 注明它不可缓存、不可预取、不可自动重试（重试会虚增北极星辅助指标并多落一条审计），并显式写「不要因为『GET 应当幂等』而改回 POST」。**此处自纠一次违规**：我初稿写了「响应带 `Cache-Control: no-store`」，全仓 Grep 实测该表述**零命中** —— 这是新造口径，违反「不新造口径」纪律，已撤为需求层面的性质陈述，具体响应头留给 `openapi.yaml` 定。

      **验证（全部实测）**：`Grep D-2` 全仓确认无待定残留；PRD 表格 operations 按正则逐行点算为 39 行、`/track/events` 与 favorite 双 operation 均已核对；openapi `/track/events` 存在于 L1531（确认 41 含埋点）；三类差异的数字验算两侧均吻合实测值；`Cache-Control|no-store|no-cache` 全仓零命中（据此撤回新造口径）。**临时探针 `docs/_probe_api_count.py` 已按「验证脚手架用完即删、结论不随工具消失」纪律删除**，其全部结论已落进架构 §9.0。

      **本轮方法论增量两条。** ① **形态三（问题设定不准确）的危害比预想的更大 —— 它不只是「补错了东西」，而可能是「把正确的改成错的」。** 本轮若照登记把 39 改成 41，一个自洽的数字就被改坏了，而表格上会显示「已完成」。**故凡登记项涉及具体数字，实读原文时必须先问「这两个数是同一个口径吗」，而不是直接比大小。** ② **纪律的适用范围必须与纪律本身一起记住，否则它会被扩大使用。** 「PRD 是需求侧唯一权威源」被我扩大到接口方法动词上，直接导致一次错误裁定建议（1 处改动被放大为 9 处）。**纪律条文只写结论、不写适用边界时，它迟早会被用在边界外**——这与形态二「纪律缺三件套则效力为零」是同一个问题的另一面：前者缺执行力，后者缺适用范围。

- [x] **ce-compound Full 重跑：补上 lightweight 当初跳过的三项校验**（条目 [103]，2026-09-07）：用户要求对条目 [101] 沉淀的 learning 补跑跨引用检查、重复检测与语义接地校验。**三项各有独立命中，Full 的必要性被实测证实 —— lightweight 产出的 learning 有 9 处失准，其中 4 处是事实性错述而非仅行号漂移。**

      **① 跨引用检查（5 处，全为引用侧失准）。** L43/L106/L113 三处写「六处『（定案后）』条件写法」—— 实测「六处」在源头（安全方案 §11.6）指的是**落点改动数**，条件写法实体只有**三处**，且历史表述是不带括号的「D-1 定案后」；数字张冠李戴。L96 的 blockquote 声称引原文但非逐字（丢了「上线前」「批量」「且不得低于短信通道」三个限定，还自行添了一句），已改为安全方案 §6.4 的逐字原文。L81 `§5.1.1（L314）` 与 L119 `#L649-L760` 两处行号已漂移（前者实际在 L292，后者尾端越界约 5 行）—— **按姊妹篇 `cross-document-reference-verification.md` 已定的「行号会静默漂移」纪律，两处一律降级为纯章节号引用，本文档自此无任何行号锚点，可自免于后续漂移。**

      **② 重复检测（CONCEPTS.md 10 项）。** 最严重的一项：**「缺口登记项」形态二的描述与「可判定门禁」词条几乎逐字重复定义同一概念，且无交叉引用** —— 已改为「其产物即可判定门禁，见该词条」。另修：「闸门」（机器脚本）与「可判定门禁」（人工可验）的术语碰撞已登记到 Flagged ambiguities 并注明「项目正文中大量以『门禁』称机器脚本，读到时须按上下文回判」；两者共享的「守的必须是最先崩的那个」原则已双向互引；探针族（一致性探针 / 离线探针 / 前置断言）已统一归入守门型；R-ID 词条「三份架构文档」过时（现有 5 份）改为「各份」；「写守卫」的 `WHERE` 子句与「时间不可逆项」的 `datetime` 两处实现细节按「术语表不含实现细节」规则抽象化；「权威源」词条补上待办项适用性与「须声明适用边界」两点（正是本轮 ④ 的教训）；「缺口登记项」补上阶段转换说明（登记表是待裁定项的合法载体，但裁定后产物必须移出）。

      **③ 语义接地校验（33 条断言，查出 4 处 contradicted）。** 这是 lightweight 完全跳过、也最有价值的一项 —— 它查的不是引用格式而是**内容是否与被引原文一致**：（a）「其余三项分别防的是权限与残留」与安全方案 §11.4 原文直接冲突（①防的是注册速率，可用时间换量，不属权限与残留），已改；（b）Examples 表摘录行非逐字（原文含 `POST /posts` 与 `/contact` 两个具体接口，被我泛化成「发布与联系接口」，违反「特征句必须逐字复制」纪律），已改为逐字；（c）「缺口表只留一句『已裁定，见 XX』」不准确 —— 实际是缺口表行只留指向、判据另立裁定小节，已改；（d）**形态四写的「不复述对方的处理方案」与本轮 §11.5 的实际做法相反** —— §11.5 恰恰复述了对方三点取证事实。这是一条**纪律与自身举例互斥**的错述，若入库会被当作既成规则执行，已改为区分「取证留痕」与「第二份规格」，并补上判断闭环的三个必查项。另按 completeness 反馈补：「三处」这个数**只在清理前可数**（清理后实体已不存在，回数必得零），故已注明须在同一次改动里当场记下实测值与出处 —— 这与「关于工具的可数事实必须回代码数」是同一类风险的两种表现。

      **验证**：机械校验脚本（`validate-frontmatter.py` + `validate-doc-claims.py`）在两轮改动后均报 `0 flags` / `OK`；语义接地子代理为唯一未通过机械校验能力覆盖的检查项，其 4 处 contradicted 已逐项按引证原文修完。

      **流程偏离记录（须留痕）**：本轮未按 `references/research.md` 派 Context Analyzer / Solution Extractor / Related Docs Finder 三角色，也未写 `context.json` / `solution.md` / `related.json` artifact，未跑 session-history 步骤 —— 原因是本轮目标是**更新既有 doc 而非新建**，前两个角色的产物（问题分类、方案提取）已存在于 doc 内。实际派了 3 个 search 子代理承担 Related Docs Finder + 跨引用核验 + 词条核验，另派 1 个语义接地子代理。**可发现性检查（步骤 4 的 Discoverability Check）按规则整段跳过**：本仓无 `AGENTS.md` / `CLAUDE.md`，规则明写「若两者皆不存在则完全跳过此项」。overlap 判定为 **Moderate**（同 area、不同角度；主题 B「纪律可执行性」有 6 篇触及，其中 `visual-feedback-loop-latency.md` 与 `design-parameter-review-and-gating.md` 高度重叠），按 `assembly.md` 应「新建 + 标记待整合」，但本轮 doc 已存在故走更新路径，**整合评审留给 ce-compound-refresh**。

      **本轮方法论增量一条：机械校验与语义校验查的不是同一类东西，前者通不过后者。** 本轮机械校验全程 `0 flags`，而语义校验查出 4 处事实性错述 —— **因为机械校验只验「路径/SHA/链接是否存在」，它无法发现「引用存在但内容说反了」。** 故「校验脚本全绿」不构成 learning 可入库的证据；凡 learning 中出现引自他处的判断句、摘录行、可数事实，必须有一次以原文为准的逐条比对。这与既有的「全绿与真的做完在观感上完全一致」是同一条原则在校验工具层面的实例。

- [x] **部署架构设计文档 + IaC 基础设施代码（Batch1 全量交付）**（条目 [104]，2026-09-07）：根据《系统总体架构设计文档》§4、《可观测性架构方案》§7、《技术栈选型说明》、《系统安全设计方案》、《DevSecOps 接入方案》五份上游文档，完成部署架构与基础设施即代码设计，双份交付物：
  - ① **《部署架构设计文档》**（`deploy/docs/部署架构设计文档.md`）：13 章覆盖部署拓扑、四容器职责、内存预算落地（MySQL 450MB / Redis 150MB / JVM 750MB / Caddy 64MB / OS 约 634MB，合计 2048MB）、40G 盘七项分配表的 IaC 落地（含两条硬性配置：binlog `binlog_expire_logs_seconds` 与日志轮转 `totalSizeCap`）、网络暴露面与安全组、TLS 终止（Caddy ACME，TLS 1.2 下限）、密钥注入唯一链路（`.env` → Compose `env_file` → 容器环境变量 → `application-prod.yml` `${}` 占位 → `CryptoConfig`，三条纪律）、备份恢复（mysqldump 日备 + OSS 7 份 + 恢复演练）、发版回滚（重启窗口与 P95 排除）、10 条部署门禁验收清单（G1–G10）、批次标注、决策依据索引、盘分配口径冲突裁定（说明文档.md:3579 与可观测性 §7 不一致，以后者为唯一口径源）。
  - ② **IaC 基础设施代码**（`deploy/` 目录下 11 个文件）：`docker-compose.yml`（四服务、仅 Caddy 映射端口、`env_file` 注入密钥、健康检查、mem_limit 1414m 合计）、`Dockerfile`（多阶段 Maven 构建 + Temurin 21 JRE Alpine、非 root 运行、不 COPY .env）、`config/Caddyfile`（TLS 1.2 下限、301 跳转、access log 过滤 Authorization）、`config/my.cnf`（`innodb_buffer_pool_size=256M`、`binlog_expire_logs_seconds=259200`）、`config/redis.conf`（`maxmemory 128mb`、`maxmemory-policy volatile-lru`、`appendfsync everysec`）、`config/logback-spring.xml`（JSON 单行、轮转 50MB/14 天/totalSizeCap 3GB）、`scripts/setup-swap.sh`（2GB swap 文件 + swappiness 10）、`scripts/backup-mysql.sh`（mysqldump --single-transaction + gzip + OSS 上传 + 本地留 1 份）、`scripts/deploy.sh`（一键部署五步：pull → build → compose build → backup → restart）、`scripts/gate-check.sh`（G1–G10 门禁自检）、`.env.example`（全部密钥变量占位模板）。门禁脚本在 WSL2 内 `docker compose config` 语法验证通过，G1–G10 全部逻辑正确。

  **盘分配口径冲突已裁定并回写**：`说明文档.md:3579` 的「OS 8G / 日志 4G / 备份 6G / 余量 6G」与《可观测性架构方案》§7 的七项分配表不一致，以后者为唯一口径源（后者是 T1 阈值 8GB 的直接来源且有可验证的数学推导）。**该行已就地回写**，标注口径作废理由与替代项，保留原行仅作历史留痕。

  **验证**：`docker compose config --quiet` 在 WSL2 内通过（exit code 0）；门禁脚本 `gate-check.sh` 在 WSL2 上 5 PASS / 1 FAIL（FAIL 为 WSL drvfs 权限位 777 所致，属环境限制，ECS 的 ext4 上不会出现）。

- [x] **部署交付物自评审 + 逐项核实修复**（条目 [105]，2026-09-07）：对条目 [104] 的双份交付物做实测评审（不凭记忆，全部以 docker / bash 实跑核对），共识别 16 项问题并分级为 P0×3 / P1×5 / P2×5 / P3×3。**核实阶段推翻 3 项误判、修正 1 项修复方案**，其余 12 项确认成立并已全部修复。

  **P0 阻塞项（3 项，均确证成立并修复）**：
  - ① `my.cnf` 用了 MySQL 8.4 **已移除**的 `expire_logs_days`。实测 `mysqld --validate-config` 报 `unknown variable` 并 abort，即容器根本起不来。修复：改为 `binlog_expire_logs_seconds = 259200`；同时删除已 deprecated 且默认即 ROW 的 `binlog-format`。
  - ② 埋点库 DDL（`V1__init_track_schema.sql`）缺 `CREATE DATABASE` / `USE`。`docker-entrypoint-initdb.d` 只会切入 `MYSQL_DATABASE` 指定的业务库，会把 `track_event_202609` 建错库、`s2s_track` 根本不存在；entrypoint 也不会对埋点库授权。修复：补 `CREATE DATABASE IF NOT EXISTS s2s_track` + `USE` + 末尾 `GRANT SELECT,INSERT,UPDATE,DELETE ON s2s_track.* TO 's2s'@'%'`（账号名已核对与 `.env.example` 的 `MYSQL_USER` 一致）。业务库 DDL 确认**不应**写 `USE`（硬编码库名会与 `.env` 脱耦），仅补执行方式说明注释。
  - ③ `Dockerfile` 依赖的 `pom.xml` / `src/` / `deploy/models/` 当前均不存在（项目根目录是 Flutter 工程），`docker compose build app` 在编码期开工前必然失败。修复：Dockerfile 与部署文档 §2.2 均加前置条件说明块，明确这些是编码阶段交付物。

  **P1 高优（5 项，4 项成立 + 1 项修复方案被修正）**：
  - ④ Caddy 内存口径分裂（文档 50MB / compose `mem_limit: 64m`）→ 统一取 64MB（`mem_limit` 是硬上限，ACME 续期期间有峰值，50MB 余量过窄），文档 §3.1 表格同步改为 Caddy 64MB / OS 约 634MB / 合计 2048MB，并写入「表与 compose 必须逐项一致」的纪律。
  - ⑤ `logback-spring.xml` 依赖 `logstash-logback-encoder` 但文档未记录该 Maven 坐标 → 部署文档 §4.2 补依赖前置说明块。
  - ⑥ `gate-check.sh` 的 G1 依赖 `python3`（ECS 精简镜像可能未预装）→ 改为 `docker compose config` + `awk` 逐服务解析 `published` 键，全脚本仅依赖 bash/awk/grep/stat/docker。
  - ⑦ `deploy.sh` 中 `docker compose up -d --no-deps app --wait`，flag 写在位置参数之后属易碎写法 → 改为 `--no-deps --wait app`。
  - ⑧ **修复方案已修正**：评审原判「Alpine 不含 curl/wget，需 `apk add curl`」。实测 `eclipse-temurin:21-jre-alpine` **自带 `/usr/bin/curl`，仅缺 wget**。故问题定性（healthcheck 用 wget 会永远不 healthy）成立，但修复简化为 healthcheck 改用 `curl -sf`，**无需 `apk add`**，避免徒增镜像体积；Dockerfile 纪律① 相应从「禁止包含 curl/wget」改为「禁止包含 Maven 等构建工具」并注明 curl 例外。

  **P2 中优（5 项，4 项成立 + 1 项误判）**：
  - ⑨ **误判已推翻**：评审称 `LABEL version="${APP_VERSION:-latest}"` 会写入字面量。实测确认——有 `ARG APP_VERSION` 声明时 LABEL 会正常展开，**缺 ARG 声明才是真正原因**。修复：Dockerfile 补 `ARG APP_VERSION=latest`、LABEL 改 `${APP_VERSION}`、compose 的 build 块补 `args: APP_VERSION`。
  - ⑩ compose 中 `command: --defaults-extra-file=...` 与 `!includedir` 机制重复 → 删除并改为注释说明；同时补 `.cnf` 必须 `chmod 644` 的陷阱说明（实测 world-writable 配置文件会被 mysqld **静默忽略**，仅打一行 Warning，WSL drvfs 与 sftp 复制均会触发）。
  - ⑪ `redis.conf` 慢查询注释把单位写错（10000 微秒说成 100ms）→ 更正为 10ms 并补取值理由。
  - ⑫ `backup-mysql.sh` 用 `set -a && source .env` 会把全部密钥（含 pepper / AEAD 主密钥）灌入脚本环境 → 改为逐变量 `grep -oP` 只取备份必需的 4 个变量，并补库名缺失校验。
  - ⑬ 内存预算表与 compose `mem_limit` 合计不一致 → 对齐为 1414m（caddy 64 + app 750 + mysql 450 + redis 150）。

  **P3 低优（3 项，2 项成立 + 1 项误判）**：
  - ⑭ **误判已推翻**：评审称 `.env.example` 中 JSON 值的引号会被 `env_file` 保留导致解析失败。实测三种写法（无引号 / 双引号 / 单引号包裹 JSON）均正确解析，**引号不会被保留**，无需修改。
  - ⑮ 门禁表 G10 参数名与 G1 命令、G8 备份路径（`/tmp` → `/var/backups/s2s`）等文档细节已同步更正。

  **评审过程中另修复的 4 处实现 bug**：`gate-check.sh` G1 原写法 `grep 'ports:' | grep -v 'caddy'` 恒 FAIL（`ports:` 行不含服务名，无法判断归属）；G2 原写法 `grep 'COPY.*\.env'` 误命中 Dockerfile 注释里的「禁止 COPY .env」文字；`logback-spring.xml` 审计 appender 内写成 `<maxFileSize="50MB"/>` 属性式自闭合标签（XML 语法错误）；同文件误用 `DuplicateMessageFilter` 做脱敏（该 filter 与脱敏无关，已删除并注明脱敏在应用代码侧完成、由门禁 G9 验收）。

  **修复后回归验证（4 项全通过）**：
  1. `docker compose config --quiet` exit 0；展开后仅 `caddy` 服务有 `published`（80 / 443），app / mysql / redis 均无 → G1 判据成立。
  2. `my.cnf` 在 mysql:8.4 内 `chmod 644` 后执行 `mysqld --validate-config`，退出码 0、**零 ERROR 零 Warning**，`expire_logs_days` 已彻底移除。（注：直接挂载 drvfs 路径会因 777 被静默忽略而"假通过"，故验证脚本先 `cp` 到容器内 ext4 再 `chmod 644`，这一步本身也复现了 ⑩ 的陷阱。）
  3. `deploy/scripts/` 下 4 个脚本 `bash -n` 语法检查全部 OK。
  4. 埋点 DDL 确认含 `CREATE DATABASE` + `USE` + `GRANT`；业务 DDL 确认无硬编码库名。

  **本次修复涉及 12 个文件**：`deploy/config/my.cnf`、`deploy/config/redis.conf`、`deploy/config/logback-spring.xml`、`deploy/docker-compose.yml`、`deploy/Dockerfile`、`deploy/scripts/gate-check.sh`、`deploy/scripts/deploy.sh`、`deploy/scripts/backup-mysql.sh`、`docs/database/ddl/V1__init_schema.sql`、`docs/database/ddl/V1__init_track_schema.sql`、`deploy/docs/部署架构设计文档.md`、`说明文档.md`。所有临时验证脚本已清理，`deploy/` 目录下仅保留正式文件。

  **遗留前置条件（不属缺陷，需在编码阶段闭合）**：① Java 工程骨架（`pom.xml` / `src/` / `deploy/models/`）尚不存在，`docker compose build app` 需待编码期开工后才能实跑；② `pom.xml` 中须引入 `logstash-logback-encoder`，否则 logback 配置启动即失败；③ 上线前须完整跑一遍备份恢复演练（门禁 G8 的完整验收）。

- [x] **多环境与容器化环境搭建（IaC 四类交付物）**（条目 [106]，2026-09-07）：以条目 [104] 的 IaC 代码为基座，按《部署架构设计文档》与《技术栈选型说明》完成 dev / staging / prod 三环境化改造，交付「可运行多环境 + 容器镜像基线 + 配置中心 + 密钥库」四类产物。

  **三项选型（用户确认）**：① 多环境定义 → **Profile 切换**（Compose base + 环境 override + 环境目录隔离，同机按需切换，2 核 2G 无法并行）；② 密钥库 → **手动静态管理**（分环境 `.env` 文件，不入 Git，与既有方案一致）；③ 配置中心 → **Profile 外部化**（Spring Boot 多 Profile + 外部化配置目录挂载，零额外进程）。

  **① 可运行多环境**：
  - `deploy/docker-compose.yml` 改造为**环境无关 base**：移除全部 `ports` 与 `mem_limit`（下沉到 override）；四服务统一 `env_file: ./env/.env.${S2S_ENV:-prod}`；Caddyfile 按 `./config/Caddyfile.${S2S_ENV:-prod}` 插值挂载；`volumes` 统一收敛到 base（**因 Compose 数组语义是整体覆盖而非逐项合并**，override 重复声明会导致命名卷丢失、证书与日志不落盘）。
  - `deploy/compose/docker-compose.{dev,staging,prod}.yml` 三套 override：dev 全服务绑 `127.0.0.1`（app 8080 / mysql 3306 / redis 6379）、caddy 标 `profiles: ["tls"]` 默认不创建、mem_limit 放宽至合计 3164m、`JAVA_OPTS: -Xmx1200m`、Actuator 全开；staging 与 prod **仅两处差异**（caddy 端口绑 `127.0.0.1:8443/8080` vs `0.0.0.0:443/80`、Caddyfile 自签 vs ACME），mem_limit 与判据完全一致（app 750m / mysql 450m / redis 150m / caddy 64m = 1414m）。
  - `deploy/scripts/env-up.sh`（新建）：三环境唯一启动入口，把「base + override + `--env-file` + `S2S_ENV`」四件配套物封装为一条命令；`assert_no_other_env()` 读 `.current-env` 记账文件 + `docker ps` 双重确认，状态文件残留时校验容器是否仍存活，实现三环境互斥保护；支持 `--config-only` / `--down` / `--` 透传。
  - `deploy/scripts/{deploy,gate-check,backup-mysql}.sh` 全部改造为多环境版本，四脚本使用**完全一致的 `resolve_env()`**（位置参数 → `.current-env` → 报错），杜绝各脚本自行发明环境解析逻辑。

  **② 容器镜像基线**：`deploy/images.lock`（新建）为单一镜像台账，格式 `<镜像名>:<tag> <digest|PENDING>`，5 条记录覆盖 caddy:2.8-alpine / mysql:8.4 / redis:7.4-alpine / maven:3.9-eclipse-temurin-21 / eclipse-temurin:21-jre-alpine；`deploy/scripts/image-baseline.sh`（新建）三模式：`--verify`（默认，被 env-up.sh 启动前与门禁 G11 两处调用）/ `--refresh`（pull 后 `sed -i` 就地回写 digest，分隔符用 `|` 以避开镜像名中的 `/` 与 `:`）/ `--pull`。**tag 是可变引用、digest 才是不可变标识**，故 latest 或缺 tag 直接 FAIL；digest 为 PENDING 或本机未拉取判 WARN 不阻塞（避免离线环境无法启动）。头部明确禁止凭空编造 sha256。

  **③ 配置中心**：`deploy/config/app/application-{dev,staging,prod}.yml` 三套外部化配置，挂载至 `/opt/s2s/config`（镜像 WORKDIR=`/opt/s2s`，即容器内 `./config/`，Spring Boot 标准外部化路径，优先级高于 jar 内同名文件）。dev 开 `show-sql` 与 `org.hibernate.orm.jdbc.bind: TRACE`（已注明 staging/prod 绝不可开，会打印绑定参数即明文口令）；staging 与 prod 判据一致（WARN 日志、hikari 10/5、仅暴露 health）。**边界纪律**：本目录入 Git 可审计、禁含任何密钥字面量、只允许 `${ENV_VAR}` 占位；数据源连接串与口令刻意**不在 yml 中重复定义**，只由 compose `environment:` 单一来源注入，避免「改了没生效」。

  **④ 密钥库**：`deploy/env/.env.{dev,staging,prod}.example` 三套模板（原单份 `deploy/.env.example` 已删除）；`.gitignore` 新增 `deploy/env/.env*` + `!deploy/env/.env*.example`（**两行顺序不可颠倒**，否则否定模式失效）+ `deploy/.current-env`；新增门禁 G3b 校验密钥文件未被 git 跟踪。**关键机制辨析**：`--env-file` 只供 compose 自身 `${VAR}` 插值、**不注入容器**；`env_file:` 才把整份密钥注入容器进程环境 —— 两者都必需，缺一即残。

  **门禁扩充与环境分级**：`gate-check.sh` 从 G1–G10 扩至 G1–G11（新增 G1b 校验 dev 数据面端口必须绑 `127.0.0.1`、G3b 密钥文件未入 Git、G11 镜像基线）。**仅两项按环境降级**：G1（数据面端口）在 dev 降为 INFO 且其合法性依赖 G1b 兜底；G3（密钥文件权限 600）在 dev 降为 WARN（WSL/NTFS 权限位固定 777 属环境限制）。G6/G7 在 dev 下 SKIP。其余判据三环境完全一致，不因环境放水。G10 补 `MYSQL_PWD` 注入（从 env 文件提取后经 `docker exec -e` 传入，**不落命令行**以免进 `ps` 输出）。

  **两处易错点已修正**：① `JAVA_OPTS` 变量名是硬约束 —— Dockerfile ENTRYPOINT 为 `sh -c "java $JAVA_OPTS -jar app.jar"`，写成 `JVM_OPTS` 会被**静默忽略**，堆上限失效直接 OOM，已在三套 override 中校验无误写；② staging/prod 的 app 无 `ports` 映射（G1 强制），宿主机 `curl localhost:8080` 指向的端口根本不存在，故 `deploy.sh` 健康检查改为 `docker exec s2s-app curl -sf http://localhost:8080/actuator/health | grep -q '"status":"UP"'`，借容器网络命名空间访问。

  **文档同步**：`deploy/docs/部署架构设计文档.md` 新增 **§14 多环境与容器化环境搭建**（8 子节：§14.1 选型与文件布局 / §14.2 环境切换 / §14.3 三环境差异矩阵 / §14.4 配置中心 / §14.5 门禁环境分级判定 / §14.6 容器镜像基线 / §14.7 首次搭建步骤 / §14.8 已知局限），并同步更新 §3.3（mem_limit 下沉到 override）、§6.1/§6.2（dev/staging 走 `tls internal` 自签、Caddyfile 改 `{$SITE_DOMAIN}` 变量）、§7.1/§7.2/§7.3（注入链路改 `deploy/env/.env.<env>` → `application-{profile}.yml`）、§8.2/§8.3（备份脚本多环境版）、§9.2/§9.4（一键发版与回滚四件配套物）、§10.1/§10.2（门禁表扩至 G11）共 7 处，消除单环境心智残留。**§14.2–§14.6 编号已被大量代码注释引用，不可随意重排。**

  **验证（已实跑，非推断）**：
  1. **三套 `docker compose config` 全部 exit 0**（WSL2 内 Docker Compose v2.35.1，临时从 `.example` 复制出 `.env.{dev,staging,prod}` 供 compose 插值，校验后即删除）。
  2. **G1 判据成立**：展开后 staging 的 `published` 仅 8443/8080、prod 仅 443/80，且均归属 `caddy` 服务；app/mysql/redis 三个数据面服务在 staging/prod 下**完全无 `published` 键**。
  3. **G1b 判据成立**：dev 展开后 8080/3306/6379 三个端口均带 `host_ip: 127.0.0.1`，无一暴露到 `0.0.0.0`。
  4. **`env_file` 注入链路实证**：三环境 app 服务展开后各 34 个环境变量键，13 个密钥类键（`AEAD_MASTER_KEYS_JSON` / `HMAC_PEPPERS_JSON` / `JWT_SECRET` / `MYSQL_PASSWORD` / `SPRING_DATASOURCE_PASSWORD` 等）全部到位，证明 `env_file: ./env/.env.${S2S_ENV}` 的插值与注入均生效。
  5. **Profile 与 JVM 参数正确分环境**：`SPRING_PROFILES_ACTIVE` 依次展开为 dev/staging/prod；`JAVA_OPTS` 在 dev 为 `-Xmx1200m`、staging/prod 为 `-Xmx600m`；三环境**均无 `JVM_OPTS` 误写**。
  6. **mem_limit 实测合计未超预算**：dev = 1500+1024+512+128 = 3164m（caddy 因 `profiles: ["tls"]` 未激活，`config` 展开只含 3 个服务，佐证 profiles 生效）；staging 与 prod 均为 750+450+150+64 = 1414m。
  7. **Caddyfile 按环境插值命中**：staging 挂载 `config/Caddyfile.staging`、prod 挂载 `config/Caddyfile.prod`；三环境配置中心目录均挂载至 `/opt/s2s/config`。
  8. 6 个脚本 `bash -n` 全部 OK；4 个 compose 文件经 PyYAML 结构校验通过（无 `volumes`/`env_file` 重复声明，镜像 tag 与 `images.lock` 一致）。

  **遗留待实测（1 项，需在目标机器闭合）**：`image-baseline.sh --refresh` 的 `sed -i` 回写是否精确命中镜像行，并补齐 `images.lock` 与文档 §14.6 的 digest 实测值（当前 5 条均为 PENDING）—— 需能拉取镜像的网络环境。

### 2026-09-08（分支规范与质量门禁：构想 → CI 裁决 → 门禁四态改造）

- [x] **ce-ideate：分支规范 / 质量门禁 / Git 仓库三方向构想**（条目 [107]，2026-09-08）：用户要求「建立项目的分支规范、质量门禁标准、Git 仓库」，按 `ce-ideate` 走了接地 → 多视角发散 → 批判筛选流程，产出 `docs/ideation/2026-09-08-分支规范与质量门禁构想.md`。
      **接地阶段实测到 6 条既有事实**，其中三条直接改变了后续方向：① 仓库 `origin` 已是 `https://github.com/liujie12/s2s.git`，但**首次提交尚未发生**（条目 [89] 已侧面印证：三份架构文档 `git mv` 报 `not under version control`）；② 全仓 **45 个 `.sh` 文件权限位实测 45/45 全为 `100644`、`100755` 为 0** —— 门禁脚本自己没有执行位；③ `gate-check.sh` 中裸 `echo "[SKIP]"` 实测 **12 处**（`:122 :136 :193 :201 :237 :251 :271 :280 :287 :320 :336 :360`），全部不进任何计数器。构想文档里最初写的「9 处」是估数，本轮读完全文后已更正为 12 处。
      **存活 7 主 + 2 顺手**：S1 条目即分支 + 两类 tag 命名空间 / S2 不可逆标识冻结闸门 / **S3 SKIP 补成真计数器 + 抽出 `gate-lib.sh` 外壳** / S4 门禁结论写进 `git notes` / S5 lint 棘轮（只卡改动文件面）/ S6 GitHub Actions 是否翻案 / S7 未跟踪项白名单（新增 G12）/ S8 双份技能包收口 / S9 `release-gate.log` 搬出 `build/`。另有 **13 条被否决候选各附理由与复活阈值**（如 GitFlow、多长期分支、commitlint 强制、语义化版本自动发布等，共同否决理由是「1 人团队 + 38 天工期下，规则的维护成本高于它挡住的风险」）。
      **一次流程偏离须留痕**：用户中途反馈「怎么这么长时间还没搞定」，据此**跳过了 Phase 3 的批判环节与基础校验代理**，直接用已完成的 4 个发散代理产出合成交付物；另有 1 个代理返回空结果（重派后正常）、1 个代理结果丢失未补。因此该构想文档的 13 条否决理由**未经独立批判轮复核**，属于已知质量折扣。

- [x] **ce-pov 裁决 S6：GitHub Actions 从「⚪ 可不做」翻案为 Trial，但翻的不是构想里那一条**（条目 [108]，2026-09-08）：用户选「先展开 S6，讨论 CI 是否翻案」。判定这是 `ce-pov` 的**外部采纳（Adopt）形态**而非 `ce-brainstorm`（议题是「是否采纳一个外部平台」，不是「怎么设计」），按双底线门取证后给出**Trial** 分级。
      **裁决前先撤回了构想阶段自己的一条佐证。** 构想里把「45 个 `.sh` 无执行位」当作「本地不可判定、故需要 CI」的证据 —— 但它只需本地一条 `git ls-files -s '*.sh'` 即可查出。**它不是「本地不可判定」的反例，而是「本地没人想到要查」的例子**，两者对应的解法完全不同（后者该加一条本地判据，不是引入一个平台）。已在裁决中明确撤回。
      **同时推翻了构想给出的落地路径。** 构想写的是「把 `gate-check.sh` 装进 workflow」，实读后确认**它是部署门禁而非代码门禁**：13 项里 G1/G1b/G2/G4/G10/G11 依赖运行中的 docker 容器、G6 依赖真实域名、G3/G8 依赖服务器文件权限、G7 是纯人工项。装进 workflow 会**全部走 SKIP 分支，产出「绿勾但内容为空」** —— 比不做更糟，因为它会制造已验证的错觉。故 CI 范围重新限定为**代码面判据**：`flutter analyze` / `flutter test` / 红线 grep（`888888` 后门等）/ `.sh` 执行位 / `git status` 白名单。
      **外部底线取证（均已核实来源）**：SLSA Build L2 要求 provenance 由 control plane 生成签名、明文写 `not on an individual's workstation`（v1.0 已 Retired，当前 v1.2，结论不变）；GitHub Free 私有仓 2000 分钟/月、公共仓标准 runner 免费；DORA 的 CI 反馈上限约 10 分钟且自设「Objections to CI」章节；**Fowler 2024 修订版专章「When we should not use Continuous Integration」明写「没有自测试代码就上 CI 不会奏效」**；trunkbaseddevelopment.com 认可极小团队直推 trunk。**一项未能证实**：2025–2026 单人/小团队 CI 采用率的量化统计（DORA 2025 主题为 AI 辅助开发、Octoverse 2025 为 AI/TypeScript、SO 2025 无对应结论）—— 已标注为未获证的论据，不用于支撑结论。
      **三条生效条件（缺一即不成立）**：① **先修 SKIP 三态**（否则 CI 里的 SKIP 会以绿勾呈现，正是本条最想避免的失效形态）；② CI 只承载代码面判据，部署门禁留在本地；③ 等服务端有代码（当前连 `pom.xml` 都不存在，此时建 CI 是给空仓库配流水线）。
      **交接项（未执行）**：`DevSecOps接入方案.md §9` 第 11 项文案应从「⚪ 可不做（本地已可判定）」改为「🟠 编码期，前置 = 第 3 项 + SKIP 三态」，理由栏改「本地 PASS 由被判定者签发」。

- [x] **S3 落地：`gate-check.sh` 门禁四态改造，修掉 5 处「什么都没校验的通过」**（条目 [109]，2026-09-08）：按 S6 裁决的硬前置执行。**原以为只是「给 SKIP 补个计数器」，实测发现 5 处伪 PASS —— 即门禁在完全没有执行判定的情况下报通过。**
      **定四态语义**：PASS（已判定合格，不影响退出码）/ FAIL（已判定不合格，exit 1）/ **SKIP**（应当判定但缺前置条件而未能判定，结论是「不知道」，**exit 2**）/ **N/A**（按环境设计就不存在可判定对象，不影响退出码）。**SKIP 与 N/A 的分界线是「有没有可判定的对象」** —— dev 没有云安全组是 N/A；prod 上 docker 没装导致判不了是 SKIP。
      **两条设计理由必须写下，否则第一天就会被绕过。** ① **N/A 必须独立于 SKIP**：若「不适用」也计入 SKIP，prod 每次跑必然 SKIP≥1、退出码恒为 2，这条规则会立刻被 `|| true` 消解；② **SKIP 必须阻塞**：调用方若写 `if gate-check.sh; then 发版`，SKIP 归 0 就等于「没查过 = 可以发」。
      **5 处伪 PASS 逐项**：① G3 在 dev 写 `pass "（dev 环境权限检查降级）"` —— WSL/NTFS 挂载点下 `stat` 恒返 777，**权限判定从未发生过**；② G5 无条件 `pass "（人工确认项）"` —— 只打印了手动验证命令，人做没做无从得知；③ G7 同上；④ **G1 在 config 失败时同时记 FAIL 与 PASS**（真 bug：`:89` 记 FAIL 后不中断，`G1_VIOLATION` 保持空串，一路落到 `:119` 的 else 再记一次 PASS）；⑤ **G11 在镜像台账 5 项全 `PENDING`（基线实质不存在）时报 PASS**。经审查确认 `:114` G1 的 dev 分支是**真 PASS**（判据确实执行、只是 dev 判据不同且由 G1b 兜底），予以保留。
      **⑤ 的根因不在 gate-check 而在被调用方，属顺藤摸出的连带缺陷。** `image-baseline.sh` 只有 0/1 两档退出码，把 WARN（digest 为 PENDING 或镜像未拉取）折进了 0；而 G11 写作 `if bash ... --verify` 只能分辨真/假。已给 `image-baseline.sh` **增设 exit 2** 并在头部注释写明「WARN 必须走独立退出码，不可并入 0」，G11 改为按退出码 `case` 三分支。**`env-up.sh:218` 侧本就写作 `|| echo "[WARN]"` 属非阻塞调用，不受 exit 2 影响** —— 这也是必须用独立退出码而非改文案的原因：同一个信号要能被阻塞型与非阻塞型两类调用方分别处置。
      **人工确认项改为显式声明**：`GATE_MANUAL_CONFIRMED="G5,G7"` 声明则 PASS，否则 SKIP。理由是「打印一条命令然后记 PASS」等于**把判定权交给被判定者本人**，与 DevSecOps §3.2 四要素中的「留证方式」相违；这个环境变量出现在发版命令里，本身就是留证。
      **改动落点**：`gate-check.sh` 共 14 处编辑（文件头四态说明 / 新增 `skip`+`not_applicable`+`manual_confirmed` 三函数与 `SKIP_ITEMS` 名单 / G1 加 `G1_DECIDED` 短路 / **G1b 结构重构** —— 原 `if [[ dev ]]` 把整段含标题包住，导致 staging/prod 下整项在输出中彻底消失，现改为标题无条件打印、非 dev 走 N/A / G2–G10 共 12 处裸 SKIP 全部改真计数 / G4 与 G10 补 docker 未装前置分支 / G11 改 case / 汇总段四列 + SKIP 逐条列名 + 退出码三档），386 → 约 503 行；`image-baseline.sh` 2 处；`部署架构设计文档.md` §14.5 全节重写 + §10.1/§10.2 同步（退出码三档、G5/G7 声明方式、G11 台账 PENDING 时 SKIP、人工项留证理由）。
      **负向实测 5 用例全部通过（构造应当失败的输入，不用正向跑通充当证据）**：`image-baseline --verify` 台账全 PENDING → **rc=2** ✓；`gate-check dev` 有 FAIL（验 FAIL 优先于 SKIP）→ **rc=1** ✓；非法环境名 → **rc=1 且 PASS 计数 0** ✓；G5 声明前后 → **SKIP → PASS** ✓；纯 SKIP 无 FAIL 场景 → **rc=2 而非 0** ✓。两脚本 `bash -n` 均 0。改后实测汇总（Windows 本机 dev）`PASS 2 | FAIL 2 | SKIP 8 | N/A 2`，改前同场景为 `PASS 3 | FAIL 2`（3 个 PASS 里有 1 个是 G11 静默 PASS）。一次性自测器材 `.gate-selftest.tmp.sh` 已按纪律删除。
      **过程中三次退出码测量得到假结果，根因在测量方法而非被测代码。** `bash -c '... ; echo rc=$?'` 输出 `rc=`（空）—— **PowerShell 在把命令传给 bash 之前就把 `$?` 展开成了空串**，所有含 `$?` 的内联测量全部无效；改写引号时又触发 `syntax error near unexpected token '('`；`>/dev/null` 被 PowerShell 当成 Windows 路径报 `D:\dev\null`。最终改为写独立 `.sh` 脚本文件承载全部用例才拿到真值。**`exit 2` 一直是对的，之前观测到的 0 是测量假象** —— 与条目 [94] 那条「验证结果与已知事实冲突时先怀疑验证手段」同型，本轮是它在 shell 层的实例。
      **⚠️ 三项遗留（已报告，未擅自扩大范围）**：① **`gate-check.sh` 现约 `:238` 的假阴性**：若 `deploy/Dockerfile` 不存在，`grep` 返回非 0 → else 分支 → `pass "Dockerfile 中无 COPY .env 指令"`，**文件不存在被判成通过**，与 G2 镜像段当年那个假阴性同构 —— **已于条目 [110] 修复**；② **exit 2 目前无任何调用方消费** —— `deploy.sh:269` 与 `env-up.sh:247` 都只 `echo` 提示门禁命令、不读退出码，故本轮改造目前只做到「输出更诚实」，**尚未构成卡点**；③ S3 后半部分「抽出 `gate-lib.sh` 外壳」未做（原意是供出包门禁 11 步复用，其第 1/2/10/11 步全是外壳职责）。
      **本轮方法论增量两条。** ① **「给缺失的状态补个计数器」这类任务，实际工作量在于发现有多少状态被伪装成了已有状态。** 原始判断是「12 处 SKIP 没计数」，真实情况是另有 5 处根本没判定却报了 PASS —— 前者是**诚实但未统计**，后者是**不诚实**，后者危害大一个量级且不会出现在任何计数里。**故排查入口不该是「找 SKIP」，而是「逐项问这个 PASS 是靠什么判出来的」。** ② **门禁的第三态必须与退出码绑定，只改输出文案等于没改。** 若 SKIP 只体现在日志文字里而退出码仍归 0，那么所有以退出码为判据的调用方（也就是唯一会自动阻塞的那类调用方）**完全看不到这个态**。这与 CONCEPTS.md「闸门」词条那句「有效性只能靠构造应当失败的输入、确认它真的失败来证明」是同一条 —— 本轮 5 个负向用例里有 3 个断言的正是退出码而非输出内容。

- [x] **修复 G2 假阴性，并顺带查出 G9 同型缺陷**（条目 [110]，2026-09-08）：承条目 [109] 遗留项①。用户选「先修复 G2 假阴性」。
      **G2 文本层**：原写法 `if grep -nE '...' "${DEPLOY_DIR}/Dockerfile" 2>/dev/null; then fail; else pass; fi`，`Dockerfile` 不存在时 grep 返回非 0 → else → `pass "Dockerfile 中无 COPY .env 指令"`。**那个 `2>/dev/null` 还额外把 grep 的 `No such file or directory` 一并吞掉，连排查线索都不留** —— 它本是为消音而加，实际效果是掩盖了唯一的异常信号。现改为先 `[[ ! -f ]]` 断言待判对象存在，缺文件走 `skip "G2"`；同时**去掉了 `2>/dev/null`**，文件既已确认存在，grep 若仍报错就是真异常，不该被静音。
      **修的时候顺手排了一遍全脚本的同型写法，查出 G9 有同一个毛病。** 原写法只判 `[[ -d "${LOG_DIR}" ]]`，目录存在但目录下一个 `.log` 都没有时 `HITS` 为空 → else → `pass "日志中无手机号明文"`。**这一项比 G2 更危险**：G2 缺的是仓库里的文件（缺了很扎眼），而「目录在、文件没有」在真实运维中太常见 —— 服务刚起还没落盘、日志被 logrotate 搬走、挂载点没挂上，**每一种都会稳定地拿到一个绿色的 PASS**，而它恰好是唯一一道拦个人信息外泄的判据。现改为先确认扫描面非空，为空走 `skip "G9"`。
      **两处的共同形态已写入文档 §14.5 易错点，作为新增判据的检查清单**：**判据的执行结果与判据的未执行，在 shell 里都表现为「非 0 退出码 / 空输出」**，故 `if grep ...; then fail; else pass; fi` 这个句式必然把「没东西可搜」归入「搜过了很干净」。改法固定为「先断言待判对象存在，再执行判据」。§14.5 里已把 G2 文本层、G2 镜像层（写死 `s2s-app:latest` 那次）、G9 三例并列，并留下自问句：**这条 grep 搜不到，是因为真的干净，还是因为压根没东西可搜？** 至此该形态在本脚本内累计出现 3 次，**它不是偶发笔误而是句式缺陷**，故写成清单而非逐个记 bug。
      **负向实测 5 用例全过**（`bash -n` 先行通过）：A `Dockerfile` 存在 → `[PASS]` ✓；B 改名后 → `[SKIP] 未找到 .../Dockerfile` **且出现在汇总 SKIP 名单中** ✓（改前此处为 PASS）；C `/var/log/app` 存在但空 → `[SKIP] 无 .log 文件，扫描面为空` ✓（改前为 PASS）；D 写入含 `13812345678` 的日志 → `[FAIL] 发现疑似手机号明文` ✓（证明修改未把真判据一起 SKIP 掉）；E 写入已脱敏日志 → `[PASS]` ✓。**C/D/E 三例连测的意义在于：只验 C 会 SKIP 不够，还必须验 D 仍能 FAIL** —— 否则「把判据改成永远 SKIP」也能通过 C。
      **测试器材两条纪律都守住了**：① 因用例 B 需改名真实的 `deploy/Dockerfile`，器材内用 `trap restore EXIT` 保证无论脚本如何退出都会改回，末尾另打印恢复确认；② G9 用例需 `/var/log/app` 写权限，WSL 默认用户 `id -u` 为 1000 无权限，改用 `wsl -u root` 执行并在用例后 `rm -rf /var/log/app` 清理。一次性器材 `.g2-selftest.tmp.sh` 已按纪律删除，`git status deploy/` 复核无残留（`?? deploy/` 是首次提交尚未发生的既有状态，非本次产生）。
      规格同步：文档 §10.1 的 G2 行改为文本层/镜像层双判据并注明各自 SKIP 条件、G9 行注明扫描面为空时 SKIP；§14.5 易错点新增上述清单条目。

- [x] **exit 2 的消费改造：让 `deploy.sh` 真正读门禁退出码，门禁从此才是卡点**（条目 [111]，2026-09-08）：承条目 [109] 遗留项②。**这是三个条目里唯一改变外部行为的一项** —— [109][110] 把判据修准了，但改前 `deploy.sh` 从头到尾没有一行调用 `gate-check.sh`，只在发版成功后 `echo "建议执行门禁自检"`。**即：前两轮辛苦区分出来的 FAIL / SKIP / N/A 三态，没有任何调用方消费，退出码 2 写给谁看都不知道。** 按 CONCEPTS.md「可判定门禁」词条，判据 + **卡点** + 失败后果三件套里缺了中间那件，整套东西还不成立。
      **改动落点四处。** ① 新增 `run_gate_check()`（[deploy.sh](file:///d:/developer/code/aicoding/s2s/deploy/scripts/deploy.sh#L226-L281)）：门禁脚本缺失先硬拦（**门禁缺失不等于门禁通过**，否则就是 [110] 那个假阴性形态在流程层的翻版），再按 0/1/2 三档分别处置。**刻意没写成 `if bash gate-check.sh; then`** —— 那样只能分辨 0 与非 0，会把「有 FAIL」和「有 SKIP」混成一句话，用户拿不到「该修代码还是该补前置条件」这个唯一有用的信息。② 门禁插在**步骤 4**，即镜像构建之后、备份与滚动重启之前：放在构建前，G2 镜像层无从判定刚生成的那个时间戳 tag（只能 SKIP，恰好放过「新镜像里打进了 .env」这类本次发版新引入的风险）；放在重启后，FAIL 也拦不住了，新版本已在线上跑着，门禁退化为事后告知，且备份的 IO 已白花。③ 步骤号 1-5 重编为 1-6。④ 末尾提示从「建议执行门禁自检」改为「本次发版已通过门禁自查」，并在 [env-up.sh](file:///d:/developer/code/aicoding/s2s/deploy/scripts/env-up.sh#L245-L253) 把 prod 的同类措辞改为「查看当前门禁状态（发版时 deploy.sh 会自动强制执行）」+ 退出码三档说明。**这两句文案不是润色**：把一个硬卡点写成「建议」，读的人自然不会当回事，而写成「建议」的那个版本恰恰真的没在执行。
      **`--gate-allow-skip="理由"` 放行机制，以及为什么理由必填。** exit 2 默认阻塞，但 SKIP 确有正当无法判定的场合（ECS 无公网出口拉不到镜像等），需要放行口子。**关键设计是不接受裸开关形式，理由必须以 `=` 附带，空理由也拒绝，理由随后写入发版日志。** 裸开关会退化成肌肉记忆——顺手一敲就过，与 `|| true` 无异；而必须现场写一句「为什么这次可以不判定那几项」，会强迫人回头看一眼 SKIP 的到底是哪几项。该理由构成 DevSecOps §3.2 四要素里的**留证方式**。另一条边界：**放行开关只对 SKIP 生效，对 FAIL 无效**（FAIL 是判据已执行且不合格，没有「不知道」的余地），此边界单独设了负向用例 8 守着。
      **实测踩到本轮最凶的一个坑：`|| true` 会重置 `PIPESTATUS`，导致门禁彻底失效。** 起因是脚本头有 `set -euo pipefail`，直接写 `bash gate-check.sh | tee` 时门禁返回 1/2 会让整条管道非 0、`-e` 立刻终止，后面的 case 分档一次都跑不到（用户只看到无声退出）。为绕开它改写成 `| tee ... || true` —— **这一版实测四种退出码全部打印「门禁全部通过」并继续发版**，因为 `|| true` 一执行，`PIPESTATUS` 就被 `true` 的结果覆盖成 `(0)`，`gate_rc` 恒为 0。最终改为 `set +o pipefail` → 跑管道 → 读 `PIPESTATUS[0]` → `set -o pipefail`：关闭 pipefail 后管道退出码取末位的 tee（恒 0）故 `-e` 不触发，而 `PIPESTATUS[0]` 仍保留门禁的真实退出码。**这段 200 余字的坑位说明已写进函数头注释并标注「此坑已实测复现」** —— 因为它的表征是「一切正常，发版成功」，将来任何人为了消掉 `set +o pipefail` 的突兀感而改回 `|| true`，都不会看到任何报错。
      **负向实测 8 用例全过。** 器材做法：`sed` 抽出 `deploy.sh` 从头到 `parse_args "$@"` 前的函数定义区 source 进来，覆盖 `DEPLOY_DIR` 指向临时目录，用替身 `gate-check.sh` 精确控制退出码；观测点是 `run_gate_check` 之后那行 `echo REACHED_NEXT_STEP`——出现即放行。（不能直接跑真实 `deploy.sh`：它会 git pull / mvn / docker build，且在 Windows 上前置校验就过不去，测不到门禁这一段。）用例：1 rc=0 放行 ✓；2 rc=1 中止 ✓；3 rc=2 无放行中止 ✓；4 rc=2 带理由放行且打印理由 ✓；5 裸开关拒绝 ✓；6 空理由拒绝 ✓；7 门禁脚本缺失硬拦 ✓；8 `--gate-allow-skip` 不得绕过 FAIL ✓。另设**门禁正文透传双向断言**：应执行的用例必须能看到门禁自身输出（否则 tee 吞了输出，用户会拿到一个看不见 FAIL 清单的黑盒），不应执行的用例必须看不到（否则说明参数校验被绕过）。器材首版把「有没有门禁正文」写成了对所有用例的无条件断言，于是用例 5/6 两条**正确行为被报成缺陷**——器材自身的假阳性，已修正为按用例声明期望。一次性器材已按纪律删除。
      **本轮方法论增量一条：判据的严谨程度与门禁的有效性无关，只要没有调用方消费退出码，两者的乘积就是零。** [109][110] 两轮共修 7 处伪 PASS/假阴性，投入远大于本轮，但**在本轮之前，那 7 处修与不修对发版流程的实际影响完全相同**——因为发版从不看门禁结果。这正对上 CONCEPTS.md「一组门禁里通常只有一项挡住核心风险，而那一项恰好最容易被『体验优化』掉」：本项目里那一项不是某条 G 判据，**而是「有没有人读退出码」这件事本身**，它此前被优化成了一句 echo。

- [x] **抽出 `gate-lib.sh` 外壳，L2/L3 门禁共用同一份四态+退出码实现**（条目 [112]，2026-09-08）：承条目 [109] 遗留项③。用户选「开始处理 gate-lib.sh 外壳抽取」。
      **范围**：从 `gate-check.sh` 提取出 5 个函数（`pass`/`fail`/`skip`/`not_applicable`/`manual_confirmed`）+ 4 个计数器（`GATE_PASS_COUNT` 等）+ `gate_summary`（打印汇总）+ `gate_exit_code`（只算退出码，不退出）+ `gate_conclude`（汇总+退出）。库文件 [gate-lib.sh](file:///d:/developer/code/aicoding/s2s/deploy/scripts/gate-lib.sh) 96 行，[gate-check.sh](file:///d:/developer/code/aicoding/s2s/deploy/scripts/gate-check.sh) 删去 68 行再补 18 行加载逻辑，净减 50 行。
      **提取理由**：L3 出包门禁（DevSecOps §4.4 的 11 步序列：工作区干净 → 分支与 commit hash 留证 → 单车速测试 → 保留键覆盖测试 → 安全断言测试 → flutter build → 产物层后门 grep → 签名验证 → APK SHA-256 → 写 `release-gate.log` → FAIL 删产物）中第 1/2/10/11 步需要的外壳职责（四态记账、汇总、退出码三档结算）与 L2 部署门禁完全一致，但两者的判据完全不同。若各写一份，改了一处漏改另一处时「SKIP 是否阻塞」会悄悄不一致，而这种不一致没有任何检出手段——两边都能跑通，差别只在退出码，而退出码没人盯着看。
      **关键设计决策**。「加载失败必须先硬拦，不能降级为警告。」`gate-check.sh` 刻意不用 `set -e`（门禁必须跑完所有项再汇总），若 source 失败而不拦，后续 pass/fail 全是 command not found（stderr 通常被 tee 淹没），四个计数器不存在，脚本以最后一条命令的退出码收尾、极可能是 0——与 G2「Dockerfile 不存在却报 PASS」是同一形态的假阴性，只是发生在外壳层、波及全部 11 项。故 `gate-lib.sh` 的头部注释和 `gate-check.sh` 的加载段都双双重申了这一点。另 `gate_exit_code` 从 `gate_conclude` 中拆出供 L3 使用：L3 第 10 步需先写 `release-gate.log` 再退出，第 11 步需在 FAIL 时先 `rm` 产物 APK 再退出，若只用 `gate_conclude`（它自带 `exit`），这两个钩子都插不进去。
      **负向测试 15 用例**（组一：外壳库缺失/语法损坏需硬拦，不得报通过 → 2 例；组二：四态→退出码映射完整覆盖 0/1/2 三档 + N/A 不阻塞 → 6 例；组三：SKIP 逐条列名不可只报数字、manual_confirmed 精确匹配无前缀误判 → 5 例；组四：分离路径 `gate_summary + gate_exit_code` 让 L3 钩子可执行 → 2 例）。**另增变异测试**：故意注入 `echo 0` 代替 `echo 2`（SKIP 归 0 的缺陷），验证器材确有检出能力——6 例立刻报错，exit 1。器材自测 15/15 全过。修复注入后复测亦全过。一次性器材已按纪律删除。
      **过程中踩坑**：变异测试的 `sed -i` 注入缺陷后忘记还原（`cp /tmp/gl.bak` 复原失败，WSL 的 `/tmp` 与 Windows 侧不通），复测时 6 项报错才意识到库已被损坏。**本轮因复测在提交前发现了自己的破坏**——若不复测就提交，损坏的 `gate-lib.sh` 会随下个 commit 入库，在此之后直到「有人发现门禁的 SKIP 全部变成放行」为止，所有部署都是假阴性。这反过来验证了「闸门」词条的核心判据——**正向跑通不构成证据，必须构造应当失败的输入来验证**——在这里的适用对象正是修改者本人。

- [x] **分支与 tag 规范落文档 + 回填 M3 checkpoint tag**（条目 [113]，2026-09-08）：承构想文档 S1「条目即分支 + 两类 tag 命名空间」。用户裁定本轮只做「规范文档 + 回填 tag」，**明确不加强制拦截**（不做 `branch-gate.sh`、不做 pre-commit 钩子），且规范另立专题文档而非并入 DevSecOps 方案。
      **范围**：新建 [分支与版本标记规范.md](file:///d:/developer/code/aicoding/s2s/docs/architecture/分支与版本标记规范.md)，含条目即分支工作流（`item/<条目号>-<短描述>` → 分支活着时允许脏 → 验收后 `--no-ff` 并回 → 删分支）、两类 tag 命名空间（`rel/<env>/<APP_VERSION>` 发版锚点 / `checkpoint/<里程碑>` 状态快照）、提交信息收口到 Conventional Commits、`Fixes: <hash>` 降级为建议项。实打 tag 1 个：`checkpoint/M3` → `e6e7d06`，附注 tag，message 写当时验证数（42 框 + 离线布局探针 256 项 + Token 探针 125 项全绿）。
      **`--no-ff` 是整个分支模型唯一的收益点**：它强制生成 merge 节点，于是 `git log --merges --oneline` 恰好就是条目清单，回退粒度从「最近一次提交」升为「一整个已验收条目」（`git revert -m 1 <merge-hash>`）。若用 fast-forward，条目边界直接消失、`git log` 退化成扁平串，与现状无异——也就是说这条规范的价值全押在一个可以随手省略且省了不报错的参数上，故 §6 如实标注它为 ⚪ 无强制。
      **`rel/*` tag 必须与 `APP_VERSION` 严格同值**（`deploy.sh:300` 的 `TIMESTAMP`）：部署架构 §9.4 方案 B 按镜像 tag 回滚，若 git tag 另编一套号，回滚时就要做一次「git tag ↔ 镜像 tag」映射查找，而这个映射没有任何地方存着，只存在当时那个人的记忆里。同值即免映射。
      **6 个已完成里程碑只打出 1 个 tag，这是核实结果而非遗漏。** 逐个查证：独立仓库首个提交 `2320ca8` 是 2026-08-25，晚于 M1/M2 完成日（08-10）15 天；`docs/PRD.md`/`BRD.md`/`MRD.md` 在 git 里的首现是 `b65542e`「纳入资产基线」——那是**纳入本仓库**的时刻，不是**交付**的时刻；M2.5/M2.6 的完成标志（ICP 备案已提交、软著已送中国版权保护中心）全是仓库外行政事实，`git log --grep=软著` 零命中；M3.1 的 `prototype-figma/manifest.json` 虽与其完成同一天入库但因果相反（入库是纳入既有资产，完成动作是用户在 Figma 桌面版跑批次 1→5 并逐条 MCP 验收，发生在仓库外）。
      **故 M1/M2/M2.5/M2.6/M3.1 一律不打 tag。** 强行打的后果是 `git show checkpoint/M2` 显示 2026-08-25、而说明文档 §1.3 写 M2 完成于 08-10，**tag 与真源矛盾**，下一个人会花时间搞清哪个对，而正确答案是「两个都对，只是 tag 打错了对象」。这是「判不了就不要报通过」（DevSecOps §3.2 / CONCEPTS「闸门」）在文档层的同形应用：「M2 在 git 里没有锚点」是准确事实，比一个语义错误的 tag 更有价值。规范 §3.5 把这个判断连同理由一起写下来，避免后人以为是漏打。
      **规范 §6 立了一张「强制状态清单」，9 条全部标 ⚪ 无强制，一个绿灯都没有。** 这张表是本文档最需要如实维护的部分——按 [verification-code-fails-silently-as-pass.md:167](file:///d:/developer/code/aicoding/s2s/docs/solutions/workflow-issues/verification-code-fails-silently-as-pass.md#L167)「失效的门禁比没有门禁更糟」，一份没有强制的规范至少还诚实：它不会让人误以为有东西在把关。真正危险的是写了 `branch-gate.sh` 却因某个 `|| true` 从不拦截。将来给某条加强制时，须同步把 ⚪ 改掉**并写明校验脚本路径与四要素判据**。
      **本条目自身没有按 §1.3 走**（仍在 `main` 上直接提交）：规范生效对象是它写完之后的下一个条目，此处如实记在规范 §7 而不是假装已切换。tag 亦未推远端（本地领先 `origin/main` 64 个提交），故 `checkpoint/M3` 目前仍是宿主机本地状态——与 §0.1 批评的「随宿主机重建消失的锚点」同类，规范 §4.1 已点明这一欠账。

- [x] **本轮方法论增量沉淀为第九章**（条目 [114]，2026-09-08）：把 [109]→[113] 五个条目的经验反写进 [AI工程化方法论.md](file:///d:/developer/code/aicoding/s2s/AI工程化方法论.md)，新立**第九章「门禁落地与版本拓扑」**（9.1 适用边界 → 9.2 工具矩阵 → 9.3 流程骨架 → 9.4~9.12 九条准则 → 9.13 反模式 17 行 → 9.14 落地检查清单 6 组 25 项），并在索引表登记、7.7 与 8.10 各补一句指向新章的交叉引用。
      **先做的是分拣而不是直接写。** 逐条比对现有八章后，四条增量被判定为**已覆盖、不重写**：「门禁失效的默认落点是通过」已是 8.10 主准则；反向检查结构 / 命令替换里的 `exit` / 黑名单正则三种机制 8.10 的表格全中；「正向跑通不构成证据」是 8.10 判定动作 5；「验证脚本自身可能假通过」是 8.9 衍生条。**不重复已有准则是本次编辑的第一约束**——方法论文档的价值密度取决于它没有冗余，同一条准则出现两次，读者会开始怀疑其余条目是否也在注水。
      **三条是对现有准则的精化，写在新章并回指原处**：① 8.10 只说了「跳过」要成为第三态，落地才发现三态不够，必须四态——SKIP 与 N/A 的分界线是**有没有可判定的对象**，若把「不适用」并入 SKIP，prod 环境每次跑必然 SKIP≥1、退出码恒为 2，规则第一天就会被 `|| true` 绕过（9.5）；② 7.7 的四要素（执行者、工具与命令、失败判定、留证方式）**缺第五项「消费方」**——四项只规定门禁自己怎么算，不规定有没有人读（9.6）；③ 退出码须分三档 + 放行只对 SKIP 生效、对 FAIL 无效、理由必须附带（9.7）。
      **三条是完全没有的新准则**：① **门禁有效性 = 判据严谨度 × 是否有调用方消费退出码，后者为零则乘积为零**——[109][110] 两轮修 7 处伪 PASS，但在 [111] 之前那 7 处修与不修对发版流程的实际影响完全相同（9.4）；② **外壳与判据分离，且外壳只能有一份实现**——L2/L3 各写一份时「SKIP 是否阻塞」的分歧没有任何检出手段，两边都跑得通、都打印绿色结论，差别只在没人盯的退出码（9.8）；③ **变异测试针对的是器材自身**，与 8.9 衍生条方向相反（后者防「验的是空配置」，前者防「用例根本没有检出能力」），并把「注入后忘记还原」作为 ⚠️ 附带坑写进去（9.9）。
      **「这一轮为什么久」被单列成准则 9.10，这是本次沉淀里最值钱的一条。** 用户的原话是「这一论貌似不是很顺利，持续了好久」——复盘后结论不是某一步做错了，而是**顺序把最便宜的那一步放在了最后**：接通「退出码 → 调用方」的工作量远小于逐条精修判据，但它决定精修是否产生任何效果。正确顺序是「先接链路（1 天）、再精修判据（3 天）」，本轮反着做，于是前两轮产出在第三轮之前收益为零，且**那段时间里我们自认为门禁已经在保护交付**。故 9.3 流程骨架把「接通链路」定为阶段 A 并注明「最便宜，必须最先做」，9.10 给出的判定动作是每轮结束时问一句「这一轮的产出，现在已经能改变交付流程的行为了吗」。这条可迁移到任何「先有规格、后建门禁」的场景。
      **S1 的两条一并写进（9.11 / 9.12）**：「判不了就不要打 tag」——强行给无锚点的里程碑打一个相近的提交，会把「缺数据」问题伪装成「数据冲突」问题，比没有锚点更糟；「规范的强制状态清单须如实标 ⚪」——文档天然会被读成已生效的约束，整表无强制时必须显式声明「这是范围的真实反映，不是笔误」。两条与门禁侧共享同一内核：**判不了就不要报通过，没强制就不要写得像已强制**。
      **全章已去除项目专有名词**：G2/G9/`gate-lib.sh`/`deploy.sh`/PIPESTATUS 一律不出现在准则位，只以「某项目连续两轮修复了 7 处伪合格项」这类识别信号形式留在举证段——这是文首阅读约定的硬要求，也是判断一条准则是否真的可迁移的检验手段：**写不出去项目名词的表述，说明它本来就只是一次项目经历**。

- [x] **API 契约守门 + 代码质量门禁的自动化测试框架/脚手架落地，CI 以 Trial 形态接通**（条目 [115]，2026-09-08）：任务是「通过 API 接口契约、质量门禁标准搭建自动化测试框架与脚手架，输出框架/脚手架/CI 集成的测试任务」。**产出三层九件**：
      **① 测试脚手架 `test/support/`（复用层，5 文件，全部带函数级中文注释）**：[openapi_loader.dart](file:///d:/developer/code/aicoding/s2s/test/support/openapi_loader.dart)（`package:yaml` 解析 openapi.yaml，`OpenApiSpec.load()` 加载失败直接抛异常而非 skip——契约真源缺失是 FAIL 不是「不知道」；`PathOperation` 提供 isWrite/parameters/responses/referencesParameter）；[api_envelope.dart](file:///d:/developer/code/aicoding/s2s/test/support/api_envelope.dart)（统一包构造 + 三个 matcher：`isSuccessEnvelope` / `isFailureEnvelope([code])` / `hasCodeAlignedWithHttp(http)`，对齐算术是 `code ~/ 100 == http`，即 42902→429）；[mock_api_server.dart](file:///d:/developer/code/aicoding/s2s/test/support/mock_api_server.dart)（`dart:io` HttpServer 内存 mock，路由键 `METHOD path`，未登记路由返 501，记录全部收到的请求供断言幂等头等）；[repo_paths.dart](file:///d:/developer/code/aicoding/s2s/test/support/repo_paths.dart)（仓库路径 + `assertRepoLayout()`，扫描面缺失抛 StateError）；[test_support.dart](file:///d:/developer/code/aicoding/s2s/test/support/test_support.dart)（barrel + `TestFixtures`：合法/非法幂等键、25 业务码全集、8 个 Retry-After 码、码→HTTP 映射）。
      **② 守门测试 `test/gates/`（2 文件 24 项，CI 必跑）**：[openapi_contract_gate_test.dart](file:///d:/developer/code/aicoding/s2s/test/gates/openapi_contract_gate_test.dart) 契约静态守门 **14 项**——契约元信息（3.0.x/双 server）、25 业务码（示例承载码 = 24 错误码 − 后台专用 40305，且独立断言 40305 在 Forbidden 描述中含「后台」字样；码前三位与 HTTP 对齐）、写接口幂等头、Retry-After（429/40105 必带、整数秒 minimum 1）、统一响应包（ApiError.data nullable）、响应键为合法 HTTP 码、date-time 格式；[quality_gate_test.dart](file:///d:/developer/code/aicoding/s2s/test/gates/quality_gate_test.dart) 代码面质量门禁 **10 项**——**G-Q1** 后门码 888888 登记册双向核对、**G-Q2** 密钥扫描（强判据 PEM 头/阿里云 AK 不可豁免 + 弱判据 32 位 hex 带密钥上下文三分支处置 + 豁免登记册双向核对 + .gitignore 兜底）+ 判据自检 4 组、**G-Q3** `deploy/scripts/*.sh` git 索引模式位必须 100755。
      **③ 契约示范与 CI**：[api_contract_example_test.dart](file:///d:/developer/code/aicoding/s2s/test/contract/api_contract_example_test.dart) 4 项示范（dio 对内存 mock 发真实 HTTP：成功包/42902+Retry-After/缺幂等键回 40001/JSON 冒烟）——**后端开工后只需把 baseUrl 从 mock 换成真服务，断言一行不改**；[.github/workflows/ci.yml](file:///d:/developer/code/aicoding/s2s/.github/workflows/ci.yml)（push/PR 触发，Flutter **3.41.9 版本锁定**，analyze → `test/gates/` → 全量 test）；`pubspec.yaml` dev 依赖加 `yaml: ^3.1.2`（解析 3.1.3，仅守门测试用）。
      **「后端零代码阶段契约测试怎么落地」是本轮的第一个设计决策。** 没有任何服务端工程（无 pom.xml），对空 URL 发请求没有意义。落成**契约静态守门（现在就可判）+ 内存 mock 服务示范（真实 HTTP 栈，断言面向未来）**双形态：前者把契约自身的纪律变成会失败的测试，后者固定「契约测试该怎么写」的范式。静态守门不是语法校验——它判的是**跨条目一致性**：错误码表宣称 25 个码，复用响应的 examples 是否真的承载了；契约宣称「码前三位对齐 HTTP」，每个码是否真对齐；写接口宣称必带幂等头，参数列表是否真的引用。这些一致性靠人肉 review 必漏，且漏了不报错。
      **豁免与占位的双机制：判不了不得放过，豁免不得静默。** 首轮 14 项守门有 14 个写接口被报缺幂等头，逐处核实后分三类：13 个是 Batch2/3 **占位接口**（responses 只有 `{description: 占位}` 未展开），按 `x-batch≠Batch1 且 200 无 content` 识别为占位、不参与判定但 `print [INFO]` 逐个列名；1 个 `POST /posts/precheck` 是 Batch1 已展开但契约明确「不写库」（纯校验无副作用），进**豁免登记表** `{'POST /posts/precheck': '不写库'}`，且测试反向断言契约描述里必须真含佐证词「不写库」——**防止有人把有副作用的接口误登记进豁免表**。密钥扫描同构：`.example` 模板的 `aaaa.../cccc...` 32 位全同字符判为占位假值（真实密钥为随机串，全同概率 1/16³¹）；prototype 下 4 处真实形态 key 进 `registeredSecretFindings` 豁免册，每条带 provider/reason/remediation，跑测试时 `[INFO]` 显式列名并打印「须到云控制台作废更换」。
      **门禁在本轮真实拦下两类问题（不是演示，是实测 FAIL 后修复）。** ① **G-Q3**：7 个部署脚本（含 `gate-check.sh`/`gate-lib.sh` 自己）git 索引模式位全是 100644——门禁脚本自己在 Linux 上除 `bash x.sh` 外无法被调用，条目 [111] 的 `run_gate_check` 靠 `bash` 前缀侥幸可用，但任何人按文档 `./gate-check.sh` 执行都会 permission denied。已用 `git update-index --chmod=+x` 逐个修复为 100755 并复核。② **G-Q2**：`prototype/` 早期 HTML/JS 原型中硬编码 2 把真实形态 key（高德 Web Key 1 把、火山方舟 sk- key 1 把，后者注释直写「用户提供的API密钥」，3 个文件复用）。**这 4 处在本轮之前已随 git 历史存在且无任何检出手段**。处置：key 已在历史中，删代码不等于撤销泄露，豁免册 remediation 如实写明唯一彻底处置是**到高德/火山控制台作废更换**（行政动作，待用户择期执行）；prototype 不随 App 编译、不进镜像，故登记豁免而非阻塞，但豁免留证、列名、带作废提醒——与 §4.1「人工逐处确认」规格同构。
      **判据自检（变异防护）4 组负向用例**：把弱判据抽成纯函数 `looksLikeEmbeddedSecret` / `isRepeatedCharPlaceholder` / `isRegisteredFinding`，单测证明它们对真实 key 形态必须 true、对 UUID/注释/无上下文噪声必须 false、跨文件同名 key 不得冒名豁免。这直接落实方法论 9.9：**正向跑通不构成证据**——本轮 G-Q2/G-Q3 若只拿当前仓库跑一遍 PASS，无法证明扫描器真能拦东西；而它们实际上首轮就拦到了真问题，这比任何构造的变异用例都更有说服力。另修三个 Dart 语法坑：`markSkipped` 不存在（flutter_test 顶级函数是 `markTestSkipped`）、Dart 正则不支持内联 `(?i)`（用 `caseSensitive: false`）、脚手架不得直接 import `package:test`（改由 flutter_test 转出 Matcher，消掉 analyze 的 depend_on_referenced_packages）。
      **CI 边界严格遵守 S6 裁决（条目 [108]）**：workflow 只装代码面判据，**刻意不跑** `deploy/scripts/gate-check.sh`——G1–G11 判的是运行中的环境（docker/域名/IAM/安全组），ubuntu runner 上这些对象不存在，装了必然全部 SKIP，制造「绿勾但内容为空」，正是方法论 9.4「失效的门禁默认落点是通过」。部署门禁的消费方仍是发版机 `deploy.sh` 步骤 4（条目 [111]），CI 与本地共用同一份 Dart 判据（`test/gates/`），不存在「两套实现悄悄漂移」。CI 当前是 **Trial**：仓库尚未推 GitHub（本地领先 origin 64+ 提交），workflow 要在首次推送后才真正运行——已在 DevSecOps §9 第 11 项状态中如实标注「推 GitHub 后实际生效」，不提前报「已生效」。
      **验证**：`flutter analyze` **0 issue**；`flutter test` 全量 **303 项全绿**（275 既有 + 本轮 28：契约守门 14 + 质量门禁 10 + 契约示范 4）；ci.yml 经 yaml 解析校验（1 job / 7 steps）。文档同步：本条 + DevSecOps §9 落地清单第 11 项由 ⚪ 改为已关闭（附边界与前置产出清单）。**遗留**：两把 key 的控制台作废是用户侧行政动作，豁免册 remediation 已长期挂账，作废后须替换占位符并删除对应登记条目（双向核对测试会逼出这一步）。

- [x] **《AI 工程化方法论》新增第十章「零实现阶段的规格守门与例外通道」**（条目 [116]，2026-09-09）：把条目 [115] 的门禁落地经验去项目名词化沉淀，承第九章（门禁落地与版本拓扑）补其前置场景。**八准则**：①静态守门判跨条目一致性（集合双向相等/码对齐/纪律引用/条款承载），不判语法；②零实现双轨（静态轨解析规格 + 回环真实协议栈行为轨），判据对象缺失是 FAIL 不是 SKIP；③例外三种合法形态（双信号合取的占位识别+列名、佐证词自证豁免、登记册双向核对），裸声明不合法；④判据按误报率分强弱，例外通道只配弱判据，弱判据三分支（概率特征排除→登记豁免→FAIL）；⑤判据自检常驻（弱判据抽纯函数 + 漏报/误报双向单测随 CI 每次跑），是对 9.9「变异实测一次」的升级；⑥CI 反向准入（对象不在 runner 的判据留发版环境）；⑦豁免登记四字段（定位/理由/删除条件/处置动作），凭据进历史即视为泄露；⑧判据技术前提当天查官方文档、判据代码静态分析零告警。配套 17 条反模式 + 26 项落地清单，文首章节索引已登记。自检：全章无项目专有名词（Flutter/Dart/高德/火山/具体文件名 0 命中）、交叉引用编号已逐一对齐、六段结构齐备。另同步：本轮 ce-compound 产物 [docs/solutions/workflow-issues/spec-to-failing-gate-tests-with-self-proving-exemptions.md] 与 CONCEPTS.md 新增「守门测试与例外通道」5 词条，为该章的问题级支撑材料。

- [x] **代码开发前交付《编码规范》v1.0**（条目 [117]，2026-09-09）：进入代码开发阶段前，把既有全部设计资产中的编码期纪律汇编为单一文档 [docs/architecture/编码规范.md]，作为前后端共同验收口径。**取材范围**：详设 §0–§21（含 §9 后端/§19 前端两份自检清单）、技术栈选型、系统安全、可观测性、DevSecOps、部署架构 G1–G11、数据库设计/DDL、OpenAPI 契约、PRD §12.5、`lib/nfr_constants.dart` 常量真源；每条规范标注依据源章节名（不引用行号，遵详设 §0.1）。**文档十章**：总则与依据源效力 / 命名注释与组织 / 常量错误码配置 / 后端工程（包结构、横切链、两 ID 分工、幂等、限频、加解密双列+AAD、数据访问、11 项定时任务、8 字段 JSON 日志、输入校验）/ 前端工程（目录依赖、DTO 四纪律、dio 五拦截器、单飞续期、全局唯一重试、Provider 竞态、三层存储与 grid_id、埋点四段计时与队列 11 条、外呼与 888888 三层后门）/ 数据与 SQL / 测试与质量门禁（必测断言清单、G-Q1~Q3 与契约守门 14 项、压测口径）/ 提交分支出包 / §21 待关闭缺口 / 附录。
      **三条编写原则**：① **只汇编不定案**——规范不新设计任何技术口径，与依据源冲突时以依据源为准并先回写规范；② **诚实标注空白**——设计文档确无明文的两个主题（SQL 注入防护条款、日志级别使用规则）不单独立规，收入 §9「通用工程基线（非项目定案）」表并显式标注出处状态，禁止把有争议决策伪装成惯例；③ **可机器校验优先**——详设两份自检清单与既有 303 项测试/G-Q1~Q3 守门收为 §6.2 必测断言，每条尽量给出可判定动作（如「静态扫描 `lib/features/` 无 for 重试」「解密点计数==1」「`gridIdOf` 文件无 `~/`/`.toInt()`」）。
      **自检发现并修复 12 处问题**：首稿 16 个文件链接路径误拼（`aidicoding`，PowerShell 批量替换并校验 0 残留）；交叉引用「见 §7.1」实际应为 §6.2；把技术栈 §6 的「Redis 显式 TTL/非唯一存储」错挂到幂等小节（已分属）；42907 漏出「不自动重试」枚举；§3.10/§8 列表跳号补「编号沿用原表」说明；「五类半」改为详设原文「五类行为（四分类+refreshToken 单列）」；规范中残留的「用户规则 §x」与说明文档条目号 [109]–[112] 等项目外交代物引用改为自包含表述或指向部署文档 §14.5；测试数 303 标注快照日期；§0.2 第三纪律出处由「详设 §0.1」更正为 §11.1.1（前两条才是 §0.1 原文）。未改任何代码；本文档之外无其他文件改动。

- [x] **《编码规范》v1.1：全文精炼 + 组件抽提与代码复用独立成章**（条目 [118]，2026-09-09）：用户评审 v1.0 后提出两条改进：「全文要精炼具体」与「对组件抽提、代码复用要有明确要求，不可出现冗余代码」。整篇重写，380 → 361 行：
      **① 新增 §1「组件抽提与代码复用（反冗余硬要求）」，把此前散落在各章的「唯一入口」条款系统化为三张表。** 总原则三条：同一逻辑（常量/正则/分支判断/序列化映射/键拼装/横切动作）出现第 2 次前必须提取；提取落点按复用半径分层（跨域下沉 common / lib core、域内私有不上浮、跨端同算法各一份但共用测试向量——grid_id 与错误码表这类跨语言镜像是**双端各有唯一实现处，不算冗余**，这条边界不说清就会被误当成"违反 DRY"）；禁止防御性复制（把 common 抄进业务域）。**唯一实现处清单**：后端 8 组件（ResponseBodyWrapper/GlobalExceptionHandler/ErrorCode/IdempotencyInterceptor/RateLimiter+RateLimitKeys/CryptoFacade/GridIdCalculator/AuditLogWriter）+ 前端 9 组件（五个拦截器分工/ApiErrorCode/gridIdOf/buildPinCacheKey/TrackReporter.buildBatch/NfrXxx），业务代码只准调用、不准内联重写。**反冗余验收口径 5 项机器可判**：lib/features/ 无 for 重试、lib/ 无 values.byName、gridIdOf 文件无 `~/`/`.toInt()`、后端三计数（解密点==1、业务代码写 Retry-After 零命中、controller 手写 ApiResponse 包装零命中）、评审见职责重叠或复制字面量直接打回。
      **② 全文精炼**：删去所有论证性文字（"为什么"留在依据源文档，规范只留规则+出处章节号）；每条规则压缩为一句话 + 可判定动作；依据源表由 11 行单列压为双列；§0.3 适用面标记说明并入正文不再单列；两 ID 分工由 6 行表格压为一段；定时任务与缺口列表改为沿用原表编号的紧凑清单。信息量未减——对照详设 §9/§19 两份自检清单逐项核对，无条款丢失。
      **结构变化**：章节由 10 章变 11 章（复用章插入为 §1，原命名注释降为 §2 并删去与 §1 重复的函数设计小节）；文首定位语压缩为三行；变更记录表新增 v1.1 行。自检：全文件链接复核无 `aidicoding` 残留；交叉引用（§0.3→§10、§4.11→§10、§6→§10、§3.2→§7.2）逐一核对与新章号一致。

- [x] **开工前工具链选型 + 方法论第十一章沉淀**（条目 [119]，2026-09-09）：编码启动前完成两件事并沉淀。**① skills/MCP 差距分析与选型**（会话内交付，未落盘文档）：盘点已装 MCP/skills 对照技术栈，缺口经 find-skills-mcp 跨 5 源（魔搭/官方 Registry/Smithery/mcp-get/npm）三批 7 关键词检索，输出四档清单——**必装 2**：`@modelcontextprotocol/server-sequential-thinking`（官方认证，补齐个人规则 §2.1 声明但未装的硬依赖）、Dart 官方 `dart mcp-server`（SDK 3.11.5 内置，优于第三方 mcp_flutter ★373）；**建议 2**：GitHub 官方 MCP（细粒度 PAT：内容读/PR 读写/issue 读写/Actions 读、90 天、单仓库，凭证纪律对齐 G-Q2）、Redis 官方归档 MCP（联调查幂等键/限频计数）；**不装 3 并记理由**：Docker MCP（低频可命令行直跑）、Spring Boot 专用 MCP（市场无成熟方案，Context7 覆盖）、OpenAPI lint MCP（与自研契约守门 14 项重复）。安装命令只输出未代执行。**② 方法论新增第十一章「编码启动前的规范基线与工具链盘点」**（[AI工程化方法论.md] 约 2962 行起，六段齐备）：七准则——规范只汇编不定案每条可回溯 / 每条规则带可判定动作机器可判优先 / 组件抽提显式化（唯一实现处清单+反冗余口径+跨语言镜像边界）/ 设计空白诚实标注不代设计立规 / 规则声明依赖必须核验在装 / 选型官方内置优先+3 源交叉验证+不装也留痕 / 凭证最小授权泄露处置唯一；反模式 13 条；落地清单 15 项。索引表已登记。去项目名词化校验：全章零命中（Flutter/Dart/高德/OpenAPI/Redis/Java 等 grep 仅第六章既有举例 1 处命中，非本次新增）。

### 2026-09-09 — Batch1 编码迭代计划（Sprint Backlog）与《完成定义（DoD）》交付（条目 [120]）

- [x] **[120] Batch1 迭代规划与任务拆解**：按「需求基线 → 用户故事列表 → 迭代节奏计划」路径交付编码期迭代基线，补齐此前缺失的 Sprint Backlog 与 DoD 两份资产。
- **交付物一（Sprint Backlog）**：写入本文 §2.8。需求基线六项真源冻结（PRD/契约/详设/编码规范/DDL/质量基线）；用户故事 19 个（US-A1–US-T1 业务 13 个 + US-S1–US-S6 系统 6 个，全覆盖 Batch1 的 20 业务接口 + `/track/events` + 定时任务 #1–#9）；迭代节奏四周 Sprint（S1 地基 9/9–9/11、S2 写路径 9/14–9/18、S3 读路径 9/21–9/24、S4 收尾 9/28–9/30，共 15 个工作日，中秋 9/25 连休已保守计入）；条目拆解 [121]–[131] 含依赖链与条目特有验收点。**原 §2.7「第 3 周（9/11）核心闭环跑通」检查点重定基线至 S2 末（9/18）**，触发不过仍执行二次裁剪。
- **交付物二（DoD）**：新建 `docs/architecture/完成定义DoD.md` v1.0——通用 11 项（D1–D11，含三全局纪律、反冗余五口径、必测断言对号、门禁、文档同步、分支规范）+ 分类附加（后端 B1–B5/前端 F1–F5/定时任务 J1–J3/出包 R1–R4）+ 证据格式（验收证据/门禁结论/遗留豁免三行）+ 豁免纪律（FAIL 不可豁免、豁免须带删除条件）。全文只引用《编码规范》章节号、不复制条文，遵守反冗余纪律。
- **条目编号调整**：本规划工作记为 [120]；原宣布的「[120] 后端工程初始化 + common 三组件」顺延为 **[121]**，编码条目至 [131] 出包交付止。
- **发现的依据源缺陷（挂账至 [121] 修复）**：详设 §2.3 单测要求文字「枚举总数为 23（含 OK）」与实际枚举 25 个不符（40305/42907 系条目 [99] 后补），唯一口径源 PRD §12.5 为 25，已列入 [121] 验收点回写。
- **并行行政项提示**：阿里云泄露两把 key 控制台作废 + 新密钥注入 `.env` 阻塞 [121] 配置填充；高德 Android/iOS Key 申请阻塞 [126] 真机联调——均请用户侧推进。
- 验收证据：openapi.yaml `x-batch: Batch1` grep → 20 业务接口 + trackEvents；详设 §6 定时任务表 → Batch1 为 #1–#9；日历推算 9/9–9/30 weekday 16 天 − 中秋 1 天 = 15 个工作日。
- 门禁结论：SKIP（纯规划文档，无可执行门禁扫描面）。
- 遗留与豁免：无。

### 2026-09-09 — Sprint Backlog 与 DoD 一致性审查（条目 [120] 复审，五项修正落地）

对照需求原始面（PRD §0.3/§2.7、详设 §1.4/§5.7/§9、编码规范 §9 缺口表、部署 §14.5）复审两份交付物，发现 4 项缺口 + 1 项触发条件过宽，全部当日修正：

- [x] **修正① 功能范围行补漏**：§2.8.1 范围行补「信息完整度三档」+「聚合性能 POC」（原漏列 §2.7 两项「必须有」）；实名认证显式标注**空壳占位**（前端表单 + 后端接收不入库，SDK 接 Batch2，对齐详设 §5.7 cert 空壳、接口映射 0）。
- [x] **修正② 新增 US-CERT1**：用户故事表补「用户提交实名信息（空壳占位）」挂 S2；backlog [123] 同步挂 cert 空壳内容 + 验收点「不接 SDK、不入库」。裁定：**空壳占位，不真接 SDK**（若真接将冲击 15 天排期，需重审）。
- [x] **修正③ 缺口 #3/#7 消费点显式化**：[125] 验收点补「表名沿用 post_media（缺口 #3 已裁定，禁改名）」；[129] 验收点补「uk_event_dedup 四列逐字冻结（缺口 #7 已关闭）」。至此编码规范 §9 五个开放缺口（#1/#2/#4/#5/#6）+ 两个已关闭缺口（#3/#7）全部在 backlog 有落点。
- [x] **修正④ 聚合 POC 拆为 S1 前置**：原挂 [126]（S3），现按 §2.7「前置 1 天」口径拆出并入 [121]（S1，产出书面结论，POC 不通过则渲染上限下调、SLA 改写——**该处置口径已作废，按 PRD:2652 / 条目[121] KTD-6 回写为：不通过亦不得下调目标、不改写 SLA、不下调 Pin 上限，只调整下一批次阶段目标**）；[126] 改为「按 [121] POC 结论校准阈值回写常量（缺口 #2，不猜值）」。裁定：**拆为 S1 前置**。
- [x] **修正⑤ DoD D7 触发条件收窄**：契约守门 14 项由「涉及接口变更时必跑」改为「修改 openapi.yaml 时必跑」，后端实现既有契约接口改由 B5 契约示范测试覆盖，避免后端条目空转。《完成定义DoD.md》升 v1.1。
- 一致性复核：接口计数（20+1）、定时任务 #1–#9、九域分层、缺口归属、错误码 25 口径五维全部对齐需求真源，无遗漏。
- 下一步：条目 [121] 启动（ce-plan 软件实施计划 → ce-worktree 建 `item/121-server-bootstrap-common` 分支 → ce-work 执行）。**前置依赖：阿里云新密钥注入 `.env`（行政项）**。

### 2026-09-09 — 阿里云 OSS 凭证注入 `.env.dev` + dev 打开真实 OSS（条目 [115] 挂账行政项落地）

- [x] **生成 `deploy/env/.env.dev`**：由 `.env.dev.example` 复制，已核验 git 忽略命中（`deploy/env/.env.dev`）且未被跟踪，**不入库**。MySQL/Redis/AEAD/HMAC/JWT/LLM 各项沿用 dev 模板占位弱口令（模板纪律允许 dev 弱口令、禁留空）。
- [x] **注入真实 OSS 凭证（用户侧提供）**：`OSS_ACCESS_KEY_ID`（`LTAI****`，len=24，格式校验通过）+ `OSS_ACCESS_KEY_SECRET`（掩码不回显）+ `OSS_BUCKET_NAME` 由占位改为 **`s2s-bucket`**。key 本体仅落 `.env.dev` 一处，未写入任何文档/进度/其他文件。
- [x] **dev 打开真实 OSS**：`application-dev.yml` 的 `s2s.oss.enabled` 由 `false` 改 `true`（用户裁定，dev 真测媒体直传到 s2s-bucket）；注释已更新注明凭证走 `.env.dev`、回退改 false 即可。yml 入 git 但只含非敏感开关，符合「配置进 git、密钥不进 git」边界。
- **风险登记（豁免，须带删除条件）**：该 OSS key 在注入前经对话明文传递，**等同已有暴露面**。用户明确裁定**保留并接受风险**，不换发。登记四字段——定位：`deploy/env/.env.dev` 第 45–46 行；理由：用户接受对话留存风险、暂不换发；删除条件：staging/prod 注入时将重新申请**独立**最小权限子账号 key，届时本 dev key 一并评估是否作废轮换；处置动作：本机 `.env.dev` 不得外发/共享/截图，本机磁盘建议加密。**与条目 [115] 挂账的高德/火山两把泄露 key 是两回事**——那两把仍待用户到**高德/火山控制台**（非阿里云）作废，本条不关闭该挂账。
- **[121] 密钥阻塞状态**：dev 层 OSS 凭证已就位 → **dev 维度阻塞解除**；但 [121] 配置填充还依赖 `AEAD_MASTER_KEYS_JSON`/`HMAC_PEPPERS_JSON`/`JWT_SECRET` 等真实随机密钥的生成（当前为占位串），该步在 [121] 实施时按「随机生成、不入对话」方式就地完成。
- 验收证据：`git check-ignore deploy/env/.env.dev` → 命中；`git ls-files` → 空（未跟踪）；掩码核对 `OSS_ACCESS_KEY_ID=LTAI****(len=24)`、`OSS_BUCKET_NAME=s2s-bucket`。
- 门禁结论：PASS（git 忽略兜底核验通过；key 未入库）。
- 遗留与豁免：见上「风险登记」豁免 1 条（dev OSS key 保留不换发，删除条件已锚定）。

### 2026-09-09 — ce-compound 沉淀：凭证注入/作废对象核对工作流学习

- [x] **沉淀工作流学习 1 篇**：[credential-revocation-target-mismatch-from-ledger-summary.md](file:///d:/developer/code/aicoding/s2s/docs/solutions/workflow-issues/credential-revocation-target-mismatch-from-ledger-summary.md)（workflow-issues，workflow_issue / documentation / missing_validation / high）。核心：注入对象（阿里云 OSS）与作废对象（高德/火山，控制台非阿里云）被挂账摘要混淆，险些作废错对象；纪律=凭证类不可逆操作前回检出原文核对「provider+泄露位置+控制台」三元组，注入与作废拆成两个独立动作。
- [x] **交叉引用双向闭合**：在 [review-findings-require-empirical-verification.md](file:///d:/developer/code/aicoding/s2s/docs/solutions/workflow-issues/review-findings-require-empirical-verification.md) 与 [destructive-script-parameter-audit.md](file:///d:/developer/code/aicoding/s2s/docs/solutions/workflow-issues/destructive-script-parameter-audit.md) 的 Related 各补一条反向链接。
- [x] **质量校验**：frontmatter 解析安全 OK；claims 机械校验 2 paths/3 links/0 flags。
- 词汇捕获：已扫描，无合格新术语（「豁免登记册」「缺口登记项」「可判定门禁」等相邻概念此前已收录）。
- 关键提醒：本篇再次确认「阿里云泄露两把 key」是**误记**——真正待作废的是高德/火山两把（prototype/ 硬编码，入 git 历史），仍待用户到**高德/火山控制台**行政作废（见 [115] 挂账）。

### 2026-09-09 — ce-compound-refresh 全量刷新 16 篇学习（首次体检）

- [x] **范围**：`docs/solutions/` 全量 16 篇（ui-bugs 1 + workflow-issues 15），3 个并行调查子代理按 module 聚类核查（deploy+test / docs / prototype-figma+ui）。无 patterns/、无 README、无 _archived/。
- [x] **分类**：**14 Keep + 3 Update**，无 Consolidate/Replace/Delete。
- [x] **Update ×3（均为「解法仍正确、引用/事实漂移」）**：
  - verification-code-fails-silently-as-pass：gate-check.sh 三锚点漂移（→L73-98/L193-201/L228-254）+ G2 快照更新为 gate-lib `skip()/pass()/fail()` 现行形态（保 :167 外部规范钉住行不动）。
  - destructive-script-parameter-audit：更正 Related 失实指控——`explain_pins_hot.sh` 纯读无 TRUNCATE，硬编码+TRUNCATE 仅 real。
  - multi-format-deliverable-consistency：「替换/替代」禁用词已于 2026-09-02 收窄落地（verify_prd_docx.py L110-118），更新计数（4 处）+ L1613→L2035 锚。
- [x] **CONCEPTS.md 词汇捕获**：补「守门型探针」「测量型探针」两词条（与「实机核验」正交：验证vs测量 / 离线vs真机两维度），闭合 ui-bugs、visual-feedback-loop 两篇 Related 的词条级断链；ui-bugs 篇 Related 引用同步补全。
- [x] **校验**：3 篇 Update 的 frontmatter=0、claims=0 flags。
- [x] **调查自纠 1 处**：子代理曾报 credential 篇 `.gitignore:62-63` 偏 1 行，复核实为准确（规则本体在 L62-63），**未改**。
- [x] **discoverability**：无 AGENTS.md/CLAUDE.md 等指令文件，跳过（无目标表面）。
- [x] **提交**：`ca42a8f`（选择性暂存 5 文件，工作区其他无关改动 lib/、test/、PRD.md 等未带入）。
- **遗留观察（report-only，不属本次处置）**：deploy/docs/部署架构设计文档.md:187 表格残留「expire_logs_days」表述与现行实现（my.cnf/gate-check.sh G10 用 binlog_expire_logs_seconds）矛盾，建议另行修复。→ **已于 2026-09-09 修复，见下条。**

### 2026-09-09 — 修复 binlog 配置矛盾（refresh 遗留观察闭环）

- [x] **修复 2 处 `expire_logs_days` 正向残留**（refresh 只点了 1 处，扫描补发现 1 处）：
  - [部署架构设计文档.md:187](file:///d:/developer/code/aicoding/s2s/deploy/docs/部署架构设计文档.md#L187)：磁盘预算表 binlog 行「必须设 `expire_logs_days`」→「必须设 `binlog_expire_logs_seconds = 259200`（MySQL 8.4 已移除 `expire_logs_days`，见 §4.2）」。
  - [可观测性架构方案.md:329](file:///d:/developer/code/aicoding/s2s/docs/architecture/可观测性架构方案.md#L329)：同型表格行，同步改为现行参数。
- [x] **复核**：全仓 `必须设 \`expire_logs_days\`` 正向表述零命中；其余 `expire_logs_days` 提及均为「已移除/反例」说明（my.cnf 注释、gate-check.sh G10 判据注释、§4.2、§14.5 G10 警示、review-findings 学习篇、AI工程化方法论），合法保留。
- [x] **一致性**：改后与现行真源统一——my.cnf L62 `binlog_expire_logs_seconds = 259200`、gate-check.sh G10 判据（L425-442）、编码规范 §130「禁用 expire_logs_days」、§14.5 G10「参数名不可写成 expire_logs_days」。
- 门禁结论：PASS（纯文档口径修正，无代码/配置改动，门禁判据对象未变）。
- 说明：本修复不影响任何已设门禁（G10 判据本就查 `binlog_expire_logs_seconds`，文档此前只是表格行滞后）。

### 2026-09-09 — 三泳道 solo 并行开发启动（纪律修订 + 三 worktree 就位）

- [x] **纪律修订（用户裁定）**：分支模型从「条目串行」放宽为「三泳道并行」。在 [分支与版本标记规范.md](file:///d:/developer/code/aicoding/s2s/docs/architecture/分支与版本标记规范.md) 新增 **§1.3a 三泳道并行（solo 模式）**：A 后端主线 `item/<n>-backend-<域>`（关键路径）/ B 前端骨架线 `item/<n>-frontend-<域>` / C 测试线 `item/<n>-test-<域>`；泳道内严格串行、禁同泳道并开；接线/跑测是同步点非并行；各自 `--no-ff` 并回。
- [x] **流程链固化**：每条分支严格按 `ce-plan → ce-worktree → ce-work ⇄ ce-debug → ce-simplify-code → ce-code-review → ce-commit → ce-commit-push-pr → ce-resolve-pr-feedback → 合并(--no-ff) → ce-compound + 更新说明文档` 推进。
- [x] **三 worktree 建立**（`git worktree`，从 main `d26c570` 检出，互不共享工作区）：
  - `s2s-wt-backend` → 分支 `item/121-backend-init`（Agent-1 后端主线，从 [121] 起跑）
  - `s2s-wt-frontend` → 分支 `item/123-frontend-core`（Agent-2 前端骨架线，lib/core）
  - `s2s-wt-test` → 分支 `item/121-test-gates`（Agent-3 测试线，[121] 测试骨架）
- 门禁结论：SKIP（环境准备，无可执行门禁扫描面）。
- 遗留：行政三项（高德 Key 申请 / 高德+火山泄露 key 作废 / `.env.dev` 随机密钥生成）仍待用户侧推进；`可观测性架构方案.md`（466 行）仍待审定入库。
- 下一步：Agent-1 于 wt-backend 对条目 [121] 启动 ce-plan（后端工程初始化 + common 三组件）。

### 2026-09-09 — 高德 Android Key 已申请（开发版），iOS Key 待申请

- [x] **release keystore 建立**：`$env:USERPROFILE\s2s-release.jks`（RSA 2048 / alias `s2s-release` / 有效期 10000 天），口令用户自管、不入 git/对话；`.jks` 文件同样不入库（用户目录，不在仓库内）。已取 release SHA1。此项顺带补齐编码规范 §8「release 正式签名」的前置（正式 signingConfig 接线属条目 [131] 出包环节，现不动）。
- [x] **高德 Android Key 已申请**：应用 `s2s_android` 下 Key「找鸭找-开发」（Android 平台），PackageName 绑 `com.s2s.zhaoyazhao.dev`（与 [build.gradle.kts:26](file:///d:/developer/code/aicoding/s2s/android/app/build.gradle.kts#L26) 的 applicationId 逐字一致，否则报 `INVALID_USER_SCODE`）。Key 本体不入本文档/对话/git，由用户存密码管理器。→ **「高德 Key 申请」行政项 Android 维度解除**。
- [ ] **iOS Key 待申请**：在同一应用 `s2s_android`（或新建应用）下再点「添加 Key」，服务平台选 **iOS平台**，填 Bundle ID（Xcode 工程 `PRODUCT_BUNDLE_IDENTIFIER`，建议与包名一致的反向域名）。**阻塞 [126] iOS 真机联调**，请用户侧推进。
- [ ] **PackageName 正式化挂账（沿用条目 [62]）**：`.dev` 后缀为开发期临时包名（正式包名依赖 P2 企业域名主体，未办理）；P2/P3 落地后改正式包名并**重申高德 Key**（替换点仅 build.gradle.kts 一处）。
- 门禁结论：SKIP（行政项，无可执行门禁扫描面）。

### 2026-09-09 — Batch1 设计基线入库（三泳道判据对象落地）+ gitignore 补 .pydeps

- [x] **背景与裁定**：三 worktree 从 main `d26c570`（仅已跟踪基线）检出，而契约/详设/编码规范等约 70 项已交付资产滞留 main 工作区未入库——泳道 C 契约守门缺判据对象必整组 FAIL，A/B 亦无设计基线可用。用户裁定「方案 A」：文档基线直接提交 main，三泳道共享同一判据对象。
- [x] **`dc548c9` 设计基线入库（36 文件，+15509 行）**：契约 `docs/api/openapi.yaml`、详设、架构 7 篇（含可观测性架构方案——4472 行条目遗留「仍待审定入库」就此闭合）、数据库设计 + DDL + 校验脚本、编码规范、DoD、分支规范（含 §1.3a 泳道）、PRD 改版、说明文档、`.trae/rules` 编码纪律、ideation 3 件、plans 2 件、solutions/workflow-issues 9 篇。
- [x] **`4f4753f` gitignore 增补** `.pydeps/`（脚本运行时就地安装的 opencv/numpy/Pillow 等第三方库，与 `.pylibs`/`.pptx_lib` 同类，不入库）。
- [x] **滞留资产泳道归属切分（用户确认，不随本次提交）**：`lib/` 20 项改动 + `lib/core` + `nfr_constants` → 泳道 B（[123]）；`test/support|gates|contract` + `.github` CI → 泳道 C（[121] 测试骨架承接）；`test/contract_mapping_test.dart` + 5 个既有测试改动 → 泳道 B（断言 lib 域代码、依赖 lib/core 才能编译）；`application-dev.yml`（OSS dev 直传开关）→ 泳道 A。
- 遗留（report-only）：`.metadata` 平台列表被改为 web + 未跟踪 `web/` 目录，疑似误执行 `flutter create --platforms web` 的副产物，待用户确认后丢弃。
- 门禁结论：SKIP（文档入库，无可执行门禁扫描面）。
- 下一步：泳道 C 于 s2s-wt-test 继续 [121] 测试骨架 ce-plan；各泳道分支按需吸收 main 基线（merge 或 rebase）。

### 2026-09-10 — [123] 前端 `lib/core/network` 骨架 ce-plan 定稿 + 交互式评审闭环（泳道 B）

- [x] **ce-plan 定稿**：`s2s-wt-frontend/docs/plans/2026-09-09-1034-feat-lib-core-network-skeleton-plan.md`（implementation-ready；R1–R16 + KTD1–KTD10 + U1→U2→U7→U3→U4→U5→U6 七单元；3 项 session-settled 决策：先合 main 基线提交再建 core、不预建占位、mock 先行）。
- [x] **ce-doc-review 交互轮闭环**：6 评审人 15 条原始发现 → 合成 14 条（feasibility+adversarial 同域合并 1 条并共识晋升 100）→ **4 项修正全部落地、0 推迟 0 跳过**：① Verification Contract 网络层必测断言适用列补 U2；② 续期失败口径对齐 R10 三分（流程图/状态图/U5 用例「4xx」→「40101/403xx 认证类」+ 补 429 分支）；③ KTD5 增 `readSessionEpoch` 第四回调 + 主仓 AuthSessionNotifier 补单调递增代次（U3 焊接点同步改四回调）；④ KTD8/U3 改「dart-define 默认值 + 运行时注入缝」，https 守卫从注入配置读取（修测试环境恒不可达缺陷）。10 条 FYI、4 项残余关切留报告不阻塞。
- [x] **GitHub Issue #1 已建**：`https://github.com/liujie12/s2s/issues/1`（fine-grained PAT 补 Issues: Read and write 权限后创建成功）。
- ⚠️ **残余关切（执行前核实）**：评审期间主仓工作区持续变动（test/gates、test/support 等改动一度消失又部分恢复），疑似 [121] 泳道 C 并行动作；U1（worktree 与主仓基线差消除）执行时先核对基线再动手。
- 下一步：ce-work 从 U1 起执行该计划。

### 2026-09-10 — [123] ce-work U1 基线同步完成（泳道 B）：主仓批次入库 + 吸收泳道 C + dio 钉版

- [x] **主仓泳道 B 批次 4 提交入库**：`fec75d4`（NFR 常量真源 + 过渡态 ApiException）→ `5ced651`（§10.4 语义迁移 20 项 + 5 配套测试 + 2 POC 适配）→ `2c2493a`（契约映射测试脚手架）→ `361ce65`（进度记录）。越界项未提交：`.metadata`/`web/` 副产物（4498 遗留，仍待用户确认丢弃）、`application-dev.yml` OSS 开关（4497 划归泳道 A）。
- [x] **环境漂移核实（闭合 4507 残余关切）**：评审期主仓变动 = 泳道 C 单源化动作——`test/support|gates|contract` + `.github` + 6+1 项依赖已随泳道 C 在 `item/121-test-gates` 入库（`341a3cf` 等 5 提交），main 工作区同名副本系其登记后删除（4506），非异常。
- [x] **worktree 基线重建**：`item/123-frontend-core` FF 吸收 main（至 `361ce65`），再 `de41d49` merge `item/121-test-gates`（唯一冲突 `说明文档.md`，双侧条目并集保留；泳道 C 此后 --no-ff 并回 main 时血缘自然合一，无二次冲突面）。R1 口径由此完整：B 批次经 main、C 资产（含 dio 等 6+1 依赖与 lock、MockApiServer、ci.yml）经泳道 C 分支。
- [x] **dio 钉版 `19c6242`**：`^5.9.0` → 精确 `5.11.1` 并注释缺陷区间依据（R2/KTD7），lock 解析值实测 == 5.11.1；实施计划同批入库（`docs(plan)`）。
- [x] **小插曲**：worktree 陈旧 `windows/flutter/ephemeral/.plugin_symlinks` 损坏致 `flutter pub get` 工具崩溃（Cannot create link, errno=2），清理 ephemeral + `.dart_tool` 后恢复，未改任何工程文件。
- [x] **U1 验证全绿（worktree）**：`flutter analyze` 0 issue；`flutter test test/gates/` 24/24；`flutter test test/contract/` 4/4；全量 `flutter test` **303/303 全绿**——与 4508 窗口期口径精确吻合（254 基线 + 泳道 C 28 + 泳道 B 21，基线 303 恢复）；ci.yml 三段（analyze → 守门 → 全量）确认齐全；`test/contract_mapping_test.dart` 旧断言口径跑绿（U2 迁移前基线）。
- 门禁结论：PASS（G-Q1/G-Q2/G-Q3 与契约守门 24 项在分支入库态实测通过）。
- 下一步：U2（NfrNetwork + ApiErrorCode + ApiException 迁移扩展）。

### 2026-09-10 — [123] ce-work U2 完成（泳道 B）：NfrNetwork 常量真源 + 错误模型迁移 core/network

- [x] **worker 交付（proof-first）**：先写测试观察红色证据（缺实现文件加载失败 +0 -2）再实现。新增 `lib/core/network/api_error_code.dart`（ApiErrorCode 27 值 = 24 服务端码 + ok + networkFailure(-1)/parseError(-2)；ErrBehavior 表驱动映射 §12.2 六类行为 + ok 专用 none，无特例分支；fromCode 未知码抛 parseError 且 message 含实际值）、`lib/core/network/api_exception.dart`（四字段 `{code, message, requestId?, retryAfterSec?}` 对齐 §11.3 unwrap 构造形状；parseError message 定长截断上限 200——依据源未给值，注释载明取值理由）、`test/core/` 26 项测试；`lib/nfr_constants.dart` 新增 NfrNetwork 7 常量（§14.1 逐字抄录 + 章节号）；详设 §14.1 回登「常量真源登记（2026-09-10）」。
- [x] **验收矛盾裁决（worker 触发停止条件，未自行猜测）**：「旧路径不存在 + ApiFailure 零命中」与「features/domain 零改动」表面互斥。编排方裁决选项 a——KTD1「零改动」指**调用表达式**（`ApiException.parse(...)` 不变），2 行 import 路径更新属 KTD9 迁移落点的机械部分；re-export shim 会留双份真源违反编码规范 §1.1 反冗余纪律。
- [x] **编排方 inline 收口**：`listing_category.dart`/`discovery_filter.dart` import 各改 1 行；删除旧 `lib/core/api_exception.dart`（-49 行）；`contract_mapping_test.dart` 断言迁移 `.failure/ApiFailure.parseError` → `.code/ApiErrorCode.parseError`。
- [x] **提交**：`7e6828c`（NfrNetwork 常量 + 详设 §14.1 登记，KTD6）、`bd4daa4`（ApiErrorCode 吞并 ApiFailure + 迁移 core/network，8 文件 +606/-55，KTD1/KTD9）。
- [x] **U2 验证全绿（worktree）**：`flutter analyze` 0 issue；`test/core/` 26 项全绿；全量 `flutter test` **329/329**（303 基线 + 26 新增，基线只增不减）；`ApiFailure` 代码面 grep 零命中（残余 `.failure` 命中均为 auth/contact 域自有枚举与泳道 C `ApiEnvelope.failure` 工厂，非本次对象，auth 条目时按兼容构造退役条件处置）。
- 门禁结论：PASS（契约守门对 25 码全集的双向相等断言由 `test/core/api_error_code_test.dart` 与 fixtures 双向相等用例承接）。
- 下一步：U7（反冗余静态扫描与 CI 接线）。

### 2026-09-10 — [123] ce-work U7 完成（泳道 B）：反冗余静态扫描守门上线

- [x] **worker 交付（proof-first）**：新增 `test/gates/anti_redundancy_gate_test.dart`（编码规范 §1.3 前端双判据 + 13 项用例）。判据 A（详设 §14.2）：`lib/features/` 循环体重试扫描——词法剥离后定位 for/while 循环体，命中网络调用形态即违规，保守不误报口径（do-while 已知漏报面显式声明并由变异用例兜底）；判据 B（详设 §10.3）：`lib/` 全树 `values.byName` 零命中。共用 `stripDartTrivia` 词法剥离（注释/字符串掏空、插值保留、换行保留行号对应）；判据对象缺失抛 StateError 按 FAIL（部署 §14.5 防线，非 SKIP）；变异自检双向（注入必 FAIL、删除恢复、合法文件不误报）。
- [x] **CI 消费链确认（答不出消费方视为未落地）**：守门步 `flutter test test/gates/` 消费整个目录——契约守门 14 + G-Q1/G-Q2/G-Q3 共 10 + 新增 13 = 37 项同一命令一次跑出；新判据无需新增 CI 步骤即被消费，FAIL 则 code-gate 作业红挡合并。ci.yml 仅注释对齐（行为零变化）。
- [x] **编排方独立复核后提交 `1e2897b`**（2 文件 +749/-3）。
- [x] **U7 验证全绿（worktree）**：`flutter test test/gates/` 37/37（G-Q1 后门登记册双向核对在 worktree 跑绿）；`flutter analyze` 0 issue；全量 `flutter test` **342/342**（329 基线 + 13 新增，只增不减）。
- 门禁结论：PASS（反冗余判据自此机器可判、失败可挡合并，随骨架同日上线）。
- 下一步：U3（api_client + Header/Envelope 拦截器）。

### 2026-09-10 — [123] ce-work U3 完成（泳道 B）：api_client 网络底座 + Header/Envelope 拦截器

- [x] **worker 交付（proof-first，红→绿证据完整）**：生产代码 5 新建——`api_client.dart`（唯一装配处：NetworkConfig KTD8 注入缝 + release/非https 构造/装配双守卫；NetworkHooks KTD5 四回调，core 不感知 Riverpod、默认 provider 故意抛错强制 features override；buildNetworkDio 链序按 §11.1、validateStatus 恒 true（KTD2）、超时只引 NfrNetwork；mapPinsOptions 仅覆盖读超时 3s）、`HeaderInterceptor`（Authorization 唯一每次重写未登录无键；三头 containsKey 才写；幂等键仅 POST/PATCH；隐私同意态硬门实时读）、`EnvelopeInterceptor`（先拆信封再判 code；data/request_id 为 null 合法；业务码 reject(err,true) 携带两字段；非 Map 按 HTTP 状态 5xx networkFailure/其余 parseError 分流；Retry-After 解析失败回退；extra 旁路；onError 裸穿透留 U6 归一 KTD10）、`DeviceIdProvider`（首启 UUID v4 惰性落盘，标注不可信可重置）、`auth_network_wiring.dart`（KTD5 features 侧焊接，既有业务零改动）；测试支撑 `network_chain_harness`（三拦截器位显式占位）+ mock_api_server 扩 raw/rawBytes/contentType（gzip 收包明确留 U4）。
- [x] **实现期两决策（worker 报告理由，编排方采纳）**：DeviceIdProvider 独立成文件（平台存储不污染纯装配、可独立单测）；焊接点新建 auth_network_wiring.dart 而非改 main.dart（本单元无 dio 消费方，焊接独立可测，业务接线时 override 即可）。
- [x] **编排方独立复核**：通读 api_client 与两拦截器全文，纪律一致（无字面量复制、无第二个 HTTP 客户端、无新依赖）；独立复跑新增 39 + gates 37 = 76 项全绿、analyze 0 issue。
- [x] **提交**：`7b28d0f`（core 底座+拦截器+测试支撑，11 文件 +1624/-3）、`ecd2220`（KTD5 焊接点 +6 测试）。
- [x] **U3 验证全绿（worktree 提交态）**：全量 `flutter test` **381/381**（342 基线 + 39 新增：header 11/envelope 10/api_client 8/device 4/wiring 6，基线只增不减）；反冗余守门对新代码零命中。
- 门禁结论：PASS。
- 下一步：U4（GzipInterceptor 与第一天双实测，结论须回写详设 §11.5/§11.1）。

### 2026-09-10 — [123] ce-work U4 完成（泳道 B）：GzipInterceptor + 第一天双实测结论回写

- [x] **实测一（§11.5 gzip 行为，dart:io HttpServer 真栈 + 真实 GZipCodec 实体压缩）**：不注入与手工注入两条件均观察到——服务端收到 `Accept-Encoding: gzip`（前者为底层自动协商）、`response.data` 均自动解压为 Map（payload 512）、解压后 `content-encoding: gzip`/`content-length` 压缩态头保留；「显式设置即需自行解压」风险**未复现**，手动注入零收益。裁定 **GzipInterceptor 不手动注入、开关默认关并固化**，编码规范 §5.3 实测项与详设缺口 #6 关闭。
- [x] **实测二（§11.1 拦截器序，承重前提）**：探针三方向机器断言 **onRequest/onResponse/onError 均按 Interceptors.add 添加序 FIFO**（非旧图注「返回/错误向自下而上」；dio_mixin.dart 源码无 reversed 佐证）。该出入被计划 Assumptions 第 2 条显式预见并授权「以实测为准、同批回写图注」，且承重前提（onError 添加序 → `reject(err,true)` 的 40101 能到第 4/5 位、Retry 链尾归一 KTD2/KTD10）被证实而非证伪，停止条件未触发。副作用：Gzip 先于 Envelope 统计，量到完整信封明文，与线上压缩字节同口径。
- [x] **交付物**：`GzipInterceptor`（只统计不压缩；content-length 缺失降级 null；extra 三键常量化；自身不打日志，onStats 载荷仅白名单元数据，R16 用例对产物全文断言无 token）；api_client 装配位 2 兑现；mock_api_server 扩 gzip/chunked 真栈能力；8 项实测用例。
- [x] **详设硬交付回写（提交 a534e60）**：§11.5 追加「实测结论（2026-09-10）」含两条件观察值表；§11.1 图返回向重排 + 图注订正块；§11.1.1 倒序表述订正；§19 勾选并新增执行序勾项；§21 风险 #6 标记关闭。
- [x] **编排方独立复核**：通读 8 项实测用例（探针不改变消息流向、三方向序可区分、非空断言）与拦截器全文，独立复跑 8 项全绿后才提交。
- [x] **提交**：`6248bc5`（实现+测试，5 文件 +812/-13）、`a534e60`（详设回写）。
- [x] **U4 验证全绿（提交态）**：test/core/network/ 41/41、gates 37/37、analyze 0 issue、全量 **389/389**（381 + 8，只增不减）。
- 门禁结论：PASS。
- 下一步：U5（AuthRefreshInterceptor 40101 单飞续期，R10 三分口径 + 三处依据源回写）。

### 2026-09-13 — [123] ce-work U6 完成（泳道 B）：RetryInterceptor 全链路唯一重试 —— 网络骨架七单元全部收口

- [x] **worker 交付（proof-first，16 用例先红后绿）**：新建 `retry_interceptor.dart` 链尾唯一重试点——行为表驱动，仅 `autoRetry` 真实集合（50001/50301/50302/50303/networkFailure(-1)）重试，全链路首发+2 共 3 请求；42901/42902/42907、40903（且请求体逐字不注入 version）、cancel 零重试零 sleep。
- [x] **结构选型（a 案）**：外层 onError 持唯一重试循环（sleep→fetch→catch 线性推进），内层 `dio.fetch` 重放打 `retry_inner_dispatch` 标记，错误再入本拦截器立即 reject 抛回外层 await——内层绝不 sleep/二次调度；extra `retry_attempt_count` 硬上界 2，持续失败恰 3 请求、嵌套栈深有界、零第 4 请求（场景 2/2b/8 机器证明）。
- [x] **KTD10 链尾唯一归一**：纯传输 DioException（超时/连接拒绝，非 cancel）统一包装 networkFailure；已是 ApiException 的原样取用不二次包装、不丢 requestId/retryAfterSec。Retry-After 整数秒优先且**不叠加抖动**；退避 1s/2s × ±20%（毫秒 floor，ratio 注入，边界 800ms/<1200ms 与 1.6s/<2.4s 用例固化）；等待/随机双缝生产默认、测试零真睡。
- [x] **两键保全**：重试不调用 uuid 工厂（构造刻意注入但不保存不赋值，作为对称计数对象）；重放走 dio.fetch 重走全链，X-Interaction-Id/Idempotency-Key 由 HeaderInterceptor containsKey 才写逐字沿用，POST 全程 uuid 仅 1 次、GET 显式透传 0 次。
- [x] **位 5 真实装配**：buildNetworkDio 加 retrySleeper/retryRandomRatio 两可选缝，五拦截器按 §11.1 定死顺序全部就位；api_client 占位注释删除。R16：本类无任何日志、不读头/体，诊断仅含 err.type.name 与 path；终局文案保留末次 message（502/超时可区分，新增两处 contains 断言锁死）。
- [x] **U5/U3 旧测试随真实链路同步（生产码零改动）**：refresh 瞬时失败经 U5 归一 networkFailure 后到链尾命中 autoRetry，实际连锁 = 业务 3 / refresh 3（重试预算全花在原业务请求，refresh 走独立 dio 不被 Retry 嵌套），U5 场景 7 两例与 Retry 场景 9 双方固化；envelope 502 用例补 3 请求断言；3s 读超时用例改墙钟 7–14s 双侧界（真实 IO 超时，fakeAsync 无法驱动）。
- [x] **U6 验证全绿（编排方复核提交态）**：analyze 0 issue；gates+contract 56/56；全量 **443/443**（427 + 16，只增不减）；lib/core/network 零 print/debugPrint；旧 `lib/core/api_exception.dart` 早已删除零残留。
- [x] **提交**：`e27d5f7`（7 文件 +1231/-39）。
- 门禁结论：PASS。
- 下一步：Definition of Done 收口（R1–R16 对照、Verification Contract 七项、五拦截器链终验），主仓登记 [123] 网络层整体完成。

### 2026-09-12 — [123] ce-work U5 完成（泳道 B）：AuthRefreshInterceptor 40101 单飞续期

- [x] **worker 交付（proof-first，红→绿）**：新建 `auth_refresh_interceptor.dart`——只拦截已拆信封的业务 40101；`_inFlight` 共享 Future 单飞（并发/在途新到的 40101 同挂一个结论，refresh 实际调用恒 1 次），leader 内全量 catch 把异常收敛为 RefreshVerdict 返回值（`future.ignore()`），杜绝共享失败 Future 的 unhandled exception；成功后各挂起请求**各自**经业务 `dio.fetch` 重放（重走全链，两键由 HeaderInterceptor「containsKey 才写」逐字保全、Authorization 由「每次重写」换新为唯一变化头）；extra `auth_refresh_retried` 防循环；不解析 JWT exp；不含任何重试/退避/计时（U6 位继续占位）。
- [x] **R10 失败三分流 + 代次/取消**：① refresh 自身 40101/403 段 → leader 清会话恰好 1 次（onSessionCleared）、writeToken 0 次，挂起请求保留**各自原始** 40101 不重放；② 429 段 → 不清会话、带 retryAfterSec 透传；③ 网络失败/5xx/parseError → 不清会话、透传（5xx 与传输错误归一 networkFailure(-1)，重试留 U6）；判定只用 ApiErrorCode 既有枚举与 `code ~/ 100`，不新增错误码。代次变更（readSessionEpoch 发起取样/写回前比对不一致）丢弃新 Token、不重放；cancel 裸穿透、follower 取消不影响共享续期。
- [x] **KTD3 独立续期 dio**：`buildRefreshDio` 仅挂 Header + Envelope（不挂 AuthRefresh/Retry/Gzip），validateStatus 恒 true；`buildDefaultRefreshTokenExecutor` 读旧 Token → 无 body POST（契约单 Token：旧 Token 在 Authorization 头）→ 代次校验 → writeToken，全程经 NetworkHooks 新增三回调（writeToken/onSessionCleared/readSessionEpoch），core 不感知 Riverpod。AuthSessionNotifier 最小补 updateToken（不复活已登出会话、身份字段保留）与单调会话代次；auth_network_wiring.dart 七回调焊接。
- [x] **测试（真实 HTTP MockApiServer，非假 Adapter）**：新建 `auth_refresh_interceptor_test.dart` 19 用例（单飞 2 + 两键逐字 + 仅 Authorization 变化 + 防循环 + 认证类 2 + 429 Retry-After + 网络失败 3[50001 信封/502 HTML/连接拒绝，含失败后单飞字段复位] + 未登录 + 取消 2 + 代次变更 + writeToken 闸门 + KTD3 装配 + R16 源码白名单 2）；wiring 测试 +4（共 10）。
- [x] **三处依据源回写（2026-09-12，描述性注记，契约零改动）**：详设 §13.1 分支④补 R10 三分流订正注记；openapi.yaml info.description 第 5 节（HTML 注释）与 /auth/token/refresh 端点 description 补三分流/独立 dio/无请求体；架构 §6.5 第 4 步补三分流定案与代次/取消竞态。
- [x] **U5 验证全绿（worktree 未提交态，本单元未执行任何 git 写操作，待编排者复核提交）**：`flutter analyze` 0 issue；新增 19 + wiring 10/10；gates 50/50（文档回写后复跑，含 14 项契约守门）；test/features/auth/ 10/10；全量 **427/427**（merge main 后基线 404 + 19 + 4，只增不减，无折叠漏跑）。
- 门禁结论：PASS（待编排者复核）。
- 偏差决策：任务书提及「双 Token」形态，以契约为准实测落地为**单 Token**（refresh 请求无 body、Authorization 携带旧 Bearer，服务端 received 历史已固化断言）；dio 5.11.1 新实例内置 ImplyContentTypeInterceptor，KTD3 装配断言按类型过滤后业务拦截器恰 2 个。
- 下一步：U6（RetryInterceptor 全链路唯一重试点，KTD10 传输错误归一）。

### 2026-09-12 — [123] ce-work U5 完成（泳道 B · 主仓登记）：AuthRefreshInterceptor 40101 单飞续期

- [x] **背景收口**：U4 收尾时分支吸收 main（含泳道 A 后端初始化 24 提交、泳道 C 收口），两文件冲突（说明文档双侧条目并集 + 清除 main 侧一个游离合并标记；mock_api_server.dart 取分支侧整块），merge 提交 `de102b4`，analyze 0、全量 **404/404**。
- [x] **交付**：新建 `AuthRefreshInterceptor`（共享 Future 单飞，5 并发 40101 续期仅 1 次；leader 全量 catch 把异常收敛为分流返回值，共享失败 future 无未捕获异常；`dio.fetch` 重走全链重放，两键由 HeaderInterceptor「containsKey 才写」逐字保全、Authorization 唯一换新；extra 标记防二次续期循环；cancel 裸穿透；不解析 JWT exp、无任何重试/退避，U6 位继续占位）。
- [x] **R10 三分流**：refresh 自身 40101/403 段 → leader 清会话恰好 1 次、挂起请求保留各自原始 40101 不重放；429 段 → 不清会话、带整数秒 Retry-After 透传；网络失败/5xx/parseError → 不清会话、归一 networkFailure 透传（重试留 U6）。会话代次（signIn/signOut 递增、换 Token 不增）发起取样/写回前比对，在途登出换号丢弃续期结果；AuthSessionNotifier 补 updateToken（拒绝复活已登出会话）。
- [x] **KTD3 独立栈**：`buildRefreshDio` 仅挂 Header+Envelope（不挂续期/重试/Gzip），契约实测为**单 Token 模型**——续期无请求体、旧 Token 在 Authorization 头（任务书原假设双 Token，以 openapi 契约为准并固化服务端断言）。NetworkHooks 扩 writeToken/onSessionCleared/readSessionEpoch 三回调，wiring 七回调焊接。
- [x] **三处依据源回写（描述性注记，契约零改动）**：详设 §13.1 分支④补三分流订正；openapi.yaml info 第 5 节 HTML 注释 + /auth/token/refresh 端点 description；架构 §6.5 第 4 步三分流定案表与代次/取消竞态。
- [x] **编排方独立复核**：通读拦截器全文（477 行）、api_client 装配、notifier/wiring diff、三处文档 diff；抽查测试断言为真实 HTTP 服务端 received 历史配对（非 mock 空转）；独立复跑 analyze 0、全量 **427/427**（404 + 19 拦截器 + 4 wiring，只增不减）、gates 50 不回归后提交。
- [x] **提交**：`4e119bc`（实现+测试，8 文件 +1954/-18）、`52fe1e7`（三处依据源 +73/-1）。
- 门禁结论：PASS。
- 下一步：U6（RetryInterceptor 全链路唯一重试点：dio.fetch 重走全链 + extra 防递归、NfrNetwork 退避 1s/2s±20%、仅 autoRetry 行为、Retry-After 优先、uuid 全链路 1 次，七场景；U5 已为其在链尾留位）。

### 2026-09-13 — [123] ce-work U6 完成 + 网络骨架 Definition of Done 收口（泳道 B · 主仓登记）：五拦截器全部就位，443/443

- [x] **U6 交付（链尾唯一重试点）**：新建 `RetryInterceptor`——行为表驱动，仅 autoRetry 真实集合（50001/50301/50302/50303/networkFailure(-1)）重试，全链路首发+2 共 3 请求；42901/42902/42907、40903（请求体逐字不带新 version）、cancel 零重试。结构选「外层循环 + 内层 fetch 标记终止」：内层错误再入本拦截器立即 reject 抛回外层 await，绝不 sleep/二次调度，持续失败恰 3 请求、无第 4 请求、嵌套栈深有界。KTD10 链尾唯一归一（纯传输 DioException→networkFailure，cancel 裸穿透）；Retry-After 整数秒优先不抖动，退避 1s/2s±20% 引 NfrNetwork；重试不调用 uuid 工厂，两键由 containsKey 逐字保全。api_client 位 5 真实装配（sleeper/randomRatio 两缝生产默认、测试零真睡）。提交 `e27d5f7`（7 文件 +1231/-39），测试 +16。
- [x] **真实连锁固化（U5 生产码零改动）**：位 5 真实化后，refresh 瞬时失败（U5 已归一 networkFailure）到链尾命中 autoRetry——重试预算全花在**原业务请求**上，连锁为业务 3 / refresh 3（refresh 走独立 dio 不经 Retry），U5 场景 7 两例、envelope 502 用例、Retry 场景 9 双方据实更新；3s map-pins 读超时用例改墙钟 7–14s 双侧界（真实 IO 超时唯一可观测证据）。
- [x] **Definition of Done 终验（编排方逐项核）**：① analyze 0 issue；② 全量 **443/443**（303 基线起只增不减：U2 +26、U7 +13、U3 +39、U4 +8、U5 +23、U6 +16，含 main 泳道资产吸收，计数逐项可对）；③ gates+contract 56/56（G-Q1/G-Q2/G-Q3、契约 14 项、反冗余 13 项全绿）；④ R1–R16 全满足，五拦截器按 Header→Gzip→Envelope→AuthRefresh→Retry 定死顺序挂载；⑤ 旧 `lib/core/api_exception.dart` 零残留、ApiFailure 零命中、lib/features/ 无 for 重试、lib/ 无 values.byName；⑥ lib/core/network 零 print/LogInterceptor；⑦ 依据源回写齐：详设 §11.1/§11.1.1/§11.5（U4 实测订正）、§13.1（U5 三分流）、openapi.yaml、架构 §6.5；⑧ dio 钉版 5.11.1。
- [x] **分支状态**：`item/123-frontend-core` U1→U2→U7→U3→U4→U5→U6 七单元全部完成；分支说明文档已记 U6 条目（`48b9337`）。后续：条目 [123] 走 ce-code-review → --no-ff 合并回 main（合并后契约示范测试 baseUrl 切真服务断言一行不改的约定继续有效）。
- 门禁结论：PASS（网络骨架 DoD 全项通过）。
- 下一步：[123] 后半（features 域页面接线：auth 登录/发布等消费 dioProvider；Token 持久化介质安全评审；888888 后门 kDebugMode 包裹随 auth 接线处置——三项均为计划内 Deferred，不在骨架范围）。

### 2026-09-10 — [121] U-6 聚合性能客户端 POC 复跑完成（泳道 A · 主仓登记）

- [x] **POC-A 复跑复现 4.73ms 基线（条目 [63]）**：5 万点均匀 4.73→5.51ms（+16.5%，VM 抖动内）、聚集 3.61→3.45ms，簇数 980/131 逐位一致，两档线性 PASS；POC-B 开发机 CPU 基准 5 万点重绘 171.6ms/每点 3.43µs，未见超线性回潮（对照 [65] 修复后 2.54µs 同量级，修复前 49.68µs）。书面结论含阈值建议区间 10–30 m/px（推导：客户端算力非约束，密度临界反推）与三句边界声明（不以模拟器/开发机数据宣布达标；真机校准 Defer 至 [126]，OQ-4/KTD-6；POC-B 为 CPU 部分基准不含 GPU/光栅化），见 [docs/poc/2026-09-10-cluster-poc-conclusion.md](./docs/poc/2026-09-10-cluster-poc-conclusion.md)，运行日志同目录两份 .log 留档；`lib/nfr_constants.dart` 的 `clusterModeSwitchMetersPerPixel` 注释已补证据引用（**值 30 不改**，终值校准权属 [126]）。

### 2026-09-10 — [121] 后端工程初始化完成（泳道 A · s2s-wt-backend，U-1~U-5 实施 + U-7 文档回写）

- [x] **范围**：后端工程初始化 + common 三组件 + Flyway 双库迁移 + 配置外部化 + 聚合性能 POC 复跑（U-6 见上条）；实施依据 `docs/plans/2026-09-10-1038-feat-item121-backend-bootstrap-plan.md`（条目[121] 回写，依据同计划（s2s-wt-backend））。
- [x] **工作树分支 `item/121-backend-init` 六笔提交**：`969af6f`（实施计划，含 ce-doc-review 裁定落盘）→ `9a503e6`（U-1 Maven 骨架：Java 21 + Spring Boot 3.5.16 + MyBatis-Plus 3.5.17 显式钉版）→ `5c76830`（U-2 九域 17 包 package-info 骨架）→ `910897b`（U-3 common 三组件 ApiResponse/ResponseBodyWrapper/GlobalExceptionHandler + ErrorCode 25 枚举，proof-first 10 测试）→ `464f811`（U-4 Flyway 双库迁移 V1+V2 `restart_window` + initdb.d 改写为 `01-init-databases.sh`）→ `a89b1c0`（U-5 配置外部化：六类凭证硬占位 + SecretsProperties requireResolved 守卫快速失败 + `.env.example` 双向核对零差异 + deploy 三份模板 JPA 残留清理）。测试 **16/16 全绿**。
- [x] **关键实测结论**：MySQL 8.4.11 隔离容器空库实测——s2s 库 **17 表**（15 业务 + `restart_window` + `flyway_schema_history`）、s2s_track 库 **3 表**；DDL 副本 SHA-256 与 `docs/database/ddl/` 源一致；二次启动幂等；U-5 负向实测 N1–N4/P1 全过；POC-A 复跑复现 4.73ms 基线（U-6，见上条）。
- [x] **证据文件**：`C:\Users\刘杰\AppData\Local\Temp\u4-evidence.txt`、`C:\Users\刘杰\AppData\Local\Temp\u5-evidence.txt`；POC 书面结论与两份运行日志在 `docs/poc/`（`2026-09-10-cluster-poc-conclusion.md` + 两份 .log）。
- [x] **initdb.d 口径变更（HTD-4，原口径作废）**：原「MySQL 容器首启执行 `ddl/*.sql` 建表」口径作废。新链路 = `deploy/initdb.d/01-init-databases.sh` 幂等建库授权（`CREATE DATABASE IF NOT EXISTS` + `GRANT`，**不 CREATE USER、不建表**）→ 应用启动由 Flyway 迁移建表；`docs/database/ddl/` 仅存档 V1 快照，**演进真源为 migration 目录**（`src/main/resources/db/migration{,_track}/`，KTD-5）。
- [x] **U-7 文档回写（与本条同步落地，仅动失实行）**：详设 §1.2/§2.3 错误码枚举计数 24/23 → **25**（24 业务码 + `OK(0)`，`OK(0)` 豁免 `code/100==httpStatus`，与工作树 ErrorCode.java 实测 25 枚逐个数过一致）；§2.7 与 §2.8.4 [121] 行 POC 处置口径回写为 PRD:2652（不通过亦不得下调目标、不改写 SLA、不下调 Pin 上限，只调整下一批次阶段目标，KTD-6），历史「修正④」记录行保留原文并标注口径作废；§2.8.1 数据基线行回写为 Flyway 现实（见该节）。
- 门禁结论：PASS（工作树测试 16/16 全绿；本单元仅编辑两个目标文档，未触代码与配置）。
- 遗留提示：① Flyway 11.7.2 对 MySQL 8.4 打「未测试」WARN，建议后续升级跟进；② dev 库重建按 HTD-4 执行 `docker compose down -v`（既有数据可弃，D7 裁定）。
- 下一步：编排者核验后统一提交主仓（本单元未执行任何 git commit）；[122] 横切链四件于泳道 A 继续。

### 2026-09-10 — [121] 测试骨架实施完成（泳道 C ce-work U1–U4）+ 同步点协议登记

- [x] **泳道 C 资产入库（分支 `item/121-test-gates`，三提交）**：`8a0d562` 实施计划（KTD1–KTD6）；`341a3cf` 资产 11 文件（`test/support/` 5 + `test/gates/` 2 + `test/contract/` 1 + `.github/workflows/ci.yml` + `pubspec.yaml/lock`，自 main 工作区逐字搬移、SHA-256 比对 10/10 零差异，`quality_gate_test.dart` 按 KTD6 拼接化 7 处样例字面量为例外）；`b591729` 契约示范 `S2S_API_BASE_URL` 环境开关。归 B/A 的部分不随本分支：`lib/` 与 5 个既有测试改动、`contract_mapping_test.dart` → 泳道 B；`application-dev.yml` → 泳道 A（切分口径见上条 4497 行）。
- [x] **main 基线两修补（先于 FF 吸收）**：`53db396` 学习沉淀 PEM 字面量引述拆写（消除 G-Q2 强判据自扫描命中）；`f42fe16` `deploy/scripts/` 7 脚本模式位补 100755（条目 [108] 实测缺陷，G-Q3）。泳道 C `git merge main` 为 Fast-forward，KTD1 前提成立。
- [x] **全绿验证（泳道 C worktree）**：`flutter analyze` 0 issue；`flutter test test/gates/` 24/24（入库态 G-Q2 自扫描零自命中——KTD6 生效证据，G-Q3 索引模式位生效）；`flutter test test/contract/` 4/4；全量 `flutter test` **实测 282 全绿**。单源化：main 工作区 9 个同名未跟踪副本已删、空目录已清、`pubspec.yaml/lock` 已 `git restore`，main 对照跑测（28/28）与分支结论一致后才执行删除。
- [x] **同步点协议（泳道间硬约定）**：契约测试跑真服务是**同步点非并行**——须等泳道 A 接口就绪；届时仅以 `--dart-define=S2S_API_BASE_URL=<真服务基址>` 指向真服务，**断言零改动**（开关已验证：无 define 与空串 define 均 4 绿，一次性本地探针观察到 4 路请求到达证明可离 mock，探针脚本未入库）。
- [x] **测试数窗口期口径登记**：main 基线 303 项（含泳道 C 滞留 28 项 + 泳道 B 滞留 21 项）；单源化后窗口期 main 工作区实测预期 **275 项**（303−28，含泳道 B 21 项）；泳道 C 分支实测 **282 项**（254 基线 + 28 新入库）；分支合并回 main 后恢复 **303 项**，基线只增不减。
- [x] **CI Trial 状态**：`.github/workflows/ci.yml` 已入泳道 C 分支（静态分析 → gates → 全量三段）。**启用条件**：分支合并回 main 后首跑观察即 Trial；Trial 期结论只做提示不阻断，连续 3 次全绿后转正式门禁。
- 门禁结论：PASS（泳道 C 全量 282 全绿；main 侧扫描面无变化，两修补提交后 G-Q2/G-Q3 在分支入库态实测通过）。
- 下一步：本分支按流程链进入 ce-code-review → 合并（--no-ff）回 main；泳道 A/B 继续各自条目。

### 2026-09-10 — [121] 测试骨架三轮 ce-code-review 收敛 + --no-ff 合并回 main（泳道 C 收口）

- [x] **三轮独立评审，累计修复 32 项判据缺陷**：
  - 落地评审 15 项（提交 `009b5ef`）：G-Q2 密钥扫描加固（逐命中/逐字面量三分支 `classifySecretHex`、拆写通道同行坍缩+跨行拼接、已知盲区负向固化）、契约守门与 openapi.yaml 绑定、断言分层、MockApiServer 诚实性。
  - 第二轮 11 项（提交 `13a70fe`，6 文件 +496/-141）：fail-closed 扫描面（不可读/非 UTF-8/含 NUL 行/git 已跟踪但工作区缺失文件一律 FAIL，不经 WARN 转绿）、`$ref` 不可解析即红（support 层 `resolveResponse` 链式解析深度上限 8，外部/错前缀/缺键/超深 → unresolvable → FAIL）、`inspected>0`/`candidates>0` 防空扫描面假绿、MockApiServer 增 501/500 非信封用例、solutions 文档 7 处行号改符号引用、isCi 8 例自检。
  - 第三轮 6 项（提交 `2e725ad`，3 文件 +103/-39，经 2 个独立子代理交叉验证）：① **major** 两模式共用契约断言原打 `GET /categories` 不存在于契约（38 路径中仅 `/categories/tree`，`security: []` 免登录），改打真实端点，保「切真服务断言一行不改」承诺；② 密钥跨行拼接补「行尾 `//` 注释夹隔」第三类盲区并负向固化；③ G-Q2 排除规则根级锚定（`contains('/build/')` → `p=='build'||p.startsWith('build/')`）防任意层级 build 连坐；④ git 已跟踪但工作区缺失文件纳入 scanGaps；⑤ G-Q1 改计数剥除法（`collectBackdoorLines`+`uncoveredInLine`，登记位抵扣一次、同行追加不连坐）+ 变异自检；⑥ `isPlaceholder` 放宽为任意 2xx（200/201/204）带 content，防写接口占位 fail-open。
- [x] **裁决记录（两项）**：(a) 信封形态断言两模式共用、必须打契约内真实只读端点（即 `/categories/tree`）；(b) `code=0` 仅 2xx 合法，3xx+ 出现 0 即记 bad。
- [x] **合并策略**：main 已前进至 [123]（HEAD `1f4263c`，含反冗余守门 `anti_redundancy_gate_test.dart` 与 `lib/core/network` 大量新代码），merge-base `f42fe16`；文件级唯一重叠为本文件（两侧并行日志，取并集）。`git merge --no-ff item/121-test-gates --no-commit` 实测代码文件零冲突。
- [x] **合并态复验实证（G-Q2 fail-closed 当场拦下自身缺陷）**：首次 main 合并态跑 G-Q2 即 FAIL——收口文档两处转抄了 prototype 已登记高德 Key（早期原型地图联调 Key，指纹 `9c4af3b3…69d3`，作废责任在条目 [115] 与 G-Q2 登记册，非本次新增泄露）完整明文到未豁免文件；守门读工作区、命中即拒绝，删除明文后转绿。该案例实证合并态 G-Q2 与 main 全量文件共存时的真实检出力。
- [x] **历史纠正**：曾误将该 prototype Key 当作「[121] 测试探针新 Key」拟另行挂账，经 `git log -S` 与登记册核对确认为同一把已登记旧 Key，已撤销重复挂账，作废仍归口条目 [115]。
- [x] **全绿验证（泳道 C worktree 提交态）**：`flutter analyze` 0 issue；`test/gates/` + `test/contract/` 43 项全绿（含 CI=true 复跑）；全量 `flutter test` **297/297**（泳道侧口径，含其历史吸收的 lib 资产）。
- [x] **合并结果态 main 复验（合并提交 `bbf9acb`，真实计数）**：① `flutter analyze` **0 issue**；② `CI=true flutter test test/gates/ test/contract/` **43/43 全绿**（含 G-Q2 当场 FAIL→修复→转绿，见上两条实证）；③ 全量 `flutter test` **318/318 全绿**。基线核对诚实说明：main 合并前真实基线 **275 项**（17 个测试文件，无 `lib/core/network`、无 `test/core`），[121] 净增 9 个测试文件（gates 2 + contract 2 + support 5，+2073 行）带来 43 项，275+43=318 精确吻合、零丢失。**「381」是 `item/123-frontend-core` worktree 计数**——[123] U1–U3 生产码/`test/core`/反冗余守门仍在该分支（领先 main 12 提交），`1f4263c` 仅为记录其完成的纯文档提交，非 main 基线；待 [123] 合入后基线再叠加，届时同样只增不减。
- 门禁结论：PASS（G-Q1/G-Q2/G-Q3 + 契约守门 14 项在合并结果态 main 实测全绿；反冗余守门随 [123] 分支，本次不在 main 扫描面）。
- 下一步：ce-compound 学习沉淀；泳道 C（`item/121-test-gates`）关闭；[123] 分支按自身节奏合并回 main。

### 2026-09-12 — [121] 后端主线 ce-code-review 收尾 + --no-ff 合并回 main（泳道 A 收口）

- [x] **后端工程初始化 + common 三组件落地，ce-code-review 收尾：1 条 P1 已修、5 条 P2 登记待后续条目**（条目 [121]，分支 `item/121-backend-init`，工作树 `s2s-wt-backend`）：按详设 §1–§4 与编码规范 §4 完成 Batch1 后端底座，**U-1..U-7 全部 met**：① Maven 单模块（Java 21 / Spring Boot 3.5.16，依赖显式版本由 parent 托管）；② `com.s2s.server` 根包 + auth/category/post/map/contact/notify/cert/ai/track/task 九域垂直包骨架；③ common 三组件——`ApiResponse` record + `ResponseBodyWrapper`（唯一套壳/request_id 注入）、`GlobalExceptionHandler`（唯一异常→错误码映射、唯一写 Retry-After 头位置）、`ErrorCode` 25 枚举（code/100==httpStatus、8 个 needRetryAfter）；④ Flyway 双库迁移（业务库 V1 15 表 + V2 restart_window、埋点库 V1），副本与主仓 DDL SHA-256 实测一致；⑤ 配置全占位、缺变量启动快速失败（六类硬凭证 SecretsProperties 守卫 + 负向实测留证）；⑥ 聚合性能客户端 POC 书面结论（真机 P95 留 [126]，OQ-4）；⑦ 文档回写（主仓单独提交 `75cbc87`，不进泳道分支）。**测试：`mvn test` 17/17 全绿**（ApplicationYamlTest 2 / SecretsPropertiesTest 5 / ErrorCodeTest 4 / GlobalExceptionHandlerTest 2 / ResponseBodyWrapperTest 4）。
      **ce-code-review（mode:agent，run_id `20260910-070830-item121`，base `d26c5701` → head `6e9636f`，46 文件）结论 = Ready with fixes**：9 评审员（correctness/testing/maintainability/security/reliability/api-contract/data-migration/adversarial/learnings），validator 前台批 9 条候选 8 条成立、#5 字节码级核实后驳回丢弃（Boot 3.5 未匹配路由经 `NoResourceFoundException` 实走 50001 信封，「404 无信封」前提不成立）；#6/#9 按软桶规则降 testing_gaps。需求完整性 R-1..R-8、U-1..U-7 全 met。
      **P1 已修（用户裁定「仅修 P1，P2 登记」+「降为可选」，本条目内闭环）**：finding #1——compose 部署路径（env_file 仅引 `deploy/env/.env.*`，根 `.env.example` 不被 compose 引用）不注入 `SMS_KEY`/`AMAP_KEY`，而 SecretsProperties 对八凭证逐一硬守卫，**按文档化模板 `docker compose up` 整栈 app 容器必崩**；实现违背计划 OQ-3（短信凭证登记缺口供 [123]、本条目不接通）。修法：两键移除 `@NotBlank` 与 requireResolved 硬守卫，紧凑构造器经 `normalizeOptional()` 把 null/空白/`${...}` 整体占位三态归一为 null（DB/Redis/HMAC/AEAD/OSS 六项硬依赖守卫不动），新增测试场景五锁住「缺键/占位/空白→null、真实值原样保留」，ApplicationYamlTest 八占位断言保留仅补注释。修后 17/17 全绿。yml 层 `${SMS_KEY}`/`${AMAP_KEY}` 占位与根 .env.example 两键（供本机直跑）保留；[123]/[126] 落地真实通道时重新收紧为硬守卫。
      **5 条 P2 登记（用户裁定本条目不动代码，按 triage 队列建议承接条目；apply-queue 顺序 #4 → #8 → #7/#3，#4 决定 #3 用例形态）**：
      - **#2（P2/100，gated_auto，建议随最近后端条目顺手修）**：[ErrorCode.java:17](file:///D:/developer/code/aicoding/s2s-wt-backend/src/main/java/com/s2s/server/common/error/ErrorCode.java#L17) `OK(0,200,"success",false)` 的 message 与契约真源逐字不一致——详设 §2.3 与 openapi.yaml 成功示例均为 `"ok"`；每次成功响应都经 wrapper 外发 `"success"`，契约守门/前端快照会失配。修法：改 `"ok"` + ErrorCodeTest 增 25 条 message 逐字断言（现有 4 断言不含 message 是漏网原因）。
      - **#4（P2/75，manual，建议随条目 [124] 横切链接入时修，队列首位）**：`BizException.of(ErrorCode)` 对 8 个 needRetryAfter 码（40105、42901–42907）无拒收，业务代码可写出无秒数的 429，运行期只剩一条 WARN、启动/测试/契约文本全绿（green-while-red，静默缺强制 Retry-After 头，违反编码规范 §3.2）。修法：`of()` 对 needRetryAfter 码抛 IllegalArgumentException（强制走 ofRetryAfter 带秒数），ofRetryAfter 对称拒收 false 码；与 SecretsProperties「让错误不可表示」同范式。
      - **#8（P2/75，gated_auto，建议随 [124] 修）**：[ResponseBodyWrapper.java:33](file:///D:/developer/code/aicoding/s2s-wt-backend/src/main/java/com/s2s/server/common/web/ResponseBodyWrapper.java#L33) `@RestControllerAdvice` 无 basePackages——validator 经 Spring 6.2.19 字节码证实，过滤器链异常等 ERROR 派发经 BasicErrorController 的 Map 响应体会被无选择子 advice 套壳成 `{code:0,...}` 成功信封（HTTP 状态保留但业务码说谎，违反详设 §2.1「失败 code 非 0、data 恒 null」），wrapper 是后续全部接口唯一套壳处会被继承。修法：`@RestControllerAdvice(basePackages = "com.s2s.server")` + 补容器错误/404 不出现 code=0 的集成断言；/error 统一信封映射（ErrorController）按 KTD-10 仍归 [124] 设计。
      - **#7（P2/75，gated_auto，测试伞，建议随 [124] 或 common 下次改动补）**：`supports()` 是全站套壳总闸门，现有唯一断言只覆盖「已是 ApiResponse→false」负向，取反逻辑被改错测试仍绿。修法：补 supports(普通 DTO 端点)==true 正向用例配对。
      - **#3（P2/75，gated_auto，测试伞，依赖 #4 形态）**：handler 无秒数分支（17 码高频路径：40001/40301/40903）与「needRetryAfter=true 缺秒数 WARN 绊线」均无测试，误写/删守卫无回归网。修法：补两例（of(PARAM_INVALID) 无头正常返回；true 码缺秒数路径按 #4 落地形态断言 WARN 或构造期异常）。
      **另登记**：testing_gaps 11 条（含降级 #6 request_id 回写分支、#9 二次套壳运行时兜底、无 MockMvc 全链路、缺 compose 正向 health 守门即 #1 漏网直接证据、契约示范测试 baseUrl 切真服务落点待确认）；anchor-50 抑制 5 条（initdb 中途失败封存卷级联→建议尾部自检+down -v 口径登记；requireResolved 复合占位放行；ofRetryAfter 负秒数；V1 缺 KTD-5 出处注记；占位核对只钉 8 键）——全部存于评审收据 `%TEMP%\ce-code-review\20260910-070830-item121\review.json`，承接条目开工时先读。
      **⚠️ 发版/上线前必读遗留（4 项关键，详见收据 residual_risks 14 条）**：① compose JDBC URL 两处 `characterEncoding=utf8mb4` 被 mysql-connector-j 9.7.0 拒绝（编排层边界，本 diff 未修）；② Flyway 11.7.2 对 MySQL 8.4 未测试 WARN；③ **V1 迁移保留 initdb 时代 15+2 处 `DROP TABLE IF EXISTS`——staging/prod 若存在按旧 initdb 初始化的旧卷，首启 Flyway 先 DROP 再 CREATE 清库；且旧卷仅 DML 授权时应用账号缺 CREATE/ALTER 迁移即败，发版说明须明确「全新卷或手工补 GRANT ALL PRIVILEGES」**；④ restart_window 表 V2 已建但服务端启动自写一行（详设 §21#1）不在本 diff，落地前该表恒空。其余：SecretsProperties record 默认 toString 含凭证值（后续条目约定禁打/脱敏）、哨兵值 change-me 盲区（[124] crypto 解析前无信号）、Redis 懒连接不阻塞启动（[124] 后运行期暴露）、openapi 三处错误示例 message 与详设漂移待主仓回写。
      **本条目为什么耗时较长的如实记录**：耗时不在编码（U-1..U-7 早已完成），而在 Phase 3 评审门——9 路评审 + validator 独立核实中，#5/#8 的 Spring 6.2.19 /error 派发机制张力无法靠静态阅读裁决，最终做了字节码级核实才定案（#5 驳回、#8 成立）；首轮 4 份评审结果传输丢失补派一轮；随后 P1/P2 处置经用户两轮裁定（仅修 P1 + 降可选）。教训与条目 [114] 准则 9.10 同源：框架行为推断不能靠「看起来对」，负向实测/字节码核实是唯一可信信号，且这步应排在评审早期而非收尾。
      **合并回 main（2026-09-12，--no-ff，合并节点 `c519a11`）**：main 领先 23 笔、分支领先 9 笔，真正三方合并；代码文件零冲突（`.gitignore`、`application-dev.yml` 自动合并），唯一冲突为本文档两侧并行日志，取并集解决。合并前 main 工作区两处未提交改动：OSS dev 开关（`enabled: true`）作独立 chore 提交（main 侧 `9e2df0c`）；test-gates 文档修订（高德 Key 登记纠正）stash 暂存待弹回。合并由泳道 B 并行会话在 c519a11 一并提交（与 [123] U4 进度记录同节点）。P2 登记中的 worktree 绝对路径链接指向 s2s-wt-backend 工作树文件，分支删除后将失效，承接条目以评审收据 review.json 为准确路径。

---

### 2026-09-14 — [123] ce-code-review 修复批 + 复审（run `20260914-071944-66bf99f4`），复审 #1/#4 修复落地（泳道 B）

- [x] **首轮评审 13 条 actionable + 人工决策门 #2 全部修复（8 提交，base `a34e5e1`..`c466f53`，20 文件 +720/-103）**：`689801a`(#1 续期重放不重入重试预算三标记协议 + #6 carrier 归一提纯)、`87f0405`(#4 清会话失败不改裁决 + #13/AC2 refresh 5xx 走行为表)、`c225281`(#5 Header 四注入步骤 fail-open)、`c23ece5`(#7 Retry-After 共享解析纯函数 + #8 自动等待上界 900s 钳 NfrNetwork)、`cc81b0a`(#3 两 dio BaseOptions/守卫去重)、`efa3a32`(#9 墙钟上界放宽 + #10 守门 32/54 扫描面非空基线)、`8e0abe3`(#11/#12 七回调口径与 parse 规范工厂文档订正)、`c466f53`(#2 openapi refresh 操作补 400/403/429 响应键)。修复批验证：analyze 0、flutter test 459/459、gates 50/50。
- [x] **复审结论 = Ready with fixes（status: complete）**：8 reviewer（correctness/testing/maintainability/security/api-contract/reliability/adversarial/learnings；correctness 首派结果丢失已重派，8/8 齐），R1–R16/U1–U7 全 met 无 unaddressed；机械合并 7 条 primary、soft-bucket 降级 2 条（编号碰撞/文件头失实）、confidence-50 抑制 3 条（守门基线漂移/墙钟证据/首发伪造 foot-gun，均存 residual_risks）；validator 前台批 3 条 actionable 全部 validated:true（无 cross-model，Trae harness 模型族 unknown，adversarial 本地兜底）。无 P0/P1。
- [x] **本次应用 #1/#4（用户 2026-09-14 裁定「先修 #1 #4，#3 #5 稍后」，提交 `00d174f`，4 文件 +76/-74）**：
  - **#1（P2/75 gated_auto，validator true）**：新增 [dispatch_markers.dart](file:///d:/developer/code/aicoding/s2s-wt-frontend/lib/core/network/interceptors/dispatch_markers.dart) 承载 `authReplayDispatchExtraKey` 常量与 `errorWithoutExtraKey(err,key)` 唯一实现（containsKey 早退 + Map.of..remove + copyWith + DioException 五字段重建，编码规范 §1.1/§1.2）；AuthRefresh 删私有 `_stripReplayDispatchMarker`、Retry 删 `_asOuterError`+`_stripInnerMarker` 两方法，四处 reject 改一行委托；Retry 对 AuthRefreshInterceptor 的链尾到链中 import 消除（常量改从共享文件读）。`_buildTerminalError` 的 options 级 Map.of+remove 是构造全新终局错误（非错误形态剥离），语义不同保留不动。函数注释明示 dio 5.11.1 五字段口径（不拷 message，升级 dio 须复核）。
  - **#4（P3/100 gated_auto，validator true）**：[envelope_interceptor_test.dart](file:///d:/developer/code/aicoding/s2s-wt-frontend/test/core/network/envelope_interceptor_test.dart#L267) 用例名「退退避表」订正为「退回默认退避表」。
  - **验证**：flutter analyze 0 issue；定向三文件（retry/auth_refresh/envelope）54/54；全量 flutter test 459/459；test/gates 50/50。
- [x] **本次应用 #3/#5（用户 2026-09-14 裁定「先处理 #3 和 #5」，#3 递归纳管经用户决策选「纳管，加递归检测」，triage 组 [3,5] 一次落地，仅改 anti_redundancy_gate_test.dart 1 文件）**：
  - **#3（P2/75 manual/human，决策门已关闭）判据 A 拆为 A1/A2**：① A1 网络调用正则补 `request`（dio 泛型入口 `dio.request(...)` 与命名动词等价），补 for-in+dio.request 漏报夹具；② **新增判据 A2 递归重试检测**——按函数定义切分，函数体同时满足「网络调用 + 自身裸名调用（排除 `.name(` 方法调用与声明位）+ 重试驱动信号（`Future.delayed` / `catch (` 子句 / `.catchError(` 链三者任一）」三条件合取才判违规，保守不误报；游标分页递归（无延迟无 catch）、非递归延迟/catch 调别人、工厂文件豁免三条误报向夹具锁边界。已知漏报面登记文件头：`Future<T>.delayed` 带类型参数、tear-off `then(load)`、do-while（A1 既有）、复杂表达式体级联；features 存量扫描为零（ai_confirm_screen 的 Future.delayed 一次性文案切换非递归、不命中）。
  - **#5（P3/75 gated_auto）新增判据 C**：C1 机器锁定 `ApiException.parse(` 全 lib 命中数恒为基线 **13**（工厂定义 1 + 生产调用 12），文件头人工清单漂移即红，新增/删除点须三处同改（调用、文件头清单、基线常量）；C2 禁内联 `ApiException(code: ApiErrorCode.parseError, ...)`——全 lib 唯一豁免 api_exception.dart 工厂定义文件（basename 精确匹配防同名目录绕过），实测现状零内联。
  - **顺带订正**：lib Dart 文件数基线 54 -> 55（#1 的 dispatch_markers.dart 新增后未回写）。
  - **验证**：flutter analyze 0 issue；守门文件定向 23/23；test/gates **60/60**（原 50 + 新增 10 用例：A1 request 1、A2 漏报 2/误报 2、C2 漏报 1/豁免 1、缺失目录扩 2，加守护断言 3 共 10）；全量 flutter test **469/469**（原 459 + 10）。
- [x] **决策门 #2 已关闭为登记闭环（用户 2026-09-14 指令「先处理决策门 #2」，提交见下条日志）**：`retryAfterMaxSec=900` 编为 **DEC-01** 落入新增 §2.9「待裁定决策项登记（gap-register）」——owner=产品/详设维护者、三触发事件先到者触发（详设 §14.1 评审 / PRD §12.5 窗口变更 / 429 UI 倒计时语义变更）、定案前替代保障（900s 防御值行为可预期、不夹 UI 倒计时）、裁定后四步动作清单；`nfr_constants.dart:247` 注释与详设 §14.1 表格 1446 行同步指向 DEC-01，三处互指。复审 5 条 primary（#1–#5）至此全部处置完毕。
- 门禁结论：PASS（#1/#3/#4/#5 修复 + 决策门 #2 登记闭环；复审 actionable 全部关闭，DEC-01 为有 owner/有触发事件的正常挂起项而非评审欠账）。
- 下一步：[123] 分支未 push、未开 PR，可走 shipping-workflow（Residual Work Gate 四选一）；DEC-01 由 §2.9 触发事件自行到期，不阻塞出包。

**阶段验收（2026-09-14，用户指令「执行吧」启动；范围 = [123] 前端网络骨架收口 + 复审修复批，HEAD df9609c，条目级验收待后端部分）**

DoD 逐项核对（通用 11 项，证据 = 本次新鲜执行 + gates 守门断言）：

| 项 | 结论 | 证据（可复核） |
| --- | --- | --- |
| D1 三硬纪律 | PASS | 字面量：判据 C 基线守门；错误码：24+ok+2 本地负数无新增；重试：三头 containsKey 测试 |
| D2 反冗余五口径 | PASS | gates 判据 A（features 无 for/递归重试）/B（values.byName 零命中）/C（parse 13 基线 + 禁内联） |
| D3 函数级注释 | PASS | 本批新增函数均有功能/参数/返回注释 |
| D4 analyze/test | PASS | `flutter analyze` -> No issues found（14.0s）；`flutter test` -> 469/469（35s，含 gates 60/60） |
| D5 必测断言对号 | PASS | 三头 containsKey / Authorization 每次重写 / uuid 全链路仅 1 次 / /map/pins 读 3s / 40101 单飞三断言（header、retry、auth_refresh 测试文件） |
| D6 G-Q1/Q2/Q3 | PASS | gates 60/60 内含；G-Q1 双向核对绿 |
| D7 安全红线 grep | PASS（含观察①） | `real_name/id_card` lib/ 零命中；`888888` 字面量仅 auth_repository.dart:338 一处，G-Q1 已登记（reason/removeWhen 齐备） |
| D8 文档同步 | PASS | §2.9 DEC-01 三处互指；详设 §14.1 表格已指向 |
| D9 进度记录 | PASS | 本段即证据 |
| D10 分支规范 | PASS | item/123-frontend-core，Conventional Commits；--no-ff 合 main 待条目整体验收（用户 2026-09-14 决策暂不合并） |
| D11 无临时产物 | PASS | 工作区干净，无调试残留 |

分类附加（F 类）：F1 DTO 可空性/解析失败抛 parseError PASS；F2 三头注入 PASS；F3 错误码五行为 + 单飞 PASS；F4 AsyncValue 三态 / F5 CancelToken —— **N/A**（本批为 lib/core 基础设施层无 UI 页面，域接线条目验收时覆盖，扫描面为空记 N/A 不记 PASS）。

[123] 特有验收点（Backlog 表）：前端网络层必测 4 项 PASS（见 D5）；契约示范测试 baseUrl 切真服务断言一行不改 —— **未到**（后端工程未建，依赖 [121]/[122]）；cert 空壳不接 SDK 不入库 —— **未到**（条目后端 + 设置页部分未开工）。

- 验收证据：上表（命令 + 计数 + 文件指针，HEAD df9609c 工作区干净）。
- 门禁结论：**PASS**（本批范围；条目级两项「未到」不阻塞本批收口，随 [121]->[122]->[123] 后端段推进后补验）。
- 遗留与豁免：① ~~login_screen.dart:230 联调提示条引用 debugCode 未做 kDebugMode 包裹~~ **已修复（2026-09-14 用户指令）**：两处引用点 kDebugMode 编译期包裹（login_screen 提示条条件 + auth_repository sendSmsCode 桩 `code:` 表达式），常量定义行原样保持 G-Q1 登记串匹配；release 下引用折叠消除、常量树摇，产物双零由 L3 兜底；验证 analyze 0 / flutter test 469 全绿；② DEC-01 登记挂起（§2.9，owner=产品/详设维护者）；③ 实机走查面 SKIP —— 本批无用户可感知 UI，走查在域接线条目（[124]+）验收时执行。
- **用户终审：通过（2026-09-14）**。两项终审过目事项（② DEC-01 挂起登记、③ 实机走查 SKIP）随本次通过一并确认；本批（[123] 前端网络骨架收口 + 复审修复批）终审关闭。条目级验收（后端 6 接口 + cert 空壳 + 契约切真服务）与 `--no-ff` 合 main 仍待后端段完成后执行。

- **[2026-09-14] ce-compound 学习沉淀（[121] 测试骨架三轮评审加固经验）**：新增 `docs/solutions/workflow-issues/fail-closed-gate-judgments-with-mutation-self-checks.md`（12 条 fail-closed 判据模式 + 变异自检 + 合并态活体实证；frontmatter 校验 OK、机械校验 0 flags、语义校验 74 断言 = 67 verified + 7 处行号漂移已订正复跑清零）；`CONCEPTS.md` 新增「扫描面」条目、精进「守门型探针」（空候选面第二恒绿通道）与「豁免登记册」（逐命中粒度）；定向 refresh 经用户批准，3 篇同域文档 Related 补反向链接（spec-to-failing / verification-code / credential-revocation），「建立→加固」谱系互链闭合。

- **[2026-09-19] 泳道 C（测试/守门）四项并行交付**（承接 [121] 后端评审遗留 + 架构 §10.3 验收准备，与泳道 A/B 并行）：
  - **① P2 测试伞 #7/#3**：`ResponseBodyWrapperTest` 补场景五 `supports()` 正向用例（与场景三负向配对，封死取反逻辑破改）；`GlobalExceptionHandlerTest` 补场景三（无秒数正常分支：无 Retry-After 头 + code/message/data/requestId 逐字）。原场景四（needRetryAfter 码缺秒数 → WARN 绊线）因 [122] U6 落地 P2 #4（`BizException.of` 构造期拒收）而不可达，合并时删除（commit a035a39），该行为由 `BizExceptionTest` 参数化 8 码覆盖。`mvn test` **20/20 全绿**。
  - **② `scripts/seed-perf.sql` 造数脚本**（架构 §10.3.2 交付物）：15 万行长尾、可重复执行、库名断言（非 s2s_batch1 触发 ERROR 1048 中止）、生成列（`l2_category_id`/`completeness_level`）禁入 INSERT。真库实测全项精确命中：总量 150000 / 有效 100000 / 过期 active 25000 / archived 25000；200 网格、最热 3000、中位 330；48 叶子全覆盖、10101 占 15.0%；最热组合 `0_0`×10101×单 type = **1000 行**（>500，满足 §10.3.5 打满上限）。
  - **③ `idx_pins_cover` EXPLAIN 复测**（契约真实 6 列 `id,lng,lat,leaf_category_id,type,completeness_level`）：三判据全过——`type=range`（≠ALL）、`key=idx_pins_cover`、`rows=1000`（<7500=5%）；`Using where; Using index`（index-only 不回表）；反例 `SELECT *` 回表 `Using index condition` 对照成立；9 格 IN `rows=3288`；EXPLAIN ANALYZE 采样 15 次 **p95=1.74ms**（引擎内，远优于 150ms 线）；表 data 44.6MB / index 35.6MB。
  - **④ 契约示范 baseUrl 切换准备**：切换机制已就绪（`S2S_API_BASE_URL` 开关 + `isRealMode` 收敛 + mock-only 分层跳过 + 两模式共用 `/categories/tree` 信封断言），无需改动。真服务冒烟前置阻塞：后端业务 controller 未实现（含 `/categories/tree`、`/map/pins`，当前仅 common 两组件）+ `category` 表无种子（0 行）。**「断言一行不改」切真服务待 [124]/[126] 后端段交付后执行**。
### 2026-09-19 — [122] 横切链四件实施完成（泳道 A · 分支 `item/122-crosscut-chain`，U1–U8 全量）

- [x] **范围**：横切链四件（`RequestIdFilter` → `AuthInterceptor` → `RateLimitInterceptor` → `IdempotencyInterceptor`，顺序定死）+ 支撑组件（`RateLimitKeys`/`RateLimiter`/`IdempotencyCaptureFilter`/`AuthContext`/`JwtVerifier`/`UuidV4`）+ 常量类（`NfrApi`/`RateLimitThresholds`/`IdempotencyPolicy`）+ 注解（`@Idempotent`/`@RateLimit`/`RateLimitTrack`）+ logback 8 字段 JSON 单行。实施依据 `docs/plans/2026-09-15-1204-feat-item122-crosscut-chain-plan.md`（ce-plan 产出，doc-review 后定稿）。
- [x] **七单元提交**：U1 `213ef7c`（常量/注解/依赖底座 + jwtSecret 双重守卫 + jjwt/encoder 钉版）→ U2 `59a24fe`（RequestIdFilter + logback 8 字段闭环）→ U3 `1671284`（AuthInterceptor 黑名单骨架 + AuthContext + JwtVerifier）→ U4 `2ffcad0`（RateLimiter/Keys/Interceptor，Lua 原子 INCR+EXPIRE）→ U5 `475e16e`（IdempotencyInterceptor + CaptureFilter，Filter 层响应捕获）→ U6 `afcf7b8`（P2 #8/#4 随修）→ U7 `c92c813`（集成断言）。
- [x] **测试**：`mvn test` **120/120 全绿**（基线 17 零回归 + 新增 103；只增不减）。
- [x] **S1 出口三条验收**：① 幂等四类必测输入（`IdempotencyInterceptorTest` 12 用例：缺失/大写/v1→40001、同 Key 异体返首次、失败后同 Key 重执行、并发轮询重放）；② Retry-After 整数秒（`CrosscutChainIntegrationTest` 场景一 + `GlobalExceptionHandlerTest`）；③ 42907 鉴权后判定（`RateLimitInterceptorTest` guestDetail 登录态跳过 + 集成链序）。链序「限流先于幂等」由 `CrosscutChainIntegrationTest` 场景一断言（限流超限 + 缺幂等键 → 先返 429 非 40001）。
- [x] **P2 #8/#4 已修**：#8 两 advice 加 `basePackages = "com.s2s.server"`（`AdviceScopeTest` 断言 `BasicErrorController` 不匹配、业务 controller 匹配）；#4 `BizException.of/ofRetryAfter` 构造期守卫（`BizExceptionTest` 参数化 8 个 needRetryAfter 码）。
- [x] **gap-register 登记**：DEC-02 幂等轮询参数（200ms 间隔 / 10s 超时 / "PENDING" 哨兵串，KTD4）；DEC-03 黑名单 jti 兜底（`jwt:bl:{jti}` 存在即命中、值无意义，写入随 [123] logout 定案）。
- [x] **文档回写**：详设 §1.2（补 5 组件落点 + `config` 包 + `AuthContext`/`UuidV4`/`IdempotencyCaptureFilter`/`JwtVerifier`）/§3.3（匿名幂等键维度 KTD13 + 轮询参数 KTD4 + 幂等边界句）/§3.4（自然日窗口语义「键尾日期片 + 26h TTL」）；编码规范 §3.1（限频阈值 `RateLimitThresholds` 单端正源例外）；说明文档 gap-register 两项 + 本进度记录。
- **Redis 会话存储拆解口径**（防 S1 出口验收争议）：[122] 只落地「黑名单**查询**点」——`AuthInterceptor` 经 `JwtVerifier` 读 `jwt:bl:{jti}` 判 Token 是否被登出；黑名单的**写入**（logout 时把 jti 写入该键）随 [123] auth 域 logout 接口落地，键值与 TTL 一并定案（DEC-03）。
- 门禁结论：PASS（工作树 `mvn test` 120/120 全绿；静态守门 `git grep "Retry-After" src/main/java -- ':!**/GlobalExceptionHandler.java'` 零命中）。
- 下一步：ce-code-review → `--no-ff` 合并回 main。

### 2026-09-19 — 规则体系收敛 + 设计文档按前后端拆分（无条目号 · 工程基建）

- **背景**：常驻规则每轮注入 8,064 token，其中大量内容与当前任务无关；另发现 `docs/architecture/编码规范.md`（停在 v1.1）与 `.trae/rules/s2s找呀找.md` 是**同一份规范的两份副本**，双份维护。
- [x] **规则退化为薄分派器**：`.trae/rules/s2s找呀找.md` 重写为「三条硬纪律 + 安全红线 + 依据源索引 + 按需读取指引 + 每端一条硬摘要」= **1,726 字符 / ~904 token**（原 14,139 字符 / 8,064 token，**−88%**）。规范细节改为按需 `Read` 正文。
- [x] **正文归一**：规则文件与附录的全部内容并回 `docs/architecture/编码规范.md`（**v3.0**，18,416 字符），本文成为**唯一正文**；附录文件 `s2s编码规范-附录.md` 已删除。
- [x] **§3.2 错误码按前后端拆分**：后端 `ErrorCode` 定义 → §4.13，前端 `ApiErrorCode` 映射 → §5.10，§3.2 降级为索引。
- [x] **详设文档按前后端拆分**：`docs/design/前后端详细设计文档.md`（80,006 字符 / 2,007 行）→ 《后端详细设计文档》（§0–§9 + §20/§21）＋《前端详细设计文档》（§10–§19）。跨端章节 §0/§20/§21 留后端篇、前端篇链接指向；**章节号沿用拆分前编号**。字符账：切片 79,302 + 新增头部，与原文一致，**零内容丢失**（逐行相似度比对，7 处低相似全部为措辞改写或行合并）。
- [x] **引用核查**：26 处引用跨 9 个文件逐条改写（编码规范 §0.1 拆为「详设-后端／详设-前端」、安全／可观测／DevSecOps 依据源表按篇归属、3 份历史计划路径改写）。**历史叙述性标题**（如「产出《前后端详细设计文档》」，无路径无章节号）保留原名，沿革由本条说明。
- [x] 原文件 `docs/design/前后端详细设计文档.md` 已删除；`.trae/rules/` 下仅保留 1 个文件。
- [x] **落地验证（新会话实测双通过）**：① **规则注入** —— 新会话能自报出 `.trae/rules/s2s找呀找.md` 及其标题《s2s 编码红线（常驻）》；② **分派链** —— 问「幂等 Key 的正则」时先 Read `docs/architecture/编码规范.md` 正文再作答，输出与 §4.6 逐字一致（并额外核实了 `UuidV4.java` 唯一实现与 `IdempotencyInterceptor`／`RateLimitKeys` 两个消费方）。

### 2026-09-19 — 全局 `user_rules.md` 压缩（无条目号 · 工程基建）

- **背景**：全局规则（`c:\Users\刘杰\.trae-cn\user_rules.md`）每轮**每个项目**都注入，实测 3,493 字符 / ~2,956 token，其中大量是通用工程常识。
- **判据**：全局规则**只留模型推不出来的东西**——个人偏好、协作约定、环境事实；通用常识一律删。
- [x] **五轮压缩**：3,493 → **1,010 字符**（~2,956 → **~903 token**，**−71% 字符 / −70% token**）；章节由 12 节 + 行为规则 → **6 节 + 行为规则**。
- **删除项**（均为通用知识或重复表述）：Martin Fowler 坏味道 10 条清单（通用知识）、编码与重构基线（小步重构／提前返回／性能等，通用常识）、基础交互规则（深度／格式，模型默认行为）、拆 ToDoList（系统自带 TodoWrite）、中文注释与中文回答（系统级语言设置已覆盖）。
- **保留项**（模型推不出来的私有约定）：① 说明文档.md 的建／读／维护协议；② 查证优先级（Context7 → 官方文档 → 禁编造）；③ 三大「绝不允许」；④ 函数级注释（功能／参数／返回值）；⑤ 依赖检查；⑥ 输出与改动三条（最小范围编辑／完整输出例外／不复述改动）；⑦ **行为规则 5 条**（耗时预判／重试上限／无输出即异常／进度心跳／开工预告，阈值全部保全）。
- **验证**：章节编号一→六连续；5 项应删确认已删；6 项应留确认在位；行为规则 5 个阈值（>60 秒／重试 1 次／连续 3 次／约 90 秒／超 5 分钟）逐项核对全在。
- **位置说明**：该文件在**仓库外**，不进 git，此处仅作沿革记录。

### 2026-09-19 — 联调环境补建：双库重建 + 分类树种子 + 真服务首次全量启动（无条目号 · 工程基建）

**用户裁定三项口径**：① 联调业务库**重建 `s2s`**；② MySQL 先用现成 **8.0.46**（基线 8.4，留到出包前用 compose 验证）；③ 本轮种子**仅 category**（`template.fields` JSON 契约归 [124] 后端段，不提前自造 schema）。

**本机环境实况（与基线差异已核实）**：Java 21.0.7 / Maven 3.9.9；MySQL 在 **WSL2 Ubuntu-22.04 内的 Docker 容器 `mysql8`（8.0.46）**，Windows 侧**无 docker CLI、无本地 MySQL Server**（端口经 WSL2 转发到 `127.0.0.1:3306`）；Redis 为 Windows 服务 `Redis` **3.0.504**（6379，已设 requirepass）。库口径三处漂移：`.env.dev` 写 `MYSQL_DATABASE=s2s_dev`（不存在）、`.env.example` 写 `s2s`、实际还有压测库 `s2s_batch1`；**本轮统一以 `s2s`（业务）/ `s2s_track`（埋点）为准**。

- [x] **阻塞根因定位**：`s2s` 与 `s2s_batch1` **都没有 `flyway_schema_history` 表**（表是 initdb 时代手工 DDL 建的），Flyway 遇「非空 schema + 无历史表」直接拒绝启动；且 `category` / `template` 均 0 行、`system_config` 仅 1 行且 `category_tree_version='1'`（V1 种子从未执行）。
- [x] **双库重建**：`s2s` + `s2s_track` 清空重建（`s2s_track` 两张表实测各 0 行，无数据损失——**此处比用户裁定范围多动了埋点库，因不重建则该库 Flyway 同样拒绝启动**），并按 KTD-4 建应用账号 `s2s`@`%`（两库 ALL PRIVILEGES）。口径与 [121] 收尾登记的遗留 ③「全新卷或手工补 `GRANT ALL PRIVILEGES`」一致。
- [x] **新增 [V3__seed_category.sql](file:///d:/developer/code/aicoding/s2s/src/main/resources/db/migration/V3__seed_category.sql)（74 行 = 5 一级 + 21 二级 + 48 叶子）**：逐行取自 [category_tree.dart](file:///d:/developer/code/aicoding/s2s/lib/domain/category_tree.dart)（§2.4 唯一真源，编号不得重排）；`color` 取 §1.4.3 Token 键（`cat-work`/`cat-house`/`cat-vehicle`/`cat-life`/`cat-service`，与 design_tokens.dart 注释逐字一致）；`icon` 取 §6.4.2 Material Symbols 名（`work`/`home`/`directions_car`/`volunteer_activism`/`build`）；`need_cert` 取 §4.3 + Dart `requiredCert`（与 `cert.cert_type` ENUM 同集，NULL=无强制认证）；`cluster_threshold` 按 §6.15 只写一级行（工作 3 / 房屋 5 / 车辆·生活·服务 8），二级三级走列默认值以免同一阈值 15 行重复维护。**该文件落地同时闭合 `scripts/seed-perf.sql:24` 的「该表种子由后续迁移负责」挂账。**
- [x] **真服务首次全量启动 PASS**：Flyway 业务库 V1→V2→V3、埋点库 V1 **四条迁移全 success=1**；Tomcat 8080；`Started S2sServerApplication in 7.637 seconds`；`/actuator/health` → `{"status":"UP"}`。
- [x] **种子断言逐项核对（真库实测）**：分层 5/21/48、合计 **74**（`id` 无重复）；一级行 color/icon/cluster_threshold 五条逐字正确；`need_cert` 分布 `NULL 44 / enterprise 13 / personal_qualification 12 / vehicle 5`；`system_config` **2 行**（`category_tree_version='2026-08-31.1'` 与前端常量一致、`detail_quota_enabled='off'`）；结构自校验（父子关系、编号规则、同级 `sort_order` 重复）**异常 0 行**；`template` 仍 0 行（按裁定）。
- [x] **回归**：`mvn test` **123/123 全绿**（基线只增不减）。
- [x] **本次暴露并修复的 4 处缺陷（均为「单测绿但真启动挂」——切片/MockMvc 装配不覆盖全量上下文）**：
  1. [logback-spring.xml](file:///d:/developer/code/aicoding/s2s/src/main/resources/logback-spring.xml) 第 43 行 XML 注释内含连续两个短横线 → `JoranException` 解析失败，应用起不来；改为不含该字符的表述。
  2. [JwtVerifier.java](file:///d:/developer/code/aicoding/s2s/src/main/java/com/s2s/server/auth/JwtVerifier.java) 有生产构造 + 测试用私钥构造两个构造器且无 `@Autowired` → Spring 退回无参构造，`No default constructor found`；生产构造加 `@Autowired`。
  3. [IdempotencyInterceptor.java](file:///d:/developer/code/aicoding/s2s/src/main/java/com/s2s/server/common/idempotency/IdempotencyInterceptor.java) 同类问题（包级测试构造器），同法修复。
  4. JDBC URL `characterEncoding=utf8mb4` 不是合法 Java 字符集名 → `Unsupported character encoding 'utf8mb4'`；改为 `UTF-8`（服务端仍走 utf8mb4）。**同步修 [deploy/docker-compose.yml](file:///d:/developer/code/aicoding/s2s/deploy/docker-compose.yml) 两处 + [.env.example](file:///d:/developer/code/aicoding/s2s/.env.example) 两处**——该缺陷在 [121] 收尾已登记为遗留 ①（「编排层边界，本 diff 未修」），本次闭合。
- [x] **MySQL 连接参数口径已裁定并落地（用户 2026-09-19 选定方案 A）**：`useSSL=false` → **`sslMode=REQUIRED`**，同步写回 [deploy/docker-compose.yml](file:///d:/developer/code/aicoding/s2s/deploy/docker-compose.yml#L109-L121) 两处与 [.env.example](file:///d:/developer/code/aicoding/s2s/.env.example#L22-L31) 三处，**部署与本地同源**。成因：MySQL 8 默认插件 `caching_sha2_password` 在**非 TLS** 连接下首连必须索取服务端 RSA 公钥，而驱动默认禁此行为（`allowPublicKeyRetrieval=false`）→ 报 `Public Key Retrieval is not allowed`；**compose/staging/prod 的连接串同为 `useSSL=false`，原样必然同样启动失败**。改走 TLS 后该步不再发生：数据面全程加密、服务端零改动（MySQL 8 自动生成证书，工程 `my.cnf` 亦未关 TLS）、无 TLS 时 fail-closed。**刻意不采用 `allowPublicKeyRetrieval=true`** —— 那等于把库口令交给任何在 `s2s-net` 上冒充 `mysql:3306` 的进程，直接抵消架构 §4.3 / 安全 §10.1 的「强口令」控制。实测证据：`sslMode=REQUIRED` 下应用 **6.915s** 启动、`/actuator/health` = `{"status":"UP"}`、MySQL `Ssl_accepts` 累计 **20**（证明连接确实走 TLS）。残余口径（只加密、不校验证书主体）按规矩登记为 **§2.9 DEC-05**（owner = 部署/安全维护者，三触发事件，含收紧方案 `VERIFY_CA`）。
- **本机启动配方（可复现）**：先 `wsl -d Ubuntu-22.04`（**必须保持 VM 活跃**，否则容器随 VM 停止、连接超时——本次踩过一次）；注入 `SPRING_DATASOURCE_URL/-USERNAME/-PASSWORD`、`TRACK_DATASOURCE_URL`、`SPRING_DATA_REDIS_PASSWORD`、`HMAC_PEPPERS_JSON`、`AEAD_MASTER_KEYS_JSON`、`OSS_ACCESS_KEY_ID`、`OSS_ACCESS_KEY_SECRET`、`JWT_SECRET`（≥32 字节）；再 `mvn spring-boot:run -Dspring-boot.run.arguments=--logging.file.path=logs`（**`--logging.file.path` 必传**，否则 FileAppender 写容器默认路径 `/var/log/app` 在 Windows 上失败）。当前后端仅 common/横切件，业务 Controller 全未实现，故 `/categories/tree` 等业务接口仍为 404。
- 门禁结论：PASS（`mvn test` 123/123；四条 Flyway 迁移全 success；`/actuator/health` UP；74 行种子逐项核对；工作区无临时产物）。
- **登录方式演进路线已裁定（DEC-06）**：三阶段演进——① Batch1（9/30）= 短信验证码登录（PRD 既有口径，不动）；② Batch2 = 追加微信登录 + 本机号码一键登录（`user_identity` 表已预留，不改表结构）；③ Batch3（公开上架前）= 一键登录升为主登录，短信降级为 WiFi/无 SIM 备用。已写入 [说明文档 §2.9 DEC-06](file:///d:/developer/code/aicoding/s2s/说明文档.md#L296)。
- **[123] ce-plan 完成**：[docs/plans/2026-09-19-123-feat-item123-auth-domain-plan.md](file:///d:/developer/code/aicoding/s2s/docs/plans/2026-09-19-123-feat-item123-auth-domain-plan.md)。**单元共 11 个**（U1 加解密基础件 → U11 集成 + 契约示范测试）；其中 **U2 = 持久层最小设施**为 U1 落地时补入并重编号（原 U2–U10 顺延为 U3–U11）。
- **[123] U1 加解密基础件完成（2026-09-19）**：产出 `common/crypto/` 五文件（`BlindIndex` / `CryptoFacade` / `Pepper` / `MasterKey` / `KeyEncodings`）+ `common/config/CryptoConfig.java` + 两测试（21 用例）。**`mvn test` 144/144 全绿**（基线 138 + 新增 6）。期间自我纠错一处：初版把 `key_version` 编进密文前缀，与详设 §4.1**三列范式**（`xxx_hash` + `xxx_enc` + `key_version` 独立列）冲突，已按详设重写（版本号改由 `EncryptResult` 同源返回；`BlindIndex` 改返回 `byte[]` 直映 `BINARY(32)`），plan 文档 KTD5/KTD6 已回写。
- **工作方式变更（2026-09-19，用户选定）**：**弃用 worktree 隔离**，改在主仓库切分支 `item/123-auth-domain` 开发。根因（实测 8 组对照）：沙箱 `resourceAuthorization` 白名单**只对直接命令生效**，`mvn → javac` **子进程写入不被覆盖**，故 worktree 内 `mvn` 必然失败；主仓库因是 workspace 根而整树放行。已试且无效：改 `global.json` 加路径、`requires_approval`、`mvn -f <worktree>/pom.xml`、删 `target` 重跑。**残留**：目录 `d:\developer\code\aicoding\s2s-wt-lane-a` 仍在（沙箱不允许工具删除），已非登记的 worktree，需手工删。
- **持久层漏项（U1 落地时发现）**：`src/main/java` 下**无任何 entity / mapper / Mapper XML，`S2sServerApplication` 也无 `@MapperScan`**（pom 里 MyBatis-Plus 3.5.17 依赖已有）。已按用户指示**拆为独立单元 U2**，先于 auth 骨架执行。
- **[123] 会话交接文档**：[docs/plans/2026-09-19-123-handoff.md](file:///d:/developer/code/aicoding/s2s/docs/plans/2026-09-19-123-handoff.md)（U2 未开工、未提交改动分组、失败路径清单均在其中）。
- 下一步：**`ce-work` 从 U2 起实施**——U2 待写 8 文件（3 entity + 3 mapper + `@MapperScan` + `application.yml` 的 MyBatis-Plus 段）。DDL 逐列口径见 `V1__init_schema.sql`（`user` L31-50、`user_identity` L57-69、`device` L352-363）。泳道 B 的 [124]/[125] 前端段（527/527，分支 `item/124-frontend-publish`）可并行走 ce-code-review → `--no-ff` 合 main。

### 2026-09-21 — [123] 后端段 U2–U9 全部完成（持久层/骨架/发码/登录/续期/登出/资料/cert 空壳，泳道 A · 分支 `item/123-auth-domain`）

- [x] **U2 持久层最小设施**（8 文件）：`auth/entity/{UserEntity,UserIdentityEntity,DeviceEntity}` 逐列对齐 `V1__init_schema.sql`（ENUM→String、BINARY/VARBINARY→byte[]、DATETIME→LocalDateTime、TINYINT→Integer）；`auth/mapper/{UserMapper,UserIdentityMapper,DeviceMapper}` 继承 BaseMapper、无自定义方法、无 XML；`S2sServerApplication` 加 `@MapperScan("com.s2s.server.**.mapper")`（限定 mapper 包，扫根包会误把 service 接口当 mapper）；`application.yml` 加 `mybatis-plus` 段（`id-type: auto` 对齐数据库自增 + 驼峰映射固化）。
- [x] **U2 集成验证（真服务 5 项全过）**：应用带数据源启动 `Started S2sServerApplication`；业务库 Flyway 3 迁移 / 埋点库 1 迁移全 validated；`/actuator/health` = `{"status":"UP"}`；`identity_hash` byte[] 等值查询命中 + 字节往返一致（BINARY(32) 无隐式编码转换）；`@TableName("user")` 无反引号实测 `FROM user` 13 列 / `FROM device` 6 列合法（MySQL 8.0.46）。
- [x] **U3 auth 域骨架 + DTO**（9 文件）：6 个 DTO（`LoginRequest/LoginResult/SendCodeRequest/SendCodeResult/MyProfile/UpdateProfileRequest`，record + `@JsonNaming(SnakeCaseStrategy)` + Bean Validation，字段与 openapi 契约逐字对齐，`expireAt` 用 `Instant` 对齐前端 RFC3339 UTC）+ 3 个 service 骨架（`SmsService/AuthService/UserService`，@Service 空类 + 方法清单 Javadoc，方法留 U4–U8）。
- [x] **回归**：`mvn test` **144/144 全绿**（U2/U3 新增零回归）。
- [x] **本机联调口径固化**：WSL2 MySQL 8.0.46（`127.0.0.1:3306`，root/123456，业务库 `s2s` + 埋点库 `s2s_track`）+ Windows 服务 Redis（6379，密码 123456）；密钥 JSON 用 `{"version":1,"key":"<32字节hex>"}`（`version` 字段，与 `.env.example` 示例的 `id` 不同，后者会让 CryptoConfig 解析失败）；启动须 `--logging.file.path=logs`（否则 FileAppender 写 `/var/log/app` 在 Windows 失败，仅日志噪音不阻塞启动）。
- [x] **U4 短信发码**（4 新文件 + 4 改造 + 1 测试）：`AuthController.sendSmsCode`（`POST /auth/sms/send`，@Valid 入参，返回 DTO 由 `ResponseBodyWrapper` 套壳）+ `SmsService.sendCode`（四维限频<b>直调</b> `RateLimiter`：手机号 1m/1h/1d + IP 1h；验证码 dev 桩固定 `888888` 存 Redis `sms:{phone}`，TTL 300 秒）+ `SmsCodePolicy`（常量 300/888888/sms:）/`SmsCode`（存储值 `{code,expire_at,fail_count}`）+ `GlobalExceptionHandler` 补 `MethodArgumentNotValidException→40001`（message 拼字段名，对齐 openapi「缺少必填参数 grid_id」示例）。
- [x] **附带重构**：限频「窗口秒→RateLimitEntry」转换从 `RateLimitInterceptor` 私有方法上移为 `RateLimiter.entry()` 公共静态方法（渠道级三轨直调也需此逻辑），自然日 `+7200` 缓冲硬编码改 `RateLimitThresholds.NATURAL_DAY_TTL_BUFFER_SECONDS` 常量——消除字面量复制（编码规范 §1.1 + 红线「不复制字面量」）。
- [x] **回归**：`mvn test` **147/147 全绿**（新增 `SmsServiceTest` 3 用例：发码返回 300 + Redis 键 + 四轨拼装 + 超限传播；`RateLimiter`/`RateLimitInterceptor` 重构零回归）。
- [x] **U5 短信登录**（2 新 + 3 改造 + 1 测试）：`AuthController.smsLogin`（`POST /auth/sms/login`，@Valid 入参，取 X-Device-Id 头）+ `AuthService.loginBySms`（六步：agreed→40002 → 验证码比对 → 清失败计数/删码 → 盲索引查/注册 → 写 device → 签发 JWT）+ `JwtIssuer`（HS256 签发 sub/jti/iat/exp，30 天）+ `NfrAuth`（`TOKEN_TTL_DAYS=30`）。
- [x] **两个口径定案**：① 单次验证码错误 → `40001`（第 5 次 → `40105`+Retry-After）；② **第 5 次锁定**——修正 [122] 遗留 off-by-one：`RateLimitTrack.LOGIN_FAIL` 的 limit 从 `threshold(5)` 改 `threshold-1(4)`，配合 `RateLimiter` 的 `current>limit` 实现「允许 4 次失败、第 5 次锁定」，对齐前端 `count>=5`。
- [x] **一处设计回退**：plan KTD1 初稿「验证码值内 `fail_count` + 锁键 `sms:lock:{phone}`」与详设 §3.4 行 3 独立键 `rl:login:fail:{phone}` 冲突，按详设权威口径回退——`SmsCode` 去掉 `failCount` 字段，失败计数改走 `RateLimitTrack.LOGIN_FAIL` 独立键（不随验证码 5 分钟 TTL 丢失）。
- [x] **回归**：`mvn test` **152/152 全绿**（新增 `AuthServiceTest` 5 用例：agreed→40002、老用户登录、新用户注册、单次错误 40001、锁定 40105 传播）。
- [x] **U6 Token 续期**（1 新 + 3 改造 + 测试）：`AuthController.refreshToken`（`POST /auth/token/refresh`，无请求体，旧 Token 取 Authorization 头，缺头/非 Bearer → 40001）+ `AuthService.refresh`（验签允许过期 → 查 user 存在 → 重签发 30 天）+ `JwtVerifier.verifyAndGetUserIdAllowExpired`（续期专用：捕获 `ExpiredJwtException` 放行过期，签名非法/黑名单仍 40101）+ `RefreshResult`（出参 token+expire_at，openapi 内联 schema）。
- [x] **AuthController 路径调整**：去掉类级 `@RequestMapping("/auth/sms")` 改逐方法完整路径——auth 域前缀不一（`/auth/sms/*` vs `/auth/token/refresh`），类级前缀会把 refresh 拼错；send/login 路径不变。
- [x] **回归**：`mvn test` **156/156 全绿**（新增 AuthServiceTest 3 续期用例 + JwtVerifierTest 1 允许过期用例）。
- [x] **U7 登出**（3 改造 + 测试）：`AuthController.logout`（`POST /auth/logout`，无请求体，旧 Token 取 Authorization 头，缺头/非 Bearer → 40101）+ `AuthService.logout`（验签允许过期不查黑名单 → 写黑名单 `jwt:bl:{jti}` 至原过期时刻 → 清 device.push_token）+ `JwtVerifier.verifyAndGetTokenInfoAllowExpired`（返回 `TokenInfo(userId,jti,expiresAt)`，不查黑名单保证重复登出幂等）。
- [x] **回归**：`mvn test` **158/158 全绿**（新增 AuthServiceTest 2 登出用例：写黑名单+清 push_token、Token 过期跳过写黑名单）。
- [x] **U8 本人资料**（6 新 + 3 改造 + 测试）：`UserController`（`GET/PATCH /users/me`）+ `UserService.getMyProfile/updateMyProfile`（敏感词→40901、头像审核→40902、局部更新 + minProperties 校验）+ `common/moderation` 4 文件（`SensitiveWordChecker`/`MediaAuditChecker` 接口 + Noop 桩，用户定案「接口+桩」）+ `MyProfile.from(UserEntity)` 静态工厂（白名单投影唯一落点，AuthService 同步改用）。
- [x] **回归**：`mvn test` **162/162 全绿**（新增 UserServiceTest 4 用例：白名单投影、昵称敏感词 40901、头像未过审 40902、改昵称成功）。
- [x] **U9 cert 空壳**（2 新）：`cert/CertService`（`getRealnameStatus(userId)` 恒返回 `"none"`，常量 `REALNAME_STATUS_NONE`，对应详设 §0.2「Batch1 全员 none」）+ `CertServiceTest`（1 用例）。返回类型 String（对齐 `user.realname_status` 列 + openapi `RealnameStatusEnum`）。
- [x] **回归**：`mvn test` **163/163 全绿**（新增 CertServiceTest 1 用例）。
- [x] **后端段收官**：U1–U9 全部完成 —— 6 接口（发码/登录/续期/登出/查资料/改资料）+ 持久层 + 加解密基础件 + cert 空壳。
- **未提交**：U1+U2+U3+U4+U5+U6+U7+U8+U9 代码全部在工作区未 commit（分支 `item/123-auth-domain`）；A 组联调环境改动按用户指示「先不提交」。
- 下一步：**U10 前端 auth 接线**（`lib/features/auth/` 删内存 mock 改真网络 + `token_storage.dart` 新建；后门验证码 `kDebugMode` 包裹）。

### 2026-09-23 — [123] U10 前端 auth 接线完成 + 测试收尾（提交 `c806a91`）

- [x] **U10 前端 auth 接线**：新建 `lib/core/storage/token_storage.dart`（FlutterSecureStorage 安全落盘 Token，KTD7）+ `auth_repository.dart` 删内存 mock 改真网络（`sendCode`/`loginWithSms` 走 dio 调 `/auth/sms/send`、`/auth/sms/login`，服务端错误码映射 AuthFailure/AuthResult）+ `main.dart` root wiring（`networkHooksProvider` override）+ `pubspec` 加 `flutter_secure_storage` + `login_screen.dart` 删验证码过期重发逻辑。
- [x] **测试收尾 15 处失败点**：① `auth_network_wiring_test` 3 处——注入 `_MemoryTokenStorage`（FlutterSecureStorage 平台通道在测试环境无原生实现）② `login_screen_test` 6 处——`testWidgets` FakeAsync 无法推进 dio 真实 HTTP 多阶段异步链，回归不触网 `_FakeAuthRepository`（真网络映射由 `auth_repository_test.dart` 覆盖）③ G-Q1 后门码 2 处——`debugCode` 去下划线同步登记册 ④ G-Q2 密钥扫描——`git rm test/auth_repository_test.dart`（迁移至 `test/features/auth/`）+ 登记 2 条后端 Java 测试假密钥豁免（`fake-jwt-secret`/device id 假值，lineContains 用纯 hex 避免自扫描）。
- [x] **全量回归**：`flutter test` **453/453 全绿**（原 12 个测试用例失败 + G-Q2 深层暴露 3 处后端假密钥，全部修复）。
- [x] **提交**：`c806a91`（16 文件，+717/−671，当前分支 `item/124-backend-category`）；`logs/`、`.trae/` 未纳入。

---

**文档结束。** 每次重新打开项目前，**必须从阅读本节进度记录开始**，确认当前节点后再启动开发。任何里程碑/范围/资源变化，先更新"一、项目规划"与"二、实施方案"对应条目，再进入具体执行。
