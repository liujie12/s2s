/**
 * 定时任务域。
 *
 * <p>职责：单实例 {@code @Scheduled} 定时任务（到期下架、月表预建/清理、
 * 注销清理、孤儿媒体清理等，编号沿用详设 §6 原表）。<b>单实例前提：扩多
 * 实例须引入分布式锁</b>——本域所有定时任务类的类注释必须写明此前提
 * （详设 §6 硬要求）。本条目（U-2）无任务实现，仅建包占位。</p>
 *
 * <p>出处：详设 §6（定时任务）、§1.2（包结构清单）。</p>
 */
package com.s2s.server.task;
