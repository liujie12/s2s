/**
 * 地理网格包。
 *
 * <p>职责：承载 {@code GridIdCalculator}（Java 侧唯一网格算法实现处）。
 * 算法口径三端逐位一致：步长 450 微度、整数微度域运算、向负无穷取整
 * （Java 用 {@code Math.floorDiv}，禁整除截断）；grid_id 正则
 * {@code ^-?\d+_-?\d+$}；端云共用 10 条对照测试向量保证逐位一致
 * （跨语言镜像，双端各有唯一实现处，不算冗余）。</p>
 *
 * <p>出处：详设 §16（缓存与 grid_id）、§1.2（包结构清单）。</p>
 *
 * <p>落地说明：组件随地图条目落地，本条目（U-2）仅建包占位。</p>
 */
package com.s2s.server.common.geo;
