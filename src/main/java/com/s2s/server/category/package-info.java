/**
 * 类目域。
 *
 * <p>职责：三级分类树读取（含语义迁移：{@code category_id} 接受 {@code 10101}）。
 * 域内分层固定 {@code controller/service/mapper/entity/dto}；与前端
 * {@code lib/features/category/} 同名同构。</p>
 *
 * <p>出处：详设 §1.3（分层与域边界）、§1.2（包结构清单）。</p>
 */
package com.s2s.server.category;
