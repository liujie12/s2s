# 后端详细设计文档（找鸭找 · s2s）

> 版本：v1.0（2026-09-19）｜由《前后端详细设计文档》按前后端拆分而来，**章节号沿用拆分前编号**
> 范围：**后端** `com.s2s.server`（Java 21 + Spring Boot 3）—— §1–§9；另含跨端章节 §0（阅读顺序与全局纪律）、§20（交付物矩阵补项）、§21（已识别缺口）
> 前端部分见 [前端详细设计文档.md](file:///d:/developer/code/aicoding/s2s/docs/design/前端详细设计文档.md)

> **文档定位**：架构方案与数据库设计的下一层落地物，是编码前的最后一份设计交付物。
> **配套交付物**：《API 接口契约》见 [openapi.yaml](file:///d:/developer/code/aicoding/s2s/docs/api/openapi.yaml)。
> **交付范围**：Batch1（2026-09-30 内测包）= 20 个业务接口 + `/track/events`。
>
> **依据源（本文档不新造口径，一切冲突以依据源为准）**
>
> | 依据源 | 提供什么 |
> | --- | --- |
> | `docs/architecture/系统总体架构设计文档.md` | 功能域切分、接口批次矩阵、关键链路、定时任务清单、客户端配套交付物矩阵 |
> | `docs/architecture/技术栈选型说明.md` | 服务端与客户端依赖选型定案、EXPLAIN 注释纪律 |
> | `docs/architecture/可观测性架构方案.md` | 埋点事件字段、四段计时纪律、客户端队列规则 |
> | `docs/database/数据库设计文档.md` + `ddl/V1__init_schema.sql` | 字段类型真源、生成列、覆盖索引、对外视图白名单 |
> | `docs/PRD.md` §12 | 接口出入参与**错误码唯一口径**（§12.5） |
> | `lib/nfr_constants.dart` | NFR 常量真源。**本文档只引用常量名，不复制字面量** |

---
## 0. 阅读顺序与全局纪律

### 0.1 三条贯穿全文的硬纪律

| 纪律 | 说明 | 违反后果 |
| --- | --- | --- |
| **不复制字面量** | 一切阈值、TTL、上限只写 `NfrXxx.yyy` 常量名 | 双处维护必然漂移 |
| **不新增错误码** | 错误码以 PRD §12.5 为唯一口径源（现 24 个错误码 + 成功码 `0`）；新增前必须先查是否已占用，且**只能由需求侧在 PRD §12.5 定案**，设计文档不得自行占号 | 历史上 `40901` 曾被误占，返工 |
| **行号引用必带章节名** | 引用依据源时写「架构 §9.2」而非「架构第 412 行」 | 文档一改行号即失效 |

### 0.2 两处已裁定的口径冲突（2026-09-06 详细设计阶段）

| 冲突 | 裁定结果 | 已同步落点 |
| --- | --- | --- |
| `category_version` 是整数还是字符串 | **`YYYY-MM-DD.N` 字符串** | DDL 初始数据 / 数据库设计文档 §3.15 与 §7.5 / OpenAPI `CategoryVersion` schema |
| `40301` 未实名详情额度 Batch1 是否生效 | **Batch1 关闭**，由 `system_config.detail_quota_enabled='off'` 控制；代码路径完整实现、仅由开关短路 | DDL 新增配置行 / 数据库设计文档 §7.5 / OpenAPI `GET /posts/{id}` 描述 |

裁定理由（`40301`）：`cert` 域整体延后到 Batch2，Batch1 全员 `realname_status='none'`，
若额度生效则每人每天只能看 3 条详情，会把「地图 → 详情 → 联系」这条 P0 主闭环卡死。

### 0.3 本文档结构

- **第一部分（§1–§9）后端详细设计**，末节为后端自检清单
- **第二部分（§10–§21）前端详细设计**，末三节为前端自检清单（§19）、**交付物矩阵补项**（§20）与**已识别缺口**（§21）
- 两份自检清单是编码期的验收依据；§21 的缺口须在编码前逐项处理或明确接受

---
---

# 第一部分：后端详细设计

## 1. 工程结构与功能域划分

### 1.1 技术底座

Spring Boot 3 + Java 21 **单体**应用，**功能域垂直切分**。
单实例部署，因此定时任务用 Spring 进程内调度（`@Scheduled`），**无需分布式锁**。

### 1.2 包结构

```text
com.s2s.server
├── S2sApplication.java                 # 启动类
├── common/                             # 【只被依赖，不对外暴露任何接口】
│   ├── web/
│   │   ├── ApiResponse.java            # 统一响应包 {code,message,data,request_id}
│   │   ├── ResponseBodyWrapper.java    # 响应包装器（ResponseBodyAdvice）
│   │   ├── GlobalExceptionHandler.java # 全局异常处理器（@RestControllerAdvice）
│   │   ├── RequestIdFilter.java        # 生成 request_id，透传 X-Interaction-Id
│   │   ├── AuthContext.java            # 鉴权上下文唯一读写器（userId 请求属性承载，[122] U3）
│   │   └── UuidV4.java                 # UUID v4 格式校验唯一实现处（[122] U5 提取）
│   ├── error/
│   │   ├── ErrorCode.java              # 25 个错误码枚举（唯一定义处；24 业务码 + OK(0)，条目[121] 回写，依据 docs/plans/2026-09-10-1038-feat-item121-backend-bootstrap-plan.md（s2s-wt-backend））
│   │   └── BizException.java           # 业务异常，携带 ErrorCode + retryAfterSec
│   ├── idempotency/
│   │   ├── Idempotent.java             # 注解（anonymous 属性区分登录态/匿名键维度）
│   │   ├── IdempotencyInterceptor.java # Key 校验 + SETNX 占位 + 轮询重放（纯判定件）
│   │   └── IdempotencyCaptureFilter.java # Filter 层响应捕获（成功缓存/失败删占位/重放写出）
│   ├── ratelimit/
│   │   ├── RateLimiter.java            # Lua 原子 INCR+EXPIRE 计数 + 超限判定
│   │   ├── RateLimitKeys.java          # 限频键命名（账号级/渠道级分流，类型分维）
│   │   ├── RateLimit.java              # 限频注解（声明生效键轨集合）
│   │   ├── RateLimitTrack.java         # 11 行键轨枚举（阈值/窗口/超限码绑定）
│   │   └── RateLimitInterceptor.java   # 注解驱动限频拦截器（42907 鉴权后判定）
│   ├── crypto/
│   │   ├── BlindIndex.java             # HMAC-SHA256 + pepper
│   │   ├── AeadCipher.java             # AES-GCM-256，AAD 绑定
│   │   └── CryptoFacade.java           # 【全系统唯一解密入口】
│   ├── geo/
│   │   └── GridIdCalculator.java       # grid_id 整数微度域实现
│   └── audit/
│       └── AuditLogWriter.java         # audit_log 统一写入
├── config/
│   └── WebCrosscutConfig.java          # 横切链组装根（Filter/拦截器注册落点，链序定死）
├── auth/     { controller, service, mapper, entity, dto }
│   ├── AuthInterceptor.java            # Bearer JWT 验签 + 黑名单 + AuthContext 写入（[122] U3）
│   └── JwtVerifier.java                # jjwt HS256 验签 + 黑名单查询（jwt:bl:{jti}）
├── category/ { controller, service, mapper, entity, dto }
├── post/     { controller, service, mapper, entity, dto }
├── map/      { controller, service, mapper, dto }
├── contact/  { controller, service, mapper, entity, dto }
├── notify/   { controller, service, mapper, entity, dto }
├── cert/     # Batch2，Batch1 仅留包与空 service（供 post 域查实名状态）
├── ai/       # Batch2
├── track/    { controller, service, mapper, dto }   # 埋点，写 s2s_track 库
└── task/                                            # 11 项定时任务
```

### 1.3 域间调用纪律（与 `lib/features/` 同构）

| 规则 | 说明 |
| --- | --- |
| 域间只调对方 **service** | `post.PostService` 可调 `cert.CertService`，**不可**调 `cert.CertMapper` |
| **禁止跨域访问 mapper** | 一旦发生，域边界即失效，后续拆分无从下手 |
| `common` **只被依赖** | 不含任何 `@RestController`，不产生对外接口 |
| 循环依赖零容忍 | 若 A→B 且 B→A，把共享逻辑下沉到 `common` |

### 1.4 功能域与 Batch1 接口映射（20 + 1）

| 域 | Batch1 接口 | 数量 |
| --- | --- | --- |
| `auth` | `POST /auth/sms/send`、`POST /auth/sms/login`、`POST /auth/token/refresh`、`POST /auth/logout`、`GET /users/me`、`PATCH /users/me` | 6 |
| `category` | `GET /categories/tree`、`GET /templates/{leaf_category_id}` | 2 |
| `post` | `POST /media/upload/ticket`、`POST /media/{media_id}/commit`、`POST /posts/precheck`、`POST /posts`、`GET /posts/{id}`、`PATCH /posts/{id}/status`、`GET /posts/mine` | 7 |
| `map` | `GET /map/pins`、`GET /posts/search` | 2 |
| `contact` | `GET /posts/{id}/contact`、`POST /posts/{id}/report` | 2 |
| `notify` | `GET /notifications` | 1 |
| `cert` / `ai` | — | 0 |
| `track`（另计） | `POST /track/events` | 1 |

---

## 2. `common` 三组件（**必须在第一个业务接口之前落地**）

这三个组件是全系统契约的物理载体。若先写业务接口后补它们，
每个已写的 controller 都要返工。

### 2.1 组件一：统一响应包 + 响应包装器

```java
/**
 * 统一响应包。所有接口无论成功失败均返回该结构。
 *
 * @param <T> 业务负载类型
 */
public record ApiResponse<T>(
        int code,          // 业务码，0 成功；非 0 见 ErrorCode，前 3 位与 HTTP 状态码对齐
        String message,    // 中文文案，非 0 时可直接呈现给用户
        T data,            // 业务负载，失败时恒为 null
        String requestId   // 服务端生成，UI 报错唯一回显值
) { }
```

**包装器职责**（`ResponseBodyAdvice`）：

1. controller 直接返回业务 DTO（不手写 `ApiResponse.ok(...)`，避免遗漏）
2. 包装器统一套壳，注入当前请求的 `request_id`
3. 序列化用 snake_case（`data` 内字段名与 OpenAPI 一致）

**排除路径**：`/track/events` 也走统一包装（无例外）。

### 2.2 组件二：全局异常处理器

| 异常类型 | 映射 |
| --- | --- |
| `BizException` | 取其 `ErrorCode` 的 code/message/httpStatus；若 `retryAfterSec != null` 则写 `Retry-After` 头 |
| `MethodArgumentNotValidException` / `MissingRequestHeaderException` / `ConstraintViolationException` | `40001`，message 拼出具体字段名 |
| `OptimisticLockException`（自定义，mapper 更新 0 行时抛） | `40903` |
| 其余 `Exception` | `50001`，**日志打全栈**，响应体不含堆栈信息 |

**纪律**：`Retry-After` 的写入**只在这一处**发生，业务代码只负责在 `BizException`
里带上剩余秒数，不各自写 header。

### 2.3 组件三：错误码枚举（25 个，唯一定义处；24 业务码 + `OK(0)`，条目[121] 回写，依据 docs/plans/2026-09-10-1038-feat-item121-backend-bootstrap-plan.md（s2s-wt-backend））

```java
/**
 * 全系统错误码枚举。唯一口径源为 PRD §12.5。
 *
 * 分段规则：前 3 位与 HTTP 状态码对齐（40xxx→400，42xxx→429，50xxx→500 等）。
 * 新增枚举值前必须先确认该 code 未被占用（历史教训：40901 曾被乐观锁冲突误占，
 * 后修正为 40903）。
 * needRetryAfter=true 的 code 在抛异常时必须提供剩余秒数，否则视为实现缺陷。
 */
public enum ErrorCode {
    OK(0, 200, "ok", false),

    PARAM_INVALID       (40001, 400, "参数缺失或非法", false),
    AGREEMENT_REQUIRED  (40002, 400, "请先阅读并同意用户协议与隐私政策", false),

    UNAUTHORIZED        (40101, 401, "登录已过期，请重新登录", false),
    LOGIN_LOCKED        (40105, 401, "验证码错误次数过多，请稍后再试", true),

    DETAIL_QUOTA_USED   (40301, 403, "今日可查看详情数量已用完", false),
    QUALIFICATION_NEEDED(40302, 403, "该类目需先完成资质认证", false),
    CATEGORY_BANNED     (40303, 403, "该类目当前禁止发布", false),
    PUBLISH_LIMIT       (40304, 403, "未实名用户发布数量已达上限", false),
    NO_PERMISSION       (40305, 403, "无权限执行此操作", false), // 后台 RBAC 越权专用，App 端不返回

    SENSITIVE_WORD      (40901, 409, "内容包含不允许发布的词汇，请修改后重试", false),
    IMAGE_REJECTED      (40902, 409, "图片未通过审核，请更换后重试", false),
    VERSION_CONFLICT    (40903, 409, "数据已更新，请刷新后重试", false),

    POST_GONE           (41001, 410, "该信息已下架或已过期", false),

    AI_QUOTA_EXCEEDED   (42901, 429, "今日 AI 辅助次数已用完", true),
    CONTACT_LIMIT       (42902, 429, "今日查看联系方式次数已达上限", true),
    CIRCUIT_BROKEN      (42903, 429, "操作过于频繁，该功能今日已暂停使用", true),
    PUSH_LIMIT          (42904, 429, "推送发送过于频繁", true),
    SMS_LIMIT           (42905, 429, "验证码发送过于频繁，请稍后再试", true),
    TRACK_LIMIT         (42906, 429, "track rate limited", true), // 唯一不呈现给用户
    GUEST_DETAIL_LIMIT  (42907, 429, "今日浏览次数较多，登录后可继续查看", true), // 未登录详情浏览超限

    INTERNAL_ERROR      (50001, 500, "服务繁忙，请稍后重试", false),
    AI_UNAVAILABLE      (50301, 503, "AI 服务暂时不可用，请稍后重试", false),
    CERT_UNAVAILABLE    (50302, 503, "认证服务暂时不可用，请稍后重试", false),
    GEO_UNAVAILABLE     (50303, 503, "定位服务暂时不可用，请稍后重试", false);

    private final int code;
    private final int httpStatus;
    private final String message;
    private final boolean needRetryAfter;
    // 构造与 getter 省略
}
```

**单元测试要求**：断言 `code` 无重复、`code / 100` 的商与 `httpStatus` 一致（`OK(0)` 豁免）、
枚举总数为 25（含 `OK`，即 24 个业务错误码 + `OK(0)`；条目[121] 回写，依据 docs/plans/2026-09-10-1038-feat-item121-backend-bootstrap-plan.md（s2s-wt-backend），与工作树 ErrorCode.java 实测 25 枚一致）。这条测试是防止历史编号冲突复现的唯一自动化手段。

---

## 3. 请求横切链路（Filter → Interceptor → Controller）

### 3.1 执行顺序

```text
[1] RequestIdFilter          生成 request_id 入 MDC；读取 X-Interaction-Id 入 MDC
[2] AuthInterceptor          解析 Bearer JWT，未登录/失效 → 40101
[3] RateLimitInterceptor     按注解声明的维度做 Redis 计数 → 429xx（带 Retry-After）
[4] IdempotencyInterceptor   写接口校验 Idempotency-Key，命中缓存则直接返回首次结果
[5] Controller               入参校验 → service
```

`RateLimit` 在 `Idempotency` **之前**：幂等重放不应消耗限频额度以外的资源，
但限频本身必须先拦住洪水。

### 3.2 两个 ID 的分工（**极易混淆，必须逐条守住**）

| | `interaction_id` | `request_id` |
| --- | --- | --- |
| 生成方 | **客户端** | **服务端** |
| 传递 | 请求头 `X-Interaction-Id` | 响应体 `request_id` 字段 |
| 覆盖范围 | 缓存 → 网络 → 聚合 → 渲染**四段** | 单次 HTTP 请求 |
| 服务端用途 | **仅日志串联** | 日志 + UI 报错回显 |
| **严禁**用于 | 鉴权、限流、幂等判定 | — |
| 缓存命中时 | 存在（客户端自有） | **不存在**（预期行为，不得判为异常） |

### 3.3 幂等实现

```text
Redis 键：登录态 idem:{userId}:{Idempotency-Key}          TTL = NfrApi.idempotencyWindowHours
          匿名   idem:dev:{X-Device-Id}:{Idempotency-Key}（缺/非法设备头降级 idem:anon:{Idempotency-Key}）
Redis 值：首次【成功】响应的完整 JSON
```

处理流程：

1. 校验 Key 格式（正则见 OpenAPI `IdempotencyKey` 参数）→ 不符或缺失回 `40001`
2. `SETNX` 占位 → 失败说明并发/重放：轮询取首次响应（200ms 间隔 / 10s 总超时，超时回 `50001`），**原样返回**
3. 执行业务
4. **仅当业务成功**才把响应写入 Redis；失败则**删除占位键**

**幂等边界**：幂等只防「同 Key 重复提交」，不校验请求体差异（同 Key 异体返回首次结果）；
防「同一用户刷不同 Key」的滥用面由渠道限频轨钳制（§3.4），二者互补、不可互替。

四类必测输入（写成单元测试，缺一不可）：

| 输入 | 期望 |
| --- | --- |
| 缺失 `Idempotency-Key` | `40001`，**不得放行** |
| `9B1DEB4D-...`（大写）或 v1 UUID | `40001` |
| 同 Key、**请求体不同** | 返回**首次结果**，不按新请求体执行 |
| 首次失败 → 同 Key 重放 | **重新执行**（只有成功响应进缓存） |

### 3.4 限频键设计（账号级 vs 渠道级分流）

| 场景 | 键维度 | Redis 键 | 阈值 | 超限码 |
| --- | --- | --- | --- | --- |
| 短信发送（手机号） | **渠道级** | `rl:sms:phone:{phone}:{1m|1h|1d}` | 1/min、5/h、10/d | `42905` |
| 短信发送（IP） | 渠道级 | `rl:sms:ip:{ip}:1h` | 20/h | `42905` |
| 登录失败锁定 | 渠道级 | `rl:login:fail:{phone}` | 5 次 → 锁 15min | `40105` |
| 联系（账号） | **账号级** | `rl:contact:uid:{userId}:1d` | 30/d | `42902` |
| 联系（设备） | 账号级辅助 | `rl:contact:dev:{fingerprint}:1d` | 30/d | `42902` |
| 联系（IP） | 渠道级 | `rl:contact:ip:{ip}:1d` | 100/d | `42902` |
| 联系熔断 | 账号级 | `rl:contact:burst:{userId}:1m` + `fz:contact:{userId}:{date}` | 1min ≥10 次 → 当日冻结 | `42903` |
| 举报频次 | 账号级 | `rl:report:uid:{userId}:1d` | 熔断同上 | `42903` |
| 埋点上报 | 账号级 | `rl:track:uid:{userId}:1m` | 60 请求/min | `42906` |
| **未登录详情（设备）** | **无账号维** | `rl:guestdetail:dev:{deviceId}:1d` | 30/d | `42907` |
| **未登录详情（IP）** | 渠道级 | `rl:guestdetail:ip:{ip}:1d` | 100/d | `42907` |

**窗口语义**：`:1m`/`:1h` 为**滚动窗口**（TTL = 窗口秒数）；`:1d` 为**自然日**窗口——
键尾内嵌 `yyyyMMdd` 日期片（时区固定 Asia/Shanghai），TTL 给 26h（比 24h 多 2h）防跨日残留。
故 `:1d` 轨实际键形为 `rl:...:{维度}:1d:{yyyyMMdd}`，上表为表意简写（键拼装归 `RateLimitKeys`）。

**未登录详情限频的两点特殊性**（口径源：PRD §12.3 / §12.5，2026-09-06 G2 裁定）：

1. **只对未登录请求生效**：`GET /posts/{post_id}` 携带有效 Token 时**跳过此限频**，因为已登录行为可按 `user_id` 追溯。判定点在拦截器读取鉴权上下文之后，不可放在 Token 解析之前。
2. **它是唯一没有账号维的限频轨** —— 恰恰因为对象是未登录用户，所以纪律 1「账号级一律用 `user_id`」在此不适用，只剩设备与 IP 两个可低成本更换的维度。因此阈值直接沿用联系方式的设备 30 / IP 100，**不新造数字**，其定位是提高批量采集成本而非绝对阻断。

**两条纪律**：

1. **账号级风控一律用 `user_id`**（联系次数、AI 配额、举报频次、熔断），
   不用手机号——用户换绑手机号不应重置风控。
2. 渠道级（短信、登录失败）**保留手机号维度**——此时用户尚未登录，无 `user_id` 可用。
3. 限频计数键**允许丢失**（Redis 重启即清零），这是可接受的：
   限频是防滥用而非账务，不为此引入持久化成本。

---

## 4. 加解密与盲索引

### 4.1 双列范式

每个敏感字段落库为**两列 + 一个版本号**：

| 列 | 算法 | 类型 | 用途 |
| --- | --- | --- | --- |
| `xxx_hash` | HMAC-SHA256 + pepper（**确定性**） | `BINARY(32)` | 承载唯一索引与等值查询（盲索引） |
| `xxx_enc` | AES-GCM-256 + **随机 IV** | `VARBINARY` | 仅用于解密展示 |
| `key_version` | — | `TINYINT` | 密钥轮转标识 |

**为何必须两列**：AEAD 带随机 IV，同一明文两次加密结果不同，**无法建唯一索引也无法等值查询**；
而确定性哈希不可逆，**无法展示**。二者缺一不可。

### 4.2 AAD 绑定表（防跨行密文搬运）

| 字段 | AAD |
| --- | --- |
| `user_identity.identity_value_enc` | `user_id` + `identity_type` |
| `user.real_name_enc` | `user_id` |
| `post.contact_value_enc` | `post_id` |

AAD 的作用：即便攻击者把 A 帖子的密文复制到 B 帖子行，解密也会失败。

### 4.3 解密入口收敛

**全系统只有 `common.crypto.CryptoFacade` 一处可以解密。**
每次解密调用点必须显式传入 AAD 材料，且解密行为**同步写 `audit_log`**。

Batch1 的解密调用点**只有一个**：`contact.ContactService#viewContact`
（服务 `GET /posts/{id}/contact`）。任何新增解密调用点都视为架构变更，须评审。

---

## 5. 九个功能域的分层设计

### 5.1 `auth` 域

| 接口 | Service 方法 | 关键实现点 |
| --- | --- | --- |
| `POST /auth/sms/send` | `SmsService#sendCode` | 四维限频；验证码存 Redis（`sms:{phone}`）；不落库 |
| `POST /auth/sms/login` | `AuthService#loginBySms` | `agreed=false`→`40002`；失败计数达 5 → `40105` + `Retry-After`；手机号盲索引查 `user_identity`，未命中则注册；写 `device`；签发 JWT（30 天） |
| `POST /auth/token/refresh` | `AuthService#refresh` | 校验旧 Token 签名（允许已过期）+ 用户状态；重签发 |
| `POST /auth/logout` | `AuthService#logout` | Token 加入 Redis 黑名单至原过期时刻；清 `device.push_token` |
| `GET /users/me` | `UserService#getMyProfile` | 出参严格走对外视图白名单 + 本人可见 `phone_mask`/`default_radius` |
| `PATCH /users/me` | `UserService#updateMyProfile` | 昵称过敏感词→`40901`；头像 `media_id` 须 `audit_status=pass` 否则 `40902` |

**登录时的手机号处理**（三步，顺序不可换）：

1. `phoneHash = BlindIndex.hmac(phone)` → 查 `user_identity` 唯一索引
2. 命中 → 取 `user_id`；未命中 → 建 `user` + `user_identity`（`identity_value_enc`
   的 AAD 为 `user_id + identity_type`，故必须**先拿到 `user_id` 再加密**）
3. `user.phone_mask` 写脱敏串（仅本人视角展示用）

**`user` 对外视图白名单**（数据库设计文档 §3.1）：

```text
允许出现在响应中：id / nickname / avatar_url / realname_status / qualification_badges[]
本人视角额外允许：phone_mask / default_radius
禁止出现在任何 API 响应中：real_name_enc / id_card_hash
```

落地手段：`AuthorBrief` / `MyProfile` 两个 DTO 是**唯一**的 user 出参类型，
`UserEntity` **不得**直接返回。写一条集成测试扫描全部响应 JSON，
断言不含 `real_name`、`id_card` 字样。

### 5.2 `category` 域

| 接口 | Service 方法 | 关键实现点 |
| --- | --- | --- |
| `GET /categories/tree` | `CategoryService#getTree` | 读 `system_config.category_tree_version`；与入参 `version` 相等 → 返回 `data=null`（**HTTP 200，非真 304**）；否则返回全量树 |
| `GET /templates/{leaf_category_id}` | `TemplateService#getByLeaf` | 叶子不存在 → `40001` |

**缓存**：分类树与模板属静态数据，走 `NfrCache.staticL1TtlSec` 档本地 Caffeine 缓存；
配置变更后传播延迟上限见 `NfrCache.configPropagateSec`。

**版本号读写规则**：格式 `YYYY-MM-DD.N`。运营改配置时同日递增 `N`、跨日重置为当日 `.1`。
**字符串比较即可判新旧**（因为格式本身单调）。

### 5.3 `post` 域（最重的域）

#### 5.3.1 媒体两步式直传

```text
POST /media/upload/ticket
  ├─ 校验 content_type ∈ 白名单、size ≤ 上限（不符 → 40001）
  ├─ 服务端生成 media_id 与 object_key（客户端不可指定）
  ├─ 插入 post_media（post_id = NULL 的孤儿行，audit_status = pending）
  └─ 返回带签名的短时效 upload_url

（客户端直传 OSS → OSS 回调服务端）

POST /media/{media_id}/commit          【由 OSS 回调触发为主，客户端调用为兜底】
  ├─ HeadObject 二次校验实际 size 与 content-type
  ├─ 生成审核任务，写 audit_status
  ├─ ★ 原图删除与本次 commit 在【同一事务内同步完成】（不走异步补偿）
  └─ commit 成功后对象标记不可覆写
```

七条安全约束（逐条对应实现落点）：

| # | 约束 | 落点 |
| --- | --- | --- |
| 1 | 客户端不持长期密钥 | 只下发单次签名 URL |
| 2 | 桶不开公共读 | 所有读取走带签名临时 URL |
| 3 | **原图删除与 commit 同事务同步** | `commitMedia` 方法内 `@Transactional` |
| 4 | 不挂 CDN，或禁用图片处理过滤参数 | 运维配置 + 启动自检 |
| 5 | `media_id` 与 `object_key` 服务端生成 | `createTicket` 内生成 |
| 6 | OSS 侧强制 length-range + type 白名单，commit 二次校验 | 签名策略 + `HeadObject` |
| 7 | OSS 回调为 commit 触发源；24h pending 清理；commit 后不可覆写 | 回调接口 + 定时任务 #P1 |

`audit_status` 三态双视角的**代码落点**：
在 `MediaAssembler` 里做视角分流，签名为
`toDto(PostMediaEntity e, boolean isOwner)`。**禁止**在 controller 里判断视角——
一处漏判就等于把未过审图片下发给了他人。

#### 5.3.2 发布链路

```text
POST /posts
 [1] 幂等 + 参数校验
 [2] 类目校验：banned → 40303；sensitive 且用户无资质 → 40302
 [3] 未实名发布上限（40304）—— 同 40301，Batch1 由开关关闭
 [4] 敏感词：title + description + attributes 全文过滤 → 40901
 [5] 媒体校验：media_ids 全部属于当前用户且非 reject → 40902
 [6] grid_id = GridIdCalculator.of(lng, lat)
 [7] contact 双列加密（AAD = post_id）→ ★ 因此必须先 INSERT 拿到 id 再 UPDATE 密文
 [8] attributes 写 JSON；required_full / address_precise / leaf_matched 三条件写入
     → l2_category_id 与 completeness_level 由【STORED 生成列】自动派生
 [9] expire_at = now + NfrPostLifecycle.validDays
 [10] 异步产出埋点 post_published
```

**第 7 步的顺序陷阱**：`contact_value_enc` 的 AAD 绑定 `post_id`，
但 `post_id` 要 INSERT 后才有。故实现为**同一事务内**「INSERT（密文列留空）→
取自增 id → UPDATE 密文列」，**不可**为了少一次 UPDATE 而改用其他 AAD。

**两个 STORED 生成列（不可写，写入报 `ERROR 3105`）**：

| 生成列 | 表达式 |
| --- | --- |
| `l2_category_id` | `leaf_category_id DIV 100` |
| `completeness_level` | 由 `attributes` 中 `required_full` / `address_precise` / `leaf_matched` 三条件 `JSON_EXTRACT` 后计数，`CASE` 映射 3→2、2→1、else→0 |

Entity 上这两列必须标注为只读（MyBatis 的 `insert`/`update` SQL 里
**不出现**这两个列名），否则一写就报错。

#### 5.3.3 `post` 状态机与写守卫分流

```text
        ┌──── renew（顺延 expire_at）
        ↓
   ┌─────────┐  offline   ┌──────────┐  republish  ┌─────────┐
   │ active  │ ─────────→ │ offline  │ ──────────→ │ active  │
   └─────────┘            └──────────┘             └─────────┘
        │                       │
        │ 定时任务：到期            │ 定时任务：超 autoArchiveDays
        ↓                       ↓
   ┌─────────┐            ┌──────────┐
   │ expired │ ─────────→ │ archived │
   └─────────┘            └──────────┘
```

**写守卫分流（本设计最关键的一条）**：

| 变更来源 | 守卫条件 | 理由 |
| --- | --- | --- |
| **用户意图变更**（`PATCH /posts/{id}/status`） | `WHERE id=? AND version=?` | 用户看到的是某个版本的数据，必须防覆盖 |
| **系统派生变更**（定时任务到期下架、归档） | `WHERE id=? AND status IN (合法前驱集)` | 定时任务不持有用户视图的 version，若也守 version 则会被用户的并发编辑饿死 |

`version` 缺失时**服务端不得兜底**（不允许「没传就跳过校验」）——直接回 `40001`。
更新影响行数为 0 → 抛 `OptimisticLockException` → 全局处理器映射为 `40903`。

#### 5.3.4 `GET /posts/{id}` 的额度开关

```java
/**
 * 判断是否需要对未实名用户扣减详情浏览额度。
 *
 * Batch1 该开关为 off：cert 域延后导致全员 realname_status=none，
 * 额度生效会把「地图→详情→联系」P0 闭环卡死。代码路径完整保留，
 * Batch2 实名上线时把 system_config.detail_quota_enabled 置 on 即生效。
 *
 * @param user 当前登录用户
 * @return true 表示需要走额度扣减逻辑
 */
private boolean shouldApplyDetailQuota(UserEntity user) {
    if (!configService.isOn("detail_quota_enabled")) {
        return false;                       // Batch1 走这条分支
    }
    return user.getRealnameStatus() == RealnameStatus.NONE;
}
```

### 5.4 `map` 域（性能最敏感）

#### 5.4.1 `grid_id` 算法（三方逐位一致）

```java
/**
 * 计算约 500m 网格的 grid_id。
 *
 * 算法（客户端 Dart / 服务端 Java / SQL 三方必须逐位一致）：
 *   1. 先把坐标向下取整到 5 位小数：floor(x * 100000) / 100000
 *   2. 转入【整数微度域】运算，避免浮点累积误差
 *   3. 以 0.0045°（约 500m，即 450 微度）为步长，用 floor（向负无穷）取整
 *   4. 编码为 "{gx}_{gy}"
 *
 * @param lng GCJ-02 经度
 * @param lat GCJ-02 纬度
 * @return 形如 "26700_6728" 的网格标识，落 varchar(24)
 */
public static String of(double lng, double lat) {
    long lngMicro = floorToMicroDegree(lng);   // 步骤 1+2
    long latMicro = floorToMicroDegree(lat);
    long gx = Math.floorDiv(lngMicro, STEP_MICRO);   // STEP_MICRO = 450
    long gy = Math.floorDiv(latMicro, STEP_MICRO);
    return gx + "_" + gy;
}

/**
 * 将度数向下取整到 5 位小数并转为整数微度（1 微度 = 1e-5 度）。
 * 必须用 Math.floor 而非强制类型转换：负数场景下 (long) 是向零截断，
 * 会让 -0.000015 落到 0 而不是 -1，与测试向量第 9 条冲突。
 */
private static long floorToMicroDegree(double deg) {
    return (long) Math.floor(deg * 100_000d);
}
```

**10 条测试向量**（三端共用同一份，任一端不过即视为实现错误）：

| # | 输入 (lng, lat) | 期望 grid_id | 考察点 |
| --- | --- | --- | --- |
| 1 | (120.15000, 30.28000) | `26700_6728` | 常规正数 |
| 2 | (0, 0) | `0_0` | 原点 |
| 3 | (0.00450, 0.00450) | `1_1` | 步长边界（含） |
| 4 | (0.00449, 0.00449) | `0_0` | 步长边界（不含） |
| 5 | (-0.00001, -0.00001) | `-1_-1` | 负数最小偏移 |
| 6 | (-0.00450, -0.00450) | `-1_-1` | 负数边界（含） |
| 7 | (-0.00451, -0.00451) | `-2_-2` | 负数跨格 |
| 8 | (0.004500049, 0.004500049) | `1_1` | 5 位小数截断 |
| 9 | (-0.000015, -0.000015) | `-1_-1` | **关键探针，须额外断言中间微度值为 -2** |
| 10 | (120.15000, -30.28000) | `26700_-6729` | 混合正负 |

第 9 条是**唯一能暴露「用截断代替 floor」的向量**：
若误用 `(long)` 截断，中间值会得 `-1` 而非 `-2`，最终 grid 仍是 `-1_-1`（结果偶然正确），
所以**必须断言中间值**，否则这条测试形同虚设。

#### 5.4.2 `/map/pins` 查询与覆盖索引

```sql
-- EXPLAIN 结论（实测）：type=range, key=idx_pins_cover,
--   Extra = "Using where; Using index" —— 完全走覆盖索引，不回表。
--   实测 p95 = 1.37ms（500 条打满）。
-- 纪律：任何写在 XML 里的查询都要附一条 EXPLAIN 结论作为注释（技术栈选型 §7）。
SELECT id, lng, lat, leaf_category_id, type, completeness_level
FROM post
WHERE grid_id IN (...)              -- 索引第 1 列，按半径展开的网格集合
  AND leaf_category_id IN (...)     -- 第 2 列
  AND type = ?                      -- 第 3 列
  AND status = 'active'             -- 第 4 列
  AND expire_at > NOW()             -- 第 5 列
LIMIT 500;                          -- 上限见 NfrPerf.renderMaxPins
```

覆盖索引 `idx_pins_cover` 的 10 列 = **前 5 列过滤 + 后 5 列即响应字段**：

```text
(grid_id, leaf_category_id, type, status, expire_at,   ← 过滤
 id, lng, lat, completeness_level, user_id)            ← 响应/回表规避
```

**历史教训**：早期只用前 5 列 + `id` 验证过「覆盖索引成立」，
但真实响应契约还要 `lng`/`lat`/`completeness_level`，
一上真实字段立刻回表。**验证覆盖索引必须用响应契约的真实列集合**。

#### 5.4.3 紧凑序列化

```java
/**
 * 将 Pin 查询结果序列化为紧凑二维数组。
 *
 * 采用 schema + 数组而非对象数组，是为了满足体积红线：
 * gzip 后 ≤ NfrPerf.mapPayloadMaxBytes，原始 ≤ NfrPerf.mapPayloadMaxRawBytes。
 * 对象数组在 500 条时仅键名就要重复 500 次。
 *
 * 列序固定为 [id, lng, lat, category_id, type, completeness_level]，
 * 与响应中的 schema 字段一致，客户端按此列序解析。
 * type 编码：0=resource，1=demand（枚举转整数进一步压体积）。
 * 坐标保留位数见 NfrPerf.mapPinCoordDecimals。
 */
```

**聚合模式切换按 `zoom` 而非点数**：
由 `zoom` 换算 metersPerPixel，与 `NfrPerf.clusterModeSwitchMetersPerPixel` 比较。

- 远景 → `mode=cluster`，**服务端预聚合**返回 `clusters[]`
- 近景 → `mode=pin`，返回 `pins[]`，**客户端 Dart 侧**做视觉聚合
  （网格尺寸见 `NfrPerf.clusterGridSizePx`）

该阈值标注为「待 POC 实测校准」——它是体验拐点，不是可以拍脑袋的常量。

#### 5.4.4 缓存键五要素

```text
Redis 键：pins:{categoryIdsHash}:{postType}:{radius}:{gridId}:{categoryVersion}
TTL    ：NfrCache.pinSetTtlSec
```

五要素**任一缺失即回 `40001`**（不允许用默认值兜底）——
缺任一要素都会导致不同语义的请求共用缓存，返回错误数据。

网格边长见 `NfrCache.keyGridMeters`。客户端单设备缓存键上限
`NfrCache.maxKeys`，按 LRU 淘汰。

#### 5.4.5 分类树版本协商（**不报错**）

```java
/**
 * 处理客户端分类树版本与服务端不一致的情况。
 *
 * 口径（架构 §9.2.1）：版本不一致【不报错】、不阻塞、不强升、不回 40001。
 * 仅在响应中附 category_version_stale=true，由客户端异步拉取新分类树
 * 并清空全部本地 Pin 缓存。
 *
 * 理由：分类树是运营高频调整的数据，若不一致就拦截请求，
 * 每次运营改配置都会造成一次全量客户端不可用。
 */
```

### 5.5 `contact` 域

#### 5.5.1 `GET /posts/{id}/contact` —— 全系统唯一联系方式出口

```text
[1] 未登录 → 40101
[2] 熔断冻结检查（fz:contact:{userId}:{date} 存在）→ 42903 + Retry-After
[3] 三维日限：账号 30 / 设备 30 / IP 100 → 超限 42902 + Retry-After（剩余至次日 0 点秒数）
[4] 突发检测：1 分钟内 ≥10 次 → 写当日冻结键 → 42903
[5] 帖子状态：offline/expired → 41001
[6] CryptoFacade 解密 contact_value_enc（AAD = post_id）
[7] ★ 写 contact_event —— 北极星辅助指标【唯一统计点】
[8] ★ 写 audit_log —— 合规审计留痕
[9] 返回完整联系方式 + remaining_today
```

**第 7、8 步是两件事、两张表，不可合并、不可省略。**
`contact_event` 是产品指标口径的唯一来源——省掉它，北极星指标就没有数据来源；
`audit_log` 是合规留痕——省掉它，无法应对个人信息查询投诉。

其他任何接口只返回 `contact_mask`（脱敏）。落地手段：
`contact_value_enc` 的解密调用点在整个代码库里**只允许出现一次**，
用一条静态扫描测试（grep `CryptoFacade.decrypt` 调用点计数）守住。

#### 5.5.2 `POST /posts/{id}/report`

`reason` 枚举与 `report.reason` 列**严格一致**：
`false_info` / `fraud` / `wrong_category` / `harassment` / `other`。
`evidence` 落 JSON 列。举报成功同写 `audit_log`。频次异常 → `42903`。

### 5.6 `notify` 域

`GET /notifications`：分页 + `type` 筛选（`system`/`interaction`/`cert`），
附 `unread_count` 供 Tab 角标。Batch1 **只有查询**，标记已读与推送偏好为 Batch2。

### 5.7 `cert` / `ai` 域（Batch1 空壳）

Batch1 **不交付任何对外接口**，但必须保留：

- `cert.CertService#getRealnameStatus(userId)` —— 供 `post` 域判 `40302`/`40304`
  （Batch1 恒返回 `NONE`）
- 包结构与 `ErrorCode` 中的 `40302`/`42901`/`50301`/`50302` 枚举值
  （契约与客户端枚举**必须保留**，否则 Batch2 上线时客户端不认识这些码）

### 5.8 `track` 域

写 `s2s_track` 库。**禁止跨库 JOIN**（两个 database 物理隔离）。

```text
[1] 必须登录态（user_id 取登录态，请求体传了也忽略）
[2] 限频：同用户 1min 60 请求 → 42906 + Retry-After
[3] 单请求事件数 ≤ NfrApi.trackBatchMaxEvents → 超出 42906
[4] 按每条事件的 ts 归入 track_event_YYYYMM 月表
[5] 目标月表不存在（预建失败的兜底）→ 写 track_event_fallback
[6] 校验 layer_switch 四段之和 == duration_ms（容差 ±1ms），
    不符不拒收但标记异常，进数据质量看板
[7] 落库语句固定为：
      INSERT INTO track_event_YYYYMM (...) VALUES (...)
      ON DUPLICATE KEY UPDATE id = id
    命中唯一索引 uk_event_dedup(interaction_id, event_name, user_id, ts)
    即视为重复，静默跳过。
```

**第 [7] 步是客户端「每批换新 `Idempotency-Key`」的对价，不是可选优化。** 客户端为避免更严重的事故（沿用旧 Key → 命中幂等缓存 → 整批事件静默丢失，见 §17.4.1）而放弃了幂等键保护，因此「同一批被上报两次」的去重责任完整地落在这条 SQL 上。

三条实现纪律：

- **严禁 `INSERT IGNORE`。** 它把数据截断、非空列插 NULL、类型转换失败一并降级为 warning，等于在埋点写入路径上关掉全部数据质量报错。
- **严禁写成 `ON DUPLICATE KEY UPDATE props = VALUES(props)`。** 这个写法看起来更"合理"（用新值更新），但会让后到的重复批次**覆盖**首次落库的事件属性，且行数校验完全看不出异常。必须是无副作用的 `id = id`。
- **`ts` 必须用客户端上报的值，不得替换为服务端到达时刻。** 否则同批重传会拿到不同 `ts`，唯一键当场失效而不报任何错。

约束定义与完整论证见《数据库设计文档》§3.16.1，行为由 `assert_behavior.sh` 的 A8 守护。

---

## 6. 定时任务（11 项）

单实例部署 + Spring `@Scheduled`，**无需分布式锁**。
若未来扩为多实例，这 11 项必须同时引入分布式锁——本条写入代码注释作为提醒。

| # | 任务 | 频率 | 批次 | 关键约束 |
| --- | --- | --- | --- | --- |
| 1 | 帖子到期自动下架 | 每 10 分钟 | Batch1 | 守 `status IN ('active')`，**不守 version** |
| 2 | 埋点月表预建 | 每月 25 日 | Batch1 | 提前建下月表，避免月初写入失败。**必须用 `CREATE TABLE ... LIKE`** —— 逐列拼 DDL 会漏掉 `uk_event_dedup`，导致下月去重静默失效 |
| 3 | 埋点月表清理 | 每日 | Batch1 | **整表 `DROP`** 超 90 天的月表（不用 DELETE） |
| 4 | 审计日志清理 | 每日 | Batch1 | 保留 180 天 |
| 5 | 中转脱敏日志清理 | 每日 | Batch1 | 保留 30 天 |
| 6 | 取消收藏记录清理 | 每日 | Batch1 | 保留 30 天 |
| 7 | 注销冷静期到期清理 | 每日 | Batch1 | **按 `user_id` 删除全部身份行**（不是只删一行） |
| 8 | 可观测性阈值巡检 | 每周 | Batch1 | 对照 NFR 常量产出巡检报告 |
| 9 | 24h pending 媒体清理 | 每小时 | Batch1 | 清理未 commit 的孤儿 `post_media` + OSS 对象 |
| 10 | OCR 数据清理 | 每日 | Batch2 | — |
| 11 | 发布记忆清理 / 双向推送下发 | 每日 / 分钟级 | Batch2 | 推送频控 `42904` |

**第 3 项用 `DROP` 而非 `DELETE`** 的理由：埋点表数据量大，
`DELETE` 会长时间持锁并膨胀 undo，月表设计的全部意义就在于能整表丢弃。

**第 7 项的坑**：一个用户可能有多行 `user_identity`（手机号、微信等），
按 `id` 删只会删掉一行，剩余身份行会让「已注销」用户仍能被查到。

---

## 7. 数据库访问纪律

| 纪律 | 说明 |
| --- | --- |
| 不写外键 | 由应用层保证引用完整性 |
| 生成列用 STORED | 保证可被索引 |
| 禁跨库 JOIN | `s2s` 与 `s2s_track` 物理隔离 |
| 每条 XML 查询附 EXPLAIN 注释 | 技术栈选型 §7 纪律；无 EXPLAIN 结论的查询视为未完成 |
| 生成列不出现在 insert/update SQL | 否则报 `ERROR 3105` |
| Flyway 管理 schema | `V1__init_schema.sql`（15 张业务表）、`V1__init_track_schema.sql`（2 张埋点表） |

---

## 8. 性能验收口径（后端侧）

| 项 | 口径 |
| --- | --- |
| 读接口 P95 | ≤ `NfrPerf.readApiRtP95Ms`，取**服务端 `rt_ms`** |
| 出数方式 | **逐接口单独出数**（不混算），并发 20，跑 5 分钟，**丢弃前 30 秒**（预热） |
| 压测机 | **异机发压**（同机会争抢 CPU 使结果失真） |
| 环境有效性 | CPU steal 均值 ≤ 10%，超标则重跑，**最多 3 次** |
| 响应体积测量 | **必须打满 500 条**再量，小数据集量出来的体积无意义 |

---

## 9. 后端自检清单

- [ ] `ErrorCode` 枚举总数 **25**（含 `OK`；2026-09-06 G1/G2 裁定新增 `40305`、`42907`），无 code 重复，`code/100 == httpStatus`
- [ ] `needRetryAfter=true` 的 **8** 个 code（`40105` + `42901`–`42907`）抛异常时均带剩余秒数
- [ ] 幂等四类必测输入全部有单元测试
- [ ] `grid_id` 10 条测试向量全绿，且第 9 条断言了中间微度值
- [ ] `idx_pins_cover` 的 EXPLAIN 用**响应契约真实 6 列**复测，`Extra` 为 `Using where; Using index`
- [ ] 集成测试扫描全部响应 JSON，不含 `real_name` / `id_card` 字样
- [ ] `CryptoFacade.decrypt` 调用点计数 == 1
- [ ] `post` 的两个 STORED 生成列未出现在任何 insert/update SQL
- [ ] `PATCH /posts/{id}/status` 不传 `version` 返回 `40001`（不兜底）
- [ ] 定时任务 #1 守 `status` 不守 `version`；#3 用 `DROP`；#7 按 `user_id` 删全部身份行
- [ ] 埋点落库 SQL 为 `ON DUPLICATE KEY UPDATE id = id` —— **既非 `INSERT IGNORE`，也非 `props = VALUES(props)`**（§5.10 第 [7] 步）
- [ ] 埋点 `ts` 写入的是客户端上报值，不是服务端到达时刻（否则 `uk_event_dedup` 静默失效）
- [ ] 埋点月表预建任务用 `CREATE TABLE ... LIKE`，确保 `uk_event_dedup` 一并复制（逐列拼 DDL 会漏索引）
- [ ] `assert_behavior.sh` 的 A8 五个子断言全部实测通过（当前**待补跑**）

---
---

# 第二部分：前端详细设计

> 本部分的交付物清单对齐架构 §9.2「客户端配套交付物」10 行矩阵，并**补入该矩阵本身漏掉的一项**（见 §20.1）。
> 现有代码位于 [lib](file:///d:/developer/code/aicoding/s2s/lib)，本部分描述的是「从当前纯本地样例数据形态 → 接后端形态」的改造设计，**不是新建工程**。

### 架构 §9.2 交付物矩阵 → 本文档章节对照

| # | 架构 §9.2 交付物 | 本文档落点 |
| --- | --- | --- |
| 1 | 网络层（请求头注入、统一解包、gzip 协商） | §11 |
| 2 | 错误码映射（24 个集中枚举 + 码→行为） | §12 |
| 3 | 登录态续期（单飞 refresh、挂起与重放） | §13 |
| 4 | 重试与退避（白名单、指数退避加抖动、`Retry-After`） | §14 |
| 5 | 本地缓存（Pin 60s + 静态三档、缓存键五要素含 `grid_id`） | §16 |
| 6 | 埋点采集与队列 | §17 |
| 7 | 同步 Provider 异步化（含请求竞态） | §15 |
| 8 | 分类树版本协商 | §16.4 |
| 9 | 平台配置（Android 权限、iOS `Info.plist`、高德 Key） | §18.1 |
| 10 | 调试后门出包门禁 | §18.2 |
| **11** | **联系外呼与降级**（PRD §7.8）—— **架构 §9.2 矩阵本身漏项，本文档补入** | **§20.1** |
| **12** | **既有模型与契约的语义迁移** —— 同为矩阵漏项 | **§10.4** |

第 11、12 两项是本次前端视角审查补入的。前者是 Batch1 P0 闭环的**最后一步**（用户看到帖子 → 拿到联系方式 → **打出这个电话**），矩阵漏了它，意味着如果只照矩阵施工，做出来的是一个"能看不能联系"的应用；后者是接后端时最容易崩的一环（§10.4.1 会在 release 包直接 `RangeError`）。

---

## 20. 交付物矩阵补项

架构 §9.2 的 10 行矩阵漏了下面两件事，本节补上。§10.4 已覆盖第 12 项，此处只写第 11 项，并附一条与之相关的 UI 文案纪律。

### 20.1 联系外呼与降级（矩阵第 11 项，PRD §7.8）

这是 Batch1 P0 闭环的**最后一步**。链路是：看到帖子 → `GET /posts/{id}/contact` 拿到联系方式 → **实际打出这个电话**。前三步矩阵都覆盖了，第四步没人管 —— 而没有第四步，整个产品的核心价值不成立。

依赖 `url_launcher`（[pubspec.yaml](file:///d:/developer/code/aicoding/s2s/pubspec.yaml) 已在用）。

| 动作 | URI scheme | 降级路径 |
| --- | --- | --- |
| 拨号 | `tel:<号码>` | `canLaunchUrl` 返 false（平板、无 SIM 卡设备）→ **不弹「失败」**，改为「复制号码到剪贴板 + Toast 提示已复制」 |
| 发短信 | `sms:<号码>` | 同上，降级为复制 |
| 唤起导航 | 高德/百度 scheme | iOS 需 `LSApplicationQueriesSchemes` 白名单（§18.1 已登记为缺失项）；未装任何地图 App → 降级为复制地址文本 |

**三条纪律：**

1. **`canLaunchUrl` 的返回值必须判，且判 false 不等于报错。** iOS 上未声明白名单的 scheme 会**恒返 false**（§18.1），这时"打不开"的根因在配置不在设备，弹一个「拨号失败」的错误只会误导用户去检查自己的手机。降级到复制是唯一体面的处理。
2. **联系方式只能来自 `GET /posts/{id}/contact` 的响应，用完即弃。** 不写本地缓存、不写日志、不进埋点属性。它是全系统唯一的联系方式出口（后端 §5.5.1），客户端侧也必须保持这个唯一性 —— 一旦落到本地存储，加解密与 AAD 绑定那一整套服务端防护就被绕过了。
3. **外呼动作本身要埋点，但不带号码。** 事件记录「用户点了拨号」这个行为与是否走了降级分支，属性里**不含**任何联系方式内容。

### 20.2 限频提示文案不得承诺剩余次数

`ContactInfo.remaining_today` 只反映**账号维度**的剩余次数，但服务端限频实际有**四个维度**（账号 / 设备 / IP / 熔断，后端 §3.4）。

于是这个场景成立：

```
UI 显示「今日还可查看 3 次」
用户点第 2 次 → 服务端因【设备维度】超限返 42902
UI 只能说「已达上限」
```

用户看到的是应用先承诺 3 次、然后在第 2 次就自我否定。这不是限频策略的问题（多维限频是必要的风控），而是**UI 把一个维度的数字当成了全局承诺**。

**规则：**

| 做 | 不做 |
| --- | --- |
| 「今日剩余 3 次（以实际请求结果为准）」 | 「今日还可查看 3 次」 |
| 收到 `42902` 时按 `Retry-After` 展示倒计时，文案只说「已达上限，请 X 分钟后再试」 | 追问/解释是哪个维度超限（服务端刻意不区分，风控信息不外泄） |
| 拿到 `42902` 后**立即把本地显示的剩余次数刷成 0** | 继续显示 `remaining_today` 的旧值 |

最后一条尤其重要：如果不刷成 0，用户会看到"还剩 3 次"和"已达上限"同时在屏幕上，这是应用在自相矛盾。

**服务端侧不改。** 让 `remaining_today` 汇总四个维度的最小值在技术上可行，但会把风控阈值直接暴露给客户端（攻击者能反推出设备维度的限额），得不偿失。正确的解法是客户端不把它当承诺。

**`42907` 同样适用本节全部规则，且更严格**（2026-09-06 G2 裁定新增）：未登录详情浏览限频**根本没有任何剩余次数字段**下发到客户端 —— `PostDetail` 不含浏览计数，客户端也不得自行在本地累计次数来「预测」何时会超限。原因与上面同源：设备维和 IP 维的计数在服务端，客户端本地计数永远不等于服务端计数（多设备共享 IP、应用重装、清缓存都会错位），一旦本地预测与服务端判定不一致，就又制造了一次「自相矛盾」。

| 做 | 不做 |
| --- | --- |
| 收到 `42907` 才提示，文案「今日浏览次数较多，登录后可继续查看」+ 登录入口 | 提前提示「今日还可浏览 N 条」 |
| 提示只说「浏览次数较多」 | 说明是设备还是 IP 超限、或透出 30 / 100 这两个阈值 |
| 用户完成登录后**直接重试原请求**（已登录不受此限） | 登录后仍沿用 `Retry-After` 倒计时阻断用户 |

---

## 21. 已识别缺口（本文档不解决，需在编码前处理）

| # | 缺口 | 影响 | 处理建议 |
| --- | --- | --- | --- |
| 1 | `restart_window` 表未在 `V1__init_schema.sql` 建表，也无写入接口 | 可观测性 §4.2.2 的「重启窗口排除」规则**无法执行**，P95 会被预热期污染 | 补两列 DDL + 一个内部写入接口（或直接由服务端启动时自写，无需客户端接口）。**建议由服务端启动自写**，客户端不参与 —— 客户端不知道服务端何时重启 |
| 2 | `NfrPerf.clusterModeSwitchMetersPerPixel` 阈值待 POC 实测校准 | 聚合模式切换点可能偏早或偏晚，影响 `layer_switch` P95 | POC-B 阶段实测校准后回写常量，**不在编码期猜** |
| 3 | `post_media` 与架构 §5.1 的 `media` 表名差异 | 已裁定（R20）沿用 PRD 的 `post_media`，DTO 命名已统一 | 无需处理，此处仅登记以免后续再起争议 |
| 4 | 客户端 `mem_peak_mb` 的 Android 平台通道未实现 | `layer_switch` 12 字段缺一，内存问题无数据支撑 | Batch1 内实现平台通道；iOS 明确上报空值 |
| 5 | `track_drop_stat` 落表方式未定（表 or 日志） | `dropped_count` 无处落地，队列溢出仍是静默 | 可观测性 §4.6 允许「极小表或直接写日志」，Batch1 **选写日志**成本最低 |
| ~~6~~ | ~~`Accept-Encoding: gzip` 是否需手动注入未实测~~ | **已关闭（2026-09-10，U4 第一天实测）** | 按 §11.5 两步实测完成：默认不注入与显式注入两条件下 dart:io `HttpClient.autoUncompress` 均自动解压，`data` 都是 Map、解压后 `content-encoding`/`content-length` 头保留，「显式设置即需自行解压」风险未复现。裁定 **不手动注入**、开关默认关并固化，结论回写 §11.5「实测结论」。同批实测并订正：拦截器三方向均按添加顺序执行（FIFO），§11.1 图注/§11.1.1 旧「倒序」表述已改 |
| ~~7~~ | ~~埋点事件服务端去重键未在契约中固化~~ | **已关闭（2026-09-06）** | 已建唯一索引 `uk_event_dedup(interaction_id, event_name, user_id, ts)`，落库用 `INSERT ... ON DUPLICATE KEY UPDATE id = id`；见 §17.4.1、《数据库设计文档》§3.16.1、`V1__init_track_schema.sql`。**行为断言 A8 的 5 个子断言已于同日在 WSL2 内的 `mysql:8.0` 容器实测全部通过**（含 A8.1 的 `props` 保持首次值），无遗留项 |

> 缺口 #7 关闭时连带确立的一条客户端纪律：**事件入队后，去重键三列（`interaction_id`/`ts`/`event`）即冻结**，上报器不得做任何"补全"或重新取值 —— 否则唯一索引会在不报错的情况下静默失去作用。已列入 §19 自检清单。

---

**文档结束。** 两份交付物：本文档 + [openapi.yaml](file:///d:/developer/code/aicoding/s2s/docs/api/openapi.yaml)。
