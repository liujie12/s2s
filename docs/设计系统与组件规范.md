# 找鸭找 · 设计系统与组件规范

<!--
  由 prototype-figma/export-spec-doc.js 自动生成，请勿手改。

  真源是 prototype-figma/code.js 里的设计系统真源表（同时也是 Figma Variables
  与 Component master description 的来源）。改规格请改那里再重跑：

      node prototype-figma/export-spec-doc.js

  prototype-figma/probe-layout-offline.js 有断言逐项比对本文件与真源，
  手改或忘记重跑都会当场变红。本文件与 lib/design_tokens.dart 同为单向出口。
-->

> **本文件由脚本生成，请勿手改。** 改规格请改 `prototype-figma/code.js` 的真源表，
> 然后重跑 `node prototype-figma/export-spec-doc.js`。
>
> 上游判据：`docs/PRD.md` §1（设计系统）与 §1.8（无障碍红线）。
> 下游产物：Figma Variables 集合 `ZhaoYaZhao Tokens`、`lib/design_tokens.dart`。
>
> 计数：Token 43 项 · 对比度 34 组 · 动效 6 档

## 0. 怎么用这份文档

1. **取值一律查本文档**，不要从渲染图上量、也不要从别的实现里抄；
2. 本文档标「本轮未出稿」「仅状态实样板」的项，表示**19 个业务页面里没有对应画面** ——
   规格有效，但没有产品语境中的对照参考，实现时若发现规格不适用应回头提出，而非默默改掉；
3. 凡颜色相关的改动，都必须回到 §2 那张表重算 —— 那张表的比值是现算的，改了色会自动变；
4. 本文档**不含**页面级设计意图（为什么这一页这么排），那部分在 Figma 画布的标注卡与 PRD 里。

## 1. Design Token

Token 是本设计系统的唯一取值来源。Figma 侧对应 Variables 集合 `ZhaoYaZhao Tokens`，
Flutter 侧对应 `lib/design_tokens.dart`（由 `export-dart-tokens.js` 生成）。
**任何取值改动都改 `code.js` 顶部的真源表，然后重跑两个导出脚本**。

### 1.1 语义色板（16 项）

| Token | HEX |
|---|---|
| `color/primary` | `#0B7C8C` |
| `color/primary-dark` | `#075E6B` |
| `color/primary-light` | `#E6F6F8` |
| `color/accent` | `#B4531A` |
| `color/success` | `#22C55E` |
| `color/warning` | `#F59E0B` |
| `color/error` | `#EF4444` |
| `color/success-text` | `#15803D` |
| `color/warning-text` | `#B45309` |
| `color/error-text` | `#B91C1C` |
| `color/text-primary` | `#1F2937` |
| `color/text-secondary` | `#6B7280` |
| `color/text-placeholder` | `#9CA3AF` |
| `color/border` | `#E5E7EB` |
| `color/background` | `#F8FAFC` |
| `color/surface` | `#FFFFFF` |

三色拆职责（勿混用）：`success` / `warning` / `error` 原值作文字时对比度只有
2.15–3.76:1，全部不过 AA。故**原三色只用于填充、圆点、描边等非文本图形**，
一切文字用途取 `*-text` 三色。写代码时禁止把 `color/error` 传给文字参数。

### 1.2 分类色（5 大类 × 2 档）

| 原色 Token | HEX | 深色变体 | HEX |
|---|---|---|---|
| `category/cat-work` | `#3B82F6` | `category/cat-work-deep` | `#326FD1` |
| `category/cat-house` | `#8B5CF6` | `category/cat-house-deep` | `#8055E2` |
| `category/cat-vehicle` | `#F97316` | `category/cat-vehicle-deep` | `#B85510` |
| `category/cat-life` | `#10B981` | `category/cat-life-deep` | `#0B825A` |
| `category/cat-service` | `#EC4899` | `category/cat-service-deep` | `#C63C81` |

**原色只作图形**（Pin 填充、图例圆点、卡片分类色条），其上不得放承载内容的文字；
**`-deep` 只作承载白色文字或图标的底**。两者色相一致，同屏并置不构成语义割裂。
凡将来新增分类色，必须**同时**补 `-deep` 变体，不得只加原色。

### 1.3 字阶（6 档）

| 档位 | 字号 px | 字重 | 行高倍数 | 行高 px |
|---|---|---|---|---|
| `h1` | 24 | Bold | ×1.3 | 31.2 |
| `h2` | 18 | SemiBold | ×1.3 | 23.4 |
| `h3` | 16 | SemiBold | ×1.4 | 22.4 |
| `body` | 14 | Regular | ×1.5 | 21.0 |
| `small` | 12 | Regular | ×1.5 | 18.0 |
| `caption` | 11 | Medium | ×1.2 | 13.2 |

行高**不入 Variables**：它是「字号 × 倍数」的派生值，单独存一份就是第二份副本。
Figma 侧对应的 Text Style 命名为 `字阶/<档位>`（6 档）；
Paint Style 刻意**不加前缀**，名字直接沿用 role 名（`color/*` 与 `category/*`），
与变量面板同名，可肉眼对照。

### 1.4 间距与圆角

| 间距 Token | px |
|---|---|
| `spacing/xs` | 4 |
| `spacing/sm` | 8 |
| `spacing/md` | 12 |
| `spacing/lg` | 16 |
| `spacing/xl` | 24 |
| `spacing/xxl` | 32 |

| 圆角 Token | px |
|---|---|
| `radius/sm` | 4 |
| `radius/md` | 8 |
| `radius/lg` | 12 |
| `radius/xl` | 16 |
| `radius/full` | 999 |

**`2px` 是间距阶梯之外唯一获准的豁免值**（常量 `TIGHT_GAP`），
只用于「一个语义单元内部的两行」之间（圆标与其标签、色块名与其色值）。
不得把它当通用间距用，也不得加进 `spacing/*` —— 阶梯是 4 的倍数，2 破坏该规律。

### 1.5 画布规格

设计画框固定 **390 × 844**（iPhone 14 逻辑分辨率，与 Flutter 逻辑像素一致）。

## 2. 对比度实测表（34 组，含 8 组禁用）

**红线**：正文级文字 ≥ **4.5:1**（WCAG AA）；非文本图形 ≥ **3.0:1**；
纯装饰与占位符豁免。触控区最小 **44 × 44**。

⚠️ **本表的比值全部由 `export-spec-doc.js` 现算**，不是抄来的数字。
真源只登记「哪个色压哪个底 + 阈值 + 判定 + 理由」（`code.js` 的 `CONTRAST_PAIRS`），
比值按 WCAG 2.x 相对亮度公式算出。**故任何调色都会让本表数字自动变化**，
且离线探针有断言核对算值与本红线，改了色忘了回算会当场变红。

| 组合 | 实测 | 阈值 | 判定 | 说明 |
|---|---|---|---|---|
| text-primary 在 surface 上 | 14.68:1 | 4.5 | 达标 | 正文主色，最常见组合 |
| text-primary 在 background 上 | 14.03:1 | 4.5 | 达标 | 页面底色上的正文 |
| text-primary 在 primary-light 上 | 13.21:1 | 4.5 | 达标 | 浅青底信息块内的正文 |
| text-secondary 在 surface 上 | 4.83:1 | 4.5 | 达标 | 卡片内次要文字 |
| text-secondary 在 background 上 | 4.62:1 | 4.5 | 达标 | ⚠️ 余量仅 0.12。禁止再调浅，也不得用于比 background 更浅之外的任何底色 |
| primary 作文字在 surface 上 | 4.91:1 | 4.5 | 达标 | 链接与强调文字；也是「主色底 + 白字」那一组的等价判据 |
| primary 作文字在 primary-light 上 | 4.42:1 | 4.5 | **禁用此组合** | 禁用此组合。primary-light 底上的文字一律改用 primary-dark 或 text-primary |
| primary-dark 作文字在 primary-light 上 | 6.70:1 | 4.5 | 达标 | 信任卡、批注框、冲刺区标题的正确取法 |
| primary-dark 作文字在 surface 上 | 7.44:1 | 4.5 | 达标 | 白底上的深青文字 |
| text-secondary 在 primary-light 上 | 4.35:1 | 4.5 | **禁用此组合** | 禁用此组合（差 0.15）。浅青底上的次要文字改用 primary-dark |
| 白字压 error-text 底 | 6.47:1 | 4.5 | 达标 | danger 按钮、离线横幅 |
| success-text 作文字 | 5.02:1 | 4.5 | 达标 | 一切绿色文字用途 |
| warning-text 作文字 | 5.02:1 | 4.5 | 达标 | 一切橙黄色文字用途 |
| error-text 作文字 | 6.47:1 | 4.5 | 达标 | 一切红色文字用途 |
| success 作圆点图形 | 2.28:1 | 3 | 需补偿 | 低于非文本 3.0 阈值，靠「圆点辨识补偿」：完整度信息同时有文字版 |
| warning 作圆点图形 | 2.15:1 | 3 | 需补偿 | 同上，且三档圆点尺寸一致、位置固定，形成序列可辨 |
| error 作圆点图形 | 3.76:1 | 3 | 达标 | 三档中唯一自身达标者 |
| border 相邻 surface | 1.24:1 | — | 豁免 | 纯装饰分隔线，不承载信息，豁免 |
| border 相邻 background | 1.18:1 | — | 豁免 | 同上 |
| primary-light 相邻 surface | 1.11:1 | — | 豁免 | 纯装饰底色区块，豁免 |
| text-placeholder 在 primary-light 上 | 2.29:1 | 4.5 | **禁用此组合** | 禁用此组合（承载真实信息时）。浅青底上不得用 placeholder 级灰 |
| text-placeholder 在 surface 上 | 2.54:1 | — | 豁免 | 占位符豁免：WCAG 对「输入前的提示文字」不作正文要求，但它不得承载唯一信息 |
| disabled 按钮（placeholder 压 border 底） | 2.05:1 | — | 豁免 | 禁用态豁免：低对比正是「不可用」的视觉表达。故此档禁止用于任何可点元素 |
| 白字压 accent 底 | 5.01:1 | 4.5 | 达标 | 2026-08-26 由 #FF8A3D（2.35:1）压深至 #B4531A。任何 Accent 调整都必须重算此值 |
| 白字压 category/cat-work 底 | 3.68:1 | 4.5 | **禁用此组合** | 禁止承载白字，改用 category/cat-work-deep |
| 白字压 category/cat-house 底 | 4.23:1 | 4.5 | **禁用此组合** | 禁止承载白字，改用 category/cat-house-deep |
| 白字压 category/cat-vehicle 底 | 2.80:1 | 4.5 | **禁用此组合** | 禁止承载白字，改用 category/cat-vehicle-deep |
| 白字压 category/cat-life 底 | 2.54:1 | 4.5 | **禁用此组合** | 禁止承载白字，改用 category/cat-life-deep |
| 白字压 category/cat-service 底 | 3.53:1 | 4.5 | **禁用此组合** | 禁止承载白字，改用 category/cat-service-deep |
| 白字压 category/cat-work-deep 底 | 4.85:1 | 4.5 | 达标 | 一级类目顶栏标签选中态的正确取法 |
| 白字压 category/cat-house-deep 底 | 4.87:1 | 4.5 | 达标 | 同上 |
| 白字压 category/cat-vehicle-deep 底 | 4.83:1 | 4.5 | 达标 | 同上 |
| 白字压 category/cat-life-deep 底 | 4.82:1 | 4.5 | 达标 | 同上 |
| 白字压 category/cat-service-deep 底 | 4.81:1 | 4.5 | 达标 | 同上 |

**「需补偿」的含义**：该组合自身低于阈值，但靠非颜色通道补齐 ——
完整度圆点三档同尺寸同位置形成可辨序列，且完整度在信息卡内另有文字版。
**颜色永远不得是某项信息的唯一载体。**

## 3. 按钮（6 档）

内边距一律 **纵 12 / 横 16**，高度由内边距与字号撑出，**勿写死高度**。

| 档位 | 填充 | 文字 | 描边 | 形状 | 用途 |
|---|---|---|---|---|---|
| `primary` | color/primary | color/surface | 无 | 圆角 8 | 页面唯一主操作，一屏不得出现两个 |
| `secondary` | color/surface | color/primary | color/primary 1px | 圆角 8 | 与主操作并列的次选项，如「获取验证码」 |
| `ghost` | 无（透明） | color/primary | 无 | 圆角 8 | 弱化的辅助入口，如「用密码登录」 |
| `danger` | color/error-text | color/surface | 无 | 圆角 8 | 不可逆操作，如删除、下架 |
| `capsule` | color/primary | color/surface | 无 | 全圆角胶囊 | 表单底部通栏提交，宽度取 390×80% = 312（PRD §3.4.1） |
| `disabled` | color/border | color/text-placeholder | 无 | 圆角 8 | 本期不开放的功能占位，须同时降至 40% 不透明度以声明范围 |

- **一屏不得出现两个 `primary`**；
- `disabled` 档的对比度（2.05:1）**刻意不达标** —— 它表达的正是「不可用」，
  故此档**禁止用于任何可点元素**，且须同时降至 40% 不透明度以声明范围；
- Figma 侧这六档合成为一个 Component Set，变体属性为 `variant`。

## 4. 卡片

列表卡宽 **390 − 16 × 2 = 358**，
结构为「左侧 4px 分类色条 · 主体（标题 h3 + 摘要 small）· 右侧元数据」。

### 4.1 三态（3 档，全部有实处）

| 态 | 稿内实现 | 规格 | 说明 |
|---|---|---|---|
| 正常态 | 有 | 白底 color/surface + 圆角 lg + 阴影 0 2 8 rgba(0,0,0,0.04)，内边距 lg，内元素间距 md | card() 的唯一实现形态，14 页内全部列表卡走此档。判据 PRD §1.4.7 |
| 选中态 | 有 | 在正常态基础上加 1px color/primary-light 边框，其余一切不变（不改底色、不改阴影）。硬约束：边框不得占据布局尺寸 —— Figma 侧须设 strokesIncludedInLayout = false，CSS 侧须用 outline 或 box-shadow 描边而非 border，否则卡高会被顶高 2px，选中态与正常态在列表里相邻，一选中整列往下错位，看着像列表在抖 | 实处在「00 · Tokens 与组件」页的状态实样板（2026-09-02 补），业务页内无实处 —— 列表卡在原型内不存在「选中」交互（点击即跳详情页，无多选场景），故只有规范演示位。注意 primary-light 与 surface 的对比度仅 1.11:1（见 CONTRAST_PAIRS，纯装饰豁免），故边框不得作为选中与否的唯一线索，须同时有勾选框或其他形状标记。判据 PRD §1.4.7 |
| 下架态 | 有 | 整卡 opacity 50% + 右侧元数据槽位 lifecycle 填「已下架」 | 实处在状态实样板（2026-09-02 补）。「我的发布」页那张卡的 lifecycle 槽位只填「已下架」文字（见 CARD_META_SLOTS）、未施加 opacity 折扣 —— 业务页保持全亮度是刻意的：那一页的卡带「刷新重发/删除」操作组，压到 50% 会连操作入口一起压暗。opacity 50% 会把卡内所有文字的实际对比度砍半（text-primary 14.68:1 降至约 4.9:1），仍在 AA 线上，但**不得再叠加任何其他降透明度处理**。判据 PRD §1.4.7 |

### 4.2 右侧元数据三槽位

同一个位置在三个页面承载三种不同语义，故拆为三个具名槽位。
字阶与颜色**一律取自真源表，调用点无从自定** —— 否则「元数据一律 caption/primary」
这条规格就又只存在于注释里了。

| 槽位 | 图层名 | 字阶 | 颜色 | 用途 |
|---|---|---|---|---|
| `supplyDemand` | `_meta-supply-demand` | caption | color/primary | 供需属性：资源 / 需求。列表页与收藏页用，与页签当前筛选值必须一致 |
| `lifecycle` | `_meta-lifecycle` | caption | color/primary | 生命周期状态：在架 / 已下架 / 草稿（PRD §8.6 状态机）。仅「我的发布」用 |
| `freshness` | `_meta-freshness` | caption | color/primary | 时间新鲜度：今天更新 / 昨天更新 / 3 天前。空状态兜底过程态的结果卡用 |

这三格是**卡片右侧元数据**，不是标签组件：它们是纯文本、无容器无底色。
套上标记容器会把它们提到与标题同级的视觉权重。

## 5. 标签与标记（四族）

PRD §1.7 把「标签/徽章」列为必做组件，但稿内的 11 处实物**并非同类**。
按语义拆开后只有 A 族内部真的不一致、需要归一；另三族要么样本量为 1、
要么本来就已自洽、要么根本不是标签。故按族分栏，不硬压成同一条规格。

### 5.1 A 族 · 可点选择胶囊

| 项 | 取值 |
|---|---|
| 内边距 | 纵 4 / 横 8 |
| 圆角 | 全圆角（full） |
| 字阶 | caption |
| 未选中 | 底 `color/background` + 字 `color/text-secondary` |
| 选中 | 底 `color/primary`（代表某分类时取该分类 `-deep` 档）+ 字 `color/surface` |
| 实处 | `_radius-<档>` · `_sd-<供需>` · `_lv1-<分类>` · `_mode-<模式>` |

一排中选一个的可点筛选项；点击后仅换选中态，不触发页面跳转。**未选中态已归一** —— 回改前三种画法并存
（无填充无描边 / 无填充+border 描边 / background 填充），三者在同一屏里
会被读成三种不同的可点性强弱，而它们其实是同一件事。

### 5.2 B 族 · 静态标记

| 项 | 取值 |
|---|---|
| 内边距 | 纵 2（TIGHT_GAP）/ 横 4 |
| 圆角 | 4 |
| 字阶 | caption |
| 填充 / 文字 | `color/accent` / `color/surface` |
| 实处 | `_ai-tag` |

盖在内容旁的不可点标记，如「AI 推测」。当前全稿仅 1 处实物，规格系新立

### 5.3 C 族 · 浮层提示条

| 项 | 取值 |
|---|---|
| 内边距 | 纵 4 / 横 8 |
| 圆角 | 4 |
| 填充 / 文字 | `color/surface` / `color/primary-dark` |
| 字阶 | `_map-caption` = caption · `_hint` = small |
| 实处 | `_map-caption` · `_hint` |

悬浮在地图之上的一句话提示，白底以从底图中浮出；容器取值两处一致，字阶两处不同。字阶如实登记两值而非假装已统一：1px 之差不值得改动已验收画面，
且 `_hint` 是一句需读完的操作指引、`_map-caption` 是一句状态说明，前者略大有其道理。

### 5.4 D 族 · 圆角 full 但不是标签

实处：`_sheet-reset` · `_sheet-confirm` · `_filter-summary` · `_overlay-cats`

圆角 full 但语义为按钮或容器，规格不由本表定义；按钮档位一律查 BUTTON_SPECS

登记它们是为了防止下一个人再把它们数进标签里。**其中前两个是手搓按钮、
绕过了按钮真源表**，改走真源表列为遗留项。

## 6. 圆点（4 档）

这是**登记**而非硬统一：8px 的未读红点与 12px 的 Pin 角标本就不该同尺寸 ——
前者是余光可见即可的存在性提示，后者要在花花绿绿的底图上仍能辨色。
强行统一成一档会牺牲其中一头。

| 档位 | 边长 px | 实处 | 用途 |
|---|---|---|---|
| `unread` | 8 | `_bell-badge` · `_unread` · `_summary-dot` | 存在性提示：只需被余光扫到，不承载可辨识的内容 |
| `status` | 10 | `_sd-dot` · `_mi-comp-dot` · `_completeness-<档>` | 状态色点：需在正文旁清晰辨色（完整度三档、供需实心/空心） |
| `onMap` | 12 | `_legend-dot` · `_layer-<分类> 内的 _dot` | 地图与图例上的分类色点：底图有色，须更大才辨得出色相 |
| `pinBadge` | 12 | `_completeness-<档>（Pin 角标）` | Pin 右上角完整度角标：叠在分类色圆底上，须压住底色 |

## 7. 输入框（两套并列）

**为什么允许两套而不归一**：表单字段与导航栏搜索框在 5 个维度上都不同，
而这个不同是有依据的 —— 导航栏搜索框取 32 高有 PRD 明文依据
（导航栏 48 上下各留 8 呼吸；横向命中面积远超 44×44 等效值，不适用图标按钮的
44px 下限）。强行压成一套，要么让表单框缩到 32 破触控下限、要么让导航栏被撑破。

⚠️ **样本量如实标注**：状态表里标「业务页有实处」的是从既有产品画面归纳的；
标「仅状态实样板」的在 19 个业务页面里**零实处**、规格是本轮新拟的，
只在「00 · Tokens 与组件」页有一处规范演示位可供比对形态。不要以为它们是从产品画面里量出来的。

### formField

| 项 | 取值 |
|---|---|
| 高度 | 44 |
| 圆角 | 8 |
| 横向内边距 | 12 |
| 内元素间距 | 4 |
| 标签 | small / `color/text-secondary` |
| 输入文字 | body |
| 实处 | `field/<字段名> 内的 _input（可键入）` · `field/<字段名> 内的 _select（开选择器，框内右端多一个 ›）` |

表单字段，6 处实处（登录页 3 + 发布页 2 + 地址补全 1）。44 高即 PRD §1.8 触控下限，宽度默认 CANVAS.w - lg*2 = 358（页面用 lg 内边距时的可用宽），xl 内边距的登录页须显式传宽

| 状态 | 样本 | 填充 | 描边 | 文字 | 说明 |
|---|---|---|---|---|---|
| `default` | 业务页有实处 | `color/surface` | `color/border` 1px | `color/text-placeholder` | 空值且未聚焦：框内是 placeholder 灰字 |
| `filled` | 业务页有实处 | `color/surface` | `color/border` 1px | `color/text-primary` | 已有值：同一位置靠色阶从灰转实，与 selectField 已选态同口径 |
| `focus` | 仅状态实样板 | `color/surface` | `color/primary` 1.5px | `color/text-primary` | 光标在框内：换主色描边 + 加粗，不靠颜色单通道 |
| `error` | 仅状态实样板 | `color/surface` | `color/error-text` 1.5px | `color/text-primary` | 校验失败：描边换色 + 框下必配一行 caption 错误文案（_field-error） |
| `disabled` | 仅状态实样板 | `color/background` | `color/border` 1px | `color/text-placeholder` | 不可编辑：底色降为 background；标签不降级，仍须读得清 |

### navSearch

| 项 | 取值 |
|---|---|
| 高度 | 32 |
| 圆角 | 全圆角（full） |
| 横向内边距 | 12 |
| 内元素间距 | 4 |
| 标签 | 无独立标签 |
| 输入文字 | small |
| 实处 | `_nav-search（内含 _search-icon 14 / _search-text / 激活时 _search-clear 12）` |

导航栏内嵌搜索框，1 处实处（home 系列顶栏）。32 高有 PRD :1069 明文依据，不适用 44px 下限；无独立标签（占位文案即引导），宽度由调用方 layoutGrow=1 拉满

| 状态 | 样本 | 填充 | 描边 | 文字 | 说明 |
|---|---|---|---|---|---|
| `default` | 业务页有实处 | `color/background` | `color/border` 1px | `color/text-placeholder` | 无关键词：图标与文案同为 placeholder 灰 |
| `active` | 业务页有实处 | `color/background` | `color/primary` 1px | `color/text-primary` | 已输入关键词：主色描边 + 实色文字 + 右端补清空叉，让「正在过滤」在顶栏就有回执 |

**三态的取值依据（都不靠颜色单通道）**：`focus` 换主色描边**且**加粗到 1.5；
`error` 描边取 `error-text`（作文字达标）而非 `error`（作图形仅需补偿），
并**强制配一行错误文案** —— 颜色之外必须有文字通道，色盲用户才读得到哪里错了；
`disabled` 底色降为 `background`，但**标签色不降级**，标签仍须读得清。

## 8. 空态与骨架屏

### 8.1 空态三档

鸭子 IP 的形态是**由尺寸单向推导**的（≥96 full / 64–95 compact / <64 mini），
故这三个数字不只是「多大」，它们同时决定了用哪一套形。谁若把 40 顺手改成 64，
形会整体换掉而看不出是为什么 —— 这是本表存在的理由。

| 档位 | 鸭子 px | 形档 | 主文案字阶 | 主文案色 | 纵向留白 | 样本 | 用途 |
|---|---|---|---|---|---|---|---|
| `inline` | 40 | mini | caption | `color/text-secondary` | 24 | 有实处（4 处） | 列表内嵌收尾态「没有更多了」（PRD §6.7 :1291）。40 落 mini 档：它是页脚配角，用 96 的完整版会抢主列表的视觉重心 |
| `loading` | 64 | compact | body | `color/text-primary` | — | 有实处（1 处） | 注意，非空态：兜底过程态 0–1 秒骨架档的缺省图（PRD §6.4.4）。64 落 compact 档，既区别于终态的 96，又不至于小到看成页脚 |
| `terminal` | 96 | full | h3 | `color/text-primary` | 24 | 规格系新立（稿内唯一实处未走构造器，见上） | 整页空态：一条都没有，页面重心就是它。96 是 full 档下限，PRD :129 明写完整版用于「空态/缺省图（§6.4.4）」 |

⚠️ `loading` 档**不是空态**：它说「等一下」，另两档说「没有了」，语义相反。
放同一张表是因为三档共用同一个鸭子构造器与同一套尺寸推导规则，
分表登记会让「64 属于哪一档」再次无处可查。

### 8.2 骨架屏

复刻列表卡的三段结构（分类色条位 + 标题行 + 摘要行），而非画一个空灰矩形 ——
骨架屏的语义是「结构已定、内容未到」，只画灰块看不出将要填什么。

| 部件 | 尺寸 | 填充 |
|---|---|---|
| 色条位 | 4 × 44，圆角 4 | `color/border` |
| 标题条 | 180 × 16，圆角 4 | `color/border` |
| 摘要条 | 120 × 12，圆角 4 | `color/border` |
| 卡容器 | 内边距 16，圆角 12 | `color/surface` |

- 灰阶一律走 `color/border`（Token 内已有的最浅可见灰），**不新造 skeleton 专用 Token** ——
  骨架屏是同一套灰阶的临时用法，不是新的语义色；
- **条数为 3**，是 PRD 逐字给的值，不取整不省略；
- **骨架屏与真实内容不得同时可见**：骨架屏移除后内容才起淡入（见动效 `fade` 档）。

## 9. 动效（6 档）

| 档位 | 场景 | 时长 | 缓动 | 实现口径 | 判据 |
|---|---|---|---|---|---|
| `page` | 页面切换 | 260ms | ease-out | 前进右侧滑入 / 返回左侧滑出 | PRD §1.4.8 |
| `sheet` | 弹窗/抽屉 | 240ms | spring | 底部上滑；遮罩渐显单列见 mask 档 | PRD §1.4.8 / §6.9 |
| `mask` | 遮罩渐显 | 180ms | ease-out | 不透明度 0 → 40%，与 sheet 同时起步 | PRD §1.4.8 |
| `press` | 按钮反馈 | 80ms | ease-out | 按下缩放至 0.97，释放回弹 | PRD §1.4.8 |
| `fade` | 内容淡入 | 180ms | ease-out | 骨架屏占位 → 内容淡入，两者不叠加 | PRD §1.4.8 |
| `layer` | 图层切换 | 200ms | ease-out | 分类 Tab 切换 Marker 颜色重刷；资源/需求切换形状立即变、颜色渐变 | PRD §6.8 |

- `ease` 保留 PRD 原词，**不擅自换成 CSS 或 Flutter 的具名曲线** ——
  `spring` 在 Flutter 是 `SpringSimulation`、在 CSS 里根本没有对应值，
  替设计师把语义词落成具体参数超出「冻结契约」的范围；
- 动效**不入 Figma Variables**：`createVariable` 只收 COLOR / FLOAT / STRING / BOOLEAN，
  没有时间与缓动类型。故动效契约以画布 annotation + 本表承载。

## 10. 组件库（20 个 master）

### 10.1 Component Set 与变体轴

| Set | 变体属性 | 变体数 |
|---|---|---|
| `shell/bottom-tab` | `variant` | 3 |
| `ui/button` | `variant` | 6 |
| `ui/pin` | `category` · `supply` | 10 |

Figma 的变体属性**只能**从图层名读，格式硬性 `Property=Value`、多属性用 `, ` 分隔。
故同一个组件有三套并存的命名口径，各只服务一件事：
**扁平名**（如 `ui/button/primary`）是代码内部的组件身份；
**变体名**（如 `variant=primary`）只存在于 master 图层名上；
**画布名**（如 `btn/primary/发布`）是 Instance 图层名。

Set 之外另有 **1 个独立 master**（无变体轴，不参与合成）。

### 10.2 逐组件契约

以下 20 条是各 Component master 的 description 原文，
**由 `code.js` 的 `describeComponents()` 从真源表现算**（每条里的间距、圆角、
配色都不是手写值）。契约讲的是「这个组件在任何页都成立的约束」；
「这一页为什么这么设计」那类页级判据留在画布标注卡里，同一条信息不两处写。

### `shell/status-bar`

- 【用途】iOS 状态栏占位条，每个 390×844 画框的第一个子节点
- 【规格】390×44，底 color/surface，左右内边距 16，两端对齐
- 【不可改】纯视觉留白，内容为固定假值（9:41 与信号占位符）；真机由系统绘制，Flutter 侧对应 SafeArea 顶部内边距，不要照此实现
- 【判据】画布规格 390×844（iPhone 14 逻辑分辨率）

### `shell/bottom-tab/鸭圈`

- 【用途】底部主导航 · 当前高亮「鸭圈」。三个态各自一个 master，切页时换 Instance 而非改 Instance 内部
- 【规格】390×64，底 color/surface + 顶部 1px color/border；三个单元格各 130 宽 × 44 高，图标 24 + 间隙 4 + caption 文字
- 【无障碍】单元格高 44 是「透明扩展命中区」的产物：图标与文字的视觉尺寸位置全不变，只把承载点击的容器撑到 44 以满足 PRD §1.8，栏高仍为 64
- 【不可改】三键图标一律矢量（tab-map / 鸭子 IP / tab-person），禁用 emoji —— emoji 由系统字体渲染，选中时不随 paintOf(role) 变主色，且风格与矢量断裂
- 【判据】PRD §1.8 触控区 / §1.4.1.2 IP 微缩版消费位置

### `shell/bottom-tab/发布`

- 【用途】底部主导航 · 当前高亮「发布」。三个态各自一个 master，切页时换 Instance 而非改 Instance 内部
- 【规格】390×64，底 color/surface + 顶部 1px color/border；三个单元格各 130 宽 × 44 高，图标 24 + 间隙 4 + caption 文字
- 【无障碍】单元格高 44 是「透明扩展命中区」的产物：图标与文字的视觉尺寸位置全不变，只把承载点击的容器撑到 44 以满足 PRD §1.8，栏高仍为 64
- 【不可改】三键图标一律矢量（tab-map / 鸭子 IP / tab-person），禁用 emoji —— emoji 由系统字体渲染，选中时不随 paintOf(role) 变主色，且风格与矢量断裂
- 【判据】PRD §1.8 触控区 / §1.4.1.2 IP 微缩版消费位置

### `shell/bottom-tab/我的`

- 【用途】底部主导航 · 当前高亮「我的」。三个态各自一个 master，切页时换 Instance 而非改 Instance 内部
- 【规格】390×64，底 color/surface + 顶部 1px color/border；三个单元格各 130 宽 × 44 高，图标 24 + 间隙 4 + caption 文字
- 【无障碍】单元格高 44 是「透明扩展命中区」的产物：图标与文字的视觉尺寸位置全不变，只把承载点击的容器撑到 44 以满足 PRD §1.8，栏高仍为 64
- 【不可改】三键图标一律矢量（tab-map / 鸭子 IP / tab-person），禁用 emoji —— emoji 由系统字体渲染，选中时不随 paintOf(role) 变主色，且风格与矢量断裂
- 【判据】PRD §1.8 触控区 / §1.4.1.2 IP 微缩版消费位置

### `ui/button/primary`

- 【用途】页面唯一主操作，一屏不得出现两个
- 【规格】填充 color/primary｜文字 color/surface｜圆角 8｜内边距 12/16（纵/横）
- 【不可改】高度由内边距与字号撑出，勿写死；文案改写走 Instance 内 TEXT，勿脱离 master
- 【判据】PRD §1.4.6

### `ui/button/secondary`

- 【用途】与主操作并列的次选项，如「获取验证码」
- 【规格】填充 color/surface｜文字 color/primary｜描边 color/primary 1px｜圆角 8｜内边距 12/16（纵/横）
- 【不可改】高度由内边距与字号撑出，勿写死；文案改写走 Instance 内 TEXT，勿脱离 master
- 【判据】PRD §1.4.6

### `ui/button/ghost`

- 【用途】弱化的辅助入口，如「用密码登录」
- 【规格】填充 无（透明）｜文字 color/primary｜圆角 8｜内边距 12/16（纵/横）
- 【不可改】高度由内边距与字号撑出，勿写死；文案改写走 Instance 内 TEXT，勿脱离 master
- 【判据】PRD §1.4.6

### `ui/button/danger`

- 【用途】不可逆操作，如删除、下架
- 【规格】填充 color/error-text｜文字 color/surface｜圆角 8｜内边距 12/16（纵/横）
- 【不可改】高度由内边距与字号撑出，勿写死；文案改写走 Instance 内 TEXT，勿脱离 master
- 【判据】PRD §1.4.6

### `ui/button/capsule`

- 【用途】表单底部通栏提交，宽度取 390×80% = 312（PRD §3.4.1）
- 【规格】填充 color/primary｜文字 color/surface｜全圆角胶囊｜内边距 12/16（纵/横）
- 【不可改】高度由内边距与字号撑出，勿写死；文案改写走 Instance 内 TEXT，勿脱离 master
- 【判据】PRD §1.4.6

### `ui/button/disabled`

- 【用途】本期不开放的功能占位，须同时降至 40% 不透明度以声明范围
- 【规格】填充 color/border｜文字 color/text-placeholder｜圆角 8｜内边距 12/16（纵/横）
- 【不可改】高度由内边距与字号撑出，勿写死；文案改写走 Instance 内 TEXT，勿脱离 master
- 【无障碍】此档对比度刻意不达标（它表达「不可用」），故禁止用于任何可点元素
- 【判据】PRD §1.4.6

### `ui/pin/cat-work/resource`

- 【用途】地图 Marker · 工作（资源态）
- 【规格】40×40，分类色 #3B82F6（category/cat-work）｜分类色实心圆 + 白色图标
- 【命中区】视觉 40×40 落在 WCAG 2.5.8 的地图 pin「Essential」豁免内；实现侧命中区须外扩至 ≥44×44，不得靠放大图形达标（见首页主态标注）
- 【结构约束】Figma 禁止对 Instance 增删子节点。选中态（48×48 + 阴影）与带完整度角标态属结构差异，一律回退 pinRaw 原生构造，不复用本 master
- 【不可改】Marker 只承载两类信息：分类（底色+图标）+ 供需态（实心/空心）。完整度由点击后的信息卡承载，不回到 Marker
- 【判据】PRD §6.4.2 / §1.4.3

### `ui/pin/cat-work/demand`

- 【用途】地图 Marker · 工作（需求态）
- 【规格】40×40，分类色 #3B82F6（category/cat-work）｜白底 + 分类色 2px 描边 + 分类色图标，右上角 16×16 的「?」角标标明供需
- 【命中区】视觉 40×40 落在 WCAG 2.5.8 的地图 pin「Essential」豁免内；实现侧命中区须外扩至 ≥44×44，不得靠放大图形达标（见首页主态标注）
- 【结构约束】Figma 禁止对 Instance 增删子节点。选中态（48×48 + 阴影）与带完整度角标态属结构差异，一律回退 pinRaw 原生构造，不复用本 master
- 【不可改】Marker 只承载两类信息：分类（底色+图标）+ 供需态（实心/空心）。完整度由点击后的信息卡承载，不回到 Marker
- 【判据】PRD §6.4.2 / §1.4.3

### `ui/pin/cat-house/resource`

- 【用途】地图 Marker · 房屋（资源态）
- 【规格】40×40，分类色 #8B5CF6（category/cat-house）｜分类色实心圆 + 白色图标
- 【命中区】视觉 40×40 落在 WCAG 2.5.8 的地图 pin「Essential」豁免内；实现侧命中区须外扩至 ≥44×44，不得靠放大图形达标（见首页主态标注）
- 【结构约束】Figma 禁止对 Instance 增删子节点。选中态（48×48 + 阴影）与带完整度角标态属结构差异，一律回退 pinRaw 原生构造，不复用本 master
- 【不可改】Marker 只承载两类信息：分类（底色+图标）+ 供需态（实心/空心）。完整度由点击后的信息卡承载，不回到 Marker
- 【判据】PRD §6.4.2 / §1.4.3

### `ui/pin/cat-house/demand`

- 【用途】地图 Marker · 房屋（需求态）
- 【规格】40×40，分类色 #8B5CF6（category/cat-house）｜白底 + 分类色 2px 描边 + 分类色图标，右上角 16×16 的「?」角标标明供需
- 【命中区】视觉 40×40 落在 WCAG 2.5.8 的地图 pin「Essential」豁免内；实现侧命中区须外扩至 ≥44×44，不得靠放大图形达标（见首页主态标注）
- 【结构约束】Figma 禁止对 Instance 增删子节点。选中态（48×48 + 阴影）与带完整度角标态属结构差异，一律回退 pinRaw 原生构造，不复用本 master
- 【不可改】Marker 只承载两类信息：分类（底色+图标）+ 供需态（实心/空心）。完整度由点击后的信息卡承载，不回到 Marker
- 【判据】PRD §6.4.2 / §1.4.3

### `ui/pin/cat-vehicle/resource`

- 【用途】地图 Marker · 车辆（资源态）
- 【规格】40×40，分类色 #F97316（category/cat-vehicle）｜分类色实心圆 + 白色图标
- 【命中区】视觉 40×40 落在 WCAG 2.5.8 的地图 pin「Essential」豁免内；实现侧命中区须外扩至 ≥44×44，不得靠放大图形达标（见首页主态标注）
- 【结构约束】Figma 禁止对 Instance 增删子节点。选中态（48×48 + 阴影）与带完整度角标态属结构差异，一律回退 pinRaw 原生构造，不复用本 master
- 【不可改】Marker 只承载两类信息：分类（底色+图标）+ 供需态（实心/空心）。完整度由点击后的信息卡承载，不回到 Marker
- 【判据】PRD §6.4.2 / §1.4.3

### `ui/pin/cat-vehicle/demand`

- 【用途】地图 Marker · 车辆（需求态）
- 【规格】40×40，分类色 #F97316（category/cat-vehicle）｜白底 + 分类色 2px 描边 + 分类色图标，右上角 16×16 的「?」角标标明供需
- 【命中区】视觉 40×40 落在 WCAG 2.5.8 的地图 pin「Essential」豁免内；实现侧命中区须外扩至 ≥44×44，不得靠放大图形达标（见首页主态标注）
- 【结构约束】Figma 禁止对 Instance 增删子节点。选中态（48×48 + 阴影）与带完整度角标态属结构差异，一律回退 pinRaw 原生构造，不复用本 master
- 【不可改】Marker 只承载两类信息：分类（底色+图标）+ 供需态（实心/空心）。完整度由点击后的信息卡承载，不回到 Marker
- 【判据】PRD §6.4.2 / §1.4.3

### `ui/pin/cat-life/resource`

- 【用途】地图 Marker · 生活（资源态）
- 【规格】40×40，分类色 #10B981（category/cat-life）｜分类色实心圆 + 白色图标
- 【命中区】视觉 40×40 落在 WCAG 2.5.8 的地图 pin「Essential」豁免内；实现侧命中区须外扩至 ≥44×44，不得靠放大图形达标（见首页主态标注）
- 【结构约束】Figma 禁止对 Instance 增删子节点。选中态（48×48 + 阴影）与带完整度角标态属结构差异，一律回退 pinRaw 原生构造，不复用本 master
- 【不可改】Marker 只承载两类信息：分类（底色+图标）+ 供需态（实心/空心）。完整度由点击后的信息卡承载，不回到 Marker
- 【判据】PRD §6.4.2 / §1.4.3

### `ui/pin/cat-life/demand`

- 【用途】地图 Marker · 生活（需求态）
- 【规格】40×40，分类色 #10B981（category/cat-life）｜白底 + 分类色 2px 描边 + 分类色图标，右上角 16×16 的「?」角标标明供需
- 【命中区】视觉 40×40 落在 WCAG 2.5.8 的地图 pin「Essential」豁免内；实现侧命中区须外扩至 ≥44×44，不得靠放大图形达标（见首页主态标注）
- 【结构约束】Figma 禁止对 Instance 增删子节点。选中态（48×48 + 阴影）与带完整度角标态属结构差异，一律回退 pinRaw 原生构造，不复用本 master
- 【不可改】Marker 只承载两类信息：分类（底色+图标）+ 供需态（实心/空心）。完整度由点击后的信息卡承载，不回到 Marker
- 【判据】PRD §6.4.2 / §1.4.3

### `ui/pin/cat-service/resource`

- 【用途】地图 Marker · 服务（资源态）
- 【规格】40×40，分类色 #EC4899（category/cat-service）｜分类色实心圆 + 白色图标
- 【命中区】视觉 40×40 落在 WCAG 2.5.8 的地图 pin「Essential」豁免内；实现侧命中区须外扩至 ≥44×44，不得靠放大图形达标（见首页主态标注）
- 【结构约束】Figma 禁止对 Instance 增删子节点。选中态（48×48 + 阴影）与带完整度角标态属结构差异，一律回退 pinRaw 原生构造，不复用本 master
- 【不可改】Marker 只承载两类信息：分类（底色+图标）+ 供需态（实心/空心）。完整度由点击后的信息卡承载，不回到 Marker
- 【判据】PRD §6.4.2 / §1.4.3

### `ui/pin/cat-service/demand`

- 【用途】地图 Marker · 服务（需求态）
- 【规格】40×40，分类色 #EC4899（category/cat-service）｜白底 + 分类色 2px 描边 + 分类色图标，右上角 16×16 的「?」角标标明供需
- 【命中区】视觉 40×40 落在 WCAG 2.5.8 的地图 pin「Essential」豁免内；实现侧命中区须外扩至 ≥44×44，不得靠放大图形达标（见首页主态标注）
- 【结构约束】Figma 禁止对 Instance 增删子节点。选中态（48×48 + 阴影）与带完整度角标态属结构差异，一律回退 pinRaw 原生构造，不复用本 master
- 【不可改】Marker 只承载两类信息：分类（底色+图标）+ 供需态（实心/空心）。完整度由点击后的信息卡承载，不回到 Marker
- 【判据】PRD §6.4.2 / §1.4.3

## 11. 本设计系统当前的已知缺口

如实登记，避免接棒人误以为「文档里有的稿里就一定有」：

| 缺口 | 现状 | 处置 |
|---|---|---|
| Pin 变体覆盖 | 5 类 × 供需 2 态 = 10 种走组件；**选中态（48×48）与完整度角标态不走组件** | Figma 禁止对 Instance 增删子节点，这两态属结构差异，API 限制改不了。实现侧自行构造 |
| 卡片选中态 / 下架态 | 2026-09-02 已补：`card()` 支持三态，实样在「00 · Tokens 与组件」页；**业务页内仍无实处** | 见 §4.1 的说明 —— 选中态在原型内没有交互载体（列表卡点击即跳详情），实现侧引入多选时按登记表构造 |
| 输入框 focus / error / disabled | 2026-09-02 已补：`field()` 五态实样在「00 · Tokens 与组件」页；**业务页内仍无实处** | 见 §7，属新拟规格而非归纳 |
| 暗色模式 | 不做 | PRD §1.7 明写本期不做 |
| 多行输入（textarea） | 稿内零实现 | 发布页当前是四个单行框；补它属改产品形态，不属补规范 |
| 动效进 Variables | 不做 | Figma 变量类型不支持时间与缓动 |
| 画布文本挂 Text Style | Style 已注册，**既有文本节点尚未挂上 `textStyleId`** | 遗留项 |
| `_sheet-reset` / `_sheet-confirm` | 手搓按钮，绕过按钮真源表 | 遗留项：改走真源表须给按钮补一档「满宽 grow + full 圆角」 |
