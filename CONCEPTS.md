# Concepts

Shared domain vocabulary for this project — entities, named processes, and status concepts with project-specific meaning. Seeded with core domain vocabulary, then accretes as ce-compound and ce-compound-refresh process learnings; direct edits are fine. Glossary only, not a spec or catch-all.

## 文档交付链

### 权威源

需求口径的唯一裁定处。同一条口径在项目内可能出现在多个文件里，但只有权威源的表述有效，其余处一律视为副本；发生冲突时以权威源为准并回改冲突方，而不是反向修改权威源。

一份文档是不是权威源由它在传导链中的位置决定，与格式无关 —— 对外交付的正式文件通常反而是派生交付件。权威源自身也要接受同一套核验，不能因为它是源头就假定自洽。

### 派生交付件

由权威源经生成脚本产出、供对外提交或评审的成品文件。派生交付件不是编辑对象 —— 直接改它等于制造一个下次重生就会被覆盖的孤立修改。

派生交付件与权威源之间的时间差是一种静默缺陷：两个文件各自都是合法且内部自洽的，没有任何工具会报错，只有把它们放在一起逐条比对才看得出来。

### 生成脚本

把权威源渲染成派生交付件的程序。按内容来源分两型，动手改内容前必须先判明是哪一型：

- **MD 驱动型** —— 运行时读取权威源文本，改权威源即生效，但仍须有人重跑；
- **硬编码型** —— 文案以字面量写在脚本内部，从不读取任何权威源。此型的权威源退化为纯留档载体，不再具备驱动力，必须与脚本文案成对修改。

### 落盘核对

用提取出的交付件实际文本，逐条比对本轮改写过的原句片段，以确认改动真的进了成品文件。时间戳、文件大小、脚本退出码都不算落盘证据。

核对要点有三：探针要正反成对（新内容命中、旧标题消失）；特征句必须从权威源逐字复制，含行内标记；探针报未命中时先怀疑探针本身，辨别方式是打印实际落盘文本而非反复调探针直到变绿。

### 闸门

对交付件做机器校验的固化脚本，把「必须出现的规格条目」「不允许出现的痕迹词」这类约束变成可重复执行的检查。

闸门的覆盖面是按某一时刻的文档结构写死的，不会随文档增长自动扩展。文档长出新章节后，缺失的新章节不会触发任何告警，闸门反而给出全绿的假安心 —— 因此新增章节必须同步扩容闸门清单，且描述闸门的文字（段数、条目数、覆盖范围）也要回代码点，不能沿用留档转述。

## Flagged ambiguities

- 「同步」曾被同时用于指「改完权威源」与「重生派生交付件并核对落盘」两件事 —— 这是两个步骤，前者只是后者的必要条件。
