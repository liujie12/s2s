# Concepts

Shared domain vocabulary for this project — entities, named processes, and status concepts with project-specific meaning. Seeded with core domain vocabulary, then accretes as ce-compound and ce-compound-refresh process learnings; direct edits are fine. Glossary only, not a spec or catch-all.

## 文档交付链

### 权威源

需求口径的唯一裁定处。同一条口径在项目内可能出现在多个文件里，但只有权威源的表述有效，其余处一律视为副本；发生冲突时以权威源为准并回改冲突方，而不是反向修改权威源。

一份文档是不是权威源由它在传导链中的位置决定，与格式无关 —— 对外交付的正式文件通常反而是派生交付件。权威源自身也要接受同一套核验，不能因为它是源头就假定自洽。

权威源是分类目的，不是整份文档的属性：同一份文档可以是某类事项的权威源、同时是另一类的副本，因此裁定前要先问「这一类事项的口径源是哪一份」。**同一原则适用于待办项**：一条待办的裁定必须落在该类事项的权威源里，而不是就地写在登记它的那张表旁。而一条纪律写下结论却不写适用边界时，迟早会被用在边界外 —— 故权威源的声明须一并说明它覆盖哪一类内容、不覆盖哪一类。

### 派生交付件

由权威源经生成脚本产出、供对外提交或评审的成品文件。派生交付件不是编辑对象 —— 直接改它等于制造一个下次重生就会被覆盖的孤立修改。

派生交付件与权威源之间的时间差是一种静默缺陷：两个文件各自都是合法且内部自洽的，没有任何工具会报错，只有把它们放在一起逐条比对才看得出来。

### 生成脚本

把权威源渲染成派生交付件的程序。按内容来源分两型，动手改内容前必须先判明是哪一型：

- **MD 驱动型** —— 运行时读取权威源文本，改权威源即生效，但仍须有人重跑；
- **硬编码型** —— 文案以字面量写在脚本内部，从不读取任何权威源。此型的权威源退化为纯留档载体，不再具备驱动力，必须与脚本文案成对修改。

### 落盘核对

用提取出的交付件实际文本，逐条比对本轮改写过的原句片段，以确认改动真的进了成品文件。时间戳、文件大小、脚本退出码都不算落盘证据。

核对要点有三：探针要正反成对（新内容命中、旧标题消失）；特征句必须从权威源逐字复制，含行内标记；探针报未命中时先怀疑探针本身，辨别方式是打印实际落盘文本而非反复调探针直到变绿。

### 闸门

对交付件做机器校验的固化脚本，把「必须出现的规格条目」「不允许出现的痕迹词」这类约束变成可重复执行的检查。

闸门的覆盖面是按某一时刻的文档结构写死的，不会随文档增长自动扩展。文档长出新章节后，缺失的新章节不会触发任何告警，闸门反而给出全绿的假安心 —— 因此新增章节必须同步扩容闸门清单，且描述闸门的文字（段数、条目数、覆盖范围）也要回代码点，不能沿用留档转述。

闸门守的量必须是「最先崩的那个」。同一个参数往上抬时，最显眼的指标往往余量充裕，而某个不显眼的指标已经越界；守着一条永远不会触发的线，与没有闸门等价（人工可验一侧的同一原则见可判定门禁）。放宽一条过期断言时，同一次改动里要补上原本缺失的约束，否则下一个人会重犯同一个错。

闸门自身出错时的默认落点是「通过」，而非报错 —— 这是它与普通代码的根本差异。写成「探测到违规就判失败、否则判通过」的反向结构时，探测环节因对象不存在、命令失败或权限不足而失败，与「未探测到违规」在退出码上无法区分，一律落到通过一侧；校验函数在子进程里终止也只能终止那个子进程，主流程带着空值继续把余下各项跑完并给出一份格式完整、内容全假的汇总。因此闸门必须有与通过、失败并列的**第三态跳过**，把「这项没能真正执行」显式说出来；而闸门是否有效，只能由构造应当失败的输入、确认它真的失败来证明，正向跑通不构成证据。

跳过态还需再拆一次：「本项应当判定、但前置条件缺失」与「本项按当前环境设计就不存在判定对象」是两回事，分界线是**有没有可判定的对象**，不是有没有判成功。把后者也计入跳过，会让某些环境每次运行都必然跳过、结论恒为「不知道」，而恒定报警等于没有报警，规则很快被忽略失败的惯用语绕过。四态映射到退出码是三档：通过与不适用不影响退出码，不合格一档，判不了单独一档；带理由的放行只对判不了那一档生效，对不合格无效。

闸门还有一个与自身正确性无关的失效面：**结论没有出口**。判据再严谨，若调用链上没有任何一处读它的退出码（返回值被送进日志函数、被容错兜底归零、或管道末位是恒零的命令而取错了码），其有效性为零，且现场毫无异常 —— 同一份日志、同一组绿字，唯一区别在一个没被读取的整数上。故闸门落地的第一个动作是接通「退出码 → 调用方」，而非写第一条判据；接链路之前完成的判据修补，其收益按零计算。

判据自身的检出力同样需要证明，且证明应固化为随闸门每次运行的自检，而非一次性人工负向实测：把弱判据抽成不含环境依赖的纯函数，配伴随单测同时断言两件事 —— 真实违规形态必命中（漏报向）与已知合法噪声必不命中（误报向：标准格式 ID、注释行、无语义上下文的等长串）。另有一类「形态命中但正当」的模板假值（如全同字符），不在纯函数的不命中之列，而由扫描处置侧的第二个纯函数按可论证的概率特征分支排除，该分支同样需要伴随单测。判据改形时这组自检与判据同生共存，否则判据放松后无人察觉。

## 守门测试与例外通道

### 静态守门

不运行被实现对象、直接解析书面规格（接口契约、码表、方案条款）做出的机器校验，生效与否与实现就绪无关。它守的不是「文件可解析」，而是规格内部不同位置之间的承诺对齐 —— 枚举全集与示例实际承载集合双向相等、业务码前缀与 HTTP 状态逐条对齐、纪律条款被所有该引用它的操作引用、要求的响应头真的有定义承载。这类不一致人肉必漏且没有任何编译期信号。

与它相对的是行为守门：起真实协议栈对运行中的服务发真实请求。零实现期二者双轨并行：静态轨立即生效；行为轨用绑本地回环的内存级真实服务固定测试范式，断言只针对协议形态，实现就绪后仅替换基址、断言一行不改。判据对象（规格文件）缺失是 FAIL 而非 SKIP —— 规格被删光时的全绿是假阴性。

### 守门型探针

以「守一条不变量、越界即报红」为目的的探针：它的产出是 PASS/FAIL 判定，恒绿与「真的没问题」在输出上完全一样，因此其失效模式是**静默恒绿** —— 判据写死某一时刻的结构后，对象长出新结构时它不再命中任何目标，全绿反而是假阴性。与它相对的是测量型探针。守门型探针与实机核验是两个正交维度：前者分「验证(闸门)还是测量(采集)」，后者分「离线 mock 还是真机」，一个守门型探针既可以离线跑也可以实机跑。守门型探针还有第二个恒绿通道：候选面为空 —— 统计型判据（逐条对齐、逐条引用）遍历的对象集合为空时断言无对象可判而通过，因此这类判据必须先断言待判对象的计数下限，再执行判据本身；空候选面报绿与漏检同义。

### 扫描面

一道闸门实际完整读取的文件集合 —— 由它的枚举方式、解码处理、排除登记与读失败处置共同决定，而不是由它的名字或意图描述决定。它是写入面在读取侧的镜像：写入面回答「它会动哪些数据」，扫描面回答「它真的看了哪些文件」。

它的失效模式是**静默缩水**：枚举把非 ASCII 文件名转义丢失、排除规则未锚定根级而连坐任意层级的同名目录、体积上限把大文件整体跳扫、读失败时警告后继续 —— 每一种都让闸门在比名义更小的面上报绿，而「没扫」与「扫了且干净」在输出里无法区分。因此扫描面必须自证：枚举结果断言非空、且含一个已知必在的样本作为取数正确的证据；任何未能完整读取的文件一律判失败，唯一出口是显式排除登记，评审可见。合并回主干前扫描面按合并结果态重取一次 —— 分支内报绿只证明分支自己的面是干净的。

### 测量型探针

以「采集一份数据供人下结论」为目的的探针：它的产出是一组观测值（几何、计数、体积），本身不判对错，结论由人结合基线得出。它不追求不变量，因此没有守门型探针那种恒绿失效面；它的风险在于数据保真度 —— 测出来的值若与真实分布不符（见保真维度），由它推出的结论无效甚至反向。

### 豁免登记册

判据对已知正当例外（联调后门、一次性原型资产、无副作用操作）的显式登记表：命中而未登记即 FAIL。它区别于一句话声明的两条防腐规则：每条豁免必须携带一个能在被判对象自身中反向断言的佐证（见佐证词）；登记册接受**双向核对** —— 未登记的命中 FAIL，登记了但对应内容已不存在同样 FAIL（防「东西删了、登记留着凑数」），定位用「文件 + 行内稳定子串」而非行号，避免行漂移。每条登记同时写明存在理由与删除条件；豁免命中必须打印列名，静默放过等于抹掉了这一态。

豁免的处置粒度是「命中」而非「行」：同一行里登记串与一个未登记的新命中并存时，只抵扣登记串本身，同行追加的新命中仍判失败 —— 「行包含登记串即豁免整行」会把贴着登记串写进同一文件的新泄露连坐放行，粒度每粗一级，这条免费通道就宽一分。

### 佐证词

豁免登记中携带的、必须能在被判对象自身文本里反向断言存在的词或短语。豁免理由是「无写入副作用」，就在被豁免条目的规格描述中断言该词真实出现，找不到即 FAIL。它把豁免从「登记表里的一句话声明」升级为「对象自己提供的证据」，使有副作用的条目无法靠改名混进豁免。

### 占位条款

书面规格中显式标注归属后续批次（见批次）、且当前未展开实质内容的条目。占位识别必须是「延后标记」与「无实质内容」两个信号的合取 —— 只看名字会误杀，只看内容为空会漏判。占位不参与判定但必须列名打印：「未展开而跳过」与「漏检」在静默输出里无法区分。

### 强判据与弱判据

判据按误报率分两档，**例外机制只配给弱判据**。强判据形态固定、误报近零（标准私钥头、云厂商固定前缀的访问密钥标识），命中即 FAIL，代码中不提供豁免分支 —— 给强判据开口子等于为真实事故留门。弱判据形态宽松、需语义上下文辅助（等长敏感形态串加密钥命名上下文），命中后三分支：符合可论证概率特征的模板假值予以排除、命中登记册且佐证有效则列名豁免、否则 FAIL。模板假值的排除依据必须是可论证的概率特征，而非「看起来像假的」。

## 视觉素材链

### 评审载体

提供给用户做拍板决策的渲染产物 —— 凡是用户「看着它做选择」的对照页、对照图都是。

载体失真产出的是双方都有据可依的错误决策，事后极难归因，因为校验只看产物、缺陷却在提问方式里。三条约束：只呈现产品中真实存在的规格，多给一档更舒适的尺寸等于给了一个不存在的判断依据；放大只允许最近邻，平滑插值会替用户把缺陷补平；载体里的几何必须与最终产物同源，否则就是「批准的是 A、交付的是 B」。

### 素材真源

一件视觉素材几何的唯一定义处，由生成脚本按参数产出。观感调整一律回到生成脚本改参数，不手改素材文件 —— 手改会在下次重生成时被覆盖，且与消费侧静默脱钩。

### 内联副本

运行环境读不到素材文件时，消费侧代码里存的那份与真源逐字相同的几何数据。副本是环境限制下的必要妥协而非设计缺陷，但它把「一处几何」变成「两处必须逐字相同的几何」，只能靠一致性探针守住。**代码注释里的数字同样是副本**，会以同样的方式过期，且没有任何探针会发现。

## 视觉稿实现链

### 无反馈期

从写下第一行生成代码，到第一次看见它渲染出的真实产物之间的那段时间。这段时间里写下的缺陷不会消失，只会累积到出图那一刻集中付账 —— 返工量与无反馈期的长度成正比，而与代码量关系不大。

缩短它的唯一办法是在动手写产物之前先把出图通道打通：让通道把一个最小的空产物导出落盘。通道不通就先解决通道，不要先攒画布 —— 「以后能出图」是一个会让前面所有工作作废的假设。

### 实机核验

在真实运行环境（而非离线 mock）里读取产物的实际几何与属性。与离线探针互补而非替代：布局引擎的默认行为、字体度量、渲染管线只在实机现形，mock 按公式估算的值必然与实机不同。

两侧的数字都对，但不可互换。同一个约束若要同时被两侧检查，必须分别记录两侧的值并注明哪个用于防回归、哪个用于对外规范；混用会让断言在某一侧永远报红，或让文档给出实现侧量不到的数字。

### 单页闭环

「写一页 → 出一页的图 → 定稿这一页」的推进节奏，与「写完全部页面再统一出图」相对。

它同时压掉两项成本：批量看图时注意力被摊薄、小位移在缩略图里根本不成立为缺陷；以及生成器往往只支持全量重建，任一处修复都要重跑全部页面。

### 相邻对照

同一规格的两个实例出现在同一视野内且边界相接的状态 —— 横排按钮、纵向列表卡都是。

它是**小位移缺陷成立的唯一条件**：一两个像素的尺寸偏差在孤立实例上不构成缺陷，只有旁边站着一个同规格的兄弟供人比对时才现形。故判断某处偏差要不要修，看的是附近有没有对照物，而不是偏差本身有多大；反过来，某个部件「无对照物」也是它可以豁免的正当理由，但这个理由必须写下来，因为将来它可能长出对照物。

## 架构交付链

### 批次

一项功能、接口、表或索引被安排进哪一轮交付的归属标记。它不是时间概念而是范围概念 —— 决定某物「本轮做不做」，而不是「什么时候做」。

批次标记落在条目本身而不是文档结构上：同一张接口表、同一份表清单里各条目可以属于不同批次，用一列标出，而不是拆成两份文档。这样做是因为拆分后两份文档会各自漂移，而目标态的完整性正是判断当期取舍是否合理的前提。

### 数据模型预留

一项功能延到后续批次时，本批仍然把它所需的表结构与约束建好、只留字段不写逻辑的做法。

它的判据不是「以后会不会用到」，而是「不这么做，后续批次是否必须在已有真实数据的库上改表结构」。改表结构的迁移成本与风险远高于建表时多一张中间表，且迁移一旦作用于生产数据几乎无法回滚。反过来，如果延后功能不触碰既有约束，就不预留 —— 预留是为消除迁移，不是为覆盖所有可能性。

### 验证脚手架

设计阶段为了实测自己的产物而临时搭起的、属于下一阶段的工程结构。

它与验证脚本的区别在产物里有没有需要拍板的选择：一段连库执行 SQL 再断言的脚本换个人写也差不多，不含决策；而构建配置、依赖声明、目录布局的每一项都是要评审的选型，被"验证需要"的名义提前定死。判据不看文件后缀 —— 构建配置不是业务代码，却同样在替尚不存在的工程定架构。

三条约束：优先选不留决策痕迹的验证路径，附加收益要与代价相称；跨阶段的路径要求用文件命名而非物理位置来预留，命名是零成本兼容，挪位置是提前占位；验证结束即删除脚手架，但结论不随工具消失 —— 在文档里注明结论来源与工具已移除，比留着一个没人评审过、却会被下一个人当作既成事实继承的骨架更安全。

### 渠道维 / 账号维

风控与配额计数所用键的两种性质，落在同一套限频体系里但不可互换。

渠道维限的是某条外部通道本身（短信、验证入口），与账号无关，键取该通道的天然标识；账号维限的是自然人，键必须取账号的稳定内部标识，而不是任何一种可变的登录身份。二者混用的后果是单向的：账号维错用身份维的键，会让走其他身份进来的会话整体绕开限制；渠道维错用账号键，则会让通道本身失去保护。

### R-ID

计划文档里一条编号的、可被回引的架构约束。它是各份架构文档中每一处技术选择的唯一依据源 —— 文档写了而计划里没有对应 R-ID 的选型视为无依据。

R-ID 只增不改号：既有条款内容修订时保留原号并注明修订，新增条款取下一个空号。评审、复核、需求变更产生的新约束一律以新增 R-ID 的方式回补进依据源，而不是只改文档正文 —— 否则下一轮评审读计划文档时看不到这条约束存在。

### 缺口登记项

评审留下的一条待处理架构问题，在文档里带编号登记、待后续裁定。

它的形态有四种，处理方式互不相同，而登记表的统一格式会掩盖这个差别：**真定案**（只缺一个决定，应当即定，留时点会在引用处长出条件写法这类半成品规格）；**纪律不可执行**（措辞含「必须评估」「须同步考虑」，其产物即可判定门禁，见该词条）；**问题设定不准确**（登记的措辞与真实缺口不是一回事，只能靠实读被指原文发现，是四种里唯一无法从登记表本身识别的；凡登记项涉及具体数字，实读时须先确认两个数是不是同一口径，而不是直接比大小 —— 否则会把一个正确的数字照登记改错）；**不该登记**（已在别处闭环，重复登记会产生两处待办各自以为对方在跟的落空）。

因此清表的第一步是逐项判形态而非动手补 —— 照表逐项「补上」会把错的问题补一遍，且表格全绿与真的清完了在观感上完全一致。

登记表是待裁定项的合法载体，但**裁定完成后其产物必须移出**，落到该类事项的唯一口径源与下一轮会被翻开的待做项清单旁；留在登记表内的纪律等于没写（见可判定门禁的落点要求）。

### 可判定门禁

一条纪律的可执行形态：判据、卡点、失败后果三件套齐全，能由一个不了解背景的人在指定时刻动手验一遍并得出是或否。

与之相对的是只写了要求的纪律句，其实际效力为零 —— 「已经评估过了」永远可以被声称。三件套里失败后果一列不是修饰，它是将来有人要砍掉某项时唯一能拦住他的东西。拆成多项后还须标出**真正承载目标的那一项**：一组门禁里通常只有一项挡住了核心风险，其余各防一侧，而那一项也恰好最容易被「体验优化」掉 —— 这与闸门「守的量必须是最先崩的那个」是同一条原则在人工可验与机器校验两侧的表现。

门禁的落点位置与内容质量同等重要：放在下一轮开工时会被翻开的待做项清单旁才会被读到，留在缺口登记表里等于没写 —— 文档的物理布局是纪律执行链的一部分。

机器校验一侧的对应要素是**消费方** —— 具体到哪一行代码读了门禁的退出码。它与「失败后果」不是同一件事：失败后果规定「不合格之后应当怎样」，消费方回答「谁真的会因此停下」。因此描述一道门禁时，消费方与失败判定同为必填项，为空即视为该门禁尚未落地，不计入已完成（见闸门末段）。

## 数据落库链

### 盲索引双列

一个敏感字段落库时拆成的两列：一列确定性可比、承担唯一索引与等值查询，另一列可解密、只用于展示。

它解决的是一个密码学上的不可能而非实现不便 —— 「加密存储」「唯一索引」「等值查询」三个要求不能落在同一列上，因为规范的加密每次产出不同密文，唯一约束拦不住重复、等值查询也命中不了。因此判断某字段是否需要拆列，看的是它有没有同时承担查询职责与保密职责，而不是它有多敏感。同一体系内所有敏感字段应一视同仁：只在部分字段上拆列，会让漏拆的那个成为整体强度的上限。

不可逆列必须加盐（或 pepper），否则「存哈希」只是虚假的不可逆 —— 强结构化的明文（身份证、手机号这类候选空间可枚举的数据）在裸摘要下可被穷举反推，而只检查「有没有明文列」的门禁看不出这一层。

### 写守卫

一次状态写在写入条件里附加的、决定该写是否生效的约束。

它按写入来源分两型，不可统一：**用户意图写**守版本号，冲突要让用户知道并重试；**系统派生写**（定时任务、审核流转）守合法前驱状态集，非法流转表现为影响 0 行而非报错。混用的后果是单向的 —— 系统写路径若用版本守卫，会把并发冲突以错误码的形式抛给不存在的「重试者」，而用户根本没发起这次写。

### 时间不可逆项

一项数据缺陷，其代价随时间单调累积且事后无法追补 —— 缺的是「事件发生那一刻才存在」的信息。

它与「事后可改」的缺陷分属两个成本量级：冗余列错了可以回刷，索引缺了可以补建，但状态变更时刻没记、维度快照没采、保留窗口已过期的数据，任何后续改动都无法产出。因此排期收紧时优先级由此判据决定，而不是由改动大小决定 —— 一项时间戳列的优先级可以高于一整套索引重构。

判别方式：假设该项延后一个批次，缺的东西能否由那时库里的数据重新算出来。算不出来的就是时间不可逆项。

### 保真维度

一批造出来的数据里，必须与真实数据一致、否则测量结论不成立的那些性质。

它是按测量目的确定的，不是数据本身的属性：测索引与扫描行数，保真的是键的分布；测行宽与容量，保真的是字段内容长度；测并发冲突，保真的是键的重合度。因此一批数据通常只服务一个目的，换目的必须重造 —— 复用一批为别的问题造的数据，是错误结论最常见的来源。

两类失真的危害不对等。分布失真会让数字看起来就可疑（扫描行数过少、耗时无统计意义），人会警觉；**内容长度失真则会产出一个完全合理、可以直接写进文档的反向结论**，因为比例关系的分母被悄悄改变了。后者更危险，它会真的被采纳并导向相反的决策。

守法是在测量前加前置断言，让不真实的数据自己报错，而不是事后靠人判断结论可不可信。断言的阈值由「这个数字要用来做什么」推出。

### 写入面

一个脚本实际会写到的库、表与行的集合 —— 由它内部的写语句决定，而不是由它的名字或用途描述决定。

它是调用破坏性脚本前必须先取到的东西，取法是把脚本里的写语句列一遍，而不是通读实现。风险检查的覆盖面必须由参数表决定而非由注意力决定：逐个参数问「这个值会让它写到哪里」，只要有一个参数会导致写入，就必须指向空目标。作用于固定主键的状态改写比追加插入更危险 —— 追加只在表尾堆积、逐条删得掉，定点改写精确覆盖一条既有数据且原值不留副本；同族里无守卫的那条最危险，它连「原状态曾落在哪个集合里」都没留下。破坏性目标不应有默认值，否则「忘记传参」与「故意传了危险值」后果相同，且前者不留任何决策痕迹。

写入面越界不产生指向它的错误信号：脚本正常退出、断言全绿（脚本自身预期的报错反而更容易掩盖真问题）。事前预防靠写入面清单，事后检出的唯一可靠手段是执行前后各取一次目标库的快照并主动核对；而**复核的不变量必须由写入面逐项推导** —— 脚本改了哪一列就查哪一列，挑几个「看起来能代表健康度」的聚合数字来查，与没有复核在观感上完全一致，都是全绿。回滚定点改写还多一层要求：光复原「改成了什么」不够，得先知道「原本是什么」，而这个值往往只能从造数逻辑反推。

### 承载上限反推

在没有业务规模输入时，改为从已锁死的资源约束反推「能装多少」的估算方式，与「预计多少用户 → 需要多少资源」的正向估算相对。

冷启动期项目通常拿不到可信的规模预期，正向估算此时只能靠编造输入数字，产出的精度是假的。反推则只依赖两类可核实的量：资源侧的硬约束，与实测得到的单位成本。它回答不了「会不会够用」，但能回答「在哪个量级触发瓶颈、先触发的是哪一个」—— 后者才是设计阶段真正要的结论，因为最先触及的约束往往不是最显眼的那个。

## Flagged ambiguities

- 「同步」曾被同时用于指「改完权威源」与「重生派生交付件并核对落盘」两件事 —— 这是两个步骤，前者只是后者的必要条件。
- 「探针」在本项目同时指两类脚本：**测量型**（为一次决策实测数据、产出评审载体，用完即成为历史推导记录）与**守门型**（每次改动后复跑，断言必须恒真）。两者的失败含义相反 —— 测量型的数字变了是正常的，守门型的断言红了必须停下。各处出现的「一致性探针」「离线探针」「前置断言」都属守门型，只是检查对象不同（内联副本与真源、离线估算值、测量前的数据保真度），不是彼此独立的机制。
- 「验证」曾被同时用于「确认设计可执行」与「确认设计正确」两件事。前者只证明 DDL 能建出表，后者才检查约束、生成列、并发行为是否如设计所述 —— 建表成功不蕴含设计正确。
- 「不含决策」曾被当作「无害」的同义词用在验证脚本上 —— 这是两个正交的性质。验证脚手架按「含不含需要拍板的选择」判定，写入面按「会不会写到真实数据」判定；一段不含任何决策的断言脚本仍可能改掉既有业务行。
- 「闸门」与「门禁」在项目里长期混用，现已分开：**闸门**指机器可执行的固化校验脚本（CI 校验、出包前检查、断言脚本），失败由工具报出；**可判定门禁**指人工在指定时刻按判据动手验一遍的检查项，失败由人判定。两者共享「守的必须是最先崩的那个」这条原则，但载体与失败信号完全不同 —— 项目正文中大量以「门禁」称机器脚本，读到时须按上下文回判属于哪一类。
- 「跳过 / 不判」曾被同时用于三种语义，现已分开：**占位**是判据按设计对该对象不适用（条款未展开），属通过侧但须列名；**豁免**是对象存在且判据命中、凭登记佐证认定正当，属通过侧且留证；**SKIP** 是环境不具备判定条件（扫描面不可得），是独立态，「没扫过」不等于「扫了且干净」。三者在输出中必须各自可见，不能合并成一个静默的 continue。
