# AI 工程化方法论

> **文档定位**：可迁移的 AI 协作工程方法论。每章沉淀一类业务的**工具矩阵、流程骨架、判断准则**，脱离具体项目仍然成立，可整份复制到新项目。
>
> **本文档不写什么**：不写某个项目做了什么、做到第几步、产出了哪些文件——那属于过程留档。
>
> | 文档 | 回答的问题 |
> |---|---|
> | 本文档 | **怎么做**——工具、流程、判断准则 |
> | 项目留档 | **做了什么**——按时间线的决策与进度 |
> | 规格文档 | **要做成什么**——产品与规格的权威源 |
> | 问题沉淀 | **踩过什么坑**——单个问题的完整推导 |
>
> **阅读约定**
> - ⚠️ 标记的是已付出代价的坑，不是理论风险。
> - **「举证」段的作用是给出该坑的「识别信号」**——描述这个失效在现场长什么样，好让下次能认出来；它不是项目过程记录，已去除全部项目专有名词，换项目照读即可。
> - 文中出现的具体数值一律视为**某次运行的观测值**，换环境须重测。

**章节索引**（按交付链顺序排列，前一章的产出是后一章的输入）

| 章 | 阶段 | 主题 | 适用场景 |
|---|---|---|---|
| [第一章](#第一章产品文档交付链一致性) | 产品文档 | 文档交付链一致性 | 一处口径 → 多份对外交付件，保持同步与版式合规 |
| [第二章](#第二章低保真原型骨架生成) | 低保真原型 | 原型骨架生成 | 规格文档 → 结构与跳转完整的可点击骨架 |
| [第三章](#第三章高保真原型设计) | 高保真原型 | 高保真原型设计 | 原型骨架 + 规格文档 → 可交付高保真原型 |
| [第四章](#第四章架构设计与多轮评审) | 架构设计 | 架构设计与多轮评审 | 规格文档 → 可施工的架构基线，经多视角评审收口 |
| [第五章](#第五章数据库设计与实测验证) | 数据库设计 | 数据库设计与实测验证 | 架构基线 + 规格文档 → 可执行的 DDL，及经实测取证的索引 / 容量结论 |
| [第六章](#第六章详细设计与接口契约) | 详细设计 | 详细设计与接口契约 | 架构基线 + DDL → 前后端可照此编码的契约与详细设计，并守住实现期的静默陷阱 |
| [第七章](#第七章安全与-devsecops-方案设计) | 安全方案 | 安全与 DevSecOps 方案设计 | 架构基线 + 详细设计 → 按威胁模型收口的安全规格，及可执行、有留证的门禁体系 |
| [第八章](#第八章部署架构与-iac-设计) | 部署架构 | 部署架构与 IaC 设计 | 架构基线 + 安全方案 + 资源约束 → 可一键重建的基础设施代码，及经实测核验的运行时配置 |
| [第九章](#第九章门禁落地与版本拓扑) | 门禁落地 | 门禁落地与版本拓扑 | 安全方案 + 部署代码 → 真正被调用方消费的门禁实现，及可回溯的分支 / tag 拓扑 |
| [第十章](#第十章零实现阶段的规格守门与例外通道) | 前置门禁 | 零实现阶段的规格守门与例外通道 | 书面规格（契约 / 方案条款）+ 尚无实现 → 今天就会 FAIL 的静态守门、内存级行为范式，及不被例外通道掏空的判据体系 |
| [第十一章](#第十一章编码启动前的规范基线与工具链盘点) | 编码启动前 | 规范基线与工具链盘点 | 全部设计资产定稿 → 一份可对照、可判定的编码规范，及一套先盘点后补缺、官方内置优先的工具链 |

> **追加新章节的格式要求**
> 1. 在上表登记，插入到交付链的对应位置；
> 2. 章内保持六段结构：**适用边界 → 工具矩阵 → 流程骨架 → 判断准则 → 反模式 → 落地检查清单**；
> 3. 每条准则须是「换个项目仍然成立」的表述，项目细节只能出现在举证位；
> 4. 准则要给出**可操作的判定动作**，不止给结论。

---

# 第一章：产品文档交付链一致性

**问题定义**：同一套口径需要同时存在于权威源与多份对外交付件（Word / PPT / 网页）中，且必须保持一致。

**核心主张**：**权威源与派生交付件之间的时间差是一种静默缺陷。** 没有编译失败，没有脚本报错，两个文件各自都是合法且内部自洽的，只有把它们放在一起逐条比对才看得出来。因此这类问题不能靠「出错时会被发现」兜底，只能靠流程固化。

## 1.1 适用边界

| 判断项 | 适用 | 不适用 |
|---|---|---|
| 交付件数量 | 同一口径要落到两份以上文件 | 只有一份文档，无派生件 |
| 生成方式 | 由脚本生成 | 全程手写手改 |
| 提交场景 | 对外评审、备案、登记 | 内部临时参考 |

**可用工具**：Word / PowerPoint 类 COM MCP（文本提取、格式化、转 PDF）、`python-docx` / `python-pptx`（脚本化生成与落盘核对）。

## 1.2 三个基础概念

| 概念 | 定义 | 关键性质 |
|---|---|---|
| **权威源** | 口径的唯一裁定处 | 由它在传导链中的位置决定，**与格式无关**——对外的正式文件通常反而是派生件 |
| **派生交付件** | 由权威源经脚本产出的成品 | 不是编辑对象，直接改它等于制造一个下次重生就被覆盖的孤立修改 |
| **生成脚本** | 把权威源渲染成交付件的程序 | **分两型，见下** |

### 生成脚本的两型判别

**这是本章第一个必做动作：动手改内容前先判明脚本是哪一型。**

| 型 | 内容来源 | 改权威源后 |
|---|---|---|
| **权威源驱动型** | 运行时读取权威源文本 | 改了生效，**但仍须有人重跑** |
| **硬编码型** | 文案以字面量写在脚本内部 | **改了零效果**，必须同步改脚本文案 |

> **判定动作**：搜脚本里有没有读取文件的调用（`open` / `read_text` / `readlines` 等）与权威源的文件名。一处都没有就是硬编码型。
>
> 硬编码型的权威源退化为**纯留档载体**——它仍是口径的评审对象与历史记录，但不再具备驱动力，必须与脚本文案成对修改。

## 1.3 先测绘传导拓扑，再动手改内容

> **两型判别的结论要落成一张图，而不是留在脑子里。**

多份交付件混用两型时，「改哪里能生效」对每一份都不同。不画出来就会按其中一份的行为去推断全部，这是本章事故的共同起点。

**判定动作**：动手前对每个生成脚本各搜一次读文件调用，把结果画成一张传导路径图，标明每条链路的**改动入口**。

```
口径变更
  ├─ 权威源A ──────────► 生成脚本A ──► 交付件A     改动入口：权威源A
  ├─ 权威源B（纯留档）
  │   └─ 成对同步 ────► 生成脚本B ──► 交付件B     改动入口：生成脚本B
  └─ 权威源C（纯留档）
      └─ 成对同步 ────► 生成脚本C ──► 交付件C     改动入口：生成脚本C
```

图上要能一眼看出：**哪些权威源是有驱动力的，哪些已退化为纯留档。**

⚠️ 举证：按「改权威源即可」执行，改完全部权威源后重跑脚本，部分交付件内容零变化——因为那几条链路的改动入口在脚本里。返工根因就是没有先测绘拓扑。

### 衍生：硬编码槽位用口径关键词定位，不逐行读

硬编码型脚本的文案字典通常按输出单元（页码、章节）组织，规模可观，逐行读代价高且易漏。

**判定动作**：把本轮变更的口径提炼成一组关键词，一次性搜出全部待改槽位，再逐个改。

## 1.4 准则一：「改权威源」是必要条件，不是充分条件

> **权威源驱动型不等于没有同步风险——本章最贵的事故恰恰出在这一路。**

| 步骤 | 静默失败形态 |
|---|---|
| **重跑生成脚本** | 无人重跑。脚本完全正常，它确实读权威源，只是没被调用 |
| **确认落到最终文件名** | 脚本为规避文件占用而输出到临时名，打印「已生成」，但正式交付件根本没被替换 |

⚠️ 举证：权威源已新增整整一章，派生交付件仍是旧版本，整章内容不在里面。这个缺口不是任何报错暴露的，是有人追问「两者拉齐了吗」才触发核查。若临近对外提交窗口，漏出去的后果不是返工，是提交了一份与权威源不符的正式材料。

> **一次追问顶不了下一次。** 把「重跑 → 改名 → 提取全文核对」固化成流程的价值，就在于不再依赖有人想起来问。

## 1.5 准则二：落盘核对只认交付件的实际文本

> **时间戳、文件大小、脚本退出码都不是落盘证据。**

必须把交付件的文本提取出来（含表格单元格等容易漏掉的容器），逐条比对本轮改写过的原句。三条收紧规则：

| 规则 | 原因 |
|---|---|
| **探针要正反成对** | 新内容命中 + 旧标题消失。只验新增会被「新旧并存」骗过 |
| **特征句必须从权威源逐字复制，含行内格式标记** | 脑补的形式会产生假 FAIL。生成脚本可能原样渲染格式标记 |
| **探针报未命中时，先怀疑探针** | 辨别方式是打印实际落盘文本，而不是反复调探针直到变绿 |

⚠️ 举证：一次核对里唯一的 FAIL 来自特征句被剥掉了原句中的加粗标记——逐段打印实际文本后确认文档本身完整无缺。**这一条与 3.3 阶段①的同名约定是同一条，反复踩说明它不直观。**

### 衍生一：提取文本必须递归展开嵌套容器

演示文稿类格式的文本大量藏在**分组容器**里，只遍历顶层形状会漏掉绝大部分。表格单元格、页眉页脚同理。

**判定动作**：写提取函数时先判断当前对象是不是容器，是则递归，否则取文本。写完后用「提取出的总字数」与肉眼可见内容量做一次量级校对。

### 衍生二：体量落差会让人误判「没改」

**同一处改动，在长文本权威源里视觉显著，在短文本交付件里几乎看不出来。** 这不是错觉，是两者体量常差一个数量级。

**倒推出的流程要求**：这类质疑无法用「我确实改了」回应，只能用脚本提取的命中清单回应。因此**落盘核对不是给自己看的自检，是给对方看的证据**，须在交付时随附。

### 衍生三：特征句要先验它在权威源里存在，再拿去查交付件

> **「记忆挡不住的规则，要变成脚本。」** 本准则第二条早已写明「必须逐字复制」，仍被反复违反——说明靠自律不够，须由脚本承担。

两类失败必须**分开报**，因为处置方式完全相反：

| 报告 | 含义 | 处置 |
|---|---|---|
| **脑补** | 特征句在权威源里就不存在 | 改探针的词表 |
| **缺失** | 权威源有、交付件无 | 重生交付件 |

混为一谈时，一堆「脑补」造出的假缺失会淹没真缺失。

**判定动作**：探针**先在权威源侧确认每条特征句存在**，不存在的直接报「脑补」并终止该条；只有存在的才进入交付件比对。

⚠️ 一个尤其阴险的形态：**跨行内格式标记的句子**在权威源里匹配不上，却在交付件里能匹配——因为交付件提取的是纯文本，标记已被消化。**处置**：比对前统一剥掉强调与代码标记两类符号，它们最常出现在句子中间、会把连续语句切断。

### 衍生四：不要拿文件时间戳当成败判据

⚠️ 时间戳对无关操作敏感——版本控制检出、编辑器保存、脚本还原测试文件都会改它。把「派生件比权威源旧」写成 FAIL，会在任何一次这类操作后假红。

**处置**：时间序**只作提示，不参与退出码**。判成败一律靠实际文本比对。

> **这条与 1.6 的禁用词误报是同一个病。** ⚠️ 某次刚从旧闸门里删掉两个「经常假红的裸词」，转头就在新脚本里造了一条同类判据。**「一条永远报红的判据等于没有判据」这个认知，在删别人的坑时是清醒的，在写自己的判据时会失效**——因此它必须进检查清单，不能只当共识。

## 1.6 准则三：闸门会过期，且过期后比没有闸门更危险

> 写死结构清单的闸门，在文档长出新章节后会给出**全绿的假安心**。

机制：闸门的覆盖面是按某一时刻的文档结构硬编码的，不会随文档增长自动扩展。新增的章节不在清单里，缺失它就不触发任何告警。

⚠️ 举证：某闸门的章节清单硬编码止于建闸门时的最后一章。权威源新增后续章节后，**即使交付件整段丢失这些章，闸门也不会产生任何一条 FAIL 或 MISS**。这正是滞后未被自动拦住的机制性原因。

**为什么比没有闸门更危险**：没闸门时人还会手动查；有一个全绿的闸门，人就不查了。

**配套铁律**：新增章节时必须在同一次改动里同步扩容闸门清单。

### 衍生：禁用词类断言要带上下文

把某个词列为「必须 0 次出现」的黑名单，很容易撞上该词的正常用法。

⚠️ 举证：某闸门把「替换」列为版本痕迹禁用词，而权威源有正常动词用法。结果**唯一会亮红的 FAIL 与真实风险无关**，来自这条自伤式误报——闸门的红灯与真实风险已经脱钩。

**处置**：从整词黑名单收窄为带上下文的精确模式。

## 1.7 准则四：关于工具的可数事实必须回代码数

> **留档转述会成为错误口径的传播链。**

「闸门有几段」「清单有几项」「覆盖哪些范围」这类可数事实，一旦从过程留档转抄进经验文档，就会脱离代码独立传播。

**判定动作**：写进经验文档的每个关于工具的数字，都要当场回代码点一遍；引用行号必须当场核对，因为大文件在不同读取方式下行号会偏移。

**推论**：**运行时观测值要标注出处与时点。** 「模板自身有 N 处容差」这类数字是某次运行的观测结果，不是可复现常量，换模板须重测。不标注的话，下一个人会把它当常量用。

## 1.8 准则五：版式约束要变成可执行闸门，且判据须是绝对量

> **「不允许文字重叠」这类约束靠人眼翻页检查，页数一多必然漏检。**

要把它变成脚本判据，关键在于**选对判据的量纲**。

### 相对量判据会失真

用「新文本比旧文本长多少倍」来推断是否溢出，隐含假设是「旧文本刚好填满」。这个假设在模板场景下不成立：模板原文常是短占位符，任何正式文案都会超出倍率阈值，报警全是噪音。

### 绝对量判据：按容器几何估算容量

```
每行可容字数 = (容器宽 - 左右内边距) / 单字宽
所需行数     = ceil(文本长度 / 每行可容字数)
可容行数     = (容器高 - 上下内边距) / 行高
溢出         = 所需行数 > 可容行数
```

| 系数 | 处理方式 |
|---|---|
| 单字宽 | 全角与半角宽度不同，须按文本里两类字符的实际占比加权平均，不能取单一值 |
| 行高 | 取字号的固定倍数（如 1.25） |

### 关键一步：先跑模板建立容差基线

**不要拿「零报警」当目标——先确认模板自身报几处。** 模板里本就存在依赖渲染引擎自动缩放的容器，它们在几何估算下必然报警，属**设计容差而非缺陷**。不建立基线就会把模板固有特征当 bug 追。

**判定动作**：先对空白模板跑一遍闸门，记录报警数与形态，据此定处置门槛；只处置超出基线形态的项。

## 1.9 准则六：探针词表必须能区分「该出现」与「不该出现」

> **同一个词在不同交付件里、在不同句式里，应有的状态可能完全相反。用一张通用词表跨文档扫描，结论不可用。**

| 失效 | 机制 | 举证 |
|---|---|---|
| **假 MISS** | 该词在这份交付件里本就不该出现 | 市场类文档不该含实现技术名词，报「缺失」是正常的 |
| **假 BAD** | 该词出现在**否定该词的句子**里 | 权威源写着「不使用某指标作为信号」，禁用词扫描把这句判为违规残留 |

**判定动作**：探针的比对单元不能是孤立的词，必须是**本轮改写后的原句片段**——它天然携带上下文，因而能区分肯定与否定；同时它只在该出现的那份交付件里被检索，因而不会产生跨文档假 MISS。

这与 1.6 的「禁用词类断言要带上下文」是同一条原则的两个层次：那一条讲单份文档内的词义歧义，这一条讲跨文档的适用范围差异。

## 1.10 准则七：传导前先验权威源自洽

> **权威源自身可能残留旧口径。不先自检，会把残留一路传导到全部交付件。**

权威源之所以会残留，是因为口径变更往往只改「定义处」，而同一口径可能在边界表、异常表、附录里各出现一次。这些位置不在变更清单上。

**判定动作**：把本轮变更的旧口径关键词对**权威源自身**全文扫一遍，确认零残留，再开始向派生件传导。

### 衍生：同一名词的缩写形态要一并扫

口径变更常伴随名词变更，而旧名词往往有全称、缩写、口语简称多种形态。只扫其中一种会漏。

## 1.11 准则八：文件命名相似的旧件必须清干净

> **「内容源」与「交付件」命名相近时，对方会看着文件列表判断你改错了对象。**

这不只是观感问题：目录里同时存在旧版交付件与新版交付件时，**下游有实际概率提交错的那一份**。

**判定动作**：一轮同步收尾时列一次目录，把已被取代的旧交付件删除或移入归档目录；同时清理生成过程产生的临时日志。

## 1.12 准则九：交付目录必须是一键重建的产物，不是手工整理的结果

> **手工拷出来的交付目录，下一轮改动后必然脱节——而脱节了看不出来。**

这与准则一是同一个病灶在目录层的表现：单个派生文件的滞后至少还有「重跑脚本」这个动作可依赖，手工整理的目录连那个动作都没有，它只依赖「有人记得再拷一次」。

⚠️ 举证：某批产物过期两轮才被发现，成因正是收件目录由手工拷贝维护。而同一目录下往往混着多轮历史产物，**只有文件名前缀能区分哪批是当前版**，肉眼无从判断。

**判定动作**：写一个重建脚本，把「哪些文件、归到哪个子目录、当前是哪一轮」全部编码进去，并让它**自报每个文件是新建、更新还是未变**。轮次标识收成单个常量，改版时只改一处。

**历史产物是否保留**：保留有代价（仓库体积）也有价值（「某个位移是哪一轮引入的」只能靠跨轮对比排查）。**这是一个要显式裁定的取舍，不是默认行为**——裁定结果须留档，否则下一个人会按自己的偏好清理。

### 衍生：失败路径不许留下半成品

⚠️ 「先清空目标目录、再边拷边验」的写法有一个只在真失败时才现形的缺陷：校验失败时目录已被清空且只拷了一半，而**下次运行又报「新建」，看不出上一次是失败中断的**。

**判定动作**：**先分组、先验完整性与文件存在性、全部通过后才动目标目录。** 并用一次反向测试确认：故意制造一处校验失败，确认目标目录仍完好保留上一轮内容。

## 1.13 反模式

| # | 反模式 | 正确做法 |
|---|---|---|
| 1 | 直接编辑派生交付件 | 改权威源后重生（1.2） |
| 2 | 改完权威源就认为同步完成 | 重跑 + 改名 + 落盘核对（准则一） |
| 3 | 假定脚本会读权威源 | 先判两型（1.2） |
| 4 | 改了权威源却不动硬编码型脚本文案 | 成对修改（1.2） |
| 5 | 用时间戳或文件大小判断是否同步 | 提取实际文本逐条核对（准则二） |
| 6 | 特征句凭印象写 | 从权威源逐字复制，含格式标记（准则二） |
| 7 | 探针报错就调探针直到变绿 | 先打印实际文本，判断是探针错还是文档错（准则二） |
| 8 | 新增章节不扩容闸门 | 同一次改动内同步（准则三） |
| 9 | 沿用留档里关于工具的数字 | 回代码数（准则四） |
| 10 | 假定全部脚本行为一致 | 先测绘传导拓扑图，逐条标出改动入口（1.3） |
| 11 | 逐行读硬编码脚本找待改处 | 用口径关键词一次扫出全部槽位（1.3） |
| 12 | 只遍历顶层形状提取文本 | 递归展开分组容器与表格单元格（准则二） |
| 13 | 用长度倍率判断文字溢出 | 按容器几何估算容量，绝对量判据（准则五） |
| 14 | 拿「零报警」当版式闸门目标 | 先跑模板建容差基线，只处置超基线项（准则五） |
| 15 | 用通用词表跨文档扫一致性 | 用本轮改写的原句片段，按文档分别检索（准则六） |
| 16 | 默认权威源自身是对的 | 传导前先回扫权威源零残留（准则七） |
| 17 | 旧交付件留在目录里 | 收尾时删除或归档，同时清临时日志（准则八） |
| 18 | 特征句直接拿去查交付件 | 先在权威源侧验它存在；「脑补」与「缺失」分开报（准则二） |
| 19 | 拿文件时间戳当同步判据 | 时间序只作提示，不参与退出码（准则二） |
| 20 | 手工拷贝维护交付目录 | 写重建脚本，自报新建/更新/未变（准则九） |
| 21 | 校验前先清空目标目录 | 先验完整性、全通过后才动目录；反向测一次（准则九） |
| 22 | 跨文档引用写 `./` 或 `../` 相对路径 | 一律用仓库根相对路径，目录可自由重组（1.15） |
| 23 | 行号引用不带章节名 | 行号后补章节名，行号漂移后仍可定位（1.15） |
| 24 | 移动文件后凭印象「顺手改一下」引用 | 移动前后各 Grep 一次计数比对，批量替换用脚本（1.15） |
| 25 | 交付前只做正向校验（上游逐条问「实现了吗」） | 补一轮反向校验：从本阶段产物抽五类值回上游查证（1.16） |
| 26 | 标识符只查「我用的名字上游有没有」 | 双向查，还要查上游有无异名同义项（1.16） |
| 27 | 数值从进度记录 / 评审结论 / 纪要里抄 | 一律回权威源取当前值，转述件不作数（1.16） |
| 28 | 反向校验的分歧只记在本阶段登记表里 | 修正必须落到上游权威源；关闭前问「上游比打开时多了什么」（1.16） |
| 29 | 提交后才拿代码字段名去对契约 | 写第一行代码前先对一遍（1.16） |

## 1.14 环境约束（Windows + PowerShell，全书通用）

本表对全部章节生效，后续各章不再重复。

| 问题 | 表现 | 处置 |
|---|---|---|
| 控制台非 UTF-8 | 中文输出乱码 | `$OutputEncoding=[Console]::OutputEncoding=[Text.Encoding]::UTF8; chcp 65001 > $null; $env:PYTHONIOENCODING='utf-8'` |
| **不可用于改含中文的文件** | `Get-Content -Raw` + `Set-Content` 把 UTF-8 中文当 GBK 处理，**破坏整个文件** | 一律用编辑工具改文件 |
| 脚本按系统默认编码读中文文件 | `UnicodeDecodeError` | 先设 `$env:PYTHONUTF8=1`。⚠️ 只处理输出侧编码（如 `sys.stdout.reconfigure`）不解决读入侧 |
| 不支持 heredoc | `git commit -F - <<'EOF'` 直接语法错误 | 写临时文件再 `-F` 引用 |
| 多仓库嵌套 | 子目录里的 git 命令落到上层仓库 | **`git rev-parse --show-toplevel` 应是排查的第一条命令** |
| 沙箱禁止项目外写入 | `__pycache__` 写入失败 | `$env:PYTHONDONTWRITEBYTECODE='1'` |
| Python `%` 格式化 | 全角括号紧跟 `%` 被当格式符 | 改写措辞或改用 f-string |
| 目标文件被办公软件占用 | 写入静默失败 | 重跑前先确认无占用进程；注意 `~$*` 锁文件要**看清扩展名**，别把无关格式的残留当阻塞 |
| 沙箱拦截包安装 | `pip install` 报权限失败 | 装到项目内目录（`--target`）并设 `PYTHONPATH`。⚠️ **失败信息要看清是哪一步失败**——包本体可能已安装成功，报错来自 pip 写缓存等无关动作，此时直接验导入即可 |

> **⚠️ 依赖不成文等于判据无人能复核。** 脚本实测出的阈值一旦被规格文档引为权威依据，这个脚本就必须能被别人跑起来。
>
> **判定动作**：任何被规格引用的校验脚本，都要有依赖清单；`import` 失败时不要裸抛异常，须打印**装什么**与**装不上时的替代核验路径**（例如几何常量可直接读素材源文本折算）。退出码与断言失败区分开。

## 1.15 跨文档引用的路径约定（全书通用）

> **引用路径的写法决定了文档目录能不能重组。这个代价在写下第一条引用时是零，在积累了上百条之后是重构级的。**

文档之间互相引用是交付链的基本形态：架构文档引 PRD 的条款、PRD 引架构的算法、进度记录引两者。问题在于**同一条引用有三种写法，日常使用时看不出区别，只在移动文件时才分出高下**。

### 三种写法与它们的实际代价

| 写法 | 示例 | 移动引用方文件时 | 移动被引文件时 |
|---|---|---|---|
| **仓库根相对路径** ✅ | `docs/PRD.md:2432` | **不受影响** | 需改，但可全局替换 |
| 文档间相对路径 ❌ | `./PRD.md`、`../docs/PRD.md` | **每一条都要重算层级** | 需改，且各引用方改法不同 |
| 绝对路径 ❌ | `d:\developer\...\PRD.md` | 不受影响 | 需改；且**换机器/换协作者即全数失效** |

**约定：一律用仓库根相对路径。** 即从仓库根目录起算的路径，不带 `./`、`../`、盘符。

### 为什么这条值得单独立规

⚠️ 举证：本项目把三份架构文档从 `docs/` 平铺归类到 `docs/architecture/` 时，三份文档内部共有 **109 处** `docs/` 引用（主文档 65 / 可观测性 28 / 技术栈 16）。因为一律写作 `docs/PRD.md:2432` 这种根相对形式，**文件下移一层后 109 处全部仍然有效，一处未改**。整次归类的实际改动量只有 9 处 —— 那 9 处恰恰是指向被移动文件本身的引用。

若当初写成 `./PRD.md`，同一次归类要修改的就是 109 处而非 9 处，且每一处都要人工判断层级差。**这不是节省了工作量的问题，是这类重构会因为代价过高而根本不会发生** —— 文档目录于是永远保持在最初那个随手摊开的状态。

### 衍生一：行号引用必须带章节名

行号是**会漂移的锚**。被引文档只要在前面插入或删除任何一行，后面所有行号引用同时失准，而且**不会有任何报错**。

⚠️ 举证：本项目架构文档 §2.1 曾写「`docs/PRD.md:2496` 的 EXIF 剥离」，实测 2496 行是缓存 TTL 表；同句的「`PRD.md:1834-1835` 的图片内容审核」实测是自动重发开关。两个行号都在 PRD 多轮增补后失准，而它们看上去与正确引用毫无区别。

**判定动作**：行号后必须补章节名或标题名 —— `docs/PRD.md:2556`（§14 上线前验收清单）。行号负责精确定位，章节名负责漂移后仍能找回。二者缺一：只有行号会静默失效，只有章节名则大文档中定位成本过高。

### 衍生二：移动文件是一次「引用同步」作业，不是一次 mv

移动本身只要一条命令，**真正的工作量在同步引用**，而这部分极易被当成「顺手改一下」而漏项。

**判定动作**：移动前先全仓库 Grep 出旧路径的全部出现点并计数，移动后按同一模式 Grep 确认归零、再 Grep 新路径确认命中数与预期一致。两次计数对不上就是有遗漏或误伤。批量替换用脚本而非手工，手工改 7 处以上必然漏。

## 1.16 阶段交付前的反向校验（全书通用）

> **正向校验查「上游每一条有没有落地」，反向校验查「本阶段每一个具体值在上游是不是这个值」。前者永远查不出后者的问题 —— 因为本阶段新造的东西不在上游的清单里，正向遍历遍历不到它。**

每个阶段交付前都会做一次核对，而绝大多数团队只做正向的那一半：拿着上游的需求清单逐条问「这条实现了吗」。这个方向能查出遗漏，**查不出分歧**。分歧的载体是本阶段自己写下的那些具体值 —— 字段名、错误码、阈值、状态名 —— 它们在上游清单里没有对应的行，所以正向走一遍不会碰到它们中的任何一个。

### 两个方向查的是不同的集合

| | 遍历起点 | 能查出 | 查不出 |
|---|---|---|---|
| **正向校验** | 上游条目 | 漏做的需求、覆盖不全的场景 | 本阶段新造的口径、被本阶段改写的数值、与上游同名不同义的标识符 |
| **反向校验** | **本阶段产物里的每一个具体值** | 口径分歧、重复占号、过时数值、静默失准的引用 | 完全没做的需求（那是正向的职责） |

**两者必须都做，且反向的那一次成本更高** —— 因为它要求把自己写的东西当成可疑对象，逐个回上游查证，而人对自己刚写完的内容天然缺乏怀疑。

### 抽取什么、比对什么

反向校验的操作对象是**从本阶段产物中抽出的五类值**。每一类的失效形态不同，比对动作也不同：

| 类别 | 抽取对象 | 回上游比对什么 | 典型失效形态 |
|---|---|---|---|
| **标识符** | 字段名、表名、枚举值、错误码、常量名、接口路径 | ① 上游是否已有同义但**异名**的项<br>② 该编号 / 键是否**已被上游占用为另一含义** | 两套命名并存；同一个码在两份文档里含义不同 |
| **数值** | 阈值、TTL、上限、档位、超时、配额 | 上游权威源当前值是否仍是这个值 | 上游改过、本阶段抄的是历史值 |
| **状态与流转** | 状态名、流转条件、终态判定 | 状态集合是否完全一致、流转条件是否同口径 | 本阶段自行增设了一个中间态 |
| **引用锚点** | 指向上游的章节号、路径、行号 | 目标是否存在、**内容是否仍是引用时说的那件事** | 引用有效但内容已变（见 1.15 衍生一） |
| **契约结构** | 请求 / 响应字段、动词、必填性、层级 | 与上游契约逐字段比对名称与可空性 | 一方独有的字段；同名字段可空性相反 |

### 判定动作

1. **先抽后查，不要边写边查。** 交付前把上述五类值**成表列出**，再逐行回上游查证。边写边查会漏掉「写的时候确信正确」的那些项 —— 而它们恰恰是分歧的主要来源。
2. **标识符类必须双向查。** 不只查「我用的名字上游有没有」，还要查「上游有没有另一个名字表示同一件事」。⚠️ 前者能过、后者不能过，就是两套命名并存的开始。
3. **新增编号前查唯一口径源是否已占用。** 编号类（错误码、版本号、迁移序号）的冲突不报错，两处各自成立，只在合并时才现形。
4. **数值类一律回权威源取当前值，不从任何转述件抄。** ⚠️ 包括不从本项目的进度记录、评审结论、会议纪要里抄 —— 这些都是转述件（同 1.7）。
5. **引用锚点要核内容不只核存在。** 路径存在、章节存在，都不构成「引用说对了」的证据（同 7.10 衍生）。
6. **抽取用检索，不用记忆。** 五类值的抽取全部靠全文检索模式匹配，不靠回想「我写过哪些」。人能想起来的部分本来就不会错。

### 反向校验发现分歧后：修正必须落在上游

⚠️ 举证：某项目在某阶段评审中裁定了六项与上游口径相关的问题，裁定结论**全部记在本阶段文档的登记表里**，上游权威源一处未改。后果是下一阶段的实施者翻开上游文档时，看到的仍是旧口径 —— 而实施者只翻上游，不会翻上一阶段的登记表。**同一批裁定因此要在下一阶段被重新发现一次。**

更严重的一例：本阶段在上游未定义的编号区间里自行赋了一个含义，与上游后来对同一编号的定义冲突。此时两份文档都「有据可查」，且**信上游的人和信本阶段文档的人会写出行为不同的代码**。

**判定动作**：本阶段关闭前必须回答一个问题 —— **「上游权威源比我打开它的时候多了什么？」** 答案为「没多」时，说明本阶段发现的所有分歧都还停在本阶段文档里，即未回写。无权限改上游时，退而用登记 + 绑定处理时点，且时点必须落在「下一次会翻开上游文档的那个时刻」（见 7.9）。

### 各阶段的反向校验内容

按交付链，每一章的阶段交付前应抽取的具体对象：

| 阶段 | 抽取对象 | 回哪里比对 |
|---|---|---|
| 产品文档交付链（第一章） | 各派生件中的特征句、数值、版式参数 | 权威源 MD；且要反查权威源自身有无旧口径残留 |
| 低保真骨架（第二章） | 页面清单、跳转目标、组件命名 | 规格文档的信息架构与页面清单 |
| 高保真原型（第三章） | 生成器代码里的档位、尺寸、文案、标注值 | 规格文档对应条款 —— ⚠️ **生成器代码是最容易脱离规格独立演化的一类产物** |
| 架构设计（第四章） | 新增错误码、字段名、状态名、组件边界 | 规格文档的数据字典与错误码表 |
| 数据库设计（第五章） | 表名、列名、类型、约束、索引依据的数值 | 架构文档与规格文档的字段定义 |
| 详细设计与契约（第六章） | 接口路径、动词、请求 / 响应字段、必填性 | 规格文档的接口章节；两侧命名逐对比对 |
| 安全与 DevSecOps（第七章） | 纪律涉及的编号、阈值、门禁判据引用的章节 | 规格文档与架构文档；⚠️ 尤其查新造编号（见 7.9） |
| 编码期 | 代码中的模型字段名、枚举值、常量 | 契约文档与数据字典 —— ⚠️ **写代码前先对一遍，不是提交后** |

⚠️ 举证：某项目在编码期写客户端数据模型时，一次未打开契约文档，三处字段名 / 模型结构 / 状态定义与契约不符。这些偏差在写第一行代码前做一次字段名反查即可全部暴露；实际是在提交后的评审中才被发现。**当时的判断是「在解决一个通用问题」，而契约里早已把这件事写完了。**

### 各阶段的反向校验操作步骤

#### 阶段一：产品文档交付链（第一章）

**抽取对象**：各派生件中的特征句、数值、版式参数

**操作步骤**：
1. 从权威源 MD 提取所有特征句和数值，做成预期值表
2. 从各派生件（PPTX / Word / 校样）中提取实际文本，与预期值表逐句比对
3. 版式参数（字号、间距、边距）从权威源取规格，与派生件实测值比对
4. 反查权威源自身：全文检索旧口径关键词，确认无残留

**比对方式**：特征句逐字比对，数值逐项比对，版式参数实测比对

**分歧处理**：① 版式类分歧 → 直接改派生件 ② 特征句内容类分歧 → 先确认权威源才是真的，再改派生件 ③ 权威源有旧口径残留 → 先清权威源，再用清零后的版本重跑校验

#### 阶段二：低保真骨架（第二章）

**抽取对象**：页面清单、跳转目标、组件命名

**操作步骤**：
1. 从规格文档的信息架构提取页面清单——页面名、层级、父子关系
2. 从骨架文件提取所有页面名和跳转目标，与页面清单逐条比对
3. 反查规格文档中每个页面的组件清单，与骨架中实际放置的组件核对
4. 检查孤立的页面（无跳转入 / 出）、跳转目标不存在的链接

**比对方式**：命名逐字比对，跳转目标存在性比对，组件覆盖度比对

**分歧处理**：① 跳转目标不存在 → 确认是骨架漏了还是规格文档未定义 ② 组件名与规格文档不一致 → 以规格文档为准改骨架 ③ 孤立的页面 → 确认是骨架遗漏跳转还是规格文档设计如此

#### 阶段三：高保真原型（第三章）

**抽取对象**：生成器代码里的档位、尺寸、文案、标注值

**操作步骤**：
1. 从规格文档提取所有档位定义、尺寸规格、阈值范围
2. 从生成器代码中提取所有常量定义和条件分支中的数值
3. 逐项比对：档位值是否一致、尺寸是否匹配、阈值范围是否等价
4. 检查标注卡——口径是否与规格文档一致
5. **特别检查**：生成器代码中是否有自己推导的数值，而非从规格文档搬运的

**比对方式**：直接数值比对；**要注意代码中数值可能是规格文档值经过运算的结果**，此时的比对对象是「运算依据」而非「运算结果」

**分歧处理**：① 代码中多档位与规格文档档位数不一致 → 以规格文档为准改代码 ② 阈值范围代码写错 → 改代码 ③ 代码中自己推导的数值并存 → 确认推导公式是否合理，否则回退到规格文档取值

#### 阶段四：架构设计（第四章）

**抽取对象**：新增错误码、字段名、状态名、组件边界

**操作步骤**：
1. 从规格文档提取错误码表——已占用的码和含义，制成全部已占用集合
2. 从架构文档提取所有新增的错误码，逐号查是否与已有集合冲突
3. 从规格文档提取数据字典的字段名集合
4. 从架构文档提取所有字段名，逐项比对：字段名是否一致、类型是否匹配
5. 状态名比对：规格文档的状态模型与架构文档的状态模型逐项对比
6. 组件边界比对：架构文档中定义的组件边界是否与规格文档的模块划分一致

**比对方式**：错误码逐号比对，字段名逐字比对，状态模型逐项比对

**分歧处理**：① 新增错误码与已有冲突 → 重新编一个未占用的码 ② 字段名与规格文档不一致 → 以规格文档为准改架构文档 ③ 规格文档缺了某个字段定义 → 先补全规格文档，再以补全后的版本改架构文档（不得在架构文档里自行定义缺失的字段，见 7.9）

#### 阶段五：数据库设计（第五章）

**抽取对象**：表名、列名、类型、约束、索引依据的数值

**操作步骤**：
1. 从架构文档提取字段定义——字段名、类型、长度、约束
2. 从规格文档提取字段定义，三侧（规格 / 架构 / DDL）逐项比对
3. 从 DDL 提取所有表名和列名，与架构文档比对
4. 索引依据的数值（如阈值）——回规格文档取当前值，确认与索引设计一致
5. 约束条件——比对外键、唯一约束、非空约束在两侧是否一致

**比对方式**：DDL 实际语句与架构定义的逐项比对

**分歧处理**：① 字段名或类型不一致 → 以规格文档 + 架构文档为基准改 DDL ② 索引依据的数值过时 → 改索引设计，必要时重估算子 ③ 约束条件不一致 → 以架构文档为准改 DDL

#### 阶段六：详细设计与接口契约（第六章）

**抽取对象**：接口路径、动词、请求 / 响应字段、必填性

**操作步骤**：
1. 从规格文档提取所有接口定义——路径、动词、请求 / 响应字段表
2. 从详细设计文档提取所有接口定义，两侧命名逐对比对
3. 字段级比对：字段名、类型、必填性、默认值、描述
4. 检查规格文档中有但详细设计中没有的字段（遗漏）
5. 检查详细设计中有但规格文档中没有的字段（自行新增）

**比对方式**：接口路径逐条比对，字段逐字段比对

**分歧处理**：① 字段遗漏 → 补回详细设计 ② 字段自行新增 → 先确认新增是否合理，合理则回写规格文档，不合理则删除 ③ 字段名或必填性不一致 → 以规格文档为准改详细设计

#### 阶段七：安全与 DevSecOps 方案（第七章）

**抽取对象**：纪律涉及的编号、阈值、门禁判据引用的章节

**操作步骤**：
1. 从规格文档和架构文档提取所有编号区间和已占用的编号
2. 从安全方案提取所有新出现的编号，逐号查是否已占用或越界
3. 从规格文档提取所有阈值当前值
4. 从安全方案提取所有阈值，逐项比对是否一致
5. 门禁判据引用的章节——逐条回源文档确认内容是否与引用时一致

**比对方式**：编号逐号比对，阈值逐项比对，引用章节逐条核内容

**分歧处理**：① 新编号与已有冲突 → 重新编一个未占用的码 ② 阈值与规格文档不一致 → 以规格文档为准改安全方案 ③ 引用章节内容已变 → 转述改为逐字复制，或更新引用锚点 ④ 安全方案自行定义了本应属于上游的编号 → 删除，回归登记 + 回写到上游的路径

#### 阶段八：编码期

**抽取对象**：代码中的模型字段名、枚举值、常量

**操作步骤**：
1. **写第一行代码前**，从契约文档提取所有字段名和枚举值，制作成速查表
2. 从代码模型文件提取所有字段名和枚举值，与速查表逐项比对
3. 常量值——从规格文档提取当前值，与代码中的常量定义比对
4. **特别注意**：代码中自己推导的字段名和枚举值，而非从契约文档搬运的

**比对方式**：写代码前先比对，不是提交后

**分歧处理**：① 字段名与契约不一致 → 以契约为准改代码 ② 代码中自行推导了字段名 → 确认推导是否合理，否则回退到契约取值 ③ 常量值与规格文档不一致 → 改代码

**检查点位置**：在第一条数据模型类 / 结构体写完之后、任何业务逻辑之前。代码评审时只做二次确认，不做第一次发现。

## 1.17 落地检查清单

- [ ] 已判明每个脚本是权威源驱动型还是硬编码型（1.2）
- [ ] 已画出传导拓扑图，每条链路的改动入口明确（1.3）
- [ ] 传导前已回扫权威源，确认自身零残留旧口径（准则七）
- [ ] 改完权威源已重跑脚本，且确认落到最终文件名（准则一）
- [ ] 已提取交付件实际文本逐条核对，未用时间戳判断（准则二）
- [ ] 提取函数已递归展开分组容器（准则二）
- [ ] 特征句从权威源逐字复制，正反成对（准则二）
- [ ] 探针词表已确认不产生跨文档假 MISS 与否定句假 BAD（准则六）
- [ ] 版式闸门用绝对量判据，且已先跑模板建立容差基线（准则五）
- [ ] 新增章节已同步扩容闸门清单（准则三）
- [ ] 禁用词类断言已确认不撞正常用法（准则三）
- [ ] 文档里关于工具的数字都回代码数过（准则四）
- [ ] 运行时观测值已标注时点与「非可复现常量」（准则四）
- [ ] 旧交付件与临时日志已清理（准则八）
- [ ] **特征句已先在权威源侧验明存在，「脑补」与「缺失」分开报（准则二）**
- [ ] **无任何判据以文件时间戳定成败（准则二）**
- [ ] **交付目录由脚本一键重建，自报新建/更新/未变（准则九）**
- [ ] **重建脚本已反向测过失败路径，确认不留半成品（准则九）**
- [ ] **历史产物的保留与否已显式裁定并留档（准则九）**
- [ ] **被规格引用的校验脚本有依赖清单，缺依赖时打印替代核验路径（1.14）**
- [ ] **跨文档引用一律为仓库根相对路径，无 `./`、`../`、盘符（1.15）**
- [ ] **每处行号引用都带章节名或标题名（1.15）**
- [ ] **文件移动后已 Grep 确认旧路径归零、新路径命中数符合预期（1.15）**
- [ ] **交付前已做反向校验：五类值（标识符 / 数值 / 状态 / 引用锚点 / 契约结构）成表抽出并逐行回上游查证（1.16）**
- [ ] **标识符已双向查，确认上游无异名同义项、编号未被占用为另一含义（1.16）**
- [ ] **全部数值取自权威源当前值，未从进度记录或评审结论转抄（1.16）**
- [ ] **引用锚点已核内容而非仅核存在（1.16）**
- [ ] **本阶段发现的分歧已回写上游权威源；能回答「上游比打开时多了什么」（1.16）**
- [ ] **无权限回写的项已登记并绑定处理时点，时点落在下次翻开上游的时刻（1.16）**

---

# 第二章：低保真原型（骨架生成）

**问题定义**：规格文档已就绪，需要产出一份**结构、状态与跳转都完整的可点击骨架**，供下游（设计师或开发）接手精修。

**关于「低保真」这个词**：代码生成、带组件母版与 Token 网表、跳转经逐条实证的产物，真实形态是「中保真」——结构与状态完整，视觉与动效留白。这决定了本章准则的适用面：手绘线框用不上，代码生成的骨架才用得上。

**核心主张**：骨架阶段的产出物有两个，且第二个往往比第一个值钱——**一是骨架本身，二是被骨架逼出来的规格缺陷**。规格文档允许含糊表述长期共存，画框不允许，因此骨架是规格的强制消歧器。

## 2.1 适用边界

| 判断项 | 适用 | 不适用 |
|---|---|---|
| 输入状态 | 规格文档已有页面清单与状态定义 | 页面清单还没定（属规划阶段） |
| 产出形态 | 代码生成、可反复重跑 | 手绘线框、一次性草图 |
| 规模门槛 | 页面多、状态变体多，人工清点已不可靠 | 三五个页面，手画更快 |

**判定动作**：先数一遍状态变体数量。变体数超过十来个，人工清点就不再可靠，值得上代码生成；否则代码生成的脚手架成本收不回来。

> ⚠️ 举证：某骨架里过半画框都是状态变体。留档明写「状态遗漏是评审后返工的第一大来源，而人工清点大量画框不可靠」。

## 2.2 工具矩阵

### 2.2.1 技术路径：先排除，再选定

**设计工具的自动化入口通常有四五条，能用的往往只有一条。** 选路径前把每条都验证到「能否写入」这一步，并把排除理由留档——否则后续每次遇阻都会有人重新提议已被排除的路径。

| 路径类型 | 典型排除理由 |
|---|---|
| REST API | 只读（GET-only），无法写入 |
| 远程 MCP | 客户端白名单不含当前 IDE |
| 原生工程文件 | 二进制格式未公开，且已知缺陷 |
| 中间格式导入（SVG / HTML） | 丢失自动布局等结构信息 |
| **插件 API** | **通常是唯一可写入路径** |

### 2.2.2 沙箱约束：先摸清边界再设计架构

插件沙箱的限制会直接决定架构，不是实现细节：

| 约束 | 对架构的影响 |
|---|---|
| 读不到本地文件 | 图片等素材须内联进代码 |
| 跳转不支持跨页面 | **「另开一个页面放高保真版」意味着该页面必须自成完整闭环** |
| 单次执行有超时 | 生成逻辑必须可分批，且批次间依赖顺序要显式声明 |

> **判定动作**：动手前先把「跳转能不能跨页面」这类跨对象引用的限制查清。它一旦为否，就否掉了整类「并行放两个版本」的方案。

### 2.2.3 校验层：三层各管一段

| 层 | 对象 | 说明 |
|---|---|---|
| 静态检查 | 生成器源码 | 语法与诊断零错误，**这是最低门槛，不是验证** |
| 离线探针 | 生成器的数据与逻辑 | 正反成对，可在无设计工具环境下跑 |
| 实机取证 | 真实运行结果 | 逐节点接口取证，见 3.3 阶段④ |

> **静态检查全绿不构成「跑得通」的证据。**
>
> ⚠️ 举证：某生成器长期处于「语法零错误、IDE 诊断零错误、但从未在真实环境跑过」的状态，此后多轮改动全部叠加在未验证的基础上；第一次实机运行就暴露了重名缺陷。

## 2.3 流程骨架

```
① 定页面清单 → ② 建标记与索引机制 → ③ 分批生成 → ④ 实机取证 → ⑤ 回写规格
```

### 阶段 ① 定页面清单

**清单要有一份权威版本，且必须同时包含「不做什么」。**

| 动作 | 说明 |
|---|---|
| 页面清单进规格文档，不进生成器注释 | 生成器是消费方，不是定义方 |
| 明确列出「明确不做的页面」 | 否则每轮都会有人提议补上某个已被砍掉的页面 |
| 砍留改新增四分法 | 对存量改造项目，逐页标注处置方式 |

**清单在骨架阶段一定会变，要留变更痕迹。** 增补项往往恰恰是规格文档自身矛盾暴露出来的（见阶段⑤）。

### 阶段 ② 建标记与索引机制

**目标**：主态与变体可被机器区分。

> **不要用命名关键词猜测对象类型——靠生成侧打标。**
>
> 用「标题里含某几个关键词就算变体」这类黑名单做识别，下一轮新增的变体只要不含关键词就全部漏网。
>
> **判定动作**：类型信息由生成时显式写入（如统一的变体后缀标记），消费侧只读标记、不做推断。
>
> ⚠️ 举证：某生成器用关键词黑名单判定变体，新增变体一个关键词都不含，全被当成主态，导致重名画框与跳转连错风险。

**顺带一条否决判据**：看起来更权威的「只认权威清单里的页面为主态」也不可取——总有合法主态定义在别的章节里，只认一处会把它们误排除，反而制造新的丢目标缺陷。

### 阶段 ③ 分批生成

| 动作 | 说明 |
|---|---|
| 批次入口显式化 | 每批一个入口函数，依赖顺序写进文档 |
| 重跑前先清空 | 否则叠加出重复产物（同 3.3 阶段④） |
| 幂等 | 生成逻辑要能反复执行 |

> **删掉所有「找不到就兜底」的写法——失败必须可见。**
>
> 兜底看似提高鲁棒性，实际是把定位失败转化成了一个「成功但错误」的结果。
>
> ⚠️ 举证：某生成器写了「定位不到目标就退化为整框点击」，结果导航栏标题被错绑跳转，而连线计数显示全绿。

### 阶段 ④ 实机取证

| 规则 | 原因 |
|---|---|
| 只用逐节点接口验证跳转 | 聚合报告会淹没唯一有用的信号（见 3.3 阶段④） |
| **「连线数 N/N」不等于「连对了」** | 计数正确与目标正确是两件事 |
| 失败必须可见 | 见阶段③ |

### 阶段 ⑤ 回写规格

**骨架阶段的第二个产出物在这里交付。** 画框必须选定一套具体取值，因此凡规格文档里两章对同一事物写了不同版本，在这一步会全部暴露。处置流程：

1. **定位定义源**——哪一章是该事物的定义处，哪一章只是引用
2. **以定义源为准**，回改引用处
3. **回写规格文档**，而不是只在生成器里改对

> **规格文档也可能是过期的那一方，不能默认它永远对。**
>
> 冲突出现时先逐条回读原文核实，再判断哪侧为准。**方向不能预设**——有时结论是生成器错，有时恰好相反。
>
> ⚠️ 举证：多层级筛选类页面必须选定一套具体的层级取值，才发现规格文档两章对同一层级的定义对不上——「此项不澄清则代码无从落笔」。同轮还查出某页已有完整状态规格、却不在页面清单里，属规格文档内部自相矛盾。

## 2.4 三条判断准则

### 准则一：用户的观感反馈，先假设它是漏实现

> **「这里不好看 / 这里被挡住了 / 这里没有搜索框」——第一动作是回读规格原文，不是改需求，也不是发明新方案。**

**判定动作**：把反馈翻译成「规格里这一条是怎么写的」，核对现状与规格的差。差存在就是漏实现，差不存在才是新需求。

⚠️ 举证：连续多轮用户观感反馈，核对结论全部是「补漏实现」而非改需求。其中一轮反馈某标记不美观，规格原文写的是「底色圆点 **+ 类型图标**」复合形态，而实现只画了纯色圆点、把图标漏掉了。

**代价**：把漏实现当新需求，会先发明一套新方案，再在下一轮撤销它。

### 准则二：按字面执行观感词，会做出反效果

> 「把主内容区扩大、控件可以浮在内容上」→ 若理解成「所有控件常驻悬浮」，悬浮层自己就把主内容区吃掉了。

**判定动作**：观感词要先翻译成量化目标（此例是「净可视区高度」），再看方案对该目标是增是减。

⚠️ 举证：多组控件全部改为常驻悬浮后，竖向合计吃掉的高度使主内容区净可视区反而缩小，**与「扩大空间」的初衷相互抵消**；改为默认收起后，常驻件占用高度降到原方案的约四分之一。

**配套判据：收起 ≠ 隐藏。** 全部收干净，用户就失去「我现在筛的是什么」的反馈，会反复展开确认，操作成本反而上升。正确形态是压缩为一行只读摘要。可推广：**任何「折叠以让位」的设计，都要留一个当前状态的最小反馈位。**

### 准则三：占位数据会在两棒之后才爆

> **一处假数据，两棒之后才爆。**

危害链条：骨架里填了占位值 → 设计师照着骨架画 → 开发照着设计稿写 → 联调才发现对不上。**中间两棒都没有核对占位值的动机，因为它看起来就是一个合法取值。**

**判定动作**：骨架里的业务数据要有独立的内容层（fixtures）与真源表，并写正反成对的探针守住——正向验真值在位，反向验占位形态零命中。

**探针自身也会写错。** 校验脚本不是免检品，这与 3.4 准则二（测量方法本身需要验证）是同一条原则在不同层的体现。

## 2.5 反模式

| # | 反模式 | 正确做法 |
|---|---|---|
| 1 | 把用户观感反馈当新需求 | 先回读规格原文，多数情况是漏实现（准则一） |
| 2 | 按字面执行「浮上去」「扩大」 | 先翻译成量化目标再评估方案（准则二） |
| 3 | 用命名关键词黑名单识别对象类型 | 生成侧显式打标，消费侧只读标记（②） |
| 4 | 写「找不到就兜底」 | 删掉，失败必须可见（③） |
| 5 | 拿连线计数当跳转正确的证据 | 逐节点取证（④） |
| 6 | 静态检查全绿就推进 | 实机跑通才算验证（2.2.3） |
| 7 | 骨架里填占位数据待后补 | 内容层 + 真源表 + 正反探针（准则三） |
| 8 | 用绝对行号做跨文档回标 | 一律章节号锚点（同 3.3 阶段①）——⚠️ 规格文档一次增删后，全部行号回标同时失效 |
| 9 | 只在生成器里改对口径冲突 | 回写规格文档，否则下轮重犯（⑤） |

## 2.6 落地检查清单

- [ ] 技术路径的替代方案已逐条实测排除并留档（2.2.1）
- [ ] 沙箱的跨对象引用限制已查清，架构据此设计（2.2.2）
- [ ] 页面清单在规格文档里，且含「明确不做的页面」（①）
- [ ] 主态与变体靠生成侧打标区分，不靠命名推断（②）
- [ ] 生成器里没有任何「找不到就兜底」的分支（③）
- [ ] 跳转用逐节点接口取证，未拿计数当判据（④）
- [ ] 业务数据有内容层与真源表，占位形态有反向探针（准则三）
- [ ] 骨架逼出的规格缺陷已回写规格文档（⑤）
- [ ] 已实机跑通，不只是静态检查通过（2.2.3）

---

# 第三章：高保真原型设计

**问题定义**：结构与流程已定（原型骨架 + 规格文档就绪，骨架的产出见第二章），需要产出一份**下游能直接接手**的高保真原型。

**核心主张**：难点不在画得好不好，而在**三件事的可信度**——用户拍板的对象是否可追溯、机器校验是否守住了真正的约束、几何在多份副本间是否一致。这三件事失守时，产物看起来完全正常。

**而前置于这三件事的，是一件更基础的事：看得见。** 本章的返工大头不是判断错，是**判断得太晚**——在能看见产物之前写下的每一个决定，都要等到第一次出图才结算，而那时它们已经互相咬合。

> ⚠️ 举证：某轮高保真阶段跨 9 天、67 次提交，其中提交信息含「修 / 补 / 纠 / 撤」者 31 次，占 46%（2026-09 观测值，非可复现常量）。根因回溯到**出图链路第六天才打通**——前五天的全部产出都建立在「以后能出图」这一未经验证的假设上。同期两条提交信息自证代价：「首次出图挖出五处缺陷」「变体框首次逐张出图挖出四处缺陷」。

因此本章流程骨架把**通反馈通道**放在立权威源之前，且它是唯一一个「不通就不许往下走」的阶段。

## 3.1 适用边界

| 判断项 | 适用 | 不适用 |
|---|---|---|
| 输入状态 | 结构与流程已定 | 还在探索方向（属规划阶段） |
| 交付对象 | 人（设计师 / 开发） | 直接上线的生产代码 |
| 规格去向 | 需被多方长期引用 | 一次性探索，用完即弃 |

**先确定交付链的下一棒，再决定投入哪些维度。** 同一份「视觉/交互/动效/内容四维全补」，下游是开发时四维都有价值；下游是设计师时，视觉与动效两维大概率被推翻重做。

> **判定动作**：定维度前问「谁接、他能自己补上哪些」。

**两条对称的复评规则**：

- **被拒绝的方案要随前置条件复评。** Rejection 绑定的是当时的前提，不是结论本身。
- **被通过的决定，前提变了同样要复评。**

## 3.2 工具矩阵

### 3.2.1 Skill 层：流程编排

| 阶段 | Skill | 触发条件 |
|---|---|---|
| 发散收敛 | `ce-ideate` | 方向未定，需要候选与评估 |
| 边界澄清 | `ce-brainstorm` | 「做到什么程度」不清楚 |
| 拆解排序 | `ce-plan` | 方向已定，需 implementation-ready 序列 |
| 执行落地 | `ce-work` | 计划就绪，含 shipping tail |
| 单点沉淀 | `ce-compound` | 解决一个非平凡问题后（一次一个问题） |
| 自建工具 | `mcp-builder` | 反复失败判定为能力边界后，建专用 MCP |

### 3.2.2 MCP 层：能力扩展

| 能力类型 | 关键约束 |
|---|---|
| 设计工具插件 API | 沙箱通常**读不到本地文件**，几何数据须内联进代码 |
| 位图转矢量 | 有机形状的唯一可靠路径；无现成工具时自建 |
| 文档格式转换 | 目标文件被占用时静默失败，须核对落盘 |

**自建 MCP 的判断线**

> **画不出来就别硬画，去建工具。**
>
> 手写贝塞尔逼近有机形状不是「再试一次」能解决的能力问题。识别信号：**同类尝试连续失败三次以上，且每次失败原因相同**。此时正确动作是停手、说清需求、给出实现路径，用 `mcp-builder` 建专用工具一次性解决。

**工具报错先重试，再判定为能力边界。** 把偶发连接故障写成「工具限制」，代价是此后永久绕开一条本来可用的验证路径。

> **判定动作**：连接类报错（超时、连接失败）至少隔一轮重试，并换一个同类样本交叉验证，才允许写进文档当结论。

### 3.2.3 脚本层：自建校验

图形资产的正确性无法靠肉眼保证，须有脚本。**脚本分两类，失败含义相反，不可混放**：

| 类型 | 职责 | 数字变化时 | 生命周期 |
|---|---|---|---|
| **测量型** | 为一次决策实测数据、产出评审载体 | 正常，它是历史推导记录 | 决策完成即冻结 |
| **守门型** | 每次改动后复跑，断言须恒真 | **必须停下排查** | 长期维护 |

**技术选型**：`opencv-python` + `numpy` 足够覆盖光栅化、掩码测量、距离变换、连通域、IoU 比对。

**可复用的测量手法**：

| 要测什么 | 手法 |
|---|---|
| 笔画在目标尺寸下是否可辨 | `distanceTransform` 最大值 ×2 = 最大内切圆直径 |
| 形状是否断裂 | `connectedComponents` 数量 |
| 两种实现是否等价 | 掩码 IoU |
| 真实包围盒 | **渲染掩码实测**，不可按贝塞尔控制点算——控制点常落在轮廓外侧，算出的包围盒偏大，导致缩放倍率偏小 |
| 形状到边界的余量 | 求形状像素到中心的最大距离，再与边界半径比。⚠️ 须排除边界外的透明区——渲染器把透明画成白 |

### 3.2.4 出图通道：视觉反馈的唯一物理入口

**没有出图通道，本章其余全部工具都在裸奔。** 校验脚本只能守数值，视觉是否成立只能靠看。

**通道分两类，可用性完全不同：**

| 类型 | 形态 | 典型故障 |
|---|---|---|
| **进程内导出** | 设计工具插件里调导出接口 | ⚠️ **可能永久挂起**——依赖本机图形管线，Promise 不 resolve、无报错、无超时 |
| **云端渲染** | 服务端按对象 ID 批量渲染 | 需凭据；受配额与批量上限约束 |

> **判定动作（本章第一个必做动作，优先于立权威源）**：在画第一个真实组件之前，让通道把**一个最小空白产物**导出成图并落到磁盘上。通道不通就先解决通道，不许先攒产物。
>
> 这个动作的成本是几分钟，收益是把「以后能出图」从假设变成事实。

**通道选型的三条实测约定**：

| 约定 | 原因 |
|---|---|
| 进程内导出挂起时，不要加超时重试 | 它不是慢，是不会返回。加重试只是把一次等待变成多次 |
| 单对象接口不能替代批量接口 | 几十张图逐个调用会把出图变成一件「攒着一起做」的事，反而拉长无反馈期 |
| 通道换代理层调用前先直连验一次 | ⚠️ 中间层可能不透传该能力，报「找不到」时无法区分是通道坏还是中间层不支持 |

**结论要留档，与 2.2.1 的技术路径排除同理**——否则每次遇阻都会有人重新提议已被排除的通道。

## 3.3 流程骨架

```
⓿ 通反馈通道 → ① 立权威源 → ② 定视觉基准 → ③ 产素材 → ④ 落原型 → ⑤ 验收沉淀
      └────────── ③④ 之间按「一页一图一定稿」小步循环，不攒批 ──────────┘
```

### 阶段 ⓿ 通反馈通道

**目标**：出图这件事从假设变成已验证事实。判定动作见 3.2.4，本阶段唯一的通过条件是**磁盘上出现一张图**。

> **这是唯一一个「不通就不许往下走」的阶段。** 其余阶段可以带着已知缺口推进并标注，本阶段不行——它的产出是后续全部阶段的验收手段，缺了它，后面每一步都只有「写完了」没有「对不对」。

### 阶段 ① 立权威源

**目标**：规格有唯一出处，且每条都可追溯。

| 动作 | 说明 |
|---|---|
| 带阈值的条款必须附实测值 | 对比度、尺寸、性能等，要么附实测，要么显式标注「未验证」 |
| 引用一律用章节号 | 行号会随文档增删失效 |
| 派生交付件重生后核对落盘 | 核对用的特征句**必须从权威源复制原句**（含格式标记），脑补形式会产生假 FAIL（详见 1.5） |

> **「已定稿」不等于「已验证」。** 不得让未验证项以定稿身份流入实现。
>
> ⚠️ 举证：某色板多数条目标着定稿，实际只有个别条目算过对比度，其余凭观感取值——实测后修掉多处 WCAG 违规。

> **同一业务名词可能承载多套口径。** 判断某处取值是否抄错，要先确认该名词在全文有几套口径、各自归谁管，再判断此处该用哪套。只核一套会得出似是而非的结论，从而**修错方向**。

### 阶段 ② 定视觉基准

**目标**：用户拍板，且**拍板对象可追溯**。

流程：多方向候选 → 用户选方向 → 该方向出深化变体 → 用户定稿。

> **审批的载体决定了交付的可复现性。**
>
> 用户批准的是「那一次渲染结果」，不是 prompt 文字。**AI 生成的图必须立刻下载存盘**，它才成为真源。凭 prompt 复述重建，等于重新创作。
>
> **判定动作**：任何进入审批的视觉产物，落盘后再拿去问。
>
> ⚠️ 举证：曾发生「批准的是 A、交付的是 B」——交付的几何是另行手绘的，由用户发现并质问。

### 阶段 ③ 产素材

**目标**：几何有唯一定义处，副本由探针守住。

**三层结构**（缺任一层就会脱钩）：

```
生成脚本   ← 唯一改动入口，按参数产出
   ↓
素材真源   ← 几何的唯一定义处
   ↓
内联副本   ← 运行环境读不到文件时的必要妥协
   ↓
一致性探针 ← 守住真源与副本逐字相同
```

| 硬约束 | 原因 |
|---|---|
| 观感调整回生成脚本改参数 | 手改素材会在下次重生成时被覆盖，且与副本静默脱钩 |
| 仿射变换烧进路径坐标 | 设计工具与自建光栅化器对 `transform` 的支持均无保证 |
| **注释里的数字也是副本** | ⚠️ 没有任何探针会发现它过期，只能靠全量搜索旧值 |

**图形本身的两条铁律**：

- **前景层不能靠镂空表达形状。** 镂空透出的是底色，形状会消失；须反相为实体，笔画才拿到自己的像素。
- **emoji 不是图标。** 由系统字体渲染，跨端字形不一致、颜色不受主题控制、无法参与状态变化。凡要跟随语义色或参与选中态的位置，一律矢量。

### 阶段 ④ 落原型

| 动作 | 说明 |
|---|---|
| 分批执行 | 避免单次超时；有依赖的批次须标明顺序 |
| 重跑前先清空 | 否则叠加出重复产物 |
| 幂等设计 | 生成逻辑要能反复执行 |

> **不要用工具的聚合报告当验收判据，先确认它的噪音构成。**
>
> 校验类 API 常把内部容器一并计入，返回上百条噪音并**截断掉唯一有用的信号**。
>
> **判定动作**：找出该报告里「唯一能证伪」的那类信号，改用逐节点接口单独核。
>
> ⚠️ 举证：某原型校验 API 的 `unreachable` 把每个内部容器都当页面算可达性，把 `broken-reference` 淹没；改用逐节点接口才核出全部连线。

#### 推进节奏：一页一图一定稿，不攒批

> **「全部写完再统一出图」会同时放大两项成本，且这两项互相乘。**

| 成本 | 机制 |
|---|---|
| **缺陷互相掩盖** | 几十张图一起看，注意力被摊薄；小位移在缩略图里根本不成立为「缺陷」 |
| **一处修复要全量重跑** | ⚠️ 生成器通常在入口就清空画布（见「重跑前先清空」），**因此不存在只重跑某一批的局部修复**；叠加上「设计工具同一时刻只运行一个插件」，每次修复都是一轮串行等待 |

**判定动作**：动手前读一遍生成器入口，确认「能不能只重跑一部分」。答案为否时，节奏必须是**写一页 → 出这一页的图 → 定稿这一页**，而不是攒完再看。

> **这条与阶段 ⓿ 是同一件事的两端**：⓿ 保证看得见，本条保证**看得够勤**。通道通了但攒到最后才看，无反馈期一样长。


### 阶段 ⑤ 验收沉淀

| 动作 | 说明 |
|---|---|
| 守门型脚本全绿 | 前置条件，不是验收本身 |
| 用户实机跑一轮 | **看形不看数**——数由脚本负责 |
| 三处留档 | 过程留档记「做了什么」、问题沉淀记「踩过什么坑」、术语表记「名词怎么统一」 |

## 3.4 六条判断准则

前四条针对同一类失效：**产物看起来完全正常，但结论是错的。** 后两条针对另一类：**判断本身没错，但做得太晚或用错了环境。**

### 准则一：验证必须验产物本身

> **「A 与 B 一致」在 A、B 同源于一个错误时永远为真。**

**判定动作**：凡能写成「与另一份数据相同」的检查项，都要再补一条**直接检查产物属性**的——渲染成图、量实际像素、与原始输入比对。

**两个衍生形态**：

- **用正则 / 索引 / 位置约定取值的断言，结构一改就静默失效。**
- **文档里关于工具的可数事实（段数、条目数、覆盖范围）必须回代码数。** ⚠️ **留档转述会成为错误口径的传播链**（同 1.7）。

**推论：断言要防的是「看起来正常的失败」。** 断言的价值不在捕获报错（报错本来就会暴露），而在捕获**静默降级**——形状消失但仍是个漂亮色块。

> **判定动作**：凡自动生成的图形资产，都要找出一个「失败后仍视觉自洽」的场景，专门为它写断言。

### 准则二：测量方法本身需要验证

> 同一个几何量，两种方法可以测出量级完全不同的结果。

**判定动作**：得到「异常但说得通」的数字时，先换一种独立方法复测，再决定要不要动手。

**正面形态**：两套独立实现算出同一个数，**这个一致性本身就是「测量方法没错」的证据**。同理，探针换数据源后若实测值与历史输出逐位一致，即证明换源没有改变几何。

⚠️ 违背此准则的最坏后果不是白干，是**拿错数字让用户做了一次决策**。

### 准则三：评审载体的规格必须等于实际使用规格

> **错误的评审载体产出的是「双方都签过字的错误决策」**，比单方失误难纠正得多。
>
> **这类错误不会被任何脚本发现——脚本测产物，而缺陷在提问方式里。**

| 约束 | 原因 |
|---|---|
| 只呈现产品中真实存在的规格 | 多给一档更舒适的尺寸，等于给用户一个现实中不存在的判断依据 |
| 放大只允许最近邻插值 | 平滑插值会把断裂的 1px 细节补成连续边，等于用算法替用户做决策 |
| 载体里的几何须与最终产物同源 | 否则又是一次「批准的是 A、交付的是 B」 |

⚠️ 举证：一枚小尺寸图标的对照图并列了多档放大规格，其中一档在大尺寸下观感良好、据此被推荐并选定，而它在真实使用尺寸下关键结构已经断裂。**两边都有据可依，唯一的错误在于「据」里混进了不存在的规格。**

### 准则四：约束要找对那个先崩的

> 同一个参数往上抬时，**最先崩的往往不是最显眼的那个指标**。

**判定动作**：扫描参数前先问「谁会先撑不住」，把它测出来，再逐档扫描找达标上限。

⚠️ 举证：守门断言只测了主体笔画粗细，余量看着极其安全；而同一档参数下，衬托主体的细环带已经断裂。守门人当时守的是一条永远不会触发的线。

**配套铁律：放宽一条过期断言时，必须在同一次改动里补上原本缺失的约束。** 只改数字等于白挨这一刀。

> **判定动作**：放宽前先说清「原断言在保护什么、为什么这个保护现在不需要了」；说不清就是探针拦对了。

### 准则五：守门断言与被守对象同期编写

> **同一条断言，写在产物之前只值几行代码；写在产物定稿之后，要额外付一次全量重跑与一次回改。**

**必须先排除一个误解**：这不是说断言写多了浪费。小位移、触控下限、状态间尺寸一致这几类，**肉眼在缩略图上根本看不出来**，只有断言能守住。问题从来不在断言的数量，在**它们的编写时序**。

**识别信号**：断言总数在短期内链式增长，且每一步都紧跟一次「出图挖出缺陷」的提交。这说明断言正在被用作事后清算工具，而不是同期护栏。

> ⚠️ 举证：某轮两天内断言数经历十次递增，每一步背后都是一次出图挖出的缺陷；同期守门脚本的改动次数与产品代码近乎一比一（33 次对 36 次，2026-09 观测值）。**近一比一本身就是信号**——它意味着护栏没有随产物长出来，而是在追着产物补。

**判定动作**：默认把断言与被测代码放在**同一次提交**里。做不到时，记下欠账并在下一次提交补上，不要攒到阶段末。

#### 衍生：每条断言写完就反向验一次

守门型断言的失效形态是**恒绿**，而恒绿与「真的没问题」在输出上完全一样。三种典型：

| 失效 | 机制 |
|---|---|
| **补完实现让断言空转** | 断言写成「只检查缺失的那一档」，缺口补齐后循环体再无入参，恒绿 |
| **点名式判据覆盖不到新对象** | 断言逐个点名要检查的对象，此后新增的同类对象从不进入检查范围 |
| **豁免条目变成僵尸** | 被豁免的对象已改名或删除，豁免条目却仍在，替一个不存在的对象永久开口子 |

**判定动作**：每写一条断言，就**故意把被测对象改坏一次**，确认它真的报红，再改回来。判据一律**遍历真源清单**而非逐个点名，这样新增对象自动纳入。

**豁免机制本身也要被守门**：补一条「豁免名单里的每个名字都必须在产物中真实存在」的断言。

### 准则六：离线校验与实机取证测的是两回事，不可互相冒充

> **离线校验跑在模拟层上，实机取证跑在真实引擎里。凡属「引擎默认行为」的缺陷，只在实机现形。**

模拟层不复现渲染引擎的物理行为——描边对布局的挤压、字体度量、光栅化取整。它算出的几何只与显式设过的属性有关，因此**一个从未被赋值的属性的默认值造成的缺陷，在离线侧根本不存在**。

| 分工 | 能证明什么 | 不能证明什么 |
|---|---|---|
| **离线校验** | 属性值是否被显式设成约定值 | 该属性在真实引擎里的几何后果 |
| **实机取证** | 真实几何 | 无法防回归（要有人手动跑） |

**判定动作**：离线断言核**属性值**，并在注释里写明「本层不模拟几何后果，那个数字是实机量出来的」。

> ⚠️ 不写明这一句会有后患：下一个人以为几何已被守住，从此不做实机取证。

#### 衍生一：同一约束在两侧的数字不同，须双栏记录

模拟层按公式估算的行高、字宽必然与真实引擎不同。**两侧的数字都对，但混用会让断言在某一侧永远报红。**

**判定动作**：涉及尺寸的豁免项或基线值记两栏——**离线值用于防回归，实机值写进给下游看的规格**。只记一个的下场是：记离线值则规格给出下游量不到的数字，记实机值则断言在离线侧恒红。

⚠️ 此时若图省事去调低阈值、或让该对象无条件跳过检查，就造出了一条永久免检的口子。

#### 衍生二：模拟层若靠逐属性手抄搬运，漏一个就是一条假信号

**判定动作**：模拟层的对象构造与克隆逻辑改为**属性白名单驱动**，而非手抄清单。

> ⚠️ 举证：同一模拟层三次漏搬属性。第三次靠「源码明明设对了却报红」的**假红**引出——而这纯属运气：**只要模拟层的默认值方向反过来，同一个漏抄就表现为假绿，缺陷会被探针背书为「已修复」。**

**配套**：模拟层的属性默认值必须显式写出，不留 `undefined`——否则「真的设成了该值」与「属性根本不存在」在断言里无法区分。

## 3.5 一个可迁移的缺陷模型：相邻对照

本节单列，因为它同时是**缺陷的成立条件**与**修不修的裁定依据**，在准则一到六里都会用到。

> **一两个像素的尺寸偏差，在孤立对象上不构成缺陷；只有旁边站着一个同规格的兄弟供人比对时才现形。**

**成立条件**：同规格的两个实例出现在同一视野内且**边界相接**——横排按钮共用一条底线，纵向列表卡共用一条侧边。此时偏差从「尺寸不准」升格为「对不齐」，而人眼对后者极其敏感。

**典型成因（跨工具通用）**：**布局引擎默认把描边计入盒模型尺寸。** 于是「加一道 1px 边框」这个看似纯视觉的改动，实际让该对象上下各胀 1px、总高多 2px——而规格文本写的往往是「加边框，其余一切不变」。

| 侧 | 处置 |
|---|---|
| 设计工具侧 | 关掉「描边计入布局」的开关 |
| **CSS 侧** | 用 `outline` 或 `box-shadow`，**不要用 `border`**——`border` 同样占盒模型尺寸，会复现完全相同的位移 |

> **规格必须同时给出两侧的做法。** 只写设计工具侧那半句，下游看到的是一条与自己无关的插件配置，于是照「加 1px 边框」直译，位移在浏览器里重现。

**两条判定动作**：

| 场景 | 判据 |
|---|---|
| **某处偏差要不要修** | 看**附近有没有同规格的无描边兄弟**，不看偏差有多大。「无对照物」是正当的豁免理由，但必须写下来——将来它可能长出对照物 |
| **修在哪一层** | 看**这个集合会不会长出新的带描边成员**。开放集合（组件变体族）在**共用构造器里无条件修**，条件判断只会漏；闭合集合（固定几档状态）按档修即可 |

**检索同类风险按属性组合扫，不按症状扫**：判据是「带描边 + 自动布局 + 该轴自适应 + 未关掉开关」。命中量会远大于真受害者数（某轮命中上百处，真受害者只有一族），因此**命中后必须逐类核对照物**，不可一律改。


## 3.6 反模式

| # | 反模式 | 正确做法 |
|---|---|---|
| 1 | 按字面执行用户的观感描述 | **观感词不是实现描述。** 先读真源确认现状，再把观感翻译成可调参数——⚠️ 「镂空更好看」在实现层可能压根不是镂空，按字面重做会推倒一个结构上正确的实现 |
| 2 | 脚本 FAIL 就放宽断言 | 默认假设「脚本对、改动错」（见准则四配套铁律） |
| 3 | 注释写对了就以为实现合规 | **自我警示注释必须回读下方实现。** ⚠️ 读者看到注释就默认下方合规，评审时极易漏过 |
| 4 | 照着待办清单的描述动手 | **跨了多个工作日的待办项，动手前先验前提是否还成立** |
| 5 | 宣布「两种实现等价、无需改动」 | 实测一次。⚠️ 视觉上无差别的两种实现，IoU 实测未必为 1 |
| 6 | 手改生成出来的素材 | 回生成脚本改参数 |
| 7 | 用工具聚合报告当验收判据 | 先确认噪音构成，找出唯一能证伪的信号 |
| 8 | 搜一遍就认为改全了 | 补一条「禁用形态不得出现在可执行代码中」的全量静态检查兜底 |
| 9 | 先攒产物、以后再打通出图 | ⓿ 阶段先导一张空白图落盘，通道不通不许往下走（3.2.4） |
| 10 | 写完全部页面再统一出图 | 一页一图一定稿；先读生成器入口确认能否局部重跑（④） |
| 11 | 产物定稿后回头补一批断言 | 断言与被测代码同期提交（准则五） |
| 12 | 断言全绿就认为守住了 | 每条写完故意改坏一次验它报红；判据用遍历而非点名（准则五） |
| 13 | 豁免条目只管加不管清 | 补「豁免名必须真实存在」的断言，防僵尸条目（准则五） |
| 14 | 离线全绿就跳过实机取证 | 引擎默认行为类缺陷只在实机现形（准则六） |
| 15 | 离线值与实机值混用一个数字 | 双栏记录，注明哪个防回归、哪个进规格（准则六） |
| 16 | 模拟层靠手抄清单搬属性 | 改属性白名单驱动；默认值显式写出（准则六） |
| 17 | 见到小位移就一律修 | 先看有无相邻对照物；无对照是正当豁免但须写下（3.5） |
| 18 | 规格只写设计工具侧的做法 | 同时给出 CSS/客户端侧口径，否则下游直译会复现（3.5） |

## 3.7 落地检查清单

- [ ] **出图通道已导出一张空白图落盘，通道选型结论已留档（⓿ / 3.2.4）**
- [ ] 交付链下一棒是谁，据此定了投入维度（3.1）
- [ ] 权威源里带阈值的条款都附了实测值或「未验证」标注（① / 准则二）
- [ ] 进入审批的视觉产物都已落盘（②）
- [ ] 几何有唯一定义处，副本有一致性探针（③ / 准则一）
- [ ] 每条「与另一份数据相同」的断言都配了一条产物属性断言（准则一）
- [ ] 为「失败后仍视觉自洽」的场景写了专门断言（准则一）
- [ ] 评审载体只含产品中真实存在的规格，放大用最近邻（准则三）
- [ ] 已识别「谁会先崩」并为它写了守门断言（准则四）
- [ ] 校验 API 的噪音构成已确认，未拿聚合报告当判据（④）
- [ ] **已确认生成器能否局部重跑；不能则按一页一图一定稿推进（④）**
- [ ] **断言与被测代码同期提交，无「攒到阶段末」的欠账（准则五）**
- [ ] **每条守门断言都做过一次反向验证（故意改坏 → 确认报红）（准则五）**
- [ ] **判据用遍历真源清单，未用逐个点名（准则五）**
- [ ] **豁免名单有「名字必须真实存在」的反僵尸断言（准则五）**
- [ ] **离线断言只核属性值，且注释写明「几何后果由实机负责」（准则六）**
- [ ] **涉及尺寸的豁免项已双栏记录离线值与实机值（准则六）**
- [ ] **模拟层属性搬运为白名单驱动，默认值无 `undefined`（准则六）**
- [ ] **小位移缺陷已按「有无相邻对照」裁定，豁免理由已写下（3.5）**
- [ ] **描边类约束已同时给出设计工具侧与 CSS 侧口径（3.5）**

---

# 第四章：架构设计与多轮评审

**问题定义**：把规格文档转成一份**可施工**的架构基线——不只是选型清单，而是每处技术选择都能对回依据、每条验收项都能被第二个人独立执行出相同结论。

**核心主张**：**架构文档的失效方式与代码相反。** 代码写错会编译失败或报错，架构文档写错则完全静默——它内部自洽、格式合规、读起来毫无破绽，缺陷只在施工时以「这里没说怎么做」或「两份文档打架」的形式爆出，而那时改动成本已是设计期的数十倍。因此这一章的全部准则都指向同一件事：**用可执行的判定动作替代「看起来没问题」。**

## 4.1 适用边界

| 判断项 | 适用 | 不适用 |
|---|---|---|
| 上游输入 | 已有规格文档（PRD / 需求说明）作为口径源 | 需求仍在探索期，口径未定 |
| 交付目标 | 要据此施工，不是内部讨论稿 | 技术预研、方案比选 |
| 约束强度 | 有明确的人力 / 工期 / 成本硬约束 | 资源不设上限 |
| 文档数量 | 架构文档与规格文档分立，且都会被开发翻阅 | 只有一份文档 |

**这一章不覆盖**：架构风格本身怎么选（单体还是微服务、什么数据库）——那取决于具体约束，没有可迁移结论。**可迁移的是「怎么把选择固定下来并证明它没漏」。**

## 4.2 依据源模式：每处选择都要能对回一条 ID

> **架构文档最常见的腐坏形态不是写错，是「不知道为什么这么写」。**

半年后回看一处配置，没人记得当初是权衡后的结论还是随手写的默认值，于是没人敢动它，也没人敢依赖它。

**做法**：架构文档之外单独维护一份**依据源**（本项目形态为「统一计划文档」），内含一组编号约束（本项目称 R-ID）。规则三条：

| 规则 | 说明 |
|---|---|
| **架构文档不新增口径** | 它只是依据源的展开。文档里出现而依据源里没有对应编号的选型，视为无依据 |
| **编号只增不改号** | 内容修订时保留原号并注明修订日期；新约束取下一个空号 |
| **索引表双向可查** | 架构文档末尾维护「本文档小节 → 依据编号」映射表，任何一节都能查到它站在哪条约束上 |

⚠️ 举证：某轮把 JDK 版本从 17 升到 21，改动落在四份文件共七处。若无索引表，「还有哪里写着 17」只能靠全文搜索加肉眼判断；有索引表则直接定位到「R1 及其展开的三节」。

### 衍生：评审产生的新约束必须回补成新编号，不能只改正文

评审、复核、需求变更**必然**产生新约束。只改架构文档正文不回补依据源，后果是下一轮评审读依据源时看不到这条约束存在，于是同一个问题被重新讨论一遍，或者更糟——被反向改回去。

**判定动作**：每轮评审收口时问一句「本轮有几条新约束进了依据源」。答案为零而落地修正非零，说明回补漏了。

## 4.3 准则一：多视角评审要换**视角**，不是换 persona 数量

> **同质视角堆再多也只能验证「文档内部是否自洽」。**

本项目走了四轮评审，价值差异极大，原因不在 persona 数量而在视角是否真的正交：

| 轮次 | 视角 | 回收 → 合并 | 暴露的问题类型 |
|---|---|---|---|
| 一 | 服务端与治理（6 persona） | 43 → 34 | 文档内部矛盾、口径冲突 |
| 二 | **前端** | 36 → 18 | **服务端接口的客户端对侧一条都没登记** |
| 三 | **QA / 验收** | 48 → 21 | **「做完怎么证明它对」几乎是空的** |
| 四 | 回写核对 | — | 架构文档已裁定但规格文档从未同步 |

⚠️ 举证：第一轮 6 个 persona 全部站在服务端视角，回收 43 条 findings 看起来很充分，但**没有一条**指出「客户端当前零 HTTP 依赖、全部状态是同步的」这个排期级风险。它在第二轮第一个 persona 就被发现。视角的正交性比数量重要一个量级。

**判定动作**：列出已跑过的视角，问「谁会为这份架构付出代价，而他的视角还没跑过」。典型未覆盖方：下游实现方、验收方、运维方、安全方。

### 衍生：persona 评审覆盖不了「方案在真实攻击面下是否成立」

⚠️ 举证：三轮评审（服务端 / 前端 / QA）**没有任何一个 persona** 提出「图片直传给前端会不会有安全问题」，而用户一句质疑就暴露了三个真实漏洞（元数据泄露窗口、CDN 绕过处理规则、孤儿对象）。

原因是 persona 读的是文档，它们擅长发现「文档说的和文档说的不一致」，不擅长发现「文档说的在现实中不成立」。**后者需要去查外部真源**（云服务商文档、平台审核指引、语言运行时行为）。

## 4.4 准则二：subagent 的结论性判断必须实测，目录清单可以信

> **这条在本项目触发三次，每次都抓到不实 findings。**

| 轮次 | 抓到的不实断言 | 实测结果 |
|---|---|---|
| 二 | 报告引用某文件路径 | Grep 报 `os error 2`，路径根本不存在 |
| 二 | 「某枚举有两个不同定义」 | 实测只有一个，另一文件是 `import` 它 |
| 三 | 「某常量应在第 22 行」 | 第 22 行是注释、第 23 行才是常量，文档原本正确 |
| 三 | 「内存表与正文冲突」 | 两处实测一致，真冲突在另外两个文件 |

**可信度分层**：

| 类型 | 可信度 | 处置 |
|---|---|---|
| 目录清单、文件是否存在 | 高 | 可直接采信 |
| **具体行号、具体路径的断言** | **低** | **必须实测** |
| 结论性判断（「这两处冲突」） | 低 | 必须实测 |
| **persona 内部自相矛盾的 finding** | **最低** | **优先怀疑 persona 而非代码** |

⚠️ 举证：某 persona 的 `suggested_fix` 与它自己的 `description` 前后矛盾，实测后确认是 persona 读错，文档原本正确。若不实测就落地，会把正确的文档改错。

**判定动作**：落地任何一条评审 finding 前，若它引用了行号或路径，先跑一次 Grep / Read 确认。这个成本远低于改错再改回来。

## 4.5 准则三：新增枚举值前先查唯一口径源是否已占用

> **本项目最典型的自伤：我自己立了「口径源唯一」的规矩，然后自己第一个破了它。**

⚠️ 举证：架构文档 §6.4 明写「规格文档某节是错误码唯一口径源」，但我在新增乐观锁冲突码时**直接取了一个看起来空着的编号**，未查规格文档是否已占用——实际它早已被「命中敏感词」占用。这个冲突不会有任何工具报错，两份文档各自都合法，只在开发同时翻两份文档时才会撞上。

**判定动作**：新增任何**受控枚举值**（错误码、状态码、事件名、配置键）前，先在唯一口径源里搜该值。搜到即换号。

**这条的一般形态**：立了规矩之后，**规矩的第一个考验对象是立规矩的人**。凡是自己新增内容时觉得「这个不用查吧」的地方，正是规矩最容易失效的地方。

## 4.6 准则四：验收项必须能被第二个人独立执行出相同结论

> **写不到这个程度的验收清单，是「验收剧场」——每一项都能签字通过，但签字不代表功能真的达标。**

⚠️ 举证：某轮 QA 视角评审的核心判断是「三个阻塞级验收项没有一条能被第二个人独立执行」。原文只有「P95 ≤ 300ms」一个数字，没说测哪个值、谁发压、并发多少、跑多久、不过怎么办。

一条合格的验收项要回答五个问题：

| 问题 | 反例 | 正例 |
|---|---|---|
| **测哪个数** | 「响应时间」 | 服务端 `rt_ms`，不用客户端计时（后者含缓存与渲染） |
| **谁执行、用什么** | 未写 | 压测机与被测机分离（同机发压会抢 CPU） |
| **什么规模** | 未写 | 并发 20 跑 5 分钟，丢弃前 30 秒 |
| **判据** | 「达标即可」 | 逐接口单独出数，**任一不过即整项不过**（混算会让慢接口被快接口稀释） |
| **不过怎么办** | 未写 | 重跑上限 3 次，全废即判定机型不满足并进入升配评估 |

### 衍生一：免责条款必须带次数上限

⚠️ 举证：「共享 CPU 的 steal 持续 >10% 时结果不算有效数据」这条免责合理，但**没写重跑上限**，理论上可以一直重跑到某次刚好没有邻居抢占，然后拿这次数据签字。

**判定动作**：每条免责条款后面追问「这个豁免能用几次」。没有上限的豁免等于没有验收。

### 衍生二：判定权不能完全在被考核方手里

⚠️ 举证：某指标的整条判定链都在客户端进程内，客户端少计一段耗时就能把成绩改好，且不留痕迹。修法是补一条**服务端交叉核对**（客户端上报的网络耗时与服务端同 ID 的处理耗时相差须 ≤50ms）。

### 衍生三：自我满足的校验等于没有校验

⚠️ 举证：「四段耗时之和必须等于总耗时（±1ms）」看起来是好校验，但只要实现成 `第四段 = 总耗时 - 前三段`，恒等式 100% 通过而四段数据全是错的——**而这恰恰是最省事的写法**。

**判定动作**：为每条校验问一句「有没有一种省事的实现能让它必然通过」。有，就必须补一条被测方无法自证的外部校验。

### 衍生四：分母的排除项必须封闭声明

⚠️ 举证：某成功率指标有四类排除项，但没有「只有这四类」的封闭声明。实现时多排除一类就能让成功率变好，**且从看板上完全看不出来**。

**判定动作**：凡是比率型指标，分母的排除集必须写成封闭列表，新增须登记并附实测占比，未登记的排除视为该期指标无效。

### 衍生五：区分「真通过」与「真空通过」

⚠️ 举证：「不存储身份证」这条隐私审计在首批是**真空通过**——实名功能整体在第二批，此时扫描必然为空，签字毫无信息量。已改标为「本批不适用 / 下批验收」而非「已通过」。

## 4.7 准则五：跨端共算的值必须给测试向量，不能只给算法描述

> **判据是「这个值由几方独立计算」，不是「算法复不复杂」。**

⚠️ 举证：某网格 ID 由三方独立计算（服务端写入、服务端解析入参、客户端拼缓存键）。规格只写「约 500m 网格，由经纬度计算」。三方算得不一致的后果是**缓存永不命中且地图取不到数据，而且不报任何异常**。

更隐蔽的是取整方向：多数语言的 `toInt()` 向零取整、`floor` 向负无穷，对负数结果不同。而**网格步长比小数精度粗数百倍**，精度方向错了会被网格吸收，只在极少数坐标上暴露。

**判定动作**：

| 项 | 要求 |
|---|---|
| 算法 | 写成无歧义表达式（`floor(x * 100000) / 100000`，不写「截断到 5 位」） |
| 测试向量 | 给**带期望值的表**，含跨零、负边界、精度方向探针 |
| 中间值 | 同时断言中间值，否则精度错误会被下游粗粒度吸收 |
| 校验 | 给出格式正则，让非法值在入口就被拒 |

## 4.8 准则六：延期的是逻辑，不是数据

> **详细推导见问题沉淀 `docs/solutions/workflow-issues/data-model-reservation-for-deferred-features.md`，此处只留判定动作。**

一项功能被判定为「本批不做」时，**不等于本批什么都不做**。

**判定动作**：追问一句「不做预留，后续批次是否必须在**已有真实数据**的库上改表结构」。是，则本批必须建表——建表时多一张中间表成本趋近于零，等有真实用户数据再改结构，迁移不可回滚。

**衍生**：延后清单里看似独立并列的第三方接入，可能有外部合规强制共交付约束（如某平台审核指引要求上线任一第三方登录就必须同时提供其自有登录）。**判定动作**：延后项超过一个时，逐个查其外部合规依赖，不要假设它们可以拆批。

## 4.9 准则七：架构裁定必须回写规格文档

> **两份文档打架时，人会信规格文档，因为开发时真正会被翻的是它。**

⚠️ 举证：三轮评审在架构文档里做了大量裁定，但规格文档本体一直没同步。这是最危险的一类不一致——它不是「有一处写错了」，而是「有一整套裁定，开发看不到」。

**判定动作**：每轮评审收口时，把「本轮在架构文档里改了什么」逐条对照规格文档，凡是规格文档也有对应表述的，一律回写。回写完把架构文档里「规格文档需按此同步」这类**状态标记翻转**为「已于某日同步」，否则下次读的人会以为还有待办。

## 4.10 反模式

| # | 反模式 | 正确做法 |
|---|---|---|
| 1 | 多加几个 persona 提升评审质量 | 换**视角**而非加数量；问「谁会为这份架构付代价而视角未跑过」（4.3） |
| 2 | 采信 subagent 报告的行号与路径 | 落地前实测；persona 自相矛盾时优先怀疑 persona（4.4） |
| 3 | 新增枚举值时挑一个「看起来空着」的 | 先在唯一口径源里搜该值（4.5） |
| 4 | 验收项写「P95 ≤ Xms」就算写完了 | 补齐测哪个数 / 谁执行 / 什么规模 / 判据 / 不过怎么办（4.6） |
| 5 | 免责条款不写次数上限 | 每条豁免追问「能用几次」（4.6 衍生一） |
| 6 | 判定链全在被考核方进程内 | 补一条对方无法自证的外部交叉核对（4.6 衍生二） |
| 7 | 加一条恒等式校验就认为守住了 | 问「有没有省事实现能让它必然通过」（4.6 衍生三） |
| 8 | 比率型指标的排除项开放式列举 | 写成封闭列表，新增须登记附占比（4.6 衍生四） |
| 9 | 扫描为空就签「已通过」 | 区分真通过与真空通过，标注「本批不适用」（4.6 衍生五） |
| 10 | 跨端共算值只给算法描述 | 给带期望值的测试向量表，含边界与精度探针（4.7） |
| 11 | 功能延期就什么都不做 | 追问是否会导致后续在真实数据上改表结构（4.8） |
| 12 | 延后的第三方接入按独立项排期 | 逐个查外部合规强制共交付约束（4.8 衍生） |
| 13 | 架构文档裁定完就算结束 | 回写规格文档并翻转状态标记（4.9） |
| 14 | 评审修正只改正文 | 新约束回补成依据源的新编号（4.2 衍生） |
| 15 | 用「文档已自洽」判断架构成熟 | 自洽只是必要条件；还需查外部真源验证方案在现实中成立（4.3 衍生） |

## 4.11 落地检查清单

**依据与追溯**
- [ ] 每处技术选择都能对回依据源的一条编号（4.2）
- [ ] 架构文档末尾有「小节 → 依据编号」双向索引表（4.2）
- [ ] 本轮评审产生的新约束已回补为新编号，不只改了正文（4.2 衍生）

**评审充分性**
- [ ] 已列出跑过的视角，并确认下游实现方 / 验收方视角都已覆盖（4.3）
- [ ] 涉及外部服务与平台的方案已查过对方官方文档，不只靠 persona 读文档（4.3 衍生）
- [ ] 所有引用行号与路径的 finding 都已实测（4.4）

**口径一致性**
- [ ] 新增的受控枚举值都已在唯一口径源里搜过占用（4.5）
- [ ] 架构文档的裁定已回写规格文档（4.9）
- [ ] 「待同步」类状态标记已翻转为「已同步」（4.9）

**验收可执行性**
- [ ] 每条验收项都回答了测哪个数 / 谁执行 / 什么规模 / 判据 / 不过怎么办（4.6）
- [ ] 每条免责条款都带次数上限（4.6 衍生一）
- [ ] 判定链不完全在被考核方进程内（4.6 衍生二）
- [ ] 每条校验都问过「有没有省事实现能让它必然通过」（4.6 衍生三）
- [ ] 比率型指标的分母排除项是封闭列表（4.6 衍生四）
- [ ] 扫描类审计已区分真通过与真空通过（4.6 衍生五）

**跨端与延期**
- [ ] 由多方独立计算的值都给了带期望值的测试向量表（4.7）
- [ ] 每个延期项都追问过「是否导致后续在真实数据上改表结构」（4.8）
- [ ] 延后的第三方接入已逐个查外部合规共交付约束（4.8 衍生）

---

# 第五章：数据库设计与实测验证

**问题定义**：把架构基线与规格文档转成一份**可执行的 DDL**，同时给出索引、容量、保留策略这三类**带证据的结论**——而不是一份读起来合理的表结构说明。

**核心主张**：**数据库设计的产物是约束，而约束的证据必须取自线上契约，不能取自对查询的印象。** 这一阶段所有真正昂贵的错误都长着同一副面孔：造数跑通了、`EXPLAIN` 输出漂亮、数字精确到小数点后两位，结论**看起来经过了实测**——但被测的那条 SQL 不是线上会发出的那条。此时实测比不实测更危险：不实测只是没有结论，错误实测会产出一条**方向相反且带证据背书**的结论，并且真的会被采纳。

## 5.1 适用边界

| 判断项 | 适用 | 不适用 |
|---|---|---|
| 上游输入 | 架构基线已定（存储选型、分库策略、批次范围）+ 规格文档已给出字段级需求 | 选型未定、还在比选存储 |
| 交付目标 | 产出首版可执行 DDL 与迁移脚本 | 线上库的增量变更（那受既有数据约束，是另一套流程） |
| 数据现状 | **库中尚无真实数据**，结构可自由确定 | 已有真实用户数据，改结构不可回滚 |
| 验证条件 | 有可用的本地 / 容器数据库实例，能造数并跑 `EXPLAIN` | 完全无法起实例（则索引与容量结论只能标注为未验证） |

**这一章不覆盖**：范式选择、分库分表阈值、具体索引该怎么建——这些取决于具体读写形态，没有可迁移结论。**可迁移的是「怎么让索引与容量结论站得住，以及怎么不在这个阶段越界」。**

## 5.2 工具矩阵

| 工具 | 用途 | 不可替代之处 |
|---|---|---|
| **官方文档查询（如 context7 类文档检索）** | 核实索引类型、字符集、约束的**实际适用面** | 直觉与官方语义经常相反，而错误前提会污染整份设计（5.6） |
| **容器数据库实例** | 造数、`EXPLAIN` / `EXPLAIN ANALYZE`、行宽实测 | 索引是否被选中、行宽多少字节，靠推算永远不准 |
| **一次性验证脚本（shell / SQL）** | 承载造数与取证过程，可重跑 | 结论必须可复现；但脚本本身**不是交付物**（5.5） |
| **Grep 全文核查** | 复核跨文档引用与同一数字的多处副本 | 同一结论会散落在数份文档，改一处必漏其余 |
| **DDL 文件本身** | 作为字段级真源 | 设计文档描述结构，DDL 才是被执行的那份 |

## 5.3 流程骨架

```
① 读上游：从规格文档抽出字段级需求，从架构基线抽出存储与批次约束
② 结构定稿：表 / 列 / 类型 / 可空 / 默认值 / 枚举，先不定索引列序
③ 读路径清单：把「会被高频调用的查询」逐条列出，每条标注它的**真实 SELECT 列表**（取自接口响应契约）
④ 索引候选：按读路径给候选索引，只写验收标准（如「必须 index-only」），不预先规定列序
⑤ 起实例造数：分布 / 内容长度 / 查询语句三个维度都要对齐真实形态，造数前先设保真度门禁
⑥ 实测取证：EXPLAIN 定列序 → 耗时采样 → 行宽与索引体积实测
⑦ 容量反推：用实测行宽反推承载上限（先撞哪堵墙），而非预测规模
⑧ 回写：DDL、设计文档、规格文档、留档中同一数字的所有副本一次性同步
⑨ 全文一致性复核：跨文档引用、枚举取值、同名列宽度、被推翻的旧数字
```

**关键顺序**：③ 必须在 ④ 之前，且 ③ 的来源必须是**响应契约文本**而非记忆。这一步省掉，后面 ⑤⑥ 的全部证据作废（5.4）。

## 5.4 准则一：覆盖索引的判定门槛是 SELECT 列表，不是 WHERE 条件

> **`Using index` 只回答「你给的这条 SQL 是否覆盖」，它不会告诉你「你给的这条 SQL 不是线上那条」。**

覆盖索引的常见理解是「把过滤条件都放进索引」。这是**判定门槛错位**：过滤条件全在索引里只能让定位不回表，一旦 `SELECT` 列表里有任何一列不在索引内，仍然要回表取行。而验证工具在这件事上完全沉默——它忠实地评估你交给它的语句，语句本身选错了它不会有任何提示。

⚠️ 举证：某最高频读路径的覆盖索引，取证时用的语句是「主键 + 两个属性列」，`EXPLAIN ANALYZE` 输出 `Covering index scan`，据此写下「该路径走 index-only scan 是容量前提」这条最关键的设计约束。而该接口的响应契约里含**坐标两列**，这两列不在索引内——线上那条查询必然回表。把语句改成契约的真实列并把坐标补进索引后，索引宽度从 8 列变 10 列，单表二级索引体积占比由约 25% 升至约 30%，热数据全驻内存的时点提前，全部容量结论重算（均为某次运行的观测值）。

**判定动作**：

| 项 | 要求 |
|---|---|
| 证据来源 | 每条待验证 SQL 的 `SELECT` 列表必须能对回**响应契约的具体一节**，不能凭印象写 |
| 脚本形态 | 把该列表提为**单个变量**，脚本内所有取证语句共用；避免多处手写导致其中一处漂移 |
| 落地校验 | 索引 DDL 注释里显式写「前 N 列为过滤条件、后 M 列为响应字段」，并列出对应契约节号 |
| 复核信号 | 看到「覆盖索引」结论时，先数一遍**索引列集合是否包含响应字段全集**，再看 `EXPLAIN` |

## 5.5 准则二：造数的保真维度由测量目的决定，且测量目的必须回溯到线上契约

> **详细推导见问题沉淀 `docs/solutions/workflow-issues/test-data-fidelity-for-performance-claims.md`，此处只留可迁移准则。**

「造点数据测一下」隐含一个未声明的假设：造出来的数据在**与本次测量有关的性质**上与真实数据一致。这个性质集合就是**保真维度**。维度选错，测量本身依然成功，只是量的不是要量的东西。

至少三类维度会独立失真，且互不覆盖：

| 维度 | 失真形态 | 后果 |
|---|---|---|
| **分布** | 造数把行均匀撒开，真实数据高度聚集 | 单次查询命中行数远小于真实值，耗时严重偏乐观 |
| **内容长度** | 文本列填短字符串，真实是长文本 | 行宽偏小 → 容量与内存驻留结论全线偏乐观 |
| **查询语句** | 测的 SQL 与线上契约不一致 | 见 5.4，产出带证据背书的反向结论 |

**两类失真的危害不对等**，这是本准则的要点：分布失真通常让数字**明显好得可疑**，人会警觉；而内容长度与查询失真产出的是**完全合理的数字**——它落在预期区间内，读起来毫无破绽，因此会被直接采纳。

**判定动作**：

1. 造数前先写一句「本次测量要回答什么问题」，再从这句话倒推需要哪些维度保真；
2. 每个维度给**可执行门禁**并置于取证之前，门禁不过直接退出（例：命中行数低于阈值即判「分布不真实，本次 EXPLAIN 结论无效」并中止）；
3. 已发布的性能数字，追加一次反向检查：「支撑它的那条语句，此刻还等于线上契约吗」。

## 5.6 准则三：技术前提须查官方文档核实，直觉命中率低于预期

> **一个错误的技术前提不会只错一处，它会沿着设计文档、规格文档、留档层层复制。**

对某个索引类型 / 字符集 / 约束「能做什么」的直觉，往往来自二手材料或旧版本行为。而这类前提位于设计的最上游，错了以后所有下游描述都会自洽地围着它长出来，复核时反而更难发现——因为处处一致。

⚠️ 举证：某类专用索引被多处文档写成「用于按距离排序的加速手段」。查官方文档后确认：它只支持特定的几何类型、要求非空并显式声明坐标系，且**只对特定的空间谓词生效，对按距离排序本就不提供加速**——而设计里采用的列类型根本无法承载它。这条错误前提当时已扩散到规格文档与设计文档的多个小节。

**判定动作**：设计文档里凡出现「X 索引/特性用于加速 Y」这类**机制性断言**，落笔前查一次官方文档，确认三件事：① 支持的数据类型；② 硬性前提（非空、显式参数等）；③ **它实际加速的算子是哪几个**。第三条最常出错。

## 5.7 准则四：索引只规定验收标准，列序交给实测

> **在设计文档里写死列序，等于用推理替代了本可以实测的东西。**

列序是否最优取决于选择性、数据分布与优化器版本，这些在设计期都不确定。把列序当成「设计决策」写死，会带来两种损失：实测发现更优列序时要改文档（于是倾向于不改），以及后来的人无法判断这个列序是实测结论还是随手排的。

**判定动作**：索引条目分两段写——**验收标准**（如「该路径必须达成 index-only scan，不得回表」）由设计定，**列序**标注「由 `EXPLAIN` 实测确定」并在实测后回填，同时在 DDL 注释里留一句「列序由实测定」。这样后续任何人重排列序，只要验收标准不破就是合法改动。

## 5.8 准则五：无规模输入时，反推承载上限而不是预测规模

> **「够不够用」在没有规模输入时无法回答；「先撞哪堵墙」永远可以回答。**

设计期常缺少可靠的规模预测，于是容量一节容易写成「预计 X 万用户，因此没问题」——一个既无法验证也无法反驳的句子。

**替代做法**：用实测行宽与索引体积，反推在给定资源下**哪一项资源最先触顶、触顶时对应多少行**。产出形如「二级索引全驻内存在约 N 行处触顶，届时先出现的是内存压力而非磁盘容量」的结论。这类结论有三个好处：不依赖规模预测、可被后续实测推翻、直接指出该监控哪个指标。

**判定动作**：容量一节至少回答「先撞哪堵墙、在多少行处撞上、撞上前的观测指标是什么」。同时把结论所依赖的行宽标注为**某次实测的观测值**，并注明它随索引变更须重测（5.4 的举证正是索引变更导致行宽重算）。

## 5.9 准则六：设计阶段的越界，看产物里有没有需要拍板的选择

> **判据不是文件后缀，是「删掉它，本阶段的结论还成立吗」。**

设计阶段常需要起实例、造数、跑脚本，这些都会产生代码文件，但它们不必然是越界。真正的越界标志是：产物里**固化了本该由下一阶段拍板的选择**（依赖版本、目录结构、模块划分、框架配置）。

| 产物 | 是否越界 | 理由 |
|---|---|---|
| 造数与 `EXPLAIN` 取证脚本 | 否 | 删掉它，索引结论就失去证据 → 它服务于本阶段结论 |
| 工程构建配置（依赖版本、目录骨架） | **是** | 删掉它，本阶段结论不受影响；而它固化了工程搭建阶段的决策 |

⚠️ 举证：某次为了跑验证，顺手建了完整的工程骨架与构建配置。「一行业务代码都没写」不构成豁免——构建配置已经把语言版本、坐标命名、目录约定三项下阶段决策定死了，而这三项与本阶段任何结论都无关。

**判定动作**：对每个新产物问「删掉它，本阶段的结论还成立吗」。**成立即为越界**，应删除。另配一条正向要求：为本阶段结论服务的脚本要能**重跑复现**，否则结论等同于口头声明。

## 5.10 准则七：待办清单要逐项追问目的，无目的项应主动撤销

> **「该做的事」列表天然只增不减，因为删一项需要理由，加一项不需要。**

清单里会混入这样的条目：名字听起来负责（兼容性验证、扩展性评估），但对本阶段产物没有任何可能的影响——无论结果如何，DDL 一个字都不会改。它们消耗排期并制造虚假的完整感。

**判定动作**：每个待办项都追问一句「**这项做完，本阶段产物有哪一处可能因此改变**」。答不出具体位置的，当场撤销并在留档里写明撤销理由（写理由是为了避免下一轮又被加回来）。

## 5.11 准则八：跨文档章节引用会静默失效，收口前须做一次全文复核

> **章节号是一种会腐坏的引用，而没有任何工具会告诉你它已经失效。**

⚠️ 举证：某次在设计文档中间插入一节，其后所有章节号顺移，导致另一份文档里数处「见设计文档 §X」的引用全部指向错误位置。另有一处把**行号**误写成了章节号，读者按号去查只会找到不存在的小节。这两类错误都不会有任何工具报错。

一次收口复核至少覆盖四类：

| 类别 | 检查方式 |
|---|---|
| 跨文档章节引用 | 逐条打开被引位置，确认标题与引用意图一致 |
| 受控枚举取值 | 确认哪一份是真源（一般是 DDL），另一份只做展示层对照 |
| 跨库 / 跨表同名列 | 同一语义的列必须同宽；不同语义的同名列要注明差异原因 |
| 被推翻的旧数字 | 用 Grep 搜旧值，确认在所有文档中已无残留 |

**判定动作**：收口时用 Grep 搜关键标识符（索引名、指标数值、表名），逐一确认全部命中都是新定义。**只改「我记得改过的地方」必然漏**——同一结论通常有四到六份副本散落在设计文档、规格文档、计划文档与留档中。

## 5.12 准则九：字段级真源是 DDL，描述性文档是派生件

> **两份文档描述同一列而不一致时，被执行的那份赢。**

设计文档里的数据字典与 DDL 会各自演进，且不一致极易发生（类型宽度、可空性、默认值、枚举集合、生成列表达式）。必须明确单向关系：**DDL 是真源，字典是它的可读展开**。

**判定动作**：收口时做一次**结构层面的逐列对照**——列集合、顺序、类型、可空、默认值、枚举取值、索引列序、生成列表达式。目标是「结构层面零差异」；允许 DDL 注释的信息量大于文档（注释是就近说明），但**不允许结构本身有差**。发现差异时一律以 DDL 为准修文档，除非确认是 DDL 写错。

## 5.13 准则十：库名这类「无上游定义」的空缺，只会在被追问时暴露

> **凡是没有任何上游文档定义、而实现时又必须有一个值的东西，都会被默默填上一个。**

数据库名、schema 名、字符集与排序规则、时区、连接参数——这些通常不在规格文档也不在架构基线里，但 DDL 必须给出值。于是它们以「顺手写一个」的方式进入交付物，而后被下游当作既定口径引用。

**判定动作**：DDL 定稿时列出「本文件里出现、但无法对回任何上游文档的值」，逐项要么补进设计文档成为**正式条款**，要么标注为待定。识别方法很简单：对每个这类值问「这是谁定的」，答不出来的就是空缺。

## 5.14 反模式

| # | 反模式 | 正确做法 |
|---|---|---|
| 1 | 把过滤条件放进索引就宣布「覆盖索引」 | 判定门槛是 SELECT 列表全在索引内，且列表取自响应契约（5.4） |
| 2 | 取证 SQL 凭印象手写、多处各写一遍 | 提为单个变量，所有取证语句共用（5.4） |
| 3 | 「造点数据测一下」不声明保真维度 | 先写测量目的，倒推需保真的维度（5.5） |
| 4 | 造数后直接取证 | 造数与取证之间加可执行门禁，不过即中止（5.5） |
| 5 | 数字落在预期区间就认为可信 | 合理的数字才最危险；反查支撑它的语句是否等于线上契约（5.5） |
| 6 | 凭直觉断言某索引/特性能加速某操作 | 查官方文档核实类型、硬性前提、**实际加速的算子**（5.6） |
| 7 | 在设计文档里写死索引列序 | 设计只定验收标准，列序由 `EXPLAIN` 实测回填（5.7） |
| 8 | 容量一节写「预计 X 用户，没问题」 | 反推「先撞哪堵墙、多少行撞上、监控哪个指标」（5.8） |
| 9 | 用「没写业务代码」为越界产物豁免 | 问「删掉它结论还成立吗」，成立即越界（5.9） |
| 10 | 为本阶段结论服务的脚本不可重跑 | 结论必须可复现，否则等同口头声明（5.9） |
| 11 | 待办清单只增不减 | 逐项追问「做完哪一处产物会变」，答不出即撤销并记录理由（5.10） |
| 12 | 插入新小节后不检查外部引用 | 收口做一次跨文档引用复核（5.11） |
| 13 | 改数字只改「记得改过的地方」 | Grep 搜旧值确认全库无残留（5.11） |
| 14 | 设计文档与 DDL 各自演进 | 明确 DDL 为字段级真源，做逐列结构对照（5.12） |
| 15 | 库名 / 字符集这类值顺手填一个 | 列出无上游定义的值，补成正式条款或标为待定（5.13） |

## 5.15 落地检查清单

**读路径与索引**
- [ ] 每条高频读路径都标注了取自响应契约的**真实 SELECT 列表**（5.4）
- [ ] 覆盖索引的列集合已确认包含响应字段全集，而非仅过滤条件（5.4）
- [ ] 索引条目分「验收标准（设计定）+ 列序（实测定）」两段，DDL 注释注明列序来源（5.7）
- [ ] 涉及特殊索引类型的机制性断言已查官方文档核实其实际加速的算子（5.6）

**造数与取证**
- [ ] 造数前已写明本次测量要回答的问题，并据此列出需保真的维度（5.5）
- [ ] 每个保真维度都有置于取证之前的可执行门禁，不过即中止（5.5）
- [ ] 取证脚本可重跑复现，SELECT 列表提为单一变量（5.4 / 5.9）
- [ ] 已反查一遍：现有性能数字所依赖的语句此刻仍等于线上契约（5.5）

**容量**
- [ ] 行宽与索引体积是实测值，且标注为某次运行的观测值（5.8）
- [ ] 容量结论回答了「先撞哪堵墙 / 多少行撞上 / 监控哪个指标」（5.8）
- [ ] 索引发生变更后，行宽与容量结论已重测重算（5.4 / 5.8）

**阶段边界**
- [ ] 每个新产物都通过了「删掉它结论还成立吗」的检验（5.9）
- [ ] 待办清单每项都能指出「做完哪一处产物会变」（5.10）

**一致性收口**
- [ ] DDL 与数据字典完成逐列结构对照，结构层面零差异（5.12）
- [ ] 跨文档章节引用已逐条打开确认（5.11）
- [ ] 受控枚举已明确真源，展示层文案不入库（5.11）
- [ ] 跨库同名同语义列宽度一致，同名不同语义的已注明差异原因（5.11）
- [ ] 被推翻的旧数字已 Grep 确认全库无残留（5.11）
- [ ] DDL 中无上游定义的值已补成正式条款或标为待定（5.13）

---

# 第六章：详细设计与接口契约

**问题定义**：架构基线与 DDL 已定，要产出**两端各自照着就能写代码**的接口契约与详细设计。合格判据只有一条：照这份文档独立施工的两端能对接上，且不依赖任何一方的临场发挥。

**核心主张**：**这一阶段的缺陷有一个共同形态——它们在一半的情况下是对的。** 契约与详细设计不会编译失败，也不像架构文档那样至少能被「这里没说怎么做」暴露；它的失效方式是**部分生效**：一半的枚举值映射正确、一半的坐标算得一样、一半的重试沿用了正确的键。半对的缺陷比全错的贵一个量级，因为它会让人相信「机制是对的，只是某条数据有问题」，从而把排查方向永久带偏。

**因此本章的全部准则都指向同一个动作：把契约当代码读。** 写作视角只能验证「这段话是否自洽」，而本章的缺陷全部只在实现者视角下才现形——它追问的是「这行代码实际会怎么执行」和「这个名字指向的东西真的存在吗」。

## 6.1 适用边界

| 判断项 | 适用 | 不适用 |
|---|---|---|
| 上游输入 | 架构基线与字段级真源（DDL）已定 | 存储或分层还在比选 |
| 交付目标 | 两端**分别**据此施工 | 单人全栈实现，边写边定 |
| 端的数量 | 两端及以上，且用不同语言 / 运行时 | 同语言同进程内调用 |
| 契约形态 | 有机器可读的契约文件（如 OpenAPI）+ 人读的详细设计 | 只有口头约定或聊天记录 |

**这一章不覆盖**：接口风格怎么选（REST 还是 RPC）、分层怎么切——那取决于具体约束。**可迁移的是「怎么让契约在实现期不被两端各自解读成两个意思」。**

## 6.2 工具矩阵

| 工具 | 用途 | 不可替代之处 |
|---|---|---|
| **机器可读契约文件** | 字段、类型、必填、枚举、错误码的唯一真源 | 人读文档里的表格无法被脚本核对，契约文件可以 |
| **契约自检脚本** | 数接口数、`$ref` 断链、扩展字段覆盖率、错误码全集比对 | 这几类是**可数事实**，必须回文件数（同 1.7 / 3.4 准则一） |
| **静态分析器（两端各一套）** | 类型改动的波及面 | 一处基础类型改动的调用点数量，靠搜索永远不准 |
| **单元测试（映射专用）** | 契约取值 ↔ 本地枚举的双向映射 | 映射错误不报错，只表现为「一半能跑」（6.5） |
| **官方文档检索** | 第三方库与运行时的**实际行为** | 对库行为的直觉常与实际相反，而纪律建立在这个前提上（6.7） |

> **工具层的一条实测约定：长文档不要一次写完。** ⚠️ 写入类工具在单次内容过长时会间歇性丢参数（表现为报「缺少某字段」而非超长报错）。**处置**：先写骨架并留形如 `<!-- SECTION-MARKER-XXX -->` 的占位注释，再逐段把标记替换为正文，每段末尾留下一个新标记。这个模式还有个附带收益——每段都是独立可回读的改动。

## 6.3 流程骨架

```
① 抽依据：从架构基线与 DDL 抽出字段、约束、批次范围，明确「本阶段不新造口径」
② 写契约：机器可读文件先行；跨接口的全局约定内嵌在契约文件内部
③ 写详细设计：两端各自成节，凡涉及跨端一致的算法与映射，单列对照
④ 契约自检：可数事实全部回文件数（接口数、断链、错误码全集）
⑤ 换实现者视角复审：逐条追问「照这写会不会出错」，而非「写得全不全」
⑥ 落地取证：把纪律写成单测断言；涉及运行时语义差异的，实跑发布态
⑦ 回写与登记：口径冲突回写上游真源；未解决项登记成缺口，附「必须在什么之前处理」
```

**关键顺序**：⑤ 必须独立于 ③ 之后单独跑一遍，不能与写作合并。**写作视角与实现者视角无法同时在场**——写的时候在保证自洽，读的时候才会问「这行怎么执行」。

**关于 ②「全局约定内嵌进契约文件」**：基础约定、统一响应包、错误码表、客户端错误行为分类这类跨接口内容，写在契约文件的描述字段里而不是另立一份文档。判据是「读者要不要同时开两份文件才能实现一个接口」——要，就说明拆错了。

## 6.4 准则一：实现者视角的三个追问

> **同一段文字，在写作视角下完全无懈可击，在实现者视角下能查出结构性错误。差别不在仔细程度，在追问的内容。**

三个追问，按查出缺陷的密度排序：

| 追问 | 查出的缺陷类型 | 写作视角为何看不见 |
|---|---|---|
| **这行代码实际会怎么执行** | 把机制的存在误推成机制的保护力 | 论证读起来自洽，且多数情况下真的能跑 |
| **这个名字指向的东西真的存在吗** | 引用了「听起来理应存在」的类型 / 字段 / 表 | 名字的语义完全正确，只是没有实体 |
| **这两条规则同时生效时会怎样** | 见 6.6 | 每条单独读都无可指摘 |

⚠️ 举证（第一问）：某详细设计写「重试逻辑位于拦截器链最内层，重发时复用原始请求对象，因此幂等键天然沿用」。实际上该类库的拦截器**不是嵌套调用栈而是一个列表**（发出正序、返回倒序），标准重发写法会**重走完整的请求前置链**，负责写幂等键的那个拦截器再执行一次生成，幂等保护当场失效。**「位置」保护不了任何东西**——这句论证的每一个环节都成立，只有那个「因此」是凭空的。

⚠️ 举证（第二问）：详细设计的代码示例引用了两个类型，实现期发现**全项目都不存在**，是写作时凭「理应有这个东西」写下的。处置原则很重要：**按最小方案补一个只含所需能力的实现，或改写示例用已有类型表达同一语义——不为了对齐文档而新造类型。**

**判定动作**：详细设计定稿后，把里面出现的每一个类型名 / 字段名 / 方法名搜一次真源，命中为零的逐个裁定「补实现」还是「改写文档」。

## 6.5 准则二：「一半能跑」的映射比全错更贵

> **两套命名体系部分重叠时，错误的映射实现会在重叠的那一半上正常工作。**

这是本章最高频的缺陷源。典型成因有两类：

| 成因 | 形态 | 后果 |
|---|---|---|
| **部分同名** | 三套表示法（本地枚举名 / 契约字符串 / 紧凑传输码）中有一部分字面相同 | 按名字直接转换的实现，同名的那些通过、不同名的抛异常 |
| **序号偶然一致** | 传输用的整数码与本地枚举的声明顺序当前恰好相同 | 按下标取值现在能跑，靠的是巧合；任一侧增删一个值即错位 |

⚠️ 举证：某供需状态有三套表示法，其中一个取值三处同名、另一个不同名。于是「按名字取枚举」的实现**传一个值通过、传另一个抛异常**——比全错难查得多，因为它会让人相信映射是对的、只是某条数据脏了。同一处的紧凑传输码 `0`/`1` 与本地枚举下标当前完全一致，按下标取值能跑，但那是巧合。

**判定动作**：

| 项 | 要求 |
|---|---|
| 实现形态 | 一律**显式逐值映射**（switch / 字典），禁止「按名字取枚举」与「按下标取值」。显式写法**不是啰嗦，是把巧合换成约定** |
| 方向 | **上下行都要给**。只给「解析下行」这一半，调用方在上行处会就地用本地名字，而本地名字未必等于契约取值 |
| 单测 | 逐值双向断言，且**必须含不同名的那些值**——只测同名值等于没测 |

### 衍生：可空语义的字符串化会造出一个合法的错值

⚠️ 举证：某枚举参数的契约取值是字符串集合，其中一档用 `null` 表示「不限」。若上行处写成「把本地数值直接转字符串」，不限档会得到字符串 `"null"`——它是个**格式合法、语义错误**的值，服务端按参数校验拒绝，而客户端的报错通常只说「参数错误」不说是哪个参数。

**判定动作**：凡本地用 `null` / 空值 / 哨兵值表达特殊档位的字段，映射函数必须**显式处理该档**，并在单测里为它单列一条。

## 6.6 准则三：两条各自正确的规则会叠出第三种行为

> **逐条评审通过的规则集，仍可能整体错误。**

规则的正确性是在各自的上下文里论证的，而运行时它们同时生效。叠加产生的行为不属于任何一条规则的论证范围，因此**逐条复审永远查不出来**。

⚠️ 举证：规则甲「上报失败的批次退回队列**头部**」，规则乙「重试沿用原幂等键」。叠加后的实际路径是：首批失败退回队头 → 期间新采集若干条 → 重新组批（**内容已变**）→ 沿用旧键 → 服务端按幂等语义直接返回首次结果、业务逻辑一行不执行 → 客户端判成功并把整批出队。**整批数据一条未落库，且不产生任何错误日志，丢弃计数器也不会增加**（它只统计容量溢出）。可观测性系统自己成了盲区。而服务端「重复键但请求体不同须返首次结果」是幂等的**正确语义**，所以防线只能建在客户端。

**判定动作**：把本模块的规则列成清单，两两问一句「这两条同时生效时，有没有一条路径是两者都没覆盖的」。优先检查这三类组合：

| 组合类型 | 为什么危险 |
|---|---|
| **重试 / 退回 × 幂等 / 去重** | 重试改变了内容，而幂等键假设内容不变 |
| **缓存 × 失效通知** | 两侧对「什么时候算过期」的判据不同 |
| **限流 × 自动重发** | 重发把一次超限放大成持续超限 |

**取舍方向要在文档里写死。** 上例定为「**宁可重一条，不可丢一条**」——重复可在分析侧去重，丢失永久不可恢复。不写死方向，实现期会按当时最省事的方式选。

## 6.7 准则四：涉及第三方行为的纪律，取「全域正确」的写法而非「先去实测」

> **当一条纪律的正确性依赖于对第三方库 / 运行时行为的假设时，优先选择「在该行为的所有可能取值下都正确」的写法。**

这比「先实测清楚再决定」更省，且**彻底消除了假设失效的风险**——库升级、平台差异、配置不同都不会让它失效。

判断方式是画一张两行两列的决策矩阵：

| | 库行为是 A | 库行为是 B |
|---|---|---|
| **加这条防护** | 正确 | 正确 |
| **不加** | 正确 | 错误 |

矩阵长这样时，这条防护**不是待验证项，而是无条件纪律**——因为加了没有任何一种情况变坏。

⚠️ 举证：幂等键的写入改为「先判键是否已存在，不存在才生成」。无论该类库的重发是否重走前置链，这个写法都正确；不加则至少一种行为下失效。**代价是一行 `containsKey` 判断，收益是不必再关心库的实际行为。**

**反向约束**：矩阵里「加了反而变坏」的格子非空时，这条就**必须实测**，不能靠此准则跳过。

## 6.8 准则五：加约束时必须同时写下它的成立条件与失效信号

> **约束分两类：失效时会报错的，和失效时什么都不发生的。后者需要完全不同的守护方式。**

「加了一条唯一约束 / 校验 / 断言」这个动作，天然让人觉得风险已经关闭。但约束本身往往有**未被写下的成立前提**，前提一破，约束还在、不报错、只是不再挡住任何东西。

⚠️ 举证：某去重唯一键含一个时间列，其取值必须是**采集时刻**。这原本只是数据归属口径的一条规定，与去重无关；直到复核时才意识到它同时是去重的成立前提——若实现时把它改成**到达时刻**（一个看起来无害甚至更「准确」的改动），同批数据重传会拿到不同取值，唯一键当场失效。同一约束共有三条失效路径（预建的分表漏建索引 / 写入改用宽容模式 / 时间列换成到达时刻），**共同特征是全都不产生错误信号**，只表现为按次数计算的指标逐渐偏高。

**判定动作**：每加一条约束，在它旁边写下两句话：

| 要写的 | 作用 |
|---|---|
| **成立条件** | 「本约束依赖 X 取自 Y」——让下一个想改 X 的人当场看到代价 |
| **失效信号** | 「失效时不报错，表现为某指标偏高」——决定它该由测试用例守还是由断言 + 监控守 |

**推论**：**静默失效型约束必须配一条断言，且断言要能区分「约束生效」与「约束存在但没挡住」。** 只断言结果数量往往不够——见 6.9 举证。

## 6.9 准则六：容错与降级的方向要写死，且都不许指向「看起来更合理」的那一侧

本准则的两条都属同一形态：**最省事 / 最像样的那个实现，恰好是错的那个。**

### 6.9.1 宽容模式不许进入旁路链路

⚠️ 举证：某写入语句有两种写法，字面只差几个字符、都能消除重复键冲突。其中一种（宽容模式）**不只忽略冲突**，还会把数据截断、非空列写空、类型转换失败一并降级为警告。把它用在旁路链路（异步、允许丢、低关注度）上，等于**关掉这张表全部的数据质量报错**——而旁路链路本就无人盯着，脏数据可以积累很久。

另一个更隐蔽的错写法是「冲突时用新值更新」：它看起来更合理，实际会让后到的重复批次**覆盖首次写入的内容**，且**任何行数校验都看不出异常**。因此配套的断言必须**同时断言内容保持首次值**，而不只断言行数。

**判定动作**：对每一处容错开关问「它顺带宽容了什么」；对每一条断言问「有没有一种错实现能让它照样通过」（同 4.6 衍生三）。

### 6.9.2 降级不许兜成一个真实的业务取值

> **兜真实值会让数据问题伪装成设计问题，排查方向当场跑偏。**

⚠️ 举证：本地映射表查不到某个新增取值时，若兜底成任一真实分类的配色，现象是「颜色不对」——一个看起来该找设计的问题，而根因是本地映射表版本落后。改为兜一个**明显不属于既有色板的中性色 + 通用占位图标**后，现象直接指向「这条数据我不认识」。

同理，凡降级值会**流入用户可见的持久化内容**（如按分类给标题加前缀），一律**不加**而不是猜一个——猜错等于替用户改文案。

**判定动作**：为每个降级分支问「用户看到这个结果时，会去找谁」。答案指向错误的人，就是兜错了方向。

**配套**：查不到 / 取不到若是规格明文规定的**预期状态**，就不该以异常表达。用返回空值 + 调用方降级，并在类型上强制调用方处理。

## 6.10 准则七：展示层不得给出它无权判定的结论

> **前端显示「还剩 3 次」而下一次请求就被拒——应用当场自我否定。这不是偶发，是必然。**

成因是**判定维度与展示维度不对称**：服务端按多维（账号 / 设备 / IP / 熔断）限频，而返回给客户端的只有其中一维的剩余量。客户端拿一维的数字去承诺多维的结果，必错。

⚠️ 举证：某额度字段只回账号维度剩余次数，而实际限频有四维。**不能改为返回四维最小值**——那会把设备维度的限额暴露出去，可被反推出风控阈值。

**判定动作**：

| 项 | 要求 |
|---|---|
| 文案 | 剩余量一律附「以实际请求结果为准」，不做承诺式表述 |
| 状态同步 | 收到超限错误时**立即把本地剩余量刷成 0**，避免「还剩 3 次」与「已达上限」同屏 |
| 不解释维度 | 不告诉用户是哪一维超限——那等于把风控口径外泄 |

**一般形态**：**凡展示值与判定值来自不同维度集合，展示侧就只能表述为提示，不能表述为结论。**

## 6.11 准则八：清单与矩阵要验自身的闭环完整性

> **交付物矩阵是用来防漏的，而它自己会漏。**

⚠️ 举证：某架构文档的交付物矩阵有十行，看起来完整覆盖了各模块，但**整条主闭环的最后一步（拿到联系方式之后真的把电话打出去）不在矩阵里**。只照矩阵施工会做出一个「能看不能联系」的应用——而每一行本身都没错。

**判定动作**：矩阵定稿后，**不看矩阵**，把核心闭环按用户动作顺序口述一遍（看到 → 打开 → 获取 → 使用 → 完成），再逐个动作回矩阵找对应行。找不到的就是漏项。**这个动作必须绕开矩阵本身**，否则就是用它自己验它自己。

### 衍生：外部能力的「不可用」不等于「出错」

⚠️ 举证：调用系统外呼能力前的可用性探测返回否，成因常常不是设备不支持，而是**本应用未在配置里声明该协议白名单**——根因在配置不在设备。此时弹「拨号失败」会误导用户去检查自己的手机。正确处理是**降级到复制号码**，并把「配置声明」列进出包门禁。

## 6.12 准则九：验证结果与已知事实冲突时，先怀疑验证手段

> **「工具返回空结果」不等于「没有问题」。**

⚠️ 举证：主分析器被沙箱拦住（无法写系统临时目录）后改用另一个诊断接口，得到**空列表**。但这与「某基础类型刚从字符串改成整数、调用点必然一片红」的已知事实直接矛盾，故判定该结果不可信、不予采信；把缓存目录重定向到项目内后重跑，一次拿到 **77 条**错误清单（某次运行的观测值）。**若当时采信了那个空列表，会带着 77 处编译错误宣布完成。**

**判定动作**：**当验证结果与已知事实冲突时，先怀疑验证手段，而不是修正对事实的认知。** 具体做法是：动手前先给自己一个「这次改动至少应该有几处报错」的下限预期，实际结果低于下限就去查工具，而不是庆祝。

**这条与 3.4 准则二（测量方法本身需要验证）同源，但触发方向相反**：那一条由「数字异常但说得通」触发，这一条由「结果好得不该这么好」触发。**后者更危险，因为它不产生任何要去核对的动机。**

### 衍生：反复现身的教训要进静态扫描，不是再记一遍

⚠️ 某语言的断言语句在发布态不参与编译，这个教训在同一项目内**第三次现身**。第三次说明它需要的不是再记一遍，而是**进出包前的静态检查清单**——检查「有没有把不变量只写在断言里」。

**一般形态**：**同一个教训第三次出现时，处置对象要从「人的记忆」换成「机器的检查」。**

## 6.13 准则十：破坏性脚本的写入面必须先取，复核不变量由写入面推导

> **参数就是写入授权。传参前没读过它对每个参数做了什么，等于签了一张空白授权。**

**写入面** = 一个脚本实际会写到的库 / 表 / 行的集合，由它内部的**写语句**决定，而不是由它的名字或用途描述决定。

**取写入面的最小尽调**：不通读实现，只把写操作那几行列出来。

```bash
grep -nE 'INSERT|UPDATE|DELETE|TRUNCATE|DROP' <脚本>
```

⚠️ 举证：一个断言脚本的签名是 `<容器> <业务库> <埋点库>`，三个参数**都有默认值，且默认业务库正是含五十万行的压测库**。调用时对其中两个参数做了尽调（容器名、埋点库——特意确认过它不是旧版结构），唯独没查的那个正是唯一会被写入的库，于是往压测库里插了数条业务数据并改写了一条定点行。**同一个风险，只在一半的参数上防住了。**

**判定动作**：

| 项 | 要求 |
|---|---|
| 覆盖面 | **按参数表逐个过一遍**，而不是过「我正在关注的那个参数」。覆盖面由参数表决定，不由注意力决定 |
| 默认值 | **破坏性目标不应有默认值**。有默认值时，「忘记传参」与「故意传了危险值」后果相同，而前者不留任何决策痕迹 |
| 检出手段 | 写入面越界**不产生指向它的错误信号**（脚本正常退出、断言全绿，脚本自身预期的报错反而更容易掩盖真问题）。唯一可靠的检出是**执行前后各取一次目标的快照并主动核对** |

### 衍生一：定点改写比追加插入危险，且危害不对称

追加插入只在表尾堆积、可逐条删除；**作用于固定主键的改写会精确覆盖一条既有数据，且原值不留副本**。同族语句里**没有前驱守卫的那条最危险**——带守卫的至少留下了「原状态曾落在某个集合里」这一信息，无守卫的连这个都没有。

⚠️ 举证：回滚时只注意到了带守卫的那条改写，漏掉了先于它执行的**无守卫**改写，于是把该行复位成了一个错误值。原值最终只能从造数逻辑的取值公式反推出来。

### 衍生二：复核不变量必须由写入面逐项推导，不能挑代表性指标

> **「复核不变量选得不全」与「没有复核」在观感上完全一致——都是全绿。而前者更糟：它会主动宣布「已复原」并终结排查。**

⚠️ 举证：上例的回滚复核查了三项（总行数、某列的非空计数、主键最大值），**这三项恰好一项都碰不到被改写的那一列**，因此给出全绿。该错值一直留在库里，直到另一轮把结论写成可逐条核对的文档时才被查出。

**判定动作**：**脚本改了哪一列就查哪一列**，不变量清单由写入面逐项推导。挑几个「看起来能代表健康度」的聚合数字来查，等于没查。

> **这条与准则十的传参审计是同一个思维缺陷在两个环节的两次发作。** ⚠️ 在传参环节学会了「按参数逐个过一遍」，在复核环节仍然只查了自己关注的那几个量。**同一个缺陷会在流程的不同环节各犯一次，修掉前一次不会自动修掉后一次**——因此两处都要进检查清单。

### 衍生三：「不含决策」与「无害」是两个正交的性质

⚠️ 5.9 把「验证脚本、临时测试库、一次性造数」排除在阶段边界之外，理由是它们不含需要拍板的选择。这个理由**只针对决策痕迹，不针对写入面**：验证脚手架按「含不含需要拍板的选择」判定，写入面按「会不会写到真实数据」判定。**一段不含任何决策的断言脚本，仍可能改掉既有业务数据。**

## 6.14 反模式

| # | 反模式 | 正确做法 |
|---|---|---|
| 1 | 写完详细设计只做自洽复审 | 独立跑一遍实现者视角复审，追问「这行怎么执行」（6.4） |
| 2 | 文档里引用「理应存在」的类型 / 字段 | 逐个搜真源，零命中的裁定补实现或改写文档（6.4） |
| 3 | 用「按名字取枚举」或「按下标取值」做跨端映射 | 显式逐值映射，上下行都给（6.5） |
| 4 | 映射单测只覆盖两侧同名的取值 | 必须含不同名的值与可空档（6.5） |
| 5 | 把本地数值直接字符串化上行 | 显式处理 `null` / 哨兵档，否则产出格式合法的错值（6.5） |
| 6 | 规则逐条评审通过就认为规则集正确 | 两两核对叠加路径，重试×幂等类组合优先（6.6） |
| 7 | 取舍方向留给实现期决定 | 在文档里写死（如「宁可重一条不可丢一条」）（6.6） |
| 8 | 依赖第三方行为的纪律标为「待实测」 | 先画决策矩阵；全域正确的写法应无条件采用（6.7） |
| 9 | 加了约束就认为风险关闭 | 同时写下成立条件与失效信号（6.8） |
| 10 | 把宽容模式用在旁路链路 | 它顺带宽容的是全部数据质量报错（6.9.1） |
| 11 | 断言只核结果数量 | 补一条内容层断言，区分「生效」与「存在但没挡住」（6.9.1） |
| 12 | 降级兜成一个真实业务取值 | 兜明显异类的中性值，让现象指向真因（6.9.2） |
| 13 | 把规格规定的「预期空态」实现为异常 | 返回空值 + 类型强制调用方处理（6.9.2） |
| 14 | 展示层直接承诺剩余额度 | 附「以实际结果为准」，超限时本地刷零（6.10） |
| 15 | 用矩阵自己验矩阵的完整性 | 绕开矩阵口述闭环，再逐动作回查（6.11） |
| 16 | 外部能力探测为否就报「失败」 | 先分辨是配置未声明还是设备不支持（6.11 衍生） |
| 17 | 采信「零问题」的验证输出 | 与已知事实冲突时先怀疑工具；先给报错下限预期（6.12） |
| 18 | 同一教训靠再记一遍来防 | 第三次出现即转为机器检查（6.12 衍生） |
| 19 | 不读脚本就给破坏性脚本传参 | 先取写入面，按参数表逐个过（6.13） |
| 20 | 破坏性目标参数带默认值 | 去掉默认值，让漏传当场失败（6.13） |
| 21 | 回滚后挑几个聚合指标复核 | 不变量由写入面逐项推导（6.13 衍生二） |
| 22 | 用「不含决策」为写入型脚本豁免 | 两个性质正交，须另按写入面判定（6.13 衍生三） |

## 6.15 落地检查清单

**契约与真源**
- [ ] 本阶段未新造口径，每个字段都能对回架构基线或 DDL（6.3 ①）
- [ ] 跨接口全局约定内嵌在契约文件内部，实现一个接口无需开两份文件（6.3 ②）
- [ ] 契约的可数事实（接口数 / 断链 / 错误码全集）已回文件数（6.2）
- [ ] 文档中出现的每个类型名与字段名都已搜过真源，零命中项已裁定（6.4）
- [ ] 口径冲突已回写上游真源，未解决项登记为缺口并附「必须在什么之前处理」（6.3 ⑦）

**跨端一致性**
- [ ] 所有跨端映射为显式逐值实现，无「按名字取枚举」「按下标取值」（6.5）
- [ ] 映射单测覆盖不同名取值与可空 / 哨兵档，且上下行双向（6.5）
- [ ] 由多方独立计算的值给了带期望值的测试向量表，并断言中间值（同 4.7）
- [ ] 已显式写下「A 端不能照抄 B 端实现」的语义差异对照（同 4.7）

**规则与约束**
- [ ] 本模块规则已两两核对叠加路径，重试×幂等类组合已专项检查（6.6）
- [ ] 每个取舍方向已在文档里写死，未留给实现期（6.6）
- [ ] 依赖第三方行为的纪律已画决策矩阵，全域正确项已无条件采用（6.7）
- [ ] 每条新增约束旁写明了成立条件与失效信号（6.8）
- [ ] 静默失效型约束配了断言，且断言能区分「生效」与「存在但没挡住」（6.8 / 6.9.1）
- [ ] 旁路链路上无宽容模式开关（6.9.1）

**降级与展示**
- [ ] 每个降级分支都问过「用户看到这个结果会去找谁」（6.9.2）
- [ ] 降级值不落入用户可见的持久化内容（6.9.2）
- [ ] 展示值与判定值维度不一致处，文案为提示而非结论；超限时本地刷零（6.10）
- [ ] 外部能力探测为否时区分了配置未声明与设备不支持（6.11 衍生）

**完整性与验证**
- [ ] 已绕开矩阵口述核心闭环，逐动作回查无漏项（6.11）
- [ ] 每次改动前给了「至少应有几处报错」的下限预期（6.12）
- [ ] 反复现身三次的教训已转为静态检查项（6.12 衍生）

**破坏性操作**
- [ ] 调用任何写库脚本前已列出写入面（写语句清单），并按参数表逐个确认（6.13）
- [ ] 破坏性目标参数无默认值，或已确认默认值指向不存在的目标（6.13）
- [ ] 执行前后各取一次目标快照并主动核对，未依赖脚本退出码（6.13）
- [ ] 回滚复核的不变量由写入面逐项推导，覆盖每一处被改写的列（6.13 衍生二）

---

# 第七章：安全与 DevSecOps 方案设计

**问题定义**：架构与详细设计已定，要产出两件互相咬合的东西——**按威胁模型收口的安全规格**，和**把这些规格变成会失败的门禁**。合格判据只有一条：每一条安全结论都能指到一个「谁在什么时刻执行、失败长什么样、证据留在哪」的动作，指不到的一律视为未定义。

**核心主张**：**这一阶段的失效形态是「防线全都写下来了，但强度由最弱的那一环决定，而没人算过那一环是哪个」。** 安全方案天然会写成一张并列的防护清单，读起来处处设防；但攻击者只走最便宜的那条路，所以清单的长度与实际强度无关。**与前六章不同，这里没有正确性错误可查**：每一条防护单独看都成立，方案自洽，评审也挑不出毛病——失效只在「谁最先崩」这个问题被问出来时才现形。

**因此本章的全部准则都指向同一个动作：为每条防线找出它依赖的那个成本，再问这个成本是谁在承担。** 并列清单要被改写成有承重结构的东西：哪一条是地基，哪几条塌了还能补救。

## 7.1 适用边界

| 判断项 | 适用 | 不适用 |
|---|---|---|
| 上游输入 | 架构基线与详细设计已定，字段级真源已有 | 架构还在比选（此时安全约束会被当作可协商项砍掉） |
| 系统特征 | 泄露有**不可撤销**的现实后果（人身、资金、身份） | 泄露只带来合规罚款，可用赔付兜底 |
| 交付目标 | 安全规格 + 可执行门禁 **成对**产出 | 只出一份安全说明文档，不谈谁来执行 |
| 团队规模 | 判定者与被判定者可能是同一人 | 有独立安全团队做外部裁定 |

**这一章不覆盖**：具体加密算法怎么选、用哪个扫描平台——那取决于技术栈与预算。**可迁移的是「怎么让安全结论不停留在纸面」与「怎么找出整套防护体系的实际强度上限」。**

## 7.2 工具矩阵

| 工具 | 用途 | 不可替代之处 |
|---|---|---|
| **裁剪版威胁建模** | 把通用威胁清单收敛成本系统实际存在的几条攻击路径 | 不裁剪就会写出一份「什么都防」的方案，等于没有优先级；裁剪动作本身要留下「明确不防什么」的记录 |
| **产物层扫描** | 验证编译期剔除**真的发生了** | 源码层检查只能证明「代码写对了」，证明不了「产物里没有」（7.6） |
| **红线全文检索** | 把「不允许出现的写法」变成可执行判据 | 这类纪律靠评审记不住，靠检索才有下限 |
| **门禁脚本（分层）** | 秒级 / 分钟级 / 出包级各承担一段 | 全塞进最快那一层会导致整层被绕过（7.5） |
| **留证文件 + 产物哈希** | 把「这份检查通过了」与「这个具体产物」锁在一起 | 无哈希的通过记录可以为另一个未检查的产物背书（7.7） |
| **主动放弃清单** | 显式记录不引入的工具与理由 | 与「遗漏」区分开；没有这份清单，后来者无法判断某项缺失是决策还是疏忽 |

> **一条实测约定：安全方案不复制阈值字面量。** ⚠️ 阈值、TTL、上限只写常量名或指向权威源的章节，不在安全方案里重抄数字。安全方案是最容易被单独打开阅读的文档，因此也最容易成为过期数字的传播源（同 1.7）。

## 7.3 流程骨架

```
① 定特殊性：先写清「本系统的安全风险终点是什么」，据此排优先级，而不是套通用清单
② 裁威胁模型：逐条判「是否构成本系统的主威胁」，并显式写下不防的几类及理由
③ 分域组织：按攻击面切域，每域末尾直接给出可执行自检项（不留到最后统一写）
④ 找地基：对每条防线追问它依赖什么成本，标出唯一承重项（7.4）
⑤ 规格 → 门禁：每条自检项映射到具体拦截层，缺执行者/命令/失败判定/留证任一项即视为未定义
⑥ 登记不解决项：本方案发现但不归本方案解决的，登记为缺口并绑定处理时点
⑦ 回写上游：凡与上游口径冲突或需上游新增的，回写真源，不在本方案自行占号
```

**关键顺序**：④ 必须在 ⑤ 之前。**先做规格到门禁的映射，会得到一套均匀用力的门禁**——每条防线配一道检查，看起来完备，但承重项与装饰项花了同样的成本，而承重项通常需要的检查强度高一个量级。

**关于 ③「自检项写在域内」**：自检项与规格分离会让规格先写完、自检项后补，而后补时人已经忘了「这一条到底怕什么」。写在域内的自检项会被迫写成「实测什么、期望看到什么」；集中在文末补的自检项则倾向于写成「确认已实现 X」。

## 7.4 准则一：并列的防护清单要先找出唯一承重项

> **一套多维防护的实际强度，等于攻击者绕过成本最低的那一维的成本——不是各维之和。**

多维防护（多个身份维度、多层校验、多重限制）的常见写法是并列列出，暗示强度叠加。但各维的绕过成本往往差几个数量级：有的维度攻击者可以时间换量，有的可以重装设备即换，有的必须付出真实资费。**只有最贵的那一维是承重的，其余是延缓。**

⚠️ 举证：某方案按三个身份维度做限频，写成并列三行。逐维追问绕过成本后发现：其一重装即换、其二可用代理池、只有其三需要真实资费。**整套反爬体系的强度上限因此等于「批量注册账号的成本」这一个数**，与另两维无关。这个结论一旦成立，方案里就多出一条原本不存在的纪律：任何新增的注册通道，上线前必须评估其批量注册成本，且不得低于现有通道——否则等于在承重墙上开门。

**判定动作**：对每一组并列防护，逐项写出「绕过它需要付出什么」，然后把成本最低项的成本标为该组的实际强度。**如果各项成本量级相近，说明还没问到底**；如果最低项的成本近似为零，这组防护应视为不存在。

### 衍生：承重项也是最容易被「体验优化」掉的那一项

承重项之所以承重，正因为它对用户也有成本（要收验证码、要多一步操作、要等一会儿）。因此它在产品迭代中天然处于被优化的位置，而**优化它的人通常不知道自己在动地基**。

**判定动作**：承重项被识别出来后，要在它旁边写一句「这一项是本组的实际强度承载者，改动前须先重算强度上限」，并且这句话要落在**产品侧会打开的文档**里，不能只留在安全方案里。

## 7.5 准则二：门禁分层的依据是「会不会被绕过」，不是「重要不重要」

> **一道检查越重要，越容易被塞进最早的拦截层；而最早的拦截层恰恰是最脆弱的——因为它有绕过开关。**

提交前钩子、构建前检查这类早期拦截点普遍带有官方的跳过参数。**这意味着早期层的实际执行率与它的耗时成反比**：一旦超过几秒，被检查者会开始习惯性跳过，此后这一层的通过率变成 100% 且不再有任何信息量。

**分层原则**：

| 层 | 只放什么 | 判据 |
|---|---|---|
| 最早层（秒级） | 必然快、且失败原因一眼可见的检查 | 超过数秒的检查放这里，等于自愿放弃这道门禁 |
| 中间层（分钟级） | 全量测试、覆盖率类门禁、纪律型全文检索 | 允许慢，但要能在本地跑完，不依赖远端 |
| 产物层（出包时） | **验证最终产物**的检查，以及一切「必须证明编译期真的做了某事」的检查 | 这是唯一无法绕过的层：产物是分发对象本身 |

**判定动作**：把每道门禁按「被检查者能不能一键跳过」而非「这条有多重要」重新归层。最重要的那几条应该落在产物层，而不是最早层。

**关于执行载体**：门禁的**判据定义**与**执行载体**要解耦——同一份判据可以由本地脚本执行，也可以将来交给自动化流水线，规格不变。⚠️ 把判据和某个具体工具的配置文件写在一起，会导致换工具时判据一起丢失。

## 7.6 准则三：编译期剔除必须由产物层证明，源码层检查不构成证据

> **源码层的检查只能证明「代码写对了」，只有产物层的扫描能证明「编译器真的把它去掉了」。这两者之间存在真实的失效通道。**

凡是形如「这段代码只在调试态生效 / 发布时会被自动剔除」的安全结论，都依赖一个编译器行为假设。而该假设的失效方式全都不报错：被剔除的代码可能因为进了某个常量表、或被一个非调试态的调用点间接引用，而完整地留在产物里——**此时源码看起来完全合规**。

⚠️ 举证：某项目在客户端留了一个可绕过验证码的调试入口，用调试态判断包裹。三层判据分别是源码检索、编译配置评审、产物字符串扫描；**只有第三层是有效的最终判据**。若只做前两层，一个能让任何人无限注册账号的后门会随包发布——而它同时会让 7.4 那条「注册成本是最终防线」的结论直接归零。

**另一个必查项**：⚠️ **不能用「发布态不参与编译」的语句来兜底安全约束**（多数语言的断言语句属此类）。把不变量只写在这类语句里，等于在发布态什么都没写。

**判定动作**：把方案里所有形如「发布时会被剔除 / 不会生效」的句子列出来，逐条问「有没有一条检查是在**最终产物**上跑的」。没有的，这条结论降级为假设。

## 7.7 准则四：一道门禁的定义须含四项，缺一即视为未定义

> **执行者、工具与命令、失败判定、留证方式。**

> 落地阶段这四项会显出不足：它们只规定了门禁自己怎么算，没有规定**有没有人读它的结果**。须补第五项「消费方」，见 9.6。

这四项是门禁能被称为门禁的最小集合。缺失的那一项决定了它会怎么失效：

| 缺失项 | 失效形态 |
|---|---|
| 执行者 | 谁都以为别人会做 |
| 工具与命令 | 每次执行方式不同，结果不可比 |
| 失败判定 | 检查跑了，但没有任何输入能让它失败 |
| 留证方式 | 无法区分「通过了」与「没跑」 |

**判定者应当是脚本而非人工目检**：判定权与被判定对象是同一个人时（单人项目、或自己检查自己的产出），留证是唯一真实的约束。

### 衍生：通过记录必须与被检产物绑定

⚠️ 举证：某出包门禁的留证日志被要求必须包含产物的哈希值。理由是：**没有哈希，一份「通过」的日志可以被拿去为另一个未经检查的产物背书**——两者在归档目录里看起来毫无区别。哈希是把「这份证明」与「这个产物」锁在一起的唯一手段。

**判定动作**：每一份门禁留证都要能回答「它证明的是哪一个具体产物」。答案只能是哈希或同等强度的标识，不能是时间戳或文件名。

## 7.8 准则五：不引入的工具要写成主动放弃清单，附理由与重估条件

> **一份缺失项清单与一份放弃项清单，在内容上可以完全一样，在效力上相反。**

安全领域的工具清单没有上限，任何方案都必然放弃绝大多数。区别在于放弃有没有留痕：**未留痕的放弃在下一个人看来是疏忽**，于是要么被重新调研一遍（重复成本），要么被当成已知缺陷上报（信任成本）。

**判定动作**：每一项不引入的工具，写下「不引入的理由」与「什么条件下重新评估」。理由必须是本项目的具体约束（资源、规模、预算、依赖数量），不能是「暂不需要」。⚠️ 同时要给出**替代做法**——放弃一个自动化工具通常意味着接下一条人工纪律，而这条人工纪律必须同样满足 7.7 的四项。

## 7.9 准则六：安全方案发现的缺口只登记，不自行占号

> **安全评审会自然产出一批「上游缺了个口径」的发现。就地补上是最快的，也是最贵的。**

安全方案在细读时会发现上游权威源的空缺（缺一个错误码、缺一个状态、缺一条约束）。此时在安全方案里就地定义一个，会造成两种后果之一：与上游后来的定义冲突，或成为第二个口径源。

**判定动作**：本方案只做两件事——**登记**（写明缺什么、影响什么）与**绑定处理时点**（在什么事件之前必须裁定）。裁定结论落到该类事项的上游权威源，安全方案里只留一句指向。⚠️ 「登记」不等于「拖延」：一条登记项如果说不出「谁在什么时刻会读到它」，它就还没被登记（同 5.10）。

### 衍生：一条纪律的适用范围必须与纪律本身一起写下

⚠️ 举证：某项目定了「某类口径以某份文档为唯一真源」的纪律。这条纪律本身正确，但没写它覆盖哪一类内容——于是被扩用到了一类它并不覆盖的事项上，把一处本来正确的写法改成了错的，并连带放大到多处。

**一般形态**：**写下结论却不写适用边界的纪律，迟早会被用在边界外。** 而越权使用的那一次通常出自最认真执行纪律的人。

**判定动作**：每条纪律旁写明「覆盖哪一类内容、不覆盖哪一类」。⚠️ 尤其是涉及数字的纪律：实读时先确认两个数是不是同一口径，再比大小——否则会把一个正确的数字照着纪律改错。

## 7.10 准则七：安全结论要区分「已闭环」与「看起来已闭环」

> **一条被标为「已在别处解决」的安全项，只有三个条件同时成立时才真的闭环。**

| 必查项 | 不成立的后果 |
|---|---|
| 定案时点绑定了某个**确定事件**（而非「以后再说」） | 永久悬挂，且悬挂状态不可见 |
| 定案之前有**替代保障** | 从现在到定案之间是裸奔期，且没人认为自己在裸奔 |
| 这件事落在**某个人的动作清单**上 | 时点到了没有任何机制会提醒 |

三者缺一，「已闭环」就不成立——此时它不是已解决项，而是一个被误标的真缺口。

**判定动作**：对每一条「已在别处解决」的安全项，把上述三项取证结论**写下来**（对方是否真的闭环、有没有替代保障、定案时点绑定了什么），但**不搬运对方的规格细节**。留痕与第二份规格的分界是：读者据此只能知道去哪儿看，不能据此实施。

### 衍生：机械校验通不过语义校验

⚠️ 举证：某轮对安全类文档做交叉核验，机械脚本（引用路径是否存在、锚点是否断链、元数据是否齐全）连跑两轮全绿；随后换一个上下文干净的读者逐条比对断言与引证原文，查出多处**内容说反了**的引用——引用的路径真实存在，被引的章节也真实存在，只是转述的意思与原文相反。其中最严重的一条是「纪律的表述与它自己给出的举例互相矛盾」。

**一般形态**：**机械校验查的是「引用是否指向存在的东西」，语义校验查的是「引用是否说对了那个东西说的话」。前者全绿不构成后者的任何证据**（同 2.2.3）。

**判定动作**：安全方案定稿后，除脚本校验外，必须再跑一轮独立语义核验——由**没有参与写作**的一方逐条比对断言与引证原文。写作者无法核自己的转述，因为他记得的是「想写的意思」而不是「写下的字」。引用他处结论时应**逐字复制**而非转述。

## 7.11 反模式

| # | 反模式 | 正确做法 |
|---|---|---|
| 1 | 套用通用安全清单排优先级 | 先写清本系统风险的现实终点，据此重排（7.3 ①） |
| 2 | 威胁模型只列要防的 | 显式写下明确不防的几类及理由，避免过度设计（7.3 ②） |
| 3 | 自检项集中在文末统一补 | 写在每个域末尾，逼出「实测什么、期望看到什么」（7.3 ③） |
| 4 | 多维防护并列列出，暗示强度叠加 | 逐维写出绕过成本，取最低者为实际强度（7.4） |
| 5 | 承重项的说明只写在安全方案里 | 落到产品侧会打开的文档，附「改动前须重算强度」（7.4 衍生） |
| 6 | 把最重要的检查放最早的拦截层 | 按「能不能一键跳过」归层，最重要的放产物层（7.5） |
| 7 | 门禁判据写在某工具的配置文件里 | 判据定义与执行载体解耦（7.5） |
| 8 | 用源码检索证明「后门已剔除」 | 必须在最终产物上扫一遍（7.6） |
| 9 | 用发布态不参与编译的语句兜底安全约束 | 换成发布态仍生效的检查（7.6） |
| 10 | 门禁只写「要检查 X」 | 补齐执行者 / 命令 / 失败判定 / 留证四项（7.7） |
| 11 | 留证只记时间戳与结论 | 必须绑定被检产物的哈希（7.7 衍生） |
| 12 | 未引入的工具不留记录 | 写成主动放弃清单，附理由与重估条件（7.8） |
| 13 | 放弃自动化工具但不给替代纪律 | 替代的人工纪律同样要满足四项（7.8） |
| 14 | 在安全方案里就地补上游缺的口径 | 只登记 + 绑定时点，裁定落上游真源（7.9） |
| 15 | 纪律只写结论不写适用范围 | 一并写明覆盖哪一类、不覆盖哪一类（7.9 衍生） |
| 16 | 涉及数字的纪律直接比大小 | 先确认是否同一口径（7.9 衍生） |
| 17 | 标「已在别处解决」即视为关闭 | 查三项：时点绑事件、有替代保障、落在动作清单上（7.10） |
| 18 | 复述对方的规格细节以示闭环 | 只留取证结论与指向，不写第二份规格（7.10） |
| 19 | 脚本校验全绿即认为引用无误 | 再跑一轮独立语义核验，逐条比对引证原文（7.10 衍生） |
| 20 | 引用他处结论时转述 | 逐字复制（7.10 衍生） |

## 7.12 落地检查清单

**威胁模型与优先级**
- [ ] 已写清本系统安全风险的现实终点，优先级据此排定而非套用通用清单（7.3 ①）
- [ ] 威胁模型含「明确不在模型内」的几类及理由（7.3 ②）
- [ ] 每个安全域末尾都有可执行自检项，含实测动作与期望结果（7.3 ③）
- [ ] 方案内无阈值字面量，只有常量名或指向权威源的章节引用（7.2）

**防护强度**
- [ ] 每组并列防护都已逐项写出绕过成本，并标出实际强度承载者（7.4）
- [ ] 承重项旁写明「改动前须重算强度上限」，且该说明落在产品侧文档（7.4 衍生）
- [ ] 各维绕过成本量级相近的组已重新追问（量级相近说明没问到底）（7.4）

**门禁体系**
- [ ] 每道门禁按「能否一键跳过」而非「重要程度」归层（7.5）
- [ ] 最早拦截层内无耗时超过数秒的检查（7.5）
- [ ] 门禁判据与执行工具解耦，换工具不丢判据（7.5）
- [ ] 每道门禁均含执行者 / 工具与命令 / 失败判定 / 留证方式四项（7.7）
- [ ] 判定者为脚本，非人工目检（7.7）
- [ ] 每份留证均绑定被检产物的哈希（7.7 衍生）

**产物级证明**
- [ ] 所有「发布时会被剔除 / 不会生效」的结论都有在最终产物上跑的检查（7.6）
- [ ] 无任何安全约束仅写在发布态不参与编译的语句里（7.6）

**边界与缺口**
- [ ] 不引入的工具已写成主动放弃清单，含理由、重估条件、替代做法（7.8）
- [ ] 本方案未自行定义上游缺失的口径，只做登记 + 绑定时点（7.9）
- [ ] 每条登记项都能说出「谁在什么时刻会读到它」（7.9）
- [ ] 每条纪律旁写明覆盖范围与不覆盖范围（7.9 衍生）
- [ ] 涉数字的纪律执行前已确认口径一致（7.9 衍生）

**闭环与核验**
- [ ] 每条「已在别处解决」项已查三个必查项，取证结论已写下（7.10）
- [ ] 引用他处结论为逐字复制，非转述（7.10 衍生）
- [ ] 脚本校验之外已跑独立语义核验，由未参与写作的一方执行（7.10 衍生）

---

# 第八章：部署架构与 IaC 设计

**问题定义**：架构、详细设计、安全方案已定，资源上限已知，要产出两件互相咬合的东西——**说清「为什么这么部署」的部署架构文档**，和**能一键重建整套环境的基础设施代码**。合格判据只有一条：**任一份配置文件的每一行都在目标运行时里实测过**，没实测过的一律视为假设。

**核心主张**：**这一阶段的失效形态是「配置文件语法正确、评审也挑不出毛病，但在目标运行时里根本不生效」。** 与前七章不同，这里的错不在推理链上——推理是对的，是**推理所依赖的运行时事实**过期了或者从来没被核实过。一个在 MySQL 5.7 用了八年的参数在 8.4 被移除；一个权限位 777 的配置文件被守护进程静默忽略；一个「肯定不含 curl 的 Alpine 镜像」其实自带 curl。这三件事都不是靠更仔细地读代码能发现的。

**因此本章的全部准则都指向同一个动作：把每一条关于运行时行为的断言拉进容器里跑一遍。** 这个动作既用于写配置（准则一至四），也用于审配置（准则六）——**评审自己的结论同样需要实测，因为评审也是一次推理**。而**做这件审查的代码本身也在被审范围内**（准则七）：门禁与验证脚本是这一阶段唯一「出错时会放行而不是报错」的东西，它们的正确性只能由构造应当失败的输入来证明。

## 8.1 适用边界

| 判断项 | 适用 | 不适用 |
|---|---|---|
| 上游输入 | 架构、安全方案、资源配额均已定 | 机型与预算还在比选（此时资源分配表会被反复推翻） |
| 资源特征 | 存在**硬性上限**（固定内存、固定磁盘、固定带宽） | 资源可弹性伸缩，超限自动扩容 |
| 交付目标 | 文档 + 可执行 IaC **成对**产出 | 只出部署说明，实际部署靠手工敲命令 |
| 编排规模 | 单机或少量节点，编排工具本身不需要设计 | 需要设计 K8s 集群拓扑、服务网格、多可用区 |
| 团队规模 | 写配置的人与部署的人是同一人，无独立 SRE 复核 | 有独立运维团队做二次审查 |

**这一章不覆盖**：选哪个云厂商、用 Compose 还是 K8s、镜像该多小——那取决于预算与团队。**可迁移的是「怎么让资源分配表变成会失败的检查」与「怎么发现那些不报错的配置失效」。**

## 8.2 工具矩阵

| 工具 | 用途 | 不可替代之处 |
|---|---|---|
| **目标运行时容器 + 配置校验参数** | 在**真实版本**的运行时里验证配置文件语法 | 这是唯一能发现「参数已在此版本被移除」的手段；静态阅读永远发现不了（8.5） |
| **编排配置展开命令** | 把变量替换、继承、默认值全部展开后再检查 | 未展开的编排文件里看不出端口最终暴露给谁（8.7） |
| **镜像内工具探测** | 确认基础镜像自带哪些命令 | 健康检查、启动脚本依赖的每个命令都必须确认存在（8.5） |
| **资源分配表 → 门禁脚本** | 把内存/磁盘预算变成可执行断言 | 分配表写在文档里只是意图，写成脚本才有下限（8.4） |
| **单一口径源裁定记录** | 同一份分配表在多处出现时，标明哪份作废 | 无裁定记录时，两份冲突的表会各自被引用，且引用者不知道有另一份（8.3） |
| **前置条件声明块** | 标注本配置依赖的、当前尚不存在的产物 | 区分「配置写错了」与「依赖还没到」，避免下一个人误判为缺陷（8.6） |

> **一条实测约定：任何关于运行时行为的断言都不写进文档，除非它刚刚在容器里跑过。** ⚠️ 这包括参数是否存在、命令是否自带、默认值是多少、变量是否展开。这类断言的特点是**读起来极其可信**——它们通常来自多年经验，而经验的有效期恰好是运行时的大版本周期。

## 8.3 流程骨架

```
① 收口资源账本：把内存/磁盘/带宽的分配表定成唯一口径源，多份冲突时显式裁定并标注作废
② 定编排边界：明确哪些进程装在容器里、哪些留在宿主机、端口只允许一个入口
③ 写配置 → 立即实测：每写完一份运行时配置，立刻在目标版本容器里跑校验，不攒到最后
④ 分配表 → 门禁：把①的每一项变成一条可执行断言（含「未配置即视为失败」的项）
⑤ 密钥链路单线化：从文件到进程内存只留一条路径，其余写法列为违规
⑥ 声明前置条件：把依赖的、当前不存在的产物显式标注，避免被后来者误判为缺陷
⑦ 自评审 → 逐项实测核验：评审结论进入修复前必须实测，误判率会高于直觉（8.9）
⑧ 门禁自检：给每道门禁造一个应当失败的输入，确认它真的失败（8.10）
```

**关键顺序**：③ 的「立即」是硬要求。**攒到最后统一验证，会让一个失效参数的修复连带影响已经写完的文档、门禁脚本、进度记录**——本次一处 `expire_logs_days` 的更正牵动了 5 个文件。写完即测时，这个更正只影响一个文件。

**关于 ⑦**：这一步不是可选的收尾。**评审是一次纯推理，它的错误率与写作时的推理错误率同量级**，差别只在评审的结论会被当成事实直接用于修复。

**关于 ⑧**：⑧ 不能与 ④ 合并。④ 产出门禁，⑧ 验证门禁本身有效——**这是两次独立的动作，因为一道有 bug 的门禁在正常输入下与一道正确的门禁表现完全一样**（8.10）。

## 8.4 准则一：资源分配表必须逐项变成会失败的断言，尤其是「未配置即失效」的项

> **一份写在文档里的资源分配表只是意图声明。真正约束资源的是那几条「不配它就没有上限」的配置项。**

固定资源环境下的分配表（内存分配、磁盘分区、连接数上限）有两类项，代价完全不同：

| 类型 | 特征 | 漏配后果 |
|---|---|---|
| **有默认上限项** | 不配也有个默认值 | 偏离预算，但不会失控 |
| **无默认上限项** | 不配就是**无限增长** | 绕过整张分配表，直到吃满物理资源 |

第二类是分配表的真正承重项，且**它们在配置文件里看起来和第一类一模一样**——都是一行 `key = value`。

⚠️ 举证：某单机部署把 40G 磁盘分成七项，其中两项属第二类：数据库的变更日志（不设过期即无限累积）与应用日志（不设轮转上限即无限累积）。任一项漏配，都会在数月后吃满磁盘 → 数据库拒绝写入 → 全站不可用。**这两项之所以危险，正因为它们在漏配状态下的前几个月表现完全正常**，分配表的其余五项也全都还在预算内。

**判定动作**：对分配表逐项问「不配它会怎样」。答案是「无限增长」的项，必须同时满足三条：① 配置文件里已配；② 门禁脚本里有一条断言检查它已配且值在预算内；③ 断言的失败分支要覆盖「查不到这个参数」与「值为 0/永不过期」两种情况——**只检查数值范围会漏掉参数名写错的情形**。

### 衍生：分配表在文档与配置中各存一份时，两份必须逐项一致，且这一致性本身要成为门禁项

分配表天然会出现两次：文档里的表格（供人读）和配置文件里的值（供机器读）。⚠️ 两处不一致时，**先被相信的通常是文档**，因为它更易读，而实际生效的是配置。

**判定动作**：在文档的分配表旁写一句「本表与配置文件的对应项必须逐项一致，不一致视为门禁失败」，并在配置文件头部写明「改此值必须同步改文档某章某表」。这条纪律不需要脚本，但需要**双向**写下来——只在一侧写，改另一侧的人看不到。

## 8.5 准则二：运行时配置的每一行都要在目标版本容器里跑过校验参数

> **配置文件的语法正确性与它在特定版本运行时里的有效性，是两件事。后者只能由那个版本的运行时本身回答。**

主流运行时（数据库、缓存、Web 服务器）大多提供只校验配置不启动服务的参数。这个参数的价值不在于查语法错误——语法错误肉眼可见——而在于查出**在此版本已被移除或改名的参数**。

⚠️ 举证：某数据库配置使用了一个在旧版本存在多年的日志过期参数。该参数在新的大版本中被移除，替换为另一个单位不同的参数。在新版本容器里跑配置校验时，报 `unknown variable` 并直接终止——**即容器根本无法启动**。而静态阅读这份配置时，这一行看起来完全合法，因为它确实在那个运行时里正确工作了八年。

**同类必查项**：⚠️ **镜像自带哪些命令，必须探测而非推断。** 健康检查、启动脚本、备份脚本里的每一个命令都是一个「这个镜像里有它」的假设。精简镜像的工具集不遵循直觉——一个「肯定不含任何网络工具的精简镜像」可能自带某个 HTTP 客户端而缺少另一个。用错的后果是健康检查**永远不会转为健康**，而这个失效在配置文件里同样看不出来。

**判定动作**：
1. 每份运行时配置写完后，立刻在**目标版本**容器里跑一次配置校验，要求退出码为 0 且零告警；
2. 配置文件里引用的每个外部命令，在对应镜像里跑一次「这个命令在不在」的探测；
3. 校验时若被容器的默认启动流程拦截，覆盖启动入口直接调用服务主程序——**不要因为「校验命令跑不起来」就跳过校验**。

### 衍生：配置文件的权限位是一个静默失效通道

⚠️ 部分守护进程会**拒绝加载全局可写的配置文件**，且只打一行告警后继续正常启动。此时所有配置项都不生效，但进程状态健康、退出码为 0、日志里没有 ERROR。

这个陷阱的触发场景不需要任何人犯错：跨文件系统挂载（某些挂载方式会把权限统一转成全局可写）、通过传输工具复制（权限位被重置）、从压缩包解出（权限取决于打包时的状态）。

**判定动作**：① 配置文件头部写明要求的权限位；② 门禁脚本里加一条权限检查；③ **做配置校验时，若挂载路径可能是全局可写的，先把文件复制到容器内本地文件系统并改好权限再校验**——否则校验本身会因文件被忽略而「假通过」，退出码 0 但什么都没验。

## 8.6 准则三：依赖尚不存在的产物时，要写成前置条件声明，不是留一个会失败的引用

> **一份引用了未来产物的配置，与一份写错了路径的配置，在报错信息上完全一样。**

跨阶段交付时，本阶段的配置常需要引用下一阶段才会产出的东西（构建脚本引用源码目录、编排文件引用镜像构建上下文）。此时配置是对的，只是依赖还没到。

⚠️ 举证：某部署阶段产出的镜像构建文件引用了三个编码阶段才会存在的路径。这份文件本身完全正确，但在当前状态下执行构建必然失败。**若不显式声明，下一个打开它的人（包括几周后的自己）会把它当成一处路径错误去"修"**，而正确的动作是等待上游产物到位。

**判定动作**：在配置文件头部与对应文档章节**同时**写一个前置条件块，含三项：① 依赖哪些产物；② 这些产物属哪个阶段的交付物；③ 在它们到位前，执行本配置会得到什么错误。第三项是关键——**它让后来者能把"预期的失败"与"真的坏了"区分开**。

## 8.7 准则四：暴露面要用展开后的编排配置验证，不是读源文件

> **编排文件的源文本与它展开后的最终形态之间，隔着变量替换、文件继承、默认值填充和覆盖规则。暴露面只在展开后的形态里才是确定的。**

「只有一个入口暴露端口」这类纪律，靠读编排文件的源文本是验不了的：端口可能来自被继承的基础文件、来自默认值、来自另一个覆盖文件。而**用文本检索去验更糟——它会同时命中注释、命中另一个服务的段落**。

⚠️ 举证：某门禁脚本用文本检索验「除入口服务外无端口映射」，写法是「找出所有端口声明行，排除包含入口服务名的行」。这条检查**恒定失败**：端口声明行本身不含任何服务名，服务名在它上方几行。同一脚本的另一条检查用文本匹配「配置里不许出现某敏感文件的拷贝指令」，结果命中了文件头部**纪律说明注释里的那句「禁止拷贝该文件」**——检查报告了一个不存在的违规。

**判定动作**：
1. 用编排工具自带的展开命令产出最终配置，在展开结果上做检查；
2. 检查必须**逐服务**判定归属，而不是全局找关键字——用能感知层级结构的手段（结构化解析或按缩进/服务名边界匹配）；
3. 涉及「不许出现某写法」的检查，**匹配模式要锚定到行首指令位置**，避免命中注释与说明文字；
4. 门禁脚本写完后要**造一个应当失败的输入**跑一遍——恒真与恒假的检查在正常输入下的表现和正确检查完全一样。

### 衍生：门禁脚本的依赖必须限定在目标环境的最小工具集内

⚠️ 门禁脚本会在目标机器上跑，而目标机器的工具集可能远小于开发机。用了一个开发机自带、目标机未预装的解释器，这道门禁在最需要它的时刻不可用。

**判定动作**：门禁脚本只用目标环境的基础工具（shell 内建、文本处理工具、以及部署本身必然存在的容器运行时）。**每引入一个额外依赖，都要问「目标机器上一定有它吗」**。

## 8.8 准则五：密钥从文件到进程内存只留一条路径，读取时逐项取而非整体注入

> **一条链路上每多一个中转，就多一处泄露面；而"把整个密钥文件读进当前环境"是最容易被写出来的中转，也是泄露面最大的那种。**

密钥注入链路的常见写法是「把密钥文件整体加载为环境变量」，因为它一行就能写完，且后续所有变量都能直接用。代价是：**这个进程及其所有子进程，都能读到文件里的每一个密钥**，包括它完全不需要的那些。

⚠️ 举证：某备份脚本只需要数据库口令与存储桶名，共四个变量，但用了「整体加载」的写法。该脚本因此在自己的进程环境里持有了同一份文件中的全部密钥——包括与备份毫无关系的加密主密钥与哈希盐。而备份脚本是**最常被调试、最常在输出里带上环境快照**的一类脚本。

**判定动作**：
1. 密钥链路写成单向单线：文件 → 编排层注入 → 容器环境变量 → 配置占位符 → 应用配置类。列出这条链路，**其余写法（镜像内打包、代码内默认值、日志中回显）逐条标为违规**；
2. 任何读取密钥文件的脚本，**逐变量提取它需要的那几个**，不做整体加载；
3. 提取后加缺失校验——缺失时快速失败，而不是带着空值继续跑；
4. 密钥文件与备份产物**物理分离存放**：备份的意义是应对灾难，而密钥与备份同处一地时，一次泄露同时拿到密文与密钥。

## 8.9 准则六：评审结论进入修复前必须逐项实测，误判率会高于直觉

> **评审是一次纯推理，它的错误率与写作时的推理错误率同量级。差别只在——评审的结论会被当成事实直接用于修复。**

代码评审（自评或他评）产出的是一份「疑似问题清单」。这份清单的每一条都是一个推理结论，而推理依赖的运行时事实可能过期、可能从未核实。**按未经核验的评审结论去修复，等于把正确的代码改错。**

⚠️ 举证：某次部署交付物自评审识别出 16 项问题并分四级。进入修复前逐项实测，结果是：**3 项结论被推翻（约 19% 误判率）、1 项的修复方案被判定过度**。三项误判分别是——① 断言某构建指令不会展开变量（实测：声明了参数就会展开，真正原因是缺声明）；② 断言环境变量注入会保留引号（实测：三种写法都正确剥离，无需修改）；③ 断言精简镜像不含某 HTTP 客户端、需额外安装（实测：镜像自带，只需换用它）。**这三项若按评审结论修复，会产出三处"代码没错但被改了"的改动**，其中第三项还会无谓增加镜像体积。

**判定动作**：
1. 评审结论与修复动作之间**插入一道核验关卡**，任何形如「这个配置会报错」「这个写法不生效」「这个镜像不含 X」的断言，都要在容器里跑一遍；
2. 核验后把结论分三类：**成立**（照修）、**误判**（撤销该条，并记录推翻依据）、**成立但方案过度**（改用最小修复）；
3. 记录被推翻的条目及其实测证据——**这是防止同一误判在下次评审中再次出现的唯一手段**；
4. 核验的优先级按「若误判，修复动作的破坏性」排序，而不是按评审给的严重级别排序。

### 衍生：核验用的验证脚本本身也可能「假通过」

⚠️ 验证脚本的失败模式与门禁脚本相同：**在正常输入下，一个恒真的检查与一个正确的检查表现完全一样**。本次核验中，第一版配置校验脚本因文件权限问题导致被校验的配置被运行时静默忽略，脚本仍报「校验通过、退出码 0」——**它验的是一个空配置**。

**判定动作**：验证脚本给出「通过」结论时，追问一句「它是否真的读到了被验对象」。可行的手法是**故意注入一个应当被拒绝的输入**，确认脚本会失败；不会失败的脚本，它此前给出的所有「通过」都不成立。

## 8.10 准则七：门禁自身失效时的默认落点是「通过」，不是「报错」

> **普通代码出错时会崩，门禁出错时会放行。这是门禁与它所守护的代码之间最根本的差异，也是它最容易被忽略的一点。**

> 本节给出的是设计期的防御。落地期还有两条延伸：第三态「跳过」须进一步拆成「判不了」与「不适用」两态（9.5），退出码须分三档并支持带理由放行（9.7）。

门禁的输出只有两个字：通过、失败。而「检查未能真正执行」这件事，在绝大多数写法里会被折叠进「通过」。于是一道有 bug 的门禁与一道正确的门禁，在正常输入下的表现**完全一致**——包括退出码、包括汇总格式、包括那份让人放心的全绿输出。三种失效机制反复出现：

| 失效机制 | 为什么会落到「通过」一侧 | 识别信号 |
|---|---|---|
| **反向检查结构** `if <探测命令>; then 判失败; else 判通过; fi` | 探测命令因「对象不存在 / 命令本身失败 / 权限不足」返回非 0，与「已探测且未发现违规」在退出码上**无法区分**，一律落入 else | 拿一个明知不存在的对象去跑，得到的是 PASS 而不是报错 |
| **命令替换里的 `exit`** `VAR="$(校验函数)"` | 校验函数的 `exit 1` 只终止子 shell；主流程拿到空值继续往下跑，把余下各项全部跑完，给出一份格式完整、内容全假的汇总 | 传入一个应被拒绝的入参，门禁没有在第一步停下，而是照常跑完了全部检查项 |
| **黑名单正则** | 黑名单要求穷举一个开放集合的所有错误形态，漏掉任意一种即等于放行；而正则又常常匹配的是**源文件里的写法**，不是运行时真正生效的那个结构 | 构造一种没被列进黑名单的等价违规写法，门禁判通过 |

⚠️ 举证：某次门禁自检暴露的三处，恰好一一对应上表。① 镜像层检查用了反向结构，本机没有待检镜像时探测命令失败，输出的是「通过」——它从未检查过任何镜像；② 环境名解析函数在命令替换中调用，传入非法环境名时该函数确实 `exit 1` 了，但主脚本带着**空环境名**把十一项检查全跑完并给出汇总，退出码来自最后一项而非那次失败；③ 端口暴露面检查用黑名单正则枚举「不许出现的绑定写法」，漏掉了「绑到某块具体内网卡」这一形态。此外首版正则还写错了匹配位置——它按源文件的缩进假设去匹配，而编排配置**展开后**列表项的 `- ` 前缀只出现在该项的首个键上，其余键行不带前缀，因此连续误报三次。

**判定动作**：

1. **存在性判定独立成第三态**：把「对象是否存在 / 命令是否可用」从检查逻辑里拆出来先判，不存在时输出与「通过」「失败」并列的**跳过**，并说明为何跳过、如何让它不再跳过。**跳过计数不为零本身就是需要被看见的信号**——它意味着这次门禁的覆盖面小于名义覆盖面；
2. **命令替换的调用点必须显式传导失败**：写成 `VAR="$(f)" || exit 1`。这里不能靠「出错即退出」的 shell 选项兜住，因为**门禁刻意不能开这个选项**——它必须把所有检查项跑完再汇总，开了会在第一个失败处就退出、后面的项永远不被检查。两种需求在同一个脚本里冲突，只能由调用点逐处显式处理；
3. **黑名单改白名单**：不去枚举「哪些写法不许出现」，而是核对「唯一允许的取值是什么，实际是不是它」。白名单的漏检代价是误报（会被立刻发现），黑名单的漏检代价是漏报（永远不会被发现）；
4. **判定对象取展开后的结构**：一切「运行时会是什么样」的断言，都在编排/配置**展开后**的产物上做，不在源文件上做（同 8.7）；
5. **负向实测矩阵**：给每一道门禁列一行——「构造什么输入 → 期望它判失败」，逐行跑过。**正向跑通不构成门禁有效的证据**；这份矩阵才是。

### 衍生：全局参数与子命令参数的位置差异，会让「加了参数」与「参数生效」分离

⚠️ 有些命令行工具把某些参数定义为**全局参数**——只能出现在子命令之前，放到子命令之后会被当作该子命令的未知参数。当这类参数被封装进脚本、由脚本代为拼接时，位置错误的表现往往不是报错，而是**参数被静默忽略、命令以默认行为执行成功**。

**判定动作**：把这类参数**转译为等价的环境变量**，避免位置依赖，从而对同一工具的所有子命令一致生效。转译时注意——**不能无条件设置空值**：空串通常不等于「未指定」，而会被解释为「显式指定了一个空集合」，反而改变行为。设置动作必须放在「确有取值」的条件分支里。

## 8.11 准则八：部署目录必须是一键重建的产物，临时验证脚本不许留下

> **一份需要「按文档顺序手工敲十条命令」才能重建的环境，与一份没有 IaC 的环境，在灾难恢复时的差别小于人们预期。**

（同 1.12 的思路，此处是运行时版本。）部署目录里的每个文件都应有明确归属：要么是编排/配置/脚本的正式产物，要么不该存在。⚠️ 核验阶段会产生大量临时脚本（配置校验、语法检查、回归验证），这些脚本**与正式脚本混在同一目录时无法区分**，而它们通常携带临时路径、临时口令、绕过检查的开关。

**判定动作**：① 临时验证脚本用统一前缀命名，核验结束后逐个删除并确认目录只剩正式文件；② 备份与恢复流程**必须实跑一次完整恢复演练**才算交付——只写恢复步骤不跑，等于交付了一份未经测试的代码；③ 恢复演练的目标是「从备份产物重建出可用服务」，不是「能解压出备份文件」。

## 8.12 反模式

| # | 反模式 | 正确做法 |
|---|---|---|
| 1 | 资源分配表只写在文档里 | 逐项变成门禁断言，尤其「无默认上限」的项（8.4） |
| 2 | 检查「无限增长项」只验数值范围 | 同时覆盖「查不到该参数」与「值为永不过期」（8.4） |
| 3 | 分配表在文档与配置各写一份、不声明一致性要求 | 双向写下「改一侧须同步另一侧」（8.4 衍生） |
| 4 | 靠静态阅读判断配置参数是否有效 | 在目标版本容器里跑配置校验，要求零告警（8.5） |
| 5 | 推断精简镜像自带哪些命令 | 逐个探测，健康检查依赖的命令必须确认存在（8.5） |
| 6 | 校验命令被容器启动流程拦截就跳过校验 | 覆盖启动入口直接调用服务主程序（8.5） |
| 7 | 挂载全局可写路径做配置校验 | 先复制到容器内并改权限，否则校验的是空配置（8.5 衍生） |
| 8 | 引用未来产物的配置不加说明 | 写前置条件块，含依赖项/所属阶段/预期错误（8.6） |
| 9 | 读编排源文件验端口暴露面 | 在展开后的配置上逐服务判定（8.7） |
| 10 | 用全局文本检索验「某写法不许出现」 | 锚定行首指令位置，避免命中注释（8.7） |
| 11 | 门禁脚本写完不测就上线 | 造一个应当失败的输入跑一遍（8.7） |
| 12 | 门禁脚本依赖开发机自带的解释器 | 只用目标环境必然存在的工具（8.7 衍生） |
| 13 | 整体加载密钥文件为环境变量 | 逐变量提取所需的那几个（8.8） |
| 14 | 密钥与备份产物存放在一处 | 物理分离——一次泄露不应同时拿到密文与密钥（8.8） |
| 15 | 评审结论直接进入修复 | 插入实测核验关卡，误判率约五分之一（8.9） |
| 16 | 被推翻的评审条目不留记录 | 记录推翻依据，防止同一误判复现（8.9） |
| 17 | 验证脚本报「通过」即采信 | 追问它是否真读到被验对象，故意注入应失败的输入（8.9 衍生） |
| 18 | 门禁写成「探测到违规判失败、否则判通过」的反向结构 | 存在性判定拆出来先判，不存在时输出与通过/失败并列的**跳过**（8.10） |
| 19 | 校验函数在命令替换 `$( )` 里调用，靠它自己 `exit` 终止流程 | 调用点显式 `\|\| exit 1`——门禁刻意不开「出错即退出」，只能逐处传导（8.10） |
| 20 | 用黑名单正则枚举「不许出现的写法」 | 改白名单核对唯一允许取值，且取展开后的结构（8.10） |
| 21 | 门禁跑完只看有没有 FAIL | 跳过计数不为零同样要被看见——它意味着实际覆盖面小于名义覆盖面（8.10） |
| 22 | 全局参数拼在子命令之后 | 转译为等价环境变量；且不能无条件设置空值（8.10 衍生） |
| 23 | 临时验证脚本留在部署目录 | 统一前缀命名，核验后逐个删除（8.11） |
| 24 | 只写恢复步骤不跑恢复演练 | 实跑一次「从备份重建出可用服务」（8.11） |

## 8.13 落地检查清单

**资源账本**
- [ ] 分配表已定为唯一口径源，多份冲突处已显式裁定并标注作废（8.3 ①）
- [ ] 逐项问过「不配它会怎样」，「无限增长」的项已全部识别（8.4）
- [ ] 每个「无限增长」项都有：配置已配 + 门禁断言 + 覆盖两种失败情形（8.4）
- [ ] 文档分配表与配置文件的对应项逐项一致，且两侧都写了同步纪律（8.4 衍生）

**运行时实测**
- [ ] 每份运行时配置都在**目标版本**容器里跑过配置校验，退出码 0 且零告警（8.5）
- [ ] 配置中引用的每个外部命令都已在对应镜像里探测存在（8.5）
- [ ] 配置文件权限要求已写明，门禁里有权限检查（8.5 衍生）
- [ ] 配置校验未因文件被静默忽略而「假通过」（已确认真的读到了配置）（8.5 衍生）

**暴露面与门禁**
- [ ] 端口暴露面在**展开后**的编排配置上验证过，且逐服务判定归属（8.7）
- [ ] 「不许出现某写法」类检查已锚定行首，不会命中注释（8.7）
- [ ] 每道门禁都造过一个应当失败的输入并确认会失败（8.7）
- [ ] 门禁脚本的依赖全在目标环境的最小工具集内（8.7 衍生）

**门禁自检**
- [ ] 每道门禁都有一行负向实测记录：「构造什么输入 → 期望它判失败」，且逐行跑过（8.10）
- [ ] 无「探测到违规判失败、否则判通过」的反向结构；存在性判定已拆出来先判（8.10）
- [ ] 门禁输出有与通过/失败**并列的跳过态**，跳过时说明了原因与解除方式（8.10）
- [ ] 本次运行的跳过计数已被逐条看过，确认实际覆盖面等于名义覆盖面（8.10）
- [ ] 命令替换中调用的校验函数，调用点都有显式失败传导（8.10）
- [ ] 无黑名单正则；「不许出现某写法」类检查已改为白名单核对唯一允许取值（8.10）
- [ ] 全局参数已转译为环境变量，且无取值时不设置（不设空串）（8.10 衍生）

**密钥与备份**
- [ ] 密钥链路单向单线，其余写法已逐条列为违规（8.8）
- [ ] 所有读密钥文件的脚本都是逐变量提取，无整体加载（8.8）
- [ ] 提取后有缺失校验，缺失即快速失败（8.8）
- [ ] 密钥与备份产物物理分离（8.8）
- [ ] 已实跑一次完整恢复演练，产出可用服务（8.11）

**前置条件与交付卫生**
- [ ] 引用未来产物的配置都有前置条件块，含依赖项/所属阶段/预期错误（8.6）
- [ ] 部署目录内仅正式文件，临时验证脚本已全部删除（8.11）

**评审核验**
- [ ] 评审结论与修复之间有独立的实测核验关卡（8.9）
- [ ] 每条「会报错/不生效/不含某工具」类断言都已在容器里实测（8.9）
- [ ] 结论已分三类：成立 / 误判 / 成立但方案过度（8.9）
- [ ] 被推翻的条目及其实测证据已记录（8.9）
- [ ] 验证脚本本身已确认不会「假通过」（8.9 衍生）

---

# 第九章：门禁落地与版本拓扑

**问题定义**：门禁的判据已在方案里写清，接下来要把它变成真正会被执行、且执行结果真正被调用方采纳的代码；同时要让「哪个提交对应哪次交付」在仓库里可回溯。

**核心主张**：**这一阶段的失效形态是「门禁存在但不起作用」，而不是「门禁判错」。** 判据可以逐条严谨、脚本可以每次都跑、输出可以每次都正确，只要没有任何调用方读它的退出码，整套门禁对交付流程的影响就是零 —— 而它在日志里看起来与生效时完全一样。

**因此本章的全部准则都指向同一个动作**：先把「结论 → 退出码 → 调用方分支」这条链路接通并实测其阻断能力，再回头精修判据。顺序反过来，前期所有精修在链路接通之前收益均为零。

## 9.1 适用边界

| 判断项 | 适用 | 不适用 |
|---|---|---|
| 门禁形态 | 已有成文判据，需落成脚本并接入发布/部署流程 | 判据尚未定义，仍在讨论「查什么」 |
| 执行方式 | 由脚本判定、有明确退出码 | 依赖人工目检、无机器可读结论 |
| 调用关系 | 存在至少一个自动化调用方（部署脚本、出包脚本、CI 作业） | 门禁只被人手动执行、结果只用于阅读 |
| 版本拓扑 | 仓库需支撑「按交付节点回溯代码状态」 | 一次性脚本仓库，无回溯需求 |
| 团队规模 | 任意；单人团队同样适用（单人反而更容易漏掉「谁消费」这一环） | —— |

## 9.2 工具矩阵

| 用途 | 工具 / 手段 | 关键约束 |
|---|---|---|
| 门禁结论与计数 | 独立的外壳库（可被 source 的公共脚本） | 全流程只允许一份实现（9.8） |
| 退出码判定 | 从「打印摘要」中拆出的独立函数 | 调用方需要「先留证再退出」时必须能单独取码（9.8） |
| 器材有效性验证 | 变异测试：向被测脚本注入已知缺陷 | 注入后必须还原并复测，还原动作不得依赖跨文件系统的临时目录（9.9） |
| 负向行为固化 | 用例表 + 期望退出码的双向断言 | 每条用例声明自己的期望值，不允许无条件断言（8.10 判定动作 5） |
| 交付节点锚点 | 附注 tag（两套命名空间：发布用 / 节点用） | 找不到可信锚点时不打（9.11） |
| 规范文档 | 独立专题文档，含「强制状态清单」 | 未强制项须如实标未强制（9.12） |

## 9.3 流程骨架

```
阶段 A：接通链路（最便宜，必须最先做）
  1. 定义退出码分档：合格=0 / 不合格=1 / 判不了=2
  2. 让至少一个自动化调用方读取退出码，并按档分流
  3. 负向实测：人为制造「判不了」，确认调用方真的中止
  4. 加放行开关（带理由），并实测放行只对「判不了」生效

阶段 B：统一外壳
  5. 把结论函数、计数器、退出码函数抽成单一外壳库
  6. 所有门禁层级改为 source 同一份外壳
  7. 外壳加载失败必须硬中止（不可继续执行）

阶段 C：精修判据
  8. 逐项复核「什么都没校验就报合格」的伪合格项
  9. 区分「判不了」与「本环境不存在判定对象」两态
 10. 对判据脚本做变异测试，确认器材有检出能力

阶段 D：版本拓扑
 11. 定分支模型与提交约定，写明每条的强制状态
 12. 逐个交付节点找可信锚点，找不到的不打 tag 并记录原因
```

## 9.4 准则一：门禁有效性 = 判据严谨度 × 是否有调用方消费退出码

后一项为零时，乘积为零。**判据修得再准，只要没有调用方读退出码，修与不修对交付流程的实际影响完全相同。**

这条最难被发现，原因是失效现场毫无异常：脚本正常执行、结论正常打印、颜色正常、日志正常落盘，唯一的差别是那个数字没有人读。它不产生任何告警，也不出现在任何报表里。

**判定动作**
1. 对每一道门禁，回答一个问题：**哪一行代码读了它的退出码？** 答不出具体位置就视为未接通。
2. 检查调用方是否把执行动作写成了「执行 + 打印」而丢掉了返回值 —— 常见形态是把调用结果直接送进日志函数，返回值在传递过程中被吞掉。
3. 检查调用链上是否存在把非零码归零的写法（尾随的「忽略失败」惯用语、包裹的容错分支）。**只要有一处，其上游全部判据同时失效。**
4. 用负向实测确认：人为造一个必然不合格的输入，确认调用方**真的没有继续往下走**。仅确认「门禁打印了不合格」不算。

> **举证**：某项目连续两轮修复了 7 处「什么都没校验就报合格」的伪合格项，判据侧确实变严谨了。但在第三轮把退出码接进部署脚本之前，那 7 处修与不修对发布流程的影响完全一致 —— 缺失的关键项不是任何一条判据，**而是「有没有人读退出码」这件事本身**，它此前被简化成了一句打印。

## 9.5 准则二：判定结果必须四态，「判不了」与「不适用」不能合并

第七、八章已指出「跳过」要独立于「通过/失败」成为第三态。落地时会发现三态不够，必须四态：

| 态 | 含义 | 对退出码的影响 |
|---|---|---|
| 合格 | 已执行判定，结果符合要求 | 无 |
| 不合格 | 已执行判定，结果不符合要求 | 判为失败档 |
| 判不了 | **本项应当被判定**，但前置条件缺失导致未能判定 | 判为「不知道」档 |
| 不适用 | **本项按当前环境设计就不存在判定对象** | 无 |

**两者的分界线是「有没有可判定的对象」**，不是「有没有判成功」。

**判定动作**
1. 对每个「判不了」，追问一句：**这一项在当前环境下本该存在判定对象吗？** 答「不该存在」的，归为不适用。
2. 检查若把「不适用」并入「判不了」会发生什么 —— 若某环境下必然存在若干「按设计不存在」的项，则该环境每次运行都必然「判不了 ≥ 1」、退出码恒为「不知道」档。**恒定报警等于没有报警**，规则会在第一天被「忽略失败」绕过。
3. 摘要输出必须四个计数分别打印。把四态压成两态展示，等于在展示层重新丢掉刚区分出来的信息。

## 9.6 准则三：门禁定义的要素清单里必须包含「谁消费退出码」

方案阶段常把一道门禁定义为四项：**执行者、工具与命令、失败判定、留证方式**（见 7.7）。落地后可见这四项不足 —— **它们只规定了门禁自己怎么算，没有规定有没有人读结果。**

「失败判定」回答的是「门禁如何得出不合格」，「消费方」回答的是「得出不合格之后谁会因此停下」。前者齐备而后者缺失，正是 9.4 描述的静默失效。

**判定动作**
1. 门禁清单增加第五列：**消费方**（具体到脚本名与函数名，或 CI 作业的 needs 关系）。
2. 该列为空的门禁，一律视为**未落地**，不计入「已完成」。
3. 评审门禁方案时把这一列作为必填项检查，而非落地后补。

## 9.7 准则四：退出码分档，且带理由放行只对「不知道」生效

两档退出码（0/1）不足以表达「不知道」。**「判不了」既不是合格也不是不合格，把它归入任一档都会造成错误决策**：归 0 是放过未知风险，归 1 是让正常缺件的环境永久无法通行。

放行开关的三条约束：

| 约束 | 内容 | 违反后果 |
|---|---|---|
| 只对「判不了」生效 | 放行开关不得对「不合格」起作用 | 放行退化为无条件跳过门禁 |
| 理由必须随开关附带 | 开关值即理由文本，不能是裸开关 | 放行记录里只有「放行了」，无法事后审计 |
| 理由须进留证 | 放行理由写入门禁日志 | 下次无人知道当时为何放行 |

**判定动作**
1. 实测：在存在「不合格」项的前提下加放行开关，确认退出码仍是失败档。
2. 检查开关是否强制要求附带理由（不带理由应当直接报参数错误，而不是默默按空理由放行）。
3. 检查放行使用记录是否落进与被检产物绑定的留证文件（7.7 衍生）。

## 9.8 准则五：门禁的外壳与判据分离，且外壳只能有一份实现

7.5 已要求判据定义与执行载体解耦。落地时还有一层：**同一套「四态语义 + 退出码分档」在多个门禁层级里只允许有一份实现。**

抽取公共外壳的理由**不是减少重复行数**，而是消除一类无法被检出的不一致：不同层级各写一份时，「判不了是否阻塞」会悄悄产生分歧，而这种分歧**没有任何检出手段** —— 两边都能跑通、都打印正常结论，差别只在退出码，而退出码没人盯着看（回到 9.4）。

两条落地要点：

1. **退出码函数须从「打印摘要」中拆出来单独存在。** 某些调用方需要「先写留证文件 / 先删除已生成的产物，再退出」，若退出码与打印耦合在一个函数里，这类调用方只能自己复算一遍退出码 —— 于是又出现第二份实现。
2. ⚠️ **外壳加载失败必须硬中止。** 门禁脚本往往刻意不开启「出错即退出」（因为要跑完全部项再汇总），此时若外壳加载失败不显式拦截，后续所有结论函数都会变成「命令未找到」，脚本以最后一条命令的退出码收尾，**极可能是 0**。于是「外壳库丢了」在调用方眼里等于「门禁全部通过」，且一次性放过全部检查项。

**判定动作**
1. 搜索代码库里「四态计数」「退出码分档」的实现次数，超过一次即为待收口。
2. 抽取后必须做一致性实测：抽取前后在同一输入下的四个计数与退出码须完全相同。
3. 在加载外壳的下一行显式判断加载结果并中止，附注释说明为何这里必须硬中止。

## 9.9 准则六：变异测试的对象是检测器材自身

8.9 衍生条覆盖的是「验证脚本可能假通过」（验的是空配置）。本条是另一个方向：**用例集全绿，可能是因为用例根本没有检出能力。**

手段是变异测试 —— **故意向被测脚本注入一个已知缺陷，确认用例集立刻转红**。若注入缺陷后用例仍然全绿，说明这批用例在空转，此前的「全部通过」不构成任何证据。

注入点应选在语义最关键处，例如把「判不了」对应的退出码改成合格档 —— 这正是最容易被无意写错、且后果最严重的一处。

⚠️ **附带的坑：注入后忘记还原。** 一旦还原失败而未察觉，损坏的脚本会被提交入库，此后**所有**执行都是假阴性。两条防御：

1. 还原动作不得依赖跨文件系统的临时目录（跨系统的临时路径在版本工具视角下可能根本不可见，导致复制式还原静默失败）。备份应放在仓库内、或直接用编辑器改回。
2. **还原后必须复测一遍全绿，再提交。** 复测是这个坑唯一的检出手段。

**判定动作**
1. 每套门禁用例集至少做一次变异测试，并记录「注入了什么缺陷、有几条用例转红」。
2. 转红条数为零 → 用例集判定为无效，须重写。
3. 提交前跑一次完整用例集，确认与注入前的基线一致。

## 9.10 准则七：建设顺序按「先接通链路、再精修判据」，不能反过来

**一轮门禁建设耗时过长，往往不是某一步做错了，而是顺序把最便宜的那一步放在了最后。**

接通「退出码 → 调用方」这条链路的工作量，通常远小于逐条精修判据。但它决定了精修工作是否产生任何实际效果。顺序反过来时，前几轮的产出在链路接通之前**收益为零**，且这段时间里团队会误以为门禁已经在保护交付。

| 顺序 | 前期收益 | 风险暴露 |
|---|---|---|
| 先精修判据、后接链路 | 零（成果无人消费） | 期间自认为已受保护，实际完全裸奔 |
| 先接链路、后精修判据 | 立即生效（哪怕判据只有一条） | 逐轮递减 |

**这条可迁移到任何「先有规格、后建门禁」的场景**：规格越完备，越容易一头扎进判据细节，而把「谁来读结论」当作收尾工作。

**判定动作**
1. 排期时把「接通消费链路 + 负向实测阻断能力」排在第一位，即使此时只有一条判据。
2. 每轮结束时问一句：**这一轮的产出，在当前状态下已经能改变交付流程的行为了吗？** 答「还不能，要等后面某一步」的，说明顺序排错了。
3. 阶段划分以「链路是否已通」为界，而非以「判据完成了几条」为界。

## 9.11 准则八：找不到可信锚点的交付节点，不要打 tag

回填历史交付节点的 tag 时，常见诱惑是「大致找一个相近的提交打上去」。**这样做的产物是一个看起来可信、实则与真实记录矛盾的锚点，比没有锚点更糟。**

失效形态：节点记录写该节点完成于某日，而 tag 指向的提交是另一个日期。此后有人对照两者，会得到「记录与仓库矛盾，其中一个错了」的结论，并花时间去查 —— 而正确答案是「两个都对，只是 tag 打错了对象」。**它把一个「缺数据」问题伪装成了一个「数据冲突」问题。**

**判定动作**
1. 逐个节点做锚点判定，只接受三类可信锚点：提交信息明确记载该节点的提交；该节点交付物在仓库中的首次出现（且须确认是「交付」而非「从别处纳入基线」）；节点验收动作本身留下的提交。
2. 判定过程须留一张表：节点 / 是否有锚点 / 锚点提交 / 判定依据。**无锚点的行如实留空**，并写明为何无锚点（常见原因：仓库建立时间晚于该节点完成时间）。
3. ⚠️ 检查「交付物首次出现」是否其实是资产纳入 —— 一次性把多份历史交付物纳入基线的提交，**不能作为其中任何一份的交付锚点**。
4. 结果如实呈现：若若干节点确实打不出 tag，就写「本轮只打出 N 个」，不凑数。

## 9.12 准则九：规范文档必须带「强制状态清单」，未强制项如实标未强制

规范文档天然会被读成「已生效的约束」。**当文档只写规则而不写每条的强制状态时，读者会默认全部已强制，而实际可能一条都没有机器拦截。**

因此规范文档须自带一张清单，逐条标注：已强制（有拦截手段并指明位置）/ 未强制（仅约定）。

⚠️ **允许整张表全部标为未强制。** 这种情况下必须显式声明「本表当前没有一条是强制的，这是本轮范围的真实反映，不是笔误」—— 否则读者会以为该列漏填了。

这与门禁侧共享同一个内核：**判不了就不要报通过，没强制就不要写得像已强制。**

**判定动作**
1. 规范文档新增章节时同步在强制状态清单中登记，不允许只加规则不加状态行。
2. 每条「已强制」须指明拦截位置（钩子名 / 脚本名 / CI 作业名）。指不出位置的一律改标未强制。
3. 文档头部如实登记依赖的外部引用是否已入库 —— 引用的依据源若尚未纳入版本库，从干净副本读该文档时链接会失效，此事须写在文档里而非留给读者发现。

## 9.13 反模式

| # | 反模式 | 正确做法 |
|---|---|---|
| 1 | 门禁脚本写完就算落地，没有任何调用方读退出码 | 门禁定义补第五要素「消费方」，指不出消费位置即视为未落地（9.4 / 9.6） |
| 2 | 调用门禁的那一行把结果直接送进日志函数，返回值被吞 | 先取退出码存入变量，再分档处理（9.4） |
| 3 | 调用链上用「忽略失败」惯用语兜底，使非零码归零 | 该写法一律禁止出现在门禁调用链上（9.4） |
| 4 | 只验证「不合格时会打印不合格」，未验证调用方真的中止 | 负向实测以「调用方是否继续往下走」为判据（9.4） |
| 5 | 把「本环境不存在判定对象」也计入「判不了」 | 四态分开，分界线是有没有可判定对象（9.5） |
| 6 | 退出码只有 0/1 两档，「不知道」被归进其中一档 | 分三档，「不知道」独立成档（9.7） |
| 7 | 放行开关是裸开关，且对「不合格」同样生效 | 放行只对「判不了」生效，且必须附带理由并留证（9.7） |
| 8 | 多个门禁层级各写一份四态与退出码实现 | 抽单一外壳库，全部层级 source 同一份（9.8） |
| 9 | 退出码计算与摘要打印耦合在同一函数 | 拆出独立退出码函数，供需要「先留证再退出」的调用方使用（9.8） |
| 10 | 外壳加载失败不拦截，脚本继续执行 | 加载后立即判断并硬中止（9.8） |
| 11 | 用例集全绿即认为器材有效 | 做变异测试，注入已知缺陷确认用例转红（9.9） |
| 12 | 变异测试后未复测就提交 | 还原后必须复测全绿再提交，备份不放跨文件系统临时目录（9.9） |
| 13 | 先花数轮精修判据，最后才接通消费链路 | 第一轮就接通链路并实测阻断，再精修判据（9.10） |
| 14 | 给找不到锚点的历史节点强行打一个相近的 tag | 无可信锚点则不打，并记录无锚点原因（9.11） |
| 15 | 把「多份历史资产一次性纳入基线」的提交当作某份资产的交付锚点 | 区分「交付」与「纳入」，后者不作锚点（9.11） |
| 16 | 规范文档只写规则，不写每条的强制状态 | 自带强制状态清单，未强制项如实标注（9.12） |
| 17 | 强制状态清单整表未强制却不加说明 | 显式声明「当前无一条强制，是范围的真实反映」（9.12） |

## 9.14 落地检查清单

**消费链路**
- [ ] 每道门禁都能指出「哪一行代码读了它的退出码」（9.4 / 9.6）
- [ ] 门禁清单含「消费方」列，且无空行（9.6）
- [ ] 调用链上不存在把非零码归零的写法（9.4）
- [ ] 已做负向实测，确认调用方在不合格时真的中止（9.4）

**四态与退出码**
- [ ] 判定结果分四态，摘要中四个计数分别打印（9.5）
- [ ] 每个「判不了」都已追问过「本环境是否本该有判定对象」（9.5）
- [ ] 退出码分三档，「不知道」独立成档（9.7）
- [ ] 放行开关只对「判不了」生效，已在存在不合格项时实测（9.7）
- [ ] 放行必须附带理由，且理由写入留证（9.7）

**外壳单实现**
- [ ] 四态与退出码逻辑在代码库中只有一份实现（9.8）
- [ ] 退出码函数与摘要打印函数已拆分（9.8）
- [ ] 外壳加载失败会硬中止，并有注释说明原因（9.8）
- [ ] 抽取前后在同一输入下计数与退出码完全一致（9.8）

**器材有效性**
- [ ] 用例集已做变异测试，并记录注入内容与转红条数（9.9）
- [ ] 注入的缺陷已还原，且还原后复测全绿（9.9）
- [ ] 变异备份未放在跨文件系统的临时目录（9.9）

**建设顺序**
- [ ] 排期把「接通链路 + 实测阻断」排在精修判据之前（9.10）
- [ ] 每轮结束时确认本轮产出已能改变交付流程行为（9.10）

**版本拓扑**
- [ ] 每个交付节点都做过锚点判定，并留有判定表（9.11）
- [ ] 无可信锚点的节点未打 tag，且已写明原因（9.11）
- [ ] 已区分「交付」与「资产纳入」，后者未被当作锚点（9.11）
- [ ] 规范文档自带强制状态清单，逐条标注（9.12）
- [ ] 标为「已强制」的条目都指明了拦截位置（9.12）
- [ ] 整表未强制的情况已显式声明（9.12）
- [ ] 文档引用的依据源是否已入库，已如实登记（9.12）

---

# 第十章：零实现阶段的规格守门与例外通道

**问题定义**：契约、码表、安全 / DevSecOps 方案等书面规格已经评审定稿，但被实现对象（后端、客户端、部署物）一个字都还没写。这个阶段常被判定为「没法测」，于是规格一致性靠人肉评审，条款在实现期被静默违反也无人发现；而规格自身的内部矛盾 —— 承诺的错误码没有任何示例承载、码与协议状态不对齐、要求写操作携带的约束头无人引用 —— 在人肉评审中必然漏检，且漏了不报错。

**核心主张**：**不要等待实现。把规格本身解析为可断言的结构，让「规格内部跨条目一致性」与「条款本身」今天就变成 FAIL / PASS 的自动判据；同时用真实协议栈的内存级服务固定未来实现的测试范式。** 第九章的全部准则（退出码消费、四态、变异测试）在本章继续生效，本章补的是它的前置问题：判据对象是规格而非代码时守什么、以及判据都有正当例外，例外通道怎么设计才不会把判据掏空。

**与第九章的关系**：第九章默认「已有成文判据、要落地接链路」，处理的是门禁**与调用方**的关系；本章站在更早的位置，处理门禁**与规格、与例外**的关系。本章产出的测试最终仍要按第九章接通消费方（CI 作业、发版脚本），否则同样收益为零。

## 10.1 适用边界

| 判断项 | 适用 | 不适用 |
|---|---|---|
| 判据对象 | 存在机器可解析的结构化规格（接口契约、码表、数据库 schema、方案条款清单） | 只有非结构化自然语言文档，无任何可解析结构 |
| 实现状态 | 被实现对象尚未开工或尚未完成 | 实现已存在，可直接对运行物做行为测试 |
| 守门内容 | 规格内部跨条目一致性、全局纪律条款的承载与引用 | 纯主观质量（可读性、架构品味） |
| 例外处理 | 已存在或预期会出现正当例外（未展开批次、联调后门、一次性资产） | 判据无例外空间（强判据本就不配通道，见 10.7） |
| CI 选址 | 判据对象能在 CI runner 上真实获得 | 判据对象只存在于目标部署环境（归发版机，见 10.9） |

## 10.2 工具矩阵

| 用途 | 工具 / 手段 | 关键约束 |
|---|---|---|
| 规格解析 | 契约语言对应的解析库（YAML / JSON / Protobuf 等），把规格加载为可遍历结构 | 解析失败与「文件缺失」必须抛出，不得退化为空规格继续（10.5） |
| 静态守门 | 只依赖规格文件与解析库的测试，断言跨条目一致性 | 守的是不同位置之间的承诺对齐，不是「文件能解析」（10.4） |
| 行为范式固定 | 用语言自带协议栈在回环地址起最小内存服务，按「方法 + 路径」登记处理器 | 必须是真实协议栈（真实状态码 / 头 / 编解码），不用拦截器层 mock（10.5） |
| 例外管理 | 显式登记表 + 规格内佐证词 + 双向核对 | 一句话声明式豁免禁止；登记必须可反向断言（10.6 / 10.7） |
| 判据自检 | 弱判据抽纯函数 + 漏报 / 误报双向单测 | 自检随守门测试每次运行，不是一次性人工负向实测（10.8） |
| 消费方 | CI 作业、发版 / 部署脚本 | 按第九章「谁消费退出码」必填；对象不在 CI 的判据不进 CI（10.9） |

## 10.3 流程骨架

```
阶段 A：让规格可判定（不依赖任何实现）
  1. 把规格解析为可遍历结构，文件缺失 / 解析失败直接判 FAIL（不是 SKIP）
  2. 列出规格中所有「一处承诺、另一处承载」的位置对
  3. 对每一对写跨条目一致性断言（集合双向相等、编码对齐、引用遍历、条款承载）

阶段 B：固定行为测试范式
  4. 用真实协议栈在回环地址起最小内存服务，登记各操作的处理器
  5. 用生产同款客户端库对它发真实请求，断言只针对协议形态
  6. 实现就绪后仅替换基址，断言一行不改

阶段 C：设计例外通道（与判据同日上线，不事后补）
  7. 盘点正当例外，区分「未展开占位」与「命中但正当」
  8. 占位用两个信号的合取识别，跳过但列名
  9. 命中但正当的，登记并配规格内佐证词；登记册做双向核对

阶段 D：证明判据有检出力
  10. 弱判据抽纯函数，写漏报向（真实违规形态必命中）与误报向（合法噪声必不命中）单测
  11. 按误报率给判据分强弱两档，例外通道只配给弱判据

阶段 E：选址与接链路
  12. 逐条问「判据对象在 CI runner 上是否真实存在」，不存在的移到对象所在环境
  13. 按第九章接通退出码消费方，负向实测阻断能力
```

## 10.4 准则一：静态守门判「跨条目一致性」，不判语法

规格能被解析只是前提，不值得专门立闸 —— 解析器自己会报错。真正值得自动化的是规格内部**不同位置之间的承诺对齐**：这类不一致人肉必漏、机器可判，且违背时不会在任何其他环节报错。

四类高频对齐：

| 对齐类型 | 断言方式 | 人漏检的原因 |
|---|---|---|
| 集合完整性 | 枚举表宣称的全集与另一处（示例、复用响应）实际承载集合**双向相等** | 多了或少了一两项，通读时无感 |
| 编码对齐 | 宣称「码段前缀即协议状态码」时，对枚举表逐条机械验证 | 逐条核对极其枯燥，必漏 |
| 纪律引用 | 宣称「所有写操作必带某约束」时，遍历全部写操作检查是否真的引用，再断言约束自身的格式定义 | 需要交叉跳转多处 |
| 条款承载 | 要求「某错误必回某响应头」时，断言复用响应真声明了该头、头定义含类型与边界约束 | 承诺与承载相距甚远 |

⚠️ 集合完整性必须用**集合相等**而非子集，两个方向同时封死：多了 = 写了未登记的东西（承诺面之外的实现会复制它），少了 = 承诺的东西没有承载（实现方无样例可循）。失败信息要明确写出「多了什么 / 少了什么」，不能只报「集合不等」。

**判定动作**
1. 通读规格，列出所有「一处枚举 / 承诺、另一处承载 / 引用」的位置对，逐对写断言。
2. 检查每条集合类断言是否双向：删掉承载侧一项、加一项未登记项，两种变异都应让断言转红。
3. 断言信息里逐项列出差异条目，而不是只给布尔结论。

> **举证**：某契约的枚举表登记了二十余个业务码，其复用响应示例只承载了其中一部分。这类问题没有任何编译期信号，评审时两处分隔在不同章节，读者默认「写了就有」。集合相等断言让它第一次具备失败能力；而契约里的错误会沿契约向所有端的实现复制，在零代码阶段切断复制源，成本最低。

## 10.5 准则二：零实现期双轨并行，判据对象缺失是 FAIL 不是 SKIP

等待实现会让静态侧的保护整体推迟到实现期，而那时规格的内部矛盾早已被复制进多处代码。把测试拆成两轨，不要合并等待：

- **静态轨**：解析规格文件本身，只依赖文件与解析库，与实现就绪与否无关，今天就能跑。
- **行为轨**：用语言自带协议栈在回环地址起最小内存服务（真实状态码、真实响应头、真实编解码 —— 不用拦截器层 mock，因为拦截器 mock 测的是 mock 的行为），按「方法 + 路径」登记处理器；客户端用生产同款协议库发真实请求。**断言只针对协议形态，不针对实现**，实现就绪后仅替换基址，断言一行不改。

⚠️ 静态轨加载规格时，**文件缺失或解析失败必须直接判 FAIL（抛异常使整组转红），不得 skip**。第九章的四态里这里取的是「不合格」而非「判不了」：规格是这道门禁的判据对象，对象被删光时的「全绿」意味着门禁在最该响的时候沉默，比没有门禁更糟。环境不具备（如非版本库环境读不到索引）才是 SKIP —— 分界线是第九章那条「有没有可判定的对象」，此处对象按设计必须存在。

**判定动作**
1. 检查静态轨是否在装配阶段（如测试套件的一次性初始化）直接加载规格，加载失败是否使整组转红而非逐项跳过。
2. 行为轨检查三件「真实」：真实监听端口、真实状态码 / 头写出、真实编解码；凡是在客户端调用栈内部打桩的，退回重写。
3. 把内存服务的基址做成唯一替换点，grep 断言测试体中不存在对具体实现路径的引用。

> **举证**：行为轨用文件头注释写死「换成真实基址，断言一行不用改」，是这条轨存在意义的自检 —— 如果替换基址后还必须动断言，说明断言里掺了实现细节，这一轨就没有固定住范式。

## 10.6 准则三：例外只有三种合法形态，且每种都留痕、可反向验证

「判不了不得报通过」与「存在正当例外」并不矛盾，前提是例外只能以下列三种形态出现。第四种形态（一句「此处例外」的声明）不合法 —— 它无法被机器验证，会被随手登记，最终掏空判据。

1. **占位识别（不判，但列名）**：规格中显式标注「后续展开」且当前无实质内容的条目，识别后跳过。判据必须是「**明确标记** 且 **无实质内容**」两个信号的合取：只看名字会误杀同名条目，只看内容为空会漏判未打标的半成品。跳过结果必须打印列名 —— 「未展开而跳过」与「漏检」在静默输出里无法区分。
2. **自证豁免（登记表 + 对象内佐证词）**：豁免是登记表中的一条，且每条携带一个**能在被判对象自身中反向断言存在的佐证词**。例：豁免理由是「无写入副作用」，就断言该条目的规格描述文本真的包含该词。佐证词缺失即 FAIL —— 这把豁免从「声明」升级为「证据」，使有副作用的条目无法靠改名混进豁免。
3. **登记册双向核对**：命中而未登记 → FAIL；登记了但对应内容已不存在 → 同样 FAIL。后者防的是「东西删了、登记册留着凑数」，只单向核对的登记册会随时间腐烂成噪声。定位用「文件名 + 行内稳定子串」而非行号，避免行漂移使登记失效。

所有占位与豁免命中都必须在输出中列名（这是四态中「不适用 / 放行」可见性的要求，与 9.5 同源）。静默 `continue` 等于抹掉了这一态。

**判定动作**
1. 盘点代码中所有让判据「放过」的分支，逐个归类到上述三种形态；归不进去的（裸声明、按文件名整体跳过）判为违规口子。
2. 对每条豁免，回答「机器在哪一行反向断言了佐证词存在」；答不出即退回为声明式豁免。
3. 检查登记表是否有双向两条测试：未登记命中转红、过期登记转红。删掉任一被登记对象，第二条必须转红。
4. 检查占位判定是否为两个信号的合取，并确认占位清单在测试输出中被打印。

> **举证**：声明式豁免最典型的失效是「登记表只是字符串集合，理由写给人看」。一个有副作用的操作改个名即可进表，没有任何测试会失败。把表值从「是否存在」改成「必须在对象自身文本中出现的佐证词」后，混入者必须同时篡改规格描述才能通过 —— 而规格描述正是评审对象，口子被收回到人能看见的地方。

## 10.7 准则四：判据按误报率分强弱，例外通道只配给弱判据

不是所有判据都配拥有豁免通道。按误报率分两档：

| 档位 | 特征 | 处置 |
|---|---|---|
| 强判据 | 固定形态、误报率近零（标准私钥头、云厂商固定前缀的访问密钥标识、明令禁用的精确串） | 命中即 FAIL，代码中**不写**豁免分支 |
| 弱判据 | 形态宽松、需语义上下文辅助（如「定长敏感形态串 + 密钥命名上下文」） | 命中后三分支处置（见下） |

给强判据开豁免口，等于为真实事故留门：强判据的高置信度正是它不需要人工裁量的原因，一旦存在「登记一下就能过」的路径，真实命中会被同一条路径放过。

弱判据命中后严格三分支，顺序不可调换：

1. **可论证的概率特征排除**：如定长全同字符的模板假值 —— 真实随机串出现该形态的概率可计算地忽略。排除依据必须是可论证的概率特征，而非「看起来像假的」。
2. **登记豁免**：命中登记表且佐证有效 → 列名豁免（10.6 第 2 形态）。
3. **FAIL**：其余一切命中。

⚠️ 概率特征与「合法噪声」要分清：标准格式 ID（带连字符的 UUID 等）、注释行、无语义上下文的等长串，这些根本不应命中弱判据，属于判据形态要排除的误报，而不是命中后的豁免 —— 把它们塞进豁免表会让登记表混入大量与「正当例外」无关的条目。

**判定动作**
1. 逐条判据标注强弱；检查强判据的命中分支是否直通失败结果，搜索其下游是否存在任何豁免消费。
2. 弱判据的三个分支是否齐备且顺序为：概率排除 → 登记豁免 → FAIL。
3. 审查每个概率排除特征，要求给出概率论证或规范出处；「常见假值」「测试数据」这类不可论证的理由不成立。

> **举证**：某环境变量模板刻意使用全同字符的定长假值，与真实密钥等长且带密钥命名，一刀切的弱判据必然误报；但「人工看着像假的」不能入判据。全同字符在随机串中的出现概率为字符集倒数的长度减一次方，这一可计算特征才让自动排除站得住 —— 也顺带定义了它何时失效（若真实密钥空间退化到小字符集，该特征须重估）。

## 10.8 准则五：判据自检常驻 —— 从「变异实测一次」升级为「随闸每次运行」

第九章 9.9 要求对门禁脚本做变异测试：注入已知缺陷、确认用例转红、还原复测。在以**形态判据**为主的规格 / 源码扫描场景，这一做法要再进一步：把一次性的人工负向实测**固化为随守门测试每次运行的判据自检**。

做法是把弱判据抽成**不含环境依赖的纯函数**（输入一行 / 一个对象，输出是否命中），配伴随单测同时证明两个方向：

- **漏报向**：应当失败的真实违规形态，纯函数返回命中；
- **误报向**：已知合法噪声 —— 标准格式 ID、注释行、无语义上下文的等长串 —— 纯函数返回不命中。

另有一类「形态命中但正当」的模板假值（见 10.7），它不在纯函数的不命中之列，而由处置侧第二个纯函数按概率特征识别；该识别函数同样需要伴随单测，包括「别的文件里出现同样的串不得冒名豁免」这类跨对象负例。

为什么必须常驻：一次性变异实测只证明「那一刻判据有检出力」，此后任何人放松判据（改正则、加排除）都无人察觉；常驻自检让判据的每次改形都被自己的测试拦截。**正向跑通（扫描器跑了一遍、当前树干净）永远不构成证据** —— 当前树可能恰好没有违规形态；检出力只能由针对判据函数本身的双向测试证明。

**判定动作**
1. 每条弱判据是否能指到一个纯函数；直接内联在扫描循环里的判据先抽函数。
2. 每个纯函数是否同时有漏报向与误报向用例，且误报向逐条覆盖已观测到的噪声类型。
3. 自检与判据是否在同一测试目标内、随 CI 每次运行；只存在于开发者本机脚本里的不算。
4. 最强的证据仍然是真实拦截：首轮运行是否拦到过真实问题。没有真实拦截记录时，自检是底线而非充分条件。

> **举证**：判据首轮运行即真实拦到两类问题 —— 一批脚本在版本库索引中的可执行位错误（含门禁脚本自身），与早期原型资产中多处真实形态的硬编码密钥。这类「首轮命中」比构造的变异用例更能证明判据不是仪式；而硬编码密钥一旦进入版本历史，删除代码不等于撤销泄露，唯一处置是在签发侧作废旧凭据，豁免登记只能留证、不能消除泄露事实，登记条目必须把这一处置写成删除条件。

## 10.9 准则六：CI 选址按「判据对象在 runner 上是否真实存在」反向准入

CI 不是所有门禁的归集地。把判据对象在 CI 运行环境中**不存在**的检查放进 CI，它们会全部落入「不适用 / 判不了」，产出一个内容为空的绿勾。第九章指出失效门禁的默认落点是「通过」、比没有门禁更差 —— 空转绿勾正是它在 CI 页面上的样子，还多了一层「有 CI 保护」的误导。

逐条做反向准入判定：

| 判据对象 | 选址 |
|---|---|
| 规格文件、源码、索引、静态配置、脚本文本 | 进 CI（runner 检出代码即真实存在） |
| 目标机运行态（域名解析、IAM 策略、运行中容器、真实端口监听、部署机文件权限） | 留在对象真实存在的环境（发版机 / 部署机）执行，并由该环境的脚本强制消费退出码 |

CI 作业的判据集刻意「不全」不是遗漏，须在作业注释与方案文档中写明排除边界与消费方去向，防止后来者把空转项「补」进来凑完整。

**判定动作**
1. 对拟进 CI 的每条判据问一句：**runner 上有没有这个对象本身（不是它的描述）？** 答「没有」的移出发版机。
2. 检查被移出的判据在发版脚本中的消费位置是否存在（回到 9.4 / 9.6），移出不等于丢弃。
3. CI 作业头部注释列明「本作业只判代码面，部署面判据在某处消费」，并与方案文档的强制状态清单（9.12）一致。

> **举证**：把「部署脚本必须有可执行位」放进 CI 看似合理，但 runner 上的模式位来自版本库索引 —— 对象真实存在，可判；把「部署机上某目录权限」放进 CI 则只能得到恒定跳过。同一类「权限」判据，因对象位置不同而选址不同，分界线始终是对象在哪。

## 10.10 准则七：豁免登记册是到期债务，每条必须携带删除条件与处置动作

豁免不是终态。绝大多数豁免针对的是「现在改不动、但必须改」的对象（历史资产、待作废凭据、分批实现的占位），登记表如果只写「为何放过」而不写「何时、以何种动作撤销」，它会从临时通道变成永久白名单。

每条登记至少四字段：**定位**（文件 + 行内稳定子串）、**存在理由**、**删除条件**（什么状态达成后该条必须移除）、**处置动作**（达成条件前必须做什么，如凭据须在签发侧作废更换）。10.6 的双向核对保证「东西删了登记必须删」；本条保证「东西还在时，登记也不能永远留着」。

**判定动作**
1. 逐条检查登记表是否四字段齐备；缺删除条件或处置动作的，退回补全后才允许合入。
2. 删除条件应引用可核实的状态（「凭据已在控制台作废并替换为占位符」），不得是「下个版本处理」这类无锚点表述。
3. 对涉及已泄露凭据的登记，确认文档明确写出「进入版本历史即视为泄露，删除代码不构成处置」，唯一处置动作是作废更换。

## 10.11 准则八：技术前提先查官方文档，语言级差异在判据落地当天暴露

判据大量依赖正则、测试框架原语、索引查询这类语言 / 工具级行为，而它们在不同语言中的差异会直接让判据「写了但不工作」：某语言的正则不支持内联大小写标志、测试框架的「跳过」是顶级函数而非对象方法、脚手架直接依赖的测试包与主测试框架产生重复依赖告警。这类问题不神秘，但靠直觉迁移写法必中。

沿用第五章 5.6 的原则：**技术前提先查官方文档核实，不凭另一门语言的记忆写判据。** 判据类代码对正确性的要求比业务代码更高（它是别的代码的裁判），裁判自身的语法假阳性 / 假阴性更隐蔽。

**判定动作**
1. 每条判据第一次写就时，所用正则特性、跳过原语、子进程调用均查一次当前语言版本的官方文档，不按记忆迁移。
2. 判据文件纳入静态分析的零告警标准 —— 分析器对判据代码与产品代码同一把尺，info 级告警也清零。
3. 支撑脚手架不得直接引入与主测试框架并列的第二套断言库；需要的匹配原语从主框架转出，避免依赖漂移。

## 10.12 反模式

| # | 反模式 | 正确做法 |
|---|---|---|
| 1 | 实现没开工，判定「没法测」，规格一致性只靠人肉评审 | 静态轨解析规格判跨条目一致性，今天就接 CI（10.4 / 10.5） |
| 2 | 静态守门只断言「规格文件能解析」 | 守不同位置之间的承诺对齐：集合双向相等、编码对齐、引用遍历、条款承载（10.4） |
| 3 | 集合类断言只写子集方向 | 集合相等双向封死，差异逐项列入失败信息（10.4） |
| 4 | 规格文件缺失时测试逐项 skip，输出全绿 | 装配阶段加载失败直接 FAIL；对象被删光时的全绿是假阴性（10.5） |
| 5 | 行为轨用拦截器层 mock，断言耦合实现细节 | 真实协议栈 + 回环内存服务，只断协议形态，基址是唯一替换点（10.5） |
| 6 | 豁免表是字符串集合，理由写给人看 | 佐证词必须能在被判对象自身文本中反向断言，缺失即 FAIL（10.6） |
| 7 | 占位只按名字或只按内容为空识别 | 「延后标记」与「无实质内容」两个信号合取，跳过且列名（10.6） |
| 8 | 登记册只核对「命中必须已登记」 | 双向核对，过期登记同样 FAIL；用文件 + 行内子串定位（10.6） |
| 9 | 占位 / 豁免命中后静默 continue | 一律打印列名，四态在输出中各自可见（10.6） |
| 10 | 给固定形态的强判据也开豁免分支 | 强判据命中即 FAIL，无豁免消费；例外通道只配弱判据（10.7） |
| 11 | 以「看着像假值」为由排除命中 | 排除依据须是可论证的概率特征，并写明其失效条件（10.7） |
| 12 | 把标准 ID、注释行等合法噪声塞进豁免表 | 它们是判据形态层就该排除的误报，不进登记表（10.7） |
| 13 | 判据只做过一次人工变异实测，或只验证「当前树扫干净」 | 弱判据抽纯函数，漏报 / 误报双向自检随 CI 每次运行（10.8） |
| 14 | 运行态判据进 CI，恒定跳过却给出绿勾 | 反向准入：对象不在 runner 的判据移到对象所在环境并接消费方（10.9） |
| 15 | 豁免登记只写理由，无删除条件 | 四字段齐备，删除条件引用可核实状态，凭据类必须写作废更换（10.10） |
| 16 | 凭据进版本历史后以「已删除代码」作为处置 | 历史即泄露，唯一处置是签发侧作废更换，登记留证且挂账（10.8 / 10.10） |
| 17 | 凭另一门语言的记忆写正则与测试原语 | 当天查当前版本官方文档；判据代码同样静态分析零告警（10.11） |

## 10.13 落地检查清单

**可判定性**
- [ ] 结构化规格存在，并被解析为可遍历结构；解析失败 / 文件缺失直接 FAIL（10.4 / 10.5）
- [ ] 所有「一处承诺、另一处承载」的位置对都有跨条目断言（10.4）
- [ ] 集合类断言双向封死，失败信息逐项列出多了 / 少了什么（10.4）
- [ ] 行为轨为真实协议栈内存服务，断言只针对协议形态、基址唯一替换点（10.5）

**例外通道**
- [ ] 所有「放过」分支归入占位 / 自证豁免 / 双向核对三种形态，无裸声明（10.6）
- [ ] 每条豁免有佐证词反向断言；占位识别为双信号合取（10.6）
- [ ] 登记册双向核对测试齐备，定位用文件 + 行内稳定子串（10.6）
- [ ] 所有占位与豁免命中在输出中列名（10.6）
- [ ] 强判据无豁免分支；弱判据三分支顺序为概率排除 → 登记豁免 → FAIL（10.7）
- [ ] 概率排除特征有可论证依据并注明失效条件；合法噪声在形态层排除而非登记（10.7）
- [ ] 每条登记含定位 / 理由 / 删除条件 / 处置动作四字段（10.10）

**判据有效性**
- [ ] 每条弱判据对应一个纯函数（10.8）
- [ ] 漏报向（真实违规形态）与误报向（标准 ID / 注释 / 无上下文串）用例齐备（10.8）
- [ ] 模板假值识别函数有独立用例，含跨对象不可冒名负例（10.8）
- [ ] 自检与判据在同一测试目标内随 CI 每次运行（10.8）
- [ ] 有首轮真实拦截记录，或明确记录「尚无真实拦截、自检仅为底线」（10.8）

**选址与落地**
- [ ] 拟进 CI 的每条判据都回答过「对象在 runner 上是否真实存在」（10.9）
- [ ] 被移到发版环境的判据已指明其退出码消费位置（10.9 / 9.6）
- [ ] CI 作业注释与方案强制状态清单写明代码面 / 部署面边界（10.9 / 9.12）
- [ ] 判据所用正则 / 跳过原语 / 子进程行为已对当前语言版本查官方文档（10.11）
- [ ] 判据与脚手架静态分析零告警，未引入并列第二套断言库（10.11）
- [ ] 本章产出的守门测试已按第九章接通消费方并做过负向阻断实测（9.4）

---

# 第十一章：编码启动前的规范基线与工具链盘点

**问题定义**：规格定稿、守门就绪之后、第一行业务代码之前，有两件事常被跳过或做浅。其一，编码期纪律（响应包形状、错误码纪律、分层边界、安全红线、必测断言）散落在多份设计文档的几十处章节里，实现期靠记忆遵守、违反无对照 —— 评审时「感觉不对」却翻不到出处。其二，开发工具链（AI 协作工具、外部服务接入、技能包）凭记忆盘点、见热度就装 —— 规则文件里声明依赖的工具可能根本没装，新装的工具可能与既有能力重复，凭证类工具随手给了全权限。

**核心主张**：**编码规范是设计资产的「索引层」——只汇编、不定案，每条可回溯、可判定；工具链盘点是「先核已有、再补缺口、不装也留痕」——规则声明的依赖必须核验在装，选型官方内置优先、多源交叉验证，凭证最小授权。** 这两件事的收益不在开工当天，而在整个编码期：规范让每次评审有对照，盘点让每次工具调用有依据。

**与第十章的关系**：第十章守的是「规格本身的一致性与例外」，产出的是守门判据；本章站在守门就绪之后、业务编码之前，把「人会怎么写代码」与「工具会怎么被调用」两类风险前置收口。本章产出的规范条款中，可机器判定的部分应当回流为第十章式的静态守门，而不是停在纸面。

## 11.1 适用边界

| 判断项 | 适用 | 不适用 |
|---|---|---|
| 前置状态 | 设计资产（架构 / 详设 / 安全 / 数据 / 契约）已定稿，编码未启动 | 设计仍在多轮评审中，口径未冻结 |
| 规范对象 | 纪律散落于多份文档、多名工程师将并行开工 | 单人小项目、设计文档只有一份且极短 |
| 工具链对象 | 团队协作依赖 AI 工具 / 外部服务接入 / 技能包，且存在成文规则声明依赖 | 工具链已由平台团队统一配发并冻结 |
| 产出消费 | 规范将被评审对照、守门测试引用；工具清单将写入环境配置 | 规范写完无人读、工具装完无人用 |

## 11.2 工具矩阵

| 用途 | 工具 / 手段 | 关键约束 |
|---|---|---|
| 纪律提取 | 逐份通读设计资产 + 全文检索（章节名、禁令词、数值常量） | 每条提取必须带依据源章节名，不引行号（11.4） |
| 规范编写 | 单份规范文档，按「总则 / 复用 / 分端 / 数据 / 测试门禁 / 出包」组织 | 每条一句话 + 判定动作；不写论证（论证留在依据源）（11.5） |
| 反冗余验收 | 静态扫描（目录级模式零命中）、调用点计数断言、评审打回点清单 | 机器可判优先，人工评审兜底（11.5 / 11.6） |
| 空白标注 | 规范中单列「通用基线」节，显式声明非项目定案 | 不代设计立规（11.7） |
| 工具检索 | 跨市场聚合检索（官方注册表、社区市场、包仓库下载量） | 3 源以上交叉验证；先查 SDK 是否内置（11.9） |
| 依赖核验 | 规则文件全文检索工具名 × 环境配置逐项核对 | 看配置不看记忆（11.8） |
| 凭证管理 | 细粒度令牌（逐项权限、有效期、单资源范围） | 配置文件不入库；泄露处置唯一（11.10） |

## 11.3 流程骨架

```
阶段 A：汇编编码规范（只汇编，不定案）
  1. 盘点全部设计资产，逐份提取编码期纪律条款，每条标注依据源章节名
  2. 按主题组织成规范，每条压缩为一句话 + 可判定动作，删论证性文字
  3. 把散落在各处的「唯一入口」条款系统化为唯一实现处清单（业务代码只准调用）
  4. 汇编中找不到依据源的主题单列一节，显式标为通用基线（非项目定案）
  5. 通读自检：交叉引用正确、条款归属正确、与既有守门判据不矛盾

阶段 B：盘点工具链（先核已有，再补缺口）
  6. 全文检索规则 / 流程文件中出现过的工具名，逐项核对环境配置里真的装了
  7. 对照技术栈逐域做差距分析：已具备 / 缺 / 不需要
  8. 缺口项先查 SDK / 平台是否内置，再跨市场检索，3 源以上交叉验证
  9. 输出四档清单：必装 / 建议 / 已具备 / 不装；不装档逐条记理由
  10. 凭证类工具附最小授权清单与泄露处置；安装命令只输出，不代执行
```

## 11.4 准则一：规范只汇编不定案，每条可回溯到依据源

规范是设计资产的索引层，不是新的口径来源。它在三个地方会「篡权」，都要封死：新发明一条依据源里没有的纪律；把 A 文档的条款错挂到 B 主题下；用行号引用（文档一改行号即失效，引用静默指错）。

**判定动作**
1. 随机抽 10 条规范，每条能否在 10 秒内定位到依据源的具体章节；定位不到即摘录来源不明，退回。
2. 全文检索规范中的引用格式：必须带章节名，行号引用零命中。
3. 规范开头写明效力规则：与依据源冲突时以依据源为准，先回写规范再编码。

> **举证**：某规范首稿把「缓存键须显式过期、不当唯一存储」错挂到幂等小节，实际归属技术选型文档的缓存章节 —— 汇编错位让读者到错误的源头找上下文，后续的条款修订也会改错文档。另有一处把第三条全局纪律的出处标成总则，实际原文在客户端网络层章节：三条里只有两条真出自总则。

## 11.5 准则二：每条规则带可判定动作，机器可判优先

「代码要整洁」「注意性能」不可判定，写进规范等于没写。每条规则必须回答「违反时谁会发现、怎么发现」：静态扫描零命中、调用点计数、测试断言、命令输出比对，都算；「靠评审记住」不算，但可作为机器判定的兜底。

**判定动作**
1. 逐条问「违反时的发现路径」，把答案写进条款本身（如「静态扫描某目录无某模式」）。
2. 「禁止」类条款必须同时给出检测方式；给不出检测方式的，降级为「评审关注点」并显式标注。
3. 可机器判定的条款清单回流为守门测试候选，与第十章的静态守门同管。

> **举证**：某规范把「全局唯一重试点」落成「业务目录下无循环重试模式」的静态扫描，把「禁止手写响应包装」落成「控制器层手写包装调用零命中」的计数断言，把「唯一解密入口」落成「解密门面调用点计数 == 1」—— 三条都从「评审时记得查」变成「违反即红」。

## 11.6 准则三：组件抽提显式化 —— 唯一实现处清单 + 反冗余验收口径

「不要重复代码」是口号，显式列出「全系统唯一实现处」清单才可执行：每个横切能力（响应包装、异常映射、错误码、限频、幂等、解密、审计、重试、序列化映射、常量）有且只有一个实现组件，业务代码只准调用、不准内联重写。同时要写清一条边界：**跨语言镜像是双端各有唯一实现处，不算冗余** —— 两端同算法靠共用测试向量保证逐位一致；这条边界不说清，镜像会被误当成违反 DRY 打回，或被误合并成单端实现。

**判定动作**
1. 盘点设计文档中所有「唯一入口 / 唯一写点 / 全局唯一」条款，汇成清单表，每项注明职责。
2. 给每个清单项配检测：调用点计数断言、或「业务目录禁入」静态扫描。
3. 评审打回条件成文：PR 中出现与清单组件职责重叠的代码、或复制字面量，直接打回。

> **举证**：某项目汇编出后端 8 个、前端 9 个唯一实现处组件（解密门面、错误码枚举、重试拦截器、缓存键拼装等），并写明网格算法、错误码表属跨语言镜像：双端各一份实现、共用 10 条测试向量，第 9 条负坐标用例专门防取整方向错误 —— 镜像的「逐位一致」由向量对照保证，而不是靠「两边都仔细写」。

## 11.7 准则四：设计空白诚实标注，不代设计立规

汇编时必然发现设计文档没覆盖的主题。两种错法：装作没发现（规范留洞，实现期各写各的）；或自己发明一套伪装成项目口径（规范篡权，后来者分不清哪句是定案、哪句是编者偏好）。正确做法：单列一节「通用基线」，逐条标注「非项目定案」，设计文档补口径后以其为准并删除对应行。

**判定动作**
1. 汇编时维护「找不到依据源」主题清单，逐条决定：进通用基线节，或发起设计补充。
2. 通用基线节的每条都标注出处状态（如「安全文档只定字段格式，未定级别规则」）。
3. 该节禁止收录有争议的项目决策 —— 它是诚实的空白，不是规范的自留地。

> **举证**：某项目发现设计文档对两个主题（注入防护的具体条款、日志级别使用规则）无明文，遂收为通用基线表并注明「可观测方案只定了字段与格式、未定级别」；这两条在评审中按通用惯例执行，但任何一方都无权援引它们冒充项目定案口径。

## 11.8 准则五：工具链先盘点后补缺，规则声明的依赖必须核验在装

团队规则、流程文档里声明依赖的工具，盘点第一项就是核验它真的装了 —— 看配置文件，不看记忆。规则与配置的漂移极常见：规则写于某个装了该工具的环境，换机、换账号、重装之后就静默失效，而规则读者默认「写了就有」。然后才对照技术栈逐域问「已具备 / 缺 / 不需要」，重复能力只留一个。

**判定动作**
1. 全文检索规则与流程文件，列出出现过的全部工具名，逐项核对环境配置（存在且可用，不是只看装过）。
2. 画技术栈 × 工具域差距表：每个技术栈域（语言、框架、数据、缓存、地图、托管、部署）标记已有工具或「缺」。
3. 已有内置能力覆盖的方向（如语义代码检索），不再装同类第三方。

> **举证**：某项目规则要求「用顺序化思考工具拆任务」，盘点时发现该工具根本不在已装清单 —— 规则依赖与工具配置的漂移在开工前核出，补装只需一行配置；若编码期才发现，所有「按规则本该拆过」的任务拆解都要补问责。另有数据库、地图、文档查询三类工具已装且对口，直接列入「已具备」档，避免重复选型。

## 11.9 准则六：选型官方内置优先、多源交叉验证、不装也留痕

缺口项的选型优先级：**SDK / 平台内置 > 官方认证 > 社区高热度**。SDK 内置版与工具链版本天然锁定一致，且零新增依赖；第三方高 star 不代表对口（有的指标是所属仓库级而非单品热度）。同一候选至少在 3 个独立市场源高排名才可采信。**不装的候选必须记录理由**——不记录，下次还会把检索重做一遍，甚至更糟：换个人把否决过的方案装回来。

**判定动作**
1. 每个缺口先问「SDK 自带吗」，再检索市场；检索结果按「官方标识、认证标识、真实下载量、维护活跃度」排序。
2. 跨 3 个以上独立源交叉验证；注意各平台指标口径不同（仓库级 star ≠ 单品热度），不跨平台比大小。
3. 输出四档清单：必装 / 建议 / 已具备 / 不装；不装档逐条写明理由（质量差 / 与既有能力重复 / 低频可用命令行直跑）。
4. 安装命令只输出不代执行，由人决定。

> **举证**：某项目移动端开发工具选型，第三方社区版 star 数百，而官方 SDK 已内置同能力服务 —— 选内置版，天然与 CI 锁定的 SDK 版本一致。另三个候选被记入不装清单：容器管理类（社区质量一般且操作低频，命令行直跑即可）、框架专用类（市场无成熟方案，文档查询工具已覆盖）、契约 lint 类（与自研守门判据重复）。

## 11.10 准则七：凭证类工具最小授权，泄露处置唯一化

需要访问凭证的工具，授权按用途逐项裁剪：逐项列权限清单对照默认授权删掉多余的、设有效期（不设永久）、资源范围缩到单仓库 / 单项目。凭证写入的配置文件不得入版本库。泄露处置唯一：**签发侧作废换发**——「把凭证从代码里删掉」不是处置，进过历史即视为泄露。

**判定动作**
1. 安装前写出该工具所需权限清单，逐项对照裁剪；给不出用途的权限不授。
2. 令牌设有效期并登记到期换新责任人；配置文件加入不入库清单并验证忽略规则生效。
3. 泄露处置与既有门禁纪律对齐成文：历史即泄露，唯一动作是作废换发，登记留证。

> **举证**：某项目为代码托管平台工具配置细粒度令牌：四项权限（内容读 / 合并请求读写 / 工单读写 / 流水线读）、90 天有效期、单仓库范围；配置纪律直接援引项目既有门禁条款「凭据进历史即泄露，唯一处置是作废换密钥」—— 工具接入没有新造一套安全口径，而是复用已有判据。

## 11.11 反模式

| # | 反模式 | 正确做法 |
|---|---|---|
| 1 | 规范里夹带新设计口径，规范篡权当设计 | 只汇编不定案；冲突以依据源为准并先回写规范（11.4） |
| 2 | 引用依据源用行号 | 一律章节名；行号在文档改动后静默指错（11.4） |
| 3 | 条款写「要 / 应该 / 注意」，无发现路径 | 每条带判定动作；给不出机器判定的显式标为评审关注点（11.5） |
| 4 | 「不重复代码」停在口号 | 唯一实现处清单成表 + 调用点计数 / 静态扫描验收（11.6） |
| 5 | 把跨语言镜像当冗余合并，或当违反 DRY 打回 | 镜像双端各有唯一实现处，共用测试向量保证逐位一致（11.6） |
| 6 | 发现设计空白就自行发明口径写进规范 | 单列通用基线节，逐条标注非项目定案（11.7） |
| 7 | 凭记忆盘点工具链 | 看配置文件逐项核对；规则与配置的漂移开工前核出（11.8） |
| 8 | 规则声明依赖的工具从未核验是否安装 | 规则文件全文检索工具名 × 配置核对，是盘点第一项（11.8） |
| 9 | 见 star 高就装，不查官方是否内置 | SDK 内置 > 官方认证 > 社区高热度；仓库级 star 不等于单品热度（11.9） |
| 10 | 单平台检索结果直接采信 | 3 源以上交叉验证，注意各平台指标口径不可比（11.9） |
| 11 | 装了不登记、不装不记理由 | 四档清单留痕；不装理由防重复检索与错误回装（11.9） |
| 12 | 凭证给全权限、永久有效、配置入库 | 逐项最小授权 + 有效期 + 不入库 + 泄露唯一处置作废换发（11.10） |
| 13 | AI 拿到安装命令直接执行 | 命令只输出，装不装、何时装由人决定（11.9） |

## 11.12 落地检查清单

**规范汇编**
- [ ] 每条规则标注依据源章节名，行号引用零命中（11.4）
- [ ] 抽样 10 条可 10 秒定位依据源章节；无来源不明条款（11.4）
- [ ] 「禁止」类条款均配检测方式（扫描 / 计数 / 测试名）（11.5）
- [ ] 唯一实现处清单成表，每项配调用点计数或目录禁入扫描（11.6）
- [ ] 跨语言镜像边界写明，镜像算法有共用测试向量（11.6）
- [ ] 设计空白单列通用基线节，逐条标注非项目定案与出处状态（11.7）
- [ ] 规范通读自检：交叉引用、条款归属、与既有守门判据一致性（11.3 阶段 A）

**工具链盘点**
- [ ] 规则 / 流程文件中的工具名已全文检索，逐项核验在装且可用（11.8）
- [ ] 技术栈 × 工具域差距表完成，每域标已具备 / 缺 / 不需要（11.8）
- [ ] 每个缺口先查 SDK / 平台内置，再市场检索（11.9）
- [ ] 选型经 3 源以上交叉验证，指标口径差异已排除（11.9）
- [ ] 四档清单（必装 / 建议 / 已具备 / 不装）成文，不装档逐条记理由（11.9）
- [ ] 安装命令只输出未代执行，人工确认后安装（11.9）
- [ ] 凭证类工具：权限清单逐项裁剪、设有效期、配置不入库、泄露处置唯一成文（11.10）
- [ ] 规范中可机器判定的条款已登记为守门测试候选，回流第十章静态守门（11.5 / 10.4）

---

*新章节从此处向下追加，格式见文首「追加新章节的格式要求」。*

